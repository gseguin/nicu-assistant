---
phase: 39-release-v1.12.0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .planning/PROJECT.md
  - static/favicon.svg
  - static/favicon.png
  - static/apple-touch-icon.png
  - static/pwa-192x192.png
  - static/pwa-512x512.png
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04

must_haves:
  truths:
    - "package.json shows version 1.12.0"
    - "AboutSheet displays v1.12.0 automatically via __APP_VERSION__"
    - "PROJECT.md Validated list includes all v1.12 entries from Phases 36-39"
    - "App favicon is a distinct clinical icon, not a blank/white square, at all required sizes"
    - "All gates pass: svelte-check 0/0, vitest all green, Playwright all green, pnpm build succeeds"
  artifacts:
    - path: "package.json"
      provides: "Version 1.12.0"
      contains: '"version": "1.12.0"'
    - path: ".planning/PROJECT.md"
      provides: "Validated list with v1.12 entries"
      contains: "v1.12"
    - path: "static/favicon.png"
      provides: "32x32 browser tab favicon"
    - path: "static/apple-touch-icon.png"
      provides: "180x180 iOS home screen icon"
    - path: "static/pwa-192x192.png"
      provides: "192x192 PWA manifest icon"
    - path: "static/pwa-512x512.png"
      provides: "512x512 PWA manifest icon"
  key_links:
    - from: "package.json"
      to: "AboutSheet"
      via: "__APP_VERSION__ Vite define"
      pattern: '"version":\\s*"1\\.12\\.0"'
---

<objective>
Release v1.12.0: version bump, PROJECT.md documentation update, favicon generation at all standard sizes, and final gate verification.

Purpose: Ship the Feed Advance Calculator milestone with proper documentation, a real app icon, and all quality gates green.
Output: Tagged v1.12.0 release with favicon, documentation, and passing gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-release-v1.12.0/39-CONTEXT.md
@package.json
@src/app.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump + PROJECT.md update</name>
  <files>package.json, .planning/PROJECT.md</files>
  <read_first>
    - package.json (current version field)
    - .planning/PROJECT.md (full file — Validated list, Current State, Context sections)
    - .planning/REQUIREMENTS.md (all v1.12 requirement IDs and descriptions for Validated list entries)
  </read_first>
  <action>
**1. Version bump (per D-01, REL-01):**
Change `"version": "1.11.0"` to `"version": "1.12.0"` in `package.json`. No other fields change. The AboutSheet picks this up automatically via the `__APP_VERSION__` Vite define constant.

**2. PROJECT.md Validated list update (per D-02, REL-02):**
Add v1.12 entries to the `### Validated` section, grouped by phase. Follow the exact style of existing entries (bullet with `- ✓ Description — vX.Y`). Add these entries:

Phase 36 (Wave 0 — Scaffolding + Identity Hue):
- ✓ Feed Advance Calculator: `CalculatorId` union extended with `'feeds'`, registry entry with `identityClass: 'identity-feeds'` and Baby icon, NavShell ternary extended for `/feeds`, AboutSheet `feeds` entry citing `nutrition-calculator.xlsx` Sheet1 + Sheet2, new `src/lib/feeds/` module, `/feeds` route, zero new dependencies (ARCH-01..07) — v1.12
- ✓ Feed Advance Calculator identity hue: `.identity-feeds` OKLCH token pair (hue ~30 terracotta), hand-computed for 4.5:1 contrast on all 4 identity surfaces in both themes, pre-PR axe-core sweep passed (HUE-01..03) — v1.12

Phase 37 (Pure Logic + Config + Parity Tests):
- ✓ Feed Advance calculations: bedside advancement (Sheet2 parity within ~1% epsilon, weight 1.94 fixture) and full nutrition (Sheet1 parity within ~1% epsilon, weight 1.74 fixture); named constants (3.4 kcal/g dextrose, 2 kcal/ml lipid, 30 ml/oz); `feeds-config.json` with typed wrapper; parameter-matrix tests covering every frequency x cadence combination (CORE-09, FREQ-04, FULL-04..07, SAFE-06, TEST-01..04) — v1.12

Phase 38 (UI + State + Component Tests + Route + E2E + A11y):
- ✓ Feed Advance Calculator UI: `FeedAdvanceCalculator.svelte` with SegmentedToggle (Bedside Advancement / Full Nutrition modes), shared weight input, bedside mode with three per-feed outputs (trophic/advance/goal) + IV backfill, full nutrition mode with dual TPN dextrose lines + total kcal/kg/d hero, 9 advisory banners, sessionStorage persistence; trophic frequency (q2h/q3h/q4h/q6h) and advance cadence dropdowns; component tests + Playwright E2E + axe-core sweeps (CORE-01..08, FREQ-01..03/05, IV-01..03, FULL-01..03, SAFE-01..05, TEST-05..07) — v1.12

Phase 39 (Release):
- ✓ `package.json` version bumped to 1.12.0; AboutSheet reflects v1.12.0 via the `__APP_VERSION__` build-time constant; PROJECT.md Validated list updated with v1.12 entries; app favicon generated at all standard sizes (REL-01..04) — v1.12

**3. PROJECT.md Current State update (per D-03):**
Replace the `## Current State` paragraph. The new text should reflect v1.12 as shipped:
- Mention Feed Advance Calculator as the fourth clinical calculator
- Note the two modes (Bedside Advancement + Full Nutrition) matching `nutrition-calculator.xlsx`
- Update test counts to current values (will be confirmed in Task 3)
- State version 1.12.0

Change the `**In progress:**` line to `**Shipped:**` for v1.12 and remove the old "In progress" block. Update the `**Shipped:**` line to reference v1.12.

**4. PROJECT.md Context section:**
Update the `## Context` section opening paragraph to say "Shipped v1.12" and mention the Feed Advance Calculator as the fourth calculator. Add a bullet to `**Current calculators:**`:
- Feed Advance: bedside advancement (Sheet2) + full nutrition (Sheet1) modes, trophic frequency + advance cadence dropdowns, IV backfill, dual TPN dextrose lines, total kcal/kg/d hero, 9 advisory banners

Update the `*Last updated:*` footer date line to reflect v1.12 Phase 39 complete.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && node -e "const p = require('./package.json'); if(p.version !== '1.12.0') { console.error('Version is', p.version); process.exit(1); } console.log('OK: version is 1.12.0')" && grep -c "v1.12" .planning/PROJECT.md | xargs -I{} test {} -ge 4</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"version": "1.12.0"`
    - PROJECT.md Validated section contains at least 4 new v1.12 bullet entries (one per phase group)
    - PROJECT.md Current State references v1.12.0 as shipped
    - PROJECT.md Context section mentions Feed Advance as fourth calculator
    - No other package.json fields changed
  </acceptance_criteria>
  <done>package.json is at 1.12.0, PROJECT.md Validated list has all v1.12 entries, Current State and Context updated</done>
</task>

<task type="auto">
  <name>Task 2: Generate app favicon at all standard sizes</name>
  <files>static/favicon.svg, static/favicon.png, static/apple-touch-icon.png, static/pwa-192x192.png, static/pwa-512x512.png</files>
  <read_first>
    - src/app.html (favicon link tag format)
    - src/app.css (OKLCH color tokens — find the clinical blue value for the icon color)
    - vite.config.ts (PWA manifest icons configuration to confirm required sizes and maskable flag)
  </read_first>
  <action>
**Favicon design (per D-04, D-05, D-06, REL-03):**

Create an SVG source file at `static/favicon.svg` with a simple, high-contrast clinical icon. Design approach:

1. **Design:** A stylized medical cross or stethoscope silhouette on a rounded-square background. The background uses clinical blue from the app's OKLCH tokens (approximately `oklch(50% 0.13 220)` which is roughly `#2563eb` / `rgb(37, 99, 235)` in sRGB). The icon shape is white for maximum contrast. Keep the design extremely simple — it must read clearly at 16x16.

2. **Alternative design (preferred if simpler):** A rounded-square background in clinical blue with the letters "N+" or a simple plus-sign (medical cross) in white. The plus sign is a universal medical symbol and renders crisply at all sizes.

3. **SVG specifications:**
   - viewBox="0 0 512 512"
   - Rounded rectangle background with rx="80" (proportional rounding)
   - Clinical blue fill: `#2563eb` (sRGB equivalent of the app's OKLCH clinical blue)
   - White icon/symbol centered, with adequate padding (at least 15% margin on each side)
   - No gradients, no fine details — pure flat shapes only

4. **Export to PNG at all required sizes using ImageMagick:**
   ```
   convert -background none static/favicon.svg -resize 32x32 static/favicon.png
   convert -background none static/favicon.svg -resize 180x180 static/apple-touch-icon.png
   convert -background none static/favicon.svg -resize 192x192 static/pwa-192x192.png
   convert -background none static/favicon.svg -resize 512x512 static/pwa-512x512.png
   ```

5. **Verify each PNG:**
   - `file static/favicon.png` should show `32 x 32`
   - `file static/apple-touch-icon.png` should show `180 x 180`
   - `file static/pwa-192x192.png` should show `192 x 192`
   - `file static/pwa-512x512.png` should show `512 x 512`

6. **No changes to `src/app.html`** — the existing `<link rel="icon" href="%sveltekit.assets%/favicon.png" />` already points to the right file.

7. **No changes to `vite.config.ts`** — the PWA manifest already references the correct icon filenames.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && file static/favicon.png | grep "32 x 32" && file static/apple-touch-icon.png | grep "180 x 180" && file static/pwa-192x192.png | grep "192 x 192" && file static/pwa-512x512.png | grep "512 x 512" && echo "All favicon sizes correct"</automated>
  </verify>
  <acceptance_criteria>
    - static/favicon.svg exists as the source SVG with clinical blue background and white icon
    - static/favicon.png is a 32x32 PNG (not a 1x1 placeholder)
    - static/apple-touch-icon.png is 180x180
    - static/pwa-192x192.png is 192x192
    - static/pwa-512x512.png is 512x512
    - All PNGs are recognizable clinical icons, not blank/white squares
    - src/app.html unchanged (existing link tag already correct)
  </acceptance_criteria>
  <done>All four favicon PNGs replaced with a distinct clinical icon at correct sizes, SVG source committed</done>
</task>

<task type="auto">
  <name>Task 3: Final gates verification</name>
  <files></files>
  <read_first>
    - package.json (confirm version is 1.12.0 from Task 1)
  </read_first>
  <action>
**Run all quality gates (per D-07, D-08, REL-04):**

Run each gate sequentially. If any gate fails, fix the issue before proceeding to the next.

1. **svelte-check:**
   ```
   pnpm exec svelte-check
   ```
   Expected: 0 errors, 0 warnings.

2. **vitest:**
   ```
   pnpm test --run
   ```
   Expected: All tests green (currently 228 from Phase 38).

3. **Playwright (E2E + axe-core):**
   ```
   pnpm exec playwright test
   ```
   Expected: All tests green. Confirm axe-core sweep count reaches 20/20 (the target from Phase 38 success criteria). If any tests fail, diagnose and fix before marking complete.

4. **Build:**
   ```
   pnpm build
   ```
   Expected: Succeeds without errors. This also validates the new favicon files are correctly referenced in the PWA manifest.

**If any Playwright tests fail (per D-08):** Fix the failing tests. Common causes after a version bump: snapshot tests that include version strings, or PWA manifest tests that check icon checksums. Fix the root cause, do not skip tests.

**Record final counts** for the SUMMARY: total vitest tests, total Playwright tests, axe sweep count.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm exec svelte-check 2>&1 | tail -3 && pnpm test --run 2>&1 | tail -5 && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec svelte-check` exits 0 with 0 errors, 0 warnings
    - `pnpm test --run` exits 0 with all tests passing (at least 228)
    - `pnpm exec playwright test` exits 0 with all tests passing
    - `pnpm build` exits 0 with no errors
    - No skipped or pending tests that were previously passing
  </acceptance_criteria>
  <done>All four gates pass: svelte-check clean, vitest all green, Playwright all green (including 20/20 axe sweeps), build succeeds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static assets | Favicon PNGs are inert image files served as static assets — no executable content |
| Version string | Build-time constant from package.json — no runtime user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | static/favicon.png | accept | Favicons are inert PNG images with no executable payload; served via static hosting with integrity checks from PWA service worker precaching |
| T-39-02 | Information Disclosure | package.json version | accept | Version number is intentionally public (shown in AboutSheet); no sensitive information exposed |
</threat_model>

<verification>
1. `package.json` version is exactly `1.12.0`
2. PROJECT.md Validated section has v1.12 entries for Phases 36, 37, 38, 39
3. All favicon PNGs are correct dimensions and visually distinct (not blank)
4. svelte-check: 0 errors, 0 warnings
5. vitest: all green
6. Playwright: all green, axe 20/20
7. pnpm build: succeeds
</verification>

<success_criteria>
v1.12.0 is ready to ship: version bumped, PROJECT.md documented, favicon replaced at all sizes, and all quality gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/39-release-v1.12.0/39-01-SUMMARY.md`
</output>
