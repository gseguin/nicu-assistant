---
phase: 43-release-v1-13-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/HANDOFF.json
  - .planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/.continue-here.md
  - .planning/phases/40-favorites-store-hamburger-menu/40-VERIFICATION.md
  - .planning/phases/41-favorites-driven-navigation/41-VERIFICATION.md
  - e2e/uac-uvc.spec.ts
  - e2e/morphine-wean-a11y.spec.ts
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
tags: [release, version-bump, documentation, gates, orphan-cleanup, traceability-flip, verification-triage, pre-bump-e2e-fix]

must_haves:
  truths:
    - "User opening AboutSheet sees version 1.13.0 (sourced via __APP_VERSION__)"
    - "PROJECT.md Validated list contains all v1.13 entries with milestone tags (Phase 40, 41, 42 per-ID + Phase 42.1 + Phase 42.1-followup+42.2 narrative + Phase 43 release bullet)"
    - "PROJECT.md Current State paragraph mentions v1.13.0 shipped, 5 calculators (Morphine / Formula / GIR / Feeds / UAC-UVC), favorites-driven nav with hamburger, design polish sweep, STOP-red carve-out"
    - "PROJECT.md Current Milestone header reads 'v1.13 UAC/UVC Calculator + Favorites Nav (Shipped)' with a Shipped paragraph"
    - "All 41 v1.13 REQUIREMENTS.md traceability rows flipped from Pending to ✓ Validated"
    - "ROADMAP.md Progress row 'Phase 43. Release v1.13.0' reads '1/1 | Complete | 2026-04-24'"
    - "Orphan artifacts (.planning/HANDOFF.json + .planning/phases/42.1-…/.continue-here.md) deleted"
    - "40-VERIFICATION.md and 41-VERIFICATION.md each contain a Phase 43 Triage block with per-item status (verified-via-grep / manual-QA-needed / fix-required)"
    - "pnpm check exits 0 with 0 errors / 0 warnings"
    - "pnpm test:run exits 0 with all tests green"
    - "pnpm build exits 0"
    - "pnpm exec playwright test exits 0 (all 16 specs green, including the 2 previously failing tests fixed)"
    - "axe sweeps green in both light and dark themes (extended UAC/UVC + hamburger variants)"
  artifacts:
    - path: "package.json"
      provides: "Version field 1.13.0"
      contains: '"version": "1.13.0"'
    - path: ".planning/PROJECT.md"
      provides: "v1.13 Validated bullets + Current State + Current Milestone (Shipped) + footer timestamp"
      contains: "v1.13.0 UAC/UVC"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table all 41 v1.13 IDs flipped to ✓ Validated; footer timestamp updated"
      contains: "✓ Validated"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 43 Progress row marked Complete with 1/1 plans + 2026-04-24 date"
      contains: "| 43. Release v1.13.0 | v1.13 | 1/1 | Complete | 2026-04-24 |"
    - path: ".planning/phases/40-favorites-store-hamburger-menu/40-VERIFICATION.md"
      provides: "Phase 43 Triage block appended with per-item status for the 4 human_verification items"
      contains: "## Phase 43 Triage"
    - path: ".planning/phases/41-favorites-driven-navigation/41-VERIFICATION.md"
      provides: "Phase 43 Triage block appended with per-item status for the 4 human_verification items"
      contains: "## Phase 43 Triage"
    - path: "e2e/uac-uvc.spec.ts"
      provides: "Mobile-viewport beforeEach passes (drawer open succeeds at 375x667)"
    - path: "e2e/morphine-wean-a11y.spec.ts"
      provides: "Light-mode advisory axe sweep passes (zero violations)"
  key_links:
    - from: "package.json:4"
      to: "vite.config.ts:11 (__APP_VERSION__)"
      via: "build-time JSON.parse(readFileSync('package.json'))"
      pattern: "__APP_VERSION__: JSON.stringify(pkg.version)"
    - from: ".planning/PROJECT.md (Validated section)"
      to: "v1.13 phase artifacts (Phases 40, 41, 42, 42.1 + post-42.1 follow-up + 42.2 sweep)"
      via: "Per-ID bullets (D-02) for 40/41/42 + narrative bullets (D-03/D-05) for 42.1 + follow-up"
      pattern: "— v1.13"
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: "v1.13 milestone closure"
      via: "41 status flips Pending → ✓ Validated"
      pattern: "✓ Validated"
    - from: "AboutSheet (rendered)"
      to: "package.json version field"
      via: "src/lib/.../about-content.ts:12 → __APP_VERSION__ → pkg.version"
      pattern: "appVersion = __APP_VERSION__"
---

<objective>
Ship v1.13.0. Bump `package.json` from `1.12.5` to `1.13.0`, propagate the new version through the existing `__APP_VERSION__` build-time chain so AboutSheet picks it up automatically, document the milestone in PROJECT.md (per-ID bullets for Phases 40/41/42 + narrative bullets for Phase 42.1 / 42.1-follow-up / 42.2), flip all 41 v1.13 REQUIREMENTS.md traceability rows from Pending to ✓ Validated, mark the Phase 43 ROADMAP row Complete, triage the 8 `human_needed` verification-debt items in 40/41-VERIFICATION.md, delete two orphan planning artifacts, fix two pre-existing Playwright failures (no "known issue" deferrals per D-15), and drive the full clinical quality gate green (`pnpm check` 0/0, `pnpm test:run` green, `pnpm build` ✓, `pnpm exec playwright test` green, extended axe suite green in both themes).

Purpose: v1.13 is the largest milestone the project has shipped (4 phases + a critique-sweep follow-up + a clinical-safety carve-out + 14+ post-phase polish commits). This phase is the receipt: a single-source-of-truth statement in PROJECT.md, a fully-flipped traceability table, no stale planning artifacts, and a clean clinical quality gate. Pattern is identical to Phase 39 (v1.12 release) with three differences: (a) the REQUIREMENTS.md table didn't exist in v1.12 so its 41-row flip is new for v1.13, (b) Phase 42.1's design polish is ID-less work that needs narrative bullets per D-03/D-05, (c) two Playwright tests pre-fail at HEAD and must be fixed before the bump per D-15.

Output: `package.json` at 1.13.0, PROJECT.md / REQUIREMENTS.md / ROADMAP.md fully reflecting v1.13 shipped state, two orphan files deleted, two VERIFICATION.md files with appended triage blocks, two e2e specs fixed, all gates green. Phase 43 SUMMARY captured by the orchestrator after this plan completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-release-v1-13-0/43-CONTEXT.md
@.planning/phases/43-release-v1-13-0/43-RESEARCH.md
@.planning/phases/43-release-v1-13-0/43-PATTERNS.md
@.planning/phases/40-favorites-store-hamburger-menu/40-VERIFICATION.md
@.planning/phases/41-favorites-driven-navigation/41-VERIFICATION.md
@package.json
@vite.config.ts

<interfaces>
<!-- Build-time version chain (verified in Phase 43 RESEARCH.md). Executor uses these directly — no codebase exploration needed. -->

From package.json (line 4):
```json
"version": "1.12.5"
```

From vite.config.ts (lines 1-15):
```typescript
import { readFileSync } from 'fs';

const pkg = JSON.parse(readFileSync('package.json', 'utf-8'));

export default defineConfig({
  define: {
    __APP_VERSION__: JSON.stringify(pkg.version)
  },
  ...
});
```

From src/lib/.../about-content.ts (line 12):
```typescript
const appVersion = __APP_VERSION__;
```

**Implication:** Editing `package.json` line 4 from `"1.12.5"` → `"1.13.0"` is the ONLY code change needed for REL-01. AboutSheet, About entries, and PWA manifest version all auto-pick-up via the chain above. Do NOT edit `vite.config.ts`, `about-content.ts`, or `AboutSheet.svelte`.
</interfaces>

<phase_39_template>
<!-- Verbatim Phase 39 release diff (from `git show ec50793 -- .planning/PROJECT.md`). Phase 43 clones this shape with v1.13 substitutions. -->

Phase 39 PROJECT.md Current State diff (lines 9):
```diff
-**Shipped:** v1.11 Morphine Mode Removal — Single Source of Truth (2026-04-09) — …
-**In progress:** v1.12 Feed Advance Calculator — Phase 38 …
+**Shipped:** v1.12.0 Feed Advance Calculator (2026-04-10) — Fourth clinical calculator added: Feed Advance Calculator with two modes … PWA at version 1.12.0; svelte-check 0/0, vitest 228+, Playwright all green, 20/20 axe sweeps.
```

Phase 39 PROJECT.md Current Milestone diff (line 109+):
```diff
-## Current Milestone: v1.12 Feed Advance Calculator
-**Goal:** …
-**Target features:** …
-**Key context:** …
+## Current Milestone: v1.12 Feed Advance Calculator (Shipped)
+**Shipped.** Fourth clinical calculator delivered: …. Released as v1.12.0.
```

Phase 39 PROJECT.md footer diff:
```diff
-*Last updated: 2026-04-10 — v1.12 Phase 38 complete (UI + state + tests + E2E + a11y)*
+*Last updated: 2026-04-10 — v1.12.0 Phase 39 complete (release — version bump, PROJECT.md, favicon, gates)*
```
</phase_39_template>

<feeds_spec_working_pattern>
<!-- Working sibling viewport-loop pattern (e2e/feeds.spec.ts:17-37) — uac-uvc.spec.ts must structurally match this. -->

```typescript
for (const viewport of [
  { name: 'mobile' as const, width: 375, height: 667 },
  { name: 'desktop' as const, width: 1280, height: 800 }
]) {
  test.describe(`Feeds happy path (${viewport.name})`, () => {
    test.use({ viewport: { width: viewport.width, height: viewport.height } });

    test.beforeEach(async ({ page }) => {
      await page.goto('/feeds');
      await page
        .getByRole('button', { name: /understand/i })
        .click({ timeout: 2000 })
        .catch(() => {});
      // Plan 42.1-05 (D-08): on mobile (<md), inputs live behind the InputDrawer
      // bottom-sheet. Open the drawer once so the dialog is visible …
      if (viewport.name === 'mobile') {
        await page.getByLabel('Open inputs drawer').click();
      }
    });
    // ... rest of suite
```

`feeds.spec.ts` and `gir.spec.ts` both pass at HEAD with this exact shape. `uac-uvc.spec.ts` has the same structural shape but its mobile iteration's `beforeEach` times out — the viewport is not applying or the drawer trigger has not hydrated. See Task 5 action for diagnosis steps.
</feeds_spec_working_pattern>

<feeds_a11y_working_pattern>
<!-- Working sibling advisory-a11y pattern (e2e/feeds-a11y.spec.ts) — morphine-wean-a11y.spec.ts:87 must produce zero violations like this. -->

```typescript
test('feeds bedside light mode - no axe violations', async ({ page }) => {
  await page.evaluate(() => {
    document.documentElement.classList.remove('dark');
    document.documentElement.classList.add('light');
    document.documentElement.setAttribute('data-theme', 'light');
  });
  await page.getByLabel('Weight').fill('1.94');
  await expect(page.getByText('Trophic').first()).toBeVisible();
  const results = await new AxeBuilder({ page }).withTags(['wcag2a', 'wcag2aa']).analyze();
  expect(results.violations).toEqual([]);
});
```

Dark-mode sibling at `morphine-wean-a11y.spec.ts:109-131` passes; only the light-mode advisory test fails. RESEARCH A5 suspects post-42.1 Amber rescope (commit `b548ce4`) or RangedNumericInput unification (commit `0558253`) introduced a token regression on light-mode advisory background or text.
</feeds_a11y_working_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verification-debt triage (40/41-VERIFICATION.md)</name>
  <files>
    .planning/phases/40-favorites-store-hamburger-menu/40-VERIFICATION.md
    .planning/phases/41-favorites-driven-navigation/41-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/40-favorites-store-hamburger-menu/40-VERIFICATION.md (full file — 4 human_verification items at lines 13-25)
    - .planning/phases/41-favorites-driven-navigation/41-VERIFICATION.md (full file — 4 human_verification items at lines 7-19)
    - .planning/phases/43-release-v1-13-0/43-CONTEXT.md (decisions D-07 / D-08)
    - .planning/phases/43-release-v1-13-0/43-RESEARCH.md (Pitfall 3 explains that 41-VERIFICATION.md item #4 — "2 pre-existing navigation.spec.ts failures" — is now obsolete because commit `c2800df` updated the spec to use the hamburger drawer About row)
    - .planning/phases/43-release-v1-13-0/43-PATTERNS.md (section 8 — inline triage block format)
  </read_first>
  <action>
    Per D-07 / D-08 / D-11: append a Phase 43 Triage block to BOTH `40-VERIFICATION.md` and `41-VERIFICATION.md`, immediately before the trailing `*Verified:* …` footer line. Use the exact format below. RESEARCH.md flagged 8 items total (4 in each file) — CONTEXT.md says "7 human_needed items"; treat the count as 8 (RESEARCH directly verified by reading both files).

    For each item, assign one of three statuses per the D-07 rubric:
    - `verified-via-grep` — trivially confirmable from current code/test state. Cite the grep command, test path + line, or commit hash that proves it.
    - `manual-QA-needed` — requires real-device testing (iOS standalone PWA, safe-area inset, visual color rendering). Add to v1.14 backlog + mention in release-notes reminder section of the SUMMARY (D-13).
    - `fix-required` — genuine gap blocking release. Stop and fix before bump (Task 5 deals with the two known Playwright failures separately; only flag a NEW gap here).

    Suggested triage outcomes (executor confirms each via the cited evidence before writing the row):

    `40-VERIFICATION.md` (4 items):
    1. Hamburger keyboard tab/Esc/focus-return → `manual-QA-needed` (jsdom cannot fire native `<dialog>` Esc keydown; T-03/T-04/T-13 cover the code path; live keyboard sweep is a 30-second human check that ships in v1.13.1 release notes if not done pre-deploy).
    2. Fresh-install localStorage seed `{"v":1,"ids":["morphine-wean","formula","gir","feeds"]}` → `verified-via-grep` (`favorites.test.ts` T-01 + T-18 + T-19 cover seed-on-null and private-browsing throws; `+layout.svelte:42` calls `favorites.init()` confirmed in 40-VERIFICATION.md Required Artifacts table).
    3. Un-star Morphine, bottom nav still shows 4 tabs (D-OUT-01 visual confirm) → `verified-via-grep` (D-OUT-01 boundary was Phase 40 only; Phase 41 INTENTIONALLY flipped this — boundary is now obsolete because nav now reads from favorites; mark `verified-via-grep` with note "obsolete after Phase 41 D-OUT-01 flip; current behavior is bottom bar reactively shrinks/grows with favorites — see 41-VERIFICATION.md FAV-TEST-03").
    4. Reduced-motion open/close hamburger → `manual-QA-needed` (no local animations exist per code; vacuously satisfied; 10-second sanity check noted in release notes).

    `41-VERIFICATION.md` (4 items):
    1. Mobile bar visual identity + safe-area padding → `manual-QA-needed` (real iOS Safari rendering; v1.13.1 release-notes reminder per D-13).
    2. Non-favorited active route — bottom bar inactive visual state → `manual-QA-needed` (color-token visual confirmation).
    3. Desktop top nav identity indicators (border-b-2 + identity color) → `manual-QA-needed` (visual; v1.14 backlog if not done pre-deploy).
    4. "2 pre-existing navigation.spec.ts failures" → `verified-via-grep` / now-obsolete. RESEARCH Pitfall 3 confirmed `pnpm exec playwright test --list` now shows the spec at line 81 as `navigation.spec.ts:81:3 › Navigation (v1.2 restructure) › hamburger drawer About row opens the about sheet` — both Phase-40 regression failures were FIXED by commit `c2800df` ("test(42.1-05): adapt e2e specs for hero-drawer shell on mobile"). Mark `verified-via-grep` with that commit hash as evidence.

    BEFORE writing each row, the executor MUST confirm by running the cited grep / test command. If any item turns out to be `fix-required` and is NOT already covered by Task 5, surface it to the user as a pre-release blocker; do NOT silently bump. Per D-15: no "known issue" deferrals on the clinical-safety gate.

    Append exactly this Markdown block (substituting per-item rows) immediately before the `*Verified: …*` / `*Verifier: …*` footer of EACH file:

    ```markdown
    ---

    ## Phase 43 Triage (D-07 / D-08)

    **Triaged:** 2026-04-24 (pre-release)

    | # | Item (summary) | Triage Status | Rationale |
    |---|---|---|---|
    | 1 | <one-line summary of human_verification[0].test> | `verified-via-grep` \| `manual-QA-needed` \| `fix-required` | <evidence: grep command, test path + line, commit hash, or "v1.14 backlog — release-notes reminder"> |
    | 2 | … | … | … |
    | 3 | … | … | … |
    | 4 | … | … | … |

    **Disposition:** N verified-via-grep, M manual-QA-needed (deferred to v1.13.1 release-notes reminder + v1.14 backlog), 0 fix-required.
    ```

    Do NOT modify the existing frontmatter `human_verification:` YAML block — that is the input to triage; the appended Markdown block is the output. Also do NOT change the `status:` field in frontmatter; status flip from `human_needed` → `passed` is the orchestrator's job once a real-device QA pass happens.

    Single commit, conventional prefix: `docs(43): triage Phase 40 + 41 verification debt for v1.13 release`.
  </action>
  <verify>
    <automated>
      grep -c "## Phase 43 Triage" .planning/phases/40-favorites-store-hamburger-menu/40-VERIFICATION.md
      # MUST output: 1
      grep -c "## Phase 43 Triage" .planning/phases/41-favorites-driven-navigation/41-VERIFICATION.md
      # MUST output: 1
      grep -E "(verified-via-grep|manual-QA-needed|fix-required)" .planning/phases/40-favorites-store-hamburger-menu/40-VERIFICATION.md | wc -l
      # MUST be >= 4
      grep -E "(verified-via-grep|manual-QA-needed|fix-required)" .planning/phases/41-favorites-driven-navigation/41-VERIFICATION.md | wc -l
      # MUST be >= 4
      grep "Disposition:" .planning/phases/40-favorites-store-hamburger-menu/40-VERIFICATION.md
      grep "Disposition:" .planning/phases/41-favorites-driven-navigation/41-VERIFICATION.md
      # Both MUST match — confirms Disposition line present
      grep "fix-required" .planning/phases/40-favorites-store-hamburger-menu/40-VERIFICATION.md .planning/phases/41-favorites-driven-navigation/41-VERIFICATION.md
      # If any matches found, executor MUST surface as blocker before continuing to Task 2
    </automated>
  </verify>
  <acceptance_criteria>
    - Both VERIFICATION.md files contain a `## Phase 43 Triage (D-07 / D-08)` section appended after the body and before the trailing `*Verified:*` footer
    - Each triage section has a 4-row table with all 4 human_verification items (8 rows total across both files)
    - Each row has one of `verified-via-grep` / `manual-QA-needed` / `fix-required` in the Status column
    - Each row's Rationale column cites concrete evidence (grep command, file:line, commit hash, or "v1.14 backlog")
    - Each section has a `**Disposition:**` line summarizing the triage breakdown
    - Frontmatter `human_verification:` and `status:` fields are UNCHANGED
    - If any item is `fix-required` and not already in Task 5 scope, executor halts and surfaces a blocker conversation per D-15
    - Single commit lands with prefix `docs(43): triage Phase 40 + 41 verification debt for v1.13 release`
  </acceptance_criteria>
  <done>
    Both VERIFICATION.md files have appended Phase 43 Triage blocks with per-item statuses; all 8 items resolve to `verified-via-grep` or `manual-QA-needed` (no surprise `fix-required` blockers); commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete orphan planning artifacts (HANDOFF.json + .continue-here.md)</name>
  <files>
    .planning/HANDOFF.json (delete)
    .planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/.continue-here.md (delete)
  </files>
  <read_first>
    - .planning/phases/43-release-v1-13-0/43-CONTEXT.md (decisions D-09, D-10, D-11)
    - .planning/HANDOFF.json (confirm content references commit `1826069` and Phase 42.1 close — verifies it is the expected orphan, not a live state file)
    - .planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/.continue-here.md (confirm content references the closed Phase 42.1 pause)
    - .planning/phases/43-release-v1-13-0/43-PATTERNS.md (section 5 — orphan deletions)
  </read_first>
  <action>
    Per D-09 / D-10 / D-11 (delete the two orphan files in a single commit, NOT a separate housekeeping commit — but distinct from the version-bump commit per S1 release-commit granularity).

    Run:
    ```bash
    git rm .planning/HANDOFF.json
    git rm .planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/.continue-here.md
    ```

    Commit with EXACTLY this subject (suggested by RESEARCH.md, references the superseding commits):
    `chore(43): remove stale 42.1 handoff artifacts (superseded by 1ce4493/8fde90e/390aba6)`

    Optionally include a one-line body: "Phase 42.1 closed with the 6-plan sweep at commit 1826069; subsequent commits 1ce4493 (42.2 critique sweep), 8fde90e (STOP-red carve-out), and 390aba6 (recap desktop-hide) made the pause checkpoint and HANDOFF.json obsolete."

    Do NOT amend any prior commits. Do NOT create empty placeholder files. Do NOT touch any other files in this commit.
  </action>
  <verify>
    <automated>
      test ! -f .planning/HANDOFF.json && echo "OK: HANDOFF.json deleted" || (echo "FAIL: HANDOFF.json still exists" && exit 1)
      test ! -f .planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/.continue-here.md && echo "OK: .continue-here.md deleted" || (echo "FAIL: .continue-here.md still exists" && exit 1)
      git log -1 --oneline | grep -q "chore(43): remove stale 42.1 handoff artifacts" && echo "OK: commit subject matches" || (echo "FAIL: commit subject mismatch" && exit 1)
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/HANDOFF.json` no longer exists in the working tree (`test ! -f` returns 0)
    - `.planning/phases/42.1-…/.continue-here.md` no longer exists
    - HEAD commit subject is `chore(43): remove stale 42.1 handoff artifacts (superseded by 1ce4493/8fde90e/390aba6)`
    - Both deletions are in the SAME commit (single `git log -1 --stat` shows both as deleted)
    - No other files modified in this commit
  </acceptance_criteria>
  <done>
    Two orphan files deleted in one chore commit referencing the superseding commit hashes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pre-bump fix — uac-uvc.spec.ts mobile beforeEach timeout</name>
  <files>
    e2e/uac-uvc.spec.ts
  </files>
  <read_first>
    - e2e/uac-uvc.spec.ts (full file — current failing form at lines 46-66 per PATTERNS section 6)
    - e2e/feeds.spec.ts (lines 1-60 — working sibling, exact shape match per PATTERNS section 6 + interfaces.feeds_spec_working_pattern above)
    - e2e/gir.spec.ts (lines 1-40 — second working sibling)
    - .planning/phases/43-release-v1-13-0/43-RESEARCH.md (Pitfall 2, Open Question 1, Assumption A1)
    - .planning/phases/43-release-v1-13-0/43-PATTERNS.md (section 6 — three candidate fixes)
  </read_first>
  <action>
    Per D-15 (no "known issue" deferrals on clinical-safety gate) and RESEARCH.md Pitfall 2 / Pattern 6.

    Diagnosis (verified reproducible in RESEARCH session): `e2e/uac-uvc.spec.ts:98` — UAC/UVC happy path (mobile) > weight textbox has inputmode="decimal" — fails with `Test timeout of 30000ms exceeded while running "beforeEach" hook.` at the `await page.getByLabel('Open inputs drawer').click()` line. The desktop iteration of the same test passes. The page snapshot at the failure shows a desktop layout was rendered instead of the 375px mobile shell — viewport application or hydration timing is at fault.

    Step 1: Reproduce locally.
    ```bash
    pnpm exec playwright test --project=chromium --workers=1 e2e/uac-uvc.spec.ts -g "weight textbox has inputmode" --reporter=list
    ```
    Confirm the timeout reproduces.

    Step 2: Apply the smallest-diff fix in this priority order (try one, confirm green, stop):

    Candidate A (likely simplest — wait for mobile drawer button to be visible before clicking):
    ```typescript
    if (viewport.name === 'mobile') {
      const drawerButton = page.getByLabel('Open inputs drawer');
      await drawerButton.waitFor({ state: 'visible', timeout: 5000 });
      await drawerButton.click();
    }
    ```

    Candidate B (if A doesn't reveal the issue — add networkidle wait):
    ```typescript
    await page.goto('/uac-uvc');
    await page.waitForLoadState('networkidle');
    // ... rest unchanged
    ```

    Candidate C (if `test.use({ viewport })` truly does not apply — replace with explicit setViewportSize inside beforeEach):
    ```typescript
    test.beforeEach(async ({ page }) => {
      await page.setViewportSize({ width: viewport.width, height: viewport.height });
      await page.goto('/uac-uvc');
      await page
        .getByRole('button', { name: /understand/i })
        .click({ timeout: 2000 })
        .catch(() => {});
      if (viewport.name === 'mobile') {
        await page.getByLabel('Open inputs drawer').click();
      }
    });
    // (and remove `test.use({ viewport: ... })` from the describe scope)
    ```

    Step 3: After the fix, run BOTH iterations and the full uac-uvc spec:
    ```bash
    pnpm exec playwright test --project=chromium --workers=1 e2e/uac-uvc.spec.ts --reporter=list
    ```
    Both mobile and desktop iterations of every test in the file MUST pass.

    Step 4: Confirm structurally similar to feeds.spec.ts:1-60 — do NOT rewrite the viewport-loop architecture; the goal is to make the shape match `feeds.spec.ts` / `gir.spec.ts` exactly, only adjusting the missing wait/setup hook.

    Commit prefix: `fix(43): repair uac-uvc mobile viewport beforeEach hook`. If candidate A succeeds, mention "added explicit waitFor on Open inputs drawer button before click" in the commit body.

    Time-box: ~30 minutes per RESEARCH.md A1. If the fix balloons, halt and surface to user — do NOT defer per D-15.
  </action>
  <verify>
    <automated>
      pnpm exec playwright test --project=chromium --workers=1 e2e/uac-uvc.spec.ts --reporter=line
      # MUST exit 0; all tests in spec pass (both mobile + desktop iterations)
    </automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec playwright test --project=chromium --workers=1 e2e/uac-uvc.spec.ts` exits 0
    - Both mobile and desktop iterations of "weight textbox has inputmode" pass
    - Spec structure remains a 2-viewport `for ... test.describe ... test.use ... test.beforeEach` shape (do not refactor architecture)
    - Commit prefix is `fix(43):`
    - No other files modified in this commit
  </acceptance_criteria>
  <done>
    `e2e/uac-uvc.spec.ts` runs green for both mobile and desktop iterations; commit lands with `fix(43):` prefix.
  </done>
</task>

<task type="auto">
  <name>Task 4: Pre-bump fix — morphine-wean-a11y.spec.ts light-mode advisory axe violation</name>
  <files>
    e2e/morphine-wean-a11y.spec.ts
    (potentially also: src/app.css OR src/lib/morphine-wean/MorphineWeanCalculator.svelte OR src/lib/shared/components/RangedNumericInput.svelte — depends on what the axe violation reports)
  </files>
  <read_first>
    - e2e/morphine-wean-a11y.spec.ts (full file — failing test at lines 87-107, dark-mode sibling at 109-131 passes)
    - e2e/feeds-a11y.spec.ts (full file — passing dual-theme advisory pattern, role-match analog)
    - .planning/phases/43-release-v1-13-0/43-RESEARCH.md (Pitfall 2, Assumption A5 — suspect post-42.1 commits `b548ce4` Amber rescope or `0558253` RangedNumericInput unification)
    - .planning/phases/43-release-v1-13-0/43-PATTERNS.md (section 7 — diagnosis procedure)
  </read_first>
  <action>
    Per D-15 (no "known issue" deferrals on clinical-safety gate) and RESEARCH.md Pitfall 2 / A5.

    Diagnosis (verified reproducible in RESEARCH session): `e2e/morphine-wean-a11y.spec.ts:87` — "morphine wean advisory message has no axe violations in light mode" fails with axe-core surfacing a violation. Dark-mode sibling test at `:109-131` passes; the difference is light-mode token rendering. RESEARCH A5 suspects post-42.1 Amber rescope (commit `b548ce4`) or RangedNumericInput unification (commit `0558253`) introduced a token regression.

    Step 1: Run the failing test isolated, with full reporter, to read the actual violation details:
    ```bash
    pnpm exec playwright test --project=chromium --workers=1 e2e/morphine-wean-a11y.spec.ts -g "advisory message has no axe violations in light mode" --reporter=list
    ```
    Inspect the `results.violations` array output: `id` (e.g., `color-contrast`), `impact`, `target` (CSS selector), `failureSummary` (the specific contrast ratio + foreground/background colors).

    Step 2: Locate the offending token. Three likely paths per PATTERNS section 7:
    - If violation `id === 'color-contrast'` AND target is the advisory background or text → likely an OKLCH token in `src/app.css` (search for `--color-amber-*` or the advisory-banner classes used by MorphineWeanCalculator's blur-gated "Outside expected range — verify" surface).
    - If target is the RangedNumericInput range-hint text → fix the secondary-text token used by that component in light mode.
    - If target is the textbox border or focus ring → adjust the input-border or focus-ring token.

    Step 3: Apply the smallest-diff token fix (analog: v1.9 commit `ec50793` darkened GIR identity from `oklch(46% 0.12 145)` → `oklch(42% 0.12 145)` for 4.5:1 — same shape: bump lightness 2-4 percentage points). Do NOT widen the violation surface; the fix should touch the single token that fails contrast.

    DO NOT use `disableRules()` or `withTags(...)` exclusions to silence the violation — D-15 forbids deferrals on the clinical-safety gate.

    Step 4: Re-run the full morphine-wean-a11y spec to confirm BOTH light + dark + advisory-light + advisory-dark + focus variants pass:
    ```bash
    pnpm exec playwright test --project=chromium --workers=1 e2e/morphine-wean-a11y.spec.ts --reporter=line
    ```

    Step 5: Run the broader axe sweep across all 7 a11y specs to confirm the token tweak did not regress any other surface:
    ```bash
    pnpm exec playwright test --project=chromium --workers=1 e2e/*-a11y.spec.ts --reporter=line
    ```
    All axe sweeps MUST be green in both themes.

    Commit prefix:
    - If the fix is a token tweak: `fix(43): bump <token-name> for light-mode 4.5:1 contrast on morphine advisory` (touches `src/app.css` or relevant component file).
    - If the fix is in the test only (e.g., a stale assertion): `test(43): align morphine-wean-a11y advisory test with current advisory surface` (touches `e2e/morphine-wean-a11y.spec.ts` only — but D-15 strongly disfavors test-only fixes if the underlying surface really has a contrast bug).

    Time-box: ~30 minutes per RESEARCH.md A1. If the violation turns out to be a structural a11y issue requiring component refactor, halt and surface to user — do NOT defer per D-15.
  </action>
  <verify>
    <automated>
      pnpm exec playwright test --project=chromium --workers=1 e2e/morphine-wean-a11y.spec.ts --reporter=line
      # MUST exit 0; all tests in spec pass (light + dark + advisory + focus variants)
      pnpm exec playwright test --project=chromium --workers=1 e2e/feeds-a11y.spec.ts e2e/fortification-a11y.spec.ts e2e/gir-a11y.spec.ts e2e/uac-uvc-a11y.spec.ts e2e/favorites-nav-a11y.spec.ts --reporter=line
      # MUST exit 0 — confirm no regression on sibling axe specs from any token tweak
    </automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec playwright test e2e/morphine-wean-a11y.spec.ts` exits 0 with all variants passing (light + dark + advisory-light + advisory-dark + focus)
    - All 7 a11y specs (morphine-wean, feeds, fortification, gir, uac-uvc, favorites-nav, disclaimer-banner) exit 0
    - No use of `disableRules()` or rule exclusions in the modified spec — actual fix is in the surface (token, component, or markup)
    - Commit prefix is `fix(43):` (or `test(43):` only if the fix is genuinely a stale test assertion against the current correct surface)
    - Smallest-possible-diff fix; does not widen scope beyond the failing surface
  </acceptance_criteria>
  <done>
    `e2e/morphine-wean-a11y.spec.ts` runs green for all variants; broader axe suite remains green; commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 5: Version bump + PROJECT.md + REQUIREMENTS.md (38 non-REL flips) + ROADMAP.md</name>
  <files>
    package.json
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - package.json (full file — single line edit at line 4)
    - .planning/PROJECT.md (full file — Validated section 17-105, Active section 107, Current Milestone 109+, Context 130+, footer 196)
    - .planning/REQUIREMENTS.md (full file — traceability table 102-148, footer 163)
    - .planning/ROADMAP.md (full file — Phase 43 Progress row at line 163)
    - .planning/phases/43-release-v1-13-0/43-CONTEXT.md (decisions D-01, D-02, D-03, D-04, D-05, D-06)
    - .planning/phases/43-release-v1-13-0/43-PATTERNS.md (sections 1, 2, 3, 4 — version bump + Validated bullets + REQUIREMENTS flip + ROADMAP row)
    - .planning/phases/43-release-v1-13-0/43-RESEARCH.md (Pitfalls 1 + 3, Open Questions 3 + 4, Code Examples)
  </read_first>
  <action>
    This task lands the bulk of the v1.13 release receipt as a single atomic commit. All four files modified, single commit subject.

    ### A. `package.json` version bump (REL-01)

    Edit line 4 verbatim:
    ```diff
    -  "version": "1.12.5",
    +  "version": "1.13.0",
    ```

    NO other edits to `package.json`. Do NOT touch `vite.config.ts`, `about-content.ts`, `AboutSheet.svelte`, or any source — `__APP_VERSION__` is the single source of truth (see `<interfaces>` in plan context).

    ### B. `.planning/PROJECT.md` Validated bullets append (REL-02)

    Insert the following bullets at the END of the Validated list (after line 105, the v1.12 release bullet — preserves milestone-ordered chronology). The Validated list is ordered chronologically v1.0 → v1.12 → v1.13.

    Per D-02 (per-ID format for Phase 40, 41, 42):

    ```markdown
    - ✓ Favorites store + hamburger menu: reactive `$state` singleton at `src/lib/shared/favorites.svelte.ts` with localStorage persistence (key `nicu:favorites`, schema `{v:1, ids: CalculatorId[]}`), 4-cap enforcement, schema-safe 6-step recovery (D-08), first-run defaults `['morphine-wean', 'formula', 'gir', 'feeds']`, registry-order stable sort; native `<dialog>`-based HamburgerMenu component listing every registered calculator with sibling link+star rows, focus restore on close, `prefers-reduced-motion` honored, `--color-scrim` backdrop; hamburger button in NavShell title bar (leftmost, 48×48, aria-haspopup=dialog); 19 + 14 = 33 co-located vitest cases (FAV-01..07, NAV-HAM-01..05, FAV-TEST-01, FAV-TEST-02) — v1.13 (Phase 40)
    - ✓ Favorites-driven navigation: NavShell mobile bottom bar and desktop top nav rewritten to iterate `visibleCalculators` ($derived from `favorites.current` mapped through registry) preserving `min-h-14`, safe-area padding, focus outlines, identity color indicators (`identityClass`, border-b-2 on desktop active tab); non-favorited active routes do NOT add temporary tabs (`aria-current="page"` semantics preserved on hamburger drawer About row instead); favorites store seeded at module scope (D-07) so SSR and pre-init hydration render with default 4 favorites; T-20 regression guard for module-scope seed; Playwright E2E (`e2e/favorites-nav.spec.ts` — 4 tests × 2 viewports) covering open hamburger → unfavorite → favorite → reload → persist; Playwright axe (`e2e/favorites-nav-a11y.spec.ts` — light + dark on open hamburger) (NAV-FAV-01..04, FAV-TEST-03, FAV-TEST-04) — v1.13 (Phase 41)
    - ✓ UAC/UVC Calculator architecture + identity hue: `CalculatorId` union extended with `'uac-uvc'`, registry entry with `.identity-uac` OKLCH token pair (hue researched pre-PR per v1.8 decision, 4.5:1 contrast on all identity surfaces in both themes on first axe sweep), `src/lib/uac-uvc/` module (types, config, state, calculations, parity fixtures + tests), `/uac-uvc` route shell, NavShell `activeCalculatorId` ternary extended, AboutSheet `uac-uvc` entry citing `uac-uvc-calculator.xlsx` and noting the rule-of-thumb formula must be confirmed by imaging per institutional protocol (UAC-ARCH-01..05, UAC-09) — v1.13 (Phase 42)
    - ✓ UAC/UVC Calculator UI + calculations: `UacUvcCalculator.svelte` with weight input (textbox + slider, bidirectional sync, range 0.3–10 kg, `inputmode="decimal"`); two visually distinct hero result cards — UAC depth (`weight × 3 + 9` cm, parity with `uac-uvc-calculator.xlsx` cell B3) and UVC depth (`(weight × 3 + 9) / 2` cm, parity with cell B7) — using D-05 three-cue distinction (color accent + icon + layout) so a clinician cannot confuse UAC for UVC at a glance; tabular numerals + large bold values + `aria-live="polite"` + reduced-motion-gated pulse; blur-gated "Outside expected range — verify" advisory (no auto-clamp, consistent with v1.6 NumericInput); sessionStorage persistence; spreadsheet-parity tests within 1% epsilon for weights 0.3, 1.0, 2.5, 5.0, 10.0 kg; component test covering empty / valid / sync / out-of-range; Playwright E2E at mobile 375 + desktop 1280 with `inputmode="decimal"` regression guard, favorites round-trip, cap-full, reload, slider drag; Playwright axe sweeps in light + dark (UAC-01..09, UAC-TEST-01..04) — v1.13 (Phase 42)
    ```

    Per D-03 (Phase 42.1 narrative bullet):
    ```markdown
    - ✓ Phase 42.1 Design Polish + Redesign Sweep: closed the /impeccable critique residual after Phase 42 — DESIGN.md / DESIGN.json design contract landed (Identity-Inside Rule, Amber-as-Semantic, OKLCH-Only, Red-Means-Wrong, Five-Roles-Only, Tabular-Numbers, Eyebrow-Above-Numeral, 11px Floor, Tonal-Depth, Flat-Card-Default); shared `<HeroResult>` component owning above-the-fold viewport on mount across all 5 calculators; inputs collapsed to sticky drawer above bottom nav; mobile bottom nav clears content at 375 + 414 viewports with single-line tab labels and backdrop-blur; identity hue restricted to inside-the-route surfaces only (chrome carries Clinical Blue or neutral); disclaimer rewritten as dismissable banner (no first-paint-blocking modal, full text re-readable from AboutSheet, existing acknowledgments auto-migrate); morphine schedule shows 3-decimal mg precision; dock-style scroll magnification removed; CSS transitions scoped to specific selectors; root `/` ships static-HTML meta refresh redirect; D-05 third cue restored on UAC/UVC (directional arrows); 42-UI-SPEC.md amended retrospectively for post-em-dash-purge eyebrow + bits-ui Slider substitution (6 plans, closed at commit 1826069) — v1.13 (Phase 42.1)
    ```

    Per D-05 + D-06 (post-42.1 follow-up + 42.2 critique sweep + STOP-red + recap-hide narrative bullet, with explicit commit hashes):
    ```markdown
    - ✓ Phase 42.1 follow-up + 42.2 critique sweep: post-critique polish wave — em-dash purge, Amber rescope (`b548ce4`), HeroResult display-numeral rule, RangedNumericInput unification (`0558253`), drawer + nav clearance polish, mobile-nav icon centering, PWA meta refresh; 42.2 impeccable critique sweep across 6 tasks — harden / clarify / distill / shape / layout / polish (29 → 35/40, commit `1ce4493`); STOP-red clinical-safety carve-out as the single authorized exception to the Red-as-Error rule on severe-neuro GIR (commit `8fde90e`); InputsRecap desktop-hide refinement (redundant with sticky sidebar, commit `390aba6`) — v1.13 (Phase 42.1 follow-up + 42.2 critique sweep)
    ```

    Per Pattern S2 (Phase 43 release bullet — closes the v1.13 group):
    ```markdown
    - ✓ `package.json` version bumped to 1.13.0; AboutSheet reflects v1.13.0 via the `__APP_VERSION__` build-time constant; PROJECT.md Validated list updated with v1.13 entries (per-ID for Phases 40/41/42, narrative for 42.1 + follow-up); REQUIREMENTS.md traceability table all 41 v1.13 IDs flipped to ✓ Validated; ROADMAP.md Phase 43 Progress row marked Complete; orphan planning artifacts (`.planning/HANDOFF.json`, Phase 42.1 `.continue-here.md`) deleted; 40-VERIFICATION.md and 41-VERIFICATION.md triaged with per-item D-07 / D-08 status; two pre-existing Playwright failures fixed pre-bump (uac-uvc mobile beforeEach hook, morphine-wean light-mode advisory axe) per D-15 (no known-issue deferrals); full clinical gate green — svelte-check 0/0 across 4571+ files, vitest TBD-passing, `pnpm build` ✓, Playwright E2E + extended axe suite green in both themes (REL-01..03) — v1.13 (Phase 43)
    ```

    ### C. `.planning/PROJECT.md` Current State paragraph rewrite (REL-02 / D-04)

    Replace lines 7-9 (the `## Current State` section's `**Shipped:**` paragraph) with:

    ```markdown
    ## Current State

    **Shipped:** v1.13.0 UAC/UVC Calculator + Favorites Nav (2026-04-24) — Fifth clinical calculator added: UAC/UVC umbilical-catheter depth (`weight × 3 + 9` cm and half-depth UVC) using `uac-uvc-calculator.xlsx`; new hamburger menu lists every registered calculator with star toggles; favorites-driven nav (max 4) replaces the static bottom bar with first-run defaults `['morphine-wean', 'formula', 'gir', 'feeds']` preserving the v1.12 layout for existing users. Design polish sweep (Phase 42.1): HeroResult shared contract owning above-the-fold viewport on mount, DisclaimerBanner v1→v2 (dismissable, re-readable from AboutSheet), mobile bottom-nav clearance correct at 375 + 414, hero-fills-viewport InputDrawer pattern across all 5 routes, dock magnification removed, RangedNumericInput unification, scoped CSS transitions, DESIGN.md / DESIGN.json design contract enforced (Identity-Inside, Amber-as-Semantic, OKLCH-Only, Red-Means-Wrong, Tabular-Numbers, Eyebrow-Above-Numeral). Clinical-safety carve-out: STOP-red on severe-neuro GIR (single authorized exception to Red-as-Error rule). PWA at version 1.13.0; svelte-check 0/0 across 4571+ files, vitest fully green, Playwright E2E + extended axe suite (UAC/UVC + hamburger variants) green in both themes. Real-iPhone smoke test (drawer, keyboard, STOP-red contrast in standalone PWA mode) recommended post-deploy as v1.13.1 hotfix fuel — non-blocking per D-12.
    ```

    The `**In progress:**` line (if present) is removed. Update the trailing test-count "TBD-passing" placeholder with the actual `pnpm test:run` count from Task 6 (executor edits this line at gate-pass time, NOT at this task's commit — leave as `TBD-passing` here and amend in Task 6). NOTE: per RESEARCH.md A4 + A1, executor should treat the test count as fluid; the prose mentions "fully green" and Task 6 substitutes the exact count.

    ### D. `.planning/PROJECT.md` Current Milestone header rewrite (REL-02 / D-04)

    Replace lines 109-122 (the `## Current Milestone: v1.13 UAC/UVC Calculator + Favorites Nav` block + Goal + Target features bullets):

    ```markdown
    ## Current Milestone: v1.13 UAC/UVC Calculator + Favorites Nav (Shipped)

    **Shipped.** Fifth clinical calculator delivered: UAC/UVC umbilical-catheter depth with weight input (textbox + slider, 0.3–10 kg) producing two visually distinct hero result cards for UAC and UVC depth. Hamburger menu lists every registered calculator; favorites-driven nav (max 4) replaces the static bottom bar with first-run defaults preserving the v1.12 layout for existing users. Design polish sweep (Phase 42.1) closed the /impeccable critique residual: shared HeroResult contract, DisclaimerBanner v1→v2, mobile-nav clearance, hero-fills-viewport InputDrawer pattern, dock magnification removed, scoped CSS transitions; DESIGN.md / DESIGN.json design contract enforced. Clinical-safety carve-out: STOP-red on severe-neuro GIR (single authorized exception to Red-as-Error rule). Released as v1.13.0.
    ```

    ### E. `.planning/PROJECT.md` footer timestamp

    Replace line 196 (footer):
    ```diff
    -*Last updated: 2026-04-23 — v1.13 UAC/UVC Calculator + Favorites Nav milestone started*
    +*Last updated: 2026-04-24 — v1.13.0 Phase 43 complete (release — version bump, PROJECT.md, REQUIREMENTS.md, ROADMAP.md, orphan cleanup, gates)*
    ```

    ### F. `.planning/REQUIREMENTS.md` traceability table — 38 non-REL flips (REL-02)

    Per RESEARCH.md Pitfall 1 and PATTERNS section 3: flip the 38 non-REL rows from Pending to ✓ Validated NOW (the 3 REL-* rows flip in Task 6 after the gate is green per Pattern 3 ordering rule). The 38 rows by line number (verified in this plan's RESEARCH.md grep output):

    Lines 108-125: UAC-01 through UAC-TEST-04 (18 rows) → all flip to ✓ Validated
    Lines 126-130: NAV-HAM-01 through NAV-HAM-05 (5 rows) → all flip to ✓ Validated
    Lines 131-137: FAV-01 through FAV-07 (7 rows) → all flip to ✓ Validated
    Lines 138-141: NAV-FAV-01 through NAV-FAV-04 (4 rows) → all flip to ✓ Validated
    Lines 142-145: FAV-TEST-01 through FAV-TEST-04 (4 rows) → all flip to ✓ Validated

    Total: 38 rows. The diff for each is:
    ```diff
    -| UAC-01 | Phase 42 | Pending |
    +| UAC-01 | Phase 42 | ✓ Validated |
    ```

    Apply identically to all 38 rows. Lines 146-148 (REL-01, REL-02, REL-03) STAY as `Pending` until Task 6 — the version bump's gate is not yet proven at this task's commit time.

    Update the trailing footer at line 163:
    ```diff
    -*Last updated: 2026-04-23 — traceability populated by roadmap (Phases 40-43)*
    +*Last updated: 2026-04-24 — all v1.13 requirements validated at release (Phase 43)*
    ```

    NOTE: line 163 footer change is paired with Task 6's REL-* flips into the same final state. Apply it here; Task 6 only flips the 3 REL rows.

    ### G. `.planning/ROADMAP.md` Phase 43 Progress row (REL-02)

    Replace line 163:
    ```diff
    -| 43. Release v1.13.0 | v1.13 | 0/? | Not started | — |
    +| 43. Release v1.13.0 | v1.13 | 1/1 | Complete | 2026-04-24 |
    ```

    Do NOT change line 18 (milestone-row link to `[v1.13 …]` in the Milestones section) — milestone-archive of `v1.13` to `milestones/v1.13-phases/` directory is `/gsd-complete-milestone`'s job per RESEARCH.md Open Question 4, NOT Phase 43's scope. Leave that link unchanged.

    ### Commit

    Single commit, conventional prefix:
    `feat(43-01): bump v1.13.0 + PROJECT.md + REQUIREMENTS.md (38/41) + ROADMAP.md`

    Optional body: a 2–3 line summary referencing the per-ID flip count, the narrative bullets added, and that the 3 REL-* rows + final-gate test counts will land in Task 6 after the gate runs.
  </action>
  <verify>
    <automated>
      grep -E '"version": "1\.13\.0"' package.json && echo "OK: package.json bumped" || (echo "FAIL: version not bumped" && exit 1)
      grep -E '"version": "1\.12\.5"' package.json && (echo "FAIL: stale 1.12.5 still in package.json" && exit 1) || echo "OK: no stale version"

      grep -c "v1.13 (Phase 40)" .planning/PROJECT.md
      # MUST be >= 1
      grep -c "v1.13 (Phase 41)" .planning/PROJECT.md
      # MUST be >= 1
      grep -c "v1.13 (Phase 42)" .planning/PROJECT.md
      # MUST be >= 2 (ARCH bullet + UI bullet)
      grep -c "v1.13 (Phase 42.1)" .planning/PROJECT.md
      # MUST be >= 1
      grep -c "v1.13 (Phase 42.1 follow-up + 42.2 critique sweep)" .planning/PROJECT.md
      # MUST be >= 1
      grep -c "v1.13 (Phase 43)" .planning/PROJECT.md
      # MUST be >= 1

      grep "v1.13.0 UAC/UVC" .planning/PROJECT.md
      # MUST match — Current State paragraph + Validated bullet
      grep "Shipped.*v1.13.0" .planning/PROJECT.md
      # MUST match
      grep "Current Milestone: v1.13 UAC/UVC Calculator + Favorites Nav (Shipped)" .planning/PROJECT.md
      # MUST match exactly

      grep -c "1ce4493" .planning/PROJECT.md
      # MUST be >= 1 (D-06 commit hash)
      grep -c "8fde90e" .planning/PROJECT.md
      # MUST be >= 1 (D-06 commit hash)
      grep -c "390aba6" .planning/PROJECT.md
      # MUST be >= 1 (D-06 commit hash)

      # 38 non-REL flips done; REL-* still Pending until Task 6
      PENDING_COUNT=$(grep -c "| Pending |" .planning/REQUIREMENTS.md)
      [ "$PENDING_COUNT" -eq 3 ] && echo "OK: exactly 3 Pending rows remain (REL-01..03)" || (echo "FAIL: expected 3 Pending, got $PENDING_COUNT" && exit 1)
      VALIDATED_COUNT=$(grep -c "| ✓ Validated |" .planning/REQUIREMENTS.md)
      [ "$VALIDATED_COUNT" -eq 38 ] && echo "OK: exactly 38 ✓ Validated rows" || (echo "FAIL: expected 38 ✓ Validated, got $VALIDATED_COUNT" && exit 1)

      grep "all v1.13 requirements validated at release (Phase 43)" .planning/REQUIREMENTS.md
      # MUST match — footer timestamp updated

      grep -E "^\| 43\. Release v1\.13\.0 \| v1\.13 \| 1/1 \| Complete \| 2026-04-24 \|" .planning/ROADMAP.md
      # MUST match exactly — Phase 43 Progress row flipped

      # Confirm we did NOT touch source code
      git diff --name-only HEAD~1 HEAD | grep -E "^src/|^vite\.config\.ts|^about-content" && (echo "FAIL: source code modified — version chain should be untouched" && exit 1) || echo "OK: no source code edits"
    </automated>
  </verify>
  <acceptance_criteria>
    - `package.json:4` reads exactly `"version": "1.13.0",`
    - No occurrence of `"version": "1.12.5"` remains in `package.json`
    - PROJECT.md Validated list contains: ≥1 bullet tagged `— v1.13 (Phase 40)`, ≥1 tagged `— v1.13 (Phase 41)`, ≥2 tagged `— v1.13 (Phase 42)`, ≥1 tagged `— v1.13 (Phase 42.1)`, ≥1 tagged `— v1.13 (Phase 42.1 follow-up + 42.2 critique sweep)`, ≥1 tagged `— v1.13 (Phase 43)`
    - PROJECT.md Validated bullets reference commit hashes `1ce4493`, `8fde90e`, `390aba6` (D-06)
    - PROJECT.md Current State `**Shipped:**` paragraph mentions v1.13.0, fifth calculator, hamburger, favorites, Phase 42.1 polish, STOP-red carve-out
    - PROJECT.md `## Current Milestone: v1.13 UAC/UVC Calculator + Favorites Nav (Shipped)` header present with `**Shipped.**` paragraph
    - PROJECT.md footer reads `*Last updated: 2026-04-24 — v1.13.0 Phase 43 complete …*`
    - REQUIREMENTS.md traceability table: exactly 3 `| Pending |` rows remain (REL-01, REL-02, REL-03); exactly 38 `| ✓ Validated |` rows present
    - REQUIREMENTS.md footer reads `*Last updated: 2026-04-24 — all v1.13 requirements validated at release (Phase 43)*`
    - ROADMAP.md line 163 reads exactly `| 43. Release v1.13.0 | v1.13 | 1/1 | Complete | 2026-04-24 |`
    - No files in `src/` or `vite.config.ts` or `about-content.ts` modified
    - Single commit with prefix `feat(43-01):`
  </acceptance_criteria>
  <done>
    Version bumped, all v1.13 documentation reflects shipped state (Validated bullets, Current State, Current Milestone Shipped header, footer), 38 of 41 traceability rows flipped, ROADMAP Phase 43 row Complete, single commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 6: Final gate sweep + REL-* traceability flips + Current State test-count substitution</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/PROJECT.md
    (potentially: any source file fixed during gate-loop debug — only if a regression is discovered)
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (verify state after Task 5: 38 ✓ Validated, 3 Pending = REL-01..03)
    - .planning/PROJECT.md (verify Current State paragraph from Task 5; confirm "TBD-passing" placeholder text exists or note where the test-count phrase lives — Task 5's prose says "vitest fully green" so a count substitution may be cosmetic; per executor judgment)
    - .planning/phases/43-release-v1-13-0/43-CONTEXT.md (decisions D-14, D-15)
    - .planning/phases/43-release-v1-13-0/43-RESEARCH.md (Pitfall 1 — flip-order rule)
    - .planning/phases/43-release-v1-13-0/43-PATTERNS.md (Pattern S4 — gate sweep cadence)
  </read_first>
  <action>
    Run the full clinical quality gate per D-14 / D-15 in this exact order. ONLY proceed to the next gate when the previous one returns exit 0. If any gate fails, fix the underlying issue (normal debug flow per D-15 Claude's Discretion) before continuing — do NOT defer or use `--reporter=html` filtering to hide failures.

    ### Gate 1: svelte-check (D-14 baseline 0/0 across 4571 files)

    ```bash
    pnpm check
    ```
    Required: `0 ERRORS 0 WARNINGS`. File count may have grown beyond 4571 (this is fine — count is informational).

    ### Gate 2: vitest (D-14 baseline 340/340)

    ```bash
    pnpm test:run
    ```
    Required: exit 0, all tests pass. Capture the actual test count + file count from the output (e.g., `Test Files  N passed (N)` / `Tests  M passed (M)`) for use in Step E below.

    ### Gate 3: build (D-14 baseline exit 0, 45 PWA precache entries)

    ```bash
    pnpm build
    ```
    Required: exit 0, `built in <time>`, PWA precache manifest written.

    ### Gate 4: Playwright E2E (D-14 — both themes green; D-15 — no known-issue deferrals)

    ```bash
    pnpm exec playwright test --project=chromium --reporter=line
    ```
    Required: exit 0, all 16 specs pass. The 2 fixes from Tasks 3 + 4 should now be green. If any other spec surfaces a new failure (e.g., a regression introduced by the morphine-wean axe fix), fix it in this task — do NOT defer per D-15.

    ### Gate 5: Axe sweeps explicit re-run (D-14 / REL-03 — confirms extended UAC/UVC + hamburger variants green in both themes)

    ```bash
    pnpm exec playwright test --project=chromium --reporter=line e2e/morphine-wean-a11y.spec.ts e2e/fortification-a11y.spec.ts e2e/gir-a11y.spec.ts e2e/feeds-a11y.spec.ts e2e/uac-uvc-a11y.spec.ts e2e/favorites-nav-a11y.spec.ts e2e/disclaimer-banner.spec.ts
    ```
    Required: exit 0 (this is a subset of Gate 4 but worth running explicitly to ensure axe-tagged specs all green; redundant in the ideal case).

    ### After all gates pass:

    A. Flip the final 3 traceability rows in `.planning/REQUIREMENTS.md` (lines 146-148):
    ```diff
    -| REL-01 | Phase 43 | Pending |
    -| REL-02 | Phase 43 | Pending |
    -| REL-03 | Phase 43 | Pending |
    +| REL-01 | Phase 43 | ✓ Validated |
    +| REL-02 | Phase 43 | ✓ Validated |
    +| REL-03 | Phase 43 | ✓ Validated |
    ```

    After this flip, `grep -c "| Pending |" .planning/REQUIREMENTS.md` MUST return 0 for the v1.13 IDs.

    B. (Optional, executor judgment) If PROJECT.md Current State paragraph from Task 5 contains a literal "TBD-passing" or similar placeholder, replace it with the actual vitest count (e.g., `vitest 340/340`). If Task 5's prose only says "vitest fully green" without a count, this step is a no-op.

    ### Commit

    Single commit:
    `feat(43-01): green-gate REL-* traceability flips for v1.13.0`

    Optional body: brief summary of the 4 gate results (svelte-check, vitest, build, Playwright + axe) with counts.

    ### If a gate fails:

    Per D-15: no deferrals. Fix the underlying issue, commit the fix with appropriate `fix(43):` prefix, and re-run from Gate 1. If the fix scope balloons beyond ~30 minutes of work, halt and surface to user (matches RESEARCH.md A1 / Open Question 1 pattern).
  </action>
  <verify>
    <automated>
      pnpm check 2>&1 | grep -E "0 ERRORS 0 WARNINGS" && echo "OK: svelte-check 0/0" || (echo "FAIL: svelte-check not 0/0" && exit 1)
      pnpm test:run
      # MUST exit 0
      pnpm build
      # MUST exit 0
      pnpm exec playwright test --project=chromium --reporter=line
      # MUST exit 0 — all 16 specs green
      PENDING_AFTER=$(grep -c "| Pending |" .planning/REQUIREMENTS.md)
      [ "$PENDING_AFTER" -eq 0 ] && echo "OK: 0 Pending rows remain" || (echo "FAIL: $PENDING_AFTER Pending rows still present" && exit 1)
      VALIDATED_FINAL=$(grep -c "| ✓ Validated |" .planning/REQUIREMENTS.md)
      [ "$VALIDATED_FINAL" -eq 41 ] && echo "OK: all 41 v1.13 IDs ✓ Validated" || (echo "FAIL: expected 41 ✓ Validated, got $VALIDATED_FINAL" && exit 1)
      grep -E "^\| REL-01 \| Phase 43 \| ✓ Validated \|" .planning/REQUIREMENTS.md
      grep -E "^\| REL-02 \| Phase 43 \| ✓ Validated \|" .planning/REQUIREMENTS.md
      grep -E "^\| REL-03 \| Phase 43 \| ✓ Validated \|" .planning/REQUIREMENTS.md
      # All 3 MUST match exactly
    </automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` exits 0 with `0 ERRORS 0 WARNINGS`
    - `pnpm test:run` exits 0 with all tests passing
    - `pnpm build` exits 0 with PWA precache manifest written
    - `pnpm exec playwright test --project=chromium` exits 0 — all 16 specs pass (including the 2 fixed in Tasks 3 + 4)
    - All 7 axe specs green in both light and dark themes
    - REQUIREMENTS.md has exactly 41 `| ✓ Validated |` rows and 0 `| Pending |` rows for v1.13 IDs
    - REL-01, REL-02, REL-03 each flipped to `✓ Validated`
    - If Task 5 left a "TBD-passing" placeholder in PROJECT.md, it is replaced with the actual vitest count
    - Single commit with prefix `feat(43-01):` lands; if any debug-loop fixes were needed, those land as separate `fix(43):` commits BEFORE this one
    - No "known issue" deferrals; no `disableRules()` or `withTags(...)` exclusions added; D-15 honored
  </acceptance_criteria>
  <done>
    All 5 gates green; all 41 traceability rows ✓ Validated; final commit lands; v1.13.0 is shipped.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time `package.json` → `__APP_VERSION__` | Configuration string flows through Vite define at compile time; version is intentionally public clinical-provenance signal (no untrusted input) |
| service worker → user browser cache | PWA `registerType: 'prompt'` non-blocking update banner — stale v1.12.5 SW may serve until user accepts update prompt; this is intentional UX behavior, not a security issue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | `__APP_VERSION__` injection in AboutSheet | accept | Build-time constant resolved from `package.json` at config-load (`vite.config.ts:11`); no runtime input path. Source-of-truth chain VERIFIED in RESEARCH.md. |
| T-43-02 | Information Disclosure | Version string `1.13.0` in AboutSheet (clinical metadata) | accept | Version is intentionally public (clinical provenance trust signal — clinicians need to know which calculator algorithm + dataset they are using). PROJECT.md Out of Scope confirms anonymous-tool design. |
| T-43-03 | Information Disclosure | Stale service worker serves v1.12.5 to non-updated users | accept | `registerType: 'prompt'` (`vite.config.ts:17`) shows non-blocking update banner; users on stale SW see correct old behavior, not malformed/inconsistent data. D-13 release-notes reminder covers post-deploy verification. |
| T-43-04 | Repudiation | git history of v1.13 release commits | accept | Conventional commit prefixes per Pattern S5 + commit-hash references in PROJECT.md Validated bullets (D-06: `1ce4493`, `8fde90e`, `390aba6`) provide clear audit trail. No identity infrastructure (anonymous tool). |
| T-43-05 | Denial of Service | Failed `pnpm build` blocks deploy | mitigate | Gate 3 in Task 6 enforces `pnpm build` exits 0 before REL-* flips; failure halts the release per D-14 / D-15. |
| T-43-06 | Tampering | Orphan `.continue-here.md` describes obsolete pause-state and could mislead future planners | mitigate | Task 2 deletes both orphan files in a single chore commit referencing the superseding commits (D-09 / D-10 / D-11). |
| T-43-07 | Spoofing | (N/A) | accept | App is anonymous — no user identity to spoof. |
| T-43-08 | Elevation of Privilege | (N/A) | accept | No privilege boundaries; no auth surface; no admin paths. |

**Disposition rationale:** Phase 43 ships no new code paths and no new attack surface. Same threat model as Phase 39 (v1.12 release). Two threats (T-43-05, T-43-06) get explicit mitigations because they are concrete planner-controlled risks; the other six are accept-by-design given the application's anonymous + clinical-provenance posture.
</threat_model>

<verification>
After all 6 tasks complete, the planner-level success conditions are:

1. `package.json:4` reads `"version": "1.13.0"` (REL-01)
2. AboutSheet auto-renders `1.13.0` (REL-01) — verified by build pipeline + visual confirmation in dev server (executor may run `pnpm dev` and check the AboutSheet, optional)
3. PROJECT.md Validated section contains all required v1.13 bullets per D-02 / D-03 / D-05 (REL-02)
4. PROJECT.md Current State + Current Milestone (Shipped) reflect v1.13.0 (REL-02 / D-04)
5. REQUIREMENTS.md traceability table has 41 ✓ Validated rows + 0 Pending v1.13 rows (REL-02)
6. ROADMAP.md Phase 43 row marked Complete (REL-02)
7. Two orphan files deleted (D-09 / D-10 / D-11)
8. 40-VERIFICATION.md + 41-VERIFICATION.md have appended Phase 43 Triage blocks (D-07 / D-08)
9. `pnpm check` 0/0 (REL-03)
10. `pnpm test:run` exit 0 (REL-03)
11. `pnpm build` exit 0 (REL-03)
12. `pnpm exec playwright test` exit 0 — all 16 specs pass including the 2 fixed pre-bump (REL-03 / D-15)
13. Axe sweeps green in both themes (REL-03)

A meta-check: `git log --oneline | head -10` should show, in chronological order, ~5-6 Phase 43 commits with `docs(43):`, `chore(43):`, `fix(43):`, `feat(43-01):` prefixes per Pattern S5.
</verification>

<success_criteria>
v1.13.0 is shipped. AboutSheet shows v1.13.0. PROJECT.md tells the full v1.13 story (5 calculators, favorites nav, design polish, STOP-red carve-out, post-42.1 follow-up + 42.2 sweep). REQUIREMENTS.md traceability is fully closed (0 Pending for v1.13 IDs). ROADMAP shows Phase 43 Complete. No orphan planning artifacts. Verification debt triaged. Two pre-existing Playwright failures fixed (no D-15 deferrals). Full clinical gate green: svelte-check 0/0, vitest fully green, `pnpm build` ✓, Playwright E2E + extended axe suite green in both themes.
</success_criteria>

<output>
After this plan completes, the orchestrator creates `.planning/phases/43-release-v1-13-0/43-01-SUMMARY.md` per the Phase 39 template (`.planning/milestones/v1.12-phases/39-release-v1-12/39-01-SUMMARY.md`). The SUMMARY:

- Frontmatter: `tags: [release, version-bump, documentation, gates, orphan-cleanup, traceability-flip, verification-triage, pre-bump-e2e-fix]`; `dependency_graph.requires: [42-03, 42.1-06]`; `dependency_graph.provides: [v1.13.0-release]`; `decisions:` lists D-02/D-03/D-05 hybrid granularity, D-15 no-deferrals enforcement, STOP-red carve-out reference, and the 2 auto-fixed Playwright failures.
- Body: per-task results table + Auto-fixed Issues blocks (Tasks 3 + 4) + Gate Results table + Self-Check.
- Release Notes Reminder section per D-13: "Verify drawer + keyboard + STOP-red contrast on a real iPhone in standalone PWA mode post-deploy. Non-blocking; v1.13.1 hotfix fuel if a regression surfaces. v1.14 backlog candidate if iOS-specific test coverage becomes worth automating."

The orchestrator handles SUMMARY creation, STATE.md `last_activity` update, and any `/gsd-progress` follow-up. Milestone archive (move v1.13 phases to `milestones/v1.13-phases/` directory) is `/gsd-complete-milestone`'s job per RESEARCH.md Open Question 4 — explicitly NOT Phase 43's scope.
</output>
