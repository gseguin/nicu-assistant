---
phase: 10-fortification-calculator-ui
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/routes/formula/+page.svelte
  - src/lib/shell/registry.ts
  - src/lib/shared/about-content.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "/formula route renders <FortificationCalculator /> instead of the legacy <FormulaCalculator />"
    - "/formula route initializes fortificationState (not formulaState) on mount"
    - "Calculator registry Formula entry description reflects unified Fortification calculator"
    - "About sheet 'formula' entry copy describes the unified calculator (5 inputs, 4 outputs, 30 formulas across 4 manufacturers)"
    - "src/lib/formula/ remains untouched (Phase 11 owns deletion)"
  artifacts:
    - path: "src/routes/formula/+page.svelte"
      provides: "Formula route mounting FortificationCalculator"
      pattern: "FortificationCalculator"
    - path: "src/lib/shell/registry.ts"
      provides: "Updated Formula entry description"
    - path: "src/lib/shared/about-content.ts"
      provides: "Updated formula About content"
  key_links:
    - from: "src/routes/formula/+page.svelte"
      to: "src/lib/fortification/FortificationCalculator.svelte"
      via: "import + render (pure wiring — no new requirement ownership; UI-01..UI-05 are delivered by Plan 10-01)"
      pattern: "FortificationCalculator"
    - from: "src/routes/formula/+page.svelte"
      to: "src/lib/fortification/state.svelte.ts"
      via: "fortificationState.init() in onMount"
      pattern: "fortificationState.init"
---

<objective>
Wire the new FortificationCalculator into the app shell: swap the /formula route component, update the calculator registry description, and rewrite the About sheet copy. Three small, low-risk diffs that depend on Plan 10-01 having shipped the component. This plan owns ZERO requirement IDs — it is pure wiring. UI-01..UI-05 are fully delivered by Plan 10-01.

Purpose: Make the unified fortification calculator reachable from the nav and accurately described in About.
Output: Three modified files; no new files; no deletions (Phase 11 owns src/lib/formula/ removal).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-fortification-calculator-ui/10-CONTEXT.md
@.planning/phases/10-fortification-calculator-ui/10-RESEARCH.md
@.planning/phases/10-fortification-calculator-ui/10-01-PLAN.md
@src/routes/formula/+page.svelte
@src/lib/shell/registry.ts
@src/lib/shared/about-content.ts
@src/lib/fortification/FortificationCalculator.svelte
@src/lib/fortification/state.svelte.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap /formula route to FortificationCalculator</name>
  <files>src/routes/formula/+page.svelte</files>
  <action>
    Edit src/routes/formula/+page.svelte:
    1. Replace `import { formulaState } from '$lib/formula/state.svelte.js';` with `import { fortificationState } from '$lib/fortification/state.svelte.js';`.
    2. Replace `import FormulaCalculator from '$lib/formula/FormulaCalculator.svelte';` with `import FortificationCalculator from '$lib/fortification/FortificationCalculator.svelte';`.
    3. In `onMount`, replace `formulaState.init();` with `fortificationState.init();`.
    4. Replace `<FormulaCalculator />` with `<FortificationCalculator />`.
    5. Keep `setCalculatorContext({ id: 'formula', accentColor: 'var(--color-accent)' })` exactly as-is — `CalculatorId` still has 'formula'.
    6. Keep the `<svelte:head><title>Formula Recipe | NICU Assistant</title></svelte:head>` exactly as-is (avoid scope creep).
    7. Keep the `<Milk size={28}>` icon and the `<h1>Formula Recipe</h1>` heading unchanged (minimize diff).
    8. Container classes (`max-w-lg md:max-w-4xl mx-auto px-4 py-6 space-y-4`) unchanged.
    9. Do NOT delete anything from src/lib/formula/ (Phase 11). Do NOT modify the legacy formulaState file.
    10. Do NOT touch the morphine route, registry entries beyond Task 2, or any other route.
  </action>
  <verify>
    <automated>npm run check 2>&1 | grep -E "src/routes/formula/\+page" || echo "no route tsc errors"</automated>
  </verify>
  <done>
    +page.svelte imports FortificationCalculator from $lib/fortification/, calls fortificationState.init() in onMount, and renders <FortificationCalculator />. No legacy formula imports remain in this file. tsc clean for the route file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update calculator registry Formula entry description</name>
  <files>src/lib/shell/registry.ts</files>
  <action>
    Edit src/lib/shell/registry.ts:
    1. Locate the Formula entry (id: 'formula').
    2. Update the `description` field from `'Infant formula recipe calculator'` to `'Infant formula fortification calculator'`.
    3. Keep `id`, `label` ('Formula' — short fits nav), `href`, and `icon` (Milk) exactly as-is.
    4. Do NOT modify the morphine-wean entry. Do NOT add new entries. Do NOT remove entries.
    5. Do NOT change the CalculatorEntry interface.
  </action>
  <verify>
    <automated>npm run check 2>&1 | grep -E "shell/registry" || echo "no registry tsc errors"</automated>
  </verify>
  <done>
    Formula entry description reads "Infant formula fortification calculator". All other registry contents unchanged. tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite About sheet 'formula' content</name>
  <files>src/lib/shared/about-content.ts</files>
  <action>
    Edit src/lib/shared/about-content.ts:
    1. Locate the `formula:` key inside `aboutContent`.
    2. Update `title` to `'Fortification Calculator'`.
    3. Replace `description` with: `'Calculates the amount of formula powder, scoops, teaspoons, tablespoons, or HMF packets needed to fortify breast milk or water to a target calorie concentration. Implements displacement-corrected math for spreadsheet parity.'`
    4. Replace `notes` array with these three bullets (keep array length at 3 to match the morphine entry shape):
       - `'Five inputs (Base, Starting Volume, Formula, Target Calorie, Unit) and four outputs (Amount to Add, Yield, Exact kcal/oz, Suggested Starting Volume).'`
       - `'30 formulas across 4 manufacturers (Abbott, Mead Johnson, Nestlé, Nutricia). Packets unit is only available for Similac HMF.'`
       - `"Results must be verified against your institution's feeding protocol."`
    5. Keep `version: appVersion` unchanged.
    6. Do NOT modify the `morphine-wean` entry. Do NOT change the AboutContent interface or the CalculatorId import.
    7. No emojis. UTF-8 Nestlé character is fine (file already supports it).
  </action>
  <verify>
    <automated>npm run check 2>&1 | grep -E "shared/about-content" || echo "no about tsc errors"</automated>
  </verify>
  <done>
    aboutContent.formula has updated title, description, and 3 notes describing the unified fortification calculator. Morphine entry untouched. tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → component | /formula route mounts FortificationCalculator and triggers state init from sessionStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | Route swap | accept | Pure import/render swap; no new attack surface. Legacy FormulaCalculator file remains on disk but is no longer reachable from any route. Phase 11 deletes it. |
| T-10-08 | Spoofing | About sheet copy | accept | Static informational text; no user input flows through About content. |

**Plan 10-02 introduces zero new attack surface — pure wiring updates with no behavioral logic.**
</threat_model>

<verification>
- `npm run check` — no new tsc errors in the 3 modified files
- `npx vitest run` — full suite green (no regressions; legacy formula tests in src/lib/formula/ continue to pass since that directory is untouched)
- `npm run dev` smoke check (manual, optional): visit /formula → calculator renders, defaults populate (BM/180/neocate-infant/24/teaspoons), Amount to Add shows the expected `2 Teaspoons`. Toggle theme via NavShell → both modes render. Open About sheet → new copy appears.
- Verify legacy `src/lib/formula/` tree is untouched: `git status src/lib/formula/` shows clean
</verification>

<success_criteria>
- /formula route renders FortificationCalculator (reachability wiring for Plan 10-01's UI-01..UI-05)
- Registry description reflects new calculator
- About sheet describes unified calculator with accurate input/output counts and manufacturer count
- src/lib/formula/ untouched (Phase 11 boundary preserved)
- No regressions in `npx vitest run`
</success_criteria>

<output>
After completion, create `.planning/phases/10-fortification-calculator-ui/10-02-SUMMARY.md` documenting:
- Three files modified with one-line diff summary each
- Confirmation that src/lib/formula/ remains untouched
- Confirmation that no morphine-wean registry/about content was modified
- Final status of `npx vitest run` (pass count)
- Final status of `npm run check` (any pre-existing NavShell errors noted as out of scope per CONTEXT)
</output>
