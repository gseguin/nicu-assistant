---
phase: 02-shared-components
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/shared/components/DisclaimerModal.svelte
  - src/routes/+layout.svelte
  - src/lib/shared/index.ts
autonomous: true
requirements: [SC-02, SC-06]

must_haves:
  truths:
    - "DisclaimerModal is visible on first visit (no localStorage entry)"
    - "DisclaimerModal cannot be dismissed via Escape key or backdrop click"
    - "Clicking 'I Understand — Continue' hides the modal and persists acceptance to localStorage key nicu_assistant_disclaimer_v1"
    - "On subsequent visits (disclaimer already accepted), the modal does not appear"
    - "DisclaimerModal covers combined PERT dosing and formula calculation disclaimer text"
    - "All colors use var(--color-*) tokens — modal renders correctly in dark and light themes"
    - "disclaimer.init() is called inside onMount in +layout.svelte"
  artifacts:
    - path: "src/lib/shared/components/DisclaimerModal.svelte"
      provides: "bits-ui Dialog disclaimer with non-dismissable enforcement"
      contains: "escapeKeydownBehavior"
    - path: "src/routes/+layout.svelte"
      provides: "DisclaimerModal mounted in root layout with disclaimer.init() in onMount"
      contains: "disclaimer.init"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "src/lib/shared/disclaimer.svelte.ts"
      via: "disclaimer.init() in onMount"
      pattern: "disclaimer.init"
    - from: "src/lib/shared/components/DisclaimerModal.svelte"
      to: "src/lib/shared/disclaimer.svelte.ts"
      via: "disclaimer.acknowledged (open state) and disclaimer.acknowledge()"
      pattern: "disclaimer.acknowledge"
    - from: "Dialog.Content"
      to: "bits-ui Dialog"
      via: "escapeKeydownBehavior='ignore' interactOutsideBehavior='ignore'"
      pattern: "escapeKeydownBehavior"
---

<objective>
Build DisclaimerModal.svelte using bits-ui Dialog with non-dismissable enforcement (Escape and backdrop click disabled), then wire it into +layout.svelte alongside disclaimer.init() in onMount.

Purpose: NICU clinicians must acknowledge the disclaimer on first launch. The modal must be impossible to bypass accidentally. The disclaimer text covers both PERT dosing and formula calculations combined (per D-03).
Output: DisclaimerModal.svelte that opens when disclaimer.acknowledged is false and closes only via explicit button click. +layout.svelte updated to init and mount the modal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-components/02-CONTEXT.md
@.planning/phases/02-shared-components/02-RESEARCH.md

@src/lib/shared/disclaimer.svelte.ts
@src/lib/shared/types.ts
@src/routes/+layout.svelte
@src/app.css

<!-- Reference implementations (behavior source of truth) -->
@/mnt/data/src/pert-calculator/src/lib/components/DisclaimerModal.svelte
@/mnt/data/src/formula-calculator/src/lib/components/DisclaimerModal.svelte
</context>

<interfaces>
<!-- Types from Plan 01 that this plan builds against -->

From src/lib/shared/disclaimer.svelte.ts:
```typescript
export const disclaimer = {
  get acknowledged(): boolean;
  init(): void;       // call inside onMount only — reads localStorage
  acknowledge(): void; // sets acknowledged true + writes to localStorage
};
```

bits-ui Dialog API (2.16.5):
- Dialog.Root open={boolean} onOpenChange={fn} — controlled open state
- Dialog.Portal — renders overlay + content outside component tree
- Dialog.Overlay — backdrop element
- Dialog.Content escapeKeydownBehavior="ignore" interactOutsideBehavior="ignore"
- Dialog.Title id="disclaimer-title"
- Dialog.Description id="disclaimer-body"
- NO Dialog.Close — replaced by explicit acknowledge button (per SC-02 requirement)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create DisclaimerModal.svelte with non-dismissable bits-ui Dialog</name>
  <files>src/lib/shared/components/DisclaimerModal.svelte</files>
  <read_first>
    - src/lib/shared/disclaimer.svelte.ts — singleton API (acknowledged, init, acknowledge)
    - src/app.css — semantic token aliases (surface-card, text-primary, text-secondary, accent)
    - .planning/phases/02-shared-components/02-RESEARCH.md lines 246-312 (Pattern 2: bits-ui Dialog anatomy + disclaimer singleton)
    - .planning/phases/02-shared-components/02-RESEARCH.md lines 497-508 (Pitfall 6 — mount always, use open= not {#if})
    - .planning/phases/02-shared-components/02-CONTEXT.md decisions D-03, D-04, D-05
    - /mnt/data/src/pert-calculator/src/lib/components/DisclaimerModal.svelte — content reference
    - /mnt/data/src/formula-calculator/src/lib/components/DisclaimerModal.svelte — content reference
  </read_first>
  <action>
    Create src/lib/shared/components/DisclaimerModal.svelte.

    Script block imports:
    ```typescript
    import { Dialog } from 'bits-ui';
    import { disclaimer } from '$lib/shared/disclaimer.svelte.js';
    ```

    No props — the component reads disclaimer singleton directly.

    Template structure:
    ```
    Dialog.Root open={!disclaimer.acknowledged} onOpenChange={() => {}}
      Dialog.Portal
        Dialog.Overlay — fixed inset-0 z-50, dark backdrop with blur
        Dialog.Content
          escapeKeydownBehavior="ignore"
          interactOutsideBehavior="ignore"
          aria-labelledby="disclaimer-title"
          aria-describedby="disclaimer-body"
          — positioned as bottom sheet on mobile, centered modal on sm+ breakpoints
          — uses safe-area-inset-bottom for iOS home indicator clearance

          Dialog.Title id="disclaimer-title"
          Dialog.Description id="disclaimer-body"
          — disclaimer text (see below)

          Acknowledge button (NOT Dialog.Close)
            onclick={() => disclaimer.acknowledge()}
            min-h-[3.25rem] (52px — exceeds 48px minimum touch target)
            w-full, rounded-[0.625rem]
            background: var(--color-accent)
            text: white, font-semibold
            Text: "I Understand — Continue"
    ```

    Positioning CSS for the content panel:
    ```
    fixed left-1/2 -translate-x-1/2
    bottom-0 sm:bottom-auto sm:top-1/2 sm:-translate-y-1/2
    w-[min(32rem,calc(100vw-1rem))]
    rounded-t-2xl sm:rounded-2xl
    bg-[var(--color-surface-card)]
    px-6 pt-6 pb-[calc(1.5rem+env(safe-area-inset-bottom,0px))] sm:pb-6
    shadow-2xl z-50
    ```

    Combined disclaimer text (D-03 — Claude drafts covering both PERT dosing and formula calculations):
    Use this exact draft text:
    ```
    Title: "Clinical Use Disclaimer"

    Body paragraph 1:
    This app provides dosing and recipe calculations as decision support tools for licensed clinical
    professionals. Results are intended to assist — not replace — clinical judgment. All outputs must
    be reviewed by qualified NICU staff before administration.

    Body paragraph 2:
    PERT dosing calculations are based on current enzyme activity data and FDA-approved brands. Formula
    preparation calculations use manufacturer-provided caloric density and displacement values. Data
    is embedded at build time and may not reflect the latest formulary updates.

    Body paragraph 3:
    Do not use this app as the sole basis for patient care decisions. If you are not a licensed
    clinician, please close this application.
    ```

    Styling rules:
    - ALL colors via var(--color-*) tokens — no hardcoded oklch(), no 'dark:' classes on colors
    - The modal overlay: bg-slate-900/65 backdrop-blur-sm (uses Tailwind opacity modifier — acceptable since slate-900 is a Tailwind named color, not a custom oklch)
    - Title: text-xl font-semibold text-[var(--color-text-primary)]
    - Body paragraphs: text-base leading-6 text-[var(--color-text-secondary)] space-y-3
    - Button background: bg-[var(--color-accent)] — NOT hardcoded Clinical Blue oklch value

    IMPORTANT: The component is always mounted in +layout.svelte (Plan 02-01 noted Pitfall 6).
    The Dialog.Root open={!disclaimer.acknowledged} controls visibility reactively.
    Do NOT conditionally render with {#if} — always mount, use open prop.
    Do NOT include Dialog.Close anywhere — the only way to close is the acknowledge button.

    After creating the file, run: pnpm build to verify no Svelte/TS errors.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "escapeKeydownBehavior" src/lib/shared/components/DisclaimerModal.svelte → matches
    - grep "interactOutsideBehavior" src/lib/shared/components/DisclaimerModal.svelte → matches
    - grep "disclaimer.acknowledge" src/lib/shared/components/DisclaimerModal.svelte → matches
    - grep "I Understand" src/lib/shared/components/DisclaimerModal.svelte → matches
    - grep "Dialog.Close" src/lib/shared/components/DisclaimerModal.svelte → NO matches
    - grep -v "oklch(" src/lib/shared/components/DisclaimerModal.svelte → no hardcoded oklch
    - pnpm build exits 0
  </acceptance_criteria>
  <done>
    DisclaimerModal.svelte exists with bits-ui Dialog, Escape and backdrop click disabled, acknowledge button wired to disclaimer.acknowledge(), combined PERT+formula disclaimer text included, all colors via CSS tokens, build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire DisclaimerModal into +layout.svelte and call disclaimer.init() in onMount</name>
  <files>
    src/routes/+layout.svelte
    src/lib/shared/index.ts
  </files>
  <read_first>
    - src/routes/+layout.svelte — current state (theme.init() already in onMount, NavShell already imported)
    - src/lib/shared/index.ts — current state (DisclaimerModal export is commented out from Plan 01)
    - .planning/phases/02-shared-components/02-RESEARCH.md lines 469-481 (Pitfall 4 — onMount init requirement)
    - .planning/phases/02-shared-components/02-CONTEXT.md code_context — integration points section
  </read_first>
  <action>
    **Step 1: Update src/routes/+layout.svelte**

    Add two imports:
    ```typescript
    import DisclaimerModal from '$lib/shared/components/DisclaimerModal.svelte';
    import { disclaimer } from '$lib/shared/disclaimer.svelte.js';
    ```

    Add disclaimer.init() inside the existing onMount callback — alongside theme.init():
    ```typescript
    onMount(() => {
      theme.init();
      disclaimer.init();   // add this line
    });
    ```

    Add DisclaimerModal to the template — place it BEFORE the NavShell/main content div so it renders above everything:
    ```svelte
    <DisclaimerModal />
    <div class="min-h-screen flex flex-col">
      <NavShell />
      <main class="flex-1 pb-20 md:pb-0">
        {@render children()}
      </main>
    </div>
    ```

    **Step 2: Update src/lib/shared/index.ts**

    Uncomment (or add) the DisclaimerModal export:
    ```typescript
    export { default as DisclaimerModal } from './components/DisclaimerModal.svelte';
    ```

    **Step 3: Verify**
    Run: pnpm build — must pass with 0 exit code.
    Run: pnpm check — fix any type errors (ignore svelte-check $app/* module resolution issue noted in Phase 1 SUMMARY).
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "disclaimer.init" src/routes/+layout.svelte → matches
    - grep "DisclaimerModal" src/routes/+layout.svelte → matches (import + usage)
    - grep "DisclaimerModal" src/lib/shared/index.ts → non-commented export line matches
    - pnpm build exits 0
  </acceptance_criteria>
  <done>
    +layout.svelte imports and mounts DisclaimerModal.
    disclaimer.init() is called inside onMount alongside theme.init().
    DisclaimerModal is exported from the shared index barrel.
    Build passes.
  </done>
</task>

</tasks>

<verification>
- src/lib/shared/components/DisclaimerModal.svelte exists
- grep "escapeKeydownBehavior" returns a match in DisclaimerModal.svelte
- grep "disclaimer.init" src/routes/+layout.svelte returns a match
- grep "DisclaimerModal" src/routes/+layout.svelte returns import + usage
- pnpm build passes (0 exit code)
</verification>

<success_criteria>
DisclaimerModal.svelte built with bits-ui Dialog, non-dismissable via Escape/backdrop, combined PERT+formula disclaimer text, acknowledges via explicit button that writes nicu_assistant_disclaimer_v1 to localStorage. Mounted in +layout.svelte with disclaimer.init() in onMount. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-components/02-03-SUMMARY.md`
</output>
