---
phase: 02-shared-components
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/shared/components/NumericInput.svelte
  - src/lib/shared/components/ResultsDisplay.svelte
  - src/lib/shared/index.ts
autonomous: true
requirements: [SC-03, SC-04, SC-06]

must_haves:
  truths:
    - "NumericInput renders with inputmode='decimal' for mobile decimal keyboard"
    - "NumericInput mouse wheel increments/decrements value when field is focused"
    - "NumericInput clamps to min/max on blur, not on every keystroke"
    - "NumericInput shows inline error message (aria-describedby) when error prop is non-empty"
    - "NumericInput value is null when field is empty (not 0)"
    - "NumericInput hides browser number spinner arrows via CSS"
    - "ResultsDisplay announces value changes to screen readers via aria-live='polite' aria-atomic='true'"
    - "ResultsDisplay pulse animation re-runs on primaryValue change via {#key pulseTrigger}"
    - "ResultsDisplay supports optional secondaryValue/secondaryUnit/secondaryLabel for two-card layout"
    - "All color values in both components use var(--color-*) tokens — no hardcoded oklch() or named color classes from formula-calculator"
  artifacts:
    - path: "src/lib/shared/components/NumericInput.svelte"
      provides: "Decimal clinical input with wheel scroll, min/max validation, inline error"
      contains: "inputmode=\"decimal\""
    - path: "src/lib/shared/components/ResultsDisplay.svelte"
      provides: "Clinical result cards with aria-live and pulse animation"
      contains: "aria-live=\"polite\""
  key_links:
    - from: "NumericInput setupWheel action"
      to: "WheelEvent handler"
      via: "addEventListener('wheel', handler, { passive: false })"
      pattern: "passive: false"
    - from: "ResultsDisplay"
      to: "aria-live region"
      via: "aria-live='polite' aria-atomic='true' on wrapper div"
      pattern: "aria-live"
    - from: "ResultsDisplay pulse"
      to: "pulseTrigger $state"
      via: "{#key pulseTrigger} re-runs CSS animation"
      pattern: "pulseTrigger"
---

<objective>
Port NumericInput and ResultsDisplay from formula-calculator with token migration (replacing hardcoded colors with CSS custom property references), plus a fix for the SSR-unsafe Math.random() id default and a generalized ResultsDisplay prop API for both PERT and formula use cases.

Purpose: Both calculators need these components. They are well-tested in production in the formula-calculator — the job is careful port with token substitution, not reimplementation.
Output: NumericInput.svelte and ResultsDisplay.svelte in src/lib/shared/components/ using only var(--color-*) tokens, with Vitest unit tests for NumericInput validation behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-components/02-CONTEXT.md
@.planning/phases/02-shared-components/02-RESEARCH.md

@src/lib/shared/types.ts
@src/lib/shared/context.ts
@src/app.css

<!-- Reference implementations (port from here) -->
@/mnt/data/src/formula-calculator/src/lib/components/NumericInput.svelte
@/mnt/data/src/formula-calculator/src/lib/components/ResultsDisplay.svelte
</context>

<interfaces>
<!-- Token mapping from RESEARCH.md Pattern 3 — apply to BOTH components -->
Token migration table (formula hardcode → unified token):
| Formula hardcode          | Unified token (use this)                    |
|---------------------------|---------------------------------------------|
| bg-white                  | bg-[var(--color-surface-card)]              |
| border-slate-200          | border-[var(--color-border)]                |
| focus:border-clinical-500 | focus:border-[var(--color-accent)]          |
| focus:ring-clinical-500/20| focus:ring-[var(--color-accent-light)]      |
| text-slate-900            | text-[var(--color-text-primary)]            |
| text-slate-600            | text-[var(--color-text-secondary)]          |
| text-red-600              | text-[var(--color-error)]                   |
| border-red-500            | border-[var(--color-error)]                 |
| ring-red-500              | ring-[var(--color-error)]                   |
| bg-clinical-600           | bg-[var(--color-accent)] (PERT)             |
| bg-bmf-600                | use accentColor from getCalculatorContext() |

NumericInput deterministic id (replaces Math.random()):
```typescript
let idCounter = 0;
let { id = `numeric-input-${++idCounter}` } = $props();
```

ResultsDisplay unified prop API (from RESEARCH.md Pattern 4):
```typescript
{
  primaryValue: string;
  primaryUnit: string;
  primaryLabel: string;
  secondaryValue?: string;
  secondaryUnit?: string;
  secondaryLabel?: string;
  isVisible?: boolean;
  accentVariant?: 'clinical' | 'bmf';  // overrides context if provided
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port NumericInput.svelte from formula-calculator with token migration and id fix</name>
  <files>
    src/lib/shared/components/NumericInput.svelte
    src/lib/shared/components/NumericInput.test.ts
  </files>
  <read_first>
    - /mnt/data/src/formula-calculator/src/lib/components/NumericInput.svelte — full source (port basis)
    - src/app.css lines 34-55 — available semantic token aliases
    - .planning/phases/02-shared-components/02-RESEARCH.md lines 314-338 (Pattern 3: NumericInput port)
    - .planning/phases/02-shared-components/02-RESEARCH.md lines 483-495 (Pitfall 5 — Math.random() id fix)
  </read_first>
  <behavior>
    Write tests in NumericInput.test.ts FIRST, then implement:
    - Rendering test: value=null → input value is empty string (not "0")
    - Rendering test: value=5.5 → input displays "5.5"
    - Error prop test: error="Required" → error paragraph visible, aria-invalid=true, aria-describedby links to error id
    - No-error test: error="" → no error paragraph, aria-invalid=false
    - Blur clamp test: value=150, max=100 → after blur, value becomes 100
    - Blur clamp test: value=-5, min=0 → after blur, value becomes 0
    - Null preservation test: empty input → value === null (not 0)
  </behavior>
  <action>
    Write NumericInput.test.ts with Vitest + @testing-library/svelte covering the behaviors above (RED phase).
    Run tests: pnpm test:run — all must FAIL at this point (confirms tests are real).

    Then create src/lib/shared/components/NumericInput.svelte by porting formula-calculator's NumericInput with these specific changes:

    1. **Fix SSR-unsafe id default** (Pitfall 5):
       Replace: `id = Math.random().toString(36).substring(2, 9)`
       With: use a module-level counter — `let idCounter = 0;` before $props(), then `id = \`numeric-input-\${++idCounter}\``

    2. **Token migration** — replace ALL hardcoded color values using the mapping table above:
       - bg-white → bg-[var(--color-surface-card)]
       - border-slate-200 → border-[var(--color-border)]
       - focus:border-clinical-500 → focus:border-[var(--color-accent)]
       - focus:ring-clinical-500/20 → focus:ring-[var(--color-accent-light)]
       - text-slate-900 → text-[var(--color-text-primary)]
       - text-slate-600 → text-[var(--color-text-secondary)]
       - text-red-600 → text-[var(--color-error)]
       - border-red-500 → border-[var(--color-error)]
       - ring-red-500 → ring-[var(--color-error)]
       - text-ui class should remain (defined in app.css @theme)

    3. **Icon import fix**: if formula NumericInput imports from lucide-svelte, change to @lucide/svelte.

    4. **Preserve EXACTLY**:
       - inputmode="decimal" on the input element
       - setupWheel Svelte action with { passive: false } — required to call preventDefault()
       - Clamp logic in handleBlur (not handleInput)
       - null value when field is empty
       - CSS that hides spinner arrows (the <style> block)
       - aria-invalid, aria-describedby, aria-label attributes
       - slide transition on error paragraph

    5. **Do NOT change**: the wheel logic, the blur clamp logic, the null/parseFloat handling. These behaviors are clinically validated in the reference implementation.

    Run pnpm test:run — all tests must now PASS (GREEN phase).
    Run pnpm build — must exit 0.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm test:run --reporter=verbose 2>&1 | grep -E "PASS|FAIL|✓|✗"</automated>
  </verify>
  <acceptance_criteria>
    - grep "inputmode=\"decimal\"" src/lib/shared/components/NumericInput.svelte → matches
    - grep "passive: false" src/lib/shared/components/NumericInput.svelte → matches
    - grep "idCounter" src/lib/shared/components/NumericInput.svelte → matches (SSR fix)
    - grep "Math.random" src/lib/shared/components/NumericInput.svelte → NO matches
    - grep "oklch\|slate-\|clinical-500\|red-600\|bg-white" src/lib/shared/components/NumericInput.svelte → NO matches (all tokens migrated)
    - pnpm test:run exits 0 (all tests pass)
    - pnpm build exits 0
  </acceptance_criteria>
  <done>
    NumericInput.svelte exists with inputmode="decimal", non-passive wheel action, null-on-empty, min/max blur clamp, inline error display, all token colors migrated. Vitest tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port ResultsDisplay.svelte with unified prop API and token migration</name>
  <files>
    src/lib/shared/components/ResultsDisplay.svelte
    src/lib/shared/index.ts
  </files>
  <read_first>
    - /mnt/data/src/formula-calculator/src/lib/components/ResultsDisplay.svelte — full source (port basis)
    - src/lib/shared/context.ts — getCalculatorContext() for accentColor
    - src/app.css — semantic tokens
    - .planning/phases/02-shared-components/02-RESEARCH.md lines 341-377 (Pattern 4: ResultsDisplay port, unified API, ARIA pattern)
  </read_first>
  <action>
    Create src/lib/shared/components/ResultsDisplay.svelte by porting formula-calculator's ResultsDisplay.svelte with these changes:

    1. **Generalize prop API** (from RESEARCH.md Pattern 4):
       Replace the formula-specific props { waterMl, powderG, isVisible, liquidLabel, mode } with:
       ```typescript
       let {
         primaryValue,
         primaryUnit,
         primaryLabel,
         secondaryValue,
         secondaryUnit,
         secondaryLabel,
         isVisible = true,
         accentVariant,
       }: {
         primaryValue: string;
         primaryUnit: string;
         primaryLabel: string;
         secondaryValue?: string;
         secondaryUnit?: string;
         secondaryLabel?: string;
         isVisible?: boolean;
         accentVariant?: 'clinical' | 'bmf';
       } = $props();
       ```

    2. **Accent color logic**:
       If accentVariant prop is provided, use it directly.
       Otherwise, read from getCalculatorContext() for the accent color.
       Card background: when variant is 'bmf' → 'bg-[var(--color-bmf-600)]', else → 'bg-[var(--color-accent)]'
       Do NOT hardcode oklch() values for card backgrounds.

    3. **Token migration**:
       - Replace any remaining hardcoded color classes (bg-clinical-600, bg-bmf-600, border-clinical-700, border-bmf-700) with var(--color-*) references as above.
       - Text on the card (white text for contrast) can stay as 'text-white' — Tailwind named white is acceptable since it is theme-independent.

    4. **ARIA requirement** (SC-04, SC-06 — mandatory):
       The outer wrapper div MUST have:
       ```
       aria-live="polite"
       aria-atomic="true"
       aria-label="Result: {primaryValue} {primaryUnit}"
       ```

    5. **Preserve the {#key pulseTrigger} pattern**:
       Keep the $state pulseTrigger counter and $effect that increments it when primaryValue changes.
       The {#key pulseTrigger} block must wrap the value display so CSS animation re-triggers on each update.
       Replace the formula $effect pattern (which tracked waterMl/powderG) with tracking primaryValue:
       ```typescript
       $effect(() => {
         primaryValue;
         untrack(() => { pulseTrigger += 1; });
       });
       ```

    6. **Two-card layout** (optional second card when secondaryValue is provided):
       If secondaryValue is defined, render a second result card below the primary card.
       Second card uses a slightly less prominent styling (bg-[var(--color-surface-card)] with border rather than full accent background).

    7. **Icon imports**: Replace lucide-svelte with @lucide/svelte for any icon imports.

    8. **Update src/lib/shared/index.ts**:
       Uncomment both NumericInput and ResultsDisplay exports:
       ```typescript
       export { default as NumericInput } from './components/NumericInput.svelte';
       export { default as ResultsDisplay } from './components/ResultsDisplay.svelte';
       ```

    Run: pnpm build — must exit 0.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "aria-live=\"polite\"" src/lib/shared/components/ResultsDisplay.svelte → matches
    - grep "aria-atomic=\"true\"" src/lib/shared/components/ResultsDisplay.svelte → matches
    - grep "pulseTrigger" src/lib/shared/components/ResultsDisplay.svelte → at least 2 matches (state + key block)
    - grep "primaryValue" src/lib/shared/components/ResultsDisplay.svelte → matches in props and template
    - grep "oklch\|lucide-svelte\|bg-clinical-600\|bg-bmf-600" src/lib/shared/components/ResultsDisplay.svelte → NO matches
    - grep "NumericInput\|ResultsDisplay" src/lib/shared/index.ts → both present as uncommented exports
    - pnpm build exits 0
  </acceptance_criteria>
  <done>
    ResultsDisplay.svelte exists with unified prop API, aria-live="polite" aria-atomic="true", pulse animation via {#key pulseTrigger}, no hardcoded colors, optional second card. Both components exported from the shared index. Build passes.
  </done>
</task>

</tasks>

<verification>
- src/lib/shared/components/NumericInput.svelte exists with inputmode="decimal" and non-passive wheel
- src/lib/shared/components/ResultsDisplay.svelte exists with aria-live="polite" aria-atomic="true"
- No hardcoded oklch() or formula-specific color classes in either component
- pnpm test:run passes (NumericInput unit tests)
- pnpm build passes
</verification>

<success_criteria>
NumericInput and ResultsDisplay ported from formula-calculator with all hardcoded colors replaced by var(--color-*) tokens, SSR-safe id generation, unified ResultsDisplay prop API, mandatory ARIA live region, and pulse animation preserved. Both exported from shared index. Vitest unit tests for NumericInput pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-components/02-04-SUMMARY.md`
</output>
