---
phase: 13-fortification-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/fortification/FortificationCalculator.svelte
autonomous: true
requirements: [FORT-01, FORT-02, FORT-03]
must_haves:
  truths:
    - "On mobile, Target Calorie and Unit pickers sit on the same row without overflow"
    - "The Amount to Add hero card adopts morphine wean summary card visual treatment (accent-light background, bold num, tabular hierarchy)"
    - "All color values still use OKLCH CSS tokens via Tailwind bracket notation"
    - "FortificationCalculator.test.ts passes unchanged"
    - "Fortification calculations remain identical (no logic files edited)"
  artifacts:
    - path: "src/lib/fortification/FortificationCalculator.svelte"
      provides: "Polished Fortification calculator UI"
      contains: "grid-cols-2"
  key_links:
    - from: "FortificationCalculator.svelte hero <section>"
      to: "morphine wean summary card styling"
      via: "bg-[var(--color-accent-light)] + tabular bold numeric hierarchy"
      pattern: "bg-\\[var\\(--color-accent-light\\)\\]"
---

<objective>
Polish the Fortification calculator: (1) pair Target Calorie + Unit pickers on a single mobile row, (2) restyle the "Amount to Add" hero to match morphine wean result card visual treatment, (3) sweep spacing/typography/empty-state to match `.impeccable.md`. Visual/markup only — no logic, state, or config changes.

Purpose: Close FORT-01/02/03. Bring Fortification in line with the rest of the v1.4 polish milestone.
Output: A refined `FortificationCalculator.svelte` that passes all existing tests unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.impeccable.md
@CLAUDE.md
@src/lib/fortification/FortificationCalculator.svelte
@src/lib/fortification/FortificationCalculator.test.ts
@src/lib/morphine/MorphineWeanCalculator.svelte
@src/app.css

<interfaces>
Morphine summary card reference markup (the visual target for the Fortification hero):

```svelte
<div class="card px-4 py-3 flex items-center justify-between bg-[var(--color-accent-light)] animate-summary-pulse">
  <div class="flex flex-col">
    <span class="text-2xs font-medium text-[var(--color-text-secondary)]">Start</span>
    <span class="text-base font-bold num text-[var(--color-text-primary)]">{value}</span>
  </div>
  ...
</div>
```

Key visual ingredients to port to the Fortification hero:
- `bg-[var(--color-accent-light)]` surface tint (brand accent, clinical blue family)
- `num` class for tabular numerics
- `font-bold text-[var(--color-text-primary)]` for result value
- Uppercase tracking-wide secondary label
- `card` base class for surface / radius / padding

Hard test constraints (MUST NOT break — see FortificationCalculator.test.ts):
- The literal text "Amount to Add" must remain, wrapped in a `<section>` (test uses `getByText('Amount to Add').closest('section')`)
- The `<section>` must keep `aria-live="polite" aria-atomic="true"`
- Inside the hero, the numeric value rendered by `formatAmount(result.amountToAdd)` must match `/^\s*2\s*$/` for the default state (so no wrapping whitespace/punctuation changes)
- Inside the hero, the unit label (e.g. "Teaspoons") must remain a distinct text node matchable by `within(hero).getByText('Teaspoons')`
- 4 elements with `[data-select-trigger]` must remain (handled by SelectPicker, do not touch)
- 1 element with `role="spinbutton"` must remain (the NumericInput — do not touch)
- `var(--color-` must still appear in innerHTML AND `text-[var(--color-text-(primary|secondary|tertiary))]` pattern must match
- The Verification dd values "183.5 mL", "23.5 kcal/oz", "180 (6.1 oz)" must remain exactly as produced by the current bindings — do not change the expressions or formatting inside `{result.yieldMl.toFixed(1)} mL` etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile row pairing for Target Calorie + Unit pickers (FORT-01)</name>
  <read_first>
    - src/lib/fortification/FortificationCalculator.svelte (lines 150-175 — Inputs card markup)
    - src/lib/fortification/FortificationCalculator.test.ts (UI-01, UI-04 — the picker label contracts)
    - src/lib/shared/components/SelectPicker.svelte (to confirm trigger min-height ≥ 48px is self-contained)
  </read_first>
  <files>src/lib/fortification/FortificationCalculator.svelte</files>
  <action>
Within the Inputs `<section class="card flex flex-col gap-4">` block, wrap the two trailing SelectPickers ("Target Calorie (kcal/oz)" and "Unit") in a 2-column grid container so they render side-by-side on all viewports (mobile-first). Leave Base, Starting Volume, and Formula as single-column rows above.

Exact markup to replace the current two SelectPicker lines:

```svelte
<div class="grid grid-cols-2 gap-4">
  <SelectPicker
    label="Target Calorie (kcal/oz)"
    bind:value={kcalStr}
    options={kcalOptions}
  />
  <SelectPicker label="Unit" bind:value={unitStr} options={unitOptions} />
</div>
```

Rules:
- Use `grid grid-cols-2 gap-4` — matches the parent's vertical `gap-4` rhythm.
- Do NOT add a breakpoint prefix (mobile-up — stays paired at every width, per phase brief).
- Do NOT change any `label` prop string — UI-01 matches by exact accessible name including "Target Calorie (kcal/oz)" and "Unit".
- Do NOT introduce overflow-hidden, min-w-0 hacks, or modify SelectPicker itself. The SelectPicker label wraps if needed; the kcal label is the longer of the two and must remain intact.
- Do NOT edit any other file.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm vitest run src/lib/fortification/FortificationCalculator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'grid grid-cols-2' src/lib/fortification/FortificationCalculator.svelte` returns at least one match inside the Inputs card
    - `grep -c 'label="Target Calorie (kcal/oz)"' src/lib/fortification/FortificationCalculator.svelte` equals 1
    - `grep -c 'label="Unit"' src/lib/fortification/FortificationCalculator.svelte` equals 1
    - Vitest `UI-01` and `UI-04` pass (4 `[data-select-trigger]`, 1 spinbutton, all 5 labels resolvable)
    - `git diff --stat` shows only `FortificationCalculator.svelte` modified
  </acceptance_criteria>
  <done>Target Calorie and Unit triggers render on one row on a 375px-wide viewport without overflow; all fortification tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Amount-to-Add hero restyle to match morphine result theming (FORT-02)</name>
  <read_first>
    - src/lib/morphine/MorphineWeanCalculator.svelte lines 256-277 (summary card) — the visual reference
    - src/lib/fortification/FortificationCalculator.svelte lines 177-196 (current hero block)
    - src/lib/fortification/FortificationCalculator.test.ts (UI-02 default + UI-02 live recalc — the hero contract)
    - src/app.css — confirm `--color-accent-light` and `--color-text-primary/secondary/tertiary` tokens exist
  </read_first>
  <files>src/lib/fortification/FortificationCalculator.svelte</files>
  <action>
Replace the current hero `<section>` block (lines ~178-196) with a restyled version that adopts the morphine summary card visual language. MUST preserve:
- The outer `<section>` element with `aria-live="polite" aria-atomic="true"`
- The literal text node "Amount to Add" (UI-02 uses `getByText('Amount to Add').closest('section')`)
- The numeric expression `{formatAmount(result.amountToAdd)}` rendered as its own text (regex `/^\s*2\s*$/` must still match — no punctuation, currency, or whitespace wrappers around the number text node)
- The `{unitLabel}` rendered as its own text node matchable by `within(hero).getByText('Teaspoons')` — keep it as a separate `<span>` with only that text content
- At least one class using `text-[var(--color-text-primary)]` or `text-[var(--color-text-secondary)]` or `text-[var(--color-text-tertiary)]` (UI-05 regex)

Exact replacement markup:

```svelte
<!-- Hero: Amount to Add -->
<section
  class="card bg-[var(--color-accent-light)] px-5 py-5"
  aria-live="polite"
  aria-atomic="true"
>
  <span
    class="text-2xs font-semibold uppercase tracking-wide text-[var(--color-text-secondary)]"
  >
    Amount to Add
  </span>
  {#if result}
    <div class="mt-3 flex items-baseline gap-2">
      <span class="text-5xl font-bold num text-[var(--color-text-primary)]"
        >{formatAmount(result.amountToAdd)}</span
      >
      <span
        class="text-lg font-semibold text-[var(--color-text-secondary)]"
        >{unitLabel}</span
      >
    </div>
  {:else}
    <p class="mt-3 text-sm text-[var(--color-text-tertiary)]">
      Enter a starting volume to see the recipe.
    </p>
  {/if}
</section>
```

Rules:
- Use `bg-[var(--color-accent-light)]` — the same brand accent tint used by the morphine summary card.
- Use `num` class for the numeric `<span>` (tabular numerics, matches morphine).
- Use `text-5xl font-bold` for the hero number — this is the "large bold numeral" the design principles and `.impeccable.md` call for. The morphine summary uses `text-base` because it packs three values; this hero shows a single result, so larger is correct.
- DO NOT wrap `{formatAmount(result.amountToAdd)}` in any character that would leak into its text node (no `&nbsp;`, no prefix/suffix string). The regex `/^\s*2\s*$/` tolerates only whitespace.
- DO NOT merge the unit label into the same `<span>` as the number — the test uses `within(hero).getByText('Teaspoons')` which needs an exact text-node match.
- DO NOT add `animate-*` classes in this task (Phase 16 owns animation).
- DO NOT touch the Verification `<section>` below — its dd values "183.5 mL", "23.5 kcal/oz", "180 (6.1 oz)" are asserted byte-for-byte.
- DO NOT import anything new.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm vitest run src/lib/fortification/FortificationCalculator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'bg-\[var(--color-accent-light)\]' src/lib/fortification/FortificationCalculator.svelte` matches on the hero section line
    - `grep -n 'Amount to Add' src/lib/fortification/FortificationCalculator.svelte` still returns exactly 1 line
    - `grep -n 'aria-live="polite"' src/lib/fortification/FortificationCalculator.svelte` still present on the hero section
    - `grep -n 'formatAmount(result.amountToAdd)' src/lib/fortification/FortificationCalculator.svelte` returns exactly 1 match, wrapped in a `<span>` containing `num`
    - `grep -c 'text-\[var(--color-text-' src/lib/fortification/FortificationCalculator.svelte` is ≥ 3
    - Vitest `UI-02: renders default outputs (Neocate parity case)` passes
    - Vitest `UI-02: live recalc when volumeMl mutates after render` passes
    - Vitest `UI-05: uses OKLCH design tokens, not hardcoded colors` passes
  </acceptance_criteria>
  <done>Hero card has the brand-accent tinted surface, a dominant bold tabular numeric, and a secondary unit label — matching morphine wean result theming — while every fortification test still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Spacing, typography, and empty-state sweep (FORT-03)</name>
  <read_first>
    - src/lib/fortification/FortificationCalculator.svelte (whole file, post-Task 2)
    - src/lib/morphine/MorphineWeanCalculator.svelte lines 307-313 (dashed empty state reference)
    - .impeccable.md
    - src/lib/fortification/FortificationCalculator.test.ts (UI-02 Verification assertions — MUST NOT break)
  </read_first>
  <files>src/lib/fortification/FortificationCalculator.svelte</files>
  <action>
Final sweep to align the rest of the calculator with `.impeccable.md` and the morphine calculator's conventions. Scope is limited to:

1. **Outer container rhythm:** keep the top-level `<div class="space-y-6">` wrapper. Do not change.

2. **Verification card label parity:** change the Verification eyebrow label from `text-xs font-semibold ... uppercase tracking-wide` to `text-2xs font-semibold uppercase tracking-wide text-[var(--color-text-secondary)]` — matching the morphine summary eyebrows (`text-2xs`). Do the same for the "Amount to Add" eyebrow if Task 2 used `text-2xs` already (it does).

3. **Verification dt/dd typography:** in the Verification section, change the dt class from `text-sm text-[var(--color-text-secondary)]` to `text-sm font-medium text-[var(--color-text-secondary)]` and keep the dd class as `text-base font-semibold num text-[var(--color-text-primary)]`. DO NOT change the rendered text content of any dd — the test asserts "183.5 mL", "23.5 kcal/oz", "180 (6.1 oz)" exactly. DO NOT change `{result.yieldMl.toFixed(1)} mL` or the other expressions.

4. **Verification card spacing:** change `<dl class="mt-2 flex flex-col gap-2">` to `<dl class="mt-3 flex flex-col gap-3">` for a calmer rhythm.

5. **Empty-state consistency:** the current hero empty-state (touched in Task 2) already uses dashed-border-free style inside the accent hero; no further change needed there. Ensure the Verification card is only rendered when `result` is truthy (it already is — `{#if result}`). No change.

6. **No new imports, no new props, no new components, no JS/TS changes.**

Rules:
- DO NOT add animation utilities (reserved for Phase 16).
- DO NOT edit any non-markup expression — the only things touching `{...}` bindings are the class attributes.
- DO NOT alter any rendered text content in the Verification dd nodes or the hero — tests pin these strings.
- DO NOT change the `"Unit"` or `"Target Calorie (kcal/oz)"` labels.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm vitest run src/lib/fortification/FortificationCalculator.test.ts && pnpm vitest run src/lib/fortification/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'gap-3' src/lib/fortification/FortificationCalculator.svelte` matches the Verification `<dl>`
    - `grep -c 'text-2xs font-semibold uppercase tracking-wide' src/lib/fortification/FortificationCalculator.svelte` is ≥ 2 (hero eyebrow + Verification eyebrow)
    - `grep -n 'font-medium text-\[var(--color-text-secondary)\]' src/lib/fortification/FortificationCalculator.svelte` matches Verification dt elements
    - Vitest file `FortificationCalculator.test.ts` — ALL tests pass (UI-01 through UI-05)
    - Full fortification folder vitest run green (calculations.test.ts + component test)
    - `git diff src/lib/fortification/calculations.ts src/lib/fortification/fortification-config.json src/lib/fortification/state.svelte.ts src/lib/fortification/types.ts` is empty (no changes to these files)
    - `git diff --stat` shows only `src/lib/fortification/FortificationCalculator.svelte` modified across the whole plan
    - `grep -n 'hardcoded\|#[0-9a-fA-F]\{3,6\}\|rgb(\|rgba(' src/lib/fortification/FortificationCalculator.svelte` returns no matches (OKLCH tokens only)
  </acceptance_criteria>
  <done>Component markup matches `.impeccable.md` rhythm and morphine calculator conventions; all tests pass; no logic files touched.</done>
</task>

</tasks>

<verification>
Phase-level checks:

1. `pnpm vitest run src/lib/fortification/` — all fortification tests green, including spreadsheet parity in `calculations.test.ts`.
2. `pnpm vitest run` — full suite still green.
3. `pnpm build` — app builds without TypeScript / Svelte errors.
4. `git diff --stat` — only `src/lib/fortification/FortificationCalculator.svelte` modified.
5. Visual (human checkpoint happens in a later phase review, not here): at 375px width, Target Calorie and Unit are on one row with ≥48px touch targets; hero card reads as a clinical result panel with bold tabular numeric and brand-accent surface.
</verification>

<success_criteria>
- FORT-01: Mobile row pairing implemented via `grid grid-cols-2 gap-4` wrapper around Target Calorie + Unit SelectPickers.
- FORT-02: Hero card uses `bg-[var(--color-accent-light)]`, bold tabular numeric, secondary unit label — matching morphine wean result theming — while keeping the `<section>` + "Amount to Add" label + aria-live wiring intact.
- FORT-03: Verification and hero eyebrows unified on `text-2xs` uppercase tracking-wide; Verification dl rhythm calmed to `gap-3`.
- All 7 assertions in `FortificationCalculator.test.ts` (UI-01 through UI-05 including both UI-02 variants and all three UI-03 variants) pass unchanged.
- No edits to `calculations.ts`, `state.svelte.ts`, `fortification-config.json`, or `types.ts`.
- No hardcoded color literals; OKLCH tokens only.
</success_criteria>

<output>
After completion, create `.planning/phases/13-fortification-polish/13-01-SUMMARY.md` documenting:
- Diff summary of the three markup changes
- Confirmation of test suite green
- Screenshots (if available) of mobile + desktop, light + dark hero
</output>
