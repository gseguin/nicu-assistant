---
phase: 53-favorites-safety-net-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shared/favorites.test.ts
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
must_haves:
  truths:
    - "SAFE-02: a favorites store loaded with ['morphine-wean','formula','pert','gir'] yields current === ['morphine-wean','formula','gir'] (pert dropped, stored order preserved)"
    - "SAFE-03: first-run defaults (no localStorage seed) do not contain 'pert' and match the alphabetical baseline ['feeds','formula','gir','morphine-wean']"
    - "SAFE-01 (unit level): the recover() filter is proven by SAFE-02 — unknown ID is silently dropped with no crash"
    - "Full vitest suite passes at 410 tests (408 baseline + 2 new tests)"
  artifacts:
    - path: "src/lib/shared/favorites.test.ts"
      provides: "SAFE-02 and SAFE-03 test blocks appended after T-21"
      contains: "SAFE-02"
  key_links:
    - from: "favorites.test.ts SAFE-02"
      to: "favorites.svelte.ts recover() step 4"
      via: "vi.resetModules() + await import('./favorites.svelte.js') + favorites.init()"
      pattern: "valid\\.has\\(id\\)"
    - from: "favorites.test.ts SAFE-03"
      to: "favorites.svelte.ts defaultIds()"
      via: "first-run init() with no localStorage seed"
      pattern: "CALCULATOR_REGISTRY\\.map.*slice"
---

<objective>
Add two regression-test blocks (SAFE-02 and SAFE-03) to `src/lib/shared/favorites.test.ts` proving that the existing `recover()` filter silently drops `'pert'` from stored favorites and that first-run defaults never contain `'pert'`.

Purpose: SAFE-01/02/03 are the final requirements before Phase 54 release. The filter is already live in `favorites.svelte.ts` — this phase adds the documented, historically specific test coverage that locks it against regression. No production source files are modified.

Output: Two appended `describe` blocks in `favorites.test.ts`; vitest passes at 410/410; SAFE-01 unit proof established.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SAFE-02 — pert upgrade-path regression test</name>
  <files>src/lib/shared/favorites.test.ts</files>

  <read_first>
    - src/lib/shared/favorites.test.ts — read in full before touching; T-21 (lines 208-226) is the direct template; replicate its describe/it/localStorage.setItem/vi.resetModules()/import/init/toEqual structure exactly
    - src/lib/shared/favorites.svelte.ts — read lines 36-59 (recover()) and 17-19 (defaultIds()) to understand the behavior under test; DO NOT EDIT this file
    - src/lib/shell/registry.ts — confirm 5 entries, no pert; ensures valid.has('pert') is false in the test environment
  </read_first>

  <behavior>
    - Test: localStorage seeded with {v:1, ids:['morphine-wean','formula','pert','gir']} → favorites.current after init() equals ['morphine-wean','formula','gir'] (pert dropped, stored order preserved per D-21)
    - Meaningful failure: if recover() step 4 filter is removed, received value would be ['morphine-wean','formula','pert','gir'] — a clear, legible failure
  </behavior>

  <action>
    Append a new top-level describe block after T-21's closing brace (after line 226). Follow the identical structure of T-21: standalone top-level describe (not nested inside 'favorites store'), vi.resetModules() called inside the test body before import (not relying solely on beforeEach, which belongs to the inner describe), localStorage.setItem BEFORE the dynamic import, then init(), then expect([...favorites.current]).toEqual([...]).

    Block name: 'SAFE-02 — pert upgrade path regression (v1.15+ users)'
    Test name: 'SAFE-02: stored favorites containing pert silently drops it, preserving valid-id order'

    localStorage seed (key: 'nicu:favorites'):
      JSON.stringify({ v: 1, ids: ['morphine-wean', 'formula', 'pert', 'gir'] })

    Expected result:
      expect([...favorites.current]).toEqual(['morphine-wean', 'formula', 'gir'])

    Include a comment inside the test citing D-04 (53-CONTEXT) to document why 'pert' literal is used (real historical upgrade scenario: v1.15 users had 'pert' favorited). Add a secondary comment noting the D-19 grep gate is already non-zero (CLAUDE.md/PRODUCT.md hits), so this literal does not change the gate state.

    The expect order is ['morphine-wean','formula','gir'] — NOT alphabetical — because recover() preserves stored insertion order verbatim (D-21). The stored input minus 'pert' IS ['morphine-wean','formula','gir'], not a re-sorted array.

    Do NOT edit recover(), defaultIds(), validIds(), or any other source file. Do NOT wrap the describe in another describe.
  </action>

  <verify>
    <automated>cd /home/ghislain/src/nicu-assistant && pnpm test:run --reporter=verbose 2>&1 | grep -E "(SAFE-02|Tests.*pass)"</automated>
  </verify>

  <done>favorites.test.ts contains a describe block named 'SAFE-02 — pert upgrade path regression (v1.15+ users)' with a passing test; vitest output confirms test count increased by 1 from 408</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SAFE-03 — first-run defaults never-contain-pert regression guard</name>
  <files>src/lib/shared/favorites.test.ts</files>

  <read_first>
    - src/lib/shared/favorites.test.ts — read lines 197-206 (T-20) as the structural template for a no-localStorage-seed first-run assertion; also note T-01 (line 18-26) for how init() with empty storage yields defaults
    - src/lib/shared/favorites.svelte.ts — lines 17-19: defaultIds() returns CALCULATOR_REGISTRY.map((c) => c.id).slice(0, FAVORITES_MAX); with 5 alphabetical entries this yields ['feeds','formula','gir','morphine-wean']
    - src/lib/shell/registry.ts — confirm alphabetical order: feeds, formula, gir, morphine-wean, uac-uvc (first 4 are the defaults)
  </read_first>

  <behavior>
    - Test A: favorites.current after init() with no localStorage seed does NOT contain 'pert'
    - Test B: favorites.current equals exactly ['feeds','formula','gir','morphine-wean'] — the full regression guard documenting the complete baseline (not just absence of 'pert')
  </behavior>

  <action>
    Append a new top-level describe block after the SAFE-02 block. Same standalone structure as T-20/T-21: top-level describe, vi.resetModules() inside the test body before import, NO localStorage.setItem (first-run scenario), then favorites.init(), then two expect assertions.

    Block name: 'SAFE-03 — first-run defaults never contain pert (regression guard)'
    Test name: 'SAFE-03: first-run defaults are the v1.13 four-calculator baseline with no pert'

    Two assertions in order:
      1. expect(favorites.current).not.toContain('pert')
      2. expect([...favorites.current]).toEqual(['feeds', 'formula', 'gir', 'morphine-wean'])

    CRITICAL ORDER: The expected array MUST be ['feeds','formula','gir','morphine-wean'] — alphabetical order per D-19 registry alphabetization. Do NOT write ['morphine-wean','formula','gir','feeds'] (the historical v1.13 visual order). The ROADMAP "v1.13 baseline" wording refers to the same 4 IDs, not the ordering — the actual ordering is alphabetical per D-19. T-01, T-20, and 6 other tests already assert this exact alphabetical array.

    Include a comment explaining why the expected order is alphabetical (D-19: post-v1.13 registry alphabetization changed the physical order; conceptual baseline = same 4 calculators, no pert; the live ordering is alphabetical).

    Do NOT call localStorage.setItem before init() — this is a first-run scenario (no stored state). Do NOT edit any source file.
  </action>

  <verify>
    <automated>cd /home/ghislain/src/nicu-assistant && pnpm test:run --reporter=verbose 2>&1 | grep -E "(SAFE-03|Tests.*pass)"</automated>
  </verify>

  <done>favorites.test.ts contains a describe block named 'SAFE-03 — first-run defaults never contain pert (regression guard)' with a passing test; vitest output confirms test count is 410 (408 baseline + 2 new tests)</done>
</task>

<task type="auto">
  <name>Task 3: Full suite verification + SAFE-01 browser-level documentation</name>
  <files>src/lib/shared/favorites.test.ts</files>

  <read_first>
    - src/lib/shared/favorites.test.ts — verify the final file has SAFE-02 and SAFE-03 blocks correctly placed after T-21 with no structural errors; check that no source files were accidentally modified
  </read_first>

  <action>
    Run the full vitest suite and confirm 410/410 pass. Verify no regressions in the existing 408 tests.

    If pnpm test:run exits non-zero, read the failure output, identify whether the failure is in SAFE-02, SAFE-03, or a pre-existing test, and fix only the new test blocks. Do NOT modify any existing test. Do NOT touch source files.

    SAFE-01 browser-level note (per D-06): The SAFE-02 unit assertion IS the hard automated gate for SAFE-01 (it proves recover() drops 'pert'). The browser-level portion — "bottom-bar / hamburger / desktop nav render with only valid IDs; no console errors, no missing-icon placeholders" — is verified manually by seeding localStorage with {v:1, ids:['morphine-wean','formula','pert','gir']} via browser DevTools, loading the app (pnpm dev), and observing clean 3-tab bottom bar with no console errors. This manual check is a developer gate, not an automated assertion.
  </action>

  <verify>
    <automated>cd /home/ghislain/src/nicu-assistant && pnpm test:run 2>&1 | grep -E "Tests.*410 passed|Tests.*passed"</automated>
  </verify>

  <done>pnpm test:run exits 0; output reads "410 passed (410)"; both SAFE-02 and SAFE-03 appear green in verbose output; no pre-existing test regressions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies only a vitest test file. No runtime code, no new inputs, no network calls, no trust boundaries created or modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan. The Package Legitimacy Audit is N/A — confirmed in RESEARCH.md §"Package Legitimacy Audit". |

**Honest assessment:** This plan appends ~25 lines of vitest test code to one test file. No authentication, session handling, input validation, cryptography, access control, or network surface is touched. ASVS L1 categories V2–V6 do not apply. No new attack surface is introduced.
</threat_model>

<verification>
**Automated gate (blocking):**
```
pnpm test:run
```
Must exit 0 with "410 passed (410)".

**SAFE-01 browser-level gate (manual, non-blocking for phase close):**
1. Open browser DevTools → Application → Local Storage → set key `nicu:favorites` to `{"v":1,"ids":["morphine-wean","formula","pert","gir"]}`
2. Reload the app (pnpm dev)
3. Observe: bottom bar shows exactly 3 tabs (morphine-wean, formula, gir); hamburger menu lists these 3 as favorited; no console errors; no missing-icon placeholder
4. Expected: clean load, no runtime error, 'pert' silently absent from all nav surfaces

**Regression guard:**
```
grep -n "SAFE-02\|SAFE-03" src/lib/shared/favorites.test.ts
```
Must return 2 matching lines (the test names).

**No-source-change guard:**
```
git diff --name-only HEAD | grep -v favorites.test.ts
```
Must return empty (only favorites.test.ts modified).
</verification>

<success_criteria>
1. `pnpm test:run` exits 0 with 410 tests passing (2 new tests added to baseline of 408).
2. `favorites.test.ts` contains describe block 'SAFE-02 — pert upgrade path regression (v1.15+ users)' asserting ['morphine-wean','formula','pert','gir'] → ['morphine-wean','formula','gir'].
3. `favorites.test.ts` contains describe block 'SAFE-03 — first-run defaults never contain pert (regression guard)' asserting not.toContain('pert') AND toEqual(['feeds','formula','gir','morphine-wean']).
4. No changes to `favorites.svelte.ts`, `registry.ts`, or any other source file.
5. SAFE-01 unit proof established by SAFE-02 passing (recover() demonstrably drops 'pert').
</success_criteria>

<output>
Create `.planning/phases/53-favorites-safety-net-verification/53-01-SUMMARY.md` when done.
</output>
