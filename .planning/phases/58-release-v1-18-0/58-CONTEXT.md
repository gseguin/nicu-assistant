# Phase 58: Release v1.18.0 - Context

**Gathered:** 2026-05-29
**Status:** Ready for planning

> Captured in `--auto` mode: all gray areas auto-selected, recommended option chosen for each. Derived from the Phase 54 v1.17.0 release pattern (`642969c`, `e7dc13a`, `2033469`, `433725a`), the project's `__APP_VERSION__` build-time inject (`vite.config.ts:10-11` + `src/lib/shared/about-content.ts:3,13`), and the explicit constraint that Playwright browser binaries are NOT installed locally. Review and adjust before planning if any default is wrong.

<domain>
## Phase Boundary

Phase 58 ships **v1.18.0**: bumps `package.json` 1.17.0 → 1.18.0, syncs docs (PROJECT.md, MILESTONES.md, REQUIREMENTS.md traceability), and runs the full automated clinical gate. It is the milestone-close ship-it phase (REL-01..03).

**In scope:**
- `package.json` version field 1.17.0 → 1.18.0 (single string edit).
- `package-lock.json` regenerated by `pnpm install` (committed alongside).
- `.planning/PROJECT.md` Validated-list promotion of v1.18 entries (move from Active → Validated with phase refs), Current State → Shipped section, footer date.
- `.planning/MILESTONES.md` append a concise v1.18 entry (style: prior milestone entries — one bullet per major shipped item: seam, 4 adapters, auto-persist consolidation, 9 duplicate-effect deletions).
- `.planning/REQUIREMENTS.md` traceability table: flip REL-01..03 to ✓ Complete (the other 10 IDs — SEAM-01..04, MIG-01..04, AUTO-01..02 — are already ✓ from Phases 55–57).
- Run the local clinical gate: svelte-check 0/0, `pnpm vitest run`, `pnpm build`.
- Create a HUMAN-UAT.md carrying the Playwright/axe portion of REL-03 forward to CI (Phase 54 precedent).

**Explicitly NOT in scope:**
- **Git tag / push.** Tagging `v1.18.0` and pushing to a remote are EXPLICITLY NOT in scope for execution. The user confirmed at chain start that they would handle push/tag themselves after milestone close. The phase ships the version-bumped commits on main; the user tags/pushes as a manual step.
- **AboutSheet text edits for the version.** None needed — `about-content.ts:13` already reads `__APP_VERSION__` at build time; the version string updates automatically with the package.json bump. There is no hardcoded version anywhere in `src/` (confirmed).
- **Architecture review candidate 2** (config pass-throughs) — future milestone.
- **ML_PER_OZ clinical constant** (candidate 4) — out of milestone, needs clinician sign-off.
- **v1.15.1 SMOKE-01..10 real-iPhone gate** — carries forward independently from v1.18 (Phase 50 deferral).
- **v1.17 deferred Playwright/UAT items** — those remain in their own deferred-items table, not re-opened here.

**The hard gate:** `package.json` is `1.18.0`, all 13 v1.18 requirement IDs are ✓ in REQUIREMENTS.md, PROJECT.md + MILESTONES.md tell the v1.18 story correctly, svelte-check 0/0, vitest fully green, `pnpm build` OK. The Playwright/axe portion of REL-03 is captured in HUMAN-UAT.md as `status: partial` (deferred to CI) — same close-state pattern Phase 54 used for v1.17.0.

</domain>

<decisions>
## Implementation Decisions

### Phase shape — two plans, matching Phase 54
- **D-01:** Split into TWO plans:
  - **58-01 DOC sync:** PROJECT.md Validated-list promotion + MILESTONES.md v1.18 entry + REQUIREMENTS.md traceability flips for REL-01..03 + Current State / footer rewrites. NO version bump in this plan. No code changes.
  - **58-02 REL ship:** `package.json` 1.17.0 → 1.18.0, `pnpm install` to regenerate lockfile, run full local clinical gate (svelte-check + vitest + build), create HUMAN-UAT.md for the deferred Playwright/axe sweeps, mark milestone Complete in STATE.md/ROADMAP.md tracking.
  - **Why:** Same shape as Phase 54 (54-01 DOC + 54-02 final gate + REL groundwork). Splitting cleanly isolates the doc churn from the version bump + lockfile change, so a code reviewer can read each commit set in one mental chunk. Sequential — 58-02 depends on 58-01's doc work being landed first.

### Version bump mechanics (REL-01)
- **D-02:** REL-01 is a single string edit in `package.json`: `"version": "1.17.0"` → `"version": "1.18.0"`. Vite's `define: { __APP_VERSION__: JSON.stringify(pkg.version) }` (`vite.config.ts:10-11`) injects it at build time; `src/lib/shared/about-content.ts:13` reads `__APP_VERSION__` and renders it in the AboutSheet. **No source file under `src/` needs editing.** Run `pnpm install` after the edit to regenerate `package-lock.json` (the npm registry mirror in `pnpm-lock.yaml` is the actual lockfile this project uses — check both; commit whatever changed). Grep-confirm no hardcoded "1.17" version string anywhere in `src/` post-bump.

### Playwright + axe deferral (REL-03 — load-bearing)
- **D-03:** Playwright browser binaries are NOT installed locally (confirmed: `~/.cache/ms-playwright` is empty/missing). Running `pnpm playwright test` locally would fail with "browser not installed" before any test ran. **Defer the Playwright E2E + extended axe sweep portion of REL-03 to CI**, mirroring Phase 54's precedent for v1.17.0 (the v1.17 STATE.md "Deferred Items" table includes "Phase 54 Playwright live no-regression run — Pending — CI; browser binaries unavailable locally (D-02); svelte-check 0/0 + vitest 410/410 passed").
- **D-04:** Create `58-HUMAN-UAT.md` with `status: partial` carrying the Playwright + extended axe sweep items as Pending. This makes the deferral surface in `/gsd:progress` and `/gsd:audit-uat`. The HUMAN-UAT.md follows the GSD template (`status` / `source` / `started` / `updated` frontmatter + `## Tests` list with `result: pending`).
- **D-05:** Phase verification + milestone close proceed with `status: passed` on the automated gates (svelte-check + vitest + pnpm build) and `partial` on REL-03's Playwright portion captured in HUMAN-UAT.md — same close-state as v1.17.0 Phase 54.

### PROJECT.md evolution (REL-02 part 1)
- **D-06:** Move v1.18 entry from `### Active` to `### Validated` with the format used by prior milestones: one consolidated bullet per major delivered item, suffixed with `— v1.18 (Phase XX)`. Prior milestones use 1–N bullets; v1.18 needs at minimum:
  - PersistentValue<T> persistence seam (SEAM-01..04) — v1.18 (Phase 55)
  - Theme/disclaimer/favorites/lastEdited migrated onto the seam (MIG-01..04) — v1.18 (Phase 56)
  - Auto-persist behind CalculatorStore via single `$effect.root()` in constructor; 9 duplicate per-fragment effects deleted (AUTO-01..02) — v1.18 (Phase 57)
  - Release v1.18.0; `__APP_VERSION__` reflects 1.18.0 (REL-01..03) — v1.18 (Phase 58)
- **D-07:** Update PROJECT.md "Current State" — move v1.18 from `In progress:` to `Shipped:` and write a one-paragraph milestone summary (locality + leverage — storage failure now handled once; 9 hand-rolled effects + 4 hand-rolled localStorage adapters consolidated to 1 seam + 1 in-class effect; behavior-preserving). Update the `Previously Shipped:` list to add `- v1.18 Persistence Seam — Phases 55-58` at the top. Bump the footer to today's date.

### MILESTONES.md entry (REL-02 part 2)
- **D-08:** Append a new v1.18 entry to `.planning/MILESTONES.md` following the style of prior entries: 4–6 bullets covering the seam, the 4 adapter migrations (with the key byte-shape facts: theme unquoted, favorites `{v:1,ids}`, disclaimer v1 audit trail, lastEdited recover-hook null-guard), the auto-persist consolidation (in-class `$effect.root()` + 9 deletions), and the release stats (svelte-check 0/0, vitest 451/451). One sentence summarizing milestone intent at the top of the entry; one acknowledging the deferred Playwright/axe CI gate at the bottom.

### REQUIREMENTS.md traceability flip (REL-02 part 3)
- **D-09:** SEAM-01..04, MIG-01..04, AUTO-01..02 are already flipped to ✓ Complete by `phase.complete` for Phases 55–57. The REL plan flips REL-01, REL-02, REL-03 to ✓ Complete. After this, 13/13 v1.18 requirement IDs are ✓. The `Future Requirements (deferred)` table entry for "Architecture review candidate 2" stays as-is (out of v1.18 scope).

### Clinical gate run (REL-03 automated portion)
- **D-10:** Run in this exact order (each must pass before the next runs in the plan's verification block):
  1. `pnpm install` (regenerate lockfile after package.json bump)
  2. `pnpm exec svelte-check --tsconfig ./tsconfig.json` — expect 0 errors / 0 warnings
  3. `pnpm vitest run` — expect 451+ tests green (baseline + any new ones; current count is 451)
  4. `pnpm build` — expect success (adapter-static SPA output)
  5. Grep gate: `grep -rn '"version".*1\.17' src/ package.json` — expect ONE match (package.json itself, which now reads 1.18.0 — actually expect ZERO matches for `1.17` and exactly ONE for `1.18.0` in package.json), and no `"1.17"` string anywhere in `src/`.
  6. **Skipped — Playwright:** `pnpm playwright test` and `pnpm playwright test --grep axe` are LEFT UNRUN in this phase; the HUMAN-UAT.md captures the deferral.

### Milestone close + tagging policy
- **D-11:** After REL-02's tracking commit, the `phase.complete` CLI for Phase 58 will mark the milestone as 100% complete (`is_last_phase: true`). The orchestrator's auto-advance routing will detect milestone-complete and offer `/gsd:complete-milestone v1.18`. **Per user instruction at chain start, do NOT push or tag without asking** — the chain stops at milestone-complete with a clear "ready to tag/push v1.18.0" status; the user runs `git tag v1.18.0 && git push` manually.
- **D-12:** Do NOT include `git tag` or `git push` in any plan task. The phase's git activity is local-only: edit files, `pnpm install`, commit, mark complete.

### Claude's Discretion
- The EXACT bullet text for PROJECT.md Validated entries — planner picks final wording matching the style of v1.17 entries.
- Whether to add a one-line "Persistence Seam" subsection under PROJECT.md `## Context` or just rely on the Validated list (Phase 54 only added Validated bullets — recommend same).
- Whether MILESTONES.md needs a section divider before v1.18 (check prior entries for the convention).
- Plan 58-02 task split: clinical-gate run as one task vs. one task per check command — planner judges; recommend one task with sequential `<verify><automated>` blocks since each gate's failure is a stop condition.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Milestone scope & locked decisions
- `.planning/REQUIREMENTS.md` §"REL — release" — REL-01..03 acceptance language; traceability table at the bottom.
- `.planning/ROADMAP.md` §"Phase 58: Release v1.18.0" — goal + 3 success criteria.
- `.planning/STATE.md` §"Accumulated Context › Decisions" — the [v1.18] decisions (esp. behavior-preserving hard gate); `## Deferred Items` table for the carried-forward v1.15.1 SMOKE items + the v1.17 Phase 54 Playwright-to-CI precedent.
- `.planning/PROJECT.md` §"Current Milestone: v1.18 Persistence Seam" + § "Active" + § "Validated" — the section structure to edit.
- `.planning/MILESTONES.md` (tail) — the historical entry style; v1.17 / v1.15.1 / v1.14 / v1.13 entries are the immediate models.

### The version-bump surface (single file edit + lockfile)
- `package.json` line 5: `"version": "1.17.0"` (the single edit point — change to `1.18.0`).
- `pnpm-lock.yaml` — regenerated by `pnpm install` after the bump (commit alongside).
- `vite.config.ts` lines 10-11: `define: { __APP_VERSION__: JSON.stringify(pkg.version) }` — the build-time inject; NO edit needed.
- `src/lib/shared/about-content.ts:13` — `appVersion = ...__APP_VERSION__...` — reads the inject; NO edit needed.

### The Phase 54 release pattern (closest analog)
- `642969c docs(54-02): complete clinical gate + REL-04 groundwork plan` — the final-gate task structure.
- `2033469 docs(54-01): DOC-05 add v1.17 MILESTONES entry + REL-01 bump package.json to 1.17.0` — the doc + version commit; NOTE: Phase 54 bundled the package.json bump WITH the DOC plan (54-01), not separately. This phase (58) cleanly separates them: 58-01 = doc-only, 58-02 = version bump + gate. The separation is cleaner because v1.18 has more doc surface (a real seam, four adapter migrations to summarize).
- `e7dc13a docs(54-01): complete documentation sync and version bump plan` — the v1.17 plan SUMMARY (style reference for the new SUMMARY artifacts).

### Tests to confirm green (regression contract)
- `src/lib/shared/persistent-value.test.ts` — the Phase 55 seam test surface (26 tests).
- `src/lib/shared/migration-56-01.test.ts`, `src/lib/shared/migration-56-02.test.ts` — Phase 56 byte-identity tests (11 tests across both).
- `src/lib/shared/favorites.test.ts` — Phase 56 hard-gate regression contract (21 + SAFE-02/03 tests).
- `src/lib/shell/calculator-store.test.ts` — Phase 57 hard-gate + the 3 new auto-persist tests (14 tests total).
- Full suite baseline: **451 tests green** before Phase 58 begins.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **`__APP_VERSION__` build-time inject** (`vite.config.ts:10-11`) — the only mechanism needed to propagate the version bump. Zero src/ edits. The AboutSheet picks it up automatically.
- **Phase 54 release pattern** — the closest analog. Same constraint (Playwright deferred to CI), same MILESTONES.md append style, same PROJECT.md Validated-list promotion mechanic, same `phase.complete` → milestone-close flow.
- **`gsd-sdk query phase.complete "58"`** automatically detects `is_last_phase: true` and updates ROADMAP + STATE + REQUIREMENTS traceability table; the plan does NOT manually flip the requirement IDs — the CLI does it.

### Established Patterns
- **No hardcoded version strings anywhere in `src/`** — every prior release has worked this way (vetted in Phase 54 DOC-02/03 and prior). The grep gate at REL-01 close (D-10 step 5) catches regressions.
- **HUMAN-UAT.md for CI-deferred items** — established by Phase 54 + Phase 53 (favorites UAT). `status: partial` keeps it visible in `/gsd:progress` until run.
- **Milestone-close auto-advance offers `/gsd:complete-milestone`** — Phase 54 → v1.17 archive followed the same routing.

### Integration Points
- After Phase 58: `package.json` reads `1.18.0`; AboutSheet shows `v1.18.0` at runtime via `__APP_VERSION__`; PROJECT.md/MILESTONES.md/REQUIREMENTS.md tell the v1.18 story; 13/13 IDs ✓; svelte-check 0/0; vitest 451+/451+; build OK; HUMAN-UAT.md carries the Playwright/axe deferral to CI. The user then manually `git tag v1.18.0 && git push --tags origin main` outside of this phase.

</code_context>

<specifics>
## Specific Ideas

- Use the EXACT phrasing from prior MILESTONES.md entries (terse, technical, factual). Don't editorialize. One sentence intro, 4–6 bullets, optional one-line deferred-items note at the end.
- The PROJECT.md Validated bullets should mirror the style of the v1.17 promotion (`✗ Pediatric EPI PERT Calculator ... v1.17` plus `✓ v1.17 Remove PERT Calculator ... v1.17`) — concise, phase-tagged.
- HUMAN-UAT.md should reuse the Phase 54 wording template for the Playwright deferral so future audits can grep for the pattern.
- The package.json edit + `pnpm install` MUST happen in the same task so that the commit captures both `package.json` and the regenerated lockfile together; do NOT split.

</specifics>

<deferred>
## Deferred Ideas

- **`git tag v1.18.0` + `git push`** — explicitly NOT in scope. User handles manually after milestone close. (D-11, D-12)
- **Playwright E2E + extended axe sweep run** — deferred to CI per Phase 54 precedent (browsers unavailable locally). HUMAN-UAT.md captures the deferral. (D-03, D-04)
- **Architecture review candidate 2** (config pass-throughs) — future milestone.
- **ML_PER_OZ clinical constant** (candidate 4) — out of milestone, needs clinician sign-off.
- **v1.15.1 SMOKE-01..10 real-iPhone gate** — carries forward independently.
- **v1.17 deferred items** — Phase 53 SAFE-01 browser-visual + Phase 54 Playwright live no-regression run + v1.15.1 quick_task manifest stale entries — all stay in `.planning/STATE.md` `## Deferred Items` and roll forward across milestones until resolved.

</deferred>

---

*Phase: 58-Release v1.18.0*
*Context gathered: 2026-05-29*
