---
phase: 58-release-v1-18-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
autonomous: true
requirements:
  - REL-02

must_haves:
  truths:
    - "PROJECT.md ### Active section no longer contains the v1.18 ⏳ line"
    - "PROJECT.md ### Validated section contains 4 new v1.18 bullets (Phase 55, 56, 57, 58), each suffixed — v1.18 (Phase XX)"
    - "PROJECT.md ## Current State block describes v1.18 as Shipped, not In progress"
    - "PROJECT.md ## Previously Shipped list has v1.18 at the top"
    - "PROJECT.md footer date reads 2026-05-29 with a v1.18 summary sentence"
    - "MILESTONES.md top entry is v1.18 Persistence Seam (Shipped: 2026-05-29), above v1.17"
    - "MILESTONES.md v1.18 entry has Known deferred items, Key accomplishments (4 bullets covering seam, adapters, auto-persist, release)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Updated milestone narrative — v1.18 promoted to Validated + Shipped"
      contains: "v1.18 (Phase 55)"
    - path: ".planning/MILESTONES.md"
      provides: "v1.18 milestone entry at top of file"
      contains: "v1.18 Persistence Seam (Shipped: 2026-05-29)"
  key_links:
    - from: ".planning/PROJECT.md ### Active"
      to: ".planning/PROJECT.md ### Validated"
      via: "Move ⏳ v1.18 line → 4 ✓ bullets"
      pattern: "v1\\.18 \\(Phase 5[5-8]\\)"
    - from: ".planning/MILESTONES.md"
      to: "## v1.18 Persistence Seam"
      via: "Prepend above ## v1.17 entry"
      pattern: "## v1\\.18 Persistence Seam"
---

<objective>
Sync the two milestone-narrative planning docs (PROJECT.md + MILESTONES.md) with v1.18 completion. No code changes, no version bump — this plan is doc-only so that the milestone narrative is in place before the version bump lands in Plan 58-02.

Purpose: Isolates doc churn from the version bump + lockfile change so each commit set reads cleanly. Per D-01: 58-01 = doc-only, 58-02 = version bump + clinical gate.

Output: PROJECT.md with v1.18 promoted from Active → Validated + Shipped; MILESTONES.md with v1.18 prepended as the newest entry above v1.17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/MILESTONES.md
@.planning/phases/58-release-v1-18-0/58-CONTEXT.md
@.planning/phases/58-release-v1-18-0/58-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PROJECT.md — Active → Validated, Current State → Shipped, Previously Shipped, footer</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    Read .planning/PROJECT.md in full before editing. Key sections to locate:
    - Line ~134: `### Active` — contains the `⏳ v1.18 —` line to REMOVE.
    - `### Validated` section — add 4 bullets after the last existing validated entry (the ✓ v1.17 Remove PERT Calculator line).
    - Lines ~9-12: `## Current State` block — rewrite from `**In progress:** v1.18...` to `**Shipped:** v1.18...`
    - Lines ~154-158: `## Previously Shipped` list — prepend one line at top.
    - Last line (footer): `*Last updated: ...` — update date + summary.

    Also read .planning/phases/58-release-v1-18-0/58-RESEARCH.md §"PROJECT.md Edit Map for 58-01" for exact wording drafts.
  </read_first>
  <action>
    Make the following edits to .planning/PROJECT.md (read the file first to get exact line locations):

    **Edit 1 — Remove from `### Active`:**
    Delete the entire line:
    `- ⏳ v1.18 — Hoist the hand-rolled localStorage pattern in the shared global singletons onto one deep persistence seam (architecture deepening, candidate 1)`

    After deletion, `### Active` section should be empty (or contain only `## Current Milestone: v1.18 Persistence Seam` block if that section is separate — remove that entire `## Current Milestone:` section too, since it's v1.18-specific in-flight metadata that belongs in Shipped prose now).

    **Edit 2 — Add to `### Validated`** (append after the last existing ✓ v1.17 entry):
    ```
    - ✓ PersistentValue<T> persistence seam: guarded read/write/remove + JSON-or-raw codec + recover hook (single test surface SEAM-01..04) — v1.18 (Phase 55)
    - ✓ Theme / disclaimer / favorites / lastEdited adapters migrated onto the seam as thin wrappers — byte-identical keys + shapes (MIG-01..04); 3 regression suites green with zero edits — v1.18 (Phase 56)
    - ✓ Auto-persist consolidated behind CalculatorStore via single $effect.root() in constructor; 9 duplicate per-fragment effects deleted (5 *Inputs.svelte + 4 *Calculator.svelte parents); 60s lastEdited debounce + no Svelte-5 effect re-entry (AUTO-01..02) — v1.18 (Phase 57)
    - ✓ Release v1.18.0 — package.json bumped; AboutSheet reflects via __APP_VERSION__; clinical gate green (svelte-check 0/0 across 4592 files, vitest 451+/451+, pnpm build OK); Playwright + extended axe sweeps deferred to CI (REL-01..03) — v1.18 (Phase 58)
    ```

    **Edit 3 — Rewrite `## Current State` block:**
    Replace the existing `**In progress:** v1.18 Persistence Seam...` paragraph (which describes Phases 57, 56, 55 as prior work and says "next is Phase 58") with a single `**Shipped:** v1.18...` paragraph. Keep the `**Prior in milestone:**` and `**Shipped:** v1.15.1...` text below it — only replace the current top paragraph.

    The new `**Shipped:**` paragraph should read (exact wording at executor's discretion, must include these facts):
    `**Shipped:** v1.18 Persistence Seam — Phases 55–58 complete (2026-05-28..2026-05-29). Storage failure is now handled once: a single PersistentValue<T> seam owns guarded read/write/remove with JSON-or-raw codec and a recover/migrate hook. The four shared global singletons (theme, disclaimer, favorites, lastEdited) are now thin adapters over the seam — byte-identical storage keys and persisted JSON shapes throughout (behavior-preserving). Nine hand-rolled duplicate $effect persist blocks and four hand-rolled localStorage adapters collapsed to one seam + one in-class $effect.root() in CalculatorStore. Gates: svelte-check 0/0 (4592 files), vitest 451/451, pnpm build OK. Playwright + extended axe sweeps deferred to CI.`

    **Edit 4 — Update `## Previously Shipped` list:**
    Add as the FIRST line in the `## Previously Shipped` list (before the v1.17 entry):
    `- v1.18 Persistence Seam — Phases 55–58`

    **Edit 5 — Update footer:**
    Change the `*Last updated: ...` line at the bottom of the file to:
    `*Last updated: 2026-05-29 — v1.18 Persistence Seam shipped: PersistentValue<T> seam + 4 adapter migrations + auto-persist consolidation; released v1.18.0.*`
  </action>
  <verify>
    <automated>grep -c "v1.18 (Phase 5" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md</automated>
    Expected output: 4 (one per Phase 55, 56, 57, 58)

    <automated>grep -c "v1.18" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md</automated>
    Expected: at least 6 matches (4 Validated bullets + Previously Shipped + footer + Shipped paragraph)

    <automated>grep "⏳ v1.18" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md</automated>
    Expected: ZERO matches (the ⏳ Active line was removed)

    <automated>grep "v1.18 Persistence Seam" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md</automated>
    Expected: at least 1 match (Previously Shipped entry)
  </verify>
  <done>
    PROJECT.md has:
    - Zero ⏳ v1.18 lines in ### Active
    - Exactly 4 ✓ v1.18 bullets in ### Validated, suffixed — v1.18 (Phase 55/56/57/58)
    - ## Current State reads "Shipped: v1.18" (not "In progress")
    - ## Previously Shipped top entry is v1.18 Persistence Seam
    - Footer date is 2026-05-29 with v1.18 summary
  </done>
</task>

<task type="auto">
  <name>Task 2: Prepend v1.18 entry to MILESTONES.md</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    Read .planning/MILESTONES.md in full before editing. Confirm:
    - The file starts with `# Milestones` followed by the `## v1.17 Remove PERT Calculator (Shipped: 2026-05-24)` entry.
    - The v1.17 entry structure (heading + **Phases completed:** + **Known deferred items at close:** + **Notes:** + **Key accomplishments:** + `---`) is the exact style to follow.
    - The new v1.18 entry goes ABOVE the v1.17 entry (immediately after `# Milestones`), separated by `---`.

    Also read .planning/phases/58-release-v1-18-0/58-RESEARCH.md §"MILESTONES.md Edit Map for 58-01" for the draft entry text.
  </read_first>
  <action>
    Prepend the following v1.18 entry to .planning/MILESTONES.md, inserting it immediately after the `# Milestones` heading and before the `## v1.17` line. The entry ends with `---` which serves as the separator from the v1.17 entry below it.

    The exact block to insert (verbatim):

    ## v1.18 Persistence Seam (Shipped: 2026-05-29)

    **Phases completed:** 55, 56, 57, 58

    **Known deferred items at close:**
    - Phase 58 Playwright E2E + extended axe sweeps — Pending CI (browser binaries unavailable locally; see 58-HUMAN-UAT.md)
    - (carried) v1.15.1 SMOKE-01..10 real-iPhone gate
    - (carried) v1.17 deferred items

    **Key accomplishments:**
    - PersistentValue<T> persistence seam: guarded read/write/remove + JSON-or-raw codec + recover hook; single test surface (26 co-located tests) covering SSR, parse-failure, write-throw, and migrate-hook expressiveness (SEAM-01..04)
    - Four shared singletons migrated onto the seam as thin behavior-preserving adapters: theme stores 'dark' unquoted (FOUC-safe); disclaimer pvV1 read-only (audit trail preserved); favorites custom codec wraps {v:1,ids}; lastEdited recover hook guards Number('')===0 → null (MIG-01..04); favorites.test.ts + calculator-store.test.ts + DisclaimerBanner.test.ts stayed green with zero edits
    - Auto-persist folded behind CalculatorStore via single $effect.root() in constructor (after init, SSR-guarded, app-lifetime cleanup discarded); 9 duplicate $effect(...persist...) blocks deleted (5 *Inputs.svelte + 4 *Calculator.svelte parents — the latter caught by code review that flagged scope gap); re-entry impossible by construction + 60s STAMP_DEBOUNCE_MS as defense-in-depth (AUTO-01..02)
    - Released v1.18.0: package.json 1.17.0 → 1.18.0; AboutSheet reflects via __APP_VERSION__ build-time constant; clinical gate green (svelte-check 0/0 across 4592 files, vitest 451+/451+, pnpm build OK); Playwright + extended axe sweeps deferred to CI (REL-01..03)

    **Notes:**

    - Architecture review candidate 1 (storage seam) shipped end-to-end. Behavior-preserving milestone — zero user-visible change; identical storage keys and persisted byte shapes throughout. Direct localStorage callers in non-test src/ at close: persistent-value.ts (the seam), calculator-store.svelte.ts (one tenant of the same pattern; future migration candidate), plus two intentional null-probes (theme.init prefers-color-scheme fallback + favorites.init first-run write-back).

    ---

    (Then the existing ## v1.17 entry follows on the next line — no additional blank line needed between --- and ## v1.17.)
  </action>
  <verify>
    <automated>grep -c "v1.18 Persistence Seam" /home/ghislain/src/nicu-assistant/.planning/MILESTONES.md</automated>
    Expected: 1

    <automated>grep -n "## v1.18\|## v1.17" /home/ghislain/src/nicu-assistant/.planning/MILESTONES.md | head -4</automated>
    Expected: v1.18 line appears BEFORE v1.17 line (lower line number)

    <automated>grep "Shipped: 2026-05-29" /home/ghislain/src/nicu-assistant/.planning/MILESTONES.md</automated>
    Expected: 1 match (the v1.18 entry heading)

    <automated>grep "58-HUMAN-UAT.md" /home/ghislain/src/nicu-assistant/.planning/MILESTONES.md</automated>
    Expected: 1 match (the Known deferred items note)
  </verify>
  <done>
    MILESTONES.md has a new ## v1.18 Persistence Seam entry at the top (above ## v1.17), with Known deferred items (including 58-HUMAN-UAT.md reference), 4 Key accomplishments bullets, Notes block, and a trailing --- separator.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts → repository | Markdown file edits committed to main; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering | .planning/PROJECT.md edit | accept | Pure markdown; no PII, no executable paths. Read-before-write pattern prevents clobber. |
| T-58-02 | Tampering | .planning/MILESTONES.md edit | accept | Pure markdown; prepend-only. Grep gate confirms insertion succeeded. |
| T-58-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |

This is a documentation-only plan for a client-side offline-first PWA. No new code paths, no new persistence surface, no trust boundaries crossed beyond file edits to planning artifacts.
</threat_model>

<verification>
After both tasks complete, run:

```bash
# 1. v1.18 Validated bullets present (expect 4)
grep -c "v1.18 (Phase 5" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md

# 2. No ⏳ Active line remains
grep "⏳ v1.18" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md && echo "FAIL: Active line still present" || echo "PASS: Active line removed"

# 3. MILESTONES.md v1.18 entry present and ordered before v1.17
grep -n "## v1\.1[78]" /home/ghislain/src/nicu-assistant/.planning/MILESTONES.md | head -4
# v1.18 line number must be less than v1.17 line number

# 4. MILESTONES.md deferred items reference
grep "58-HUMAN-UAT.md" /home/ghislain/src/nicu-assistant/.planning/MILESTONES.md

# 5. Previously Shipped updated
grep "v1.18 Persistence Seam" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
```
</verification>

<success_criteria>
- `grep -c "v1.18 (Phase 5" .planning/PROJECT.md` returns 4
- `grep "⏳ v1.18" .planning/PROJECT.md` returns zero matches
- `grep -n "## v1.18\|## v1.17" .planning/MILESTONES.md` shows v1.18 at a lower line number than v1.17
- `grep "Shipped: 2026-05-29" .planning/MILESTONES.md` returns 1 match
- `grep "58-HUMAN-UAT.md" .planning/MILESTONES.md` returns 1 match
- `grep "v1.18 Persistence Seam" .planning/PROJECT.md` returns at least 1 match (Previously Shipped)
- No package.json changes, no src/ changes (zero code changes in this plan)
</success_criteria>

<output>
Create `.planning/phases/58-release-v1-18-0/58-01-SUMMARY.md` when done.
</output>
