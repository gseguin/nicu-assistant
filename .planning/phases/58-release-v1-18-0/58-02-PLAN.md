---
phase: 58-release-v1-18-0
plan: 02
type: execute
wave: 2
depends_on:
  - 58-01
files_modified:
  - package.json
  - .planning/phases/58-release-v1-18-0/58-HUMAN-UAT.md
autonomous: true
requirements:
  - REL-01
  - REL-03

must_haves:
  truths:
    - "package.json version field reads 1.18.0"
    - "No hardcoded 1.17 version string anywhere in src/"
    - "svelte-check exits 0/0 (zero errors, zero warnings)"
    - "pnpm vitest run exits 0 with 451+ tests green"
    - "pnpm build exits 0 (adapter-static SPA output)"
    - "58-HUMAN-UAT.md exists with status: partial carrying Playwright + axe deferral to CI"
    - "No git tag or git push is run"
  artifacts:
    - path: "package.json"
      provides: "Version bumped to 1.18.0 (single string edit)"
      contains: '"version": "1.18.0"'
    - path: ".planning/phases/58-release-v1-18-0/58-HUMAN-UAT.md"
      provides: "Playwright E2E + extended axe sweep deferral artifact for CI"
      contains: "status: partial"
  key_links:
    - from: "package.json version"
      to: "vite.config.ts __APP_VERSION__ define"
      via: "Vite reads pkg.version at build time; no src/ edit needed"
      pattern: '"version": "1\\.18\\.0"'
    - from: "pnpm vitest run"
      to: "451 tests"
      via: "Full suite baseline; Phase 58 adds zero new tests"
      pattern: "451"
---

<objective>
Ship v1.18.0: bump package.json 1.17.0 → 1.18.0, run the full local clinical gate in order (pnpm install → svelte-check 0/0 → vitest 451+/451+ → pnpm build OK), and create 58-HUMAN-UAT.md capturing the Playwright + extended axe sweep deferral to CI. Milestone narrative (PROJECT.md, MILESTONES.md) is already in place from Plan 58-01.

Purpose: REL-01 (version bump), REL-03 (automated clinical gate). REL-02's REQUIREMENTS.md traceability flip is handled automatically by `gsd-sdk query phase.complete "58"` — no manual REQUIREMENTS.md edit.

Output: package.json at 1.18.0; clinical gate green; 58-HUMAN-UAT.md artifact for CI Playwright deferral.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-release-v1-18-0/58-CONTEXT.md
@.planning/phases/58-release-v1-18-0/58-RESEARCH.md
@package.json
</context>

<interfaces>
<!-- Build-time version propagation chain — executor uses these directly, no codebase exploration needed. -->

From vite.config.ts (lines 7-11):
  const pkg = JSON.parse(readFileSync('package.json', 'utf-8'))
  define: { __APP_VERSION__: JSON.stringify(pkg.version) }
  — reads package.json at build entry; no caching; no src/ edit needed.

From src/lib/shared/about-content.ts (line 13):
  appVersion = ... __APP_VERSION__ ...
  — reads the Vite define; already works; no edit needed.

Conclusion: the ONLY edit is package.json line 4 "version" field. Zero src/ files change.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Version bump package.json 1.17.0 → 1.18.0 + run clinical gate</name>
  <files>package.json</files>
  <read_first>
    Read package.json to confirm line 4 currently reads `"version": "1.17.0"` (the single edit point).
    Read .planning/phases/58-release-v1-18-0/58-RESEARCH.md §"Research Findings Q3" to confirm pnpm-lock.yaml will NOT change on a version-only bump — commit only package.json unless git status shows unexpected lockfile diff.
    Read .planning/phases/58-release-v1-18-0/58-CONTEXT.md D-10 for the exact gate order.
  </read_first>
  <action>
    **Step 1 — Edit package.json (single string change):**
    Change line 4 from `"version": "1.17.0"` to `"version": "1.18.0"`. No other changes to package.json.

    **Step 2 — Run pnpm install (per D-02):**
    ```
    pnpm install
    ```
    Expected: exits 0. pnpm-lock.yaml will NOT change (confirmed by git history of the v1.17 bump commit — lockfile was unchanged for a version-only bump). After running, check `git status`: if pnpm-lock.yaml shows no diff (expected), commit only package.json. If it shows a diff for any unexpected reason, commit both.

    **Step 3 — Grep gate: no hardcoded version strings in src/:**
    ```
    grep -rn '1\.17' src/
    ```
    Expected: ZERO matches. Any match is a hardcoded version string that must be fixed before continuing. (Per research Q7: zero such strings exist — this gate should pass immediately.)

    Also confirm package.json now reads 1.18.0:
    ```
    grep '"version"' package.json
    ```
    Expected: `  "version": "1.18.0",`

    **Step 4 — Clinical gate, in order (D-10). Each must pass before running the next:**

    Gate 1 — svelte-check (BLOCKING):
    ```
    pnpm exec svelte-check --tsconfig ./tsconfig.json
    ```
    Expected: `Found 0 errors and 0 warnings` (across 4592 files). If any errors: STOP and fix before proceeding.

    Gate 2 — vitest (BLOCKING):
    ```
    pnpm vitest run
    ```
    Expected: 451+ tests pass, 0 failures. Baseline is 451 (Phase 57 close). Phase 58 adds zero new tests, so expect exactly 451. If any failures: STOP and investigate before proceeding.

    Gate 3 — build (BLOCKING):
    ```
    pnpm build
    ```
    Expected: exits 0, adapter-static SPA output. The `__APP_VERSION__` define will inject "1.18.0" into the build — no verification of the built output is needed beyond exit code 0.

    **Step 5 — Playwright gate (SKIP — deferred to CI per D-03):**
    Do NOT run `pnpm playwright test` or `pnpm exec playwright test`. Browser binaries are not installed locally (confirmed by CONTEXT D-03 and Phase 54 precedent). The Playwright + extended axe sweep portion of REL-03 is captured in 58-HUMAN-UAT.md (Task 2).

    **CRITICAL: Do NOT run `git tag` or `git push` (D-11, D-12). Phase commits are local-only.**
  </action>
  <verify>
    <automated>grep '"version"' /home/ghislain/src/nicu-assistant/package.json</automated>
    Expected: `  "version": "1.18.0",`

    <automated>grep -rn '1\.17' /home/ghislain/src/nicu-assistant/src/</automated>
    Expected: ZERO matches

    <automated>pnpm --dir /home/ghislain/src/nicu-assistant exec svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5</automated>
    Expected: `Found 0 errors and 0 warnings`

    <automated>pnpm --dir /home/ghislain/src/nicu-assistant vitest run 2>&1 | tail -10</automated>
    Expected: exit 0, all tests passed, count >= 451

    <automated>pnpm --dir /home/ghislain/src/nicu-assistant build 2>&1 | tail -5</automated>
    Expected: exit 0, no errors
  </verify>
  <done>
    package.json version is "1.18.0"; no hardcoded 1.17 strings in src/; svelte-check 0/0; vitest 451+/451+ green; pnpm build exits 0. pnpm-lock.yaml unchanged (or committed if unexpectedly changed). No git tag, no git push.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 58-HUMAN-UAT.md — Playwright + axe deferral to CI</name>
  <files>.planning/phases/58-release-v1-18-0/58-HUMAN-UAT.md</files>
  <read_first>
    Read .planning/phases/58-release-v1-18-0/58-RESEARCH.md §"Q4: HUMAN-UAT.md Template" for the exact Phase 54 format (frontmatter fields, ## Tests section structure, ## Summary block). The new file mirrors that template for the two Playwright deferred items.

    Read .planning/phases/58-release-v1-18-0/58-CONTEXT.md D-04 and D-05 to confirm: status: partial, items are pending (not blocked), local automated gates are already green.
  </read_first>
  <action>
    CREATE (new file) `.planning/phases/58-release-v1-18-0/58-HUMAN-UAT.md` with the following exact content:

    ---
    status: partial
    phase: 58-release-v1-18-0
    source: [58-VERIFICATION.md]
    started: 2026-05-29
    updated: 2026-05-29
    ---

    ## Current Test

    [Awaiting CI Playwright + extended axe sweep run on the v1.18.0 build]

    ## Tests

    ### 1. Playwright E2E (chromium + webkit-iphone projects)
    expected: All E2E specs pass in both projects against the v1.18.0 build (adapter-static SPA output). Chromium + webkit-iphone Playwright projects both green with no new failures vs. the Phase-57 baseline.
    result: [pending — CI]

    ### 2. Extended axe sweeps (light + dark themes)
    expected: All axe-core accessibility checks pass in both themes (WCAG 2.1 AA) against the v1.18.0 build.
    result: [pending — CI]

    ## Summary

    total: 2
    passed: 0
    issues: 0
    pending: 2
    skipped: 0
    blocked: 0

    ## Notes

    Per D-03 (58-CONTEXT): Playwright browser binaries are unavailable in the local development environment. The Playwright E2E + extended axe sweep portion of REL-03 is deferred to CI for the v1.18.0 build, mirroring the Phase 54 v1.17.0 close-state pattern. Local automated gates (svelte-check 0/0, vitest 451+/451+, pnpm build OK) are green. Auto-approved at Phase 58 close under auto-mode; persists for a CI confirmation. Does not block the v1.18 release or the milestone archive.
  </action>
  <verify>
    <automated>test -f /home/ghislain/src/nicu-assistant/.planning/phases/58-release-v1-18-0/58-HUMAN-UAT.md && echo "EXISTS" || echo "MISSING"</automated>
    Expected: EXISTS

    <automated>grep "status: partial" /home/ghislain/src/nicu-assistant/.planning/phases/58-release-v1-18-0/58-HUMAN-UAT.md</automated>
    Expected: 1 match

    <automated>grep -c "pending — CI" /home/ghislain/src/nicu-assistant/.planning/phases/58-release-v1-18-0/58-HUMAN-UAT.md</automated>
    Expected: 2 (one per deferred test item)

    <automated>grep "total: 2" /home/ghislain/src/nicu-assistant/.planning/phases/58-release-v1-18-0/58-HUMAN-UAT.md && grep "pending: 2" /home/ghislain/src/nicu-assistant/.planning/phases/58-release-v1-18-0/58-HUMAN-UAT.md</automated>
    Expected: both match
  </verify>
  <done>
    58-HUMAN-UAT.md exists with status: partial, 2 pending tests (Playwright E2E + extended axe), summary total:2/pending:2, and Notes explaining the CI deferral per D-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json version field → Vite build | Trusted: single string edit to a local file; no network call, no dependency resolution |
| pnpm install → pnpm-lock.yaml | Trusted: version-only bump; lockfile expected unchanged (Q3 verified) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03 | Tampering | package.json version edit | accept | Single string change to version field; no PII, no executable code paths. Clinical gate (svelte-check + vitest + build) validates no side effects. |
| T-58-04 | Information Disclosure | pnpm install | accept | Version-only bump; lockfile expected unchanged; no new packages installed. Package Legitimacy Audit not applicable (phase installs no new packages). |
| T-58-05 | Tampering | 58-HUMAN-UAT.md creation | accept | New planning artifact; no executable content; grep gate confirms correct status field. |
| T-58-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. pnpm install is a lockfile-sync step only; expected to make no changes. |

This is a version-bump + clinical-gate phase for a client-side offline-first PWA. No new code paths. No new persistence surface. No new external dependencies. The single source change is the package.json version field.
</threat_model>

<verification>
Final phase verification — run after both tasks are committed:

```bash
# REL-01: package.json reads 1.18.0
grep '"version"' /home/ghislain/src/nicu-assistant/package.json
# Expected: "version": "1.18.0"

# REL-01: no hardcoded version strings in src/
grep -rn '1\.17' /home/ghislain/src/nicu-assistant/src/
# Expected: zero matches

# REL-02 (doc side — from 58-01): v1.18 bullets in PROJECT.md
grep -c "v1.18 (Phase 5" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
# Expected: 4

# REL-02 (MILESTONES.md): v1.18 entry present
grep "v1.18 Persistence Seam" /home/ghislain/src/nicu-assistant/.planning/MILESTONES.md
# Expected: 1 match

# REL-03 (automated gates): all three must have passed cleanly during execution
# (verified by exit codes and output captured during Task 1)

# REL-03 (Playwright): captured in HUMAN-UAT.md — deferred to CI
grep "status: partial" /home/ghislain/src/nicu-assistant/.planning/phases/58-release-v1-18-0/58-HUMAN-UAT.md
# Expected: 1 match

# Confirm no git tag or push was run
git -C /home/ghislain/src/nicu-assistant tag | grep v1.18
# Expected: zero tags (tagging is a manual user step after milestone close)
```
</verification>

<success_criteria>
- `grep '"version"' package.json` outputs `  "version": "1.18.0",`
- `grep -rn '1\.17' src/` returns zero matches
- svelte-check exits 0/0 (run log in summary)
- vitest run exits 0 with count >= 451 (run log in summary)
- pnpm build exits 0 (run log in summary)
- 58-HUMAN-UAT.md exists with status: partial and pending: 2
- No git tag v1.18.0 was created (user manual step)
- No git push was run (user manual step)
- REQUIREMENTS.md traceability flip for REL-01..03 happens via `gsd-sdk query phase.complete "58"` (not a manual task in this plan)
</success_criteria>

<output>
Create `.planning/phases/58-release-v1-18-0/58-02-SUMMARY.md` when done.
</output>
