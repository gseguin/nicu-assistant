---
phase: 34-tech-debt-majors-release-v1.10
plan: 01
type: execute
wave: 1
depends_on: [32-01, 33-01]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - src/**/*.ts
  - src/**/*.svelte
  - .planning/PROJECT.md
  - .planning/phases/34-tech-debt-majors-release-v1.10/NOTES.md
autonomous: false
requirements: [DEBT-MAJ-01, DEBT-MAJ-02, REL-01, REL-02, REL-03]

must_haves:
  truths:
    - "@types/node is on v25 and svelte-check stays clean (0/0)"
    - "typescript is on v6 and full gate (svelte-check + vitest + pnpm build + Playwright axe 16/16) is green"
    - "package.json version is 1.10.0"
    - "Running app's AboutSheet renders v1.10.0 via __APP_VERSION__ (no source edit to about-content.ts)"
    - "PROJECT.md Validated list has v1.10 entries appended and retired entries struck through with a 'retired in v1.10' note"
  artifacts:
    - path: "package.json"
      provides: "Version 1.10.0, @types/node ^25.x, typescript ^6.x"
      contains: '"version": "1.10.0"'
    - path: ".planning/PROJECT.md"
      provides: "v1.10 Validated entries + strikethrough on retired items"
      contains: "v1.10"
    - path: ".planning/phases/34-tech-debt-majors-release-v1.10/NOTES.md"
      provides: "Decision log for any TS 6 strictness/deprecation findings (created only if TS 6 surfaces issues worth documenting or re-deferring)"
  key_links:
    - from: "package.json version"
      to: "AboutSheet v1.10.0"
      via: "vite.config.ts define __APP_VERSION__ -> src/lib/shared/about-content.ts"
      pattern: "__APP_VERSION__"
---

<objective>
Close v1.9's two deferred dep majors and ship v1.10 as a clean self-describing release.

Purpose: `@types/node` 22→25 and `typescript` 5→6 were explicitly deferred in Phase 30-02 per the plan's non_goals guardrail. Phase 34 picks them up atomically, then bumps the package version and updates PROJECT.md. This closes the v1.10 milestone.

Output:
- `@types/node` on v25, `typescript` on v6, full gate green
- `package.json` at 1.10.0, AboutSheet auto-reflecting v1.10.0 via `__APP_VERSION__`
- PROJECT.md Validated list updated with v1.10 entries + strikethrough on REF-01 and the Target-GIR-summary-hero portion of GIR-SWAP-01..03
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-impeccable-polish-tech-debt-sweep/30-02-SUMMARY.md
@.planning/phases/31-release-v1.9/31-01-SUMMARY.md
@package.json
@tsconfig.json
@vite.config.ts
@src/lib/shared/about-content.ts

<interfaces>
<!-- Key wiring the executor needs. No codebase exploration required. -->

From package.json (current state):
```json
"version": "1.9.0",
"devDependencies": {
  "@types/node": "^22.19.17",
  "typescript": "^5.9.3"
}
```

From vite.config.ts:11 (build-time version injection — unchanged from Phase 31):
```ts
define: {
  __APP_VERSION__: JSON.stringify(pkg.version)
}
```

From src/lib/shared/about-content.ts:12 (consumer — NO edits needed):
```ts
const appVersion = `v${typeof __APP_VERSION__ !== 'undefined' ? __APP_VERSION__ : '0.0.0'}`;
```
All three AboutSheet entries (morphine-wean, formula, gir) reference `appVersion` via the `version` field. A successful `pnpm build` is sufficient verification of REL-02 — exactly as Phase 31-01 did it.

From tsconfig.json (current include list — retained during Phase 30-02 to fix the `.svelte-kit/ambient.d.ts` override gotcha):
```json
"include": [
  "src/**/*",
  "vitest.config.ts",
  ".svelte-kit/ambient.d.ts",
  ".svelte-kit/non-ambient.d.ts",
  ".svelte-kit/types/**/$types.d.ts"
]
```
TS 6 MUST NOT regress this include list. If TS 6 surfaces new config requirements, update in-place; do NOT drop SvelteKit ambient includes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEBT-MAJ-01 — Bump @types/node 22 → 25</name>
  <files>package.json, pnpm-lock.yaml</files>
  <action>
Dev-only type package bump. Smallest blast radius — do this first as a sanity check that the worktree gate is green before touching TS.

Steps:
1. Baseline gate (must be green before starting):
   - `pnpm install` (ensure node_modules present — Phase 30-02 hit this on a fresh worktree)
   - `pnpm check` → expect 0 errors / 0 warnings
   - `pnpm test:run` → expect all passing
2. Bump: `pnpm add -D @types/node@^25` (latest within the v25 major)
3. Re-run gate: `pnpm check` (MUST stay 0/0) + `pnpm test:run` (MUST stay green)
4. If `pnpm check` surfaces new errors from v25 type changes (e.g. Node global typings narrowed, new DOM lib conflicts), fix them atomically in the same task. Do NOT proceed to Task 2 with a dirty svelte-check.
5. Commit atomically:
   `chore(34-01): DEBT-MAJ-01 — bump @types/node 22 → 25`

Why this is separate from Task 2: @types/node is dev-only and additive — it's a clean isolated bump. If Task 2 (TS 6) goes sideways, Task 1 stays committed and shippable.
  </action>
  <verify>
    <automated>pnpm check && pnpm test:run</automated>
  </verify>
  <done>
- `package.json` shows `"@types/node": "^25.x.x"`
- `pnpm-lock.yaml` updated
- `pnpm check` → 0 errors / 0 warnings
- `pnpm test:run` → all green
- Atomic commit on branch
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DEBT-MAJ-02 — Bump typescript 5 → 6</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json (if needed), src/**/*.ts (only if strictness forces), src/**/*.svelte (only if strictness forces), .planning/phases/34-tech-debt-majors-release-v1.10/NOTES.md (only if deferrals needed)</files>
  <behavior>
After this task:
- `typescript` resolves to ^6.x in `pnpm-lock.yaml`
- `pnpm check` (svelte-check) reports 0 errors / 0 warnings
- `pnpm test:run` (vitest 205/205 from v1.9 baseline) stays green
- `pnpm build` produces a valid PWA bundle
- Full Playwright + 16/16 axe sweep stays green
- `tsconfig.json` retains ALL SvelteKit ambient includes (see interfaces block)
  </behavior>
  <action>
Major version. This is the risk-bearing task in the plan. Execute methodically, one issue at a time.

**Step 1 — Bump:**
```
pnpm add -D typescript@^6
pnpm install
```

**Step 2 — Full gate (expect possible breakage):**
```
pnpm check        # svelte-check via TS 6
pnpm test:run     # vitest
pnpm build        # adapter-static + vite-pwa
pnpm exec playwright test   # full E2E incl. 16/16 axe sweeps
```

**Step 3 — Triage findings:**

For each new error / warning / deprecation surfaced by TS 6:

a. **Classify** (one line in NOTES.md if it's worth remembering):
   - *Trivial*: one-line type annotation fix, obvious deprecation swap (e.g. removed `{}` inference, stricter conditional types, narrower `unknown` defaults). Fix inline.
   - *Non-trivial*: >1-line change per file, multiple files affected, requires understanding a Svelte 5 runes type interaction, changes a public interface shape, or touches clinical calculation code. **STOP** and escalate (see Pause Criteria below).

b. **Fix trivially-classified items atomically.** One conceptual fix per commit if more than one file is touched. Re-run `pnpm check` after each fix to prevent cascade-debugging.

**Step 4 — Likely TS 6 surface areas to watch (prior-knowledge hints, not a checklist):**
   - Stricter `unknown` / `{}` inference in generic positions — may hit shared component prop generics
   - Tighter `satisfies` narrowing — may hit config JSON import sites (watch `gir-config.json`, `fortification-config.json`, `morphine-config.json` import boundaries)
   - Deprecation removals from TS 5.x — check for any `// @ts-expect-error TS5.x` or deprecated-API markers
   - `verbatimModuleSyntax: true` is already on — no new type-only import fallout expected
   - `.svelte-kit/ambient.d.ts` resolution — the Phase 30-02 include fix MUST survive. If TS 6 regresses this, re-apply the fix in tsconfig.json, do NOT drop the SvelteKit ambient includes.

**Step 5 — Address config-level changes if TS 6 requires them:**
   - If TS 6 deprecates any option currently in `tsconfig.json` (current set: `rewriteRelativeImportExtensions`, `allowJs`, `checkJs`, `esModuleInterop`, `forceConsistentCasingInFileNames`, `resolveJsonModule`, `skipLibCheck`, `sourceMap`, `strict`, `moduleResolution: bundler`, `verbatimModuleSyntax`, `target: ES2022`), update in place. Do NOT relax strictness to paper over errors.

**Step 6 — Final gate:**
```
pnpm check       # 0 errors / 0 warnings
pnpm test:run    # all passing
pnpm build       # PWA built, 35-ish precache entries
pnpm exec playwright test   # 16/16 axe green + E2E pass
```

**Step 7 — Commit strategy:**
   - `chore(34-01): DEBT-MAJ-02 — bump typescript 5 → 6` (the pure bump + lockfile)
   - Followed by one atomic commit per trivial fix: `fix(34-01): <one-line description> for TS 6 compatibility`
   - If any items are re-deferred per Pause Criteria, a final `docs(34-01): NOTES.md — TS 6 deferred items` commit

---

**Pause Criteria — STOP and escalate to the user if ANY of these are true:**

1. A single source file needs more than a one-line change to compile under TS 6.
2. The fix touches clinical calculation code (anything under `src/lib/gir/`, `src/lib/morphine-wean/`, `src/lib/formula/` — specifically the `*-config.json` / `*-utils.ts` / parity-tested math).
3. More than 5 files need edits to clear `pnpm check`.
4. `pnpm build` fails in a way not fixable by a type-only change (e.g. runtime behavior change, Vite-plugin incompat).
5. `@sveltejs/kit` or `svelte` itself surfaces TS-6-incompatible type declarations from the installed version, requiring a framework bump that wasn't planned.
6. Any Playwright E2E or axe assertion regresses post-TS-6 that can't be traced to a single obvious type-fallout line.

On pause: create `.planning/phases/34-tech-debt-majors-release-v1.10/NOTES.md` with:
- Exact TS version attempted
- Each blocking error verbatim (error code, file:line, message)
- Classification per error (trivial/non-trivial/blocking)
- Recommendation: fix-in-this-phase vs. re-defer-to-v2.0-experiment-phase
- Then ask the user for fix-vs-defer decision on each blocking item

On re-defer: revert the TS bump commit, leave Task 1 (@types/node 25) committed, proceed to Task 3 on TS 5.9. Record the deferral rationale in NOTES.md and mirror a short note in PROJECT.md Key Decisions (added under Task 5 — REL-03).
  </action>
  <verify>
    <automated>pnpm check && pnpm test:run && pnpm build && pnpm exec playwright test</automated>
  </verify>
  <done>
EITHER:
- (Happy path) `package.json` shows `"typescript": "^6.x.x"`, full gate green: svelte-check 0/0, vitest all passing, `pnpm build` ✓, Playwright 48/3/0 + 16/16 axe green. Commit(s) on branch.

OR:
- (Deferred path) TS 6 bump reverted after Pause Criteria triggered, NOTES.md documents exact blockers + rationale, user has been asked fix-vs-defer, and a deferral decision is recorded. Task 1's @types/node 25 bump remains committed. Proceed to Task 3 on TS 5.9.
  </done>
</task>

<task type="auto">
  <name>Task 3: REL-01 — Bump package.json version 1.9.0 → 1.10.0</name>
  <files>package.json</files>
  <action>
Single-line change: `"version": "1.9.0"` → `"version": "1.10.0"`. Same pattern as Phase 31-01 commit `87787f4`.

Do NOT edit `pnpm-lock.yaml` manually — the `"name": "nicu-assistant"` lockfile version field will update on the next install implicitly. If `pnpm-lock.yaml` shows a diff after editing `package.json`, stage that too.

Commit: `chore(34-01): REL-01 bump package.json version 1.9.0 → 1.10.0`
  </action>
  <verify>
    <automated>grep -q '"version": "1.10.0"' package.json</automated>
  </verify>
  <done>
- `package.json` line 4 shows `"version": "1.10.0"`
- Commit on branch
  </done>
</task>

<task type="auto">
  <name>Task 4: REL-02 — Verify AboutSheet reflects v1.10.0 via __APP_VERSION__</name>
  <files>(verification only — NO source edits)</files>
  <action>
**This task MUST NOT edit `src/lib/shared/about-content.ts`.** Phase 31-01 already verified that the AboutSheet version is sourced from `__APP_VERSION__` (Vite `define` injecting `pkg.version`). The Task 3 version bump propagates automatically.

Verification steps:
1. Run `pnpm build`. Expect it to complete successfully and produce a PWA bundle with ~35 precache entries.
2. Optionally, grep the built bundle for the version string to confirm injection:
   `grep -r "1.10.0" .svelte-kit/output/client/_app/immutable/ 2>/dev/null | head -3` — should find hits in the built JS chunks.
3. Optionally spot-check at runtime: `pnpm preview` → open app → tap the About button on any calculator → confirm it reads `v1.10.0`. (Optional — build success is sufficient per Phase 31 precedent.)

No commit (verification-only task).
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
- `pnpm build` completes successfully
- `src/lib/shared/about-content.ts` is UNCHANGED in git status (verification by `git diff --stat src/lib/shared/about-content.ts` showing no output)
- (Optional) grep of built bundle confirms 1.10.0 injection
  </done>
</task>

<task type="auto">
  <name>Task 5: REL-03 — Update PROJECT.md Validated list with v1.10 entries + strikethrough retired items</name>
  <files>.planning/PROJECT.md</files>
  <action>
Two edits to `.planning/PROJECT.md` Validated list section.

**Edit A — Strikethrough retired entries (modify existing lines):**

Find the v1.8 REF-01 entry. Current line (reference):
```
- ✓ GIR population reference card: IDM/LGA, IUGR, Preterm/NPO starting ranges (REF-01) — v1.8
```
Replace with:
```
- ~~✓ GIR population reference card: IDM/LGA, IUGR, Preterm/NPO starting ranges (REF-01) — v1.8~~ — retired in v1.10 (population reference card removed; Phase 32 GIR-SIMP-03)
```

Find the v1.9 GIR-SWAP-01..03 entry. Current line (reference):
```
- ✓ GIR titration hero swap: Δ rate (ml/hr ▲/▼ with increase/decrease label) is the bedside hero on every bucket card; GIR mg/kg/min demoted to the secondary row; neutral STOP-card treatment for the Δ=0 "current state" bucket (GIR-SWAP-01..03) — v1.9
```
Replace with:
```
- ✓ GIR titration hero swap: Δ rate (ml/hr ▲/▼ with increase/decrease label) is the bedside hero on every bucket card; GIR mg/kg/min demoted to the secondary row; neutral STOP-card treatment for the Δ=0 "current state" bucket (GIR-SWAP-01..03) — v1.9 — ~~Target GIR summary hero card portion~~ retired in v1.10 (summary hero card removed; grid-level Δ rate hero retained — Phase 32 GIR-SIMP-01)
```

(Note: the grid-level Δ rate hero STAYS — we only strikethrough the summary-hero-card portion, not the entire entry.)

**Edit B — Append v1.10 entries (add at end of Validated list, just before `### Active`):**

Append exactly these lines:

```
- ✓ GIR Simplification: Target GIR summary hero card removed; per-card Fluids|Rate|GIR secondary row removed; "Starting GIR by population" reference card removed; severe-neuro STOP treatment gated unconditionally on `bucketId === 'severe-neuro' || targetGirMgKgMin <= 0`; `aria-live` selection announcements dropped (redundant with visible Δ rate); click/tap visual treatment + radiogroup a11y preserved (GIR-SIMP-01..07) — v1.10
- ✓ GIR Dock Magnification: morphine-wean-style scroll-driven dock magnification ported to `GlucoseTitrationGrid.svelte` (MAX_SCALE 1.06, radius 2.5, rAF-throttled); mobile-only (`innerWidth < 768`) + `prefers-reduced-motion` guards; `MutationObserver` re-run on row changes; 16/16 axe sweeps remain green (GIR-DOCK-01..04) — v1.10
- ✓ Tech debt majors closed: `@types/node` 22 → 25, `typescript` 5 → 6 — full gate green (svelte-check 0/0, vitest all passing, `pnpm build` ✓, Playwright 16/16 axe green) (DEBT-MAJ-01, DEBT-MAJ-02) — v1.10
- ✓ `package.json` version bumped to 1.10.0; AboutSheet reflects v1.10.0 via the `__APP_VERSION__` build-time constant sourced from `package.json`; PROJECT.md Validated list updated with v1.10 entries and retired entries struck through (REL-01, REL-02, REL-03) — v1.10
```

**Edit C (conditional) — If Task 2 was re-deferred:**

Modify the third new entry (DEBT-MAJ-01/02) to reflect the partial closure:

```
- ✓ Tech debt majors — partial: `@types/node` 22 → 25 landed; `typescript` 5 → 6 re-deferred after TS 6 introduced non-trivial breakage (see `.planning/phases/34-tech-debt-majors-release-v1.10/NOTES.md` for exact blockers). Full gate otherwise green (DEBT-MAJ-01 closed, DEBT-MAJ-02 deferred) — v1.10
```

And add a Key Decisions row (in the same file, Key Decisions table):

```
| TS 6 re-deferred after Phase 34 attempt | <one-line rationale from NOTES.md> | Deferred — v1.10 / Phase 34-01 |
```

**Edit D — Update the "Last updated" footer line:**

Change the last line of the file from:
```
*Last updated: 2026-04-09 — v1.10 GIR Simplification + Dock + Tech Debt milestone started*
```
to:
```
*Last updated: 2026-04-09 — v1.10 GIR Simplification + Dock + Tech Debt milestone shipped*
```

Commit: `docs(34-01): REL-03 — PROJECT.md Validated list for v1.10 (append + strikethrough retired)`
  </action>
  <verify>
    <automated>grep -q "v1.10" .planning/PROJECT.md && grep -q "~~✓ GIR population reference card" .planning/PROJECT.md && grep -q "retired in v1.10" .planning/PROJECT.md</automated>
  </verify>
  <done>
- `.planning/PROJECT.md` Validated list contains 4 new v1.10 entries (or 4 with conditional Edit C if deferred)
- REF-01 line is struck through with strikethrough markdown + "retired in v1.10" note
- GIR-SWAP entry has the summary-hero-card portion struck through (not the whole entry)
- "Last updated" footer reflects "milestone shipped"
- Commit on branch
  </done>
</task>

</tasks>

<risk_and_checkpoints>

## Primary Risk: TS 6 Major Bump

TypeScript 6 is the only real unknown in this plan. Tasks 1, 3, 4, 5 are all mechanical and low-risk (@types/node is dev-only, version bump is one line, AboutSheet is verification-only, PROJECT.md is docs-only).

**Why `autonomous: false`:** Task 2's Pause Criteria explicitly allow the executor to stop and escalate. Setting `autonomous: false` lets the executor pause cleanly without breaking the run contract. If Task 2 sails through (likely — Svelte 5 runes + strict mode is already idiomatic and the tsconfig is already tight), no checkpoint is triggered and the plan runs through to completion.

## What to watch

1. **`.svelte-kit/ambient.d.ts` include override** — Phase 30-02 found and fixed this. TS 6 MUST NOT regress it. If `pnpm check` starts reporting `$app/state` / `$app/navigation` / `virtual:pwa-*` as missing modules, the fix is in the tsconfig `include` list, not in the source code.

2. **vite-plugin-pwa Vite 8 peer warning** — Already noisy in Phase 30-02. Not TS-related. Ignore.

3. **Clinical code preservation** — If TS 6 type strictness points at anything under `src/lib/gir/`, `src/lib/morphine-wean/`, or `src/lib/formula/` that would require changing runtime behavior, STOP. Type annotations can change; calculation math cannot.

4. **Ordering** — Task 1 (@types/node) before Task 2 (TS 6) so if TS 6 blows up, @types/node 25 is still shippable. Task 3 (version bump) AFTER both dep tasks so the release commit reflects the upgraded state. Task 5 (PROJECT.md) LAST so we can honestly claim the full gate stayed green.

</risk_and_checkpoints>

<verification>

**Per-task gates** are in each `<verify>` block. Phase-level final gate (run after Task 5 commits):

```bash
pnpm check                        # 0 errors / 0 warnings
pnpm test:run                     # all vitest passing (baseline 205/205 from v1.9)
pnpm build                        # PWA built, ~35 precache entries, v1.10.0 injected
pnpm exec playwright test         # 48 passed / 3 skipped / 0 failed, 16/16 axe green
```

All four MUST be green before declaring the phase shipped.

</verification>

<success_criteria>

- [ ] DEBT-MAJ-01: `@types/node` resolves to ^25.x in `pnpm-lock.yaml`; `pnpm check` 0/0
- [ ] DEBT-MAJ-02: `typescript` resolves to ^6.x in `pnpm-lock.yaml`; full gate green — OR — formally re-deferred with NOTES.md + Key Decisions entry and @types/node 25 still landed
- [ ] REL-01: `package.json` shows `"version": "1.10.0"`
- [ ] REL-02: `pnpm build` succeeds; `src/lib/shared/about-content.ts` unchanged in git (auto-propagation via `__APP_VERSION__`)
- [ ] REL-03: `.planning/PROJECT.md` has 4 new v1.10 Validated entries, REF-01 struck through with retirement note, GIR-SWAP entry has summary-hero-card portion struck through, "Last updated" footer updated
- [ ] Final gate: svelte-check 0/0, vitest all passing, `pnpm build` ✓, Playwright 16/16 axe green

</success_criteria>

<output>
After completion, create `.planning/phases/34-tech-debt-majors-release-v1.10/34-01-SUMMARY.md` per the summary template. Include:
- Commit hashes for each task (mirrors Phase 31-01 format)
- Final gate values (svelte-check, vitest, pnpm build, Playwright + axe)
- Per-requirement traceability table (DEBT-MAJ-01, DEBT-MAJ-02, REL-01, REL-02, REL-03) with evidence citations
- If TS 6 was re-deferred: full NOTES.md excerpt with the blocking errors and fix-vs-defer rationale
- Cross-cutting observations for the next milestone (v2.0 onwards)
</output>
