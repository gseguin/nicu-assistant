---
phase: 52-code-purge-test-suite-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pert/PertCalculator.svelte
  - src/lib/pert/PertCalculator.test.ts
  - src/lib/pert/PertInputs.svelte
  - src/lib/pert/PertInputs.test.ts
  - src/lib/pert/calculations.ts
  - src/lib/pert/calculations.test.ts
  - src/lib/pert/calculator.ts
  - src/lib/pert/config.ts
  - src/lib/pert/config.test.ts
  - src/lib/pert/state.svelte.ts
  - src/lib/pert/state.test.ts
  - src/lib/pert/types.ts
  - src/lib/pert/pert-config.json
  - src/lib/pert/pert-parity.fixtures.json
  - src/routes/pert/+page.svelte
  - e2e/pert.spec.ts
  - e2e/pert-a11y.spec.ts
  - src/lib/shell/registry.ts
  - src/lib/shell/NavShell.test.ts
autonomous: true
requirements:
  - PURGE-01
  - PURGE-02
  - PURGE-03
  - PURGE-06
  - TEST-01
  - TEST-08
must_haves:
  truths:
    - "src/lib/pert/ directory does not exist (all 14 files gone)"
    - "src/routes/pert/+page.svelte does not exist; navigating to /pert returns SvelteKit not-found fallback"
    - "e2e/pert.spec.ts and e2e/pert-a11y.spec.ts do not exist"
    - "CALCULATOR_REGISTRY exports 5 entries in alphabetical order (feeds, fortification, gir, morphine, uacUvc)"
    - "src/lib/shell/registry.ts no longer imports pertModule"
    - "NavShell.test.ts no longer asserts PERT in desktop nav (T-07/T-08/T-09)"
    - "vitest pnpm test:run exits 0 at commit boundary"
    - "pnpm check reports 0 errors at commit boundary"
  artifacts:
    - path: "src/lib/shell/registry.ts"
      provides: "CALCULATOR_REGISTRY with 5 alphabetical entries"
      contains: "feedsModule"
      forbids: "pertModule"
    - path: "src/lib/shell/NavShell.test.ts"
      provides: "desktop nav assertions for 5-calculator world"
      forbids: "PERT"
  key_links:
    - from: "src/lib/shell/registry.ts"
      to: "src/lib/shell/NavShell.svelte + src/lib/shell/HamburgerMenu.svelte"
      via: "CALCULATOR_REGISTRY iteration"
      pattern: "CALCULATOR_REGISTRY"
    - from: "src/lib/shell/NavShell.test.ts"
      to: "src/lib/shell/registry.ts"
      via: "render assertions on tabs.length and tabs[N].textContent"
      pattern: "tabs\\[\\d\\]\\.textContent"
---

<objective>
Delete the PERT calculator slice in full and remove its registry entry in a single atomic commit. The deletion + registry edit + NavShell.test.ts surgery MUST land together so vitest stays green at the commit boundary (per D-02 + Risk R-1 in 52-RESEARCH.md).

Purpose: Remove all source code for the out-of-clinical-scope PERT (Pediatric Enzyme Replacement Therapy) calculator. PERT is pediatric, not neonatal — it does not belong in this product.

Output: Repo has 5 calculators (feeds, formula, gir, morphine-wean, uac-uvc); `src/lib/pert/`, `src/routes/pert/`, `e2e/pert.spec.ts`, `e2e/pert-a11y.spec.ts` gone; `CALCULATOR_REGISTRY` length is 5; vitest + Playwright green modulo pre-existing baseline failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-code-purge-test-suite-repair/52-CONTEXT.md
@.planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md
@.planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md

<interfaces>
<!-- Post-edit shape of src/lib/shell/registry.ts (per RESEARCH §2.1 + PATTERNS §Per-File): -->

```typescript
import type { CalculatorEntry } from './calculator-module.js';
import { feedsModule } from '$lib/feeds/calculator.js';
import { fortificationModule } from '$lib/fortification/calculator.js';
import { girModule } from '$lib/gir/calculator.js';
import { morphineModule } from '$lib/morphine/calculator.js';
import { uacUvcModule } from '$lib/uac-uvc/calculator.js';

// Same alphabetical-by-id order as before — D-19 invariant guarded by
// src/lib/shell/__tests__/registry.test.ts.
export const CALCULATOR_REGISTRY: readonly CalculatorEntry[] = [
  feedsModule,
  fortificationModule,
  girModule,
  morphineModule,
  uacUvcModule
];
```

<!-- NavShell.test.ts has assertions that PERT is the 5th tab (`tabs[4]`) and that `tabs.length === 6`. The instant pertModule leaves the registry, these tests turn red. Edit MUST land in this commit. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the entire src/lib/pert/ slice (14 files)</name>
  <files>src/lib/pert/PertCalculator.svelte, src/lib/pert/PertCalculator.test.ts, src/lib/pert/PertInputs.svelte, src/lib/pert/PertInputs.test.ts, src/lib/pert/calculations.ts, src/lib/pert/calculations.test.ts, src/lib/pert/calculator.ts, src/lib/pert/config.ts, src/lib/pert/config.test.ts, src/lib/pert/state.svelte.ts, src/lib/pert/state.test.ts, src/lib/pert/types.ts, src/lib/pert/pert-config.json, src/lib/pert/pert-parity.fixtures.json</files>
  <read_first>
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §1 (exhaustive file list with LOC counts)
    - .planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md §"Files to DELETE" (14-row table)
    - .planning/phases/52-code-purge-test-suite-repair/52-CONTEXT.md D-02 (atomicity requirement)
  </read_first>
  <action>Delete the entire src/lib/pert/ directory and all 14 files within it via `rm -r src/lib/pert/` (PURGE-01). After deletion, `ls src/lib/pert/` MUST fail with "No such file or directory". This includes 5 co-located *.test.ts files (PertCalculator.test.ts, PertInputs.test.ts, calculations.test.ts, config.test.ts, state.test.ts) per the co-located test convention (D-14 + PATTERNS §E). Do NOT use `git rm` — use plain `rm -r` so that the staging happens via the subsequent `git add -A` in the commit step.</action>
  <verify>
    <automated>! test -d src/lib/pert/ && ! test -f src/lib/pert/calculator.ts && ! test -f src/lib/pert/state.svelte.ts</automated>
  </verify>
  <done>The src/lib/pert/ directory and all 14 files are deleted from the working tree.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the /pert route + 2 e2e specs</name>
  <files>src/routes/pert/+page.svelte, e2e/pert.spec.ts, e2e/pert-a11y.spec.ts</files>
  <read_first>
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §1 (route + e2e LOC counts)
    - .planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md §Inverse Pattern (route deletion is the inverse of route addition; SvelteKit auto-generates 404 for unknown routes)
  </read_first>
  <action>Delete `src/routes/pert/+page.svelte` and the parent directory `src/routes/pert/` via `rm -r src/routes/pert/` (PURGE-02). Delete `e2e/pert.spec.ts` and `e2e/pert-a11y.spec.ts` via `rm` (TEST-01). After these deletions, SvelteKit will automatically return its not-found fallback for any navigation to `/pert` (no additional fallback wiring needed — adapter-static + 200.html fallback handles this).</action>
  <verify>
    <automated>! test -d src/routes/pert/ && ! test -f e2e/pert.spec.ts && ! test -f e2e/pert-a11y.spec.ts</automated>
  </verify>
  <done>All three files (route + 2 e2e specs) and the `src/routes/pert/` directory are removed.</done>
</task>

<task type="auto">
  <name>Task 3: Remove pertModule from src/lib/shell/registry.ts</name>
  <files>src/lib/shell/registry.ts</files>
  <read_first>
    - src/lib/shell/registry.ts (lines 12-29 inclusive — see RESEARCH §2.1 post-edit shape)
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §2.1 (exact line numbers + post-edit snippet)
    - .planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md §"src/lib/shell/registry.ts" (Per-File Pattern Assignments)
  </read_first>
  <action>Edit `src/lib/shell/registry.ts` (PURGE-03): (a) DELETE line 17 entirely — `import { pertModule } from '$lib/pert/calculator.js';`; (b) DELETE line 27 entirely — `  pertModule,` from the `CALCULATOR_REGISTRY` array literal. Post-edit, the file MUST match the snippet in RESEARCH.md §2.1: 5 imports (feeds, fortification, gir, morphine, uac-uvc in alphabetical order by module variable name), preserved comment block "Same alphabetical-by-id order as before — D-19 invariant…", and `CALCULATOR_REGISTRY` containing exactly `[feedsModule, fortificationModule, girModule, morphineModule, uacUvcModule]`. No trailing-comma cleanup needed (`uacUvcModule` is the last entry and already has no trailing comma per Risk R-5).</action>
  <verify>
    <automated>! grep -q "pertModule" src/lib/shell/registry.ts && grep -cE '^\s*(feedsModule|fortificationModule|girModule|morphineModule|uacUvcModule),?$' src/lib/shell/registry.ts | grep -q '^5$'</automated>
  </verify>
  <done>`pertModule` import and array entry deleted; the 5 remaining modules in alphabetical-by-variable-name order; `pnpm check` reports 0 errors against this file.</done>
</task>

<task type="auto">
  <name>Task 4: Edit src/lib/shell/NavShell.test.ts to remove PERT assertions (R-1 critical fix)</name>
  <files>src/lib/shell/NavShell.test.ts</files>
  <read_first>
    - src/lib/shell/NavShell.test.ts (lines 16, 162, 191-197, 209, 223 — see RESEARCH §2.12)
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §2.12 + §8 Risk R-1 (this file is NOT in CONTEXT.md's TEST list but MUST be edited in 52-01 or vitest goes red instantly when pertModule leaves the registry)
    - .planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md §"src/lib/shell/NavShell.test.ts" + §B (Registry-Test Renumber Pattern, applied here to nav-tab assertions)
  </read_first>
  <action>Apply 7 surgical edits to `src/lib/shell/NavShell.test.ts` per RESEARCH.md §2.12:

(a) Line 16 — Comment generalize: replace `// D-20 (pert workstream Phase 1): defaults are alphabetical first 4 registry entries.` with `// D-20: defaults are alphabetical first 4 registry entries.`

(b) Line 162 — Comment generalize: replace `// D-21 (pert workstream Phase 1): recover() no longer re-sorts by registry order.` with `// D-21: recover() no longer re-sorts by registry order.`

(c) Line 191 (T-07 desktop tab count) — change `expect(tabs).toHaveLength(6);` to `expect(tabs).toHaveLength(5);`

(d) Line 196 — DELETE entirely: `expect(tabs[4].textContent).toMatch(/PERT/i);`

(e) Line 197 — renumber `tabs[5]` to `tabs[4]`: `expect(tabs[4].textContent).toMatch(/UAC/i);`

(f) Line 209 (T-08 desktopTabs count) — change `expect(desktopTabs).toHaveLength(6);` (the comment `// NAV-ALL-01: registry-driven, immune to favorites` is preserved) to `expect(desktopTabs).toHaveLength(5);`

(g) Line 223 (T-09 desktopTabs count) — change `expect(desktopTabs).toHaveLength(6);` (the comment `// edge case: favorites empty, desktop still full` is preserved) to `expect(desktopTabs).toHaveLength(5);`

Per D-13 (scrub ALL 'PERT'/'pert' from active test files): after these 7 edits, this file MUST contain ZERO matches for word-boundary `pert|PERT`.</action>
  <verify>
    <automated>! grep -iwE 'pert|PERT' src/lib/shell/NavShell.test.ts && pnpm test:run src/lib/shell/NavShell.test.ts 2>&1 | grep -qE '(Tests:.*0 failed|✓|passed)'</automated>
  </verify>
  <done>Zero PERT references in NavShell.test.ts (word-boundary grep returns nothing); the file's vitest cases pass against the 5-calculator registry.</done>
</task>

<task type="auto">
  <name>Task 5: Verify vitest + svelte-check green at commit boundary</name>
  <files></files>
  <read_first>
    - .planning/phases/52-code-purge-test-suite-repair/52-CONTEXT.md D-02 + D-05 (every commit boundary green)
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §5 (expected post-52-01 vitest: 489 − 56 = 433 tests passing in 42 files; svelte-check 0/0)
  </read_first>
  <action>Run `pnpm test:run` and confirm exit code 0 with all suites green. Expected count post-52-01: 433 tests in 42 test files (489 baseline minus 56 deleted PERT tests; the +1 favorites T-21 regression test arrives in Plan 52-03, not here). Run `pnpm check` and confirm 0 errors / 0 warnings. If either gate fails, STOP — do not commit. The most likely failure modes per RESEARCH §6: (a) a stray import of `pertModule` somewhere not caught by greps — fix it; (b) a vitest case referencing PERT that was missed — note it in the SUMMARY and either fix here or defer to 52-03 with explicit justification. The commit MUST be green.</action>
  <verify>
    <automated>pnpm test:run 2>&1 | tail -20 && pnpm check 2>&1 | tail -5</automated>
  </verify>
  <done>Vitest exits 0 (433 tests passing in 42 files); svelte-check reports 0 errors / 0 warnings.</done>
</task>

</tasks>

<verification>
- All 14 files under `src/lib/pert/` deleted; directory does not exist.
- `src/routes/pert/+page.svelte` and the parent directory deleted.
- `e2e/pert.spec.ts` and `e2e/pert-a11y.spec.ts` deleted.
- `src/lib/shell/registry.ts` has no `pertModule` import or array entry; 5 entries remain in alphabetical-by-variable-name order.
- `src/lib/shell/NavShell.test.ts` contains zero PERT references (word-boundary grep).
- `pnpm test:run` exits 0 with 433 tests passing in 42 files.
- `pnpm check` reports 0 errors / 0 warnings.
- Commit message references PERT explicitly per D-17 (e.g., "remove PERT calculator source + registry + NavShell test surgery (52-01)").
</verification>

<success_criteria>
- PURGE-01 satisfied: `src/lib/pert/` directory does not exist.
- PURGE-02 satisfied: `src/routes/pert/+page.svelte` does not exist; navigating to `/pert` returns SvelteKit not-found fallback at preview time.
- PURGE-03 (partial — registry edit) satisfied: `src/lib/shell/registry.ts` no longer imports `pertModule`; `CALCULATOR_REGISTRY` array has 5 entries in alphabetical order. (Type-union + about-content edits land in 52-02; full PURGE-03 closure attestation happens at phase boundary.)
- PURGE-06 (partial — no orphaned consumer breaks) satisfied: removing `pertModule` cascades cleanly to NavShell/HamburgerMenu without runtime errors. (`.identity-pert` CSS removal lands in 52-02.)
- TEST-01 satisfied: both PERT e2e specs deleted.
- TEST-08 (partial — baseline preserved) satisfied: Playwright spec count drops by 32 (16 per project); pre-existing baseline failures unchanged.
- Atomic green commit: vitest + svelte-check both green at the commit boundary (D-02 + D-05).
</success_criteria>

<output>
Create `.planning/phases/52-code-purge-test-suite-repair/52-01-SUMMARY.md` documenting:
- Total LOC removed (~2,654 per RESEARCH §1).
- Vitest delta (489 → 433 = −56).
- Playwright delta (264 → 232 = −32 for the 2 deleted specs; the further −2 from drawer-no-autofocus iteration arrives in 52-03).
- Any deviations from RESEARCH.md §2.1 / §2.12 (none expected; flag if found).
- Confirmation that NavShell.test.ts (R-1) was edited in this plan rather than deferred to 52-03.
</output>
</content>
