---
phase: 52-code-purge-test-suite-repair
plan: 03
type: execute
wave: 3
depends_on:
  - 52-02
files_modified:
  - src/lib/shell/__tests__/registry.test.ts
  - src/lib/shell/HamburgerMenu.test.ts
  - src/lib/shell/CalculatorPage.test.ts
  - src/lib/shell/calculator-store.test.ts
  - src/lib/shared/favorites.test.ts
  - e2e/desktop-full-nav.spec.ts
  - e2e/drawer-no-autofocus.spec.ts
autonomous: true
requirements:
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
  - TEST-07
  - TEST-08
must_haves:
  truths:
    - "registry.test.ts asserts 5 calculators (no PERT entry test) with UAC/UVC at index [4]"
    - "HamburgerMenu.test.ts asserts 5 menu links (no PERT link assertion)"
    - "CalculatorPage.test.ts uses identity-gir / 'GIR inputs' as test scaffold (no identity-pert / 'PERT inputs')"
    - "favorites.test.ts has new T-21 regression test using generic literal 'unknown-calculator-id'"
    - "desktop-full-nav.spec.ts asserts tab count 5 (no PERT tab assertion at nth(4))"
    - "drawer-no-autofocus.spec.ts ROUTES array has 5 entries (no /pert)"
    - "calculator-store.test.ts it() description no longer mentions PERT"
    - "Zero PERT word-boundary matches across src/ and e2e/ (excluding the 17 deleted files which no longer exist)"
    - "vitest pnpm test:run exits 0 with 434 tests passing (433 + new T-21)"
    - "Playwright pnpm exec playwright test runs both chromium + webkit-iphone with no PERT-specific failures"
  artifacts:
    - path: "src/lib/shared/favorites.test.ts"
      provides: "T-21 regression test for unknown-calculator-id forward compatibility"
      contains: "unknown-calculator-id"
    - path: "src/lib/shell/__tests__/registry.test.ts"
      provides: "registry tests aligned with 5-calculator world; UAC/UVC asserted at index [4]"
      forbids: "'pert'"
    - path: "e2e/drawer-no-autofocus.spec.ts"
      provides: "ROUTES array with 5 calculator routes"
      forbids: "/pert"
  key_links:
    - from: "src/lib/shared/favorites.test.ts T-21"
      to: "src/lib/shared/favorites.svelte.ts recover() filter"
      via: "localStorage seed -> init() -> assert current[]"
      pattern: "unknown-calculator-id"
    - from: "e2e/desktop-full-nav.spec.ts tab count assertions"
      to: "src/lib/shell/registry.ts CALCULATOR_REGISTRY"
      via: "rendered NavShell with 5 tabs"
      pattern: "toHaveCount\\(5\\)"
---

<objective>
Surgical edits to 7 test files: drop PERT-specific assertions, fix the 6 -> 5 count-bumps, renumber successor indices (UAC/UVC moves from [5] to [4]), substitute test scaffold values from `identity-pert` / `'PERT inputs'` to `identity-gir` / `'GIR inputs'`, and append a new T-21 favorites regression test using the generic literal `'unknown-calculator-id'` per D-11.

Purpose: Complete the test-file scrub (D-13) so the grep gate at phase boundary returns zero PERT hits in active code, and add the D-11 regression guard for users whose v1.15+ localStorage contained `'pert'` as a favorite.

Output: Vitest 434/434 (433 + new T-21); Playwright runs both projects with no PERT-specific failures; the grep gate `git grep -niwE 'pert|PERT' -- ':(exclude).planning/'` returns only the 2 allow-listed top-level docs (CLAUDE.md + PRODUCT.md, deferred to Phase 53 per Risk R-3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-code-purge-test-suite-repair/52-CONTEXT.md
@.planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md
@.planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md
@.planning/phases/52-code-purge-test-suite-repair/52-02-SUMMARY.md

<interfaces>
T-07 (behavioural twin of the new T-21) lives in src/lib/shared/favorites.test.ts lines 64-74. Use it as the template for T-21: same setItem-then-import-then-init-then-assert shape. T-21 differs from T-07 by (a) explicit name + JSDoc citing D-11 + Phase 52; (b) generic literal 'unknown-calculator-id' instead of 'ghost'; (c) 4-id array (matches realistic favorites max). STORAGE_KEY, vi, and dynamic import of favorites.svelte.js are already in scope per T-07 and T-09.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: registry.test.ts — drop "fifth entry" PERT block + renumber UAC + narrow expected array</name>
  <files>src/lib/shell/__tests__/registry.test.ts</files>
  <read_first>
    - src/lib/shell/__tests__/registry.test.ts (lines 10, 46-59 inclusive — see RESEARCH §2.6 + PATTERNS §B)
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §2.6 (exact line edits)
    - .planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md §B (Registry-Test Renumber Pattern with full concrete excerpt)
  </read_first>
  <action>Three structural edits (TEST-03, per PATTERNS §B):

(a) Line 10 — narrow the expected-ids array. Replace `expect(ids).toEqual(['feeds', 'formula', 'gir', 'morphine-wean', 'pert', 'uac-uvc']);` with `expect(ids).toEqual(['feeds', 'formula', 'gir', 'morphine-wean', 'uac-uvc']);`

(b) Lines 46-52 — DELETE the entire `it('includes PERT calculator as fifth entry', ...)` block (8 LOC including the closing `});`). The block contains 5 expect() calls against CALCULATOR_REGISTRY[4] for id 'pert', label 'PERT', href '/pert', description 'Pediatric EPI PERT calculator', and identityClass 'identity-pert'. Remove all of it.

(c) Lines 54-59 (post-delete: now ~46-51) — renumber UAC/UVC "sixth" -> "fifth" AND `[5]` -> `[4]` everywhere in this `it()` block:
- `it('includes UAC/UVC calculator as sixth entry', ...)` -> `it('includes UAC/UVC calculator as fifth entry', ...)`
- All 4 `expect(CALCULATOR_REGISTRY[5].…)` -> `expect(CALCULATOR_REGISTRY[4].…)` (id, label, href, identityClass).

Per D-06 (surgical posture): do NOT refactor to data-driven assertion. Keep the it()-per-ordinal-entry shape; just shrink it by one.</action>
  <verify>
    <automated>! grep -iwE 'pert|PERT' src/lib/shell/__tests__/registry.test.ts && grep -q "CALCULATOR_REGISTRY\[4\].id).toBe('uac-uvc')" src/lib/shell/__tests__/registry.test.ts && pnpm test:run src/lib/shell/__tests__/registry.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Zero PERT references in registry.test.ts; UAC/UVC asserted at index [4] with "fifth entry" description; the file's vitest cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: HamburgerMenu.test.ts — drop PERT link assertion + fix count + generalize comment block</name>
  <files>src/lib/shell/HamburgerMenu.test.ts</files>
  <read_first>
    - src/lib/shell/HamburgerMenu.test.ts (lines 37-50 — see RESEARCH §2.7 + PATTERNS §"src/lib/shell/HamburgerMenu.test.ts")
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §2.7 (exact line edits)
    - .planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md §A (Comment Generalization concrete example)
  </read_first>
  <action>Three edits (TEST-04, per PATTERNS §A + §D):

(a) Lines 37-39 — Replace the 4-line comment block currently reading "beforeEach already called favorites.init() with defaults. After D-19/D-20 (pert workstream Phase 1), the registry is alphabetized to 6 entries: feeds, formula, gir, morphine-wean, pert, uac-uvc. The default favorites are the first 4 alphabetical entries; pert and uac-uvc remain non-favorited." with the generalized 4-line version: "beforeEach already called favorites.init() with defaults. The registry is alphabetized to 5 entries: feeds, formula, gir, morphine-wean, uac-uvc. The default favorites are the first 4 alphabetical entries; uac-uvc remains non-favorited." Preserve leading tab indentation; preserve intent — defaults computation rationale.

(b) Line 44 — change `expect(screen.getAllByRole('link')).toHaveLength(6);` to `expect(screen.getAllByRole('link')).toHaveLength(5);`

(c) Line 49 — DELETE entirely the assertion `expect(screen.getByRole('link', { name: /PERT/i })).toBeTruthy();` The surviving 5 `getByRole('link', { name: /…/i })` assertions for Morphine, Formula, GIR, Feeds, UAC/UVC are the shape.

Post-edit: this file MUST contain ZERO PERT word-boundary matches.</action>
  <verify>
    <automated>! grep -iwE 'pert|PERT' src/lib/shell/HamburgerMenu.test.ts && grep -q "toHaveLength(5)" src/lib/shell/HamburgerMenu.test.ts && pnpm test:run src/lib/shell/HamburgerMenu.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Zero PERT references; link count assertion is 5; vitest passes for this file.</done>
</task>

<task type="auto">
  <name>Task 3: CalculatorPage.test.ts — swap identity-pert -> identity-gir and 'PERT inputs' -> 'GIR inputs'</name>
  <files>src/lib/shell/CalculatorPage.test.ts</files>
  <read_first>
    - src/lib/shell/CalculatorPage.test.ts (lines 45, 70, 72, 117, 119 — see RESEARCH §2.8 + PATTERNS §C)
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §2.8 (5 exact line edits with rationale for choosing GIR)
    - .planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md §C (Sibling-Substitution Pattern: use a real, surviving calculator's identifier — GIR specifically because its identity class follows the canonical `.identity-{slug}` pattern and the label is unambiguous)
  </read_first>
  <action>Five mechanical replacements (TEST-05, per PATTERNS §C):

(a) Line 45 — `    identityClass: 'identity-pert',` -> `    identityClass: 'identity-gir',`

(b) Line 70 — `    const mod = makeModule({ identityClass: 'identity-pert' });` -> `    const mod = makeModule({ identityClass: 'identity-gir' });`

(c) Line 72 — `    expect(container.querySelector('.identity-pert')).not.toBeNull();` -> `    expect(container.querySelector('.identity-gir')).not.toBeNull();`

(d) Line 117 — `    const mod = makeModule({ inputsLabel: 'PERT inputs' });` -> `    const mod = makeModule({ inputsLabel: 'GIR inputs' });`

(e) Line 119 — `    const aside = screen.getByRole('complementary', { name: 'PERT inputs' });` -> `    const aside = screen.getByRole('complementary', { name: 'GIR inputs' });`

Rationale for GIR (recorded for SUMMARY): the substitution exercises the same `.identity-{slug}` CSS contract against a real surviving identity hue (avoids adding a synthetic test class to app.css). `identity-gir` and `'GIR inputs'` both exist on a real surviving calculator and grep back to `src/lib/gir/calculator.ts`.</action>
  <verify>
    <automated>! grep -iwE 'pert|PERT' src/lib/shell/CalculatorPage.test.ts && [ $(grep -c "identity-gir" src/lib/shell/CalculatorPage.test.ts) -eq 3 ] && [ $(grep -c "'GIR inputs'" src/lib/shell/CalculatorPage.test.ts) -eq 2 ] && pnpm test:run src/lib/shell/CalculatorPage.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Zero PERT references; 3 occurrences of `identity-gir` and 2 of `'GIR inputs'`; vitest passes for this file.</done>
</task>

<task type="auto">
  <name>Task 4: calculator-store.test.ts — generalize it() description (D-13)</name>
  <files>src/lib/shell/calculator-store.test.ts</files>
  <read_first>
    - src/lib/shell/calculator-store.test.ts (line 54)
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §3.7 (single-line edit)
    - .planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md §A (Comment Generalization)
  </read_first>
  <action>Single edit per D-13: at line 54, replace `it('round-trips via vi.resetModules() + dynamic import (mirrors PERT pattern)', async () => {` with `it('round-trips via vi.resetModules() + dynamic import', async () => {`. The parenthetical `(mirrors PERT pattern)` leaves; the test body is unchanged. Post-edit, this file MUST have ZERO PERT word-boundary matches.</action>
  <verify>
    <automated>! grep -iwE 'pert|PERT' src/lib/shell/calculator-store.test.ts && pnpm test:run src/lib/shell/calculator-store.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Zero PERT references; vitest passes for this file.</done>
</task>

<task type="auto">
  <name>Task 5: favorites.test.ts — fix comment + add T-21 regression test (D-11)</name>
  <files>src/lib/shared/favorites.test.ts</files>
  <read_first>
    - src/lib/shared/favorites.test.ts (lines 64-74 for T-07 template; line 119 for the comment fix; line ~207 for T-21 append location)
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §2.9 (T-21 body verbatim + rationale Option A)
    - .planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md §F (Favorites Regression Test concrete excerpt)
    - .planning/phases/52-code-purge-test-suite-repair/52-CONTEXT.md D-11 (generic literal 'unknown-calculator-id' MUST be used, NOT 'pert')
  </read_first>
  <action>Two edits (TEST-06 comment + T-21 addition per D-11):

(a) Line 119 — In the body of T-11 (`it('T-11 toggle: add to favorites (under cap)', ...)`), update the post-D-19 alphabetization listing comment: replace `// post-D-19 alphabetization that's: feeds, formula, gir, morphine-wean, pert, uac-uvc.` with `// post-D-19 alphabetization that's: feeds, formula, gir, morphine-wean, uac-uvc.` (preserves leading 3-tab indent).

(b) After T-20 (around line 207, at the appropriate top-level location after the last `describe` or `it` closes), APPEND a new T-21 describe block per PATTERNS §F. Structure (adjust tab indentation to match the surrounding file convention):

- Outer `describe('T-21 — unknown-calculator-id forward compatibility regression', () => { ... })`.
- Inner `it('T-21: unknown id (e.g. a removed calculator) is silently filtered, preserving order of valid ids', async () => { ... })`.
- Body, in order: a JSDoc-style block comment citing "D-11 (Phase 52): regression guard for users whose stored favorites included a calculator id that was later removed from the registry. The recover() filter MUST drop the unknown id without error and MUST preserve the order of the surviving valid ids. Uses a generic literal so this test stays feature-agnostic across future removals."
- `localStorage.setItem(STORAGE_KEY, JSON.stringify({ v: 1, ids: ['morphine-wean', 'formula', 'unknown-calculator-id', 'gir'] }));`
- `vi.resetModules();`
- `const { favorites } = await import('./favorites.svelte.js');`
- `favorites.init();`
- `expect([...favorites.current]).toEqual(['morphine-wean', 'formula', 'gir']);`

CRITICAL per D-11: the literal MUST be `'unknown-calculator-id'`, NOT `'pert'`. This keeps the grep gate clean AND makes the test feature-agnostic for future calculator removals. Verify `STORAGE_KEY`, `vi`, and `import('./favorites.svelte.js')` are already in scope (they are — T-07 + T-09 use the exact same imports). Post-edit, this file MUST have ZERO PERT word-boundary matches.</action>
  <verify>
    <automated>! grep -iwE 'pert|PERT' src/lib/shared/favorites.test.ts && grep -q "unknown-calculator-id" src/lib/shared/favorites.test.ts && grep -q "T-21" src/lib/shared/favorites.test.ts && pnpm test:run src/lib/shared/favorites.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Zero PERT references; T-21 describe block present using generic 'unknown-calculator-id' literal; vitest passes for this file (+1 new test vs pre-52-03).</done>
</task>

<task type="auto">
  <name>Task 6: desktop-full-nav.spec.ts — drop PERT tab assertion + count-bumps</name>
  <files>e2e/desktop-full-nav.spec.ts</files>
  <read_first>
    - e2e/desktop-full-nav.spec.ts (lines 16, 28, 33-34, 57, 79, 94 — see RESEARCH §2.10)
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §2.10 (all 7 edits enumerated; D-07 implementer's call resolved to "delete line 33 + renumber line 34")
    - .planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md §"e2e/desktop-full-nav.spec.ts" + §B + §D
  </read_first>
  <action>Seven edits (TEST-08 spec-side; D-07 resolved per RESEARCH recommendation):

(a) Line 16 — update describe/test title: change `'NAV-ALL-TEST-01a: desktop @1280 renders all 6 calculators in registry (alphabetical) order'` to `'NAV-ALL-TEST-01a: desktop @1280 renders all 5 calculators in registry (alphabetical) order'`

(b) Line 28 — `await expect(tabs).toHaveCount(6);` -> `await expect(tabs).toHaveCount(5);`

(c) Line 33 — DELETE entirely: `await expect(tabs.nth(4)).toContainText('PERT');` (per D-07, the recommended phrasing is delete + renumber successor)

(d) Line 34 (now at line 33 after delete) — renumber: change `await expect(tabs.nth(5)).toContainText('UAC/UVC');` to `await expect(tabs.nth(4)).toContainText('UAC/UVC');`

(e) Line 57 — `await expect(desktopNav.getByRole('tab')).toHaveCount(6);` -> `await expect(desktopNav.getByRole('tab')).toHaveCount(5);`

(f) Line 79 — `await expect(desktopNav.getByRole('tab')).toHaveCount(6);` -> `await expect(desktopNav.getByRole('tab')).toHaveCount(5);`

(g) Line 94 — `await expect(desktopNav.getByRole('tab')).toHaveCount(6);` -> `await expect(desktopNav.getByRole('tab')).toHaveCount(5);`

NOTE on line 63: do NOT edit it — that's a mobile favorites count assertion (favorites are 4-capped, NOT registry-driven), so the number does not change with PERT removal (confirmed in RESEARCH §2.10 Edit 5).

Post-edit, this file MUST contain ZERO PERT word-boundary matches.</action>
  <verify>
    <automated>! grep -iwE 'pert|PERT' e2e/desktop-full-nav.spec.ts && [ $(grep -c "toHaveCount(5)" e2e/desktop-full-nav.spec.ts) -eq 4 ] && grep -q "tabs.nth(4)).toContainText('UAC/UVC')" e2e/desktop-full-nav.spec.ts</automated>
  </verify>
  <done>Zero PERT references; 4 occurrences of `toHaveCount(5)` (replacing 4 prior `toHaveCount(6)`); UAC/UVC asserted at `tabs.nth(4)`.</done>
</task>

<task type="auto">
  <name>Task 7: drawer-no-autofocus.spec.ts — drop /pert from ROUTES + generalize comments</name>
  <files>e2e/drawer-no-autofocus.spec.ts</files>
  <read_first>
    - e2e/drawer-no-autofocus.spec.ts (lines 4-5, 10-11, 14-21 — see RESEARCH §2.11)
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §2.11 (all 3 edits)
    - .planning/phases/52-code-purge-test-suite-repair/52-PATTERNS.md §A (Comment Generalization) + §"e2e/drawer-no-autofocus.spec.ts"
  </read_first>
  <action>Three edits (TEST-02, per PATTERNS §A + array-drop):

(a) Lines 4-5 — update header count comment. Replace the 2-line tail currently reading "both `chromium` and `webkit-iphone` projects (Phase 47 D-15 — both projects run all e2e specs by default). 6 routes x 2 projects = 12 cases." with the same text but ending "5 routes x 2 projects = 10 cases." (Update only the trailing arithmetic; preserve the rest of the header block.)

(b) Lines 10-11 — generalize the cautionary comment. Replace the current 2-line block "IMPORTANT: ROUTES lists ALL SIX calculators (mobile-nav-clearance.spec.ts omits /pert; we must NOT inherit that omission, FOCUS-03 requires all six covered)." with "IMPORTANT: ROUTES lists EVERY active calculator route — FOCUS-03 requires every drawer-bearing route to be covered, no omissions." (2-line block, preserves leading `//` slashes and indentation per file convention.)

(c) Line 20 — DELETE entirely the `'/pert'` entry from the ROUTES array. Post-edit, the array MUST contain exactly the 5 entries `'/morphine-wean'`, `'/formula'`, `'/gir'`, `'/feeds'`, `'/uac-uvc'` (one per line, with the final entry having no trailing comma if the existing array style elsewhere in e2e/ uses that convention; otherwise leave a trailing comma — match Prettier behavior of the file).

Post-edit, this file MUST contain ZERO PERT word-boundary matches.</action>
  <verify>
    <automated>! grep -iwE 'pert|PERT' e2e/drawer-no-autofocus.spec.ts && [ $(grep -cE "^\s*'/(morphine-wean|formula|gir|feeds|uac-uvc)'" e2e/drawer-no-autofocus.spec.ts) -eq 5 ] && ! grep -q "/pert" e2e/drawer-no-autofocus.spec.ts</automated>
  </verify>
  <done>Zero PERT references; ROUTES array has exactly 5 entries; no /pert anywhere in the file.</done>
</task>

<task type="auto">
  <name>Task 8: Verify full grep gate + vitest 434/434 + Playwright baseline</name>
  <files></files>
  <read_first>
    - .planning/phases/52-code-purge-test-suite-repair/52-CONTEXT.md D-13, D-18, D-19, D-20, D-21 (grep gate + verification expectations)
    - .planning/phases/52-code-purge-test-suite-repair/52-RESEARCH.md §4 (post-Phase-52 expected grep output: zero hits in src/ and e2e/), §5 (vitest 434, Playwright 230), §8 Risk R-3 (CLAUDE.md + PRODUCT.md allow-listed)
  </read_first>
  <action>Final phase-boundary verification:

(a) Run the D-19 word-boundary grep gate: `git grep -niwE 'pert|PERT' -- ':(exclude).planning/' ':(exclude)milestones/'`. Expected result: ONLY 2 hits, both in `CLAUDE.md` and `PRODUCT.md` (top-level docs deferred to Phase 53 per Risk R-3 — these are the allow-listed residuals). If ANY hit appears in `src/` or `e2e/`, STOP — that's a regression; fix it before committing.

(b) Run `pnpm test:run`. Expected: 434 tests passing in 42 test files (433 from end of 52-02 + 1 new T-21 = 434). If the count differs, audit the SUMMARY for the discrepancy.

(c) Run `pnpm check`. Expected: 0 errors / 0 warnings.

(d) Run `pnpm build`. Expected: build succeeds; `du -sh build/` shows ~745-755 KB (down from 772 KB baseline; build/pert.html no longer present).

(e) Run `pnpm exec playwright test --list`. Expected: total test count drops by 32 spec deletions + 2 route iterations vs baseline 264 (post: 230 tests across both projects).

(f) Run `pnpm exec playwright test` (full Playwright run; LONG, allow ~10-15min if needed). Per D-21, the failure count MUST be ≤ 32 (the pre-existing baseline). PERT-specific failures should drop to 0. If failure count > 32, that's a regression introduced by Phase 52 — investigate and fix before committing.

Document all 6 results in the SUMMARY per D-20 attestation requirements.</action>
  <verify>
    <automated>git grep -niwE 'pert|PERT' -- ':(exclude).planning/' ':(exclude)milestones/' | grep -vE '^(CLAUDE\.md|PRODUCT\.md):' | wc -l | grep -q '^0$' && pnpm test:run 2>&1 | tail -5 && pnpm check 2>&1 | tail -5</automated>
  </verify>
  <done>Grep gate returns only CLAUDE.md + PRODUCT.md (2 allow-listed residual hits); vitest 434/434; svelte-check 0/0; pnpm build succeeds with bundle shrinkage in the 15-25KB range; Playwright failure count did not increase from 32 baseline.</done>
</task>

</tasks>

<verification>
- registry.test.ts has zero PERT references; UAC/UVC asserted at index [4]; expected ids array has 5 entries.
- HamburgerMenu.test.ts has zero PERT references; link count is 5; comment block generalized.
- CalculatorPage.test.ts uses `identity-gir` (3 sites) and `'GIR inputs'` (2 sites); zero PERT references.
- calculator-store.test.ts it() description scrubbed of PERT.
- favorites.test.ts has T-21 describe block using generic `'unknown-calculator-id'` literal; line 119 comment updated; zero PERT references.
- desktop-full-nav.spec.ts has 4 `toHaveCount(5)` (replacing 4 `toHaveCount(6)`); tabs.nth(4) asserts UAC/UVC; zero PERT references.
- drawer-no-autofocus.spec.ts ROUTES array has 5 entries; zero `/pert`; comments generalized.
- D-19 grep gate `git grep -niwE 'pert|PERT' -- ':(exclude).planning/' ':(exclude)milestones/'` returns ONLY CLAUDE.md and PRODUCT.md (Phase 53 scope, allow-listed).
- `pnpm test:run` exits 0 with 434 tests passing in 42 files (+1 from T-21 vs 52-02 close).
- `pnpm check` reports 0 errors / 0 warnings.
- `pnpm build` succeeds; `du -sh build/` shows shrinkage from 772 KB baseline.
- `pnpm exec playwright test` total count drops to ~230 (264 baseline − 32 spec deletions − 2 route iterations); failure count ≤ 32 (D-21 baseline preserved).
- Commit message references PERT explicitly per D-17 (e.g., "scrub PERT test references + add T-21 regression test (52-03)").
</verification>

<success_criteria>
- TEST-02 satisfied: `e2e/drawer-no-autofocus.spec.ts` ROUTES iterates the 5 remaining calculator routes; no `/pert`.
- TEST-03 satisfied: `registry.test.ts` "fifth entry" PERT block deleted; alphabetical list narrowed; total registry length expected = 5.
- TEST-04 satisfied: `HamburgerMenu.test.ts` PERT assertion removed; comment block + count updated.
- TEST-05 satisfied: `CalculatorPage.test.ts` test scaffold uses real surviving calculator's identity class (`identity-gir`) and inputsLabel (`'GIR inputs'`); no `identity-pert` references.
- TEST-06 satisfied: `favorites.test.ts` comment fixed; T-21 regression test added using generic `'unknown-calculator-id'` literal (per D-11) — covers the SAFE-02 intent for Phase 53.
- TEST-07 satisfied: `pnpm test:run` exits 0; all suites green at 434/434.
- TEST-08 satisfied: `pnpm exec playwright test` runs both chromium + webkit-iphone with no PERT-specific failures; pre-existing baseline unchanged.
- Phase 52 boundary closure: grep gate clean (only 2 allow-listed top-level docs remain, deferred to Phase 53); test suite green; build succeeds; bundle shrinkage measurable; D-20 attestation data captured in SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/52-code-purge-test-suite-repair/52-03-SUMMARY.md` documenting:
- The 7 test-file edits applied (registry.test.ts, HamburgerMenu.test.ts, CalculatorPage.test.ts, calculator-store.test.ts, favorites.test.ts +T-21, desktop-full-nav.spec.ts, drawer-no-autofocus.spec.ts).
- The 6 phase-boundary verification results per D-20 attestation:
  1. Vitest delta: end-of-52-02 433 -> end-of-52-03 434 (+1 from T-21). Baseline 489 - PERT 56 + T-21 1 = 434. MATCH.
  2. D-19 grep gate result: enumerate the residual hits (expected: CLAUDE.md + PRODUCT.md only; both Phase 53 scope).
  3. `pnpm build`: succeeded; duration; bundle size delta.
  4. `pnpm check`: 0 errors / 0 warnings.
  5. `du -sh build/`: post-52 size vs 772 KB baseline.
  6. Playwright failure count: baseline 32 vs post-52 count (D-21: must NOT increase).
- D-07 resolution: which option was chosen for desktop-full-nav.spec.ts line 33 (recommendation was delete + renumber successor; document the actual chosen approach).
- Any deviations from RESEARCH.md §2.6 - §2.11 (none expected; flag if found).
- Phase 52 final state: ready for Phase 53 (Favorites Safety Net + Verification).
</output>
</content>
