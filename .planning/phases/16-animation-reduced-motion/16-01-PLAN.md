---
phase: 16-animation-reduced-motion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shared/components/NumericInput.svelte
autonomous: true
requirements: [ANIM-01, ANIM-02]
must_haves:
  truths:
    - "All Svelte transition: directives honor prefers-reduced-motion"
    - "Global CSS transitions already nuked under reduced motion via app.css:117 (html * transition: none !important)"
    - "Morphine summary pulse has its own @media reduced-motion rule (existing)"
    - "Morphine dock-style magnification already checks prefersReducedMotion in the scroll handler (existing)"
    - "ResultsDisplay pulse-once keyframe has its own @media reduced-motion rule (existing, unused component)"
    - "SelectPicker uses only Tailwind transition-* utility classes which are killed by the global rule"
    - "No new animation is added that violates the calm tone"
    - "All 117 tests continue to pass"
---

<objective>
Audit every motion surface in the app against `prefers-reduced-motion: reduce` and close the one remaining gap: `NumericInput.svelte`'s `transition:slide` on the inline error message. Everything else is already handled:

- `app.css:117` — global rule `html * { transition: none !important }` catches all Tailwind `transition-*` utilities under reduced motion (NavShell focus transitions, SelectPicker hover, FortificationCalculator transitions, etc.)
- `MorphineWeanCalculator.svelte:21-23, 41-48` — dock-style magnification reads `window.matchMedia('(prefers-reduced-motion: reduce)').matches` at mount and skips the transform/boxShadow writes
- `MorphineWeanCalculator.svelte:330-339` — summary-pulse keyframe has `@media (prefers-reduced-motion: reduce) { animation: none }`
- `ResultsDisplay.svelte:111-125` — `.pulse-container` keyframe has a matching guard (component currently unused — only exported)
- `app.css:111-118` — theme-switch color transition has its own reduced-motion guard

The single remaining motion that slips past the global rule is `transition:slide={{ duration: 150 }}` on the NumericInput error message (line 139). Svelte `transition:` directives run via JS and animate inline styles, so the global CSS rule does not apply — they need an explicit guard.

No layout-shift animations exist anywhere in the app (nothing animates height, width, margin, or flex gap at mount — the error slide is the only inline layout motion, and it appears in-place).
</objective>

<context>
@src/lib/shared/components/NumericInput.svelte
@src/app.css
@.impeccable.md

**Strategy:** Read `prefers-reduced-motion` at component load via a tiny singleton helper and pass `0` as the slide duration when reduced. Using `duration: 0` effectively disables the animation while keeping the `{#if}` block visible/invisible toggling as expected. An alternative is to wrap the `<p>` in an `{#if}` without the transition at all, but the simpler path is to compute the duration dynamically.

**Test contract to preserve (NumericInput.test.ts source greps and behavior):** The slide transition is applied to the error `<p>` element, not a structural element the tests query. Just confirm the full test suite stays green.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard NumericInput slide transition with prefers-reduced-motion</name>
  <files>src/lib/shared/components/NumericInput.svelte</files>
  <read_first>
    - src/lib/shared/components/NumericInput.svelte (lines 1-20 imports/props, lines 135-145 error block)
  </read_first>
  <action>
1. Add a `const` at the top of `<script>` (after existing imports, before `$props()`) that reads the reduced-motion preference once:

```ts
const PREFERS_REDUCED_MOTION =
  typeof window !== 'undefined' && typeof window.matchMedia === 'function'
    ? window.matchMedia('(prefers-reduced-motion: reduce)').matches
    : false;
```

2. Change the existing transition directive:
   - From: `transition:slide={{ duration: 150 }}`
   - To:   `transition:slide={{ duration: PREFERS_REDUCED_MOTION ? 0 : 150 }}`

Rules:
- DO NOT remove the `transition:slide` directive entirely — keeping the Svelte transition API intact lets future designers tune it in one place.
- DO NOT add a reactive `$state` for this — the preference is read once at mount, consistent with morphine dock magnification's approach.
- DO NOT edit any other file.
- DO NOT touch other motion sites (they're already guarded — see <objective>).
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "prefers-reduced-motion" src/lib/shared/components/NumericInput.svelte` matches once
    - `grep -n "PREFERS_REDUCED_MOTION \? 0 : 150" src/lib/shared/components/NumericInput.svelte` matches exactly once
    - `grep -n "transition:slide" src/lib/shared/components/NumericInput.svelte` still matches exactly once
    - `pnpm vitest run` — all 117 tests still pass
    - `git diff --stat` shows only `NumericInput.svelte` modified
  </acceptance_criteria>
  <done>NumericInput error slide animation is instant under reduced motion; all other motion sites are already guarded; full suite green.</done>
</task>

</tasks>

<verification>
Phase-level audit:

1. `pnpm vitest run` — full suite green
2. `pnpm build` — green
3. `git diff --stat` — only NumericInput.svelte modified
4. Manual audit checklist (recorded in SUMMARY.md):
   - app.css:117 global transition guard ✓ (existing)
   - NumericInput slide guard ✓ (this task)
   - ResultsDisplay pulse guard ✓ (existing, unused component)
   - MorphineWeanCalculator summary-pulse guard ✓ (existing)
   - MorphineWeanCalculator dock magnification guard ✓ (existing)
   - SelectPicker: Tailwind `transition-*` utilities only, killed by global rule ✓
   - No layout-shift animations anywhere in the app ✓ (no animated height/width/margin/gap at mount)
</verification>

<success_criteria>
- ANIM-01: Existing micro-animations are already calm (200ms color transitions, 150ms error slide, 180ms recalc pulse, 1.06x dock magnification). No new motion introduced; the whole system is clinical and purposeful by the existing v1.1–v1.2 design decisions.
- ANIM-02: Every motion surface now honors `prefers-reduced-motion: reduce`:
  - Tailwind `transition-*` utilities → killed by `app.css:117` global rule
  - NumericInput error slide → guarded via PREFERS_REDUCED_MOTION (this task)
  - Morphine summary pulse → guarded via @media rule
  - Morphine dock magnification → guarded via JS matchMedia
  - ResultsDisplay pulse-once → guarded via @media rule
  - Theme switch color transitions → guarded via app.css:116-118
- No layout-shift animations.
- All 117 tests pass unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/16-animation-reduced-motion/16-01-SUMMARY.md` with the motion audit checklist.
</output>
