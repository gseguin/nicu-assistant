---
phase: 03-calculators
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/pert/DosingCalculator.svelte
  - src/routes/pert/+page.svelte
autonomous: true
requirements:
  - PERT-01
  - PERT-02
  - PERT-03
  - PERT-04
  - PERT-05
  - CC-01
  - CC-02
  - CC-03

must_haves:
  truths:
    - "PERT meal mode accepts fat grams + lipase rate + brand/strength and produces capsule count"
    - "PERT tube-feed mode has independent inputs and produces its own capsule count"
    - "Switching between meal and tube-feed tabs preserves both states"
    - "All 5 FDA brands (Creon, Zenpep, Pancreaze, Pertzye, Viokace) appear in meal mode brand picker"
    - "Tube-feed mode shows Pancreaze 2600/37000 and Pertzye 4000/24000 strengths not present in meal mode"
    - "Empty or invalid fat grams shows inline error and blocks calculation"
    - "No BUILD_FLAGS or branded footer code exists in the ported component"
  artifacts:
    - path: "src/lib/pert/DosingCalculator.svelte"
      provides: "Full PERT calculator UI — meal and tube-feed modes"
    - path: "src/routes/pert/+page.svelte"
      provides: "PERT route page — mounts DosingCalculator, sets context, inits state"
  key_links:
    - from: "src/routes/pert/+page.svelte"
      to: "src/lib/pert/state.svelte.ts"
      via: "pertState.init() in onMount"
      pattern: "pertState\\.init"
    - from: "src/routes/pert/+page.svelte"
      to: "src/lib/shared/context.ts"
      via: "setCalculatorContext in onMount"
      pattern: "setCalculatorContext"
    - from: "src/lib/pert/DosingCalculator.svelte"
      to: "src/lib/pert/dosing.ts"
      via: "calculateCapsules call"
      pattern: "calculateCapsules"
    - from: "src/lib/pert/DosingCalculator.svelte"
      to: "src/lib/shared/components/SelectPicker.svelte"
      via: "brand and strength dropdowns"
      pattern: "SelectPicker"
    - from: "src/lib/pert/DosingCalculator.svelte"
      to: "src/lib/shared/components/NumericInput.svelte"
      via: "fat grams input"
      pattern: "NumericInput"
    - from: "src/lib/pert/DosingCalculator.svelte"
      to: "src/lib/shared/components/ResultsDisplay.svelte"
      via: "capsule count output"
      pattern: "ResultsDisplay"
---

<objective>
Port the PERT dosing calculator from the standalone app into the unified app, refactoring in a single pass to use Phase 2 shared components and the state module created in Plan 01.

Purpose: Delivers full PERT feature parity — meal mode, tube-feed mode, independent state per mode, keyboard navigation, all 5 FDA brands — as a working calculator on the /pert route.

Output: DosingCalculator.svelte + updated +page.svelte
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-calculators/03-CONTEXT.md
@.planning/phases/03-calculators/03-RESEARCH.md
@.planning/phases/03-calculators/03-01-SUMMARY.md

<interfaces>
<!-- Phase 2 shared components — use these, do not recreate -->
From src/lib/shared/components/SelectPicker.svelte:
Props: options: SelectOption[], value: string (bindable), label?: string, class?: string
SelectOption = { value: string; label: string; group?: string }

From src/lib/shared/components/NumericInput.svelte:
Props: value: number | null (bindable), label?: string, placeholder?: string, suffix?: string,
       error?: string, min?: number, max?: number, step?: number, id?: string

From src/lib/shared/components/ResultsDisplay.svelte:
Props: primaryValue: string, primaryUnit: string, primaryLabel: string,
       secondaryValue?: string, secondaryUnit?: string, secondaryLabel?: string,
       isVisible?: boolean, accentVariant?: 'clinical' | 'bmf'

From src/lib/shared/context.ts:
setCalculatorContext({ id: 'pert', accentColor: 'var(--color-accent)' })

From src/lib/pert/state.svelte.ts (created in Plan 01):
pertState.current  — { activeMode: 'meal'|'tube-feed', meal: {...}, tubeFeed: {...} }
pertState.init()   — call in onMount
pertState.persist() — call in $effect on state change
pertState.reset()  — clears state and sessionStorage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port DosingCalculator.svelte to src/lib/pert/</name>
  <read_first>
    - /mnt/data/src/pert-calculator/src/lib/components/DosingCalculator.svelte
    - src/lib/pert/dosing.ts
    - src/lib/pert/medications.ts
    - src/lib/pert/tube-feed/medications.ts
    - src/lib/pert/state.svelte.ts
    - src/lib/shared/components/SelectPicker.svelte
    - src/lib/shared/components/NumericInput.svelte
    - src/lib/shared/components/ResultsDisplay.svelte
    - src/app.css
  </read_first>
  <files>src/lib/pert/DosingCalculator.svelte</files>
  <action>
    Port the ~977-line DosingCalculator.svelte from /mnt/data/src/pert-calculator/src/lib/components/DosingCalculator.svelte into src/lib/pert/DosingCalculator.svelte. Perform the refactor in a single pass (per D-02). Do not copy-then-refactor.

    **Changes to make during the port:**

    1. REMOVE BUILD_FLAGS entirely:
       - Remove `import { BUILD_FLAGS, getCalculatorFeatureVisibility } from '$lib/build-flags'`
       - Remove `footerVisible` and `footerHidden` state variables
       - Remove any template block that conditionally renders a branded footer
       - This is safe — non-clinical promotional element

    2. ADAPT import paths:
       - `from '$lib/dosing'` → `from '$lib/pert/dosing'`
       - `from '$lib/medications'` → `from '$lib/pert/medications'`
       - `from '$lib/clinical-config'` → `from '$lib/pert/clinical-config'`
       - `from '$lib/tube-feed/clinical-data'` → `from '$lib/pert/tube-feed/clinical-data'`
       - `from '$lib/tube-feed/medications'` → `from '$lib/pert/tube-feed/medications'`

    3. REPLACE local SelectPicker/NumericInput with shared components:
       - Remove any `import SelectPicker from './SelectPicker.svelte'` or local clone
       - `import SelectPicker from '$lib/shared/components/SelectPicker.svelte'`
       - `import NumericInput from '$lib/shared/components/NumericInput.svelte'`
       - `import ResultsDisplay from '$lib/shared/components/ResultsDisplay.svelte'`
       - Adapt usage to match Phase 2 API:
         - SelectPicker: use `options` (SelectOption[]) and `bind:value`; brand options must use `value` field (not `id`)
         - NumericInput: use `bind:value` (number|null), `error`, `suffix`, `min`, `max`, `step`
         - ResultsDisplay: use `primaryValue/primaryUnit/primaryLabel` and `secondaryValue/secondaryUnit/secondaryLabel`; set `isVisible` when result is ready

    4. WIRE to pertState module (NOT local $state for mode and inputs — state must survive navigation):
       - Mode tabs read/write `pertState.current.activeMode`
       - Meal inputs read/write `pertState.current.meal.*`
       - Tube-feed inputs read/write `pertState.current.tubeFeed.*`
       - Add `$effect(() => { pertState.persist(); })` to persist on any state change
       - Do NOT call `pertState.init()` here — that is done in the route page's onMount

    5. PRESERVE from source (do not change logic):
       - calculateCapsules() call signature
       - validateFatGrams() call
       - getStrengths() helper that branches on mode (meal uses medications.ts, tube-feed uses tube-feed/medications.ts)
       - Keyboard navigation on mode tabs (ArrowLeft/Right/Home/End)
       - Lipase rate display (units/kg/meal or units/kg/feed labels)
       - Error display for invalid inputs

    6. NO hardcoded colors:
       - No `oklch(...)`, `bg-white`, `text-slate-*`, `bg-clinical-*`, `text-clinical-*`, `border-clinical-*`
       - Use: `text-[var(--color-text-primary)]`, `text-[var(--color-text-secondary)]`, `bg-[var(--color-surface)]`, `bg-[var(--color-surface-card)]`, `bg-[var(--color-surface-alt)]`, `border-[var(--color-border)]`, `bg-[var(--color-accent)]`, `text-[var(--color-accent)]`

    Keep the component as a single file (do not split sub-components unless it aids readability at discretion). The goal is correctness and feature parity, not brevity.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm exec tsc --noEmit 2>&1 | grep -E "DosingCalculator|pert/" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/pert/DosingCalculator.svelte exists
    - `grep -n "build-flags\|footerVisible\|footerHidden" src/lib/pert/DosingCalculator.svelte` returns zero matches
    - `grep -n "bg-white\|text-slate-\|bg-clinical-\|text-clinical-\|border-clinical-\|oklch(" src/lib/pert/DosingCalculator.svelte` returns zero matches
    - `grep -n "from '@lucide/svelte'\|@lucide/svelte" src/lib/pert/DosingCalculator.svelte` confirms lucide imports use @lucide/svelte (not lucide-svelte)
    - `grep -n "pertState\|pertState\.persist" src/lib/pert/DosingCalculator.svelte` — references to pertState exist
    - TypeScript reports zero errors for this file
  </acceptance_criteria>
  <done>DosingCalculator.svelte ported with all refactors applied; no BUILD_FLAGS; no hardcoded colors; uses Phase 2 shared components; wired to pertState; zero TS errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PERT route page</name>
  <read_first>
    - src/routes/pert/+page.svelte
    - src/lib/pert/state.svelte.ts
    - src/lib/shared/context.ts
    - src/lib/pert/DosingCalculator.svelte
  </read_first>
  <files>src/routes/pert/+page.svelte</files>
  <action>
    Replace the skeleton content in src/routes/pert/+page.svelte with the real PERT calculator.

    The page must:
    1. Import `onMount` from 'svelte'
    2. Import `setCalculatorContext` from '$lib/shared/context.js'
    3. Import `pertState` from '$lib/pert/state.svelte.js'
    4. Import `DosingCalculator` from '$lib/pert/DosingCalculator.svelte'

    In onMount (per Pattern 3 from RESEARCH.md):
    ```typescript
    onMount(() => {
      setCalculatorContext({
        id: 'pert',
        accentColor: 'var(--color-accent)'
      });
      pertState.init();
    });
    ```

    Template: render DosingCalculator inside the existing max-w-md container. Keep the `<svelte:head>` title tag: "PERT Dosing | NICU Assistant".

    Remove all skeleton animated divs and the "coming in Phase 3" notice.

    The page itself does NOT manage any inputs or results — that is all DosingCalculator's responsibility.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm exec tsc --noEmit 2>&1 | grep -E "routes/pert" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pertState.init\|setCalculatorContext\|onMount" src/routes/pert/+page.svelte` confirms all three are present
    - `grep -n "Phase 3\|animate-pulse\|skeleton" src/routes/pert/+page.svelte` returns zero matches (skeleton removed)
    - `grep -n "DosingCalculator" src/routes/pert/+page.svelte` confirms component is imported and used
    - pnpm exec tsc --noEmit reports zero errors for this file
  </acceptance_criteria>
  <done>PERT route page replaces skeleton with DosingCalculator; onMount sets context and inits state; zero TS errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm exec tsc --noEmit` — zero errors in pert/ and routes/pert/
2. `pnpm build` — exits 0
3. `grep -rn "build-flags\|footerVisible" src/lib/pert/` — zero matches
4. `grep -rn "bg-white\|text-slate-\|bg-clinical-\|oklch(" src/lib/pert/DosingCalculator.svelte` — zero matches
5. `grep -rn "from 'lucide-svelte'" src/lib/pert/` — zero matches (must be @lucide/svelte)
6. `pnpm dev` — /pert route renders PERT calculator (not skeleton)
</verification>

<success_criteria>
- /pert route shows full PERT calculator with meal and tube-feed tabs
- Meal mode: all 5 FDA brands available; capsule count appears when fat grams, lipase rate, brand, and strength are entered
- Tube-feed mode: independent inputs; Pancreaze/Pertzye have additional tube-feed strengths
- Invalid fat grams shows inline error from validateFatGrams()
- No hardcoded colors, no BUILD_FLAGS, no skeleton content
- pnpm build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-calculators/03-02-SUMMARY.md`
</output>
