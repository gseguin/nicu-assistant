---
phase: 03-calculators
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/lib/pert/__tests__/dosing.test.ts
  - src/lib/formula/__tests__/formula.test.ts
autonomous: false
requirements:
  - PERT-01
  - PERT-02
  - PERT-04
  - PERT-05
  - FORM-01
  - FORM-02
  - FORM-03
  - FORM-04
  - FORM-05
  - CC-01
  - CC-02
  - CC-03

must_haves:
  truths:
    - "calculateCapsules() produces correct output for all 5 FDA brands in meal and tube-feed mode"
    - "calculateRecipe() produces correct water mL and powder grams for representative brands"
    - "calculateBMF() produces correct EBM mL and powder grams; returns null when targetKcalOz <= baselineKcalOz"
    - "Switching PERT to formula to PERT via nav does not clear either calculator's inputs"
    - "Switching formula tabs (modified to BMF) does not clear the other mode's inputs"
    - "Both calculators show inline errors for invalid numeric inputs and block calculation"
    - "Both calculators render in dark mode with no hardcoded colors"
  artifacts:
    - path: "src/lib/pert/__tests__/dosing.test.ts"
      provides: "Unit tests for calculateCapsules and validateFatGrams"
    - path: "src/lib/formula/__tests__/formula.test.ts"
      provides: "Unit tests for calculateRecipe, calculateBMF, validateRecipeInputs, validateBMFInputs"
  key_links:
    - from: "src/lib/pert/__tests__/dosing.test.ts"
      to: "src/lib/pert/dosing.ts"
      via: "direct import of calculation functions"
      pattern: "calculateCapsules"
    - from: "src/lib/formula/__tests__/formula.test.ts"
      to: "src/lib/formula/formula.ts"
      via: "direct import of calculation functions"
      pattern: "calculateRecipe|calculateBMF"
---

<objective>
Add unit tests for both calculators' pure business logic functions, then perform a manual visual verification of the complete app — both calculators functional, state preserved across navigation, dark mode correct.

Purpose: Establishes a regression safety net for the clinical calculation logic and confirms the full Phase 3 goal is achieved through human verification of a working app.

Output: 2 test files + human sign-off on the complete calculator experience
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-calculators/03-CONTEXT.md
@.planning/phases/03-calculators/03-02-SUMMARY.md
@.planning/phases/03-calculators/03-03-SUMMARY.md

<interfaces>
<!-- PERT calculation functions under test -->
From src/lib/pert/dosing.ts:
  calculateCapsules(totalLipase: number, formulaDefinition: FormulaDefinition): number
  calculateTotalLipase(fatGrams: number, lipaseUnitsPerGram: number): number
  validateFatGrams(raw: string): string | null  // returns error string or null

<!-- Formula calculation functions under test -->
From src/lib/formula/formula.ts:
  calculateRecipe(brand: BrandConfig, targetKcalOz: number, volumeMl: number): FormulaResult
  calculateBMF(brand: BrandConfig, targetKcalOz: number, volumeMl: number, baselineKcalOz: number): BMFResult | null
  validateRecipeInputs(brandId: string|null, targetKcalOz: number|null, volumeMl: number|null): ValidationResult
  validateBMFInputs(brandId: string|null, targetKcalOz: number|null, volumeMl: number|null, baselineKcalOz: number|null): ValidationResult
  calculateScoops(g_powder: number, brand: BrandConfig): number | null
  calculatePackets(g_powder: number, packetLabel: string): number | null

<!-- Vitest test placement (from Phase 2 decisions) -->
Test files go in src/lib/**/__tests__/ to match vitest include pattern
Import test utilities from 'vitest': { describe, it, expect, beforeEach }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for PERT and formula business logic</name>
  <read_first>
    - src/lib/pert/dosing.ts
    - src/lib/pert/medications.ts
    - src/lib/pert/clinical-config.json
    - src/lib/pert/tube-feed/medications.ts
    - src/lib/formula/formula.ts
    - src/lib/formula/formula-config.ts
    - src/lib/formula/formula-config.json
  </read_first>
  <files>
    src/lib/pert/__tests__/dosing.test.ts,
    src/lib/formula/__tests__/formula.test.ts
  </files>
  <behavior>
    PERT dosing.test.ts:
    - validateFatGrams('') returns non-null error string (empty input is invalid)
    - validateFatGrams('0') returns non-null error string (zero grams is invalid)
    - validateFatGrams('15') returns null (valid)
    - validateFatGrams('abc') returns non-null error string (non-numeric is invalid)
    - calculateTotalLipase(15, 2000) returns 30000 (15g fat x 2000 units/g)
    - calculateCapsules with Creon 6000 and totalLipase 30000 returns correct capsule count (derive expected value from clinical-config.json formula)
    - calculateCapsules with a tube-feed brand/strength combination returns a positive integer capsule count (smoke test)

    formula.test.ts:
    - validateRecipeInputs(null, 24, 100) has brandId error
    - validateRecipeInputs('enfamil-ar', null, 100) has targetKcalOz error
    - validateRecipeInputs('enfamil-ar', 24, 100) all fields null (valid)
    - calculateRecipe(brand, 24, 100) result has mL_water > 0, g_powder > 0
    - validateBMFInputs('similac-hm-fortifier', 24, 100, null) has baselineKcalOz error
    - calculateBMF(bmfBrand, 24, 100, 20) result has mL_ebm > 0, g_powder > 0 (targetKcalOz 24 > baseline 20)
    - calculateBMF(bmfBrand, 20, 100, 24) returns null (targetKcalOz 20 <= baseline 24)
    - calculateScoops does not throw and returns number or null for any brand with valid g_powder
  </behavior>
  <action>
    Create src/lib/pert/__tests__/dosing.test.ts:
    - Import { describe, it, expect } from 'vitest'
    - Import calculation functions from '$lib/pert/dosing'
    - Import MEDICATIONS or CLINICAL_CONFIG as needed to get a real FormulaDefinition for Creon 6000
    - Read clinical-config.json to understand the formula structure; derive the expected capsule count mathematically before writing the assertion
    - Write tests for all behavior cases listed above
    - Keep tests focused on pure function correctness — no DOM, no Svelte, no mocking

    Create src/lib/formula/__tests__/formula.test.ts:
    - Import { describe, it, expect } from 'vitest'
    - Import calculation functions from '$lib/formula/formula'
    - Import BRANDS or getBrandById from '$lib/formula/formula-config' to get a real BrandConfig
    - Select a well-known brand (e.g., Enfamil Infant 20 cal or Similac Advance 20 cal) for calculateRecipe tests
    - Select a breast milk fortifier brand for calculateBMF tests
    - Write tests for all behavior cases listed above
    - Do not test UI behavior or sessionStorage — those require component testing
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm test run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test run` exits 0
    - PERT test file has at least 7 test cases covering validateFatGrams (4 cases), calculateTotalLipase (1), calculateCapsules (2)
    - Formula test file has at least 8 test cases covering validateRecipeInputs (3), calculateRecipe (1), validateBMFInputs (1), calculateBMF (2), calculateScoops (1)
    - No test is skipped or marked .todo
    - `grep -rn "\.todo\|\.skip" src/lib/pert/__tests__/ src/lib/formula/__tests__/` returns zero matches
  </acceptance_criteria>
  <done>Both test files pass; all cases green; no skips; pnpm test exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of both calculators</name>
  <files>none</files>
  <action>Human verification step — no code changes. Run pnpm dev and follow the how-to-verify steps below.</action>
  <verify>Human confirms all steps in how-to-verify pass before typing "approved".</verify>
  <done>User types "approved" — both calculators verified to be fully functional with state preservation and correct dark mode.</done>
  <what-built>
    Complete Phase 3: Both PERT and formula calculators are live on their routes with full feature parity, sessionStorage-backed state persistence, and dark mode support.

    PERT (/pert): Meal mode + tube-feed mode tabs, all 5 FDA brands (Creon, Zenpep, Pancreaze, Pertzye, Viokace), capsule count calculation, fat grams validation
    Formula (/formula): Modified mode + BMF mode tabs, 40+ brands with manufacturer groups, water/powder output, dispensing measures, BMF EBM output with amber styling

    Unit tests for all pure calculation functions are passing.
  </what-built>
  <how-to-verify>
    Run: pnpm dev (then open http://localhost:5173)

    PERT Meal mode (3 min):
    1. Navigate to PERT tab — calculator loads (no skeleton)
    2. Enter fat grams = 15, select lipase rate, select Creon 6000 — capsule count appears
    3. Clear fat grams — inline error appears; result disappears
    4. Switch to Tube-feed tab — inputs are independent (meal state preserved)
    5. Enter tube-feed inputs — Pancreaze shows 2600 and 37000 strengths (tube-feed specific)
    6. Switch back to Meal tab — meal inputs still filled from step 2

    Formula Modified mode (3 min):
    7. Navigate to Formula tab — calculator loads
    8. Select any brand — brand name appears in picker (NOT "Select Brand...")
    9. Enter target kcal/oz = 24, volume = 100 — water mL and powder grams appear
    10. If brand has scoops/packets — dispensing measures appear below results
    11. Switch to BMF tab — separate inputs; modified state preserved
    12. Enter BMF inputs with targetKcalOz less than or equal to baselineKcalOz — error shown, no result

    Cross-calculator state (2 min):
    13. Fill in PERT inputs, switch to Formula, fill in formula inputs
    14. Switch back to PERT — PERT inputs are still there
    15. Switch back to Formula — formula inputs are still there

    Dark mode (1 min):
    16. Toggle dark mode — both calculators adapt correctly
    17. BMF result card uses amber (not blue) in dark mode
    18. No hardcoded white backgrounds visible
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Before checkpoint:
1. `pnpm test run` — all tests pass
2. `pnpm build` — exits 0
3. `pnpm exec tsc --noEmit` — zero errors across entire project

During checkpoint:
- Human verifies both calculators functional
- Human verifies state persists across tab switches
- Human verifies dark mode correct for both calculators
</verification>

<success_criteria>
- pnpm test run exits 0 (all calculation tests pass)
- pnpm build exits 0
- Human approves both calculators in checkpoint
- Phase 3 goal achieved: "Both PERT and formula calculators are fully functional inside the unified app with feature parity to their standalone predecessors and isolated, preserved state across tab switches"
</success_criteria>

<output>
After completion, create `.planning/phases/03-calculators/03-04-SUMMARY.md`
</output>
