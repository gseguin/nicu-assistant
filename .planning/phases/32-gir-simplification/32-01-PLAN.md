---
phase: 32-gir-simplification
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [GIR-SIMP-01, GIR-SIMP-02, GIR-SIMP-03, GIR-SIMP-04, GIR-SIMP-05, GIR-SIMP-06, GIR-SIMP-07]
files_modified:
  - src/lib/gir/GirCalculator.svelte
  - src/lib/gir/GlucoseTitrationGrid.svelte
  - src/lib/gir/GlucoseTitrationGrid.test.ts
  - src/lib/gir/GirCalculator.test.ts
  - e2e/gir.spec.ts

must_haves:
  truths:
    - "GirCalculator renders only: inputs → CURRENT GIR hero → advisories → titration grid → disclaimer. No summary hero card. No population reference card."
    - "Each bucket card (mobile + desktop) shows only the bucket range label and the Δ rate hero (or STOP / — for edge cases). No Fluids/Rate/GIR secondary row."
    - "Click/tap on a bucket card applies border-l-4 + --color-identity-hero fill and keeps role=radiogroup/radio, roving tabindex, keyboard nav, focus rings."
    - "The severe-neuro bucket ALWAYS renders STOP dextrose infusion in both mobile and desktop branches, regardless of targetGirMgKgMin value."
    - "No aria-live attribute remains on the (now-deleted) target-guidance hero. CURRENT GIR hero's aria-live is untouched (not a bucket-selection announcement)."
    - "vitest green, svelte-check 0/0, Playwright axe 16/16 green."
  artifacts:
    - path: src/lib/gir/GirCalculator.svelte
      provides: "Simplified container (inputs + current-GIR hero + grid only)"
      max_lines: 200
    - path: src/lib/gir/GlucoseTitrationGrid.svelte
      provides: "Bucket grid with range label + Δ rate hero only, unconditional severe-neuro STOP"
    - path: src/lib/gir/GlucoseTitrationGrid.test.ts
      provides: "Component tests including severe-neuro unconditional STOP assertion"
  key_links:
    - from: "GlucoseTitrationGrid bucket card"
      to: "stopInfusion render branch"
      via: "row.bucketId === 'severe-neuro' || row.targetGirMgKgMin <= 0"
      pattern: "severe-neuro.*\\|\\|.*targetGirMgKgMin"
---

<objective>
Strip the GIR calculator to its essentials. Remove the Target GIR summary hero card, the per-card Fluids/Rate/GIR secondary row, and the bottom "Starting GIR by population" reference card. Preserve all selection visuals and a11y scaffolding on the bucket grid. Fix the severe-neuro STOP gating so it triggers on bucketId unconditionally (deferred from v1.9 per GIR-CRITIQUE.md §2). Update all tests to match.

Purpose: Follow-up clinician feedback — the Δ rate hero swap in v1.9 was the right move, but the surrounding chrome is noise. Simpler = faster to read = safer at bedside. Also closes the v1.9-deferred clinical correctness issue where the severe-neuro fixture has `targetGirMgKgMin: 9.7` (positive) and the current `<= 0` gate does not catch it.

Output: A GIR calculator where the bucket grid is the sole focal point below the inputs + current GIR hero. All gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-impeccable-polish-tech-debt-sweep/GIR-CRITIQUE.md
@.planning/phases/29-gir-titration-hero-swap/29-01-SUMMARY.md
@src/lib/gir/GirCalculator.svelte
@src/lib/gir/GlucoseTitrationGrid.svelte
@src/lib/gir/GlucoseTitrationGrid.test.ts
@src/lib/gir/GirCalculator.test.ts
@src/lib/gir/state.svelte.ts
@e2e/gir.spec.ts
@e2e/gir-a11y.spec.ts

<interfaces>
<!-- Current source landmarks extracted so executor does not re-discover -->

GirCalculator.svelte regions (current, 249 lines):
  - L1-61   <script> — calculation, deltaHero(), state
  - L63-116 inputs card (weight, dextrose, dex advisory, fluid order)
  - L118-153 CURRENT GIR hero section  — KEEP (aria-live stays; not bucket-selection)
  - L155-171 Tier 2 advisories (high/low GIR) — KEEP
  - L173-186 Titration grid header + <GlucoseTitrationGrid> — KEEP
  - L187-229 TARGET-GUIDANCE HERO (ADJUST RATE / HYPERGLYCEMIA / TARGET REACHED) — DELETE (GIR-SIMP-01)
  - L232-248 "Starting GIR by population" reference card — DELETE (GIR-SIMP-03)
  - L24-30  selectedRow / targetPulseKey $derived — DELETE (dead after hero removal)
  - L43-54  deltaHero() helper — DELETE (no remaining caller after hero removal;
            GlucoseTitrationGrid has its own formatDelta())
  - Import on L8: `AlertTriangle, Info` — KEEP (still used by advisories)
  - `selectedBucketId` in state.svelte.ts — RETAINED for visual continuity (GIR-SIMP-04)

GlucoseTitrationGrid.svelte regions (current, 200 lines):
  - L1-91   <script> — helpers, keyboard, refs. KEEP all.
  - L93-152 MOBILE branch (<div class="flex ... sm:hidden">)
      - L102 `{@const stopInfusion = row.targetGirMgKgMin <= 0}` — CHANGE to
             `row.bucketId === 'severe-neuro' || row.targetGirMgKgMin <= 0`
      - L136-149 secondary-row grid (Fluids / Rate / GIR) — DELETE (GIR-SIMP-02)
  - L154-200 DESKTOP branch (<div class="hidden sm:grid ...">)
      - L161-165 header row (Range / Δ rate / Target fluids / Target rate / Target GIR)
             — Target fluids/rate/GIR columns removed (GIR-SIMP-02). Keep Range + Δ rate.
      - L156 grid-cols-[minmax(140px,1.4fr)_1fr_1fr_1fr_1.2fr]
             — SIMPLIFY to two columns (Range + Δ rate), e.g.
             grid-cols-[minmax(140px,1.4fr)_1fr]
      - L169 `{@const stopInfusion = row.targetGirMgKgMin <= 0}` — same change as mobile
      - L186 STOP branch col-span-4 → col-span-1 (only Δ rate column remains)
      - L188-191, L194-196 Target fluids/rate/GIR cells — DELETE
  - ariaLabelFor() at L39-50: keep as-is. It still references targets in the SR
    string — that is the SR-only equivalent, not visible per-card content.

GlucoseTitrationGrid.test.ts (current):
  - Line 8: severe-neuro fixture has `targetGirMgKgMin: 9.7` (POSITIVE!) — this
    is why the current `<= 0` gate misses severe-neuro. The TDD test must assert
    this row renders STOP regardless.

e2e/gir.spec.ts:
  - L39-43: assertion on /ADJUST RATE|HYPERGLYCEMIA|TARGET REACHED/ — DELETE
  - Sweep for any population reference / "IDM" / "Starting GIR by population"
    assertions and delete.

GirCalculator.test.ts:
  - L13 `getByText('Starting GIR by population')` — DELETE
  - L36-40 "renders population reference card" block — DELETE entirely
  - Sweep for any assertion on the summary hero strings above.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Line-range audit (no edits)</name>
  <files>src/lib/gir/GirCalculator.svelte, src/lib/gir/GlucoseTitrationGrid.svelte, src/lib/gir/GlucoseTitrationGrid.test.ts, src/lib/gir/GirCalculator.test.ts, e2e/gir.spec.ts, e2e/gir-a11y.spec.ts</files>
  <action>
    Read each file fresh and confirm the line ranges and landmarks listed in the
    `<interfaces>` block above are still accurate. Do not edit anything.
    Produce a short audit note listing, for each file, the exact line numbers of
    every region to delete or change. If reality drifts from the interfaces block,
    record deltas — subsequent tasks use this audit as their map.

    Also grep e2e/gir.spec.ts for `IDM`, `Starting GIR`, `Fluids`, `population`,
    `ADJUST RATE`, `HYPERGLYCEMIA`, `TARGET REACHED` and list every line number.
    Do the same in e2e/gir-a11y.spec.ts.
  </action>
  <verify>
    <automated>node -e "console.log('audit-only task, no verify command')"</automated>
  </verify>
  <done>Audit note captured; all line numbers for subsequent tasks confirmed or revised.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — severe-neuro unconditional STOP test (GIR-SIMP-06)</name>
  <files>src/lib/gir/GlucoseTitrationGrid.test.ts</files>
  <behavior>
    - Mobile branch: when a row with `bucketId: 'severe-neuro'` has
      `targetGirMgKgMin: 9.7` (positive, realistic severe-neuro fixture),
      the rendered card MUST contain the literal text `STOP` and
      `dextrose infusion`, AND must NOT contain the Δ-rate increase/decrease
      hero (no `▲`, no `▼`, no `(increase)`, no `(decrease)`).
    - Desktop branch: same assertion via the desktop grid render path
      (set viewport wide OR render both branches; the existing test file
      pattern renders both simultaneously since they are CSS-hidden). At
      least one DOM node for the severe-neuro row must contain `STOP`.
    - Test name: "severe-neuro bucket renders STOP unconditionally even when targetGirMgKgMin > 0"
    - Must FAIL against current source (current gate is `<= 0` only).
  </behavior>
  <action>
    Add a new `it(...)` to GlucoseTitrationGrid.test.ts using the existing test
    harness pattern (import `render` from @testing-library/svelte, import
    GlucoseTitrationGrid, use the fixture already in that file which has
    severe-neuro with targetGirMgKgMin: 9.7). Do NOT modify source yet.
    Run vitest for this file only and CONFIRM the new test fails. Commit.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/gir/GlucoseTitrationGrid.test.ts</automated>
  </verify>
  <done>New test exists, fails with a clear "expected STOP, received ▲..." style diff, committed as `test(32-01): RED severe-neuro unconditional STOP`.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN — severe-neuro gating fix in both branches (GIR-SIMP-06)</name>
  <files>src/lib/gir/GlucoseTitrationGrid.svelte</files>
  <action>
    In GlucoseTitrationGrid.svelte change BOTH occurrences of
      `{@const stopInfusion = row.targetGirMgKgMin <= 0}`
    (mobile branch ~L102, desktop branch ~L169) to:
      `{@const stopInfusion = row.bucketId === 'severe-neuro' || row.targetGirMgKgMin <= 0}`

    Do not touch anything else in this task — secondary-row deletion comes next.
    Run vitest for GlucoseTitrationGrid.test.ts and confirm the Task-2 RED
    test now passes. Run svelte-check and confirm 0/0.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/gir/GlucoseTitrationGrid.test.ts &amp;&amp; pnpm svelte-check</automated>
  </verify>
  <done>Severe-neuro test green; svelte-check 0/0; commit `fix(32-01): GIR-SIMP-06 — severe-neuro STOP gates on bucketId in both branches`.</done>
</task>

<task type="auto">
  <name>Task 4: Remove per-card secondary metrics row (GIR-SIMP-02)</name>
  <files>src/lib/gir/GlucoseTitrationGrid.svelte</files>
  <action>
    Mobile branch (~L136-149): delete the entire
      `<div class="grid grid-cols-3 gap-2 text-ui border-t ... pt-2 mt-2">`
    block including its three Fluids/Rate/GIR children. Card now ends after
    the Δ rate / STOP / em-dash branch.

    Desktop branch:
    - Change grid template from
        `grid-cols-[minmax(140px,1.4fr)_1fr_1fr_1fr_1.2fr]`
      to
        `grid-cols-[minmax(140px,1.4fr)_1fr]`
    - Header row (~L161-165): delete the three header divs "Target fluids",
      "Target rate", "Target GIR". Keep "Range" and "Δ rate".
    - STOP branch (~L186): change `col-span-4` to `col-span-1` (only the Δ rate
      column remains). Keep the STOP text content.
    - No-change branch (~L188-191): delete the three target-value divs; keep
      only the em-dash Δ-rate div.
    - Delta branch (~L193-196): delete the three target-value divs; keep only
      the Δ rate cell.

    Do NOT touch ariaLabelFor() — screen-reader strings still include target
    context, which is fine (SR-only, not visible chrome).

    Run vitest on GlucoseTitrationGrid.test.ts — expect some existing tests to
    fail if they assert on "Fluids" / "ml/kg/d" / "Target fluids" header / etc.
    Those assertions get deleted in Task 7. For THIS task, it is acceptable for
    pre-existing per-card-secondary-row assertions to fail; the severe-neuro test
    from Task 2 MUST still pass. Run svelte-check 0/0.
  </action>
  <verify>
    <automated>pnpm svelte-check &amp;&amp; pnpm vitest run src/lib/gir/GlucoseTitrationGrid.test.ts -t "severe-neuro"</automated>
  </verify>
  <done>Secondary row removed from both branches; severe-neuro test still passes; svelte-check 0/0; commit `fix(32-01): GIR-SIMP-02 — remove per-card secondary metrics row`.</done>
</task>

<task type="auto">
  <name>Task 5: Remove Target GIR summary hero card + dead code (GIR-SIMP-01, GIR-SIMP-05)</name>
  <files>src/lib/gir/GirCalculator.svelte</files>
  <action>
    In GirCalculator.svelte:

    1. Delete the entire TARGET-GUIDANCE HERO block (~L187-229) — the
       `{#key targetPulseKey}` wrapper and its `<section class="card ... aria-live="polite">`.
       This removes the ADJUST RATE / HYPERGLYCEMIA / TARGET REACHED eyebrow,
       the STOP/—/Δ body, and the aria-live attribute (GIR-SIMP-05).
       NOTE: The aria-live on the CURRENT GIR hero at ~L123 is a DIFFERENT
       section — LEAVE IT ALONE. GIR-SIMP-05 only drops the bucket-selection
       announcement, which lived on the now-deleted target-guidance hero.

    2. The titration grid `<section>` (~L175-185) becomes the last content
       inside the `{#if result}` block. Make sure the `{/if}` balances.

    3. Delete now-dead `<script>` code:
       - `let selectedRow = $derived(...)` at ~L24-28
       - `let targetPulseKey = $derived(...)` at ~L30
       - `type DeltaHero = ...` at ~L43-46
       - `function deltaHero(...)` at ~L48-54

    4. Keep `handleSelectBucket`, `selectedBucketId` binding, and the
       `<GlucoseTitrationGrid>` usage with `selectedBucketId` prop — the
       selection state is preserved for visual continuity (GIR-SIMP-04).

    5. Run svelte-check — expect 0 errors, 0 warnings. Any "unused import" or
       "unused variable" warning must be resolved in this task.
  </action>
  <verify>
    <automated>pnpm svelte-check</automated>
  </verify>
  <done>Summary hero gone; dead script code removed; svelte-check 0/0; commit `fix(32-01): GIR-SIMP-01 GIR-SIMP-05 — remove Target GIR summary hero + aria-live`.</done>
</task>

<task type="auto">
  <name>Task 6: Remove "Starting GIR by population" reference card (GIR-SIMP-03)</name>
  <files>src/lib/gir/GirCalculator.svelte</files>
  <action>
    Delete the entire POPULATION REFERENCE CARD `<section class="card px-5 py-4">`
    (~L232-248) including the grid of IDM/LGA, IUGR, Preterm-or-NPO rows and the
    closing disclaimer paragraph. The outer `<div class="space-y-6">` at L63 now
    closes directly after the `{#if result}` titration block.

    Verify the file ends cleanly (no orphan closing tags). Run svelte-check.
  </action>
  <verify>
    <automated>pnpm svelte-check</automated>
  </verify>
  <done>Reference card gone; svelte-check 0/0; commit `fix(32-01): GIR-SIMP-03 — remove Starting GIR by population reference card`.</done>
</task>

<task type="auto">
  <name>Task 7: Update component + e2e tests (GIR-SIMP-07)</name>
  <files>src/lib/gir/GirCalculator.test.ts, src/lib/gir/GlucoseTitrationGrid.test.ts, e2e/gir.spec.ts, e2e/gir-a11y.spec.ts</files>
  <action>
    GirCalculator.test.ts:
    - Delete L13 `expect(screen.getByText('Starting GIR by population')).toBeTruthy();`
    - Delete the entire `it('renders population reference card with all three rows', ...)`
      block at ~L36-41.
    - Grep the file for any assertion on 'ADJUST RATE' / 'HYPERGLYCEMIA' /
      'TARGET REACHED' / 'IDM' / 'IUGR' / 'Preterm' / 'Starting GIR' and delete
      those assertions too.

    GlucoseTitrationGrid.test.ts:
    - Delete any assertion on the per-card secondary row text: "Fluids",
      "ml/kg/d" (as a visible label), "Target fluids" / "Target rate" /
      "Target GIR" desktop headers.
    - KEEP the severe-neuro STOP test added in Task 2.
    - Do NOT delete assertions on ariaLabelFor() SR strings (those still exist
      in source and still contain target numbers — that is acceptable).

    e2e/gir.spec.ts:
    - Delete L39-43: the `/ADJUST RATE|HYPERGLYCEMIA|TARGET REACHED/` assertion
      and its surrounding comment block.
    - Grep for `Starting GIR`, `IDM`, `Preterm`, `Fluids` and delete any
      assertion that references the removed chrome. Keep assertions on
      bucket selection, Δ rate hero, CURRENT GIR hero, inputs.
    - If a whole `test(...)` block becomes empty, delete the whole block.

    e2e/gir-a11y.spec.ts:
    - Sweep for assertions on the same removed strings. A11y sweeps are
      usually structural (axe.run + screenshots) so there may be nothing to
      change — confirm via grep.

    Run full vitest suite and confirm green.
  </action>
  <verify>
    <automated>pnpm vitest run</automated>
  </verify>
  <done>All stale assertions gone; vitest all-green; commit `test(32-01): GIR-SIMP-07 — remove stale assertions for deleted GIR chrome`.</done>
</task>

<task type="auto">
  <name>Task 8: Parity fixtures review (GIR-SIMP-06 follow-up)</name>
  <files>src/lib/gir/gir-parity.fixtures.json, src/lib/gir/calculations.test.ts</files>
  <action>
    Open gir-parity.fixtures.json and calculations.test.ts. The severe-neuro
    row currently carries a calculated `deltaRateMlHr` value (since the math
    runs before the UI gate). Decide:
    - If parity fixtures expect `deltaRateMlHr` on the severe-neuro row that
      matches `calculations.ts` output, LEAVE THEM — the gating is UI-level,
      math is unchanged per REQUIREMENTS.md ("rendering/gating change, not a
      math change").
    - If a fixture explicitly claims severe-neuro should emit `deltaRateMlHr: 0`
      or `null`, update ONLY that fixture cell to match the (unchanged)
      calculations.ts output, because the calculator never emitted 0 for it.

    Run calculations.test.ts and gir-config.test.ts to confirm parity is
    intact. If everything is already green with no edits, note "no changes
    needed" and skip the commit.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/gir/calculations.test.ts src/lib/gir/gir-config.test.ts</automated>
  </verify>
  <done>Parity tests green; either `chore(32-01): parity fixtures — severe-neuro row aligned with calculations output` OR a note that no change was required.</done>
</task>

<task type="auto">
  <name>Task 9: Full gate sweep — vitest + svelte-check + Playwright axe</name>
  <files>(no edits; verification only)</files>
  <action>
    Run the non-negotiable gate commands in order and capture output:
    1. `pnpm svelte-check` — must be 0 errors, 0 warnings.
    2. `pnpm vitest run` — all tests green.
    3. `pnpm playwright test e2e/gir.spec.ts e2e/gir-a11y.spec.ts` — all green,
       16/16 axe sweeps green in both themes.
    4. `pnpm build` — PWA builds successfully.

    If any gate fails, STOP and fix the root cause in the appropriate earlier
    task file (do not paper over). If all four pass, record the final counts
    (vitest passed/total, axe 16/16, svelte-check 0/0) in the summary note.
  </action>
  <verify>
    <automated>pnpm svelte-check &amp;&amp; pnpm vitest run &amp;&amp; pnpm playwright test e2e/gir.spec.ts e2e/gir-a11y.spec.ts &amp;&amp; pnpm build</automated>
  </verify>
  <done>All four gates green; counts recorded; commit (if any final fixups applied) `chore(32-01): final gate sweep green — vitest + svelte-check + axe 16/16 + build`.</done>
</task>

</tasks>

<verification>
- Visual: GirCalculator page shows only inputs → CURRENT GIR hero → (advisories if any) → titration header + grid. Nothing below the grid except the existing disclaimer text inside the grid header.
- Bucket cards show: range label + Δ rate hero (or STOP for severe-neuro/hyperglycemia, or em-dash for no-change). No Fluids/Rate/GIR strip.
- Severe-neuro card: STOP dextrose infusion, in both viewport widths.
- Click a card: border-l-4 + identity-hero fill applied, keyboard Arrow/Home/End still moves selection, focus ring visible.
- svelte-check 0/0, vitest green, Playwright axe 16/16, pnpm build green.
</verification>

<success_criteria>
1. `GirCalculator.svelte` contains no "ADJUST RATE" / "HYPERGLYCEMIA" / "TARGET REACHED" / "Starting GIR by population" / "IDM" / "IUGR" / "Preterm" literals.
2. `GlucoseTitrationGrid.svelte` contains no "Fluids" / "Target fluids" / "Target rate" / "Target GIR" visible-chrome literals (ariaLabelFor() SR strings are allowed to retain target context).
3. Both mobile and desktop branches gate stopInfusion on `row.bucketId === 'severe-neuro' || row.targetGirMgKgMin <= 0`.
4. `selectedBucketId` still bound into `<GlucoseTitrationGrid>` from GirCalculator and still persisted via `girState.persist()`.
5. `role="radiogroup"` / `role="radio"` / roving `tabindex` / keyboard handlers / focus-visible ring classes intact.
6. The `aria-live="polite"` on the deleted target-guidance hero is gone. The `aria-live` on the CURRENT GIR hero is untouched.
7. Quality gates: `pnpm svelte-check` 0/0, `pnpm vitest run` all green, `pnpm playwright test e2e/gir.spec.ts e2e/gir-a11y.spec.ts` all green including 16/16 axe, `pnpm build` ok.
</success_criteria>

<output>
After completion, create `.planning/phases/32-gir-simplification/32-01-SUMMARY.md` summarizing:
- Final line counts for GirCalculator.svelte (was 249) and GlucoseTitrationGrid.svelte (was 200)
- Severe-neuro fix: before/after gate expression and which branches were touched
- Test deltas: assertions removed, assertion added (severe-neuro STOP)
- Gate results: vitest N/N, svelte-check 0/0, axe 16/16, build ✓
- Any NOTES.md deferrals (should be none — this phase closes the v1.9 deferral)
</output>
