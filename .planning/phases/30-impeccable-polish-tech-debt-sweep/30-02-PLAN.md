---
phase: 30-impeccable-polish-tech-debt-sweep
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/**
  - .planning/PROJECT.md
  - .planning/phases/30-impeccable-polish-tech-debt-sweep/NOTES.md
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04]
must_haves:
  truths:
    - "Dependencies are current within their current major versions across Svelte, Vite+Tailwind, testing, and other groups"
    - "Full test suite (vitest + Playwright) passes after each dep group bump and at the end"
    - "Dead code from v1.5–v1.8 is removed or explicitly re-deferred with rationale"
    - "svelte-check reports zero NEW warnings across src/ beyond the documented pre-existing list"
    - "The ESLint situation is investigated and a decision is recorded in PROJECT.md Key Decisions"
    - "Phase 29 deferred-items.md entries are each addressed (fixed, suppressed with rationale, or re-deferred with rationale)"
    - "16/16 axe sweeps remain green after dep updates"
  artifacts:
    - path: "package.json"
      provides: "Updated dep versions within current majors"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions entries for ESLint disposition and re-deferred cleanups"
      contains: "ESLint"
    - path: "src/lib/shell/__tests__/registry.test.ts"
      provides: "Registry length assertion corrected to 3 or replaced with key-set assertion"
  key_links:
    - from: "package.json dep bump"
      to: "pnpm test + pnpm svelte-check + axe sweep"
      via: "per-group verification gate"
      pattern: "Group [A-D]"
    - from: "Phase 29 deferred-items.md"
      to: "Plan 30-02 tasks"
      via: "pickup list"
      pattern: "registry\\.test\\.ts|svelte-check|state_referenced_locally"
---

<objective>
Sweep tech debt: update deps in groups within current majors (DEBT-01), remove dead code from v1.5–v1.8 (DEBT-02), clean lint/TS with ESLint disposition (DEBT-03), address or re-defer prior milestone deferrals including Phase 29 deferred-items.md (DEBT-04). Framework plan with per-group gates; executor has discretion on suppression vs. fix decisions where documented.

Purpose: DEBT-01..04. Keeps v1.9 shipping on clean foundations without inviting major-version breakage. Depends on Plan 30-01 so the polish pass locks visuals before deps shift under them.

Output: Updated `package.json`/`pnpm-lock.yaml`; cleaned `src/`; PROJECT.md decision entries; atomic commit per dep group.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-impeccable-polish-tech-debt-sweep/30-CONTEXT.md
@.planning/phases/29-gir-titration-hero-swap/deferred-items.md
@package.json
</context>

<non_goals>
- No major version upgrades (no SvelteKit 3, Svelte 6, Vite 9, Tailwind 5)
- No new features, no new calculators
- No architectural refactors — dead code sweep is surgical (unused exports, unreferenced files, commented blocks only)
- No `any` casts to silence svelte-check — explicit `// @ts-expect-error: <reason>` only
- No changes to visual/UX behavior landed in Plan 30-01
</non_goals>

<executor_discretion>
1. **Dep group order:** CONTEXT.md Decision 3 lists groups A/B/C/D but does not mandate order. Suggested order: D (lowest blast radius) → C → B → A (highest blast radius). If a group's internal deps have interlocks, bump them together in that group's commit.
2. **Group breakage:** If a group's bump breaks tests, revert the group and try each dep individually within the group. If a specific dep cannot be bumped (transitive conflict), re-defer it in NOTES.md with rationale and move on.
3. **svelte-check pre-existing errors:** Per deferred-items.md, classify each:
   - Genuine type bug → fix (e.g. `GlucoseBucket id` narrowing in GirCalculator.svelte)
   - Environment noise (svelte-check not seeing Vite virtuals / `$app/state` / `$app/navigation`) → suppress via svelte-check config tweak (e.g. `tsconfig.json` `types`, `svelte.config.js`, or `svelte-check --ignore` pattern), NOT `any` casts
   - Svelte 5 warnings (SegmentedToggleHarness `state_referenced_locally`) → fix or explicitly suppress with rationale comment
4. **ESLint decision:** If the repo has no ESLint install, you may either (a) add it with a minimal config that mirrors existing Prettier/svelte-check style, or (b) drop the ESLint requirement from DEBT-03 in favor of `svelte-check` + Prettier only. Either choice is valid — document in PROJECT.md Key Decisions.
5. **Dead code:** Use `knip` or manual grep for unused exports. If removal requires touching >3 files or changes a public component API, defer.
</executor_discretion>

<tasks>

<task type="auto">
  <name>Task 1: Phase 29 deferred-items pickup + ESLint investigation</name>
  <files>
    src/lib/shell/__tests__/registry.test.ts,
    src/lib/gir/GirCalculator.svelte,
    src/lib/shared/components/SegmentedToggleHarness.svelte,
    svelte.config.js,
    tsconfig.json,
    package.json,
    .planning/PROJECT.md,
    .planning/phases/30-impeccable-polish-tech-debt-sweep/NOTES.md
  </files>
  <action>
    Work through `.planning/phases/29-gir-titration-hero-swap/deferred-items.md`:

    1. **`registry.test.ts:9`** — update the length assertion from 2 → 3, OR replace with a key-set assertion (e.g. `expect(CALCULATOR_REGISTRY.map(c => c.id)).toEqual(['morphine','formula','gir'])`). Pick the latter — more resilient. Run `pnpm test`. Atomic commit: `fix(30-02): registry test asserts key set not length`.

    2. **Pre-existing svelte-check errors** — classify each and handle:
       - `GirCalculator.svelte:13:56` (GlucoseBucket id narrowing) → fix the type narrowing properly (likely a `satisfies` or explicit discriminant)
       - `NavShell.svelte:2:24` (`$app/state`), `+layout.svelte:12,44` (`virtual:pwa-*`), `+page.svelte:2:24` (`$app/navigation`) → environment noise; fix by adjusting `svelte-check` config or `tsconfig` `types`/`compilerOptions` so these modules resolve. NOT by casting to `any`.
       - `SegmentedToggleHarness.svelte:5:22` (`state_referenced_locally`) → fix per Svelte 5 guidance or suppress with an explicit rationale comment if the warning is a false positive.
       - Any that cannot be resolved → document in NOTES.md with specific rationale and keep them on the "known documented-and-deferred" list.
       Atomic commits per fix: `fix(30-02): svelte-check {file} — {one-line}`.

    3. **ESLint investigation** — check `package.json` scripts and `devDependencies` for ESLint. Run whatever lint command exists (or confirm none exists).
       - If none exists: decide per CONTEXT.md Decision from Phase 29 note — either add a minimal ESLint config OR drop ESLint from DEBT-03 in favor of svelte-check + Prettier.
       - Document the decision in `.planning/PROJECT.md` under Key Decisions with date and rationale.
       - Atomic commit: `docs(30-02): PROJECT.md — ESLint decision recorded` (plus any code changes in a separate commit if ESLint is added).

    All under DEBT-03 + DEBT-04.
  </action>
  <verify>
    <automated>pnpm test && pnpm svelte-check 2>&1 | tee /tmp/svelte-check-task1.log && grep -q 'ESLint' .planning/PROJECT.md</automated>
  </verify>
  <done>
    registry.test.ts corrected; svelte-check pre-existing errors each classified and handled (fixed or documented-deferred with rationale); ESLint decision recorded in PROJECT.md Key Decisions. `pnpm svelte-check` has zero NEW warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dependency updates in groups D → C → B → A with per-group gate</name>
  <files>
    package.json,
    pnpm-lock.yaml
  </files>
  <action>
    Per CONTEXT.md Decision 3, bump deps in groups within current majors. Suggested order: D → C → B → A (ascending blast radius).

    For EACH group:
    1. Run `pnpm outdated` to see what's bumpable within the current major
    2. Bump all deps in the group to their latest within-major version
    3. `pnpm install`
    4. **Per-group gate (BLOCKING):**
       - `pnpm test` (vitest)
       - `pnpm test:e2e` (Playwright)
       - `pnpm svelte-check` (no new warnings)
       - `pnpm build` (ensure adapter-static SPA still builds)
       - Targeted axe sweep on one calculator per group (or full 16/16 for Group A since it touches Svelte core)
    5. If gate passes: atomic commit `chore(30-02): bump Group {X} — {list of deps}`
    6. If gate fails: `git checkout -- package.json pnpm-lock.yaml && pnpm install`, then try deps individually within the group. Anything that can't be bumped: document in NOTES.md and move on.

    **Groups (from CONTEXT.md Decision 3):**
    - Group D: `@lucide/svelte`, `bits-ui`, `@vite-pwa/sveltekit`
    - Group C: `vitest`, `@playwright/test`, `@vitest/*`
    - Group B: `vite`, `@tailwindcss/vite`, `tailwindcss`
    - Group A: `svelte`, `@sveltejs/kit`, `@sveltejs/adapter-static`, `@sveltejs/vite-plugin-svelte`

    **Major-version guardrail:** NEVER bump across a major. If `pnpm outdated` shows SvelteKit 3 available, stay on 2.x. Same for Svelte 5 → 6, Vite 8 → 9, Tailwind 4 → 5.

    DEBT-01.
  </action>
  <verify>
    <automated>pnpm test && pnpm test:e2e && pnpm svelte-check && pnpm build</automated>
  </verify>
  <done>
    Each of Groups A/B/C/D has either (a) a passing atomic commit bumping all deps within major, or (b) a NOTES.md entry explaining why one or more deps in the group couldn't be bumped. Full test suite + build + svelte-check green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dead code sweep + final verification gate (16/16 axe + full suite)</name>
  <files>
    src/**,
    .planning/PROJECT.md,
    .planning/phases/30-impeccable-polish-tech-debt-sweep/NOTES.md
  </files>
  <action>
    1. **Dead code sweep (DEBT-02):** Find unused exports, unreferenced files, commented-out blocks deferred from v1.5–v1.8. Tools:
       - `pnpm dlx knip` if available, OR
       - Manual: `grep -rn "// TODO\\|// FIXME\\|// XXX\\|/\\* deferred" src/` for stale markers
       - `tsc --noEmit` + IDE unused-export inspection
       Remove everything that's safe. If a file appears unused but removal would touch >3 consumers or change a public surface, defer to NOTES.md.
       Atomic commits: `chore(30-02): remove dead code — {area}`.

    2. **Prior-milestone deferred cleanups (DEBT-04):** Grep for `@deferred` or similar markers across `src/` and `.planning/phases/*/NOTES.md` from v1.5–v1.8. For each: fix it, OR re-defer with rationale in `.planning/PROJECT.md` Key Decisions. Atomic commits per fix.

    3. **Final gate (BLOCKING — all must pass):**
       - `pnpm test` (vitest) green
       - `pnpm test:e2e` (Playwright) green
       - `pnpm svelte-check` — zero NEW warnings vs. Task 1 baseline (any remaining pre-existing documented in NOTES.md)
       - `pnpm build` green
       - Lint command (ESLint if added in Task 1, else skipped per documented decision) green
       - **Full 16/16 axe sweep** in light + dark (morphine 6 + fortification 4 + gir 6) — the v1.8 "axe BEFORE PR" gate
       - `pnpm outdated` shows no bumpable-within-major deps remaining (or remaining ones are NOTES.md-documented)

    4. **Cleanup:** Delete `critique/` screenshot dir (per CONTEXT.md Decision 5 — transient, not committed). Retain the `*-CRITIQUE.md` files from Plan 30-01 (those ARE committed).
  </action>
  <verify>
    <automated>pnpm test && pnpm test:e2e && pnpm svelte-check && pnpm build</automated>
  </verify>
  <done>
    Dead code removed or re-deferred with rationale; prior-milestone deferrals each addressed or explicitly re-deferred in PROJECT.md Key Decisions; final gate passes (tests, e2e, svelte-check, build, 16/16 axe). DEBT-02 and DEBT-04 satisfied.
  </done>
</task>

</tasks>

<verification>
End-of-plan gate (all must pass):
- [ ] `pnpm test` green
- [ ] `pnpm test:e2e` green
- [ ] `pnpm svelte-check` — zero new warnings
- [ ] `pnpm build` green
- [ ] ESLint green OR documented as dropped in PROJECT.md
- [ ] 16/16 axe sweeps green in light + dark
- [ ] `pnpm outdated` — no within-major bumps remaining (or remaining ones explained in NOTES.md)
- [ ] DEBT-01..04 each trace to a specific task above
- [ ] No major version upgrades performed
- [ ] Phase 29 deferred-items.md entries each addressed
</verification>

<success_criteria>
1. Deps current within majors with atomic commit per group (DEBT-01)
2. Dead code sweep complete; removals committed or deferred with rationale (DEBT-02)
3. svelte-check zero new warnings; ESLint decision recorded in PROJECT.md (DEBT-03)
4. Prior milestone deferrals addressed or re-deferred with rationale (DEBT-04)
5. Phase 29 deferred-items.md fully handled (registry test + 5 svelte-check entries)
6. 16/16 axe remains green (the "axe BEFORE PR" v1.8 policy)
7. Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/30-impeccable-polish-tech-debt-sweep/30-02-SUMMARY.md` summarizing: deps bumped per group, deps deferred (with reason), dead code removed, Phase 29 deferred-items disposition, ESLint decision, final gate status, and any cross-cutting observations for Phase 31 release.
</output>
