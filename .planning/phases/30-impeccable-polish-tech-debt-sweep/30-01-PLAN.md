---
phase: 30-impeccable-polish-tech-debt-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/gir/**
  - src/lib/morphine/**
  - src/lib/formula/**
  - src/lib/shell/**
  - src/lib/shared/components/**
  - src/app.css
  - .planning/phases/30-impeccable-polish-tech-debt-sweep/critique/**
  - .planning/phases/30-impeccable-polish-tech-debt-sweep/NOTES.md
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04]
must_haves:
  truths:
    - "A documented impeccable:critique pass exists for GIR, Morphine, and Formula in both themes at mobile 375 + desktop 1280 with severity-rated findings"
    - "Every P1 finding from the critique is fixed and visible in the running app"
    - "Every P2 finding addressable without architectural change is fixed"
    - "Axe-core reports 16/16 sweeps green in light + dark after polish — no WCAG 2.1 AA regressions"
    - "Full vitest + Playwright suites pass after the polish loop"
  artifacts:
    - path: ".planning/phases/30-impeccable-polish-tech-debt-sweep/critique/GIR-CRITIQUE.md"
      provides: "GIR critique findings with P0-P3 severity"
    - path: ".planning/phases/30-impeccable-polish-tech-debt-sweep/critique/MORPHINE-CRITIQUE.md"
      provides: "Morphine critique findings with P0-P3 severity"
    - path: ".planning/phases/30-impeccable-polish-tech-debt-sweep/critique/FORMULA-CRITIQUE.md"
      provides: "Formula critique findings with P0-P3 severity"
    - path: ".planning/phases/30-impeccable-polish-tech-debt-sweep/NOTES.md"
      provides: "Deferred P3 items + P2 items requiring architectural change, with rationale"
  key_links:
    - from: ".impeccable.md"
      to: "critique/*-CRITIQUE.md"
      via: "critique rubric applied by impeccable:critique skill"
      pattern: "P0|P1|P2|P3"
    - from: "polish commits"
      to: "axe sweep (16/16)"
      via: "axe BEFORE PR gate"
      pattern: "pnpm.*axe|playwright.*axe"
---

<objective>
Run `impeccable:critique` across all three calculators (GIR → Morphine → Formula per CONTEXT.md Decision 1), classify findings by severity, and fix every P1 and every P2-addressable-without-architectural-change finding using atomic commits with re-verification after each fix. End gate: 16/16 axe sweeps green and full test suite green.

Purpose: POLISH-01..04. Phase 30 is discovery-driven — the real work list is produced during execution by the critique pass. This plan is a **framework with gates**, not a fixed task list. The executor has documented authority to make judgment calls on severity triage inside the gates.

Output: `critique/*-CRITIQUE.md` per calculator; NOTES.md of deferred items; atomic commits for each fix; passing test + axe gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-impeccable-polish-tech-debt-sweep/30-CONTEXT.md
@.impeccable.md
@.claude/skills/impeccable/SKILL.md
@src/app.css
</context>

<non_goals>
- No new calculators, no new features
- No new OKLCH tokens, no new identity hues (token bumps allowed ONLY for contrast fixes on existing tokens)
- No architectural refactors — P2 items requiring architectural change get deferred to NOTES.md
- No major version dependency upgrades (that's Plan 30-02, and only within-major anyway)
- No changes to the `impeccable` skill itself
- No GIR-SWAP follow-up — Phase 29 is closed
</non_goals>

<executor_discretion>
The executor will make real judgment calls that this plan cannot predict. Documented authority:

1. **Severity calls (P1 vs P2 vs P3):** Use `.impeccable.md` + impeccable skill rubric. When ambiguous, err toward higher severity for clinical-trust-affecting findings (legibility, hierarchy, numeric clarity), lower severity for purely aesthetic.
2. **"Addressable without architectural change":** If a P2 fix touches >3 files across subsystems OR requires changing a shared component's public API OR requires a new token, it is architectural → defer to NOTES.md with rationale.
3. **Token bumps:** Only on existing OKLCH tokens, only for WCAG contrast, must be verified with axe before commit.
4. **Trivial P3s:** If cheap (<5 min, 1 file), fix inline in the P2 pass. Otherwise defer.
5. **P0 finding:** STOP. Escalate. P0 indicates a pre-existing failure we missed; do not self-resolve.
</executor_discretion>

<tasks>

<task type="auto">
  <name>Task 1: Critique pass — all 3 calculators, both themes, both viewports</name>
  <files>
    .planning/phases/30-impeccable-polish-tech-debt-sweep/critique/GIR-CRITIQUE.md,
    .planning/phases/30-impeccable-polish-tech-debt-sweep/critique/MORPHINE-CRITIQUE.md,
    .planning/phases/30-impeccable-polish-tech-debt-sweep/critique/FORMULA-CRITIQUE.md
  </files>
  <action>
    Run the `impeccable:critique` skill against each calculator in this order (per CONTEXT.md Decision 1):
    1. GIR (`/gir` route) — most recent layout change, most likely to surface findings
    2. Morphine (`/morphine` route) — typographically mature, baseline reference
    3. Formula (`/formula` route) — most settled

    For each calculator, capture findings at BOTH themes (light + dark) AND BOTH viewports (mobile 375, desktop 1280) — 4 captures per calculator, 12 total.

    Per CONTEXT.md Decision 5: store screenshots in `critique/` subfolder as transient evidence (NOT committed to git; delete at phase end).

    Produce one `{CALC}-CRITIQUE.md` per calculator with:
    - Screenshot paths (local only)
    - Findings list, each with: ID (e.g. GIR-F1), severity (P0/P1/P2/P3), location (file:line or component), description, suggested fix, architectural-change? flag
    - Summary counts per severity

    **STOP condition:** If any P0 finding appears, halt Task 2 and escalate to user per CONTEXT.md Decision 2. P0 means pre-existing failure — do not silently fix.

    This task is pure discovery — no src/ changes yet. Commit the three critique files as one atomic commit: `docs(30-01): critique pass for GIR, Morphine, Formula`.
  </action>
  <verify>
    <automated>test -f .planning/phases/30-impeccable-polish-tech-debt-sweep/critique/GIR-CRITIQUE.md && test -f .planning/phases/30-impeccable-polish-tech-debt-sweep/critique/MORPHINE-CRITIQUE.md && test -f .planning/phases/30-impeccable-polish-tech-debt-sweep/critique/FORMULA-CRITIQUE.md && grep -qE 'P[0-3]' .planning/phases/30-impeccable-polish-tech-debt-sweep/critique/GIR-CRITIQUE.md</automated>
  </verify>
  <done>
    Three CRITIQUE.md files exist with severity-rated findings for both themes at mobile 375 + desktop 1280. No P0 findings (or P0 escalated). POLISH-01 satisfied.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: P1 fix loop — atomic commits with re-verify after each</name>
  <files>
    src/lib/gir/**,
    src/lib/morphine/**,
    src/lib/formula/**,
    src/lib/shell/**,
    src/lib/shared/components/**,
    src/app.css
  </files>
  <behavior>
    - Every P1 finding listed in critique/*-CRITIQUE.md has a matching commit referencing its finding ID (e.g. `fix(30-01): GIR-F1 — ...`)
    - After EACH fix: `pnpm test` (vitest) passes, `pnpm svelte-check` has no NEW warnings, relevant axe sweep for the touched calculator passes
    - Tests updated alongside fix where behavior-visible (TDD: if fix changes rendered output, update component test first to assert the new expectation, then fix)
    - No P1 finding is silently deferred — if one is deemed un-fixable, it must be explicitly moved to NOTES.md with rationale AND flagged to the user
  </behavior>
  <action>
    For each P1 finding from Task 1 (in critique doc order):
    1. If visual: capture "before" screenshot to `critique/before-{finding-id}.png`
    2. If the fix changes component-observable behavior: update the relevant vitest component test FIRST (RED)
    3. Apply the minimal fix (GREEN)
    4. If token bump: verify axe immediately for the touched calculator before committing
    5. Run `pnpm test` + `pnpm svelte-check` (expect: no new warnings)
    6. Capture "after" screenshot to `critique/after-{finding-id}.png`
    7. Atomic commit: `fix(30-01): {finding-id} — {one-line description}`

    **Token rules (POLISH-04):** Bumps allowed only on existing OKLCH tokens in `src/app.css`, only to preserve WCAG 2.1 AA. Run axe on the touched calculator after ANY token bump; if axe regresses, revert.

    **Discretion:** If a P1 on inspection is actually P2 or P3, document the re-classification in the CRITIQUE.md file and handle in the appropriate pass.
  </action>
  <verify>
    <automated>pnpm test && pnpm svelte-check</automated>
  </verify>
  <done>
    Every P1 from Task 1 is either (a) fixed with atomic commit + passing tests + passing targeted axe, or (b) explicitly deferred in NOTES.md with rationale and user-visible flag. POLISH-02 satisfied.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: P2 fix loop + NOTES.md + final axe gate (16/16)</name>
  <files>
    src/lib/gir/**,
    src/lib/morphine/**,
    src/lib/formula/**,
    src/lib/shell/**,
    src/lib/shared/components/**,
    src/app.css,
    .planning/phases/30-impeccable-polish-tech-debt-sweep/NOTES.md
  </files>
  <behavior>
    - Every P2 finding is either fixed (atomic commit) or deferred to NOTES.md with a specific rationale tagged "architectural" or "out-of-scope"
    - Trivially cheap P3 findings (<5 min, single file) may be fixed inline during this pass
    - Final axe sweep reports 16/16 green in BOTH themes (morphine 6 + fortification 4 + gir 6)
    - Full `pnpm test` + `pnpm test:e2e` pass
  </behavior>
  <action>
    1. For each P2 finding: apply the "addressable without architectural change" filter (see executor_discretion). If architectural → NOTES.md with rationale; if addressable → same atomic-commit loop as Task 2 (test first if behavior-visible, fix, re-verify).
    2. For trivially cheap P3s: fix inline and note in commit message.
    3. Write `.planning/phases/30-impeccable-polish-tech-debt-sweep/NOTES.md` listing:
       - P2 findings deferred as architectural (each with rationale)
       - P3 findings deferred (roll-up is fine)
       - Any token bumps applied during this plan and the before/after contrast ratios
    4. **Final axe gate (BLOCKING):** run the full axe sweep — all 16 sweeps (morphine 6 + fortification 4 + gir 6) in light AND dark themes. Must be 16/16 green. If any regression: revert the offending fix OR bump the token that caused the regression (contrast-only rule).
    5. **Final test gate (BLOCKING):** `pnpm test && pnpm test:e2e` must be green.
    6. Commit NOTES.md and any final fixes as `docs(30-01): NOTES + final gate`.
  </action>
  <verify>
    <automated>pnpm test && pnpm test:e2e && pnpm svelte-check && test -f .planning/phases/30-impeccable-polish-tech-debt-sweep/NOTES.md</automated>
  </verify>
  <done>
    All P2-addressable findings fixed; architectural P2s + deferred P3s documented in NOTES.md with rationale; 16/16 axe sweeps green in both themes; full test suite green. POLISH-03 and POLISH-04 satisfied.
  </done>
</task>

</tasks>

<verification>
End-of-plan gate (all must pass before marking plan complete):
- [ ] `pnpm test` green (vitest)
- [ ] `pnpm test:e2e` green (Playwright)
- [ ] `pnpm svelte-check` — no NEW warnings beyond pre-existing list in deferred-items.md
- [ ] 16/16 axe sweeps green in light + dark
- [ ] POLISH-01..04 each trace to a specific task above
- [ ] Zero new OKLCH tokens introduced; any token bumps are existing tokens for contrast only
- [ ] No files outside `files_modified` touched
</verification>

<success_criteria>
1. `critique/GIR-CRITIQUE.md`, `MORPHINE-CRITIQUE.md`, `FORMULA-CRITIQUE.md` exist with severity-rated findings at both themes and both viewports (POLISH-01)
2. Every P1 has a `fix(30-01): {finding-id}` commit (POLISH-02)
3. Every P2-addressable has a commit; every P2-architectural is in NOTES.md with rationale (POLISH-03)
4. Any token bumps preserve WCAG 2.1 AA per axe verification (POLISH-04)
5. 16/16 axe sweeps green; full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/30-impeccable-polish-tech-debt-sweep/30-01-SUMMARY.md` summarizing: critique finding counts by severity per calculator, P1/P2 fixes landed, deferrals with rationale, token bumps (if any), and final gate status.
</output>
