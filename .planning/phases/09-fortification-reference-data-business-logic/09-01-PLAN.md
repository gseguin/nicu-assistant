---
phase: 09-fortification-reference-data-business-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/fortification/types.ts
  - src/lib/fortification/fortification-config.json
  - src/lib/fortification/fortification-config.ts
  - src/lib/fortification/fortification-config.test.ts
autonomous: true
requirements: [REF-01, REF-02]
must_haves:
  truths:
    - "Fortification reference table is embedded as JSON and editable without TypeScript changes"
    - "All formulas from xlsx Calculator tab rows A3:D35 are present with displacement_factor, calorie_concentration, grams_per_scoop"
    - "TypeScript types and a parser/validator load the JSON config at build time"
    - "Parser unit tests confirm exact row count, required fields, and spot-check known values"
  artifacts:
    - path: src/lib/fortification/types.ts
      provides: "FortificationFormula, BaseType, UnitType, TargetKcalOz, FortificationInputs, FortificationResult types"
    - path: src/lib/fortification/fortification-config.json
      provides: "Embedded formula reference table transcribed from recipe-calculator.xlsx Calculator tab A3:D35"
    - path: src/lib/fortification/fortification-config.ts
      provides: "Typed loader/validator that imports the JSON and exports a FortificationFormula[] array"
    - path: src/lib/fortification/fortification-config.test.ts
      provides: "Parser tests: exact row count, required fields present, spot-check values (Neocate Infant, Similac HMF)"
  key_links:
    - from: src/lib/fortification/fortification-config.ts
      to: src/lib/fortification/fortification-config.json
      via: "JSON import + runtime validation"
      pattern: "import .* from .*fortification-config.json"
---

<objective>
Create the `src/lib/fortification/` module foundation: shared types, an embedded JSON reference table transcribed from the recipe-calculator.xlsx Calculator tab, a typed loader, and parser unit tests. This is purely additive — no existing files are touched, and Phase 11 will be able to delete `src/lib/formula/` cleanly later.

Purpose: Lock down the data shape and reference values that Plan 09-02's `calculateFortification` will consume. Clinicians (or maintainers) can edit the JSON without touching TypeScript. Addresses REF-01 and REF-02.
Output: Four new files under `src/lib/fortification/`, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-fortification-reference-data-business-logic/09-RESEARCH.md
@CLAUDE.md
@src/lib/morphine/types.ts
@src/lib/morphine/calculations.ts
@src/lib/formula/formula-config.json
@recipe-calculator.xlsx

<interfaces>
<!-- The morphine module is the canonical reference layout. Mirror it. -->

From src/lib/morphine/types.ts:
```typescript
export type WeanMode = 'linear' | 'compounding';
export interface WeanStep { step: number; doseMg: number; doseMgKgDose: number; reductionMg: number; }
```

Module pattern (morphine):
- src/lib/morphine/types.ts        -> pure type exports
- src/lib/morphine/morphine-config.json -> embedded clinical config
- src/lib/morphine/calculations.ts -> pure functions importing config + types
- src/lib/morphine/calculations.test.ts -> co-located vitest

Replicate as:
- src/lib/fortification/types.ts
- src/lib/fortification/fortification-config.json
- src/lib/fortification/fortification-config.ts  (loader/validator — morphine doesn't need one because its config is 3 fields; fortification has ~30 rows so a validator is justified)
- src/lib/fortification/fortification-config.test.ts
</interfaces>

Existing `src/lib/formula/formula-config.json` is the source of truth for the manufacturer mapping (Task 2 reads it to derive manufacturer by brand-name match). DO NOT import from it at runtime — Phase 11 will delete it.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define fortification types and read xlsx to confirm row inventory</name>
  <files>src/lib/fortification/types.ts</files>
  <behavior>
    - `BaseType` is the literal union `'breast-milk' | 'water'` (per D-01-locked union style)
    - `UnitType` is `'grams' | 'scoops' | 'teaspoons' | 'tablespoons' | 'packets'`
    - `TargetKcalOz` is the literal union `22 | 24 | 26 | 28 | 30` (per RESEARCH §8 D-01)
    - `FortificationFormula` has: `id: string`, `name: string`, `manufacturer: string`, `displacement_factor: number`, `calorie_concentration: number`, `grams_per_scoop: number` (manufacturer included now per RESEARCH §8 D-02)
    - `FortificationInputs` has: `formula: FortificationFormula`, `base: BaseType`, `volumeMl: number`, `targetKcalOz: TargetKcalOz`, `unit: UnitType`
    - `FortificationResult` has: `amountToAdd: number`, `yieldMl: number`, `exactKcalPerOz: number`, `suggestedStartingVolumeMl: string` (formatted string per RESEARCH §4 — rendering is the calculator's responsibility, not the UI's)
    - All exports compile under TypeScript strict mode
  </behavior>
  <action>
    1. Read `recipe-calculator.xlsx` Calculator tab and inspect rows A3:D35 to confirm exact row count, header columns, and a few values. Use `python3` with `openpyxl` if available, or fall back to `unzip -p recipe-calculator.xlsx xl/worksheets/sheet1.xml` and parse the XML inline. Do NOT add any new dependencies. Record the exact row count (a single integer N) and the column header names in your task notes — Task 2's verify command will hard-code this exact N.
    2. Create `src/lib/fortification/types.ts` with all the types described in `<behavior>`.
    3. Per RESEARCH §8 D-02, `manufacturer` is REQUIRED (non-optional `string`) — Plan 10 needs it for grouping. Task 2 derives values from the existing `src/lib/formula/formula-config.json` brand list.
    4. Use `interface` for object types and `type` for unions, matching morphine's style.
    5. No JSDoc beyond a single one-line comment per type — keep it tight, like morphine/types.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`src/lib/fortification/types.ts` exists, exports all required named types, `tsc --noEmit` exits 0, and the executor has noted the actual xlsx row count (an integer N) for Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transcribe xlsx Calculator tab into fortification-config.json</name>
  <files>src/lib/fortification/fortification-config.json</files>
  <behavior>
    - File is valid JSON with a top-level `formulas` array
    - Each entry has exactly: `id`, `name`, `manufacturer`, `displacement_factor`, `calorie_concentration`, `grams_per_scoop`
    - Row count equals the integer N established in Task 1 (executor fills the literal into the verify command before running it)
    - Spot-check: `Neocate Infant` is present with values that yield 183.5 mL and ~23.5101662125341 kcal/oz for 180 mL + Breast milk + 24 kcal/oz + Teaspoons → amountToAdd 2 tsp (validated in Plan 09-02)
    - Spot-check: `Similac Human Milk Fortifier` (verbatim xlsx label) is present — the only formula that supports Packets
  </behavior>
  <action>
    1. Read every row from the xlsx Calculator tab A3:D35 (use the same extraction approach from Task 1).
    2. For each row, write a JSON entry. `id` is a kebab-case slug of `name` (e.g. `neocate-infant`, `similac-human-milk-fortifier`). `name` is verbatim from the xlsx. `displacement_factor`, `calorie_concentration`, `grams_per_scoop` are the literal numeric values from the xlsx — preserve full precision, do not round.
    3. **Manufacturer mapping (per RESEARCH §8 D-02 / N-3):** open `src/lib/formula/formula-config.json` and build an in-memory map from `brand` (or `name`) → `manufacturer`. For each xlsx row, look up the formula name in this map (case-insensitive, trim whitespace). If found, use that manufacturer string verbatim. If not found, set `manufacturer: ""` (empty string) and add the brand name to a list that the SUMMARY.md will surface for human review. DO NOT guess manufacturers from name prefixes.
    4. If a row has a blank `grams_per_scoop` (some liquid concentrates do), use `0`.
    5. Write the JSON with 2-space indent, sorted by `name` alphabetically for clinician scanability.
    6. Do NOT inline any documentation or comments — JSON has no comments and the file must round-trip cleanly.
    7. Per RESEARCH §8 D-05 / decision lock, do not reuse `src/lib/formula/formula-config.json` as a runtime source. This is a fresh transcription from the xlsx; the legacy config is consulted ONLY at transcription time for the manufacturer lookup.
  </action>
  <verify>
    <automated>node -e "const c = require('./src/lib/fortification/fortification-config.json'); const N = /* TODO: executor inserts exact integer from Task 1 */ 0; if (!Array.isArray(c.formulas)) throw new Error('formulas not array'); if (c.formulas.length !== N) throw new Error('row count mismatch: expected ' + N + ' got ' + c.formulas.length); const neo = c.formulas.find(f =&gt; f.name.toLowerCase().includes('neocate infant')); if (!neo) throw new Error('Neocate Infant missing'); const hmf = c.formulas.find(f =&gt; f.name.toLowerCase().includes('similac') &amp;&amp; f.name.toLowerCase().includes('human milk fortifier')); if (!hmf) throw new Error('Similac HMF missing'); for (const f of c.formulas) { for (const k of ['id','name','manufacturer','displacement_factor','calorie_concentration','grams_per_scoop']) { if (!(k in f)) throw new Error('missing key ' + k + ' in ' + f.name); } } console.log('OK: ' + c.formulas.length + ' formulas, all keys present, Neocate + HMF found');"</automated>
  </verify>
  <done>JSON file exists, parses, contains exactly N entries (where N = row count from Task 1, hard-coded into the verify command), every row has all 6 required fields, Neocate Infant and Similac HMF present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Loader + parser unit tests</name>
  <files>src/lib/fortification/fortification-config.ts, src/lib/fortification/fortification-config.test.ts</files>
  <behavior>
    - `getFortificationFormulas()` returns `FortificationFormula[]` typed against the JSON
    - `getFormulaById(id)` returns the formula or `undefined`
    - Tests assert: array length equals the exact integer N from Task 1, every entry has all required fields, every `displacement_factor` is a finite number ≥ 0, every `calorie_concentration` is a finite number > 0, Neocate Infant lookup succeeds, Similac HMF lookup succeeds
    - No mocks — uses the real JSON
  </behavior>
  <action>
    1. Create `src/lib/fortification/fortification-config.ts`:
       ```typescript
       import type { FortificationFormula } from './types.js';
       import config from './fortification-config.json';

       const formulas: FortificationFormula[] = config.formulas;

       export function getFortificationFormulas(): FortificationFormula[] {
         return formulas;
       }

       export function getFormulaById(id: string): FortificationFormula | undefined {
         return formulas.find((f) => f.id === id);
       }
       ```
    2. Confirm `tsconfig.json` already has `resolveJsonModule: true` (SvelteKit default — verified by morphine compiling). Do NOT modify tsconfig.
    3. Create `src/lib/fortification/fortification-config.test.ts` mirroring morphine's test style: `describe` blocks per concern, `it` per assertion, no setup helpers, no mocks. Cover all behaviors listed in `<behavior>`. Hard-code the exact N from Task 1 in the row-count assertion.
    4. Test colocation only — file lives next to the source, NOT in a `__tests__/` dir (per project memory).
  </action>
  <verify>
    <automated>npx vitest run src/lib/fortification/fortification-config.test.ts</automated>
  </verify>
  <done>Loader and tests exist, all parser tests pass, no type errors, no mocks used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time JSON import | Static asset bundled into the SPA — no runtime fetch, no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | fortification-config.json | accept | Bundled at build time, integrity protected by service worker hash from Phase 4 PWA setup. No runtime mutation path. |
| T-09-02 | Information Disclosure | types.ts, fortification-config.ts | accept | No PHI, no secrets — clinical reference data only, public information from manufacturer literature. |
| T-09-03 | Denial of Service | parser at module load | mitigate | Validation in test suite catches malformed config at CI time, not runtime. JSON is small (~30 entries) — no parse-time DoS surface. |

No new attack surface introduced. Pure additive logic in a clinical PWA with no network/storage/auth touchpoints.
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run src/lib/fortification/` passes
- `git status` shows only the four new files under `src/lib/fortification/`
- No files outside `src/lib/fortification/` are modified
</verification>

<success_criteria>
1. REF-01 satisfied: JSON config embeds exactly N formulas (N established in Task 1) with `displacement_factor`, `calorie_concentration`, `grams_per_scoop`, and `manufacturer` for Phase 10.
2. REF-02 satisfied: The reference table is in JSON, editable by a clinician without touching any `.ts` file.
3. Phase 9 v1.2 regression guard: only additive changes — `npx vitest run` (full suite) still green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-fortification-reference-data-business-logic/09-01-SUMMARY.md` describing: actual xlsx row count N, the manufacturer mapping table built from `src/lib/formula/formula-config.json`, the list of brands with empty manufacturer (for human review), any rows with notable values (e.g. 0 grams_per_scoop for liquids), and the test file's coverage.
</output>
