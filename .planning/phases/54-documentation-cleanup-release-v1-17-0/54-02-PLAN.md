---
phase: 54-documentation-cleanup-release-v1-17-0
plan: 02
type: execute
wave: 2
depends_on:
  - 54-01
files_modified:
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - REL-02
  - REL-03
  - REL-04

must_haves:
  truths:
    - "pnpm check exits 0 with 0 errors and 0 warnings (svelte-check hard gate)"
    - "pnpm test:run exits 0 with all 410 tests passing (vitest hard gate)"
    - "Playwright structural check: e2e/pert.spec.ts and e2e/pert-a11y.spec.ts are absent; remaining spec count is correct"
    - "pnpm build succeeds; AboutSheet version auto-propagates to v1.17.0 via __APP_VERSION__ chain"
    - "STATE.md frontmatter status field reads complete"
    - "PROJECT.md Last-updated footer reflects release date and v1.17.0 release summary"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Milestone close groundwork — status: complete"
      contains: "status: complete"
    - path: ".planning/PROJECT.md"
      provides: "Updated Last-updated footer with release date"
      contains: "Last updated: 2026-05-"
  key_links:
    - from: "package.json version 1.17.0"
      to: "vite.config.ts __APP_VERSION__ define"
      via: "readFileSync at build time"
      pattern: "__APP_VERSION__"
    - from: "__APP_VERSION__"
      to: "src/lib/shared/about-content.ts:13 appVersion"
      via: "Vite define constant injection"
      pattern: "appVersion"
  decisions_honored:
    - "D-01: REL-03 Playwright gate is no-regression vs ~32 baseline, NOT all-green; hard gates are svelte-check 0/0 and vitest 410/410"
    - "D-02: If Playwright matrix is environment-blocked, record structural spec-count check and defer live run to CI — do NOT block milestone on unrunnable local Playwright"
    - "D-03: /gsd:complete-milestone is NOT invoked here — REL-04 task does footer + STATE groundwork only; archive is operator's explicit top-level command after this phase verifies"
    - "D-06: REL-02 is verify-only — pnpm build propagates v1.17.0 automatically via __APP_VERSION__; do NOT edit about-content.ts"
---

<objective>
Run the full clinical gate (REL-03), verify the AboutSheet version propagation (REL-02), then prepare the milestone close groundwork: update STATE.md status to complete and update the PROJECT.md footer (REL-04).

Purpose: Confirm the repo is release-ready at v1.17.0 and ready for the operator to run /gsd:complete-milestone.

Output: Green clinical gate confirmation, STATE.md status=complete, PROJECT.md footer updated to release date.
</objective>

<execution_context>
@/home/ghislain/.claude/get-shit-done/workflows/execute-plan.md
@/home/ghislain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/54-documentation-cleanup-release-v1-17-0/54-RESEARCH.md
@.planning/phases/54-documentation-cleanup-release-v1-17-0/54-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: REL-03 — Run full clinical gate (svelte-check + vitest + Playwright structural + pnpm build)</name>
  <files></files>

  <read_first>
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-RESEARCH.md (REL-03 section — gate commands, baselines, Playwright fallback per D-02; REL-02 section — AboutSheet verification via pnpm build)
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-CONTEXT.md (D-01 — Playwright is no-regression gate, not all-green; D-02 — local Playwright fallback; D-06 — REL-02 is verify-only)
  </read_first>

  <action>
    Run all four gates sequentially from /home/ghislain/src/nicu-assistant. All commands must run from the project root.

    GATE 1 — svelte-check (HARD BLOCKING):
      pnpm check
    Expected: 0 errors, 0 warnings. If any errors or warnings appear, STOP and report them — do not proceed to Gate 2.

    GATE 2 — vitest (HARD BLOCKING):
      pnpm test:run
    Expected: 410 tests pass, 0 fail. If any tests fail, STOP and report — do not proceed.

    GATE 3 — Playwright structural check (always run regardless of environment):
      ls e2e/
    Confirm e2e/pert.spec.ts and e2e/pert-a11y.spec.ts are ABSENT. Count the remaining .spec.ts files.

    GATE 3b — Playwright live run (attempt; fallback per D-02):
      pnpm exec playwright test --reporter=list
    Expected: Failure count must NOT exceed the Phase 52 D-21 baseline of ~32 pre-existing failures (28 axe dlitem + 2 disclaimer-banner + 2 calc-UI). The 4 retired PERT axe sweeps and 2 PERT e2e specs must not appear in results.
    FALLBACK (per D-02): If Playwright cannot run because the browser binaries are missing or the dev server cannot start, record: "Playwright live run deferred to CI — environment-blocked per D-02. Structural check confirmed PERT specs absent." Do NOT treat this as a blocking gate failure.

    GATE 4 — pnpm build (REL-02 verification):
      pnpm build
    Expected: Build succeeds. This triggers Vite to inject __APP_VERSION__ = "1.17.0" from package.json. After build succeeds, check the build output for the version string:
      grep -r "1.17.0" dist/ | head -5
    Expected: One or more matches confirming 1.17.0 appears in the dist output (the AboutSheet content will contain it).

    Record all gate results clearly in the task output with pass/fail/fallback status for each.
  </action>

  <verify>
    <automated>
      cd /home/ghislain/src/nicu-assistant && pnpm check 2>&1 | tail -5
      cd /home/ghislain/src/nicu-assistant && pnpm test:run 2>&1 | tail -10
      ls /home/ghislain/src/nicu-assistant/e2e/pert*.spec.ts 2>&1
      grep -r "1\.17\.0" /home/ghislain/src/nicu-assistant/dist/ 2>/dev/null | head -3
    </automated>
  </verify>

  <acceptance_criteria>
    - pnpm check exits 0 with output containing "0 errors" and "0 warnings"
    - pnpm test:run exits 0 with output containing "410 passed" (or equivalent all-pass line) and "0 failed"
    - ls e2e/pert*.spec.ts returns "No such file or directory" or empty (both PERT spec files absent)
    - pnpm build exits 0
    - grep "1.17.0" dist/ returns at least one match (AboutSheet version propagated to build output)
    - If Playwright cannot run: the task output explicitly records "deferred to CI per D-02" with the structural check result
  </acceptance_criteria>

  <done>Hard gates green: svelte-check 0/0 and vitest 410/410. Build succeeds with v1.17.0 in output. Playwright: either no-regression confirmed (failure count ≤ 32) or deferred to CI per D-02 with structural check showing PERT specs absent.</done>
</task>

<task type="auto">
  <name>Task 2: REL-04 — Flip STATE.md to complete + update PROJECT.md footer</name>
  <files>.planning/STATE.md, .planning/PROJECT.md</files>

  <read_first>
    - .planning/STATE.md (the YAML frontmatter block — fields status, stopped_at, last_updated need updating; also update progress.percent and completed_phases)
    - .planning/PROJECT.md (the Last-updated footer line at the bottom of the file — line 241 currently)
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-RESEARCH.md (REL-04 section — exact current text for both files; what NOT to do: do NOT invoke /gsd:complete-milestone, do NOT move files to milestones/)
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-CONTEXT.md (D-03 — archive is operator's separate step; D-06)
  </read_first>

  <action>
    TWO FILE EDITS.

    EDIT 1 — STATE.md frontmatter (lines 1-15 approximately).

    Update these YAML fields:
      status: planning  →  status: complete
      stopped_at: Phase 54 context gathered  →  stopped_at: Phase 54 complete
      last_updated: "2026-05-24T00:31:18.750Z"  →  last_updated: "<current ISO timestamp at time of edit>"
      progress.completed_phases: 2  →  progress.completed_phases: 3
      progress.total_plans: 4  →  progress.total_plans: 6
      progress.completed_plans: 4  →  progress.completed_plans: 6
      progress.percent: 67  →  progress.percent: 100

    Also update the prose section under ## Current Position:
      Plan: Not started  →  Plan: 54-02 complete
      Status: Ready to plan  →  Status: Complete

    Also update the ## Session Continuity block:
      Last session: 2026-05-24T00:31:18.744Z  →  Last session: <current timestamp>
      Stopped at: Phase 54 context gathered  →  Stopped at: Phase 54 complete — v1.17.0 released

    Also update ## Operator Next Steps to reflect the archive step:
      Replace the current content with:
        - Run `/gsd:complete-milestone` to archive v1.17 to `.planning/milestones/v1.17-{REQUIREMENTS,ROADMAP,phases}/`.

    Do NOT move any files to .planning/milestones/ from within this task (per D-03).

    EDIT 2 — PROJECT.md Last-updated footer.

    The current footer (last line of the file):
      *Last updated: 2026-05-23 — Phases 52–53 complete; PERT source fully removed + favorites upgrade-safety regression locked, suite green at 410/410*

    New footer (use today's date 2026-05-23 as the release date):
      *Last updated: 2026-05-23 — v1.17.0 released; PERT calculator fully removed, documentation synced to 5-calculator reality, clinical gate green (svelte-check 0/0, vitest 410/410)*
  </action>

  <verify>
    <automated>
      grep "^status:" /home/ghislain/src/nicu-assistant/.planning/STATE.md
      grep "^stopped_at:" /home/ghislain/src/nicu-assistant/.planning/STATE.md
      grep "percent:" /home/ghislain/src/nicu-assistant/.planning/STATE.md
      grep "Last updated" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md | tail -1
    </automated>
  </verify>

  <acceptance_criteria>
    - grep "^status:" STATE.md returns "status: complete"
    - grep "^stopped_at:" STATE.md returns "stopped_at: Phase 54 complete"
    - grep "percent:" STATE.md returns "percent: 100"
    - grep "Last updated" PROJECT.md returns the new footer containing "v1.17.0 released"
    - STATE.md does NOT contain any /gsd:complete-milestone invocation or file-move commands
    - STATE.md Operator Next Steps says to run /gsd:complete-milestone (instruction, not execution)
  </acceptance_criteria>

  <done>STATE.md frontmatter status=complete, percent=100, stopped_at=Phase 54 complete. PROJECT.md footer updated to release date and v1.17.0 release summary. Repo is archive-ready for operator to run /gsd:complete-milestone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pnpm scripts → filesystem | Build commands write to dist/; read-only for gate verification |
| State file edits | YAML frontmatter in .planning/STATE.md; no secrets, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02 | Tampering | .planning/STATE.md, .planning/PROJECT.md | accept | Documentation-only edits in a local git repo. No secrets, no PII, no network, no runtime code change. Accidental wrong-string edit mitigated by acceptance_criteria grep gates. |
| T-54-03 | Denial of Service | pnpm build | accept | Build runs locally; no deploy step in this phase. Failure = gate stops; no partial release artifact reaches production. |
| T-54-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. Package Legitimacy Gate N/A. |
</threat_model>

<verification>
After both tasks in this plan:
1. pnpm check exits 0 — 0 errors 0 warnings
2. pnpm test:run exits 0 — 410/410
3. e2e/pert.spec.ts and e2e/pert-a11y.spec.ts absent from e2e/
4. pnpm build exits 0; grep "1.17.0" dist/ has matches
5. grep "^status:" .planning/STATE.md returns "status: complete"
6. grep "Last updated" .planning/PROJECT.md | tail -1 contains "v1.17.0 released"
7. No /gsd:complete-milestone call inside any task (per D-03)
</verification>

<success_criteria>
- Hard clinical gates green: svelte-check 0/0, vitest 410/410
- pnpm build succeeds with v1.17.0 in dist output (REL-02 verified)
- Playwright: no-regression confirmed or deferred to CI per D-02 with structural check
- STATE.md status=complete, percent=100 (REL-04 groundwork)
- PROJECT.md footer updated to v1.17.0 release date summary (REL-04 groundwork)
- Repo is archive-ready; next operator action is /gsd:complete-milestone
</success_criteria>

<output>
Create `.planning/phases/54-documentation-cleanup-release-v1-17-0/54-02-SUMMARY.md` when done.
</output>
