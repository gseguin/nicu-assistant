---
phase: 54-documentation-cleanup-release-v1-17-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - package.json
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-06
  - REL-01

must_haves:
  truths:
    - "PROJECT.md Validated list no longer contains the PERT entry; a new ### Invalidated / Removed subsection exists containing it with v1.17 removal annotation"
    - "PROJECT.md Context section describes five clinical calculators with no PERT bullet and no 'Six clinical calculators' heading or sentence"
    - "PROJECT.md Architecture section lists 5 calculator route shells and 5 state singletons — /pert and pert references absent"
    - "PROJECT.md Key Decisions table row for 'Ship PERT as self-contained workstream' shows outcome: ❌ Removed in v1.17 — out of clinical scope (pediatric, not neonatal)"
    - "MILESTONES.md has a v1.17 entry above the v1.15.1 entry following the established heading format"
    - "package.json version field reads 1.17.0"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Updated documentation — 5-calculator reality, PERT invalidated"
      contains: "### Invalidated / Removed"
    - path: ".planning/MILESTONES.md"
      provides: "v1.17 milestone entry at top"
      contains: "## v1.17 Remove PERT Calculator"
    - path: "package.json"
      provides: "Version bump to 1.17.0"
      contains: '"version": "1.17.0"'
  key_links:
    - from: "package.json"
      to: "vite.config.ts:11"
      via: "readFileSync + define.__APP_VERSION__"
      pattern: "__APP_VERSION__"
    - from: ".planning/PROJECT.md"
      to: "### Invalidated / Removed subsection"
      via: "DOC-01 edit"
      pattern: "### Invalidated / Removed"

decisions_honored:
  - "D-01: REL-03 gate criterion — addressed in plan 02"
  - "D-02: Playwright local fallback — addressed in plan 02"
  - "D-03: /gsd:complete-milestone NOT invoked from executor — REL-04 groundwork in plan 02"
  - "D-04: DOC-01..06 are verbatim edits — each task uses exact old_string/new_string from 54-RESEARCH.md"
  - "D-05: Historical .planning/ PERT references stay intact except where DOC-01..06 explicitly change them (per D-05)"
  - "D-06: package.json version field only; REL-02 is verify-only in plan 02"
  - "D-07: MILESTONES.md v1.17 entry shape follows v1.15.1 template — Phases, Deferred, Notes, Key accomplishments"
---

<objective>
Apply all six DOC edits to PROJECT.md and MILESTONES.md to bring documentation in sync with the 5-calculator reality, then bump package.json to 1.17.0.

Purpose: Satisfy DOC-01..06 and REL-01. After this plan, the documentation reflects the post-PERT state and the build system is primed to emit v1.17.0 in the AboutSheet.

Output: Updated .planning/PROJECT.md (Validated→Invalidated move, Context/Architecture fixes, Key Decisions update), new v1.17 entry in .planning/MILESTONES.md, package.json version 1.17.0.
</objective>

<execution_context>
@/home/ghislain/.claude/get-shit-done/workflows/execute-plan.md
@/home/ghislain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/MILESTONES.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-documentation-cleanup-release-v1-17-0/54-RESEARCH.md
@.planning/phases/54-documentation-cleanup-release-v1-17-0/54-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOC-01 — Move PERT Validated entry to new Invalidated/Removed subsection</name>
  <files>.planning/PROJECT.md</files>

  <read_first>
    - .planning/PROJECT.md (full file — the Validated list, Active subsection, and surrounding context all needed to place the new subsection correctly)
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-RESEARCH.md (DOC-01 section — exact quoted old_string and placement notes)
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-CONTEXT.md (D-04 — verbatim edits only; D-05 — do NOT touch historical text outside the named DOC edits)
  </read_first>

  <action>
    Two-part edit to PROJECT.md — per D-04 (verbatim execution), per DOC-01 requirement, per 54-RESEARCH.md DOC-01 section.

    PART A — Remove the PERT line from ### Validated.

    Find this exact line (currently around PROJECT.md line 118):
      - ✓ Pediatric EPI PERT Calculator (sixth clinical calculator) shipped as self-contained workstream (`milestones/ws-pert-2026-04-26/`, internal phase numbering 01-05, did NOT consume main-roadmap phase numbers): `CalculatorId` extended with `'pert'`, `.identity-pert` OKLCH hue, `src/lib/pert/` module with config + state + parity fixtures + tests, `/pert` route, AboutSheet entry, registry entry — v1.15

    Delete that line from the ### Validated block.

    PART B — Create the new ### Invalidated / Removed subsection.

    This subsection does NOT currently exist. Insert it after the last entry of ### Validated (the v1.15.1 Release line that ends with "— v1.15.1 (Phase 51)") and BEFORE the ### Active subsection.

    The new subsection to insert:

      ### Invalidated / Removed

      - ✗ Pediatric EPI PERT Calculator (sixth clinical calculator) shipped as self-contained workstream (`milestones/ws-pert-2026-04-26/`, internal phase numbering 01-05, did NOT consume main-roadmap phase numbers): `CalculatorId` extended with `'pert'`, `.identity-pert` OKLCH hue, `src/lib/pert/` module with config + state + parity fixtures + tests, `/pert` route, AboutSheet entry, registry entry — v1.15 — v1.17: out of clinical scope (pediatric, not neonatal)

    CRITICAL: The ### Active subsection must immediately follow this new subsection with one blank line. Confirm the resulting ordering is: ### Validated (with PERT removed) → blank line → ### Invalidated / Removed (new, with the moved PERT entry) → blank line → ### Active.

    Do NOT touch any other line in the ### Validated list. The "all six calculators" reference in the Current State block (line 11, in the v1.15.1 shipped description) is a historical fact under D-05 — leave it unchanged.
  </action>

  <verify>
    <automated>
      grep -n "### Invalidated / Removed" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
      grep -c "### Invalidated / Removed" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
      grep -n "v1.17: out of clinical scope (pediatric, not neonatal)" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
      grep -n "✓ Pediatric EPI PERT" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
    </automated>
  </verify>

  <acceptance_criteria>
    - grep -c "### Invalidated / Removed" returns 1
    - grep -n "v1.17: out of clinical scope (pediatric, not neonatal)" returns exactly one match containing the PERT bullet with ✗ prefix
    - grep -n "✓ Pediatric EPI PERT" returns 0 (the ✓ PERT line is gone from Validated)
    - The file has the order: ### Validated → ### Invalidated / Removed → ### Active (confirm with grep -n "### Validated\|### Invalidated\|### Active")
  </acceptance_criteria>

  <done>PROJECT.md Validated list no longer contains the PERT entry; the new ### Invalidated / Removed subsection exists with the PERT entry annotated "— v1.17: out of clinical scope (pediatric, not neonatal)"</done>
</task>

<task type="auto">
  <name>Task 2: DOC-02/03 — Fix six-calculator references in PROJECT.md Context and Architecture</name>
  <files>.planning/PROJECT.md</files>

  <read_first>
    - .planning/PROJECT.md (already in context from Task 1; focus on the ## Context section starting around line 162 and the Architecture sub-block)
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-RESEARCH.md (DOC-02 section — four exact strings A/B/C/D; DOC-03 section — same String D plus the CalculatorStore all-6 line; Pitfall 3 and Pitfall 6 warnings)
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-CONTEXT.md (D-04 — verbatim; D-05 — historical record untouched)
  </read_first>

  <action>
    Four edits in the ## Context section and one edit in the Architecture sub-block. All per D-04 (verbatim). Scope is strictly lines within ## Context (lines ~162+). Do NOT touch ## Current State (lines ~8-11).

    EDIT A — "Six clinical calculators" paragraph opener.
    Old: **Shipped v1.15.1** under package v1.16.x. Six clinical calculators, all with the v1.13 `<HeroResult>` above-the-fold pattern
    New: **Shipped v1.15.1** under package v1.16.x. Five clinical calculators, all with the v1.13 `<HeroResult>` above-the-fold pattern

    EDIT B — Six-calculator list heading.
    Old: **Six clinical calculators:**
    New: **Five clinical calculators:**

    EDIT C — Remove the PERT bullet from the calculator list.
    Delete the line:
      - PERT: Pediatric Enzyme Replacement Therapy dosing (v1.15 workstream)
    The surrounding bullets (UAC/UVC above and the blank/tech-stack line below) remain.

    EDIT D — CalculatorPage route enumeration (covers both DOC-02 and DOC-03 per RESEARCH.md).
    Old: Shared `<CalculatorPage>` shell + `CalculatorModule` contract collapses all 6 calculator route shells (`/morphine-wean`, `/formula`, `/gir`, `/feeds`, `/uac-uvc`, `/pert`) into single-line route imports (refactor `0ec8f98`)
    New: Shared `<CalculatorPage>` shell + `CalculatorModule` contract collapses all 5 calculator route shells (`/morphine-wean`, `/formula`, `/gir`, `/feeds`, `/uac-uvc`) into single-line route imports (refactor `0ec8f98`)

    EDIT E — CalculatorStore "all 6" factual update (Claude's discretion per RESEARCH.md Pitfall 6 — factually correct, same task).
    Old: all 6 calculator state singletons migrated to it
    New: all 5 calculator state singletons migrated to it

    CRITICAL: The "Six clinical calculators" reference in ## Current State line 11 ("single source of truth across all six calculators") is a v1.15.1 historical fact under D-05 — do NOT change it. Scope all edits to ## Context section only.
  </action>

  <verify>
    <automated>
      grep -n "Six clinical calculators" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
      grep -n "Five clinical calculators" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
      grep -n "PERT: Pediatric Enzyme" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
      grep -n "all 5 calculator route shells" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
      grep -n "all 5 calculator state singletons" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
    </automated>
  </verify>

  <acceptance_criteria>
    - grep -n "Six clinical calculators" returns only matches inside ## Current State (historical) — zero matches in ## Context section
    - grep -n "Five clinical calculators" returns 2 matches (the bold heading and the paragraph opener), both in ## Context section
    - grep -n "PERT: Pediatric Enzyme" returns 0 (bullet removed)
    - grep -n "all 5 calculator route shells" returns 1 match
    - grep -n "all 5 calculator state singletons" returns 1 match
    - grep -n "/pert" in the Context/Architecture area returns 0 (the route was removed from the enumeration)
  </acceptance_criteria>

  <done>PROJECT.md Context section describes five clinical calculators with no PERT bullet; route enumeration and state-singleton count both read 5; historical v1.15.1 "all six calculators" in Current State is untouched</done>
</task>

<task type="auto">
  <name>Task 3: DOC-04 (verify absent) + DOC-06 — Confirm no Glossary; update Key Decisions PERT outcome</name>
  <files>.planning/PROJECT.md</files>

  <read_first>
    - .planning/PROJECT.md (focus on ## Key Decisions table and full-file grep for "Glossary" or "Acronyms" sections)
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-RESEARCH.md (DOC-04 section — confirmed no-op; DOC-06 section — exact old and new Outcome cell text)
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-CONTEXT.md (D-04 — verbatim; specifics block — exact DOC-06 replacement string)
  </read_first>

  <action>
    TWO ACTIONS in this task:

    ACTION 1 — DOC-04 verify-absent (no file edit).
    Confirm PROJECT.md has no ## Glossary or ## Acronyms section by searching the file. This requirement is satisfied vacuously — the Glossary section does not exist. Record the finding in the task output: "DOC-04: Glossary/acronyms section absent from PROJECT.md — requirement satisfied vacuously; no edit required." No file change.

    ACTION 2 — DOC-06 Key Decisions table update.
    Find the exact row in the ## Key Decisions table (currently around line 214):

    Old Outcome cell text:
      ⚠️ Revisit — v1.15 PERT later removed in v1.16 (out of clinical scope)

    New Outcome cell text (per D-06, per CONTEXT.md specifics block):
      ❌ Removed in v1.17 — out of clinical scope (pediatric, not neonatal)

    The full row structure (Decision | Rationale | Outcome) stays intact. Only the Outcome cell changes. The historical decision row is retained for traceability (per D-04 / REQUIREMENTS.md DOC-06).

    The precise table row to find and edit:
      | Ship PERT as self-contained workstream (not main-roadmap phase) | PERT was a sixth calculator that needed its own architecture/identity/UI/tests cycle independent of the v1.15.1 iOS hotfix work proceeding in parallel; workstream model lets it ship without consuming main-roadmap phase numbers | ⚠️ Revisit — v1.15 PERT later removed in v1.16 (out of clinical scope) |

    Replace only the Outcome cell portion. The Decision and Rationale columns are unchanged.
  </action>

  <verify>
    <automated>
      grep -n "## Glossary\|## Acronyms" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
      grep -n "Revisit.*v1.15 PERT later removed" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
      grep -n "Removed in v1.17.*out of clinical scope (pediatric, not neonatal)" /home/ghislain/src/nicu-assistant/.planning/PROJECT.md
    </automated>
  </verify>

  <acceptance_criteria>
    - grep "## Glossary\|## Acronyms" returns 0 (confirmed absent — DOC-04 no-op)
    - grep "Revisit.*v1.15 PERT later removed" returns 0 (old Outcome cell gone)
    - grep "Removed in v1.17.*out of clinical scope (pediatric, not neonatal)" returns 1 (new Outcome cell present in the Key Decisions table row)
    - The Decision and Rationale columns of the "Ship PERT" row are unchanged (still reads "Ship PERT as self-contained workstream..." and the full rationale text)
  </acceptance_criteria>

  <done>DOC-04: Glossary absence confirmed, no edit made. DOC-06: Key Decisions "Ship PERT" outcome updated to ❌ Removed in v1.17 — out of clinical scope (pediatric, not neonatal); historical row retained.</done>
</task>

<task type="auto">
  <name>Task 4: DOC-05 — Add v1.17 entry to MILESTONES.md + REL-01 — Bump package.json to 1.17.0</name>
  <files>.planning/MILESTONES.md, package.json</files>

  <read_first>
    - .planning/MILESTONES.md (the v1.15.1 entry at the top is the exact format template to mirror; read it to capture the heading format and subsection order)
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-RESEARCH.md (DOC-05 section — format template quoted; target shape for v1.17 entry per D-07; Pitfall 1 warning about annotating the wrong location; REL-01 section — package.json current value and target)
    - .planning/phases/54-documentation-cleanup-release-v1-17-0/54-CONTEXT.md (D-07 — MILESTONES entry shape; D-06 — version bump mechanics)
    - .planning/STATE.md (Deferred Items table — SMOKE-01..10 + REL-04-smoke carry-forward, to populate the "Known deferred items at close" subsection)
    - .planning/phases/52-code-purge-test-suite-repair/52-01-SUMMARY.md, 52-02-SUMMARY.md, 52-03-SUMMARY.md (source for Key accomplishments bullets)
    - .planning/phases/53-favorites-safety-net-verification/53-01-SUMMARY.md (source for Key accomplishments bullets)
  </read_first>

  <action>
    TWO FILE EDITS in this task.

    EDIT 1 — MILESTONES.md: Insert the v1.17 entry ABOVE the current ## v1.15.1 entry (i.e., at the very top of the file, right after the "# Milestones" heading line).

    The new entry follows the v1.15.1 heading format exactly (per D-07). This phase (54) is plan 1 of phase 54, so Phase 54 contributes 2 plans total (54-01 + 54-02). Insert the following block:

    ---

    ## v1.17 Remove PERT Calculator (Shipped: 2026-05-23)

    **Phases completed:** 3 phases (52, 53, 54), 6 plans total (Phase 52: 3 plans, Phase 53: 1 plan, Phase 54: 2 plans)

    **Known deferred items at close:** 11 (SMOKE-01..10 + REL-04 smoke-sign-off portion) — see STATE.md Deferred Items. SMOKE-10 re-scoped to 5 calculators when eventually run (PERT removed).

    **Notes:**
    - v1.16 milestone label intentionally skipped — package version drifted to 1.16.x during v1.15.1 quick-task patches before milestone close. v1.17 re-aligns the milestone label with the package version (1.16.1 → 1.17.0).
    - v1.15 PERT Calculator (shipped as self-contained workstream `ws-pert-2026-04-26`) removed in this milestone — out of clinical scope (pediatric, not neonatal). See PROJECT.md Invalidated / Removed. Workstream archive preserved as historical record.

    **Key accomplishments:**

    - Atomic PERT purge across Phases 52–53: deleted `src/lib/pert/` (14 files, 2166 LOC, 56 vitest tests), `src/routes/pert/+page.svelte`, `e2e/pert.spec.ts`, and `e2e/pert-a11y.spec.ts` (32 Playwright tests); removed `pertModule` from `CALCULATOR_REGISTRY` (5 alphabetical entries remain); narrowed `CalculatorId` union to `'morphine-wean' | 'formula' | 'gir' | 'feeds' | 'uac-uvc'`; deleted `pert:` from `about-content.ts` and `.identity-pert` from `app.css` (both themes).
    - Test suite repaired to green immediately after purge: registry.test.ts, HamburgerMenu.test.ts, CalculatorPage.test.ts, calculator-store.test.ts, favorites.test.ts, drawer-no-autofocus.spec.ts, desktop-full-nav.spec.ts all updated for 5-calculator world; T-21 regression sentinel added; vitest dropped from 454 to 410/410 (44 PERT-owned tests retired); svelte-check 0/0 throughout.
    - Favorites upgrade-safety regression locked (Phase 53, SAFE-01..03): existing `recover()` filter proven to silently drop `'pert'` from stored favorites for v1.15+ upgraders (SAFE-02 asserts order-preserved drop); SAFE-03 guards first-run defaults `['feeds','formula','gir','morphine-wean']` contain no `'pert'`.

    ---

    IMPORTANT PLACEMENT: The new block goes between "# Milestones" (the H1 heading) and "## v1.15.1 iOS Polish & Drawer Hardening" (the current first entry). Use a blank line before and after the new block as separator.

    EDIT 2 — package.json: Change the version field.
    Old: "version": "1.16.1",
    New: "version": "1.17.0",

    No other package.json edits. Do NOT run pnpm install — a plain version field bump does not change any resolved dependency and pnpm does not update the lockfile for it (per RESEARCH.md REL-01 section; assumption A1).
  </action>

  <verify>
    <automated>
      grep -n "## v1.17 Remove PERT Calculator" /home/ghislain/src/nicu-assistant/.planning/MILESTONES.md
      grep -n "## v1.15.1" /home/ghislain/src/nicu-assistant/.planning/MILESTONES.md | head -1
      node -e "const p=JSON.parse(require('fs').readFileSync('/home/ghislain/src/nicu-assistant/package.json','utf-8')); console.log(p.version)"
    </automated>
  </verify>

  <acceptance_criteria>
    - grep -n "## v1.17 Remove PERT Calculator" returns line number lower than the line number of "## v1.15.1" (v1.17 entry is above v1.15.1 entry)
    - MILESTONES.md contains "Phases completed: 3 phases (52, 53, 54), 6 plans total"
    - MILESTONES.md contains "SMOKE-01..10 + REL-04 smoke-sign-off"
    - MILESTONES.md contains the "v1.16 milestone label intentionally skipped" note
    - node version check prints "1.17.0"
    - package.json does NOT contain "1.16.1" in the version field
  </acceptance_criteria>

  <done>MILESTONES.md has a v1.17 entry as the first ## heading after the H1, following the established format with Phases/Deferred/Notes/Key accomplishments subsections. package.json version is 1.17.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor→filesystem | Markdown text edits; no untrusted input, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | .planning/PROJECT.md, MILESTONES.md, package.json | accept | No attack surface — documentation + version-bump edits to static planning files in a local git repo. No secrets, no PII, no network, no runtime code change. Tampering risk = accidental wrong-string edit, mitigated by exact old_string matching and acceptance_criteria grep gates. |
| T-54-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — Package Legitimacy Gate N/A (per RESEARCH.md Package Legitimacy Audit section). |
</threat_model>

<verification>
After all four tasks:
1. grep -c "### Invalidated / Removed" .planning/PROJECT.md == 1
2. grep -c "✓ Pediatric EPI PERT" .planning/PROJECT.md == 0
3. grep -c "Five clinical calculators" .planning/PROJECT.md == 2
4. grep -c "PERT: Pediatric Enzyme" .planning/PROJECT.md == 0
5. grep -c "all 5 calculator route shells" .planning/PROJECT.md == 1
6. grep -c "Removed in v1.17.*out of clinical scope (pediatric, not neonatal)" .planning/PROJECT.md == 1
7. grep -n "## v1.17" .planning/MILESTONES.md returns a line number lower than grep -n "## v1.15.1" .planning/MILESTONES.md
8. node -e "console.log(JSON.parse(require('fs').readFileSync('package.json','utf-8')).version)" prints 1.17.0
</verification>

<success_criteria>
- All six DOC edits applied (DOC-01: PERT moved to Invalidated/Removed; DOC-02: Context five-calculator; DOC-03: route enumeration 5; DOC-04: Glossary absent confirmed; DOC-05: v1.17 MILESTONES entry at top; DOC-06: Key Decisions outcome updated)
- REL-01: package.json reports "version": "1.17.0"
- No historical text in Current State or v1.15.1 MILESTONES entry was changed
- git diff shows exactly the named files modified with the expected content changes
</success_criteria>

<output>
Create `.planning/phases/54-documentation-cleanup-release-v1-17-0/54-01-SUMMARY.md` when done.
</output>
