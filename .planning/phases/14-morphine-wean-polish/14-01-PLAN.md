---
phase: 14-morphine-wean-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/morphine/MorphineWeanCalculator.svelte
autonomous: true
requirements: [MORPH-01, MORPH-02]
must_haves:
  truths:
    - "Morphine step cards use text-2xs eyebrows matching Fortification polish"
    - "Summary + Verification rhythm aligned with .impeccable.md (calmer gaps)"
    - "All 41 morphine tests continue to pass unchanged"
    - "Dock-style magnification on step cards continues to work"
    - "MORPH-02 vacuously satisfied: calculator does not use SelectPicker; new shared picker is available across the app"
  artifacts:
    - path: "src/lib/morphine/MorphineWeanCalculator.svelte"
      provides: "Polished morphine wean calculator UI"
      contains: "text-2xs"
---

<objective>
Apply the same visual polish language from Phase 13 (Fortification) to the Morphine Wean calculator: eyebrow parity on step cards (`text-xs` → `text-2xs font-semibold uppercase tracking-wide`), dosing-meta secondary text weight alignment. MORPH-02 is vacuously satisfied — the morphine calculator has no SelectPicker consumers, so the new shared picker applies by existing import (no regression surface).

Scope is strictly markup / classes. No logic, state, config, or test edits.
</objective>

<context>
@src/lib/morphine/MorphineWeanCalculator.svelte
@src/lib/morphine/MorphineWeanCalculator.test.ts
@src/lib/fortification/FortificationCalculator.svelte (Phase 13 reference)
@.impeccable.md

Test contract to preserve (from MorphineWeanCalculator.test.ts):
- `screen.getByLabelText('Dosing weight')`, `'Max morphine dose'`, `'Decrease per step'` (3 NumericInput labels)
- `screen.getByText(/Step 1 — Starting dose/)` — the literal phrase
- `screen.getByText('Enter values above to generate weaning schedule.')`
- `screen.getByText('Start')` and `screen.getByText('Total reduction')`
- Step card className MUST contain `will-change-transform` and MUST NOT contain `border-l-`
- Clear button className MUST contain `text-secondary` (substring) and MUST NOT contain `text-tertiary` — current class `text-[var(--color-text-secondary)]` satisfies this
- Spreadsheet-parity in `calculations.test.ts` — untouched by design

No edits to: `calculations.ts`, `state.svelte.ts`, `morphine-config.json`, `types.ts`, `MorphineWeanCalculator.test.ts`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step card eyebrow + meta typography sweep (MORPH-01)</name>
  <files>src/lib/morphine/MorphineWeanCalculator.svelte</files>
  <read_first>
    - src/lib/morphine/MorphineWeanCalculator.svelte (lines 281-306 — schedule step card block)
    - src/lib/morphine/MorphineWeanCalculator.test.ts (test contract)
    - src/lib/fortification/FortificationCalculator.svelte (Phase 13 polish reference for eyebrow classes)
  </read_first>
  <action>
Within the step card (`<div data-step-index={i} …>`), make the following class adjustments ONLY:

1. The step eyebrow `<span>` (currently `text-xs font-semibold text-[var(--color-text-secondary)]`) changes to:
   `text-2xs font-semibold uppercase tracking-wide text-[var(--color-text-secondary)]`

2. The reduction meta `<span>` (currently `text-xs font-medium text-[var(--color-text-tertiary)]`) changes to:
   `text-2xs font-medium text-[var(--color-text-tertiary)]`

3. The dose/kg meta `<div>` (currently `text-xs text-[var(--color-text-secondary)] num`) changes to:
   `text-2xs font-medium text-[var(--color-text-secondary)] num`

Rules:
- DO NOT change the literal text content of any element — "Step 1 — Starting dose", "Step {step}", reduction format, numeric bindings.
- DO NOT remove `will-change-transform` from the step card root (test asserts it).
- DO NOT add a `border-l-*` class to the step card (test asserts its absence).
- DO NOT touch the Clear button class (test asserts substring `text-secondary`, absence of `text-tertiary`).
- DO NOT touch the summary card (lines ~262-276) — Phase 13 deliberately mirrored it; no further change needed.
- DO NOT touch the mode switcher, Inputs card, or empty state.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm vitest run src/lib/morphine/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'text-2xs font-semibold uppercase tracking-wide' src/lib/morphine/MorphineWeanCalculator.svelte` is ≥ 1
    - `grep -n 'Step 1 — Starting dose' src/lib/morphine/MorphineWeanCalculator.svelte` still returns exactly 1 match (text content unchanged)
    - `grep -n 'will-change-transform' src/lib/morphine/MorphineWeanCalculator.svelte` still returns exactly 1 match
    - `grep -n 'border-l-' src/lib/morphine/MorphineWeanCalculator.svelte` returns no matches on step card
    - `pnpm vitest run src/lib/morphine/` — all tests pass (MorphineWeanCalculator.test.ts + calculations.test.ts)
    - `git diff src/lib/morphine/calculations.ts src/lib/morphine/state.svelte.ts src/lib/morphine/morphine-config.json src/lib/morphine/types.ts` is empty
    - `git diff --stat` shows only `src/lib/morphine/MorphineWeanCalculator.svelte` modified
  </acceptance_criteria>
  <done>Step card eyebrows + dose-meta typography align with the Phase 13 Fortification polish language; all morphine tests pass.</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run src/lib/morphine/` — green (MorphineWeanCalculator.test.ts + calculations.test.ts)
2. `pnpm vitest run` — full suite green
3. `pnpm build` — green
4. `git diff --stat` — only MorphineWeanCalculator.svelte modified
5. `grep -rn "import.*from '\$lib/shared/components/SelectPicker.svelte'" src/lib/morphine/` — zero matches (calculator has no SelectPicker consumers; MORPH-02 vacuously satisfied by the fact that the shared picker is already the one Phase 12 rewrote)
</verification>

<success_criteria>
- MORPH-01: Step card eyebrows + meta typography match `.impeccable.md` rhythm and Phase 13 Fortification polish.
- MORPH-02: Vacuously satisfied — morphine calculator has no SelectPicker usages to regress.
- All morphine tests pass unchanged.
- Dock magnification logic untouched (lines 14-93).
- Spreadsheet-parity baseline unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/14-morphine-wean-polish/14-01-SUMMARY.md`.
</output>
