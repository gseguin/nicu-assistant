---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shared/components/SelectPicker.svelte
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04, SEARCH-05, SEARCH-06]
must_haves:
  truths:
    - "SelectPicker accepts a searchable prop defaulting to false"
    - "When searchable=true and open, a text input renders inside the dialog"
    - "Typing filters options by case-insensitive substring across label and group"
    - "An empty filter result shows a 'No matches' element with role='status'"
    - "ArrowDown from search input moves focus to first filtered option; ArrowUp from first option returns focus to search input"
    - "Enter in the search input selects the option when exactly one filtered match remains"
    - "Reopening the picker resets searchQuery to '' and focuses the search input"
    - "When searchable=false the component behaves byte-identically to v1.4 (existing tests T-01..T-11 pass)"
  artifacts:
    - path: src/lib/shared/components/SelectPicker.svelte
      provides: "Searchable opt-in prop, search input, filtered listbox, no-matches state, conditional combobox ARIA"
      contains: "searchable"
  key_links:
    - from: src/lib/shared/components/SelectPicker.svelte
      to: filteredOptions
      via: "$derived filter on label + group"
      pattern: "filteredOptions"
---

<objective>
Add an opt-in `searchable` prop to the shared SelectPicker that layers search-input,
substring filtering, "No matches" state, bidirectional keyboard traversal between
input and listbox, Enter-to-select-single-match, and reset-on-reopen — without
changing any behavior for existing non-searchable consumers.

Purpose: Foundation for SEARCH-01..06. The Formula picker wires it up in Plan 02.
Output: Modified SelectPicker.svelte with all searchable behavior in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-searchable-selectpicker/18-RESEARCH.md
@src/lib/shared/components/SelectPicker.svelte
@src/lib/shared/types.ts
@src/test-setup.ts

<interfaces>
From src/lib/shared/types.ts:
```ts
export interface SelectOption {
  value: string;
  label: string;
  group?: string;
}
```

Current SelectPicker `$props()` (lines 7-19) takes: label, options, value (bindable),
placeholder, ariaLabel?, etc. Existing trigger uses `aria-haspopup="dialog"` and the
component uses native `<dialog>.showModal()` with roving tabindex on options.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add searchable prop, state, derived filter, and conditional trigger ARIA</name>
  <files>src/lib/shared/components/SelectPicker.svelte</files>
  <read_first>
    - src/lib/shared/components/SelectPicker.svelte (full file — must understand current line layout 1-242)
    - .planning/phases/18-searchable-selectpicker/18-RESEARCH.md sections "Current SelectPicker.svelte structure", "Recommended derived-state shape", "Trigger ARIA — conditional combobox"
  </read_first>
  <action>
    Modify src/lib/shared/components/SelectPicker.svelte:

    1. In the `$props()` destructure (around lines 7-19), add `searchable = false` with type `boolean` (optional prop).

    2. In the state block (around lines 29-32), add:
       - `let searchInput = $state<HTMLInputElement | null>(null);`
       - `let listboxEl = $state<HTMLDivElement | null>(null);`
       - `let searchQuery = $state('');`

    3. In the derived block (around lines 34-41), add:
       - `const normalizedQuery = $derived(searchQuery.trim().toLowerCase());`
       - `const isSearching = $derived(searchable && normalizedQuery.length > 0);`
       - `const filteredOptions = $derived(isSearching ? options.filter((o) => o.label.toLowerCase().includes(normalizedQuery) || (o.group?.toLowerCase().includes(normalizedQuery) ?? false)) : options);`
       - `const showGrouped = $derived(hasGroups && !isSearching);`

    4. Update the trigger `<button>` (around line 118) so role/aria flips ONLY when searchable:
       - `aria-haspopup={searchable ? 'listbox' : 'dialog'}`
       - `aria-controls={searchable ? listboxId : undefined}`
       - `role={searchable ? 'combobox' : undefined}`
       Keep `aria-labelledby={labelId}` and `aria-expanded` unchanged.

    5. Bind `listboxEl` to the listbox container div (around line 153): add `bind:this={listboxEl}`.

    Per D-research SEARCH-05: default `searchable=false` so all existing call sites
    (FortificationCalculator.svelte:153,166,170,174) and tests T-01..T-11 are unchanged.
    DO NOT touch the grouped rendering branches in this task.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/shared/components/SelectPicker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "searchable" src/lib/shared/components/SelectPicker.svelte` shows the prop, the derived isSearching, and the conditional ARIA wiring (≥4 hits)
    - `grep -n "filteredOptions" src/lib/shared/components/SelectPicker.svelte` shows the $derived
    - `grep -n "bind:this={listboxEl}" src/lib/shared/components/SelectPicker.svelte` shows binding
    - `grep -n "showGrouped" src/lib/shared/components/SelectPicker.svelte` shows derived flag
    - All 11 existing SelectPicker tests still pass (T-01..T-11) — defaults must be byte-equivalent
    - `pnpm svelte-kit sync && pnpm tsc --noEmit` passes (no type errors)
  </acceptance_criteria>
  <done>
    Searchable plumbing exists; existing tests pass; no consumer behavior change yet.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add search input, flat-when-searching template branch, "No matches", and keyboard handlers</name>
  <files>src/lib/shared/components/SelectPicker.svelte</files>
  <read_first>
    - src/lib/shared/components/SelectPicker.svelte (current state after Task 1)
    - .planning/phases/18-searchable-selectpicker/18-RESEARCH.md sections "Code Example — proposed searchable block", "Common Pitfalls" (all 6), "How the Pert Reference Differs"
    - /mnt/data/src/pert-calculator/src/lib/components/SelectPicker.svelte (handleSearchKeydown reference only — DO NOT copy styling)
  </read_first>
  <action>
    Continue editing src/lib/shared/components/SelectPicker.svelte:

    1. Add `handleSearchKeydown(e: KeyboardEvent)` function:
       - On `ArrowDown`: `e.preventDefault()`, then `await tick()` and call `focusOption(0)` against `filteredOptions`. If `filteredOptions.length === 0`, do nothing.
       - On `Enter`: ALWAYS `e.preventDefault()` (Pitfall #3 — prevent enclosing form submit). If `filteredOptions.length === 1`, call the existing `select(filteredOptions[0].value)` (or whatever the existing select helper is named — match the file).
       - On `Escape`: let native dialog handle it (don't intercept).

    2. Extend `handleListboxKeydown` ArrowUp branch (around lines 74-92): if `searchable && focusedIndex <= 0`, set `focusedIndex = -1`, call `searchInput?.focus()`, and `e.preventDefault()`. Otherwise existing behavior.

    3. Update `openPicker()` (around lines 47-55): set `searchQuery = ''` BEFORE `dialog.showModal()`. After `await tick()`, branch: if `searchable` then `searchInput?.focus()`; else existing `focusOption(...)` path.

    4. Update `handleClose()` (around lines 98-102): also `searchQuery = ''` (belt-and-suspenders for SEARCH-06 / Pitfall #4).

    5. Add `$effect(() => { searchQuery; focusedIndex = -1; })` near other effects (Pitfall #2 — keep focusedIndex in sync with filteredOptions).

    6. In the template, AFTER the `<header>` block (around line 151) and BEFORE the listbox container, add:
    ```svelte
    {#if searchable}
      <div class="border-b border-[var(--color-border)] px-4 py-3">
        <input
          bind:this={searchInput}
          type="text"
          class="w-full min-h-12 bg-transparent text-sm font-medium text-[var(--color-text-primary)] outline-none placeholder:text-[var(--color-text-tertiary)]"
          placeholder="Search..."
          aria-label="Filter {label}"
          bind:value={searchQuery}
          onkeydown={handleSearchKeydown}
        />
      </div>
    {/if}
    ```

    7. Restructure the grouped/flat rendering (lines 161-217). Current shape iterates `hasGroups ? groups : options`. Change the outer guard to `showGrouped` (NOT `hasGroups`). When `showGrouped` is false, render the flat each over `filteredOptions` (NOT `options`). The non-searchable, non-grouped path must continue to render `filteredOptions` which equals `options` when not searching — verify equivalence.

    8. After the flat `{#each}` close, add:
    ```svelte
    {#if searchable && filteredOptions.length === 0}
      <p role="status" class="px-4 py-3 text-sm text-[var(--color-text-tertiary)]">
        No matches
      </p>
    {/if}
    ```

    Tailwind 4 + CSS custom properties only. Use `var(--color-*)` tokens, NOT pert's
    `text-primary` Tailwind classes (Pitfall: theme system mismatch). Wrapper
    `min-h-12` honors CLAUDE.md 48px touch target.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/shared/components/SelectPicker.test.ts src/lib/fortification/FortificationCalculator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "handleSearchKeydown" src/lib/shared/components/SelectPicker.svelte` shows function definition AND `onkeydown` wiring (≥2 hits)
    - `grep -n "No matches" src/lib/shared/components/SelectPicker.svelte` shows the empty state with role="status"
    - `grep -n 'role="status"' src/lib/shared/components/SelectPicker.svelte` finds the No-matches node
    - `grep -n "showGrouped" src/lib/shared/components/SelectPicker.svelte` shows the template guard now uses `showGrouped` (not `hasGroups`)
    - `grep -n "searchQuery = ''" src/lib/shared/components/SelectPicker.svelte` appears in BOTH `openPicker` and `handleClose` (≥2 hits)
    - `grep -n "min-h-12" src/lib/shared/components/SelectPicker.svelte` shows 48px touch target on search input wrapper or input
    - `grep -n 'aria-label="Filter' src/lib/shared/components/SelectPicker.svelte` shows the input has a real label (not just placeholder)
    - All 11 existing SelectPicker tests still pass (T-01..T-11)
    - FortificationCalculator UI-04 trigger-count test still passes (4 triggers)
    - `pnpm tsc --noEmit` clean
  </acceptance_criteria>
  <done>
    SelectPicker fully supports searchable mode. Existing consumers and tests
    remain green. No consumer has wired `searchable={true}` yet — that lands in Plan 02.
  </done>
</task>

</tasks>

<verification>
- pnpm vitest run src/lib/shared/components/SelectPicker.test.ts → 11/11 green
- pnpm vitest run src/lib/fortification/FortificationCalculator.test.ts → green (UI-04 still finds 4 triggers)
- pnpm tsc --noEmit → clean
- Grep checks in acceptance_criteria all hit
</verification>

<success_criteria>
- searchable prop exists with default false
- filteredOptions $derived filters by label + group, case-insensitive substring
- Search input renders only when searchable=true, has real aria-label and 48px target
- "No matches" empty state uses role="status"
- ArrowDown from input → first option; ArrowUp from first option → input
- Enter selects when filteredOptions.length === 1, always preventDefault
- openPicker and handleClose both reset searchQuery to ''
- All 11 existing SelectPicker tests pass unchanged
- showGrouped = hasGroups && !isSearching guards template to avoid empty-group rendering
</success_criteria>

<output>
After completion, create `.planning/phases/18-searchable-selectpicker/18-01-SUMMARY.md`
</output>
