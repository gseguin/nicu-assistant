---
plan_id: 42.1-06
phase: 42.1
phase_name: Design Polish + Redesign Sweep (Impeccable Critique Remainder)
plan_name: Polish Residue + Cross-Cutting Tests + Visual Regression + UI-SPEC Amendment
wave: 5
depends_on: ["42.1-01", "42.1-02", "42.1-03", "42.1-04", "42.1-05"]
files_modified:
  - src/lib/feeds/FeedAdvanceCalculator.svelte
  - src/lib/morphine/MorphineWeanCalculator.svelte
  - src/lib/shell/HamburgerMenu.svelte
  - src/lib/gir/GirCalculator.svelte
  - .planning/phases/42-uac-uvc-calculator/42-UI-SPEC.md
files_created:
  - e2e/mobile-nav-clearance.spec.ts
  - .planning/ui-reviews/42.1-screenshots/README.md
files_deleted: []
autonomous: true
requirements: [D-25, D-27, D-28, D-29, D-30, D-31, D-32, D-33]
estimated_commits: 8
---

# Plan 42.1-06: Polish Residue + Cross-Cutting Tests + Visual Regression + UI-SPEC Amendment

## Objective

Close the small-but-load-bearing residue: move the feeds SegmentedToggle into the inputs card (D-27), purge the lone `border-l` on morphine "Total reduction" cell (D-28), fix the hamburger drawer header padding asymmetry (D-29), promote `text-2xs` advisory copy to `text-ui` per the 11 px Floor (D-30), append a retroactive amendment to `42-UI-SPEC.md` capturing the post-em-dash eyebrow + bits-ui Slider declaration (D-25), add the cross-cutting `e2e/mobile-nav-clearance.spec.ts` (PATTERNS §7), and prepare the `b-*` visual regression capture directory + README. Ships in Wave 5 after Plans 1-5 land — depends on every prior plan because it verifies the system as a whole and amends the SPEC retroactively.

## Must-Haves (goal-backward verification)

- [ ] D-27: `src/lib/feeds/FeedAdvanceCalculator.svelte` — the `<SegmentedToggle>` (Bedside / Full Nutrition) currently at lines ~165-172 has been MOVED into the top of the inputs card (or into the shared weight card so it remains visible across modes). It is no longer in an isolated standalone position between the weight card and inputs card.
- [ ] D-28: `src/lib/morphine/MorphineWeanCalculator.svelte` — the lone `border-l border-[var(--color-border)]` + `pl-3` on the "Total reduction" cell (around line 191) is REMOVED. Visual separation handled by parent `flex gap-4` instead. (May be moot if Plan 2 T4's HeroResult refactor already removed this cell; in that case this task is a verification no-op.)
- [ ] D-29: `src/lib/shell/HamburgerMenu.svelte` — the drawer header at lines 71-92 has symmetric padding between the title and the close button. Either the `<h2>` gains `pl-1` OR the close button has matching offset. Visual judgment call documented in commit body.
- [ ] D-30: Every `text-2xs` site that is NOT a label (eyebrow, nav-tab label, range hint) is promoted to `text-ui`. PATTERNS §28 confirms `GirCalculator.svelte:177` is one such site. Plan T4 enumerates every site via `grep` before rewriting.
- [ ] D-25: `.planning/phases/42-uac-uvc-calculator/42-UI-SPEC.md` has a NEW appended section `## Post-Shipment Amendment (Phase 42.1)` per PATTERNS §33 — DOES NOT rewrite earlier sections; preserves audit trail. Section dated 2026-04-24, references back to 42.1-CONTEXT.md D-25, lists eyebrow change (em-dash → middle-dot), bits-ui Slider declaration, and asset inventory addition (`ArrowDownToLine`/`ArrowUpFromLine`).
- [ ] D-32: `e2e/mobile-nav-clearance.spec.ts` exists per PATTERNS §7. Iterates 5 calculator routes × 2 viewports (375×667, 414×896). Each test scrolls to bottom of page and asserts the last `<main>` child's `getBoundingClientRect().bottom <= bottom-nav top`. ≥ 10 test combinations pass.
- [ ] D-32 + visual regression: `.planning/ui-reviews/42.1-screenshots/README.md` exists with the capture protocol (5 routes × 2 viewports × 2 themes = 20 captures + hamburger-open + banner-visible + AboutSheet-Disclaimer-row = 23 total per CONTEXT visual regression scope). Document file naming `b-{route}-{viewport}-{theme}.png` and the diff-against-`a-*` workflow.
- [ ] D-31: All cross-cutting DESIGN.md named rules verified present in Plan 1-5 commits via grep audit. Rule citations documented in this plan's summary.
- [ ] D-33: All 6 plans have a SUMMARY.md per the established convention. T8 verifies presence.
- [ ] All tests stay green: svelte-check 0/0; `pnpm test:unit` 0; `pnpm test:e2e` 0 (with the new mobile-nav-clearance spec passing); `pnpm test:axe` 0; `pnpm build` 0.

## Threat Model

| Threat | Severity | Mitigation | Verified by |
|--------|----------|------------|-------------|
| D-27 SegmentedToggle move breaks the existing mode-switch behavior | low | The component is moved, not modified; its bind:value wiring stays the same. PATTERNS §30 cites FortificationCalculator's pattern as the analog | Existing FeedAdvanceCalculator.test.ts mode-switch coverage stays green |
| D-30 text-size promotion makes a previously-tight layout overflow | low | text-ui (13px) vs text-2xs (11px) is +2px line-height ≈ negligible at the advisory copy sites. Plan T4 visually inspects each site | Visual regression in T7 catches any layout shift |
| Retroactive UI-SPEC amendment confuses readers about what was originally specced | low | Per PATTERNS §"retroactive UI-SPEC amendment pattern", the amendment is APPENDED — not in-place rewrite. Original sections remain so historical accuracy is preserved | Verify via Read that original sections are intact and the new section appears at end with date stamp |
| `e2e/mobile-nav-clearance.spec.ts` flakes on slow CI due to viewport timing | low | Spec uses `await page.waitForLoadState('networkidle')` before geometry assertion; the layout calc from Plan 1 T5 is deterministic | Local run of the spec 3x in a row to confirm stability |
| Identity-Inside conformance test (Plan 1) still passes after all 5 plans land — i.e., no plan re-introduced `--color-identity` in chrome | low | Plan 6 T8 explicitly re-runs `identity-inside.test.ts` as a regression gate | Test exits 0; assertion messages would name any new offender |

No novel security threats. Visual regression captures are stored under `.planning/ui-reviews/` and not committed to repo — README documents the workflow.

## Tasks

<task id="42.1-06-T1">
  <action>
    Move the feeds `<SegmentedToggle>` (D-27). Per PATTERNS §30, FortificationCalculator places its SegmentedToggle inside the inputs card top — mirror that exactly.

    1. Read `src/lib/feeds/FeedAdvanceCalculator.svelte` to find the current SegmentedToggle position (around lines 165-172 per UI-SPEC).
    2. Move the SegmentedToggle markup from its standalone position INTO the top of the shared weight card (so the toggle remains visible regardless of mode). Or, if the layout is mode-conditional, place it at the top of EACH mode's inputs card so it's the first interactive element.
    3. Confirm bind:value wiring stays intact (no logic change). Confirm mode-switching behavior unchanged in component test + e2e.

    NOTE: If Plan 5's hero-drawer shell wraps inputs in `<InputDrawer>`, the SegmentedToggle ends up inside the drawer when flag-ON. That's correct — toggle stays at top of inputs.
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/src/lib/feeds/FeedAdvanceCalculator.svelte
    - /mnt/data/src/nicu-assistant/src/lib/fortification/FortificationCalculator.svelte (line 172 — SegmentedToggle inside inputs card analog)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§30 — feeds toggle move)
  </read_first>
  <acceptance_criteria>
    - `grep -F '<SegmentedToggle' src/lib/feeds/FeedAdvanceCalculator.svelte` returns 1 match (no duplication)
    - Visual inspection (or grep) confirms SegmentedToggle is no longer in a standalone wrapper between weight + inputs cards
    - `pnpm svelte-check` exits 0
    - `pnpm test:unit src/lib/feeds/FeedAdvanceCalculator.test.ts` exits 0
    - `pnpm test:e2e -- --grep "feeds"` exits 0
  </acceptance_criteria>
  <commit_message>
    refactor(42.1-06): move feeds SegmentedToggle into inputs card (D-27)
  </commit_message>
</task>

<task id="42.1-06-T2">
  <action>
    Remove the lone `border-l` on the morphine "Total reduction" cell (D-28).

    1. Read `src/lib/morphine/MorphineWeanCalculator.svelte` around line 191 to find the cell with `border-l border-[var(--color-border)] pl-3`.
    2. If Plan 2 T4 already removed this cell (the morphine HeroResult refactor reshaped the summary chip), this task is a verification no-op — confirm and document. Otherwise:
    3. Remove the `border-l border-[var(--color-border)] pl-3` classes. Visual separation should now come from the parent's `flex gap-4` (or whatever the parent layout uses). Per PATTERNS §25, side-stripe ban + Identity-Inside Rule both apply.
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/src/lib/morphine/MorphineWeanCalculator.svelte (after Plan 2 + Plan 4 edits)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§25 — border-l removal)
  </read_first>
  <acceptance_criteria>
    - `grep -F 'border-l border-[var(--color-border)] pl-3' src/lib/morphine/MorphineWeanCalculator.svelte` returns NO matches
    - `pnpm svelte-check` exits 0
    - `pnpm test:unit src/lib/morphine/MorphineWeanCalculator.test.ts` exits 0
  </acceptance_criteria>
  <commit_message>
    polish(42.1-06): remove lone border-l on morphine total reduction (D-28)
  </commit_message>
</task>

<task id="42.1-06-T3">
  <action>
    Fix the hamburger drawer header padding asymmetry (D-29).

    1. Read `src/lib/shell/HamburgerMenu.svelte:71-92` to see the current header structure (`<header class="flex items-center justify-between border-b border-[var(--color-border)] px-5 py-4">` containing a flex-col with `<h2>` + optional cap message, and an icon-btn close button).
    2. The asymmetry per CONTEXT D-29: title has no left padding while close button sits at `px-5` right edge but is centered in 48×48 hit area. Visual judgment per PATTERNS §17.
    3. Recommended fix: add `pl-1` to the `<div class="flex flex-col">` containing the `<h2>` to match the close button's effective right offset. OR: drop the close button's `min-h-[48px] min-w-[48px]` to remove its implicit centering. Pick whichever results in cleaner alignment per visual inspection.
    4. Document the chosen fix in commit body.
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/src/lib/shell/HamburgerMenu.svelte (lines 71-92)
    - /mnt/data/src/nicu-assistant/src/lib/shared/components/AboutSheet.svelte (lines 40-57 — analog header padding)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§17 — drawer header padding)
  </read_first>
  <acceptance_criteria>
    - File `src/lib/shell/HamburgerMenu.svelte` reflects the chosen padding fix (verifiable by reading the file post-edit and confirming a class change in the header block)
    - `pnpm svelte-check` exits 0
    - `pnpm test:unit src/lib/shell/__tests__/HamburgerMenu.test.ts` exits 0
  </acceptance_criteria>
  <commit_message>
    polish(42.1-06): symmetric padding on hamburger drawer header (D-29)
  </commit_message>
</task>

<task id="42.1-06-T4">
  <action>
    Promote `text-2xs` advisory copy to `text-ui` per the 11 px Floor (D-30).

    1. Run `git grep -F 'text-2xs' src/lib/` to enumerate every site.
    2. For each site, classify:
       - LABEL (eyebrow above hero, nav-tab label, in-card eyebrow, range hint under input) → KEEP `text-2xs` (intended use).
       - ADVISORY (paragraph, "Verify against your protocol", "Institution-specific", warning/info copy) → CHANGE to `text-ui`.
    3. Known sites per PATTERNS §28:
       - `src/lib/gir/GirCalculator.svelte:177` — `text-2xs text-[var(--color-text-tertiary)]` "Institutional titration helper. Verify against your protocol before acting." → CHANGE to `text-ui text-[var(--color-text-tertiary)]`.
       - `src/lib/feeds/FeedAdvanceCalculator.svelte:339-340` — likely advisory copy → CHANGE to `text-ui`.
    4. Also enumerate sites in `MorphineWeanCalculator.svelte`, `FortificationCalculator.svelte`, `UacUvcCalculator.svelte` if any. Use a `git grep -F 'text-2xs' src/lib/` audit to be thorough.
    5. Document every classification + change in commit body for review.
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/src/lib/gir/GirCalculator.svelte (line 177)
    - /mnt/data/src/nicu-assistant/src/lib/feeds/FeedAdvanceCalculator.svelte (lines 339-340)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§28 — text-2xs → text-ui pattern + enumeration guidance)
    - /mnt/data/src/nicu-assistant/DESIGN.md (§3 — 11 px Floor rule)
  </read_first>
  <acceptance_criteria>
    - `grep -nF 'text-2xs' src/lib/gir/GirCalculator.svelte` does NOT return line 177 (or whatever the advisory line is)
    - For every changed site: post-edit file shows `text-ui` at the location previously holding the advisory `text-2xs`
    - `pnpm svelte-check` exits 0
    - `pnpm test:e2e` exits 0
    - `pnpm test:axe` exits 0 (font-size promotion shouldn't break axe; verify)
  </acceptance_criteria>
  <commit_message>
    polish(42.1-06): promote text-2xs advisory copy to text-ui per 11px Floor (D-30)
  </commit_message>
</task>

<task id="42.1-06-T5">
  <action>
    Append the retroactive Phase 42 UI-SPEC amendment per D-25 + PATTERNS §33.

    1. Read the END of `.planning/phases/42-uac-uvc-calculator/42-UI-SPEC.md` to find the natural insertion point (after the last existing section, before the file end).
    2. APPEND (do NOT modify earlier sections — preserves audit trail):

       ```markdown

       ---

       ## Post-Shipment Amendment (Phase 42.1)

       **Amended:** 2026-04-24
       **Source:** `.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-CONTEXT.md` D-25

       The following items in this SPEC do not reflect what shipped (em-dash purge from `917ecf2` + bits-ui Slider substitution from Phase 42 implementation):

       ### Eyebrow text (post-em-dash purge)

       - Original: `UAC DEPTH — ARTERIAL` / `UVC DEPTH — VENOUS`
       - Shipped: `UAC DEPTH · ARTERIAL` / `UVC DEPTH · VENOUS` (middle-dot U+00B7, never em-dash)

       ### Slider primitive

       - Original spec implied native `<input type="range">`.
       - Shipped: bits-ui `<Slider.Root>` / `<Slider.Range>` / `<Slider.Thumb>` (`bits-ui ^2.18.0` — pre-approved primitive library for this codebase).
       - Keyboard matrix coverage (Melt UI builder underneath): ArrowLeft/Right/Up/Down (step), Home/End (min/max), PageUp/PageDown (large step). Verified by `e2e/uac-uvc.spec.ts`.

       ### Asset Inventory addition

       - `ArrowDownToLine` (UAC) and `ArrowUpFromLine` (UVC) icons were inadvertently removed during a v1.13 cleanup pass. Restored in Phase 42.1 plan 02 (D-24 + D-26) via the new `<HeroResult icon={...}>` prop.

       ### `<HeroResult>` adoption

       - Phase 42 shipped UAC/UVC with two custom hero blocks. Phase 42.1 plan 02 (D-07) refactored both to consume the shared `<HeroResult>` component — no functional change, but the eyebrow/numeral/unit/icon contract now matches every other calculator.
       ```

    3. Confirm previous content untouched: `git diff .planning/phases/42-uac-uvc-calculator/42-UI-SPEC.md` shows ONLY the appended block (zero deletions, zero modifications above the new `---` divider).
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/.planning/phases/42-uac-uvc-calculator/42-UI-SPEC.md (full file — to find the append point)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§33 — retroactive amendment pattern)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-CONTEXT.md (D-25)
  </read_first>
  <acceptance_criteria>
    - `grep -F '## Post-Shipment Amendment (Phase 42.1)' .planning/phases/42-uac-uvc-calculator/42-UI-SPEC.md` returns 1 match
    - `grep -F '**Amended:** 2026-04-24' .planning/phases/42-uac-uvc-calculator/42-UI-SPEC.md` returns 1 match
    - `git diff .planning/phases/42-uac-uvc-calculator/42-UI-SPEC.md` shows only ADDITIONS (no deletions or modifications above the new section)
  </acceptance_criteria>
  <commit_message>
    docs(42.1-06): append retroactive Phase 42 UI-SPEC amendment (D-25)
  </commit_message>
</task>

<task id="42.1-06-T6">
  <action>
    Create `e2e/mobile-nav-clearance.spec.ts` per PATTERNS §7:

    ```ts
    import { test, expect } from '@playwright/test';

    const ROUTES = [
      { path: '/morphine-wean', name: /Morphine Wean/i },
      { path: '/formula', name: /Formula/i },
      { path: '/gir', name: /GIR/i },
      { path: '/feeds', name: /Feeds/i },
      { path: '/uac-uvc', name: /UAC\/UVC/i }
    ];

    const VIEWPORTS = [
      { width: 375, height: 667, name: 'iPhone-SE' },
      { width: 414, height: 896, name: 'iPhone-14-Pro-Max' }
    ];

    test.describe('Mobile bottom-nav clearance (D-09)', () => {
      test.beforeEach(async ({ page }) => {
        await page.addInitScript(() => {
          localStorage.setItem('nicu_assistant_disclaimer_v2', 'true');
        });
      });

      for (const viewport of VIEWPORTS) {
        for (const route of ROUTES) {
          test(`${route.path} content does not overlap bottom nav at ${viewport.name}`, async ({ page }) => {
            await page.setViewportSize({ width: viewport.width, height: viewport.height });
            await page.goto(route.path);
            await page.getByRole('heading', { name: route.name }).waitFor({ state: 'visible' });
            await page.evaluate(() => window.scrollTo(0, document.body.scrollHeight));
            await page.waitForLoadState('networkidle');

            const overlap = await page.evaluate(() => {
              const main = document.querySelector('main');
              const nav = document.querySelector('nav[aria-label="Calculator navigation"]');
              const lastChild = main?.lastElementChild?.getBoundingClientRect();
              const navRect = nav?.getBoundingClientRect();
              return {
                lastBottom: lastChild?.bottom ?? 0,
                navTop: navRect?.top ?? Infinity
              };
            });
            expect(overlap.lastBottom).toBeLessThanOrEqual(overlap.navTop);
          });
        }
      }
    });
    ```

    Note: the bottom-nav `<nav>` has `aria-label="Calculator navigation"` — confirm via reading NavShell.svelte. The selector `nav[aria-label="Calculator navigation"]:last-of-type` may be needed if there are TWO matching navs (desktop + mobile both labeled). Use `.last()` in Playwright OR scope to a specific viewport-conditional class.
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/e2e/navigation.spec.ts (lines 26-41 — bottom-nav locator analog)
    - /mnt/data/src/nicu-assistant/e2e/uac-uvc-a11y.spec.ts (lines 20-35 — beforeEach pattern)
    - /mnt/data/src/nicu-assistant/src/lib/shell/NavShell.svelte (confirm bottom nav aria-label)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§7 — full spec pattern)
  </read_first>
  <acceptance_criteria>
    - File `e2e/mobile-nav-clearance.spec.ts` exists
    - `pnpm test:e2e -- --grep "Mobile bottom-nav clearance"` exits 0 with 10 tests passing (5 routes × 2 viewports)
    - File contains `getBoundingClientRect` geometry assertion via `page.evaluate`
  </acceptance_criteria>
  <commit_message>
    test(42.1-06): mobile-nav clearance e2e at 375 + 414 (D-09 verification)
  </commit_message>
</task>

<task id="42.1-06-T7">
  <action>
    Create the visual regression capture directory + README. Capture protocol per UI-SPEC §Visual Regression Scope + CONTEXT specifics:

    1. Create directory: `.planning/ui-reviews/42.1-screenshots/` (parent likely exists; check).
    2. Create `.planning/ui-reviews/42.1-screenshots/README.md`:

       ```markdown
       # Phase 42.1 Visual Regression Capture

       **Workflow:** Capture `b-*` screenshots after Phase 42.1 lands, diff against pre-existing `a-*` baseline at repo root.

       ## Scope (23 captures total)

       Routes (5) × Viewports (2) × Themes (2) = 20:
       - `/morphine-wean` × {375x667, 1280x800} × {light, dark}
       - `/formula` × ...
       - `/gir` × ...
       - `/feeds` × ...
       - `/uac-uvc` × ...

       Plus 3 special states:
       - Hamburger drawer open
       - Disclaimer banner visible (clear localStorage to simulate fresh install)
       - AboutSheet open at Disclaimer row

       ## Naming

       `b-{route-slug}-{viewport}-{theme}.png` — e.g. `b-morphine-wean-mobile-light.png`, `b-uac-uvc-desktop-dark.png`.

       Special states: `b-hamburger-open-mobile.png`, `b-banner-visible-mobile-light.png`, `b-aboutsheet-disclaimer-mobile-light.png`.

       ## Diffing

       Pair each `b-` capture with its `a-*` counterpart at repo root (e.g. `a-morphine-mobile-light.png` ↔ `b-morphine-wean-mobile-light.png`). Use any image-diff tool (e.g. ImageMagick `compare`).

       Document material differences in `42.1-VERIFICATION.md` (TBD).

       ## Capture instructions

       1. `pnpm dev` (or `pnpm preview` post-build).
       2. Set theme via the toggle in title bar.
       3. Set viewport via browser DevTools.
       4. Use Playwright codegen + `page.screenshot({ path: '.planning/ui-reviews/42.1-screenshots/b-...png' })` for repeatable captures.

       ## Notes

       Captures are NOT committed to the repo (large binary artifacts). The `.planning/ui-reviews/` directory exists in `.gitignore` for that reason — confirm before pushing.
       ```
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/.gitignore (confirm `.planning/ui-reviews/` exclusion)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-UI-SPEC.md (§Visual Regression Scope)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-CONTEXT.md (specifics on a-* baseline)
  </read_first>
  <acceptance_criteria>
    - Directory `.planning/ui-reviews/42.1-screenshots/` exists
    - File `.planning/ui-reviews/42.1-screenshots/README.md` exists with the capture protocol
    - File contains the 23-capture scope and `b-{slug}-{viewport}-{theme}.png` naming convention
  </acceptance_criteria>
  <commit_message>
    docs(42.1-06): visual regression capture README + protocol (D-32)
  </commit_message>
</task>

<task id="42.1-06-T8">
  <action>
    Final regression sweep + cross-cutting audit + plan summary:

    1. `pnpm svelte-check` → 0/0
    2. `pnpm test:unit` → all green; explicitly run `src/lib/shell/identity-inside.test.ts` and confirm both NavShell + HamburgerMenu assertions PASS (regression gate per Plan 1)
    3. `pnpm test:e2e` → all green; explicitly confirm `mobile-nav-clearance.spec.ts` (10 tests) + `disclaimer-banner.spec.ts` (5+) + `hero-drawer.spec.ts` (4) all pass
    4. `pnpm test:axe` → 0 violations both themes both flag states
    5. `pnpm build` → exits 0

    DESIGN.md cross-cutting audit (D-31):
    - `git grep -E '(border-l-[2-9]|border-r-[2-9]|border-t-[2-9]|border-b-[2-9])\b' src/lib/` → no side-stripe borders remain
    - `git grep -F '#000' src/` → no hex (OKLCH-Only Rule)
    - `git grep -F '#fff' src/` → no hex
    - `git grep -F '—' src/` → no em-dashes in user-rendered strings (em-dash ban)

    Confirm all 6 plan summaries exist (D-33):
    - `ls .planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-0{1,2,3,4,5,6}-SUMMARY.md` returns 6 files

    Write `.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-06-SUMMARY.md`:
    - Files changed: list count
    - Decisions honored: D-25, D-27, D-28, D-29, D-30, D-31 (audit results), D-32 (test added), D-33 (verified)
    - Tests added: e2e/mobile-nav-clearance.spec.ts (10 tests)
    - DESIGN.md named rules cited: 11 px Floor (D-30), Identity-Inside (audit), OKLCH-Only (audit), em-dash ban (audit)
    - Cross-cutting audit results (grep counts for each rule)
    - Visual regression: README created; captures NOT yet performed (separate manual step post-merge)
    - Phase 42.1 status: COMPLETE pending visual regression sign-off
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-CONTEXT.md (D-31, D-32, D-33)
    - /mnt/data/src/nicu-assistant/DESIGN.md (cross-cutting named rules to verify)
  </read_first>
  <acceptance_criteria>
    - `pnpm svelte-check` exits 0
    - `pnpm test:unit src/lib/shell/identity-inside.test.ts` exits 0 (both assertions pass — regression gate)
    - `pnpm test:e2e` exits 0
    - `pnpm test:axe` exits 0
    - `pnpm build` exits 0
    - `git grep -E '(border-l-[2-9]|border-r-[2-9]|border-t-[2-9]|border-b-[2-9])\b' src/lib/` returns NO matches
    - `git grep -F '#000' src/lib/ src/app.css` returns NO matches (OKLCH-Only)
    - `git grep -F '#fff' src/lib/ src/app.css` returns NO matches
    - `ls .planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-0{1,2,3,4,5,6}-SUMMARY.md` returns all 6 files
    - File `42.1-06-SUMMARY.md` exists with cross-cutting audit results
  </acceptance_criteria>
  <commit_message>
    docs(42.1-06): final summary + cross-cutting audit - phase 42.1 complete
  </commit_message>
</task>

## Verification (after all tasks complete)

- `pnpm svelte-check` → 0 errors, 0 warnings
- `pnpm test:unit` → all green; identity-inside.test.ts both assertions PASS
- `pnpm test:e2e` → all green; mobile-nav-clearance (10 tests) + disclaimer-banner (5+) + hero-drawer (4) + navigation root-redirect deep-link case all green
- `pnpm test:axe` → 0 violations both themes
- `pnpm build` → exits 0
- Cross-cutting audit greps all return 0 matches per T8 acceptance criteria
- 6 plan summaries exist in the phase directory

## Notes

- Plan 6 is the verification + closer plan. It depends on Plans 1-5 because it audits the system as a whole (Identity-Inside conformance, em-dash ban, OKLCH-Only, side-stripe ban) and ships the cross-cutting tests (mobile-nav-clearance, visual regression README) that prove the phase succeeded.
- D-25's retroactive UI-SPEC amendment is documentation-only (no code dependency). It lives here in Plan 6 because it's the natural closing-act doc edit that captures what shipped vs. what was originally specced.
- The visual regression captures are deliberately NOT performed by the automated execution. They require human inspection and judgment per UI-SPEC §Visual Regression Scope. This plan creates the directory + README + capture protocol; a manual post-merge step (or a separate `/gsd-ui-review` invocation) performs the captures and produces `42.1-VERIFICATION.md`.
- The `text-2xs` → `text-ui` change (D-30) may have small layout impact at the advisory copy sites — visual regression catches this. If significant overflow is found post-capture, a Phase 42.2 fix is the right path (this plan ships the typographic upgrade per the 11 px Floor; layout-rebalance is a separate concern).
- Phase 43 (Release v1.13.0) blocks on Phase 42.1 per ROADMAP. Plan 6's completion = the gate clear for Phase 43 to start.
