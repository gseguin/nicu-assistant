# Phase 42.1: Design Polish + Redesign Sweep — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `42.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-24
**Phase:** 42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co
**Areas discussed:** Critique-driven scope (priority direction, scope size, palette policy), DESIGN.md North Star, palette state, frontmatter format
**Source critique:** `/impeccable critique` re-run on 2026-04-24, scored 26/40

---

## Phase Scope (Critique-Driven)

### Priority Direction

| Option | Description | Selected |
|--------|-------------|----------|
| Fix clipping + palette + heroes first | Address the two P0s (mobile-nav clipping, 5-hue palette fragmentation) and the family-consistency P1 (`<HeroResult>`). | ✓ |
| Disclaimer modal + animations sweep first | Tackle calm-clinical brand erosion: disclaimer, dock magnification, transitions, drawer animation. | |
| Polish residue first | Knock out smaller P2/P3s before structural work. | |

**User's choice:** Fix clipping + palette + heroes first (Recommended)
**Notes:** Sets the wave order for plans — Wave 1 = P0 structural (clipping + Identity-Inside enforcement); Wave 2 = brand promise (`<HeroResult>` + disclaimer); Wave 3 = decoration removal + polish residue.

### Scope Size

| Option | Description | Selected |
|--------|-------------|----------|
| P0 + P1 only (v1.13 ship) | Mobile-nav, palette, hero, modal, 42-UI-REVIEW residue. Defer P2s to v1.14. | |
| P0 + P1 + P2 (everything found today) | All 6 priority issues + 8 residual-list items. Bigger phase but ships v1.13 with full polish. | |
| P0 only + 42-UI-REVIEW residue | Smallest viable. v1.14 polish milestone for the rest. | |
| Everything plus structural redesign | Add provocative-question work: hero-fills-viewport, monochrome-with-amber palette, disclaimer-as-banner refactor. v2-redesign scope. | ✓ |

**User's choice:** Everything plus structural redesign
**Notes:** Phase 42.1 carries the complete impeccable critique residual + structural redesign + 42-UI-REVIEW spec drift. Phase 43 (Release v1.13.0) blocks on 42.1. User confirmed shape **A** (single expanded phase, ~6-8 plans) over shape B (split structural / polish) or shape C (three sub-phases).

### Palette Policy

| Option | Description | Selected |
|--------|-------------|----------|
| Consolidate now to single-accent + amber | Identity hues deprecated. Clinical blue is sole accent. Amber survives for BMF only. | |
| Keep identity system, tighten scope | Identity hues remain a system; new calculators add new identity tokens. But identity color restricted to inside-the-route surfaces only — never in chrome. | ✓ |
| Defer the call, document current state with deprecation flag | DESIGN.md describes 5-hue system today; mark identity-* "under review in 42.1". | |
| Keep as-is, only fix the screens not the palette | Don't touch palette tokens. Live with 5 hues. | |

**User's choice:** Keep identity system, tighten scope (Recommended)
**Notes:** User clarified the priority shift mid-decision: "There will be additional calculators." This made consolidate-now the wrong answer — it would have locked future calculators out of inheriting an identity hue. The Identity-Inside Rule (DESIGN.md) is the resolution: identity-as-system survives, scope contracts to inside-the-route only. Preserves v1.8 decision (identity hue research BEFORE PR) intact.

---

## DESIGN.md (Generated via /impeccable document)

### Creative North Star

| Option | Description | Selected |
|--------|-------------|----------|
| The Quiet Reference | Like a well-thumbed clinical handbook on the NICU shelf. Authority through precision and restraint. The result is the only thing that demands attention. | ✓ |
| The Bedside Companion | Like a trusted colleague at the cot-side: warm, calm, never raises its voice. Designed to work one-handed in dim light between alarms. | |
| The Calm Instrument | Like a precision medical device. Tabular numerals, intentional dark mode, zero ornament. The interface disappears into the calculation. | |

**User's choice:** The Quiet Reference (Recommended)
**Notes:** Anchors the system as a clinical handbook on a phone, not a clinical app. Frames every downstream decision (no decoration without function, no surprise, calculators are siblings not personalities).

### Palette State (re-asked after "more calculators coming" clarification)

| Option | Description | Selected |
|--------|-------------|----------|
| Document the target palette (post-42.1, single accent + amber) | DESIGN.md describes consolidated system. Anchors 42.1 work. | |
| Document what ships today (5 hues) | Accurate to codebase right now. Re-run /impeccable document after 42.1. | |
| Document both — mark current as deprecated | 5-hue today, flag tokens under review in 42.1. | |

**User's choice:** Question superseded by clarification — DESIGN.md documents the **target system** (Identity-Inside Rule applied), with identity tokens preserved and new calculators inheriting the pattern.
**Notes:** The "consolidate to single accent + amber" framing was wrong before the clarification. Post-clarification, DESIGN.md captures the target with identity-as-system + scoped chrome.

### Frontmatter Format

| Option | Description | Selected |
|--------|-------------|----------|
| OKLCH in frontmatter, accept Stitch warning | Project doctrine. Single source of truth. Stitch linter warning, not error. | ✓ |
| Hex in frontmatter, OKLCH in prose | Strict Stitch compliance. Two values to keep in sync. | |

**User's choice:** OKLCH in frontmatter, accept Stitch warning (Recommended)
**Notes:** Project has explicit OKLCH-only doctrine in `app.css`. Hex would be approximations. DESIGN.md frontmatter ships 29 OKLCH color tokens; DESIGN.json sidecar carries 8-step tonal ramps in OKLCH.

---

## Critique Findings (Source Material — Not Re-Asked)

For audit traceability, the 6 priority issues from the 2026-04-24 `/impeccable critique` that drive the decisions in CONTEXT.md:

1. **[P0] Mobile bottom-nav occludes content on every screen** → CONTEXT D-09, D-10, D-11
2. **[P0] Five-hue identity palette is now visibly fragmented** → CONTEXT D-01 through D-05 (Identity-Inside Rule)
3. **[P1] Disclaimer modal is over-locked and never re-accessible** → CONTEXT D-12 through D-15 (banner refactor)
4. **[P1] Calculator-family inconsistency: 5 different hero shapes** → CONTEXT D-06, D-07, D-08 (`<HeroResult>` + structural redesign)
5. **[P2] Morphine schedule shows 4-decimal "spreadsheet noise"** → CONTEXT D-20 through D-22 (`formatMg`)
6. **[P2] Dock-style scroll magnification is decoration, not function** → CONTEXT D-16

Plus the residual list:
- Server-side 307 redirect for `/` → D-19
- Scope CSS transitions → D-17
- Reconsider animations → D-18
- 42-UI-REVIEW residue (D-05 third cue, eyebrow drift, bits-ui Slider doc) → D-24, D-25, D-26

## Claude's Discretion

- **Banner copy wording** (D-12 starter sentence): proposed `"Clinical decision support, not medical advice. Verify all values before clinical use."` — refine in plan with legal-team review.
- **Inputs drawer interaction details** (D-08): tap-to-expand vs swipe-up vs explicit handle — decide in plan based on `<HeroResult>` layout outcomes.
- **`<HeroResult>` API surface** (D-06): exact slot/prop names and TypeScript signature — decide in plan.
- **Decimal precision threshold** (D-20): `toFixed(3)` for mg confirmed; whether to apply `formatMg` to displays NOT in MorphineWeanCalculator (e.g. if formula calculator surfaces a mg value) deferred to plan-level audit.

## Deferred Ideas

- Keyboard shortcuts (`/` to focus search, `Tab` to first input, `Cmd+K` palette)
- Print stylesheet for GIR titration grid
- Copy-result button + send-to-chart
- Universal `formatNumber(value, decimals)` shared utility
- Tablet side-rail nav (replace mobile bottom-nav at `md` and up)
- Morphine schedule visual grouping every 5 steps
- Richer inline tooltips on calculator surfaces
- `/impeccable polish` final pass after 42.1 ships (handled by routine `trig_014XUDEDwK7ve2dKS5dP8JZQ` firing 2026-05-07)
