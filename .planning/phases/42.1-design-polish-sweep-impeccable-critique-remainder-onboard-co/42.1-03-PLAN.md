---
plan_id: 42.1-03
phase: 42.1
phase_name: Design Polish + Redesign Sweep (Impeccable Critique Remainder)
plan_name: Disclaimer Banner Refactor + AboutSheet Hoist + Disclaimer Row
wave: 3
depends_on: []
files_modified:
  - src/lib/shared/disclaimer.svelte.ts
  - src/lib/shared/__tests__/shared-components.test.ts
  - src/lib/shared/about-content.ts
  - src/lib/shared/components/AboutSheet.svelte
  - src/lib/shell/NavShell.svelte
  - src/routes/+layout.svelte
files_created:
  - src/lib/shared/components/DisclaimerBanner.svelte
  - src/lib/shared/components/DisclaimerBanner.test.ts
  - e2e/disclaimer-banner.spec.ts
files_deleted:
  - src/lib/shared/components/DisclaimerModal.svelte
  - e2e/disclaimer.spec.ts
autonomous: false
requirements: [D-12, D-13, D-14, D-15]
estimated_commits: 7
---

# Plan 42.1-03: Disclaimer Banner Refactor + AboutSheet Hoist + Disclaimer Row

## Objective

Replace the first-paint-blocking `DisclaimerModal` with a 1-line dismissable `<DisclaimerBanner>` mounted at the layout level, hoist `<AboutSheet>` from `NavShell.svelte` to `+layout.svelte` so the banner's "More" link can open it, add a dedicated Disclaimer row to AboutSheet so the full text is always re-readable, and migrate localStorage from `nicu_assistant_disclaimer_v1` to `_v2` so existing acknowledgments carry forward (clinicians who already dismissed the modal do NOT see the banner). Ships in Wave 3 — independent of Plans 1, 2, 4 (touches different files), gated by user review of banner copy per RESEARCH risk #2.

`autonomous: false` because banner copy needs a user checkpoint per RESEARCH risk #2 (legal-equivalent sign-off).

## Must-Haves (goal-backward verification)

- [ ] D-12: `src/lib/shared/components/DisclaimerBanner.svelte` exists with the structure mandated by UI-SPEC §DisclaimerBanner — `role="region"`, `aria-label="Clinical use disclaimer"`, body `text-ui` weight 500, "More" inline button calling `onShowFull` callback, dismiss `.icon-btn` X button with `aria-label="Dismiss disclaimer"`. `bg-[var(--color-surface-alt)]` surface; 1 px bottom `--color-border`. NO em-dash anywhere.
- [ ] D-12: Banner renders only when `disclaimer.initialized && !disclaimer.acknowledged`. Hidden after dismiss persists.
- [ ] D-13: `src/lib/shared/about-content.ts` AboutContent interface gains a `disclaimer: string` field; a single `DISCLAIMER` constant is referenced from all 5 calculator entries (DRY per PATTERNS §21).
- [ ] D-13: `src/lib/shared/components/AboutSheet.svelte` renders a "Disclaimer" section with `<h3>` heading and `<p>` body using `content.disclaimer`. Section labelled via `aria-labelledby="about-disclaimer-heading"`.
- [ ] D-14 (with RESEARCH finding 1 correction): `src/lib/shared/disclaimer.svelte.ts` migrates from key `nicu_assistant_disclaimer_v1` to `nicu_assistant_disclaimer_v2`. `init()` reads BOTH keys: if either is `'true'`, sets `_acknowledged = true`. If only `_v1` is set, also writes `_v2`. `acknowledge()` writes `_v2` only. v1 key is NOT deleted (preserves audit trail).
- [ ] D-15: `src/lib/shared/components/DisclaimerModal.svelte` is DELETED. `+layout.svelte:6` import and `:68` mount are removed. `<DisclaimerBanner onShowFull={...} />` mounted in its place.
- [ ] D-15 + UI-SPEC §Modified Components: `<AboutSheet>` HOISTED from `NavShell.svelte:133` up to `+layout.svelte`. `aboutOpen` state moves to layout; `NavShell` accepts an `aboutOpen = $bindable(false)` prop OR an `onAboutOpen?: () => void` callback (executor picks the cleaner path — recommend bindable per PATTERNS §12).
- [ ] D-15: `e2e/disclaimer.spec.ts` is DELETED (or rewritten in place as `e2e/disclaimer-banner.spec.ts`). New file covers ≥ 5 banner scenarios per UI-SPEC §DisclaimerBanner acceptance.
- [ ] `src/lib/shared/__tests__/shared-components.test.ts` disclaimer singleton block updated with v1→v2 migration scenarios (PATTERNS §4 — 6 coverage targets).
- [ ] All other tests stay green: svelte-check 0/0; `pnpm test:unit` 0; `pnpm test:e2e` 0; `pnpm test:axe` 0.

## Threat Model

(Per CONTEXT D-31 + workflow step 5.55 default-enabled security gate.)

| Threat | Severity | Mitigation | Verified by |
|--------|----------|------------|-------------|
| localStorage tampering: a malicious user pre-sets `_v2='true'` to bypass the banner | low | Disclaimer is informational, not an authentication boundary. The full disclaimer text remains accessible from AboutSheet (D-13). Tampering only affects the user's own banner visibility, not clinical safety | DisclaimerBanner.test.ts scenario 6 covers private-mode write-throw — same defensive code path |
| Migration path leaks PII or sensitive data | none | Migration writes only the literal string `'true'` to a non-sensitive key | Code inspection — disclaimer.svelte.ts handles only boolean acknowledgment state |
| AboutSheet hoist drops the existing focus-restoration semantics | low | `aboutOpen = $bindable(false)` preserves the open/close lifecycle; AboutSheet still owns its focus-trap and Esc handling via bits-ui Dialog primitives | Existing AboutSheet tests remain green; new e2e covers banner→AboutSheet→close→banner-dismissed flow |
| Banner copy could be misread legally as "we acknowledge no clinical responsibility" without the full text on hand | medium | D-13 mandates the full disclaimer text remains in AboutSheet; banner has a "More" link to it; user review checkpoint (autonomous: false) before committing copy | Plan checkpoint awaits user sign-off on banner sentence before T1 commit. Copy: "Clinical decision support, not medical advice. Verify all values before clinical use." |
| Stale `disclaimer.svelte.ts` test mocks reference v1 key only and miss v2 read path | low | Disclaimer singleton test block (existing in shared-components.test.ts:9-69) updated per PATTERNS §4 with 6 explicit migration scenarios | shared-components.test.ts and DisclaimerBanner.test.ts cover both keys |

## Tasks

<task id="42.1-03-T1">
  <action>
    USER CHECKPOINT — present the proposed banner copy to the user for sign-off (per RESEARCH risk #2):

    Proposed body sentence: `"Clinical decision support, not medical advice. Verify all values before clinical use."`
    Proposed AboutSheet full disclaimer text: `"Clinical decision support, not medical advice. Verify all values before clinical use. This app does not replace clinical judgment; all outputs must be reviewed by qualified NICU staff before administration."`

    Wait for user to ACK or provide alternative copy. Document the final agreed copy in this commit's message body. If user supplies alternative copy, use that verbatim — do NOT paraphrase.

    Do NOT proceed to subsequent tasks until copy is locked.
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-CONTEXT.md (D-12 — starter copy)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-RESEARCH.md (Risk #2 — legal-equivalent sign-off)
    - /mnt/data/src/nicu-assistant/src/lib/shared/components/DisclaimerModal.svelte (existing modal copy for comparison)
  </read_first>
  <acceptance_criteria>
    - User has explicitly acknowledged the banner sentence + AboutSheet full text (or supplied alternatives)
    - Final agreed copy documented in commit message body
  </acceptance_criteria>
  <commit_message>
    chore(42.1-03): lock disclaimer banner copy

    Banner: "<final agreed banner sentence>"
    AboutSheet full: "<final agreed full text>"
  </commit_message>
</task>

<task id="42.1-03-T2">
  <action>
    Update `src/lib/shared/disclaimer.svelte.ts` to perform v1→v2 migration per PATTERNS §"DisclaimerBanner migration pattern":

    ```ts
    // src/lib/shared/disclaimer.svelte.ts
    const DISCLAIMER_KEY_V1 = 'nicu_assistant_disclaimer_v1';
    const DISCLAIMER_KEY_V2 = 'nicu_assistant_disclaimer_v2';

    let _acknowledged = $state(false);
    let _initialized = $state(false);

    export const disclaimer = {
      get acknowledged(): boolean { return _acknowledged; },
      get initialized(): boolean { return _initialized; },
      init(): void {
        const v2 = localStorage.getItem(DISCLAIMER_KEY_V2);
        const v1 = localStorage.getItem(DISCLAIMER_KEY_V1);
        if (v2 === 'true' || v1 === 'true') {
          _acknowledged = true;
          if (v1 === 'true' && v2 !== 'true') {
            try {
              localStorage.setItem(DISCLAIMER_KEY_V2, 'true');
              // Do not delete v1 — preserves audit trail
            } catch { /* private mode */ }
          }
        }
        _initialized = true;
      },
      acknowledge(): void {
        _acknowledged = true;
        try { localStorage.setItem(DISCLAIMER_KEY_V2, 'true'); } catch {}
      }
    };
    ```

    Update `src/lib/shared/__tests__/shared-components.test.ts` disclaimer singleton block (lines 9-69) with the 6 scenarios from PATTERNS §4:
    1. Fresh install (no v1, no v2): not acknowledged
    2. v1='true', v2 absent on init: acknowledged true; v2 written; v1 still present
    3. v2='true' on init: acknowledged true; no v1 read needed
    4. acknowledge() writes v2 only
    5. localStorage.setItem throws (private mode): in-memory acknowledge still works, no crash
    6. v1='true' AND v2='true' both present: acknowledged true; no extra writes (idempotent)
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/src/lib/shared/disclaimer.svelte.ts (current shape)
    - /mnt/data/src/nicu-assistant/src/lib/shared/__tests__/shared-components.test.ts (current disclaimer test block)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§"DisclaimerBanner migration pattern" + §4 — test scenarios)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-RESEARCH.md (§D-14 — v1 key correction; key is `_v1` not bare)
  </read_first>
  <acceptance_criteria>
    - `grep -F "DISCLAIMER_KEY_V1 = 'nicu_assistant_disclaimer_v1'" src/lib/shared/disclaimer.svelte.ts` returns the constant
    - `grep -F "DISCLAIMER_KEY_V2 = 'nicu_assistant_disclaimer_v2'" src/lib/shared/disclaimer.svelte.ts` returns the constant
    - `grep -F "v1 === 'true' && v2 !== 'true'" src/lib/shared/disclaimer.svelte.ts` returns the migration write line
    - `pnpm test:unit src/lib/shared/__tests__/shared-components.test.ts` exits 0 with all 6 disclaimer migration scenarios passing
  </acceptance_criteria>
  <commit_message>
    feat(42.1-03): migrate disclaimer key v1→v2 with backward compat (D-14)
  </commit_message>
</task>

<task id="42.1-03-T3">
  <action>
    Edit `src/lib/shared/about-content.ts` to add the disclaimer field (D-13):

    1. Add a top-level constant: `const DISCLAIMER = '<full disclaimer text from T1>'` (use the user-locked copy from T1).
    2. Add `disclaimer: string;` to the AboutContent interface.
    3. For each of the 5 calculator entries (`morphine-wean`, `formula`, `gir`, `feeds`, `uac-uvc`), add `disclaimer: DISCLAIMER` (NOT `disclaimer: '...full text inline...'` — reference the constant for DRY).

    Then update `src/lib/shared/components/AboutSheet.svelte` to render the disclaimer section per PATTERNS §20:

    Add (above or below the description block — recommend below the description, above the notes list):
    ```svelte
    {#if content.disclaimer}
      <section aria-labelledby="about-disclaimer-heading" class="mt-4">
        <h3
          id="about-disclaimer-heading"
          class="text-2xs font-semibold tracking-wide uppercase text-[var(--color-text-tertiary)]"
        >
          Disclaimer
        </h3>
        <p class="mt-2 text-base leading-relaxed text-[var(--color-text-secondary)]">
          {content.disclaimer}
        </p>
      </section>
    {/if}
    ```
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/src/lib/shared/about-content.ts (current AboutContent interface + 5 entries)
    - /mnt/data/src/nicu-assistant/src/lib/shared/components/AboutSheet.svelte (current render shape)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§20 + §21 — AboutSheet + about-content patterns)
  </read_first>
  <acceptance_criteria>
    - `grep -F "const DISCLAIMER = " src/lib/shared/about-content.ts` returns the constant
    - `grep -F "disclaimer: string" src/lib/shared/about-content.ts` returns the interface field
    - `grep -cF "disclaimer: DISCLAIMER" src/lib/shared/about-content.ts` returns `5` (one per calculator)
    - `grep -F 'about-disclaimer-heading' src/lib/shared/components/AboutSheet.svelte` returns the labelledby line
    - `pnpm svelte-check` exits 0
  </acceptance_criteria>
  <commit_message>
    feat(42.1-03): add Disclaimer row to AboutSheet (D-13)
  </commit_message>
</task>

<task id="42.1-03-T4">
  <action>
    Create `src/lib/shared/components/DisclaimerBanner.svelte` per PATTERNS §3 + UI-SPEC §DisclaimerBanner:

    ```svelte
    <script lang="ts">
      import { X } from '@lucide/svelte';
      import { disclaimer } from '$lib/shared/disclaimer.svelte.js';

      let { onShowFull }: { onShowFull?: () => void } = $props();
    </script>

    {#if disclaimer.initialized && !disclaimer.acknowledged}
      <div
        role="region"
        aria-label="Clinical use disclaimer"
        class="flex items-center gap-3 border-b border-[var(--color-border)] bg-[var(--color-surface-alt)] px-4 py-2 text-ui"
      >
        <p class="flex-1 text-[var(--color-text-primary)]">
          <!-- Use the locked banner copy from T1 -->
          Clinical decision support, not medical advice. Verify all values before clinical use.
          <button type="button" class="underline" onclick={onShowFull}>More</button>
        </p>
        <button
          type="button"
          class="icon-btn min-h-[48px] min-w-[48px]"
          aria-label="Dismiss disclaimer"
          onclick={() => disclaimer.acknowledge()}
        >
          <X size={18} aria-hidden="true" />
        </button>
      </div>
    {/if}
    ```

    Honors UI-SPEC §DisclaimerBanner visual contract: surface alt, text-ui body, .icon-btn dismiss, role="region", em-dash ban.
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/src/lib/shell/UpdateBanner.svelte (banner skeleton analog)
    - /mnt/data/src/nicu-assistant/src/lib/shell/HamburgerMenu.svelte (lines 84-91 — icon-btn X dismiss analog)
    - /mnt/data/src/nicu-assistant/src/lib/shared/disclaimer.svelte.ts (singleton API)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§3 — DisclaimerBanner full pattern)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-UI-SPEC.md (§DisclaimerBanner — visual contract)
  </read_first>
  <acceptance_criteria>
    - File `src/lib/shared/components/DisclaimerBanner.svelte` exists; `pnpm svelte-check` exits 0
    - `grep -F 'role="region"' src/lib/shared/components/DisclaimerBanner.svelte` returns 1 line
    - `grep -F 'aria-label="Clinical use disclaimer"' src/lib/shared/components/DisclaimerBanner.svelte` returns 1 line
    - `grep -F 'aria-label="Dismiss disclaimer"' src/lib/shared/components/DisclaimerBanner.svelte` returns 1 line
    - `grep -F 'bg-[var(--color-surface-alt)]' src/lib/shared/components/DisclaimerBanner.svelte` returns 1 line
    - `grep -F '—' src/lib/shared/components/DisclaimerBanner.svelte` returns NO matches (em-dash ban)
    - `grep -F 'disclaimer.acknowledge()' src/lib/shared/components/DisclaimerBanner.svelte` returns the dismiss handler
  </acceptance_criteria>
  <commit_message>
    feat(42.1-03): add DisclaimerBanner shared component (D-12, D-15)
  </commit_message>
</task>

<task id="42.1-03-T5">
  <action>
    Create `src/lib/shared/components/DisclaimerBanner.test.ts` with the 6 coverage targets from PATTERNS §4:

    1. Fresh install (no v1, no v2 keys) → banner visible, not acknowledged.
    2. Migration: pre-set `_v1='true'`, init, banner hidden, `_v2` written.
    3. Steady state: pre-set `_v2='true'`, init, banner hidden.
    4. Dismiss button click → acknowledged, banner hides, `_v2` persisted.
    5. "More" link calls `onShowFull` callback (use `vi.fn()` spy passed as prop).
    6. localStorage.setItem throws → in-memory acknowledged true, no crash.

    Use vitest + @testing-library/svelte. Co-located next to the component (NOT in `__tests__/`). Pattern from SegmentedToggle.test.ts.
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/src/lib/shared/components/SegmentedToggle.test.ts (vitest+RTL setup analog)
    - /mnt/data/src/nicu-assistant/src/lib/shared/__tests__/shared-components.test.ts (lines 9-69 — disclaimer singleton + private-mode mock pattern)
    - /mnt/data/src/nicu-assistant/src/lib/shared/components/DisclaimerBanner.svelte (the component under test)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§4 — coverage targets)
  </read_first>
  <acceptance_criteria>
    - File `src/lib/shared/components/DisclaimerBanner.test.ts` exists
    - `pnpm test:unit src/lib/shared/components/DisclaimerBanner.test.ts` exits 0 with 6+ tests passing
    - Test imports from `'vitest'` and uses `localStorage.clear()` in beforeEach
  </acceptance_criteria>
  <commit_message>
    test(42.1-03): cover DisclaimerBanner with 6 scenarios (D-32)
  </commit_message>
</task>

<task id="42.1-03-T6">
  <action>
    Hoist `<AboutSheet>` from NavShell to layout, mount the new DisclaimerBanner, and delete the modal (D-15):

    1. Edit `src/lib/shell/NavShell.svelte`:
       - Add `aboutOpen = $bindable(false)` to the `$props()` destructure (currently NavShell has NO props — add a `let { aboutOpen = $bindable(false) } = $props();` block at top of `<script>`).
       - REMOVE the local `let aboutOpen = $state(false);` declaration on line 18.
       - REMOVE line 133 `<AboutSheet calculatorId={...} bind:open={aboutOpen} />`.
       - REMOVE the AboutSheet import on line 7.
       - Keep the HamburgerMenu's `onAbout={() => (aboutOpen = true)}` callback wiring on line 131 — it still flips the prop, which the parent layout binds to.

    2. Edit `src/routes/+layout.svelte`:
       - REMOVE line 6 import: `import DisclaimerModal from '$lib/shared/components/DisclaimerModal.svelte';`
       - ADD imports:
         - `import DisclaimerBanner from '$lib/shared/components/DisclaimerBanner.svelte';`
         - `import AboutSheet from '$lib/shared/components/AboutSheet.svelte';`
         - `import { CALCULATOR_REGISTRY } from '$lib/shell/registry.js';` (if not already present)
         - `import { page } from '$app/state';` (if not already present — for activeCalculatorId derivation)
       - ADD `let aboutOpen = $state(false);`
       - ADD an `activeCalculatorId` $derived (mirroring NavShell's existing derivation): `const activeCalculatorId = $derived((CALCULATOR_REGISTRY.find((c) => page.url.pathname.startsWith(c.href))?.id) ?? 'morphine-wean');`
       - REMOVE line 68: `<DisclaimerModal />`
       - ADD in its place: `<DisclaimerBanner onShowFull={() => (aboutOpen = true)} />`
       - CHANGE `<NavShell />` on line 71 to `<NavShell bind:aboutOpen />`
       - ADD before the closing `</div>`: `<AboutSheet calculatorId={activeCalculatorId} bind:open={aboutOpen} />`

    3. DELETE `src/lib/shared/components/DisclaimerModal.svelte`.

    4. Confirm the banner sits between the title bar and `<main>` per UI-SPEC §DisclaimerBanner — i.e., AFTER `<NavShell />` (which renders the header) and BEFORE `<main>`. Reposition the `<DisclaimerBanner />` line accordingly if needed.
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/src/lib/shell/NavShell.svelte (current AboutSheet mounting on :133)
    - /mnt/data/src/nicu-assistant/src/routes/+layout.svelte (current DisclaimerModal mounting on :68)
    - /mnt/data/src/nicu-assistant/src/lib/shared/components/AboutSheet.svelte (calculatorId + open prop signature)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§12 — hoist pattern + bindable shape)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-UI-SPEC.md (§Modified Components — +layout.svelte rows)
  </read_first>
  <acceptance_criteria>
    - File `src/lib/shared/components/DisclaimerModal.svelte` does NOT exist (`test ! -f src/lib/shared/components/DisclaimerModal.svelte`)
    - `grep -F 'DisclaimerModal' src/routes/+layout.svelte` returns NO matches
    - `grep -F 'import DisclaimerBanner' src/routes/+layout.svelte` returns 1 line
    - `grep -F '<DisclaimerBanner onShowFull=' src/routes/+layout.svelte` returns 1 match
    - `grep -F '<NavShell bind:aboutOpen' src/routes/+layout.svelte` returns 1 match
    - `grep -F '<AboutSheet' src/routes/+layout.svelte` returns 1 match
    - `grep -F 'AboutSheet' src/lib/shell/NavShell.svelte` returns NO matches (hoisted out)
    - `grep -F 'aboutOpen = $bindable' src/lib/shell/NavShell.svelte` returns 1 line
    - `pnpm svelte-check` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <commit_message>
    refactor(42.1-03): hoist AboutSheet to layout; banner replaces modal (D-15)
  </commit_message>
</task>

<task id="42.1-03-T7">
  <action>
    Replace the e2e disclaimer modal spec with a banner spec (D-15):

    1. DELETE `e2e/disclaimer.spec.ts` (or rename to `e2e/disclaimer-banner.spec.ts` and rewrite — recommend rename to preserve git history; executor's call).

    2. Create (or rewrite) `e2e/disclaimer-banner.spec.ts` covering 5 scenarios per UI-SPEC §DisclaimerBanner + PATTERNS §8:
       - Fresh install: clear localStorage via `addInitScript`, navigate to `/morphine-wean`, assert banner visible with role="region" and "clinical decision support" text.
       - Dismiss + reload: click X, reload, assert banner hidden.
       - v1→v2 migration: `addInitScript` setting `_v1='true'`, navigate, assert banner NOT visible AND `localStorage.getItem('nicu_assistant_disclaimer_v2') === 'true'`.
       - "More" link opens AboutSheet at Disclaimer row: click "More", assert AboutSheet visible (`role="dialog"` or whatever AboutSheet uses), assert text contains "Disclaimer" heading.
       - Keyboard dismiss: Tab to dismiss button, Enter, assert banner hidden.

    Use the test pattern from `e2e/uac-uvc-a11y.spec.ts` (beforeEach, navigate, awaited locators).
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/e2e/disclaimer.spec.ts (the spec being replaced)
    - /mnt/data/src/nicu-assistant/e2e/uac-uvc-a11y.spec.ts (test.beforeEach pattern)
    - /mnt/data/src/nicu-assistant/src/lib/shared/components/DisclaimerBanner.svelte (under test)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-PATTERNS.md (§8 — banner spec coverage)
  </read_first>
  <acceptance_criteria>
    - File `e2e/disclaimer.spec.ts` does NOT exist (deleted) OR `git mv` preserved its history into `e2e/disclaimer-banner.spec.ts`
    - File `e2e/disclaimer-banner.spec.ts` exists
    - `pnpm test:e2e -- --grep "disclaimer"` exits 0 with 5+ tests passing
  </acceptance_criteria>
  <commit_message>
    test(42.1-03): rewrite disclaimer e2e for banner (D-15, D-32)
  </commit_message>
</task>

<task id="42.1-03-T8">
  <action>
    Run full regression sweep + write summary:

    1. `pnpm svelte-check` → 0/0
    2. `pnpm test:unit` → all green
    3. `pnpm test:e2e` → all green
    4. `pnpm test:axe` → 0 violations both themes (banner now visible at startup; AboutSheet Disclaimer row in both themes)

    Add an axe assertion in the banner spec (or extend existing axe sweeps) for the banner-visible state in both themes per UI-SPEC §axe Sweep Scope item 2.

    Then write `.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-03-SUMMARY.md` covering:
    - Files changed: list count
    - Decisions honored: D-12, D-13, D-14 (with v1→v2 migration correction per RESEARCH finding 1), D-15
    - Tests added: DisclaimerBanner.test.ts (6), e2e/disclaimer-banner.spec.ts (5+) = ~11 new
    - Tests updated: shared-components.test.ts disclaimer block (6 migration scenarios)
    - DESIGN.md named rules cited: 11 px Floor (banner uses text-ui), Tonal-Depth (surface-alt), Identity-Inside (no identity color in banner — neutral chrome)
    - Banner copy locked: include the final user-approved sentence
    - Deferred: D-29 hamburger header padding (Plan 6)
  </action>
  <read_first>
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-CONTEXT.md (D-31, D-32, D-33)
    - /mnt/data/src/nicu-assistant/.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-UI-SPEC.md (§axe Sweep Scope)
  </read_first>
  <acceptance_criteria>
    - `pnpm svelte-check` exits 0
    - `pnpm test:unit` exits 0
    - `pnpm test:e2e` exits 0
    - `pnpm test:axe` exits 0
    - `pnpm build` exits 0
    - File `42.1-03-SUMMARY.md` exists with the listed sections + locked banner copy
  </acceptance_criteria>
  <commit_message>
    docs(42.1-03): plan summary - banner refactor complete
  </commit_message>
</task>

## Verification (after all tasks complete)

- `pnpm svelte-check` → 0 errors, 0 warnings
- `pnpm test:unit` → all green (281 baseline + 6 banner + 6 migration scenarios for singleton)
- `pnpm test:e2e` → all green (new disclaimer-banner.spec.ts replaces old)
- `pnpm test:axe` → 0 violations both themes; banner visible state covered
- `pnpm build` → exits 0
- `test ! -f src/lib/shared/components/DisclaimerModal.svelte` → exit 0 (file deleted)
- `git grep -F 'DisclaimerModal' src/` → no matches
- `git grep -F 'nicu_assistant_disclaimer_v2' src/lib/shared/disclaimer.svelte.ts` → 2 matches (constant + acknowledge write)
- `git grep -F 'aria-label="Clinical use disclaimer"' src/lib/shared/components/DisclaimerBanner.svelte` → 1 match

## Notes

- **Autonomous: false** — banner copy needs user sign-off in T1. The rest of the plan is autonomous once copy is locked.
- The migration intentionally does NOT delete the v1 key — preserves audit trail per PATTERNS §"DisclaimerBanner migration pattern". A future Phase 43.x cleanup may remove v1 keys after the migration window closes.
- AboutSheet hoist is the key architectural shift this plan introduces. NavShell becomes simpler (no `aboutOpen` state ownership); layout becomes the single source of truth for global UI state (banner + sheet + nav) — sets up Plan 5's sticky-drawer pattern, which also lives at the layout level.
- This plan ships in parallel with Plans 1, 2, and 4 in the wave structure — no file overlap with them. Plan 6 (residue) does NOT touch disclaimer paths, so this plan is fully drainable.
- D-13 also adds the HMF tooltip note (D-23) into `aboutContent.formula.notes` — wait, no, D-23 is in Plan 2 T6. This plan only adds the `disclaimer` field. The HMF note lives in a separate `notes` array entry per Plan 2 T6 — confirm no conflict by reading both plans before executing.
