# Phase 42.1 — Post-Critique Follow-up Fixes

Date: 2026-04-24
Scope: Four findings surfaced by the post-Phase-42.1 `/impeccable` critique re-run.
Branch: `worktree-agent-a2b27a1f5e77acc3a` (based on `e66e7bf`).

## Findings (4)

| # | Priority | Finding | Status | Commit |
|---|----------|---------|--------|--------|
| 1 | P1 | Em-dashes in drawer summaries and GIR titration grid violate DESIGN.md "no em-dash in user-rendered copy" | Done (pre-spawn) | `e66e7bf` |
| 2 | P1 | Feeds advisories rendered with `bmf-*` tokens — violates DESIGN.md Amber-as-Semantic Rule (Amber is reserved exclusively for BMF fortifier) | Done | `b548ce4` |
| 3 | P2 | Two calculators lack a `text-display` (44 px) numeral on their primary hero — breaks the "result is the interface" promise and the Eyebrow-Above-Numeral Rule | Done | `3321d9e` + `f00658a` + `86c5a6c` |
| 4 | P2 | Morphine "Clear inputs" button at 36 px violates 48 px touch floor; 3 morphine NumericInputs miss explicit `showRangeHint`/`showRangeError` props (parity with Feeds + UAC/UVC) | Done | `2331ec5` |

## Task 2 — Rescope Amber to BMF-semantic only

**Commit:** `b548ce4` — `fix(42.1-followup): rescope Amber tokens to BMF only, neutralize Feeds advisory (P1)`

**Change:** `src/lib/feeds/FeedAdvanceCalculator.svelte`

- Replaced `bg: var(--color-bmf-50)` / `border-color: var(--color-bmf-300)` / `text-[var(--color-bmf-600)]` on the triggered-advisory block with neutral tokens:
  - `bg-[var(--color-surface-alt)]`
  - `border-[var(--color-border)]`
  - `text-[var(--color-text-secondary)]` on the AlertTriangle glyph
  - Body text stays `text-[var(--color-text-primary)]` but steps down from `text-base` to `text-ui` (DESIGN.md 13 px advisory floor).
- Added a 5-line comment above the `{#each}` explaining why the advisory is neutral rather than amber, so a future developer cannot accidentally re-introduce amber.

**Verification:** `grep -n "bmf-" src/lib/feeds/FeedAdvanceCalculator.svelte` — 0 results. `pnpm exec vitest run src/lib/feeds/FeedAdvanceCalculator.test.ts` — 12/12 green.

**Note on residual `bmf-*` usage:** `src/lib/gir/GirInputs.svelte` (Dextrose >12.5% advisory) still uses the same `bmf-50` / `bmf-300` / `bmf-600` trio. This is the identical Amber-as-Semantic violation but was **outside this task's stated scope** (the spec scoped Task 2 to Feeds advisories, and acceptance grep targeted only `src/lib/feeds/FeedAdvanceCalculator.svelte`). Flagging it here for follow-up — the fix is a copy of what was applied to Feeds.

## Task 3 — Enforce HeroResult display-numeral rule

Goal: every HeroResult must contain at least one `text-display` (44 px / font-black) element so the numeral is always the largest object on the card.

### 3a — Codify the rule in `HeroResult.svelte` (+ tests)

**Commit:** `86c5a6c` — `test(42.1-followup): document HeroResult display-numeral rule (P2)`

- Added a JSDoc-style HTML comment block at the top of `HeroResult.svelte` stating the contract (locked 2026-04-24), pointing to DESIGN.md's Eyebrow-Above-Numeral + Hero Result sections, and explaining the default-props path auto-honors the rule while the children snippet path must carry it explicitly.
- Added two tests in `HeroResult.test.ts`:
  1. `default-props path always renders a text-display numeral` — pins the `text-display font-black` classes on the default-value span so any future refactor that drops them breaks this test.
  2. `custom children snippet paths must include a text-display element (documentation)` — narrative test that records the contract in prose; passes tautologically so it is permanent.

### 3b — Morphine summary: promote Total reduction to display numeral

**Commit:** `3321d9e` — `refactor(42.1-followup): promote Total reduction to display numeral on morphine summary (P2)`

- `src/lib/morphine/MorphineWeanCalculator.svelte`: replaced the 3-cell Start/Step/Total-reduction strip (largest text was `text-base`, 16 px) with:
  - Eyebrow `WEANING SUMMARY` (2xs label-role, UPPERCASE, identity color)
  - Display numeral: `{formatPercent((totalReduction / first.doseMg) * 100)}` in `text-display font-black` neutral text, paired with "Total reduction" in ui-weight secondary text on the same baseline
  - Secondary line: `Start {formatMg(first.doseMg)} to Step {last.step} {formatMg(last.doseMg)} mg` in `text-ui` `text-secondary` — readable but clearly subordinate. Uses "to" (not arrow glyph or em-dash) per DESIGN.md copy rules.
- `MorphineWeanCalculator.test.ts`: replaced the old "summary card shows start dose, end dose, and total reduction percentage" test with a new test that asserts the `.text-display` element inside `[data-testid="morphine-summary"]` renders a percentage, that "Total reduction" is still present, and that the secondary line still contains the start-dose value (`0.124`) and the word "Step".
- E2E (`e2e/morphine-wean.spec.ts`) — pre-existing test that asserts `summary` contains `90.00%` and `Total reduction` continues to pass against the new shape (semantic content unchanged).

### 3c — GIR: split hero into paired HeroResults

**Commit:** `f00658a` — `refactor(42.1-followup): split GIR hero into paired HeroResults with display numerals (P2)`

- `src/lib/gir/GirCalculator.svelte`: replaced the single-HeroResult-with-two-stacked-rows shape (where "INITIAL RATE" was `text-title`, 22 px, smaller than "CURRENT GIR" at `text-display`) with two HeroResults in a `grid grid-cols-1 md:grid-cols-2 gap-4` — mirrors the UAC/UVC paired-hero pattern.
  - First hero: `eyebrow="CURRENT GIR"`, `value={result.currentGirMgKgMin.toFixed(1)}`, `unit="mg/kg/min"` — default-props path, auto-renders a 44 px numeral.
  - Second hero: `eyebrow="INITIAL RATE"`, `value={result.initialRateMlHr.toFixed(1)}`, `unit="ml/hr"` — same.
  - Mobile stacks vertically; md+ shows side-by-side.
- Empty state retained as a single HeroResult with a children snippet (empty-state message — no numeral to render).
- `GirCalculator.test.ts` — all 9 tests continue to pass unchanged (existing assertions target `CURRENT GIR`, `INITIAL RATE`, `mg/kg/min`, `ml/hr` text presence; none asserted on the single-HeroResult shape).
- `e2e/gir.spec.ts` — asserts on `CURRENT GIR` text and `mg/kg/min` / `ml/hr` presence; structural change is transparent. Verified by inspection; no spec edits required.

### 3d — UAC/UVC verification (no change)

Read `src/lib/uac-uvc/UacUvcCalculator.svelte`:

- Each of the two paired HeroResults renders via a children snippet that contains:
  - `<span class="text-title font-black tracking-tight text-[var(--color-identity)] uppercase">UAC</span>` — 22 px label-role promoted to a visible ID
  - `<span class="num text-display font-black text-[var(--color-text-primary)]">{result.uacCm.toFixed(1)}</span>` — 44 px display numeral
- **text-display (44 px) > text-title (22 px)** — the numeral is larger than the promoted UAC/UVC label. Contract satisfied, **no change needed**.

### Acceptance check for Task 3

- `grep -L "text-display" src/lib/{morphine,fortification,gir,feeds,uac-uvc}/*Calculator.svelte` returns two files: `fortification/FortificationCalculator.svelte` and `feeds/FeedAdvanceCalculator.svelte`.
  - **Both** use the HeroResult **default-props path** for their primary result hero (`<HeroResult eyebrow="..." value={...} unit="..." />`) — the `text-display` span is rendered by HeroResult's default snippet internally.
  - Only their empty-state branches use children snippets (no numeral to render — by design).
  - **Intent of the rule (at-least-one display-size numeral on every populated hero) is satisfied.** The literal grep check returns these two as false positives because the display class lives in the shared component, not in the calculator file.

## Task 4 — Morphine touch-floor + range-hint parity

**Commit:** `2331ec5` — `polish(42.1-followup): enforce 48 px touch floor + range-hint parity on morphine inputs (P2)`

- `src/lib/morphine/MorphineWeanInputs.svelte`:
  - Bumped Clear-inputs button from `min-h-[36px]` to `min-h-[48px]`, `py-1.5` to `py-2`, and `text-2xs` to `text-ui` so the label scales with the taller hit area without crowding.
  - Added `showRangeHint={true}` and `showRangeError={true}` explicitly to all three NumericInputs (Dosing weight, Max morphine dose, Decrease per step), matching the explicit pattern used in `GirInputs.svelte` and `UacUvcInputs.svelte` (semantically a no-op — NumericInput defaults both to `true` — but explicit pass-through is the family convention).

### Acceptance check for Task 4

- `grep -n "min-h-\[36" src/lib/morphine/MorphineWeanInputs.svelte` — 0 results.
- `grep -c "showRangeHint={true}" src/lib/morphine/MorphineWeanInputs.svelte` — 3 (>= 3).
- `grep -c "showRangeError={true}" src/lib/morphine/MorphineWeanInputs.svelte` — 3 (>= 3).

## Final verification

| Gate | Result |
|------|--------|
| `pnpm check` | 0 errors / 0 warnings / 4567 files |
| `pnpm test:run` | **332 passed / 332** (baseline was 330 before Task 3a; +2 new HeroResult documentation tests) |
| `pnpm build` | Exits 0 — `Wrote site to "build"` |
| E2E (spot-check by inspection) | `e2e/morphine-wean.spec.ts` and `e2e/gir.spec.ts` pre-existing assertions compatible with the new shapes — no spec edits required |

## Commit timeline

```
2331ec5 polish(42.1-followup): enforce 48 px touch floor + range-hint parity on morphine inputs (P2)      [Task 4]
86c5a6c test(42.1-followup): document HeroResult display-numeral rule (P2)                                [Task 3a]
f00658a refactor(42.1-followup): split GIR hero into paired HeroResults with display numerals (P2)        [Task 3c]
3321d9e refactor(42.1-followup): promote Total reduction to display numeral on morphine summary (P2)      [Task 3b]
b548ce4 fix(42.1-followup): rescope Amber tokens to BMF only, neutralize Feeds advisory (P1)              [Task 2]
e66e7bf fix(42.1-followup): purge em-dashes from drawer summaries + GIR titration grid (P1)               [Task 1, pre-spawn]
```

## Deviations from the spec

1. **Morphine summary secondary-line copy.** Spec suggested `"Start X.XXX → Step 10 Y.YYY mg"` using an arrow glyph; I rendered `"Start 0.124 to Step 10 0.012 mg"` (word "to", no glyph) to keep the copy screen-reader-friendly and consistent with the em-dash ban — the arrow is a Unicode glyph that SRs announce as "right arrow," which reads as noise in a dose-reduction sentence. Functional meaning is identical and the pre-existing E2E assertion (`90.00%` + `Total reduction`) is untouched.

2. **`grep -L "text-display"` acceptance check caveat.** As described in Task 3d, Feeds and Fortification still appear in the inverted-match list even though they honor the rule — they use the HeroResult default-props path where the display numeral lives inside the shared component. Documented above; no additional action needed.

3. **GirInputs.svelte Amber residue.** Not in Task 2's stated scope, documented above for potential follow-up. Same violation pattern as Feeds; fix would be a copy of `b548ce4`.

4. **Worktree base reset.** The worktree HEAD on entry was `307d218` (a Prettier-format commit on a different lineage) rather than the expected `e66e7bf`. Per the base-check protocol, I did a `git reset --hard e66e7bf` to correct it. All five new commits in this branch are rooted on `e66e7bf` as intended.
