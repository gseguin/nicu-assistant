---
phase: 42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co
plan: 01
subsystem: ui
tags: [svelte, tailwind, navigation, chrome, design-system, identity-inside-rule, oklch, accessibility]

# Dependency graph
requires:
  - phase: 42-uac-uvc-calculator
    provides: chrome surfaces (NavShell, HamburgerMenu) + 281-test green baseline
provides:
  - identity-inside conformance test (static-source regex enforcement)
  - chrome aligned to Clinical Blue (var(--color-accent)) per D-02
  - hamburger drawer star icons neutralized (D-03)
  - shortened "Morphine" tab label across registry + every nav-tab assertion (D-10)
  - mobile-nav clearance via safe-area calc replaces universal pb-20 (D-09)
  - sole glassmorphism license documented on bottom nav (D-11)
affects: [42.1-02, 42.1-03, 42.1-04, 42.1-05, 42.1-06, future-calculators]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Identity-Inside Rule conformance test (static-source regex via node:fs in vitest)
    - Tailwind 4 calc(theme(spacing.16)+env(safe-area-inset-bottom,0px)+1rem) arbitrary value pattern
    - bg-[var(--color-surface)]/95 backdrop-blur as the SOLE glassmorphism license, with HTML-comment guard

key-files:
  created:
    - src/lib/shell/identity-inside.test.ts
  modified:
    - src/lib/shell/NavShell.svelte
    - src/lib/shell/HamburgerMenu.svelte
    - src/lib/shell/registry.ts
    - src/routes/+layout.svelte
    - src/lib/shell/__tests__/registry.test.ts
    - src/lib/shell/NavShell.test.ts
    - src/lib/shell/HamburgerMenu.test.ts
    - e2e/navigation.spec.ts
    - e2e/favorites-nav.spec.ts

key-decisions:
  - "Identity tokens preserved (D-04 sacred): app.css:214-263 byte-identical, only scope changed"
  - "HamburgerMenu calculator-link focus ring (line 102) keeps var(--color-identity) — link is INSIDE the calculator's identity-class row context per UI-SPEC nuance, not chrome"
  - "favorites-nav.spec.ts also updated even though plan only enumerated navigation.spec.ts, because it asserts the same nav-tab label downstream"

patterns-established:
  - "Identity-Inside conformance test: static-source regex via node:fs.readFileSync — adding a new chrome surface that uses --color-identity will fail CI before it can ship"
  - "Tailwind 4 accepts theme() inside arbitrary values: pb-[calc(theme(spacing.16)+env(safe-area-inset-bottom,0px)+1rem)] compiles cleanly under @tailwindcss/vite ^4.2.2 — literal-fallback NOT needed"
  - "Glassmorphism license discipline: a single HTML comment immediately above the licensed surface is the project's mechanism for preventing pattern propagation"

requirements-completed: [D-01, D-02, D-03, D-04, D-09, D-10, D-11]

# Metrics
duration: ~25min
completed: 2026-04-23
---

# Plan 42.1-01: Chrome Identity-Inside Enforcement + Mobile-Nav Clearance Summary

**Chrome alignment: NavShell + HamburgerMenu chrome now uses Clinical Blue (var(--color-accent)) per the Identity-Inside Rule, identity hue tokens preserved as per-calculator system; static-source regex test prevents regression; pb-20 universal mobile clearance replaced with safe-area calc; bottom nav gains the system's sole licensed glassmorphism.**

## Performance

- **Wave:** 1
- **Tasks:** 7 completed
- **Commits:** 6 task commits + 1 summary commit
- **Files modified:** 9
- **Files created:** 2 (identity-inside.test.ts, this SUMMARY.md)

## Accomplishments

- Identity-Inside Rule (D-01) is now self-enforcing via `src/lib/shell/identity-inside.test.ts` (2 assertions: NavShell chrome + HamburgerMenu Star color)
- All 5 hue references in chrome class strings (NavShell desktop tab `border-b-2`, NavShell desktop tab `text-`, NavShell mobile bottom tab `text-`, NavShell focus rings ×2, HamburgerMenu Star color, HamburgerMenu favorites button focus ring) swapped to `var(--color-accent)` (Clinical Blue) or neutral text tokens
- "Morphine Wean" tab label shortened to "Morphine" everywhere it appears as a NAV TAB; route h1 and AboutSheet title preserved as full name per CONTEXT D-10
- Universal `pb-20` mobile nav clearance replaced with `pb-[calc(theme(spacing.16)+env(safe-area-inset-bottom,0px)+1rem)]` — Tailwind 4 accepted the `theme()` inside `[]` (no literal-fallback needed)
- Stale "magnification headroom" comment from Phase 33 purged
- Bottom nav surface gains `bg-[var(--color-surface)]/95 backdrop-blur` with HTML-comment marking it as the sole glassmorphism license

## Task Commits

Each task was committed atomically (--no-verify per orchestrator instruction for parallel-wave execution):

1. **T1: Identity-Inside conformance test** — `6488310` (test)
2. **T2: NavShell chrome → Clinical Blue** — `bd1d63c` (feat)
3. **T3: Morphine Wean → Morphine label rename + downstream test updates** — `b2ecb9a` (refactor)
4. **T4: Hamburger Star color neutralized** — `e29e499` (feat)
5. **T5: pb-20 → safe-area calc + stale comment fix** — `5d67cdb` (fix)
6. **T6: backdrop-blur on mobile bottom nav** — `081444e` (feat)
7. **T7: Verification + plan summary** — pending (this commit)

## Files Created/Modified

### Created
- `src/lib/shell/identity-inside.test.ts` — 2-assertion vitest static-source conformance test enforcing D-01 (Identity-Inside Rule) for NavShell.svelte and HamburgerMenu.svelte
- `.planning/phases/42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co/42.1-01-SUMMARY.md` — this file

### Modified
- `src/lib/shell/NavShell.svelte` — desktop tab + mobile bottom tab active state and focus ring colors swapped from `var(--color-identity)` to `var(--color-accent)`; bottom nav surface gains `/95 backdrop-blur` and HTML license comment
- `src/lib/shell/HamburgerMenu.svelte` — favorites star icon color uses `var(--color-text-primary)` (favorited) / `var(--color-text-tertiary)` (not favorited); favorites star button focus ring uses `var(--color-accent)`. The calculator-link focus ring at :102 (still `var(--color-identity)`) is intentionally preserved per the plan — it sits inside the calculator's identity-class row context
- `src/lib/shell/registry.ts` — `label: 'Morphine Wean'` → `label: 'Morphine'` (description unchanged)
- `src/routes/+layout.svelte` — `pb-20` → `pb-[calc(theme(spacing.16)+env(safe-area-inset-bottom,0px)+1rem)]`; stale "magnification headroom" comment replaced with calc breakdown
- `src/lib/shell/__tests__/registry.test.ts` — label assertion updated to 'Morphine'
- `src/lib/shell/NavShell.test.ts` — three `/Morphine Wean/i` regex assertions updated to `/Morphine/i`
- `src/lib/shell/HamburgerMenu.test.ts` — link/button name regex updates: `Morphine Wean` → `Morphine` (link names + favorite-button names) — comments left intact (descriptive only, not assertions)
- `e2e/navigation.spec.ts` — bottom-nav tab text assertion updated to `'Morphine'`
- `e2e/favorites-nav.spec.ts` — line 98 nav-tab text assertion updated to `'Morphine'` (added beyond plan's strict enumeration because it's the same downstream NAV TAB assertion)

## Decisions Honored

- **D-01 Identity-Inside Rule:** chrome class strings in `NavShell.svelte` no longer reference `var(--color-identity)`; HamburgerMenu Star color no longer references it. Conformance test enforces.
- **D-02 Active chrome → Clinical Blue:** `border-[var(--color-accent)] text-[var(--color-accent)]` (desktop tab); `font-semibold text-[var(--color-accent)]` (mobile bottom tab); both `focus-visible:outline-[var(--color-accent)]`.
- **D-03 Hamburger Star icons neutral:** favorited = `var(--color-text-primary)`, not-favorited = `var(--color-text-tertiary)`; star button focus ring = `var(--color-accent)`.
- **D-04 Identity tokens preserved:** `app.css:214-263` UNTOUCHED (`git diff src/app.css` is empty for this plan).
- **D-09 Mobile nav clearance:** `pb-20` → `pb-[calc(theme(spacing.16)+env(safe-area-inset-bottom,0px)+1rem)]`. Build green; theme() inside arbitrary value supported by Tailwind 4.2.2 — no literal-fallback needed.
- **D-09 Stale comment:** `magnification headroom` purged.
- **D-10 Morphine label shortened:** registry `label: 'Morphine'`; route h1 keeps `Morphine Wean` (per CONTEXT — full name in route header).
- **D-11 Bottom nav glassmorphism:** `bg-[var(--color-surface)]/95 backdrop-blur` with HTML-comment "sole glassmorphism license" guard.

DESIGN.md named rules cited: Identity-Inside, Tonal-Depth (`/95` opacity respects neutral tonal layering rather than introducing a new layered surface).

## Tests

- **New:** `src/lib/shell/identity-inside.test.ts` (2 assertions, both green after T4)
- **Modified:** `registry.test.ts` (1 assertion), `NavShell.test.ts` (3 regex assertions, all `/Morphine Wean/i` → `/Morphine/i`), `HamburgerMenu.test.ts` (10 regex updates: 3× `/Morphine Wean/i` link names + 4× `/Remove Morphine Wean from favorites/i` button names + 3× `/Add Morphine Wean to favorites/i` button names), `e2e/navigation.spec.ts` (1 assertion), `e2e/favorites-nav.spec.ts` (1 assertion)
- **All passing:** 283/283 unit tests (281 baseline + 2 new identity-inside)

## Verification Results

- **`pnpm check` (svelte-check):** 0 errors / 0 warnings / 0 files-with-problems
- **`pnpm test:run` (vitest unit):** 283/283 passing (281 baseline + 2 new identity-inside)
- **`pnpm exec playwright test --grep "navigation|morphine-wean"`:** 16 passed / 5 failed
  - The 5 failures are PRE-EXISTING baseline failures unrelated to this plan:
    1. `morphine-wean.spec.ts:18 displays 10-step weaning schedule with defaults` — searches for `/Step 1 — Starting dose/` (em-dash) but source uses colon `Step 1: Starting dose` after the em-dash purge in commit 917ecf2 (before this phase)
    2. `morphine-wean-a11y.spec.ts:47 schedule visible (light)` — same em-dash text dependency
    3. `morphine-wean-a11y.spec.ts:64 schedule visible (dark)` — same em-dash text dependency
    4. `navigation.spec.ts:17 top title bar shows app name, info, and theme toggle` — searches for `getByRole('button', { name: /about/i })` in header, but the About button lives inside the hamburger drawer, not the top header (NavShell only has hamburger + theme buttons in the header). Verified by checking out the base commit's source.
    5. `navigation.spec.ts:79 info button opens the about sheet` — same About-in-header dependency
  - The label-rename downstream tests (D-10) PASS: `mobile bottom nav has only calculator tabs filling full width` (uses `'Morphine'` literal), `switching between calculators preserves navigation`, `desktop layout shows all controls in top bar`, `theme toggle switches between light and dark mode`.
- **`pnpm build`:** exits 0; Tailwind 4.2.2 compiled `pb-[calc(theme(spacing.16)+env(safe-area-inset-bottom,0px)+1rem)]` and `bg-[var(--color-surface)]/95 backdrop-blur` cleanly.
- **`git diff src/app.css`:** empty (D-04 guarantee — `app.css` not touched).
- **`git grep -F 'var(--color-identity)' src/lib/shell/NavShell.svelte`:** 0 matches.
- **`git grep -F 'var(--color-identity)' src/lib/shell/HamburgerMenu.svelte`:** 1 match (line 102 calculator-link focus ring inside identity-class row context — exempt per plan).
- **`git grep -F 'pb-20' src/`:** 0 matches.
- **`git grep -F 'magnification headroom' src/`:** 0 matches.
- **`git grep -F "label: 'Morphine Wean'" src/lib/shell/registry.ts`:** 0 matches.

## Deviations from Plan

### Auto-fixed: e2e/favorites-nav.spec.ts also updated

- **Found during:** T3 (label rename downstream verification via `git grep "'Morphine Wean'" src/ e2e/`)
- **Issue:** Plan T3 enumerated only `e2e/navigation.spec.ts` and the three component test files for nav-tab label updates, but `e2e/favorites-nav.spec.ts:98` contains the identical nav-tab assertion `await expect(tabs.nth(0)).toContainText('Morphine Wean')` and would have failed e2e CI after the registry label change.
- **Fix:** updated `tabs.nth(0).toContainText('Morphine Wean')` → `tabs.nth(0).toContainText('Morphine')` in `e2e/favorites-nav.spec.ts:98`.
- **Verification:** `git grep -F "'Morphine Wean'" e2e/` returns no nav-tab assertion matches; only the route-h1 heading lookups in `morphine-wean-a11y.spec.ts` and `pwa.spec.ts` remain (both correct per CONTEXT D-10 — h1 keeps full name).
- **Committed in:** `b2ecb9a` (Task 3 commit, alongside the other downstream test updates).

### Auto-fixed: NavShell.test.ts regex broadened from `/Morphine Wean/i` to `/Morphine/i`

- **Found during:** T3 (NavShell.test.ts assertions enumerated in plan as one of the four downstream test files)
- **Issue:** Three assertions used `expect(tabs[0].textContent).toMatch(/Morphine Wean/i)` — the regex would no longer match after the label rename to `'Morphine'`.
- **Fix:** updated all three to `/Morphine/i`. (The bare regex matches both old and new text — chosen instead of an exact-string assertion to avoid coupling to a specific spelling format.)
- **Committed in:** `b2ecb9a` (Task 3 commit).

### No deviation: Tailwind 4 accepted theme() inside arbitrary value

- **Note:** Plan T5 documented a literal-fallback `pb-[calc(4rem+env(safe-area-inset-bottom,0px)+1rem)]` in case Tailwind 4 rejected `theme(spacing.16)` inside `[]`. Build succeeded on first try with the `theme()` form — no fallback needed.

**Total deviations:** 2 auto-fixed (both label-rename downstream coverage extensions, neither changes plan scope).
**Impact on plan:** Both deviations strengthen test coverage of the label rename across all e2e test files, preventing CI breakage. No scope creep.

## Issues Encountered

- **Worktree node_modules absent:** Initial `pnpm test:run` failed with "vitest: not found". Resolved by running `pnpm install --prefer-offline` once at start. Worktree CI flow assumption.
- **Worktree path vs main repo path:** First Write call to create `identity-inside.test.ts` used the main-repo absolute path; cleaned up and re-wrote into the worktree path `/mnt/data/src/nicu-assistant/.claude/worktrees/agent-ae3f25959647c0847/...`. All subsequent edits used worktree paths exclusively.
- **5 pre-existing e2e failures masquerading as regressions:** Initial concern that label-rename or chrome-color changes broke e2e tests. Confirmed by checking out the base commit's source and re-running — the same 5 failures appear on base. Both root causes are independent of this plan and tracked under future plans (em-dash `Step 1 — Starting dose` text needs the same purge applied to source, About button placement test needs to be aligned with the hamburger drawer reality).

## Next Plan Dependencies

- **Plan 42.1-02 may proceed:** No blocking dependency.
- **Plan 42.1-04 (CSS transitions scoping per D-17) explicitly owns `src/app.css` edits** — this plan honored the D-04 byte-identical guarantee. Plan 4 must not unintentionally touch the `.identity-{name}` token block at lines 214-263.
- **Plan 42.1-06 mobile-nav-clearance Playwright spec** will visually validate D-09 at 375 + 414 viewports — this plan only changed the class string and verified `pnpm build` compiles.

---
*Phase: 42.1-design-polish-sweep-impeccable-critique-remainder-onboard-co*
*Plan: 01 (Wave 1)*
*Completed: 2026-04-23*
