---
phase: 19-tab-identity-token
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/lib/morphine/MorphineWeanCalculator.svelte
  - src/lib/fortification/FortificationCalculator.svelte
  - src/lib/shared/components/NumericInput.svelte
  - src/lib/shared/components/SelectPicker.svelte
autonomous: true
requirements: [IDENT-02, IDENT-03, IDENT-04]

must_haves:
  truths:
    - "Morphine result hero card background is --color-identity-hero; Formula 'Amount to Add' hero card background is --color-identity-hero"
    - "Eyebrow labels ('Start', 'Step N', 'Total reduction', 'Step N — ...', 'Amount to Add', 'Verification') render in --color-identity"
    - "NumericInput focus ring uses --color-identity in both border and ring positions; idle border stays on --color-border"
    - "SelectPicker trigger and option focus-visible outlines use --color-identity; the hover border on the trigger uses --color-identity"
    - "SelectPicker check marks (lines ~238, 257, 281) are NOT changed — ctx.accentColor stays untouched"
    - "MorphineWeanCalculator mode-tab pills (linear/compounding) focus-visible and active text read --color-identity (they are inside the calculator body — IDENT-03)"
  artifacts:
    - path: "src/lib/morphine/MorphineWeanCalculator.svelte"
      provides: "Result hero + eyebrows + mode-tab focus on --color-identity"
      contains: "--color-identity-hero"
    - path: "src/lib/fortification/FortificationCalculator.svelte"
      provides: "Result hero + eyebrows on --color-identity"
      contains: "--color-identity-hero"
    - path: "src/lib/shared/components/NumericInput.svelte"
      provides: "Focus ring reads --color-identity"
      contains: "var(--color-identity)"
    - path: "src/lib/shared/components/SelectPicker.svelte"
      provides: "Focus-visible outlines read --color-identity; checkmarks untouched"
      contains: "var(--color-identity)"
  key_links:
    - from: "MorphineWeanCalculator hero surface"
      to: "--color-identity-hero (declared in .identity-morphine wrapper)"
      via: "CSS cascade from identity-morphine wrapper on route page"
      pattern: "bg-\\[var\\(--color-identity-hero\\)\\]"
    - from: "NumericInput focus ring"
      to: "--color-identity set by route-level .identity-* wrapper"
      via: "CSS cascade through component instance"
      pattern: "focus:ring-\\[var\\(--color-identity\\)\\]"
---

<objective>
Wire the remaining three body surfaces — result heroes (IDENT-02), focus-visible outlines inside the calculator body (IDENT-03), and eyebrow labels (IDENT-04) — to `--color-identity` and `--color-identity-hero`. Touch only the specific lines enumerated in RESEARCH.md §File-by-File Edit Map; leave picker checkmarks, idle input borders, and route-header icons on their existing neutral/accent tokens.

Purpose: Complete the four identity surfaces so each calculator visibly identifies itself.
Output: Morphine + Formula body surfaces read per-route identity via CSS cascade from the wrappers added in Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-tab-identity-token/19-RESEARCH.md
@.planning/phases/19-tab-identity-token/19-01-SUMMARY.md
@src/lib/morphine/MorphineWeanCalculator.svelte
@src/lib/fortification/FortificationCalculator.svelte
@src/lib/shared/components/NumericInput.svelte
@src/lib/shared/components/SelectPicker.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap tokens on MorphineWeanCalculator hero, eyebrows, and mode-tab focus</name>
  <read_first>
    - src/lib/morphine/MorphineWeanCalculator.svelte lines 195-300 (contains mode-tab pills ~line 202, hero card ~line 262, eyebrows ~lines 264, 269, 273, 289)
    - .planning/phases/19-tab-identity-token/19-RESEARCH.md §File-by-File Edit Map entry 4, §Focus-Visible Audit, §Eyebrow Audit
  </read_first>
  <action>
    In src/lib/morphine/MorphineWeanCalculator.svelte, make ONLY the following edits (line numbers are guidance; search for the exact class strings if numbers drift):

    **Line 202 (mode-tab pills — linear/compounding) — IDENT-03:**
    - `focus-visible:ring-[var(--color-accent-light)]` → `focus-visible:ring-[var(--color-identity)]`
    - Active state `text-[var(--color-accent)]` → `text-[var(--color-identity)]`
    - Hover `hover:text-[var(--color-accent)]` → `hover:text-[var(--color-identity)]`
    - Rationale (from §File-by-File Edit Map entry 4): linear/compounding tabs are INSIDE the calculator body, so they fall under IDENT-03, not IDENT-05.

    **Line 262 (result hero — start→end summary card) — IDENT-02:**
    - `bg-[var(--color-accent-light)]` → `bg-[var(--color-identity-hero)]`

    **Lines 264, 269, 273 (eyebrow labels "Start", "Step N", "Total reduction") — IDENT-04:**
    - `text-[var(--color-text-secondary)]` → `text-[var(--color-identity)]` on each of these three `<span>` elements only (not surrounding cards).

    **Line 289 (step card eyebrow "Step 1 — Starting dose" / "Step N") — IDENT-04:**
    - `text-[var(--color-text-secondary)]` → `text-[var(--color-identity)]` on this span only.

    **Do NOT touch:**
    - Any other `text-[var(--color-text-secondary)]` occurrences that are NOT eyebrows (body text, hints, etc.) — only the six eyebrow spans enumerated in §Eyebrow Audit table.
    - The calculation values / numeric text color.
    - Any surrounding card backgrounds other than line 262.
  </action>
  <acceptance_criteria>
    - `grep -c "bg-\[var(--color-identity-hero)\]" src/lib/morphine/MorphineWeanCalculator.svelte` returns exactly 1
    - `grep -c "text-\[var(--color-identity)\]" src/lib/morphine/MorphineWeanCalculator.svelte` returns at least 4 (four eyebrows + active mode tab)
    - `grep -c "ring-\[var(--color-identity)\]" src/lib/morphine/MorphineWeanCalculator.svelte` returns at least 1
    - `grep -c "bg-\[var(--color-accent-light)\]" src/lib/morphine/MorphineWeanCalculator.svelte` returns 0 (hero moved off accent-light)
    - `grep -c "hover:text-\[var(--color-accent)\]" src/lib/morphine/MorphineWeanCalculator.svelte` returns 0
    - Existing component tests still pass: `pnpm exec vitest run src/lib/morphine`
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/lib/morphine --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <done>Morphine hero + 4 eyebrows + mode-tab focus all on identity tokens; no other edits.</done>
</task>

<task type="auto">
  <name>Task 2: Swap tokens on FortificationCalculator hero + eyebrows</name>
  <read_first>
    - src/lib/fortification/FortificationCalculator.svelte lines 180-220 (hero card ~185, eyebrow "Amount to Add" ~190, eyebrow "Verification" ~214)
    - .planning/phases/19-tab-identity-token/19-RESEARCH.md §File-by-File Edit Map entry 5, §Result Hero Mapping, §Eyebrow Audit
  </read_first>
  <action>
    In src/lib/fortification/FortificationCalculator.svelte, make ONLY the following edits:

    **Line 185 (result hero — "Amount to Add" card) — IDENT-02:**
    - `bg-[var(--color-accent-light)]` → `bg-[var(--color-identity-hero)]`

    **Line 190 (eyebrow "Amount to Add") — IDENT-04:**
    - `text-[var(--color-text-secondary)]` → `text-[var(--color-identity)]` on the eyebrow span only.

    **Line 214 (eyebrow "Verification") — IDENT-04:**
    - `text-[var(--color-text-secondary)]` → `text-[var(--color-identity)]` on the eyebrow span only.

    **Do NOT touch:**
    - The large numeric value's color (stays on current primary/result token).
    - Any other `text-[var(--color-text-secondary)]` occurrences that are not one of the two eyebrows above.
    - Any `--color-bmf-*` references (IDENT-06 — BMF stays reserved for future fortifier-mode semantic signaling).
    - The `<Milk>` icon or other card chrome outside the two spans and the hero background.
  </action>
  <acceptance_criteria>
    - `grep -c "bg-\[var(--color-identity-hero)\]" src/lib/fortification/FortificationCalculator.svelte` returns exactly 1
    - `grep -c "text-\[var(--color-identity)\]" src/lib/fortification/FortificationCalculator.svelte` returns exactly 2
    - `grep -c "bg-\[var(--color-accent-light)\]" src/lib/fortification/FortificationCalculator.svelte` returns 0
    - `grep -niE "color-bmf.*identity|identity.*color-bmf" src/lib/fortification/FortificationCalculator.svelte` returns no results (IDENT-06 guard)
    - Existing fortification tests still pass
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/lib/fortification --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <done>Formula hero + 2 eyebrows on identity tokens; BMF tokens untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Wire focus-visible outlines in NumericInput and SelectPicker (IDENT-03)</name>
  <read_first>
    - src/lib/shared/components/NumericInput.svelte lines 110-140 (focus state ~line 126)
    - src/lib/shared/components/SelectPicker.svelte lines 170-290 (trigger ~176, options ~250/274, checkmarks ~238/257/281)
    - .planning/phases/19-tab-identity-token/19-RESEARCH.md §File-by-File Edit Map entries 6 and 7, §Focus-Visible Audit, §Pitfall 2
  </read_first>
  <action>
    **In src/lib/shared/components/NumericInput.svelte (line ~126):**
    - `focus:border-[var(--color-accent)]` → `focus:border-[var(--color-identity)]`
    - `focus:ring-[var(--color-accent-light)]` → `focus:ring-[var(--color-identity)]`
    - If there is a scale-focus state `border-[var(--color-accent)]` in the same component, also swap it to `border-[var(--color-identity)]`.
    - **DO NOT** change the idle border `border-[var(--color-border)]` — it must stay neutral (IDENT-07: input borders stay neutral at rest; identity only activates on focus).

    **In src/lib/shared/components/SelectPicker.svelte:**
    - **Line ~176 (trigger button):**
      - `hover:border-[var(--color-accent)]` → `hover:border-[var(--color-identity)]`
      - `focus-visible:outline-[var(--color-accent)]` → `focus-visible:outline-[var(--color-identity)]`
    - **Line ~250 (option button):** `focus-visible:outline-[var(--color-accent)]` → `focus-visible:outline-[var(--color-identity)]`
    - **Line ~274 (option button):** `focus-visible:outline-[var(--color-accent)]` → `focus-visible:outline-[var(--color-identity)]`

    **CRITICAL — DO NOT TOUCH (from §Pitfall 2 + §File-by-File Edit Map entry 7):**
    - Lines ~238, 257, 281 — the `style="color: {accentColor}"` expressions on the selection checkmarks. These use `ctx.accentColor` from context.ts and MUST stay on `--color-accent`. Picker check marks are explicitly out of scope per REQUIREMENTS "Out of Scope".
    - The `ctx.accentColor` wiring in `src/lib/shared/context.ts` — do NOT edit this file at all.
    - The idle (unfocused) border color anywhere.
  </action>
  <acceptance_criteria>
    - `grep -c "var(--color-identity)" src/lib/shared/components/NumericInput.svelte` returns at least 2 (focus border + focus ring)
    - `grep -c "focus:border-\[var(--color-accent)\]" src/lib/shared/components/NumericInput.svelte` returns 0
    - `grep -c "focus:ring-\[var(--color-accent-light)\]" src/lib/shared/components/NumericInput.svelte` returns 0
    - `grep -c "border-\[var(--color-border)\]" src/lib/shared/components/NumericInput.svelte` returns at least 1 (idle border preserved)
    - `grep -c "focus-visible:outline-\[var(--color-identity)\]" src/lib/shared/components/SelectPicker.svelte` returns at least 3 (trigger + 2 option buttons)
    - `grep -c "hover:border-\[var(--color-identity)\]" src/lib/shared/components/SelectPicker.svelte` returns at least 1
    - `grep -c "accentColor" src/lib/shared/components/SelectPicker.svelte` is unchanged from baseline (checkmarks still bound to ctx.accentColor — Pitfall 2 guard)
    - `git diff src/lib/shared/context.ts` shows NO changes
    - Existing component tests pass: `pnpm exec vitest run src/lib/shared`
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec vitest run src/lib/shared --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <done>Focus-visible outlines read --color-identity; idle borders, checkmarks, and ctx.accentColor all untouched.</done>
</task>

</tasks>

<verification>
- Full test suite green: `pnpm exec vitest run`
- Typecheck green: `pnpm exec svelte-kit sync && pnpm exec tsc --noEmit`
- IDENT-06 guard (BMF not reused as identity): `grep -rniE "identity.*bmf|bmf.*identity" src/` returns no results
- IDENT-07 guard (shell chrome untouched): `git diff src/lib/shared/components/AboutSheet.svelte src/lib/shared/components/DisclaimerModal.svelte src/lib/shared/context.ts` shows NO changes
- Pitfall 2 guard: `git diff src/lib/shared/components/SelectPicker.svelte` shows NO change on lines bearing `style="color: {accentColor}"` or `ctx.accentColor`
- Pitfall 3 guard: `git diff src/routes/morphine-wean/+page.svelte src/routes/formula/+page.svelte` shows NO change on the Syringe / Milk icon lines (~line 23) — only the wrapper class line 21 changed (in Plan 01)
</verification>

<success_criteria>
- Both result hero cards render with `--color-identity-hero` background (IDENT-02)
- All six eyebrow labels render with `--color-identity` text color (IDENT-04)
- NumericInput focus + SelectPicker trigger/option focus-visible outlines read `--color-identity` (IDENT-03)
- Idle input borders, SelectPicker checkmarks, ctx.accentColor, AboutSheet, DisclaimerModal, route-header icons, and BMF tokens all unchanged
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-tab-identity-token/19-02-SUMMARY.md`
</output>
