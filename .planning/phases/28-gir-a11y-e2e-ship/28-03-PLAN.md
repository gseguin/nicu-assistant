---
phase: 28-gir-a11y-e2e-ship
plan: 03
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - e2e/gir-a11y.spec.ts
  - src/app.css
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - "Axe reports zero violations on /gir in light mode"
    - "Axe reports zero violations on /gir in dark mode"
    - "Axe reports zero violations with focus ring visible"
    - "Axe reports zero violations with Dextrose>12.5 advisory rendered"
    - "Axe reports zero violations with a bucket selected in both light and dark"
  artifacts:
    - path: "e2e/gir-a11y.spec.ts"
      provides: "6 axe-core sweeps for GIR"
      contains: "AxeBuilder"
  key_links:
    - from: "e2e/gir-a11y.spec.ts"
      to: ".identity-gir OKLCH tokens"
      via: "axe color-contrast checks"
      pattern: "wcag2aa"
---

<objective>
Land 6 axe-core a11y sweeps for GIR (TEST-05) matching the exact Morphine/Fortification pattern, and — contingent on axe results — tune `.identity-gir` OKLCH values in `src/app.css` if contrast fails.

Purpose: Pre-empt the Phase 20 Morphine green-hue contrast trap. Green hue 145 trips luminance-based contrast checks faster than blue 220 at the same L%.
Output: `e2e/gir-a11y.spec.ts` with 6 variants passing; optionally tuned OKLCH tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-gir-a11y-e2e-ship/28-RESEARCH.md
@e2e/morphine-wean-a11y.spec.ts
@src/app.css
@src/lib/gir/GirCalculator.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create e2e/gir-a11y.spec.ts with 6 axe sweep variants (TEST-05)</name>
  <files>e2e/gir-a11y.spec.ts</files>
  <read_first>
    - e2e/morphine-wean-a11y.spec.ts (exact fork source)
    - .planning/phases/28-gir-a11y-e2e-ship/28-RESEARCH.md (Pattern 1 — complete copy-paste block)
    - src/lib/gir/GirCalculator.svelte (confirm heading "Glucose Infusion Rate" and label strings)
  </read_first>
  <action>
    Create `e2e/gir-a11y.spec.ts` by pasting the Pattern 1 block from RESEARCH.md verbatim. Verify:
    1. Heading selector `getByRole('heading', { name: 'Glucose Infusion Rate' })` matches the actual `<h1>`/`<h2>` text in GirCalculator.svelte — correct if the component uses different casing/text.
    2. Label strings 'Weight', 'Dextrose', 'Fluid order' match actual component labels — correct if different.
    3. All 6 test blocks present: light, dark, focus-ring light, dextrose advisory light, selected-bucket light, selected-bucket dark.
    4. Each variant uses `.withTags(['wcag2a', 'wcag2aa'])` and `expect(results.violations).toEqual([])`.
    5. Theme-toggle pattern: `no-transition` class + `classList.add('dark')` + `waitForTimeout(250)` — verbatim from research.
    6. Advisory variant injects `page.addStyleTag` to kill transitions post-toggle.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/gir-a11y.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - 6 tests present (one per variant)
    - All 6 green OR Task 2 triggered for OKLCH tuning if Variant 5/6 fails color-contrast
    - grep finds 6 occurrences of `AxeBuilder` in the file
  </acceptance_criteria>
  <done>6 GIR axe sweeps defined and running.</done>
</task>

<task type="auto">
  <name>Task 2: CONTINGENT — Tune .identity-gir OKLCH if axe flags contrast</name>
  <files>src/app.css</files>
  <read_first>
    - src/app.css (lines 199-202 light hero, 213-217 dark hero — `.identity-gir` block)
    - .planning/phases/28-gir-a11y-e2e-ship/28-RESEARCH.md (Pattern 5 — OKLCH tuning ladder + Pitfall 3)
    - Full axe output from Task 1 (Playwright report)
  </read_first>
  <action>
    1. If Task 1 passed all 6 sweeps: this task is a NO-OP — record "no tuning needed" in SUMMARY and mark complete.
    2. If Variant 5 (selected bucket light) OR Variant 6 (selected bucket dark) fails `color-contrast`:
       - First attempt (L bump): change light hero from `oklch(94% 0.045 145)` → `oklch(95% 0.04 145)` (match Morphine Phase 20 exactly). Change dark hero from `oklch(30% 0.09 145)` → `oklch(32% 0.08 145)`.
       - Re-run `pnpm exec playwright test e2e/gir-a11y.spec.ts`.
       - If STILL failing: second attempt (chroma drop): `oklch(96% 0.035 145)` light / keep dark.
       - Re-run.
       - If still failing: report blocker in SUMMARY with axe violation details — do NOT touch `--color-accent*` or shell chrome tokens.
    3. ONLY touch lines 199-202 (light) and 213-217 (dark) inside the `.identity-gir` block. No other CSS changes permitted.
    4. If other variants (1-4) fail: that's a different bug — diagnose separately, report in SUMMARY.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/gir-a11y.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - All 6 GIR a11y sweeps green
    - Only `.identity-gir` block touched in src/app.css (verify with `git diff src/app.css`)
    - SUMMARY records: tuning needed Y/N, final OKLCH values, axe pass/fail progression
  </acceptance_criteria>
  <done>All 6 axe sweeps green; any OKLCH changes scoped to .identity-gir.</done>
</task>

</tasks>

<verification>
- `pnpm exec playwright test e2e/gir-a11y.spec.ts` → 6/6 passing
- `git diff src/app.css` (if touched) shows edits only inside `.identity-gir` block
</verification>

<success_criteria>
TEST-05 satisfied: 6 GIR axe sweeps green in light+dark+focus+advisory+selected variants. OKLCH tuned only if necessary and only within `.identity-gir`.
</success_criteria>

<output>
Create `.planning/phases/28-gir-a11y-e2e-ship/28-03-SUMMARY.md` with: 6 variant pass/fail, OKLCH tuning applied (Y/N + final values), total project sweep count (should be 16 after this plan).
</output>
