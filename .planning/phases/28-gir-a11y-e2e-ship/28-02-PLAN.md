---
phase: 28-gir-a11y-e2e-ship
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - src/lib/gir/GirCalculator.test.ts
  - src/lib/gir/GlucoseTitrationGrid.test.ts
  - e2e/gir.spec.ts
autonomous: true
requirements: [TEST-02, TEST-04, TEST-06]
must_haves:
  truths:
    - "Component tests exercise ArrowUp/Left/Right, Space, Enter keys on titration grid"
    - "Component tests verify bucket-selection propagates to target hero in GirCalculator"
    - "Component tests verify GIR>12 and GIR<4 advisories render"
    - "E2E happy path runs on mobile (375) and desktop (1280) and selects a bucket"
    - "E2E asserts all three NumericInputs have inputmode='decimal'"
  artifacts:
    - path: "e2e/gir.spec.ts"
      provides: "Playwright happy-path + inputmode assertion for GIR"
      contains: "inputmode"
    - path: "src/lib/gir/GirCalculator.test.ts"
      provides: "Extended GirCalculator component coverage"
    - path: "src/lib/gir/GlucoseTitrationGrid.test.ts"
      provides: "Full keyboard nav coverage"
  key_links:
    - from: "e2e/gir.spec.ts"
      to: "/gir route"
      via: "page.goto('/gir')"
      pattern: "goto.*gir"
---

<objective>
Land the component-test coverage gaps (TEST-02), the Playwright happy-path spec with mobile+desktop viewports (TEST-04), and the inputmode regression assertion (TEST-06).

Purpose: Ship-gate testing. Existing GIR component tests only cover ArrowDown/Home/End/click — gaps: ArrowUp, ArrowLeft, ArrowRight, Space, Enter, valid-input flow, bucket-updates-target-hero, GIR>12 and GIR<4 advisories. No E2E exists for GIR.
Output: Extended `*.test.ts` files + new `e2e/gir.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-gir-a11y-e2e-ship/28-RESEARCH.md
@src/lib/gir/GirCalculator.test.ts
@src/lib/gir/GlucoseTitrationGrid.test.ts
@src/lib/gir/GirCalculator.svelte
@src/lib/gir/GlucoseTitrationGrid.svelte
@e2e/morphine-wean.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend GlucoseTitrationGrid.test.ts with ArrowUp/Left/Right + Space/Enter (TEST-02)</name>
  <files>src/lib/gir/GlucoseTitrationGrid.test.ts</files>
  <read_first>
    - src/lib/gir/GlucoseTitrationGrid.test.ts (existing tests + makeRows helper)
    - src/lib/gir/GlucoseTitrationGrid.svelte (handleKeydown — confirms Space/Enter don't move cursor)
    - .planning/phases/28-gir-a11y-e2e-ship/28-RESEARCH.md (Pattern 3, GlucoseTitrationGrid test block)
  </read_first>
  <behavior>
    - ArrowUp from first row wraps to last (bucketId 'gt70' — verify actual id in config)
    - ArrowRight from first advances to next (equivalent to ArrowDown)
    - ArrowLeft on a selected row moves backward (equivalent to ArrowUp)
    - Space on focused radio fires onselect for the SAME row (no move)
    - Enter on focused radio fires onselect for the SAME row (no move)
  </behavior>
  <action>
    Append the 5 test blocks from RESEARCH.md Pattern 3 (GlucoseTitrationGrid section) verbatim. Before committing, open `src/lib/gir/gir-config.json` and confirm the actual bucketIds used for the wrap case (first row id, last row id, row index 2 id) — correct the string literals in the tests if they differ from the research doc's guesses ('gt70', 'lt40', 'severe-neuro', '40-50'). The research doc explicitly notes "verify actual id". Tests use `fireEvent.keyDown` (consistent with existing file).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/gir/GlucoseTitrationGrid.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - 5 new tests present and passing
    - No existing test regressed
    - grep finds 'ArrowUp', 'ArrowLeft', 'ArrowRight', 'Enter', "' '" in test file
  </acceptance_criteria>
  <done>Full keyboard matrix (↑/↓/←/→/Home/End/Space/Enter) covered for GlucoseTitrationGrid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend GirCalculator.test.ts with valid-flow + bucket-update + advisory tests (TEST-02)</name>
  <files>src/lib/gir/GirCalculator.test.ts</files>
  <read_first>
    - src/lib/gir/GirCalculator.test.ts (existing tests + girState import pattern)
    - src/lib/gir/GirCalculator.svelte (confirm label strings "Weight"/"Dextrose"/"Fluid order" and advisory text)
    - src/lib/gir/state.svelte.ts (how girState.current is mutated in tests)
    - .planning/phases/28-gir-a11y-e2e-ship/28-RESEARCH.md (Pattern 3, GirCalculator test block)
  </read_first>
  <behavior>
    - Valid inputs render Current GIR + Initial rate text ("mg/kg/min" present)
    - Clicking a radio row updates state so TARGET GIR text appears
    - Weight=1 / Dex=20 / Fluid=150 → "GIR >12 ... hyperinsulinism" advisory visible
    - Weight=3.1 / Dex=5 / Fluid=40 → "Below basal glucose utilization" advisory visible
  </behavior>
  <action>
    Append the 4 test blocks from RESEARCH.md Pattern 3 (GirCalculator section) verbatim. Reset `girState.current` (or reinitialize) in `beforeEach` so tests don't leak state. Use `fireEvent.click` from `@testing-library/svelte`. If the existing file already has a setup block, reuse it. In Vitest+jsdom the grid renders BOTH mobile+desktop DOMs — `screen.getAllByRole('radio')` returns 12; index `[2]` in the RESEARCH block is fine (40-50 bucket in the first-rendered layout).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/gir/GirCalculator.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - 4 new tests present and passing
    - grep finds 'TARGET GIR' and 'hyperinsulinism' and 'Below basal' in test file
  </acceptance_criteria>
  <done>GirCalculator empty-state + valid-flow + advisory + bucket-propagation covered.</done>
</task>

<task type="auto">
  <name>Task 3: Create e2e/gir.spec.ts with mobile+desktop happy path + inputmode assertion (TEST-04, TEST-06)</name>
  <files>e2e/gir.spec.ts</files>
  <read_first>
    - e2e/morphine-wean.spec.ts (fork reference)
    - src/lib/gir/GirCalculator.svelte (confirm exact label strings for getByLabel)
    - .planning/phases/28-gir-a11y-e2e-ship/28-RESEARCH.md (Pattern 2, Option B block — use verbatim)
    - .planning/phases/28-gir-a11y-e2e-ship/28-01-SUMMARY.md (empirical radio counts from Wave 0)
  </read_first>
  <action>
    Create `e2e/gir.spec.ts` by pasting the Option B block from RESEARCH.md Pattern 2 verbatim. Then:

    1. Update the `toHaveCount` assertion to match the empirical counts recorded in 28-01-SUMMARY.md. Expected: 6 at BOTH mobile and desktop (replace the `viewport.name === 'mobile' ? 6 : 12` placeholder with a flat `6` — the research note explicitly says "In Playwright the count is 6 at either breakpoint").
    2. If Wave 0 observed different numbers, use those. Add a comment citing 28-01-SUMMARY.md.
    3. Keep the three tests: (a) happy-path enter→hero→select→target, (b) empty-state, (c) inputmode="decimal" on all three fields.
    4. Use `page.getByLabel('Weight')`, `page.getByLabel('Dextrose')`, `page.getByLabel('Fluid order')` — verify exact label text matches component (correct if needed).
    5. Dismiss disclaimer via the exact pattern: `await page.getByRole('button', { name: /understand/i }).click({ timeout: 2000 }).catch(() => {});`
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/gir.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - File exists with 3 tests × 2 viewports = 6 test cases
    - All pass against dev/preview server
    - grep finds `inputmode` and `decimal` in the spec file
    - grep finds `'3.1'`, `'12.5'`, `'80'` (the canonical input trio)
  </acceptance_criteria>
  <done>Happy path + inputmode regression spec green on mobile 375 and desktop 1280.</done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run src/lib/gir/` all green
- `pnpm exec playwright test e2e/gir.spec.ts` all green
- Grep confirms new keys/strings present
</verification>

<success_criteria>
TEST-02, TEST-04, TEST-06 all satisfied. Component and E2E specs exercise the full keyboard matrix, happy path, and inputmode contract.
</success_criteria>

<output>
Create `.planning/phases/28-gir-a11y-e2e-ship/28-02-SUMMARY.md` listing new test names, pass counts, and actual label strings used.
</output>
