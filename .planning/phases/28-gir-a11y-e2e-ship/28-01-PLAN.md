---
phase: 28-gir-a11y-e2e-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shared/types.ts
  - src/lib/shell/NavShell.svelte
  - playwright.config.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "CalculatorId type includes 'gir'"
    - "AboutSheet on /gir route shows GIR-specific content (via activeCalculatorId)"
    - "Playwright chromium browser is installed and E2E runnable"
  artifacts:
    - path: "src/lib/shared/types.ts"
      contains: "'gir'"
    - path: "src/lib/shell/NavShell.svelte"
      contains: "startsWith('/gir')"
  key_links:
    - from: "NavShell.svelte"
      to: "AboutSheet"
      via: "activeCalculatorId prop"
      pattern: "activeCalculatorId"
---

<objective>
Wave 0 blockers: fix two latent bugs that must land BEFORE DOC-01 can compile, and verify the Playwright environment so downstream E2E/a11y waves don't stall.

Purpose: `CalculatorId` missing `'gir'` will TS-error the moment AboutSheet gains a GIR entry; NavShell currently renders Morphine content on `/gir`. Both are blocking prerequisites for Plan 04 (DOC-01).
Output: Type extension, NavShell branch fix, Playwright env verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-gir-a11y-e2e-ship/28-RESEARCH.md
@src/lib/shared/types.ts
@src/lib/shell/NavShell.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CalculatorId type to include 'gir'</name>
  <files>src/lib/shared/types.ts</files>
  <read_first>
    - src/lib/shared/types.ts (line 7 — current union)
    - .planning/phases/28-gir-a11y-e2e-ship/28-RESEARCH.md (Pattern 4, "extend CalculatorId" diff)
  </read_first>
  <action>
    Change line 7 from:
      `export type CalculatorId = 'morphine-wean' | 'formula';`
    to:
      `export type CalculatorId = 'morphine-wean' | 'formula' | 'gir';`
    No other edits in this file.
  </action>
  <verify>
    <automated>grep -q "'morphine-wean' | 'formula' | 'gir'" src/lib/shared/types.ts && pnpm exec svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep finds the updated union in src/lib/shared/types.ts
    - svelte-check reports zero new TS errors introduced by this change
  </acceptance_criteria>
  <done>CalculatorId union contains 'gir'; type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add /gir branch to NavShell activeCalculatorId</name>
  <files>src/lib/shell/NavShell.svelte</files>
  <read_first>
    - src/lib/shell/NavShell.svelte (lines 11-13)
    - .planning/phases/28-gir-a11y-e2e-ship/28-RESEARCH.md (Pattern 4, NavShell diff + Pitfall 4)
  </read_first>
  <action>
    Replace lines 11-13 with:
    ```
      const activeCalculatorId = $derived<CalculatorId>(
        page.url.pathname.startsWith('/formula') ? 'formula'
        : page.url.pathname.startsWith('/gir') ? 'gir'
        : 'morphine-wean'
      );
    ```
    No other edits in this file.
  </action>
  <verify>
    <automated>grep -q "startsWith('/gir')" src/lib/shell/NavShell.svelte && pnpm exec svelte-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep finds the /gir branch
    - svelte-check passes
  </acceptance_criteria>
  <done>AboutSheet on /gir will receive calculatorId='gir' (content entry lands in Plan 04).</done>
</task>

<task type="auto">
  <name>Task 3: Verify Playwright chromium install + confirm real-viewport radio count</name>
  <files>playwright.config.ts (read-only unless install missing)</files>
  <read_first>
    - playwright.config.ts
    - .planning/phases/28-gir-a11y-e2e-ship/28-RESEARCH.md (Pattern 2 + "Note on radio count" + Pitfall 1)
  </read_first>
  <action>
    1. Run `pnpm exec playwright install --with-deps chromium` to ensure chromium binary is present (idempotent).
    2. Start dev server (background) via `pnpm run dev &` OR use existing build: `pnpm run build && pnpm run preview &`.
    3. Run a tiny inline Playwright assertion to confirm the radio count at mobile 375x667 and desktop 1280x800 on /gir. Create TEMPORARY file `e2e/_gir-wave0-probe.spec.ts` with a single test per viewport that navigates to /gir, fills Weight=3.1/Dex=12.5/Fluid=80, and logs `console.log('radio count:', await page.getByRole('radio').count())`. Run `pnpm exec playwright test _gir-wave0-probe --reporter=line`.
    4. Record the observed counts in the task SUMMARY (expected: 6 at both). If count differs from 6, FLAG as blocker and stop — Plan 02 needs correct count.
    5. DELETE `e2e/_gir-wave0-probe.spec.ts` after recording results (leave repo clean).
  </action>
  <verify>
    <automated>pnpm exec playwright --version && test ! -f e2e/_gir-wave0-probe.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - playwright --version prints 1.58+ or newer
    - probe file deleted after use
    - SUMMARY records empirical radio counts for mobile + desktop
  </acceptance_criteria>
  <done>Environment ready for Plan 02 (E2E) and Plan 03 (a11y); radio-count expectation confirmed empirically.</done>
</task>

</tasks>

<verification>
- `grep -c "'gir'" src/lib/shared/types.ts` ≥ 1
- `grep -c "startsWith('/gir')" src/lib/shell/NavShell.svelte` ≥ 1
- `pnpm exec svelte-check` passes
- `pnpm exec playwright --version` succeeds
</verification>

<success_criteria>
CalculatorId supports 'gir', NavShell routes AboutSheet correctly on /gir, Playwright chromium is installed, and empirical radio count at both viewports is recorded (expected 6).
</success_criteria>

<output>
Create `.planning/phases/28-gir-a11y-e2e-ship/28-01-SUMMARY.md` recording: diffs applied, svelte-check result, empirical radio counts for mobile (375) and desktop (1280).
</output>
