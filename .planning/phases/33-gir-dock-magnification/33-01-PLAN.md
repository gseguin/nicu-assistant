---
phase: 33-gir-dock-magnification
plan: 01
type: execute
wave: 1
depends_on: [32-01]
files_modified:
  - src/lib/gir/GlucoseTitrationGrid.svelte
autonomous: true
requirements: [GIR-DOCK-01, GIR-DOCK-02, GIR-DOCK-03, GIR-DOCK-04]

must_haves:
  truths:
    - "On mobile (<768px) without reduced-motion, GIR bucket cards scale with scroll progress (max 1.06) with 3 cards visibly magnified at once."
    - "On desktop (>=768px) or with prefers-reduced-motion, GIR bucket cards render flat — no transform, no boxShadow, no zIndex."
    - "When the row set changes (e.g. severe-neuro gating flip), magnification re-applies without a manual scroll."
    - "Click/tap selection visual (border-l-4 + identity-hero fill) remains intact on the magnified card."
    - "Playwright axe sweeps remain 16/16 in both light and dark themes."
  artifacts:
    - path: "src/lib/gir/GlucoseTitrationGrid.svelte"
      provides: "Dock magnification on mobile bucket cards, mirroring MorphineWeanCalculator pattern"
      contains: "data-bucket-index"
  key_links:
    - from: "GlucoseTitrationGrid mobile bucket card wrapper"
      to: "updateMagnification scroll handler"
      via: "data-bucket-index attribute + gridContainer querySelectorAll"
      pattern: "data-bucket-index"
    - from: "onMount cleanup"
      to: "window scroll listener + rAF + MutationObserver"
      via: "return () => { removeEventListener / cancelAnimationFrame / disconnect }"
      pattern: "removeEventListener\\(.scroll."
---

<objective>
Port the morphine-wean dock magnification pattern to `GlucoseTitrationGrid.svelte` so that, after Phase 32 thins the bucket cards, the mobile GIR bucket list scales with scroll progress — drawing the eye to the currently-focused bucket like the macOS Dock.

Purpose: After Phase 32 removes the per-card secondary metrics row, the bucket cards become visually uniform and scannable but lose a focal cue. Dock magnification restores a focal cue without adding content, mirroring the proven morphine-wean interaction.

Output: Updated `GlucoseTitrationGrid.svelte` with a mobile-only, reduced-motion-safe scroll-driven magnification that reuses the morphine pattern verbatim (inline clone, not extracted — premature abstraction with only 2 call sites).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-gir-simplification/32-01-PLAN.md
@src/lib/morphine/MorphineWeanCalculator.svelte
@src/lib/gir/GlucoseTitrationGrid.svelte
@e2e/gir-a11y.spec.ts

<interfaces>
<!-- Canonical morphine magnification block (lines 17-100 of MorphineWeanCalculator.svelte) -->
<!-- The executor should clone this block into GlucoseTitrationGrid's onMount, renaming: -->
<!--   scheduleContainer → gridContainer -->
<!--   [data-step-index] → [data-bucket-index] -->
<!--   activeStepIndex tracking → DROP (not needed; GIR uses selectedBucketId for selection, not a floating "active" concept) -->

Key constants to preserve exactly:
  const MAX_SCALE = 1.06;
  const MAX_SHADOW_BOOST = 4;
  // radius 2.5 in: const t = Math.max(0, 1 - dist / 2.5);
  // scroll-driven floating index: scrollProgress = scrollY / scrollMax; floatingIdx = scrollProgress * (n - 1);

Guards to preserve exactly:
  const prefersReducedMotion = typeof window.matchMedia === 'function'
    ? window.matchMedia('(prefers-reduced-motion: reduce)').matches
    : false;
  const isMobile = () => typeof window !== 'undefined' && window.innerWidth < 768;
  // When !isMobile() || prefersReducedMotion → clear transform/boxShadow/zIndex and return.

Cleanup block (must copy verbatim):
  return () => {
    window.removeEventListener('scroll', onScroll);
    if (rafId !== null) cancelAnimationFrame(rafId);
    mutObserver.disconnect();
  };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-bucket-index + gridContainer ref to mobile branch</name>
  <files>src/lib/gir/GlucoseTitrationGrid.svelte</files>
  <action>
In `GlucoseTitrationGrid.svelte`:

1. Add a `let gridContainer: HTMLElement | undefined = $state();` declaration near the existing `rowRefs` state.
2. Bind the MOBILE branch root `<div class="flex flex-col gap-3 sm:hidden" ...>` with `bind:this={gridContainer}`. Do NOT add it to the desktop branch — magnification is mobile-only (GIR-DOCK-02).
3. On each mobile bucket card wrapper (the `<div bind:this={rowRefs[i]} role="radio" ...>` inside the mobile `{#each}`), add `data-bucket-index={i}`. This mirrors morphine's `data-step-index` pattern and decouples the magnification query from class names.
4. Add a `transition-transform` utility alongside the existing `transition-colors` on the mobile card wrapper so the scale change animates smoothly. Keep `transition-colors` intact — the selection color change must still animate.
5. Do NOT touch the desktop branch. Do NOT touch any other component.

Per GIR-DOCK-01/02 and the locked planning decision: INLINE the morphine pattern in Task 2 rather than extracting to `$lib/shared`. Leave a comment marker in this task's diff area only if natural; the main "cloned from morphine" comment lands in Task 2.
  </action>
  <verify>
    <automated>pnpm svelte-check 2>&1 | tail -20</automated>
  </verify>
  <done>
- `gridContainer` state declared and bound to mobile `<div class="flex flex-col gap-3 sm:hidden">` only.
- Every mobile bucket card wrapper has `data-bucket-index={i}`.
- Mobile card wrapper classes include both `transition-colors` and `transition-transform`.
- Desktop branch untouched.
- `svelte-check` reports 0/0 errors.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Port morphine dock magnification logic (inline, mobile-only, reduced-motion safe)</name>
  <files>src/lib/gir/GlucoseTitrationGrid.svelte</files>
  <behavior>
    - On mobile (<768px) without reduced-motion: cards at the floating scroll index receive scale up to 1.06; cards further than radius 2.5 from the index receive scale 1.
    - On desktop or prefers-reduced-motion: querySelectorAll finds the cards but clears transform/boxShadow/zIndex and returns.
    - scroll handler is rAF-throttled (no double-scheduling while a frame is pending).
    - MutationObserver on gridContainer re-runs updateMagnification when rows add/remove (severe-neuro gating change, etc.).
    - onMount teardown removes scroll listener, cancels pending rAF, disconnects MutationObserver.
  </behavior>
  <action>
In `GlucoseTitrationGrid.svelte`:

1. Add `import { onMount } from 'svelte';` at the top of the `<script lang="ts">` block (alongside the existing imports).

2. Immediately after the existing `let rowRefs` / `gridContainer` declarations, add the onMount block. CLONE the morphine pattern from `src/lib/morphine/MorphineWeanCalculator.svelte` lines 21–100 verbatim, with these targeted edits:

   - Rename `scheduleContainer` → `gridContainer` throughout.
   - Rename the querySelector target from `'[data-step-index]'` → `'[data-bucket-index]'`.
   - Delete all `activeStepIndex` handling (lines ~73–76 of morphine). GIR has no equivalent — selection is user-driven via `selectedBucketId` from the parent, not scroll-driven. Specifically drop:
       `const bestIdx = Math.round(floatingIdx);`
       `if (bestIdx !== activeStepIndex) { activeStepIndex = bestIdx; }`
   - Keep `MAX_SCALE = 1.06`, `MAX_SHADOW_BOOST = 4`, radius `2.5`, shadow formula, `zIndex = t > 0.3 ? '1' : ''` EXACTLY as in morphine.
   - Keep the `prefersReducedMotion` and `isMobile()` guards exactly. Keep the "clear styles and return" branch so that resizing from mobile to desktop wipes any leftover transforms.
   - Keep the `rafId` throttle pattern exactly.
   - Keep MutationObserver with `{ childList: true, subtree: true }` on `gridContainer`.
   - Keep the cleanup `return () => { ... }` block exactly (listener removal + cancelAnimationFrame + disconnect).

3. Add a header comment block above the onMount call:

```ts
// Dock-style magnification: scroll-driven scaling of mobile bucket cards.
// Cloned verbatim from MorphineWeanCalculator.svelte (Phase 5) — extraction to
// $lib/shared was considered and rejected in Phase 33 planning: only 2 call sites,
// both small, extraction would be premature abstraction. If a 3rd call site lands,
// extract then. See .planning/phases/33-gir-dock-magnification/33-01-PLAN.md.
// Guards: mobile-only (window.innerWidth < 768) AND !prefers-reduced-motion.
```

4. Do NOT touch the desktop branch. Do NOT add resize listeners (morphine doesn't have one; consistent with the pattern).

5. The magnification sets inline `style.transform`, `style.boxShadow`, `style.zIndex`. Verify this does NOT conflict with the click/tap selection visual — selection uses Tailwind classes (`border-l-4` + `bg-[var(--color-identity-hero)]`) which apply independently of inline transform.
  </action>
  <verify>
    <automated>pnpm svelte-check 2>&1 | tail -20 && pnpm vitest run 2>&1 | tail -30</automated>
  </verify>
  <done>
- `onMount` imported; magnification block cloned from morphine with the `activeStepIndex` handling removed and `scheduleContainer`/`data-step-index` renamed.
- Header comment explains the clone-vs-extract decision and references this plan.
- `MAX_SCALE = 1.06`, `MAX_SHADOW_BOOST = 4`, radius `2.5` preserved exactly.
- Guards: `prefersReducedMotion` + `isMobile()` both present; desktop/reduced-motion branch clears styles and returns.
- rAF throttle + MutationObserver + cleanup teardown all present and match morphine byte-for-byte (minus the rename/drop edits above).
- `svelte-check` clean; all vitest suites green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mobile visual sanity check + axe 16/16 re-verify</name>
  <what-built>
Dock magnification on `GlucoseTitrationGrid.svelte` mobile branch: scroll-driven floating index, MAX_SCALE 1.06, 3 cards visibly magnified at once, MutationObserver refresh on row changes, mobile+reduced-motion guards, full cleanup on unmount. Click/tap selection preserved.
  </what-built>
  <how-to-verify>
Automated (Claude runs these before handing off):

1. `pnpm svelte-check` → 0 errors / 0 warnings.
2. `pnpm vitest run` → all suites green.
3. `pnpm exec playwright test e2e/gir-a11y.spec.ts` → 16/16 green in both themes (GIR-DOCK-04).
4. `pnpm exec playwright test` (full e2e) → all green; no regression from inline style injection.
5. `pnpm build` → clean.

Human verification (user runs these on a real mobile viewport):

6. Open the GIR tab in mobile viewport (dev tools, <768px). Scroll the bucket list. Expect: the card nearest the scroll-derived index scales to ~1.06 with a soft shadow; two neighbors scale slightly; far cards are flat. No jank (rAF-smooth).
7. Tap a bucket card. Expect: `border-l-4` + identity-hero fill appear AND the card keeps its current scale/shadow from magnification (transform and selection class coexist).
8. Resize viewport to >=768px. Expect: all cards immediately flat — no leftover transform/shadow/zIndex. Resize back: magnification resumes on next scroll tick.
9. Toggle OS "Reduce Motion" ON, reload. Expect: mobile cards flat, no magnification, no jank.
10. Trigger severe-neuro gating (input a value that flips the row set). Expect: MutationObserver refreshes magnification without requiring a scroll.

Report any regression — especially any axe-core hit introduced by the inline styles (shouldn't happen; transform/boxShadow/zIndex are not a11y-impacting, but GIR-DOCK-04 requires re-verification).
  </how-to-verify>
  <resume-signal>Type "approved" after 16/16 axe green + manual mobile sanity check passes, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm svelte-check`: 0/0
- `pnpm vitest run`: all green
- `pnpm exec playwright test e2e/gir-a11y.spec.ts`: 16/16 in both themes
- `pnpm exec playwright test`: all green
- `pnpm build`: clean
- Mobile sanity: magnification visible and smooth; desktop flat; reduced-motion flat; MutationObserver refresh works; click/tap selection visual intact.
</verification>

<success_criteria>
1. `GlucoseTitrationGrid.svelte` implements dock magnification using the morphine pattern: floating index from `scrollY/scrollMax`, `MAX_SCALE = 1.06`, `MAX_SHADOW_BOOST = 4`, radius 2.5, rAF-throttled handler, onMount cleanup. [GIR-DOCK-01]
2. Magnification is guarded by `window.innerWidth < 768` AND `!prefers-reduced-motion`; desktop and reduced-motion users see flat cards. [GIR-DOCK-02]
3. `MutationObserver({ childList: true, subtree: true })` on `gridContainer` re-runs magnification when rows change. [GIR-DOCK-03]
4. Playwright axe: 16/16 green in light and dark themes post-change. [GIR-DOCK-04]
5. Desktop branch untouched; click/tap visual (border-l-4 + identity-hero fill) still works alongside the transform.
6. Inline clone decision documented in a header comment above onMount, referencing this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/33-gir-dock-magnification/33-01-SUMMARY.md` documenting:
- The clone-vs-extract decision and rationale
- Any deviations from morphine's pattern (notably: `activeStepIndex` dropped)
- Axe 16/16 confirmation
- Any deferred items
</output>
