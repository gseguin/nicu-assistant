---
phase: "57"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shell/calculator-store.svelte.ts
  - src/lib/shell/calculator-store.test.ts
  - src/lib/gir/GirInputs.svelte
  - src/lib/morphine/MorphineWeanInputs.svelte
  - src/lib/fortification/FortificationInputs.svelte
  - src/lib/feeds/FeedAdvanceInputs.svelte
  - src/lib/uac-uvc/UacUvcInputs.svelte
autonomous: true
requirements:
  - AUTO-01
  - AUTO-02

must_haves:
  truths:
    - "CalculatorStore owns auto-persist via $effect.root() in the constructor — zero *Inputs.svelte files contain a persist effect"
    - "Editing any calculator input still persists across reload, including when the inputs fragment is mounted alone in the mobile InputDrawer (no component needed to activate persistence)"
    - "The lastEdited 60s stamp-debounce holds under rapid sequential mutations — Svelte 5 effect re-entry does not occur"
    - "calculator-store.test.ts stays fully green and has at least 3 new tests pinning auto-persist, drawer-only-mount, and 60s debounce"
    - "Full vitest suite (448+ tests) stays green, including FortificationInputs.test.ts and MorphineWeanInputs.test.ts"
  artifacts:
    - path: "src/lib/shell/calculator-store.svelte.ts"
      provides: "CalculatorStore class with $effect.root auto-persist in constructor"
      contains: "JSON.stringify(this.current)"
    - path: "src/lib/shell/calculator-store.test.ts"
      provides: "Regression contract + 3 new auto-persist tests"
      contains: "auto-persist (D-05)"
  key_links:
    - from: "src/lib/shell/calculator-store.svelte.ts constructor"
      to: "this.persist()"
      via: "$effect.root(() => $effect(...))"
      pattern: "effect\\.root"
    - from: "module-scope new CalculatorStore<T>(...) in state.svelte.ts"
      to: "auto-persist root effect"
      via: "constructor runs at import time — before any component mounts"
      pattern: "effect_root"
---

<objective>
Fold the copy-pasted auto-persist `$effect` from 5 `*Inputs.svelte` components into one canonical location: the `CalculatorStore<T>` constructor. This is a zero-new-API, behavior-preserving refactor — identical bytes are written to identical storage keys. The only observable change is that the mechanism now lives in the class rather than in each component, meaning it works correctly even when an inputs fragment is mounted alone in the mobile InputDrawer (no component effect needed; the singleton's root effect fires at import time).

Purpose: Eliminate 5 identical copies of `$effect(() => { JSON.stringify(state.current); state.persist(); })`. Persistence becomes a class-level invariant instead of a per-component responsibility. The drawer-only-mount persistence path (AUTO-02) is preserved automatically by D-03: the state singleton is a module-scope export; its constructor (and thus the root effect) runs on first `import`, before any component mounts.

Output: One updated class file (+7 lines), one updated test file (+35 lines), five shorter component files (−6 lines each). No new files, no new packages, no API changes.
</objective>

<execution_context>
@/home/ghislain/.claude/get-shit-done/workflows/execute-plan.md
@/home/ghislain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-auto-persist-behind-calculatorstore/57-CONTEXT.md
@.planning/phases/57-auto-persist-behind-calculatorstore/57-RESEARCH.md
@.planning/phases/57-auto-persist-behind-calculatorstore/57-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add $effect.root auto-persist to CalculatorStore constructor + new tests</name>
  <files>src/lib/shell/calculator-store.svelte.ts, src/lib/shell/calculator-store.test.ts</files>

  <read_first>
    - src/lib/shell/calculator-store.svelte.ts — read in full; the exact constructor (lines 28–39) and SSR guard forms (lines 61, 76) are the patterns to follow
    - src/lib/shell/calculator-store.test.ts — read in full; reuse type Shape, makeDefaults, KEY, and the afterEach/beforeEach structure for new tests
    - .planning/phases/57-auto-persist-behind-calculatorstore/57-CONTEXT.md — D-01, D-02, D-07, D-08 (exact mechanism, ordering, cleanup, SSR guard)
    - .planning/phases/57-auto-persist-behind-calculatorstore/57-RESEARCH.md — §"Exact constructor change (D-01 + D-08)", §"New tests for D-05", Pitfall 1 (await tick() mandatory), Pitfall 2 (spy after construction)
    - .planning/phases/57-auto-persist-behind-calculatorstore/57-PATTERNS.md — §"calculator-store.svelte.ts (service, event-driven addition)", §"Test Pattern"
  </read_first>

  <behavior>
    - Test (a): Constructing a CalculatorStore, then mutating .current.a, then await tick() → setItem called with KEY and JSON containing a=99
    - Test (b): Same flow with no render() call anywhere in the test → proves the class-level effect fires without any component mounted (drawer-only-mount proof)
    - Test (c): Two rapid mutations separated only by await tick() → lastEdited.current is the same timestamp after both (60s debounce suppresses the second stamp)
  </behavior>

  <action>
    STEP 1 — Write the new tests first (RED phase):

    Add `import { tick } from 'svelte';` at the top of calculator-store.test.ts (after the existing vitest import line).

    Append a new describe block at the end of calculator-store.test.ts, after the last existing describe. The block is titled `'CalculatorStore — auto-persist (D-05)'` and contains three async tests reusing the existing `Shape`, `makeDefaults`, and `KEY` constants. Per the PATTERNS.md §"Test Pattern":

    Test (a) "mutating .current triggers auto-persist without calling persist() directly":
    - Construct store with storageKey: KEY, defaults: makeDefaults
    - Install spy via `vi.spyOn(Storage.prototype, 'setItem')` AFTER construction (excludes initial benign write — D-02, Pitfall 2)
    - Set store.current.a = 99
    - await tick()
    - Assert spy called with (KEY, JSON.stringify({ a: 99, b: 'x' }))

    Test (b) "auto-persist fires without any component mounted — proves drawer-only-mount path":
    - Construct store (no render/mount call anywhere)
    - Install spy AFTER construction
    - Set store.current.b = 'drawer'
    - await tick()
    - Check spy.mock.calls.at(-1): index 0 is KEY, JSON.parse(index 1).b === 'drawer'

    Test (c) "60s debounce: rapid mutations do not re-stamp lastEdited within the window":
    - Construct store, await tick() (lets initial effect fire)
    - Set store.current.a = 10, await tick()
    - Capture stamp1 = store.lastEdited.current; assert typeof stamp1 === 'number'
    - Set store.current.a = 11, await tick()
    - Capture stamp2 = store.lastEdited.current
    - Assert stamp2 === stamp1 (debounce suppressed re-stamp)

    Run `pnpm vitest run src/lib/shell/calculator-store.test.ts` — tests (a)(b)(c) MUST be RED (auto-persist not yet installed). If they are green before the class change, stop and report.

    STEP 2 — Add auto-persist to the constructor (GREEN phase):

    In src/lib/shell/calculator-store.svelte.ts, insert the following block immediately after `this.init();` at line 38, before the closing `}` of the constructor.

    Use the wrapping-guard form (matching persist() and reset() on lines 61 and 76), NOT the early-return form used by init(). The block:

    `// Auto-persist: fires on every mutation of this.current.`
    `// $effect.root() required because CalculatorStore is a class, not a component.`
    `// SSR guard: skip on server (no localStorage, would create a phantom root effect).`
    `if (typeof localStorage !== 'undefined') {`
    `  $effect.root(() => {`
    `    $effect(() => {`
    `      JSON.stringify(this.current);`
    `      this.persist();`
    `    });`
    `  });`
    `}`

    Critical rules from CONTEXT.md D-07 and D-08:
    - Do NOT assign or store the return value of $effect.root() — discard it (singleton lives app lifetime).
    - Do NOT add a try/catch around $effect.root() — persist() already wraps setItem in try/catch.
    - The guard form is `if (typeof localStorage !== 'undefined')` wrapping the block, NOT `if (typeof localStorage === 'undefined') return` (that is init()'s early-return form — do not mix forms).

    STEP 3 — Run GREEN verification:

    `pnpm vitest run src/lib/shell/calculator-store.test.ts` — all 13 tests (10 existing + 3 new) must pass.
  </action>

  <verify>
    <automated>pnpm vitest run src/lib/shell/calculator-store.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm vitest run src/lib/shell/calculator-store.test.ts` exits 0 with 13 tests passing (10 existing + 3 new)
    - `grep -c 'JSON.stringify(this.current)' src/lib/shell/calculator-store.svelte.ts` outputs 1
    - `grep -c 'effect\.root' src/lib/shell/calculator-store.svelte.ts` outputs 1
    - `grep -c 'await tick()' src/lib/shell/calculator-store.test.ts` outputs at least 4 (one per new test, at minimum)
    - `grep -c 'auto-persist (D-05)' src/lib/shell/calculator-store.test.ts` outputs 1 (the new describe block exists)
    - The constructor in calculator-store.svelte.ts still contains `this.init();` and the new block appears after it (not before)
  </acceptance_criteria>

  <done>CalculatorStore constructor installs `$effect.root(() => $effect(() => { JSON.stringify(this.current); this.persist(); }))` wrapped in `typeof localStorage !== 'undefined'` guard, after `this.init()`. Three new async tests in calculator-store.test.ts pass (auto-persist fires on mutation, fires without component mounted, 60s debounce suppresses re-stamp). All 13 tests in the file green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete 5 per-fragment persist effects + grep gate + full suite green</name>
  <files>src/lib/gir/GirInputs.svelte, src/lib/morphine/MorphineWeanInputs.svelte, src/lib/fortification/FortificationInputs.svelte, src/lib/feeds/FeedAdvanceInputs.svelte, src/lib/uac-uvc/UacUvcInputs.svelte</files>

  <read_first>
    - .planning/phases/57-auto-persist-behind-calculatorstore/57-PATTERNS.md — §"GirInputs.svelte (lines 22–27)", §"MorphineWeanInputs.svelte (lines 19–24)", §"FortificationInputs.svelte (lines 132–137)", §"FeedAdvanceInputs.svelte (lines 33–38)", §"UacUvcInputs.svelte (lines 26–31)", §"Grep Verification Gates"
    - .planning/phases/57-auto-persist-behind-calculatorstore/57-CONTEXT.md — D-04 (scope: delete only the comment+effect block, nothing else in each file changes), D-06 (do NOT modify existing tests)
    - .planning/phases/57-auto-persist-behind-calculatorstore/57-RESEARCH.md — Pitfall 4 (FortificationInputs.test.ts + MorphineWeanInputs.test.ts mock persist: vi.fn() — becomes unused stub, no test asserts on it, zero breakage)
    - src/lib/gir/GirInputs.svelte — one example of the deletion target; confirm line 15 import stays, lines 22–27 deleted
  </read_first>

  <action>
    For each of the five files, apply EXACTLY the deletion described in PATTERNS.md. No other lines change in any file — the singleton import line stays, all other $effect/$derived blocks stay untouched.

    GirInputs.svelte — delete lines 22–27:
    The block starting with the comment `// Persist on every change — duplicates the calculator's effect so the inputs` and ending with the closing `});` of the `$effect(() => { JSON.stringify(girState.current); girState.persist(); })` block. Line 15 `import { girState } from '$lib/gir/state.svelte.js'` STAYS.

    MorphineWeanInputs.svelte — delete lines 19–24:
    Same shape: comment line + `$effect(() => { JSON.stringify(morphineState.current); morphineState.persist(); });`. Line 13 import stays.

    FortificationInputs.svelte — delete lines 132–137:
    CAUTION: this file has many other $effect blocks (lines 34–66, 69–75, 112–130) that are NOT the deletion target. Delete ONLY the comment+effect block at lines 132–137 — the last item before `</script>`. All string-bridge mirror effects and auto-reset effects are untouched. Line 14 import stays.

    FeedAdvanceInputs.svelte — delete lines 33–38:
    Comment + `$effect(() => { JSON.stringify(feedsState.current); feedsState.persist(); });`. Line 20 import stays.

    UacUvcInputs.svelte — delete lines 26–31:
    Comment + `$effect(() => { JSON.stringify(uacUvcState.current); uacUvcState.persist(); });`. Line 20 import stays.

    After all five deletions, do NOT modify any test files (D-06).
  </action>

  <verify>
    <automated>grep -rn 'JSON.stringify.*State\.current' src/lib/gir/GirInputs.svelte src/lib/morphine/MorphineWeanInputs.svelte src/lib/fortification/FortificationInputs.svelte src/lib/feeds/FeedAdvanceInputs.svelte src/lib/uac-uvc/UacUvcInputs.svelte && echo "FAIL: matches found" || echo "PASS: zero matches"; pnpm vitest run</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn 'JSON.stringify.*State\.current' src/lib/gir/GirInputs.svelte src/lib/morphine/MorphineWeanInputs.svelte src/lib/fortification/FortificationInputs.svelte src/lib/feeds/FeedAdvanceInputs.svelte src/lib/uac-uvc/UacUvcInputs.svelte` returns ZERO matches (ROADMAP SC-1)
    - `grep -c 'JSON.stringify(this.current)' src/lib/shell/calculator-store.svelte.ts` returns 1 (the single new in-class effect — ROADMAP SC-1 complement)
    - `grep -rn 'persist' src/lib/gir/GirInputs.svelte src/lib/morphine/MorphineWeanInputs.svelte src/lib/fortification/FortificationInputs.svelte src/lib/feeds/FeedAdvanceInputs.svelte src/lib/uac-uvc/UacUvcInputs.svelte` returns ZERO matches (no residual persist calls in any *Inputs.svelte)
    - `pnpm vitest run` exits 0 with count >= 451 (448 existing + 3 new from Task 1; actual minimum is 451)
    - `pnpm vitest run src/lib/fortification/FortificationInputs.test.ts src/lib/morphine/MorphineWeanInputs.test.ts` exits 0 with no test edits (Pitfall 4: mock's persist:vi.fn() is now an unused stub — no assertions on it, tests stay green)
    - `pnpm svelte-check` exits 0 with 0 errors and 0 warnings
    - `pnpm build` exits 0 (adapter-static build succeeds)
  </acceptance_criteria>

  <done>All 5 per-fragment `$effect(() => { JSON.stringify(...State.current); ...State.persist(); })` blocks and their preceding "Persist on every change — duplicates…" comments are deleted. Zero `JSON.stringify.*State.current` matches in any *Inputs.svelte. Full vitest suite green at count >= 451. svelte-check 0/0. pnpm build OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CalculatorStore.persist() → localStorage | Existing boundary, unchanged. Writes the serialized state under the existing key. No new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | $effect.root auto-persist call | accept | Read-only effect: reads this.current, calls existing persist() which is already guarded + try/catched. No new write surface; identical bytes as before. |
| T-57-02 | Denial of Service | $effect.root infinite re-entry | accept | Re-entry is impossible by construction (proven in RESEARCH Q3): the effect reads only this.current, NOT this.lastEdited.current; writing lastEdited.current inside stamp() does not retrigger the subscribed effect. 60s STAMP_DEBOUNCE_MS is defense-in-depth. |
| T-57-03 | Information Disclosure | localStorage contents | accept | No change to keys, JSON shapes, or what is persisted. Identical to pre-Phase-57. Clinical data was already in localStorage; this phase does not expand the surface. |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | Phase 57 installs zero new packages. No supply-chain surface. |

**Assessment:** This is a net-positive consolidation. Five independent component-level persist calls are replaced by one class-level call. Storage logic is now reviewable in one location. No HIGH-severity threats; all dispositions are accept with rationale.
</threat_model>

<verification>
Full phase green when all of the following pass in sequence:

1. `grep -rn 'JSON.stringify.*State\.current' src/lib/gir/GirInputs.svelte src/lib/morphine/MorphineWeanInputs.svelte src/lib/fortification/FortificationInputs.svelte src/lib/feeds/FeedAdvanceInputs.svelte src/lib/uac-uvc/UacUvcInputs.svelte` → ZERO matches (ROADMAP SC-1)
2. `grep -c 'JSON.stringify(this.current)' src/lib/shell/calculator-store.svelte.ts` → exactly 1
3. `grep -c 'effect\.root' src/lib/shell/calculator-store.svelte.ts` → exactly 1
4. `pnpm vitest run src/lib/shell/calculator-store.test.ts` → exits 0, 13 tests (ROADMAP SC-3 — new tests pin drawer-only-mount + 60s debounce)
5. `pnpm vitest run` → exits 0, count >= 451 (ROADMAP SC-3 — full suite green including FortificationInputs + MorphineWeanInputs component tests)
6. `pnpm svelte-check` → 0 errors, 0 warnings (clinical gate)
7. `pnpm build` → exits 0 (adapter-static build OK)
</verification>

<success_criteria>
Phase 57 is complete when:

1. **AUTO-01 (ROADMAP SC-1):** `grep -rn 'JSON.stringify.*State\.current' src/lib/**/*Inputs.svelte` returns ZERO matches. `CalculatorStore` owns auto-persist via one `$effect.root()` in the constructor.

2. **AUTO-02 (ROADMAP SC-2 + SC-3):** The 3 new tests in calculator-store.test.ts pass, including test (b) which proves auto-persist fires without any component mounted (the drawer-only-mount proof). The 60s debounce test (c) confirms no re-entry. All 451+ tests green.

3. **Behavior-preserving gate:** svelte-check 0/0, vitest fully green, pnpm build OK. Zero user-visible change.
</success_criteria>

<output>
Create `.planning/phases/57-auto-persist-behind-calculatorstore/57-01-SUMMARY.md` when done.
</output>
