# v1.11 Morphine Mode Removal — Single Source of Truth

**Shipped:** 2026-04-09
**Phases:** 35 (1 phase, 1 plan, 15 tasks)
**Commits:** 1 feature commit (`08400ff`)
**Diff:** 12 files changed, +36 / −216 (net −180 LOC)

## Goal

Remove the linear/compounding mode toggle from the Morphine Wean calculator. The xlsx Sheet1 (`morphine-wean-calculator.xlsx`) is the single source of truth and uses only the linear formula.

## Formula audit (verified)

Source of truth: `morphine-wean-calculator.xlsx` Sheet1, cells B15..B24.

- Step 1: `B15 = D15 × $B$4` → weight × maxDose
- Step N>1: `B[n] = B[n-1] − ($D$15 × $B$4 × $B$5)` → previous − (weight × maxDose × decreasePct)

For inputs (3.1 kg, 0.04 mg/kg/dose, 10%), every step drops by exactly 0.0124 mg. **Fixed reduction = linear.**

`calculateLinearSchedule()` already matched this formula before the milestone. No math changes were required — this was a pure deletion + simplification milestone.

## Phases

### Phase 35: Morphine Mode Removal + Release v1.11

**Plan:** `35-01-PLAN.md` (15 tasks, single wave, single commit)

**Changes:**
- Deleted `calculateCompoundingSchedule()` from `src/lib/morphine/calculations.ts`
- Removed `WeanMode` type and `activeMode` field from `src/lib/morphine/types.ts`
- Removed `modes` block from `src/lib/morphine/morphine-config.json`
- Dropped `activeMode` from `defaultState()` in `src/lib/morphine/state.svelte.ts` (stale sessionStorage keys silently ignored via existing `{ ...defaults, ...parsed }` spread — no migration needed)
- Removed `SegmentedToggle` import + markup from `src/lib/morphine/MorphineWeanCalculator.svelte`; simplified `$derived.by` schedule to call `calculateLinearSchedule` directly; cleaned `calcKey` derived
- Rewrote `src/lib/morphine/calculations.test.ts`: deleted compounding unit tests and Sheet2 parity block; renamed Sheet1 parity block to `"Morphine wean — xlsx Sheet1 spreadsheet parity"` with 10-row row-by-row assertions
- Rewrote `src/lib/morphine/MorphineWeanCalculator.test.ts`: removed tab/mode tests, added `"does not render a weaning mode toggle"` assertion, cleaned mock state
- Updated `e2e/morphine-wean.spec.ts`: removed `switching to compounding mode` and `mode tabs have correct ARIA attributes` tests; added `does not render a weaning mode toggle` assertion
- Updated `src/lib/shared/about-content.ts`: rewrote Morphine description and notes to reference single formula + `morphine-wean-calculator.xlsx` Sheet1
- Bumped `package.json` 1.10.0 → 1.11.0
- Updated `.planning/PROJECT.md` Validated list and Context paragraph

**Quality gate:**
- `svelte-check`: 0 errors / 0 warnings across 4493 files
- `vitest`: 185/185 passing
- `pnpm build`: ✓
- `playwright`: 47 passed / 3 skipped (PWA prod only), 16/16 axe sweeps green

**Requirements closed:** MORPH-01..09 (all 9)

## Key decisions

- **Single calculator path = linear (xlsx formula).** Compounding deleted entirely — not deprecated, not feature-flagged.
- **No state migration.** Stale `activeMode` in sessionStorage is silently dropped by the existing `{ ...defaultState(), ...parsed }` pattern in `init()`.
- **`SegmentedToggle` component stays.** It's still used by Formula (`FortificationCalculator.svelte`). Only its usage in Morphine is removed.
- **Sheet1 is the sole authoritative fixture.** Sheet2 compounding parity block removed from tests.
- **No UI redesign.** Toggle row deleted; surrounding spacing collapses naturally — no restructuring.

## Rollback

Single-commit phase — `git revert 08400ff` reverses cleanly.
