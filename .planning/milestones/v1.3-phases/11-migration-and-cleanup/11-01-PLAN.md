---
phase: 11-migration-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/formula/ (DELETED)
  - src/lib/shell/NavShell.test.ts
  - src/lib/shared/components/SelectPicker.svelte
  - e2e/fortification-a11y.spec.ts (NEW)
autonomous: true
requirements: [MIG-01, MIG-02, MIG-03, MIG-04]
must_haves:
  truths:
    - "No file under src/ outside test fixtures references ModifiedFormulaCalculator, BreastMilkFortifierCalculator, or the legacy formulaState."
    - "src/lib/formula/ no longer exists in the repo."
    - "Navigating to /formula renders the new Fortification calculator (Phase 10 component)."
    - "registry.ts has exactly one Fortification entry; AboutSheet describes the unified calculator."
    - "Playwright axe-core scan of /formula reports zero WCAG 2.1 AA violations in light and dark themes."
    - "vitest, svelte-check, and vite build all pass with no NavShell.test.ts tsc errors."
  artifacts:
    - path: "e2e/fortification-a11y.spec.ts"
      provides: "axe-core a11y audit for /formula in light + dark"
      contains: "AxeBuilder"
    - path: "src/lib/shell/NavShell.test.ts"
      provides: "NavShell structural tests passing svelte-check"
  key_links:
    - from: "src/routes/formula/+page.svelte"
      to: "src/lib/fortification/FortificationCalculator.svelte"
      via: "import"
      pattern: "fortification/FortificationCalculator"
    - from: "e2e/fortification-a11y.spec.ts"
      to: "/formula route"
      via: "page.goto"
      pattern: "goto\\('/formula'\\)"
---

<objective>
Phase 11 cleanup: delete the legacy `src/lib/formula/` directory entirely (MIG-01),
verify the registry/About/route changes from Plan 10-02 (MIG-02, MIG-03), add a
Playwright axe-core audit for the new Fortification calculator in light + dark
themes (MIG-04), and fold in two carryover items: fix the pre-existing
`NavShell.test.ts` Node-types errors and improve `SelectPicker.svelte` label
association so screen readers (and axe) see a proper labeled control.

Purpose: ship a clean v1.3 with no legacy formula code, verified WCAG 2.1 AA
compliance for the new calculator, and a green `svelte-check`.

Output: deleted directory, fixed test, improved component, new e2e spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@src/lib/shell/registry.ts
@src/lib/shared/about-content.ts
@src/routes/formula/+page.svelte
@src/lib/shell/NavShell.test.ts
@src/lib/shared/components/SelectPicker.svelte
@e2e/morphine-wean-a11y.spec.ts

<interfaces>
Existing files state:
- `src/lib/formula/` contains: FormulaCalculator.svelte, ModifiedFormulaCalculator.svelte,
  BreastMilkFortifierCalculator.svelte, formula-config.json, formula-config.ts,
  formula.ts, state.svelte.ts, __tests__/formula.test.ts
- `package.json` already has `@axe-core/playwright@^4.11.1` in devDependencies — no install needed.
- `e2e/morphine-wean-a11y.spec.ts` is the canonical axe-core pattern. It uses
  `withTags(['wcag2a','wcag2aa'])` and `disableRules(['color-contrast'])` in dark
  mode (pre-existing accent contrast issue documented in TODO).
- `NavShell.test.ts` uses `readFileSync`, `resolve`, `__dirname` — needs Node types
  or `import.meta.url` refactor.
- `SelectPicker.svelte` renders the label as `<span>` with `aria-label` on the
  Trigger. The trigger currently has `aria-label="{label}: {selectedLabel}"` which
  is functional but axe may flag the orphaned `<span>` label, and `getByLabelText`
  doesn't work in tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify MIG-02 and MIG-03 (registry, About, /formula route)</name>
  <files>
    src/lib/shell/registry.ts (read-only),
    src/lib/shared/about-content.ts (read-only),
    src/routes/formula/+page.svelte (read-only)
  </files>
  <action>
    Read all three files and confirm:
    1. `registry.ts` has exactly ONE entry whose route is `/formula` and whose
       title/id corresponds to the unified Fortification calculator. No entries
       reference "Modified Formula" or "BMF" or "Breast Milk Fortifier".
    2. `about-content.ts` has Fortification copy describing the unified calculator
       (no separate Modified Formula / BMF sections).
    3. `src/routes/formula/+page.svelte` imports from `$lib/fortification/...`
       and contains NO import from `$lib/formula/`.
    Run: `grep -rn "from .*\\$lib/formula" src/` — must return zero matches.
    If any check fails, STOP and report — Plan 10-02 was incomplete.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant &amp;&amp; ! grep -rn "from .*\$lib/formula" src/ &amp;&amp; ! grep -rn "ModifiedFormula\|BreastMilkFortifier" src/lib/shell src/lib/shared src/routes</automated>
  </verify>
  <done>
    All three verifications pass. Zero imports from `$lib/formula` outside the
    directory itself. Zero references to legacy class names in registry/about/routes.
  </done>
</task>

<task type="auto">
  <name>Task 2: MIG-01 — Delete src/lib/formula/ entirely</name>
  <files>src/lib/formula/ (deleted)</files>
  <action>
    Pre-deletion safety scan. Run from repo root:
    ```
    grep -rn "\$lib/formula\|from ['\"].*formula/formula\|formulaState\|FormulaCalculator\|ModifiedFormulaCalculator\|BreastMilkFortifierCalculator\|formulaConfig" src/ \
      | grep -v "src/lib/formula/" \
      | grep -v "src/lib/fortification/"
    ```
    Expected: zero matches. If any match exists outside `src/lib/formula/` and
    `src/lib/fortification/` (the Phase 9/10 fortification code is unrelated and
    must remain untouched), fix the consumer first.

    Then delete:
    ```
    git rm -r src/lib/formula/
    ```

    Then verify nothing broke:
    - `pnpm test:run` — all vitest suites pass (the deleted formula.test.ts
      disappears with the directory; no other suite should depend on it).
    - `pnpm exec svelte-kit sync &amp;&amp; pnpm exec svelte-check --tsconfig ./tsconfig.json` — zero errors.
    - `pnpm build` — successful build.
    - Final grep: `grep -rn "ModifiedFormula\|BreastMilkFortifier" src/` — zero matches.

    Out of scope: do NOT touch `src/lib/fortification/` (Phase 9/10 code).
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant &amp;&amp; test ! -d src/lib/formula &amp;&amp; ! grep -rn "ModifiedFormula\|BreastMilkFortifier" src/ &amp;&amp; pnpm test:run &amp;&amp; pnpm build</automated>
  </verify>
  <done>
    `src/lib/formula/` does not exist. Tests pass, svelte-check is clean for these
    files, build succeeds. Grep for legacy symbols returns zero.
  </done>
</task>

<task type="auto">
  <name>Task 3: Carryover — Fix NavShell.test.ts pre-existing tsc errors</name>
  <files>src/lib/shell/NavShell.test.ts</files>
  <action>
    Replace Node-specific imports (`fs`, `path`, `__dirname`) with ESM-native
    equivalents that work in Vitest without `@types/node`:

    ```ts
    import { describe, it, expect } from 'vitest';
    import { readFileSync } from 'node:fs';
    import { fileURLToPath } from 'node:url';

    const navShellSource = readFileSync(
      fileURLToPath(new URL('./NavShell.svelte', import.meta.url)),
      'utf-8'
    );
    ```

    Use `node:` prefix on `node:fs` and `node:url` (Vite/Vitest resolve these at
    test time without needing `@types/node`; if svelte-check still complains,
    add `@types/node` to devDependencies as a fallback — minimum version matching
    the current node engines).

    Do not change any test assertions. The 7 existing structural tests must still
    pass against the unchanged `NavShell.svelte`.

    If `node:fs` import still trips svelte-check, the fallback is to add
    `@types/node@^22` to devDependencies via `pnpm add -D @types/node@^22`.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant &amp;&amp; pnpm exec svelte-kit sync &amp;&amp; pnpm exec svelte-check --tsconfig ./tsconfig.json 2>&amp;1 | grep -v "0 errors" | grep -E "NavShell.test" || true; pnpm test:run -- NavShell</automated>
  </verify>
  <done>
    `svelte-check` reports zero errors for `NavShell.test.ts`. All 7 NavShell
    structural tests still pass under Vitest. No new runtime dependencies (devDep
    addition of @types/node only if absolutely required).
  </done>
</task>

<task type="auto">
  <name>Task 4: Carryover — Fix SelectPicker label association for a11y</name>
  <files>src/lib/shared/components/SelectPicker.svelte</files>
  <action>
    Improve label association so the `<span>` is programmatically tied to the
    Select.Trigger via `aria-labelledby`. Generate a unique id per instance and
    point the trigger at it. Keep the existing `aria-label` removed in favor of
    `aria-labelledby` (do not stack both — `aria-labelledby` wins and is the
    preferred accessible name pattern for axe and screen readers).

    Concrete change:
    1. At top of `<script>`, generate id:
       ```ts
       const labelId = `select-${crypto.randomUUID()}`;
       ```
    2. On the `<span>`, add `id={labelId}`.
    3. On `<Select.Trigger>`, replace the `aria-label="{label}: {selectedLabel}"`
       attribute with `aria-labelledby={labelId}`.

    Do not modify any other behavior, classes, or layout. The selected value is
    still announced by bits-ui's Select.Trigger contents.

    Verification:
    - `pnpm test:run` — all vitest suites pass, including the morphine calculator
      tests that depend on SelectPicker. No test should break because
      `aria-labelledby` is a stricter version of the previous `aria-label`.
    - The Phase 10 FortificationCalculator component test (if it uses
      `getByLabelText`) should now pass natively without the workaround.
    - `pnpm exec svelte-check` clean.

    If any morphine test breaks because it queried by the old `aria-label` text,
    update the test selector to use `getByLabelText(label)` (the now-proper
    accessible name), not the legacy `getByLabel('label: value')` form.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant &amp;&amp; pnpm test:run &amp;&amp; pnpm exec svelte-check --tsconfig ./tsconfig.json</automated>
  </verify>
  <done>
    SelectPicker uses `aria-labelledby` linking trigger to the visible label
    `<span>`. All vitest suites pass. svelte-check clean.
  </done>
</task>

<task type="auto">
  <name>Task 5: MIG-04 — Add Playwright axe-core audit for /formula (light + dark)</name>
  <files>e2e/fortification-a11y.spec.ts</files>
  <action>
    Create a new Playwright spec mirroring `e2e/morphine-wean-a11y.spec.ts`
    exactly, but for `/formula`. The spec must:

    1. `test.describe('Fortification Calculator Accessibility', ...)`
    2. `beforeEach`: `page.goto('/formula')`, wait for the Fortification heading
       to be visible, then dismiss the disclaimer modal if present (same pattern
       as morphine spec — `getByRole('button', { name: /understand|acknowledge/i }).click({ timeout: 2000 }).catch(() => {})`).
    3. Test 1 — light mode: force light theme via the same `page.evaluate` block
       as morphine (remove `dark`, add `light`, set `data-theme=light`), run
       `new AxeBuilder({ page }).withTags(['wcag2a','wcag2aa']).analyze()`,
       assert `results.violations` equals `[]`.
    4. Test 2 — dark mode: force dark theme, run AxeBuilder with the same
       `disableRules(['color-contrast'])` exclusion as morphine and the same
       TODO comment referencing the pre-existing dark accent contrast issue.
       Assert zero violations.
    5. Test 3 — with results visible: fill the form with valid inputs to render
       a fortification result (use the actual input labels from
       FortificationCalculator — read the Phase 10 SUMMARY or the component to
       confirm field labels). Wait for the result block to be visible. Run a
       light-mode axe scan. Assert zero violations.

    Determine the exact heading text and input labels from
    `src/lib/fortification/FortificationCalculator.svelte` and the Phase 10
    SUMMARY before writing the test. Use `getByLabel` (now backed by Task 4's
    SelectPicker fix).

    Run `pnpm exec playwright test e2e/fortification-a11y.spec.ts` to confirm
    all three tests pass. If color-contrast violations appear in light mode,
    do NOT disable the rule — report it; light mode is supposed to be
    contrast-clean per ROADMAP.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant &amp;&amp; pnpm exec playwright test e2e/fortification-a11y.spec.ts</automated>
  </verify>
  <done>
    `e2e/fortification-a11y.spec.ts` exists, mirrors the morphine pattern, and
    all three tests pass: light mode zero violations, dark mode zero violations
    (color-contrast excluded with TODO), with-results zero violations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→localStorage | Theme preference (no PII) |
| build→bundle | Deleted code paths must not be reachable in shipped bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Deleted formula code reintroduced via stale import | mitigate | Task 2 grep gate + svelte-check + build must all pass before merge |
| T-11-02 | Information Disclosure | Legacy clinical config (formula-config.json) leaked in bundle | mitigate | `git rm` removes from working tree; verify with `pnpm build` and inspect dist for absence of "formula-config" string |
| T-11-03 | Denial of Service (a11y) | Screen-reader users blocked by orphan-label SelectPicker | mitigate | Task 4 fixes label association; Task 5 axe audit verifies |
| T-11-04 | Repudiation | n/a (no auth, no logging) | accept | Standalone PWA, no audit trail required |
| T-11-05 | Elevation of Privilege | n/a (no roles) | accept | Single-user clinical tool |
| T-11-06 | Spoofing | n/a (no identity) | accept | No authentication surface |
</threat_model>

<verification>
End-of-phase checks (run all from repo root):

1. `test ! -d src/lib/formula` — directory gone
2. `! grep -rn "ModifiedFormula\|BreastMilkFortifier" src/` — no legacy symbols
3. `! grep -rn "from .*\\$lib/formula" src/` — no imports
4. `pnpm test:run` — all vitest suites pass
5. `pnpm exec svelte-check --tsconfig ./tsconfig.json` — zero errors (incl. NavShell.test.ts)
6. `pnpm build` — successful
7. `pnpm exec playwright test e2e/fortification-a11y.spec.ts` — three tests pass
8. `pnpm exec playwright test e2e/morphine-wean-a11y.spec.ts` — still passes (regression check after SelectPicker change)
</verification>

<success_criteria>
- MIG-01: `src/lib/formula/` deleted; grep for legacy class names returns zero outside test fixtures
- MIG-02: Task 1 verification confirms registry + About reflect unified Fortification
- MIG-03: Task 1 verification confirms `/formula` renders new calculator
- MIG-04: `e2e/fortification-a11y.spec.ts` passes with zero violations in both themes
- Carryover (NavShell): svelte-check reports zero errors for NavShell.test.ts
- Carryover (SelectPicker): aria-labelledby in place, all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-migration-and-cleanup/11-01-SUMMARY.md`
documenting: files deleted, files modified, test results, axe violation counts
(should be 0/0/0), and any unexpected blockers encountered.
</output>
