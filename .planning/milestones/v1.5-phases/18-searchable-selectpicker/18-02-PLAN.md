---
phase: 18
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/lib/fortification/FortificationCalculator.svelte
  - src/lib/shared/components/SelectPicker.test.ts
autonomous: true
requirements: [A11Y-03, SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04, SEARCH-05, SEARCH-06]
must_haves:
  truths:
    - "Formula picker in FortificationCalculator opts in via searchable={true}"
    - "Other 3 SelectPicker call sites in FortificationCalculator stay unchanged (no searchable prop)"
    - "SelectPicker.test.ts contains new T-12..T-18 tests covering SEARCH-01..06 + A11Y-03"
    - "All component tests pass under jsdom (no Playwright required for A11Y-03)"
  artifacts:
    - path: src/lib/fortification/FortificationCalculator.svelte
      provides: "Formula picker wired with searchable={true}"
      contains: "searchable"
    - path: src/lib/shared/components/SelectPicker.test.ts
      provides: "Searchable test cases T-12..T-18"
      contains: "searchableOptions"
  key_links:
    - from: src/lib/fortification/FortificationCalculator.svelte
      to: src/lib/shared/components/SelectPicker.svelte
      via: "searchable prop on Formula picker only"
      pattern: "searchable"
---

<objective>
Wire the Formula picker (and ONLY the Formula picker) at FortificationCalculator.svelte:166
with `searchable={true}`, then add component tests T-12..T-18 to SelectPicker.test.ts
that cover all of SEARCH-01..06 plus A11Y-03 keyboard traversal + Enter-single.

Purpose: Ship SEARCH-01..06 + A11Y-03 verification end-to-end.
Output: Live searchable Formula picker, full test coverage, no regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-searchable-selectpicker/18-RESEARCH.md
@src/lib/fortification/FortificationCalculator.svelte
@src/lib/shared/components/SelectPicker.test.ts
@src/lib/shared/components/SelectPicker.svelte
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire Formula picker with searchable={true}</name>
  <files>src/lib/fortification/FortificationCalculator.svelte</files>
  <read_first>
    - src/lib/fortification/FortificationCalculator.svelte (lines 140-180 — confirm picker call sites at 153, 166, 170, 174)
    - .planning/phases/18-searchable-selectpicker/18-RESEARCH.md section "Files Modified / Created"
  </read_first>
  <action>
    Edit src/lib/fortification/FortificationCalculator.svelte:

    Locate the Formula picker `<SelectPicker ... />` call at approximately line 166.
    This is the picker bound to the formula selection (the one that uses 30+
    grouped formulas with `group: manufacturer`).

    Add the prop `searchable` to that single instance. Example:
    ```svelte
    <SelectPicker
      label="Formula"
      options={formulaOptions}
      bind:value={selectedFormula}
      searchable
    />
    ```

    DO NOT add `searchable` to the other three SelectPicker instances at lines
    ~153 (Base), ~170 (Target Calorie), ~174 (Unit). They must remain unchanged
    per SEARCH-05.

    Confirm by re-running existing FortificationCalculator tests — UI-04 trigger
    count must still report 4.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/fortification/FortificationCalculator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "searchable" src/lib/fortification/FortificationCalculator.svelte` returns exactly 1 hit (only the Formula picker)
    - `grep -c "<SelectPicker" src/lib/fortification/FortificationCalculator.svelte` still returns 4
    - FortificationCalculator.test.ts UI-04 (or whichever trigger-count test) passes — 4 triggers
    - `pnpm tsc --noEmit` clean
  </acceptance_criteria>
  <done>
    Formula picker is searchable in the running app; other 3 pickers are untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add T-12..T-18 component tests for searchable mode</name>
  <files>src/lib/shared/components/SelectPicker.test.ts</files>
  <read_first>
    - src/lib/shared/components/SelectPicker.test.ts (full file — match existing test patterns, fixture style, async/tick usage, fireEvent vs userEvent)
    - .planning/phases/18-searchable-selectpicker/18-RESEARCH.md section "Testing Strategy" (T-12..T-18 spec) and "Searchable test fixture"
    - src/test-setup.ts (confirm dialog polyfill)
  </read_first>
  <behavior>
    - T-12: searchable=true renders a textbox inside the open dialog and focus lands on it after openPicker
    - T-13: typing 'abb' filters to only options whose label OR group contains 'abb' (case-insensitive)
    - T-14: typing 'zzzzzz' renders an element with role='status' and text 'No matches'
    - T-15: ArrowDown in search input moves document.activeElement to first filtered option (data-index="0"); ArrowUp from first option returns activeElement to the search input
    - T-16: with exactly one filtered match, Enter in the search input calls select → trigger label updates AND aria-expanded becomes 'false'
    - T-17: open picker, type query, close (programmatic dialog.close()), reopen → searchQuery is empty (input value === '') AND focus is on the search input
    - T-18: searchable omitted (or false) → no textbox is rendered inside the open dialog
  </behavior>
  <action>
    Append to src/lib/shared/components/SelectPicker.test.ts (do NOT create a new file
    — co-located convention per user memory).

    1. Add a new fixture near existing fixtures:
    ```ts
    const searchableOptions = [
      { value: 'sim-adv', label: 'Similac Advance', group: 'Abbott' },
      { value: 'sim-neo', label: 'Similac NeoSure', group: 'Abbott' },
      { value: 'enfa-ar', label: 'Enfamil AR', group: 'Mead Johnson' },
      { value: 'enfa-pre', label: 'Enfamil Premature', group: 'Mead Johnson' },
      { value: 'gerb', label: 'Gerber Good Start', group: 'Nestle' },
    ];
    ```

    2. Add a new `describe('SelectPicker searchable mode', () => { ... })` block (or
    extend existing top-level describe — match the file's style) with these 7 tests
    using the same render/openPicker/tick/fireEvent patterns as T-01..T-11:

    - **T-12 — search input renders & receives focus on open**:
      Render with `searchable: true, options: searchableOptions, label: 'Formula'`,
      click trigger to open, `await tick()`, query the dialog for an input via
      `getByRole('textbox', { name: /Filter Formula/i })`, expect it exists and
      `document.activeElement === <that input>`.

    - **T-13 — substring filter on label and group**:
      Open picker, type 'abb' into the search input via
      `await fireEvent.input(input, { target: { value: 'abb' } })`, `await tick()`,
      query rendered option elements (`[data-index]` or `[role="option"]` — match
      what existing tests use), expect exactly 2 (Similac Advance + Similac NeoSure
      via Abbott group). Then clear and type 'enfa', expect 2 Enfamil rows.
      Then type 'pre', expect 1 (Enfamil Premature via label substring).

    - **T-14 — No matches empty state**:
      Open picker, type 'zzzzzz', `await tick()`, query for `getByRole('status')` or
      `getByText('No matches')`, expect it exists. Expect zero `[role="option"]`.

    - **T-15 — Arrow traversal input ↔ list**:
      Open picker, search input has focus. `fireEvent.keyDown(input, { key: 'ArrowDown' })`,
      `await tick()`, expect `document.activeElement.getAttribute('data-index') === '0'`.
      Then `fireEvent.keyDown(document.activeElement!, { key: 'ArrowUp' })`, `await tick()`,
      expect `document.activeElement === searchInput`. (Use the same pattern T-05/T-06 use
      for keyboard assertions.)

    - **T-16 — Enter on single match selects and closes**:
      Open picker, type 'gerb' (1 unique match), `fireEvent.keyDown(input, { key: 'Enter' })`,
      `await tick()`. Expect the trigger button now displays 'Gerber Good Start' AND
      `trigger.getAttribute('aria-expanded') === 'false'`.

    - **T-17 — Reset on reopen**:
      Open, type 'abb', confirm filter is active. Programmatically close:
      `const dialog = container.querySelector('dialog')!; dialog.close(); await tick();`
      Then click trigger to reopen, `await tick()`. Expect search input's `value === ''`
      AND `document.activeElement === searchInput` AND all 5 options visible.

    - **T-18 — searchable=false default has no textbox**:
      Render WITHOUT the searchable prop, `options: searchableOptions`, click trigger
      to open, `await tick()`, expect `queryByRole('textbox')` returns null inside
      the dialog.

    Use `getByRole`/`queryByRole`/`fireEvent` from `@testing-library/svelte` matching
    existing patterns. Use `await tick()` after all keyboard/input events.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/shared/components/SelectPicker.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "searchableOptions" src/lib/shared/components/SelectPicker.test.ts` shows the new fixture
    - `grep -nE "T-1[2-8]|searchable mode" src/lib/shared/components/SelectPicker.test.ts` shows all 7 new test cases
    - `grep -n "No matches" src/lib/shared/components/SelectPicker.test.ts` finds T-14 assertion
    - `grep -n "ArrowDown" src/lib/shared/components/SelectPicker.test.ts` finds T-15 (in addition to existing tests if any)
    - `grep -n "Filter Formula" src/lib/shared/components/SelectPicker.test.ts` finds T-12 aria-label query
    - `pnpm vitest run src/lib/shared/components/SelectPicker.test.ts` reports ≥18 tests, all passing
    - `pnpm vitest run` (full suite) is green
  </acceptance_criteria>
  <done>
    SelectPicker.test.ts contains T-12..T-18, all pass, full test suite green,
    SEARCH-01..06 + A11Y-03 verified by component tests.
  </done>
</task>

</tasks>

<verification>
- pnpm vitest run → full suite green
- pnpm tsc --noEmit → clean
- Manual smoke (optional, recommended): pnpm dev → open Formula picker → type 'abb' → see Abbott-only → ArrowDown → first option focused → ArrowUp → back to input → close → reopen → query empty
</verification>

<success_criteria>
- Formula picker at FortificationCalculator.svelte:166 has searchable
- Other 3 SelectPicker call sites in FortificationCalculator unchanged
- All 7 new tests T-12..T-18 pass under jsdom
- All previously passing tests still pass (no regressions)
- SEARCH-01..06 each have a passing automated test
- A11Y-03 satisfied by T-15 + T-16 component tests (no E2E required)
</success_criteria>

<output>
After completion, create `.planning/phases/18-searchable-selectpicker/18-02-SUMMARY.md`
</output>
