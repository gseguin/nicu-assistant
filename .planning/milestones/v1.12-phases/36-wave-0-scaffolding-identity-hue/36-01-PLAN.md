---
phase: 36-wave-0-scaffolding-identity-hue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shared/types.ts
  - src/lib/shell/registry.ts
  - src/lib/shell/NavShell.svelte
  - src/lib/shared/about-content.ts
  - src/app.css
  - src/routes/feeds/+page.svelte
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-05, ARCH-06, ARCH-07, HUE-01]

must_haves:
  truths:
    - "App compiles with zero TypeScript errors after adding 'feeds' to CalculatorId"
    - "A 4th 'Feeds' tab appears in both mobile bottom nav and desktop top nav"
    - "Clicking/tapping the Feeds tab navigates to /feeds and shows placeholder content"
    - "The Feeds tab uses the Baby icon and 'Feeds' label"
    - "The /feeds page has identity-feeds class applying warm terracotta OKLCH hue ~30"
  artifacts:
    - path: "src/lib/shared/types.ts"
      provides: "CalculatorId union with 'feeds'"
      contains: "'feeds'"
    - path: "src/lib/shell/registry.ts"
      provides: "4th registry entry with Baby icon and identity-feeds"
      contains: "identity-feeds"
    - path: "src/lib/shell/NavShell.svelte"
      provides: "Ternary branch routing /feeds to 'feeds'"
      contains: "startsWith('/feeds')"
    - path: "src/lib/shared/about-content.ts"
      provides: "feeds key in aboutContent Record"
      contains: "feeds:"
    - path: "src/app.css"
      provides: ".identity-feeds light + dark OKLCH tokens"
      contains: ".identity-feeds"
    - path: "src/routes/feeds/+page.svelte"
      provides: "Placeholder route for feeds calculator"
      contains: "Feed Advance Calculator"
  key_links:
    - from: "src/lib/shared/types.ts"
      to: "src/lib/shared/about-content.ts"
      via: "Record<CalculatorId, AboutContent> exhaustiveness"
      pattern: "Record<CalculatorId"
    - from: "src/lib/shell/registry.ts"
      to: "src/lib/shell/NavShell.svelte"
      via: "CALCULATOR_REGISTRY drives nav rendering"
      pattern: "CALCULATOR_REGISTRY"
    - from: "src/app.css"
      to: "src/routes/feeds/+page.svelte"
      via: ".identity-feeds class applies CSS custom properties"
      pattern: "identity-feeds"
---

<objective>
Extend the app shell to compile with a visible 4th "Feeds" calculator tab, placeholder route, and OKLCH identity hue tokens.

Purpose: Unblock all downstream phases (37-39) by making the shell aware of the feeds calculator. Every type, registry, nav, about-content, CSS, and route change ships in one atomic plan.
Output: 6 files modified/created; `pnpm check` and `pnpm test` pass with zero regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-wave-0-scaffolding-identity-hue/36-CONTEXT.md
@.planning/phases/36-wave-0-scaffolding-identity-hue/36-RESEARCH.md

@src/lib/shared/types.ts
@src/lib/shell/registry.ts
@src/lib/shell/NavShell.svelte
@src/lib/shared/about-content.ts
@src/app.css
@src/routes/gir/+page.svelte
@src/lib/shared/context.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/shared/types.ts:
```typescript
export type CalculatorId = 'morphine-wean' | 'formula' | 'gir';
// Must become: 'morphine-wean' | 'formula' | 'gir' | 'feeds'

export interface CalculatorContext {
  id: CalculatorId;
  accentColor: string;
}
```

From src/lib/shell/registry.ts:
```typescript
export interface CalculatorEntry {
  id: string;
  label: string;
  href: string;
  icon: Component;
  description: string;
  identityClass: 'identity-morphine' | 'identity-formula' | 'identity-gir';
  // Must become: ... | 'identity-feeds'
}
```

From src/lib/shared/about-content.ts:
```typescript
export const aboutContent: Record<CalculatorId, AboutContent> = { ... };
// Must add 'feeds' key for exhaustiveness
```

From src/lib/shared/context.ts:
```typescript
export function setCalculatorContext(ctx: CalculatorContext): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CalculatorId union, registry, NavShell ternary, and about-content</name>
  <files>src/lib/shared/types.ts, src/lib/shell/registry.ts, src/lib/shell/NavShell.svelte, src/lib/shared/about-content.ts</files>
  <read_first>
    - src/lib/shared/types.ts (current CalculatorId union on line 7)
    - src/lib/shell/registry.ts (CalculatorEntry interface line 11, CALCULATOR_REGISTRY array lines 14-39)
    - src/lib/shell/NavShell.svelte (activeCalculatorId ternary lines 11-15)
    - src/lib/shared/about-content.ts (aboutContent Record lines 14-49)
  </read_first>
  <action>
**src/lib/shared/types.ts** (per D-07 / ARCH-01):
Change line 7 from:
```typescript
export type CalculatorId = 'morphine-wean' | 'formula' | 'gir';
```
to:
```typescript
export type CalculatorId = 'morphine-wean' | 'formula' | 'gir' | 'feeds';
```

**src/lib/shell/registry.ts** (per D-01, D-05, D-08 / ARCH-03):
1. Line 3: Add `Baby` to the lucide import:
   ```typescript
   import { Syringe, Milk, Droplet, Baby } from '@lucide/svelte';
   ```
2. Line 11: Extend identityClass union:
   ```typescript
   identityClass: 'identity-morphine' | 'identity-formula' | 'identity-gir' | 'identity-feeds';
   ```
3. After the GIR entry (after line 38, before `] as const`), append:
   ```typescript
   {
     id: 'feeds',
     label: 'Feeds',
     href: '/feeds',
     icon: Baby,
     description: 'Feed advance calculator',
     identityClass: 'identity-feeds',
   },
   ```

**src/lib/shell/NavShell.svelte** (per D-09 / ARCH-02):
Change the activeCalculatorId ternary (lines 11-15) from:
```typescript
const activeCalculatorId = $derived<CalculatorId>(
  page.url.pathname.startsWith('/formula') ? 'formula'
  : page.url.pathname.startsWith('/gir') ? 'gir'
  : 'morphine-wean'
);
```
to:
```typescript
const activeCalculatorId = $derived<CalculatorId>(
  page.url.pathname.startsWith('/formula') ? 'formula'
  : page.url.pathname.startsWith('/gir') ? 'gir'
  : page.url.pathname.startsWith('/feeds') ? 'feeds'
  : 'morphine-wean'
);
```
The `/feeds` branch MUST come BEFORE the `'morphine-wean'` fallback (Pitfall 2 from research).

**src/lib/shared/about-content.ts** (per D-10 / ARCH-04):
After the `gir` entry (after the closing `},` around line 48), add:
```typescript
feeds: {
  title: 'Feed Advance Calculator',
  version: appVersion,
  description:
    'Calculates bedside feeding advancement volumes and full TPN nutrition totals for NICU patients.',
  notes: [
    'Source: nutrition-calculator.xlsx Sheet1 (TPN full nutrition) + Sheet2 (bedside advancement).',
    'Coming soon.',
  ],
},
```

All four files MUST be edited together to avoid TypeScript exhaustiveness errors (Pitfall 3 from research).
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'feeds'" src/lib/shared/types.ts (CalculatorId union extended)
    - grep -q "Baby" src/lib/shell/registry.ts (Baby icon imported)
    - grep -q "identity-feeds" src/lib/shell/registry.ts (identityClass union extended + registry entry)
    - grep -q "startsWith('/feeds')" src/lib/shell/NavShell.svelte (ternary branch added)
    - grep -q "feeds:" src/lib/shared/about-content.ts (about-content stub added)
    - pnpm check exits 0 (zero TypeScript errors — ARCH-07 verified: no new deps)
  </acceptance_criteria>
  <done>CalculatorId includes 'feeds', registry has 4th entry with Baby icon and 'identity-feeds', NavShell routes /feeds to 'feeds', aboutContent has feeds stub. pnpm check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add .identity-feeds CSS tokens and create /feeds placeholder route</name>
  <files>src/app.css, src/routes/feeds/+page.svelte</files>
  <read_first>
    - src/app.css (identity token blocks lines 191-218, pattern for light + dark)
    - src/routes/gir/+page.svelte (route pattern reference)
    - src/lib/shared/context.ts (setCalculatorContext signature)
  </read_first>
  <action>
**src/app.css** (per D-02, D-03 / HUE-01):
After the `.dark .identity-gir` block (after line 217, before the closing `}`), add:

```css
  .identity-feeds {
    --color-identity:      oklch(50% 0.13 30);
    --color-identity-hero: oklch(94% 0.04 30);
  }
  .dark .identity-feeds,
  [data-theme="dark"] .identity-feeds {
    --color-identity:      oklch(80% 0.10 30);
    --color-identity-hero: oklch(24% 0.05 30);
  }
```

These values are from D-03. The block goes inside the existing `@layer base { ... }` block that contains the other identity classes.

**src/routes/feeds/+page.svelte** (per D-06 / ARCH-05, ARCH-06):
Create new file following the GIR route pattern but as a placeholder (no calculator component, no state init):

```svelte
<script lang="ts">
  import { onMount } from 'svelte';
  import { setCalculatorContext } from '$lib/shared/context.js';
  import { Baby } from '@lucide/svelte';

  onMount(() => {
    setCalculatorContext({
      id: 'feeds',
      accentColor: 'var(--color-identity)'
    });
  });
</script>

<svelte:head>
  <title>Feeds | NICU Assistant</title>
</svelte:head>

<div class="identity-feeds max-w-lg md:max-w-4xl mx-auto px-4 py-6 space-y-4">
  <header class="flex items-center gap-3">
    <Baby size={28} class="text-[var(--color-identity)]" aria-hidden="true" />
    <div class="flex flex-col">
      <h1 class="text-title font-bold text-[var(--color-text-primary)]">Feed Advance Calculator</h1>
      <span class="text-ui text-[var(--color-text-secondary)]">coming soon</span>
    </div>
  </header>
</div>
```

Do NOT create any files under `src/lib/feeds/` — that directory is created in Phase 37 when actual module files are needed. ARCH-05 is satisfied by the route existing and the module directory being deferred (per research A3).
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm check 2>&1 | tail -5 && pnpm test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q ".identity-feeds" src/app.css (CSS token block exists)
    - grep -q "oklch(50% 0.13 30)" src/app.css (light identity value)
    - grep -q "oklch(80% 0.10 30)" src/app.css (dark identity value)
    - test -f src/routes/feeds/+page.svelte (route file exists)
    - grep -q "Feed Advance Calculator" src/routes/feeds/+page.svelte (placeholder content per D-06)
    - grep -q "coming soon" src/routes/feeds/+page.svelte (placeholder text per D-06)
    - grep -q "identity-feeds" src/routes/feeds/+page.svelte (identity class applied)
    - grep -q "Baby" src/routes/feeds/+page.svelte (Baby icon used per D-01)
    - pnpm check exits 0
    - pnpm test exits 0 (no regressions)
  </acceptance_criteria>
  <done>.identity-feeds OKLCH tokens in app.css (light: oklch(50% 0.13 30), dark: oklch(80% 0.10 30)), /feeds placeholder route created with Baby icon, "Feed Advance Calculator — coming soon" content, identity-feeds class. Both pnpm check and pnpm test pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 36 is pure shell scaffolding — no user input, no data processing, no API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | I (Information Disclosure) | /feeds placeholder route | accept | Placeholder shows only static "coming soon" text — no sensitive data, no calculation logic. Public-facing content only. |
</threat_model>

<verification>
1. `pnpm check` exits 0 — TypeScript compilation clean with extended CalculatorId union
2. `pnpm test -- --run` exits 0 — no regressions from registry/type changes
3. `pnpm build` succeeds — static build produces /feeds route
4. Manual: visit /feeds in dev server, verify 4th tab visible, placeholder content shown with warm terracotta hue
</verification>

<success_criteria>
- CalculatorId union includes 'feeds'
- CALCULATOR_REGISTRY has 4 entries (morphine-wean, formula, gir, feeds)
- NavShell routes /feeds pathname to 'feeds' calculator ID
- aboutContent Record has feeds key (TypeScript exhaustiveness satisfied)
- .identity-feeds CSS block defines OKLCH hue ~30 tokens for light and dark
- /feeds route renders placeholder with Baby icon and "Feed Advance Calculator — coming soon"
- Zero new runtime or dev dependencies (ARCH-07)
- pnpm check, pnpm test, pnpm build all pass
</success_criteria>

<output>
After completion, create `.planning/phases/36-wave-0-scaffolding-identity-hue/36-01-SUMMARY.md`
</output>
