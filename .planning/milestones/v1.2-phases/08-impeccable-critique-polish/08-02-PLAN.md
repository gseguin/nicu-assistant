---
phase: 08-impeccable-critique-polish
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/app.css
  - src/lib/shell/NavShell.svelte
  - src/routes/+layout.svelte
  - src/lib/morphine/MorphineWeanCalculator.svelte
  - src/lib/formula/FormulaCalculator.svelte
  - src/lib/formula/ModifiedFormulaCalculator.svelte
  - src/lib/formula/BreastMilkFortifierCalculator.svelte
  - src/lib/shared/components/NumericInput.svelte
  - src/lib/shared/components/SelectPicker.svelte
  - src/lib/shared/components/ResultsDisplay.svelte
  - src/lib/shared/components/AboutSheet.svelte
  - src/lib/shared/components/DisclaimerModal.svelte
autonomous: false
requirements: [UX-03]

must_haves:
  truths:
    - "All follow-up Impeccable commands suggested by the critique have been run"
    - "All actionable recommendations from follow-up commands are implemented"
    - "The app has consistent spacing, clear visual hierarchy, and no layout issues across mobile and desktop"
    - "Both light and dark themes feel intentionally designed, not just inverted"
  artifacts:
    - path: ".planning/phases/08-impeccable-critique-polish/followup-findings.md"
      provides: "Documented findings from all follow-up Impeccable commands"
    - path: "src/app.css"
      provides: "Finalized design tokens and global styles"
  key_links:
    - from: "follow-up command output"
      to: "code changes"
      via: "Each recommendation maps to a specific file change"
      pattern: "Documented in followup-findings.md"
---

<objective>
Run all follow-up Impeccable commands suggested by the Phase 8 critique (e.g., /polish, /typeset, /arrange, /normalize) and implement their recommendations.

Purpose: The initial /critique identifies broad issues; follow-up commands provide deep, targeted assessment of specific design dimensions (typography, spacing, component arrangement, visual normalization). This ensures comprehensive polish beyond the initial pass.

Output: Finalized, polished codebase that passes all Impeccable assessments with documented follow-up findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.impeccable.md
@.planning/phases/08-impeccable-critique-polish/08-01-SUMMARY.md
@.planning/phases/08-impeccable-critique-polish/critique-findings.md

@src/app.css
@src/lib/shell/NavShell.svelte
@src/routes/+layout.svelte
@src/lib/morphine/MorphineWeanCalculator.svelte
@src/lib/formula/FormulaCalculator.svelte
@src/lib/formula/ModifiedFormulaCalculator.svelte
@src/lib/formula/BreastMilkFortifierCalculator.svelte
@src/lib/shared/components/NumericInput.svelte
@src/lib/shared/components/SelectPicker.svelte
@src/lib/shared/components/ResultsDisplay.svelte
@src/lib/shared/components/AboutSheet.svelte
@src/lib/shared/components/DisclaimerModal.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run follow-up Impeccable commands and document findings</name>
  <files>.planning/phases/08-impeccable-critique-polish/followup-findings.md</files>
  <read_first>
    - .planning/phases/08-impeccable-critique-polish/critique-findings.md (to find which follow-up commands were recommended)
    - .planning/phases/08-impeccable-critique-polish/08-01-SUMMARY.md (to understand what was already fixed)
    - .impeccable.md (design context)
    - src/app.css (current state after Plan 01 fixes)
  </read_first>
  <action>
1. Ensure the dev server is running (`pnpm dev`, run in background if not already up).
2. Read critique-findings.md to find the list of suggested follow-up commands (e.g., /polish, /typeset, /arrange, /normalize).
3. Run each follow-up Impeccable command against the live app using Playwright MCP screenshots:
   - For each command, take screenshots of all key screens (both calculators, both themes, mobile + desktop)
   - Document the command output and all recommendations
4. Compile ALL findings into `followup-findings.md`, organized by:
   - Which command produced the finding
   - Priority level (P0/P1/P2/P3)
   - Affected file(s) and specific fix needed
5. Note any findings that overlap with already-fixed items from Plan 01 (mark as "already addressed").
  </action>
  <verify>
    <automated>test -f .planning/phases/08-impeccable-critique-polish/followup-findings.md && grep -c "^##" .planning/phases/08-impeccable-critique-polish/followup-findings.md</automated>
  </verify>
  <acceptance_criteria>
    - followup-findings.md exists with findings from each follow-up command
    - Each finding includes priority, affected files, and specific fix
    - Already-addressed items are marked as such
  </acceptance_criteria>
  <done>All follow-up Impeccable commands run and findings documented with prioritized recommendations</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review follow-up findings before implementation</name>
  <files>.planning/phases/08-impeccable-critique-polish/followup-findings.md</files>
  <read_first>
    - .planning/phases/08-impeccable-critique-polish/followup-findings.md
  </read_first>
  <action>
Present followup-findings.md to the user for review. The user needs to:
1. Confirm P0/P1 findings will all be implemented
2. Decide which P2/P3 findings to implement vs defer
3. Verify no conflicting recommendations across different commands
  </action>
  <what-built>All follow-up Impeccable commands have been run. Findings are documented in followup-findings.md with priority levels and specific recommendations.</what-built>
  <how-to-verify>
    1. Read `.planning/phases/08-impeccable-critique-polish/followup-findings.md`
    2. Review P0/P1 findings — these will all be implemented
    3. Review P2/P3 findings — confirm which should be implemented vs deferred
    4. Verify no conflicting recommendations across different commands
  </how-to-verify>
  <verify>User has reviewed followup-findings.md and provided approval or modification instructions</verify>
  <acceptance_criteria>
    - User has reviewed all follow-up findings and approved the implementation plan
  </acceptance_criteria>
  <done>User approved follow-up findings for implementation</done>
  <resume-signal>Type "approved" to proceed with implementing all recommendations, or describe any findings to defer/modify</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Implement all follow-up recommendations</name>
  <files>
    src/app.css
    src/lib/shell/NavShell.svelte
    src/routes/+layout.svelte
    src/lib/morphine/MorphineWeanCalculator.svelte
    src/lib/formula/FormulaCalculator.svelte
    src/lib/formula/ModifiedFormulaCalculator.svelte
    src/lib/formula/BreastMilkFortifierCalculator.svelte
    src/lib/shared/components/NumericInput.svelte
    src/lib/shared/components/SelectPicker.svelte
    src/lib/shared/components/ResultsDisplay.svelte
    src/lib/shared/components/AboutSheet.svelte
    src/lib/shared/components/DisclaimerModal.svelte
  </files>
  <read_first>
    - .planning/phases/08-impeccable-critique-polish/followup-findings.md (all follow-up recommendations)
    - .impeccable.md (design context to ensure fixes align with brand)
    - src/app.css (current state — read before modifying)
    - All component files that have findings against them (read each before modifying)
  </read_first>
  <action>
Implement every approved recommendation from followup-findings.md, working by priority:

1. **P0 (Critical):** Fix immediately.
2. **P1 (High):** Fix all.
3. **P2 (Medium):** Fix all unless user deferred specific items.

For each fix:
- Read the affected file first
- Apply the specific change described in the finding
- Verify the change is consistent with the design context in .impeccable.md
- Check that the fix doesn't conflict with Plan 01 changes

After all fixes:
- Run `pnpm build` to verify no build errors
- Run `pnpm test` to verify all existing tests still pass
- Take final screenshots of all key screens in both themes at both viewports as a final visual check

Commit changes grouped logically.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - All approved follow-up recommendations are implemented
    - pnpm build succeeds with no errors
    - pnpm test passes (all existing tests)
    - No hardcoded color values (all use design tokens)
    - Visual consistency across mobile/desktop and light/dark themes
  </acceptance_criteria>
  <done>All follow-up recommendations implemented, build passes, tests pass, app visually polished</done>
</task>

</tasks>

<verification>
- followup-findings.md exists with findings from all follow-up commands
- All approved fixes applied to codebase
- `pnpm build` succeeds
- `pnpm test` passes
- App shows consistent spacing, clear visual hierarchy, no layout issues
- Both themes feel intentionally designed
</verification>

<success_criteria>
- All suggested follow-up Impeccable commands have been run and recommendations implemented (UX-03)
- App builds and tests pass after all changes
- Final visual inspection confirms professional-quality UI across all screens, themes, and viewports
</success_criteria>

<output>
After completion, create `.planning/phases/08-impeccable-critique-polish/08-02-SUMMARY.md`
</output>
