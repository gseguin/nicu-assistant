---
phase: 08-impeccable-critique-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shell/NavShell.svelte
  - src/routes/+layout.svelte
  - src/app.css
  - src/lib/morphine/MorphineWeanCalculator.svelte
  - src/lib/formula/FormulaCalculator.svelte
  - src/lib/formula/ModifiedFormulaCalculator.svelte
  - src/lib/formula/BreastMilkFortifierCalculator.svelte
  - src/lib/shared/components/NumericInput.svelte
  - src/lib/shared/components/SelectPicker.svelte
  - src/lib/shared/components/ResultsDisplay.svelte
  - src/lib/shared/components/AboutSheet.svelte
  - src/lib/shared/components/DisclaimerModal.svelte
autonomous: false
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "An Impeccable /critique assessment has been run against the live app and all findings are documented"
    - "All P0 and P1 recommendations from the critique are implemented in the codebase"
    - "All P2 recommendations from the critique are implemented unless explicitly deferred with justification"
    - "The app builds successfully and all existing tests pass after implementing changes"
  artifacts:
    - path: ".planning/phases/08-impeccable-critique-polish/critique-findings.md"
      provides: "Documented critique findings and recommendations"
    - path: "src/app.css"
      provides: "Updated design tokens and global styles per critique"
  key_links:
    - from: "/critique output"
      to: "code changes"
      via: "Each P0/P1/P2 recommendation maps to a specific file change"
      pattern: "Documented in critique-findings.md"
---

<objective>
Run the Impeccable /critique skill against the live NICU Assistant dev server, document all findings, and implement every actionable recommendation.

Purpose: Systematically assess the app's visual hierarchy, layout, typography, and UX quality using the Impeccable design critique framework, then fix all identified issues to bring the app to professional visual standards.

Output: Documented critique findings and a polished codebase with all P0/P1/P2 fixes applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.impeccable.md

@src/app.css
@src/lib/shell/NavShell.svelte
@src/routes/+layout.svelte
@src/lib/morphine/MorphineWeanCalculator.svelte
@src/lib/formula/FormulaCalculator.svelte
@src/lib/formula/ModifiedFormulaCalculator.svelte
@src/lib/formula/BreastMilkFortifierCalculator.svelte
@src/lib/shared/components/NumericInput.svelte
@src/lib/shared/components/SelectPicker.svelte
@src/lib/shared/components/ResultsDisplay.svelte
@src/lib/shared/components/AboutSheet.svelte
@src/lib/shared/components/DisclaimerModal.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Impeccable /critique and document findings</name>
  <files>.planning/phases/08-impeccable-critique-polish/critique-findings.md</files>
  <read_first>
    - .impeccable.md (design context for the critique)
    - src/app.css (current design tokens)
    - src/lib/shell/NavShell.svelte (navigation structure)
    - src/routes/+layout.svelte (layout structure)
  </read_first>
  <action>
1. Start the dev server: `pnpm dev` (run in background).
2. Use the Impeccable /critique skill to assess the live app. The critique should cover:
   - Visual hierarchy and layout
   - Typography and spacing
   - Color usage and contrast
   - Component consistency
   - Mobile and desktop viewpoints
   - Dark and light theme assessment
3. Take Playwright MCP screenshots of all key screens:
   - Morphine Wean calculator (default state, with results)
   - Formula calculator — Modified Formula mode (default, with results)
   - Formula calculator — Breast Milk Fortifier mode (default, with results)
   - Each screen in both light and dark mode
   - Each screen at mobile (375px) and desktop (1280px) widths
4. Document ALL findings in `critique-findings.md` organized by priority (P0 critical, P1 high, P2 medium, P3 low).
5. For each finding, note: the issue, which file(s) it affects, and the specific fix needed.
6. Record which follow-up Impeccable commands the critique suggests (e.g., /polish, /typeset, /arrange, /normalize).
  </action>
  <verify>
    <automated>test -f .planning/phases/08-impeccable-critique-polish/critique-findings.md && grep -c "^##" .planning/phases/08-impeccable-critique-polish/critique-findings.md</automated>
  </verify>
  <acceptance_criteria>
    - critique-findings.md exists with structured findings organized by priority level
    - Each finding includes the affected file(s) and specific fix description
    - Follow-up Impeccable commands are listed for Plan 08-02
    - Screenshots were taken of all key screens in both themes and both viewport sizes
  </acceptance_criteria>
  <done>Critique findings documented with prioritized recommendations and follow-up command list</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review critique findings before implementation</name>
  <files>.planning/phases/08-impeccable-critique-polish/critique-findings.md</files>
  <read_first>
    - .planning/phases/08-impeccable-critique-polish/critique-findings.md
  </read_first>
  <action>
Present critique-findings.md to the user for review. The user needs to:
1. Confirm P0/P1 findings will all be implemented
2. Decide which P2/P3 findings to implement vs defer
3. Approve the follow-up commands list for Plan 08-02
  </action>
  <what-built>Impeccable /critique has been run against the live app. All findings are documented in critique-findings.md with priority levels and specific fix recommendations.</what-built>
  <how-to-verify>
    1. Read `.planning/phases/08-impeccable-critique-polish/critique-findings.md`
    2. Review the P0/P1 findings — these will all be implemented
    3. Review the P2/P3 findings — confirm which should be implemented vs deferred
    4. Check that the follow-up commands list looks reasonable
  </how-to-verify>
  <verify>User has reviewed critique-findings.md and provided approval or modification instructions</verify>
  <acceptance_criteria>
    - User has reviewed all findings and approved the implementation plan
  </acceptance_criteria>
  <done>User approved critique findings for implementation</done>
  <resume-signal>Type "approved" to proceed with implementing all recommendations, or describe any findings to defer/modify</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Implement all critique recommendations</name>
  <files>
    src/app.css
    src/lib/shell/NavShell.svelte
    src/routes/+layout.svelte
    src/lib/morphine/MorphineWeanCalculator.svelte
    src/lib/formula/FormulaCalculator.svelte
    src/lib/formula/ModifiedFormulaCalculator.svelte
    src/lib/formula/BreastMilkFortifierCalculator.svelte
    src/lib/shared/components/NumericInput.svelte
    src/lib/shared/components/SelectPicker.svelte
    src/lib/shared/components/ResultsDisplay.svelte
    src/lib/shared/components/AboutSheet.svelte
    src/lib/shared/components/DisclaimerModal.svelte
  </files>
  <read_first>
    - .planning/phases/08-impeccable-critique-polish/critique-findings.md (the critique output with all recommendations)
    - .impeccable.md (design context to ensure fixes align with brand)
    - src/app.css (current tokens — likely needs updates)
    - All component files listed above (read each before modifying)
  </read_first>
  <action>
Implement every approved P0, P1, and P2 recommendation from critique-findings.md. Work through findings by priority:

1. **P0 (Critical):** Fix immediately. These are likely contrast failures, broken layouts, or accessibility regressions.
2. **P1 (High):** Fix all. These are visual hierarchy issues, spacing inconsistencies, or typography problems.
3. **P2 (Medium):** Fix all unless user deferred specific items in the checkpoint.

For each fix:
- Read the affected file first
- Apply the specific change described in the critique finding
- Verify the change doesn't break other components (check imports, shared styles)

After all fixes:
- Run `pnpm build` to verify no build errors
- Run `pnpm test` to verify all existing tests still pass
- Take fresh screenshots to confirm visual improvements

Commit changes grouped logically (e.g., all token changes together, all component spacing together) rather than one massive commit.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm build && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - All P0 and P1 recommendations from critique-findings.md are implemented
    - All P2 recommendations are implemented unless explicitly deferred by user
    - pnpm build succeeds with no errors
    - pnpm test passes (all existing tests)
    - No hardcoded color values introduced (all use design tokens)
  </acceptance_criteria>
  <done>All approved critique recommendations implemented, build passes, tests pass</done>
</task>

</tasks>

<verification>
- critique-findings.md exists with structured, prioritized findings
- All P0/P1/P2 fixes applied to codebase
- `pnpm build` succeeds
- `pnpm test` passes
- No visual regressions visible in screenshots
</verification>

<success_criteria>
- Impeccable /critique has been run and findings documented (UX-01)
- All actionable recommendations implemented in the codebase (UX-02)
- App builds and tests pass after all changes
- Follow-up command list is ready for Plan 08-02
</success_criteria>

<output>
After completion, create `.planning/phases/08-impeccable-critique-polish/08-01-SUMMARY.md`
</output>
