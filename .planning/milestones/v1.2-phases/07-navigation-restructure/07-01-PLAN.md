---
phase: 07-navigation-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shell/NavShell.svelte
  - src/routes/+layout.svelte
autonomous: true
requirements:
  - NAV-06
  - NAV-07
  - NAV-08
  - NAV-09

must_haves:
  truths:
    - "Mobile bottom nav contains only calculator tabs (Morphine Wean, Formula) filling full viewport width"
    - "Top title bar shows app name, info button, and theme toggle on both mobile and desktop"
    - "Info button opens the AboutSheet; theme toggle switches dark/light mode"
    - "Desktop top bar contains app name, calculator tabs, info button, and theme toggle"
  artifacts:
    - path: "src/lib/shell/NavShell.svelte"
      provides: "Restructured navigation with title bar + bottom tabs"
      contains: "NICU Assist"
    - path: "src/routes/+layout.svelte"
      provides: "Layout with title bar clearance"
      contains: "NavShell"
  key_links:
    - from: "src/lib/shell/NavShell.svelte"
      to: "$lib/shared/theme.svelte.js"
      via: "theme.toggle() call"
      pattern: "theme\\.toggle"
    - from: "src/lib/shell/NavShell.svelte"
      to: "AboutSheet"
      via: "aboutOpen state binding"
      pattern: "aboutOpen"
---

<objective>
Restructure NavShell so info and theme buttons live in a top title bar, and mobile bottom nav contains only calculator tabs at full width.

Purpose: Calculator tabs are the primary navigation for clinicians; giving them full bottom-bar width improves touch targets and visual clarity on mobile.
Output: Updated NavShell.svelte with title bar + slim bottom tabs, updated +layout.svelte for top bar clearance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/shell/NavShell.svelte
@src/lib/shell/registry.ts
@src/lib/shared/theme.svelte.ts
@src/lib/shared/components/AboutSheet.svelte
@src/routes/+layout.svelte
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/shell/registry.ts:
```typescript
export interface CalculatorEntry {
  id: string;
  label: string;
  href: string;
  icon: Component;
  description: string;
}
export const CALCULATOR_REGISTRY: readonly CalculatorEntry[];
```

From src/lib/shared/theme.svelte.ts:
```typescript
export const theme = {
  get current(): 'light' | 'dark';
  toggle(): void;
  // ...
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restructure NavShell with title bar and tab-only bottom nav</name>
  <files>src/lib/shell/NavShell.svelte</files>
  <read_first>src/lib/shell/NavShell.svelte, src/lib/shell/registry.ts, src/lib/shared/theme.svelte.ts</read_first>
  <action>
Rewrite NavShell.svelte with three layout regions. Keep all existing imports (page, CALCULATOR_REGISTRY, theme, Sun, Moon, Info, AboutSheet, CalculatorId). Keep the existing `aboutOpen` state and `activeCalculatorId` derived exactly as-is.

**1. Top title bar (always visible, both mobile and desktop):**
- A `header` element, sticky top-0, z-10, with `bg-[var(--color-surface)]` and `border-b border-[var(--color-border)]`.
- Left side: app name "NICU Assist" as a `span` with `font-semibold text-base text-[var(--color-text-primary)]`.
- Right side: info button + theme toggle button, same markup/classes as current desktop buttons (`icon-btn min-h-[48px] min-w-[48px]`), same onclick handlers.
- On desktop (md+), also include the calculator tab links between the app name and the action buttons, using `role="tablist"` on the containing div. Tab styling: same as current desktop tabs (flex items-center gap-2 px-4 py-3, border-b-2 active indicator using `--color-accent`, 48px min height).
- The desktop tabs should be hidden on mobile: wrap them in a `div` with class `hidden md:flex gap-2`.
- Use `flex items-center px-4 gap-2` on the header. A `flex-1` spacer pushes action buttons right.

**2. Mobile bottom tab bar (visible below md breakpoint only):**
- A `nav` element with `class="fixed bottom-0 left-0 right-0 md:hidden border-t border-[var(--color-border)] bg-[var(--color-surface)] pb-[env(safe-area-inset-bottom,0px)]"` and `aria-label="Calculator navigation"`.
- Inside: a single `div` with `role="tablist"` and `class="flex"`.
- Each calculator from CALCULATOR_REGISTRY renders as an `a` with `flex-1` (NO info or theme buttons here — per NAV-06, NAV-07). This makes tabs fill full width.
- Tab link classes: `flex flex-col items-center justify-center flex-1 gap-1 min-h-[48px] py-2 text-ui font-medium` with active/inactive color logic same as current.
- Each tab: `aria-label={calc.description}`, `aria-selected={isActive}`, `role="tab"`.

**3. AboutSheet:**
- Render `<AboutSheet calculatorId={activeCalculatorId} bind:open={aboutOpen} />` at the bottom, same as current.

**Key points:**
- The top header `aria-label` for the nav portion on desktop should be "Calculator navigation" (same as before).
- The top title bar itself should be a `header` (semantic), and the desktop tabs inside it should be in a `nav` with `aria-label="Calculator navigation"`.
- Do NOT add the Info or theme toggle buttons anywhere in the mobile bottom nav (per NAV-06).
- Ensure the top bar is visible on mobile too — it contains the app name, info, and theme toggle (per NAV-08).
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- grep "NICU Assist" src/lib/shell/NavShell.svelte returns a match (app name present)
- grep "Info" src/lib/shell/NavShell.svelte confirms info button exists (in title bar)
- grep "theme.toggle" src/lib/shell/NavShell.svelte confirms theme toggle exists (in title bar)
- The mobile bottom nav (`md:hidden`) contains NO Info button and NO theme toggle — grep for "Info" inside the bottom nav section shows zero matches
- grep "flex-1" in the tab links confirms calculator tabs fill full width
- grep "sticky top-0" confirms title bar is sticky
  </acceptance_criteria>
  <done>NavShell has a top title bar with app name + info + theme toggle visible on all viewports; mobile bottom nav has only calculator tabs filling full width; desktop top bar includes tabs inline; all existing functionality preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update layout for top title bar clearance and verify build</name>
  <files>src/routes/+layout.svelte</files>
  <read_first>src/routes/+layout.svelte</read_first>
  <action>
In +layout.svelte, the `main` element currently has `pb-20 md:pb-0` to clear the mobile bottom nav. The new top title bar is sticky so it does not need padding-top (content scrolls under it naturally with sticky positioning).

However, verify the layout still works:
1. The `NavShell` component is rendered BEFORE `main` — it already is. No change needed to ordering.
2. The bottom padding `pb-20` may be too large now since the bottom nav is slimmer (only tabs, no action buttons). Change `pb-20` to `pb-16` which provides enough clearance for the tab-only bottom bar (~64px).
3. Keep `md:pb-0` as desktop has no bottom nav.

After editing, run the full build to confirm no TypeScript or Svelte compilation errors:
```bash
cd /mnt/data/src/nicu-assistant && pnpm build
```

Then run the existing test suite:
```bash
cd /mnt/data/src/nicu-assistant && npx vitest run
```
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm build 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- pnpm build exits with code 0 (no compilation errors)
- vitest run shows all tests passing
- grep "pb-16" src/routes/+layout.svelte confirms updated bottom padding
  </acceptance_criteria>
  <done>Layout provides correct spacing for new nav structure; build succeeds; all existing tests pass.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. `npx vitest run` — all existing tests pass
3. NavShell.svelte contains "NICU Assist" app name in a sticky top header
4. Mobile bottom nav (md:hidden) contains ONLY calculator tab links — no Info, no theme toggle
5. Top header contains Info button and theme toggle on all viewports
6. Desktop view (md+) shows calculator tabs in the top header bar
</verification>

<success_criteria>
- NAV-06: Info and theme buttons are in top title bar, not bottom tab bar
- NAV-07: Calculator tabs fill full mobile bottom nav width (flex-1 with no competing elements)
- NAV-08: Top title bar shows app name + info + theme toggle on both mobile and desktop
- NAV-09: Desktop layout has all controls accessible in the top nav bar
- All existing tests continue to pass
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-navigation-restructure/07-01-SUMMARY.md`
</output>
