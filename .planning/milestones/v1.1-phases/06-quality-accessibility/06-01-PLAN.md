---
phase: 06-quality-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/morphine/calculations.test.ts
  - src/lib/morphine/__tests__/MorphineWeanCalculator.test.ts
autonomous: true
requirements:
  - QA-01

must_haves:
  truths:
    - "Linear mode spreadsheet parity: all 10 steps match Sheet1 reference values (weight=3.1, maxDose=0.04, decrease=10%)"
    - "Compounding mode spreadsheet parity: all 10 steps match Sheet2 reference values (weight=3.1, maxDose=0.08, decrease=10%)"
    - "Component renders mode tabs, input fields, and schedule cards"
    - "Existing 12 unit tests are not duplicated or broken"
  artifacts:
    - path: "src/lib/morphine/calculations.test.ts"
      provides: "Spreadsheet parity test suites appended to existing file"
      contains: "spreadsheet parity"
    - path: "src/lib/morphine/__tests__/MorphineWeanCalculator.test.ts"
      provides: "Component tests for MorphineWeanCalculator"
      contains: "MorphineWeanCalculator"
  key_links:
    - from: "src/lib/morphine/calculations.test.ts"
      to: "src/lib/morphine/calculations.ts"
      via: "import calculateLinearSchedule, calculateCompoundingSchedule"
      pattern: "import.*calculateLinearSchedule.*calculateCompoundingSchedule"
    - from: "src/lib/morphine/__tests__/MorphineWeanCalculator.test.ts"
      to: "src/lib/morphine/MorphineWeanCalculator.svelte"
      via: "import and render"
      pattern: "import.*MorphineWeanCalculator"
---

<objective>
Add spreadsheet parity unit tests for both morphine wean calculation modes and component-level tests for the MorphineWeanCalculator Svelte component.

Purpose: Verify calculation accuracy against clinical reference spreadsheet (QA-01) and confirm the UI component wires inputs, mode switching, and schedule rendering correctly (per D-01, D-02, D-03).
Output: Extended calculations.test.ts with parity suites + new MorphineWeanCalculator.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-quality-accessibility/06-CONTEXT.md

@src/lib/morphine/calculations.ts
@src/lib/morphine/calculations.test.ts
@src/lib/morphine/types.ts
@src/lib/morphine/morphine-config.json
@src/lib/morphine/MorphineWeanCalculator.svelte
@src/lib/shared/__tests__/shared-components.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spreadsheet parity test suites to calculations.test.ts</name>
  <files>src/lib/morphine/calculations.test.ts</files>
  <read_first>
    src/lib/morphine/calculations.test.ts
    src/lib/morphine/calculations.ts
    src/lib/morphine/morphine-config.json
    src/lib/morphine/types.ts
  </read_first>
  <action>
Append two new describe blocks to the EXISTING calculations.test.ts file (per D-03 — do not duplicate or modify the 12 existing tests).

**Block 1: `describe('Linear mode — Sheet1 spreadsheet parity')`**
Use inputs: weight=3.1, maxDose=0.04, decreasePct=0.10 (note: the function takes decimal 0.10, not integer 10).
Call `calculateLinearSchedule(3.1, 0.04, 0.10)` and assert all 10 steps match these exact Sheet1 values:

| Step | doseMg   | doseMgKgDose | reductionMg |
|------|----------|--------------|-------------|
| 1    | 0.124    | 0.04         | 0           |
| 2    | 0.1116   | 0.036        | 0.0124      |
| 3    | 0.0992   | 0.032        | 0.0124      |
| 4    | 0.0868   | 0.028        | 0.0124      |
| 5    | 0.0744   | 0.024        | 0.0124      |
| 6    | 0.062    | 0.02         | 0.0124      |
| 7    | 0.0496   | 0.016        | 0.0124      |
| 8    | 0.0372   | 0.012        | 0.0124      |
| 9    | 0.0248   | 0.008        | 0.0124      |
| 10   | 0.0124   | 0.004        | 0.0124      |

Write one `it` per step: `it('step N matches Sheet1 row', ...)` using `toBeCloseTo(value, 4)` for each field. This gives 10 tests.

**Block 2: `describe('Compounding mode — Sheet2 spreadsheet parity')`**
Use inputs: weight=3.1, maxDose=0.08, decreasePct=0.10 (per D-01: Sheet2 uses maxDose=0.08).
Call `calculateCompoundingSchedule(3.1, 0.08, 0.10)` and assert all 10 steps match these exact Sheet2 values:

| Step | doseMg   | doseMgKgDose | reductionMg |
|------|----------|--------------|-------------|
| 1    | 0.248    | 0.08         | 0           |
| 2    | 0.2232   | 0.072        | 0.0248      |
| 3    | 0.2009   | 0.0648       | 0.0223      |
| 4    | 0.1808   | 0.0583       | 0.0201      |
| 5    | 0.1627   | 0.0525       | 0.0181      |
| 6    | 0.1465   | 0.0472       | 0.0163      |
| 7    | 0.1318   | 0.0425       | 0.0146      |
| 8    | 0.1186   | 0.0383       | 0.0132      |
| 9    | 0.1068   | 0.0344       | 0.0119      |
| 10   | 0.0961   | 0.031        | 0.0107      |

Write one `it` per step using `toBeCloseTo(value, 4)`. This gives 10 more tests.

Use `toBeCloseTo(expected, 4)` (4 decimal places) throughout — the spreadsheet values are rounded to 4 places.

Do NOT modify or remove any existing test code above the new blocks.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && npx vitest run src/lib/morphine/calculations.test.ts --reporter=verbose 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "spreadsheet parity" src/lib/morphine/calculations.test.ts returns 2 (two describe blocks)
    - grep -c "step.*matches Sheet" src/lib/morphine/calculations.test.ts returns 20 (10 per mode)
    - npx vitest run src/lib/morphine/calculations.test.ts exits 0 with all 32 tests passing (12 existing + 20 new)
    - No existing test names are changed or removed
  </acceptance_criteria>
  <done>All 32 tests pass: 12 existing + 10 linear parity + 10 compounding parity. Spreadsheet values verified row-by-row.</done>
</task>

<task type="auto">
  <name>Task 2: Add component tests for MorphineWeanCalculator</name>
  <files>src/lib/morphine/__tests__/MorphineWeanCalculator.test.ts</files>
  <read_first>
    src/lib/morphine/MorphineWeanCalculator.svelte
    src/lib/morphine/state.svelte.ts
    src/lib/shared/__tests__/shared-components.test.ts
    src/lib/morphine/types.ts
    src/test-setup.ts
  </read_first>
  <action>
Create a new component test file at `src/lib/morphine/__tests__/MorphineWeanCalculator.test.ts` (per D-02). Follow the same pattern as `src/lib/shared/__tests__/shared-components.test.ts` — use `@testing-library/svelte` render + screen, and `vitest` describe/it/expect.

The component uses `morphineState` (a Svelte 5 runes-based singleton) internally. Before each test, ensure `morphineState` is initialized/reset so state does not leak between tests. Import `morphineState` from `$lib/morphine/state.svelte.js` and call `morphineState.reset()` in `beforeEach`.

**Tests to write (per D-02):**

1. **Mode tabs render**: After render, both "Linear" and "Compounding" tab buttons exist with `role="tab"`. Default active mode is "linear" (aria-selected="true").

2. **Mode switching**: Click the "Compounding" tab button. Assert it becomes aria-selected="true" and the "Linear" tab becomes aria-selected="false".

3. **Input fields render**: Three NumericInput fields render with labels "Dosing weight", "Max morphine dose", "Decrease per step". Each has role="spinbutton".

4. **Schedule renders with valid inputs**: Set morphineState values (weightKg=3.1, maxDoseMgKgDose=0.04, decreasePct=10), render component, and verify that "Step 1" text appears in the document (schedule is visible). Use `waitFor` from @testing-library/svelte if needed for reactivity.

5. **Empty state shown without inputs**: With default null values, the placeholder text "Enter values above to generate weaning schedule." is visible.

6. **Tablist has correct ARIA label**: The element with `role="tablist"` has `aria-label="Weaning mode"`.

Note: If rendering fails due to the Svelte 5 runes + morphineState singleton, mock `morphineState` with `vi.mock('$lib/morphine/state.svelte.js', ...)` providing a simple reactive object. Document the approach in a comment.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && npx vitest run src/lib/morphine/__tests__/MorphineWeanCalculator.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/lib/morphine/__tests__/MorphineWeanCalculator.test.ts
    - grep "MorphineWeanCalculator" src/lib/morphine/__tests__/MorphineWeanCalculator.test.ts finds import
    - grep "role.*tab" src/lib/morphine/__tests__/MorphineWeanCalculator.test.ts confirms tab role assertions
    - npx vitest run src/lib/morphine/__tests__/MorphineWeanCalculator.test.ts exits 0 with at least 5 tests passing
  </acceptance_criteria>
  <done>Component tests pass covering mode tabs, input rendering, schedule display, empty state, and ARIA attributes.</done>
</task>

</tasks>

<verification>
Run full morphine test suite:
```bash
npx vitest run src/lib/morphine/ --reporter=verbose
```
All tests pass (existing 12 + 20 parity + 6 component tests = ~38 total).
</verification>

<success_criteria>
- 20 spreadsheet parity tests pass (10 linear, 10 compounding) with exact row-by-row value matches
- 6+ component tests pass for MorphineWeanCalculator UI behavior
- Original 12 calculation tests remain untouched and passing
- No test duplication between existing and new test suites
</success_criteria>

<output>
After completion, create `.planning/phases/06-quality-accessibility/06-01-SUMMARY.md`
</output>
