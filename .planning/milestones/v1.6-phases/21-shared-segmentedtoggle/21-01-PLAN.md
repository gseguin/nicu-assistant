---
phase: 21
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shared/components/SegmentedToggle.svelte
  - src/lib/shared/components/SegmentedToggle.test.ts
  - src/lib/shared/index.ts
autonomous: true
requirements: [TOG-01, TOG-02, TOG-03, A11Y-03]
must_haves:
  truths:
    - "SegmentedToggle component exists in src/lib/shared/components/ and is exported from $lib/shared"
    - "Active segment renders with var(--color-identity) so it inherits route identity hue"
    - "Keyboard nav works: ArrowLeft/ArrowRight cycle, Home/End jump, Space/Enter activate"
    - "ARIA semantics use role=tablist + role=tab matching Morphine's existing pattern"
    - "Component tests cover keyboard nav, active class, and bind:value writeback"
  artifacts:
    - path: "src/lib/shared/components/SegmentedToggle.svelte"
      provides: "Generic SegmentedToggle<T extends string> component"
      contains: "role=\"tablist\""
    - path: "src/lib/shared/components/SegmentedToggle.test.ts"
      provides: "Vitest unit tests T-01..T-07"
      contains: "ArrowRight"
    - path: "src/lib/shared/index.ts"
      provides: "Named export SegmentedToggle"
      contains: "SegmentedToggle"
  key_links:
    - from: "src/lib/shared/index.ts"
      to: "src/lib/shared/components/SegmentedToggle.svelte"
      via: "export { default as SegmentedToggle }"
      pattern: "SegmentedToggle"
---

<objective>
Build the shared `SegmentedToggle` component as a near-verbatim 1:1 lift of the Morphine tablist (markup + handlers) into a generic Svelte 5 component, export it from `$lib/shared`, and write component tests covering keyboard nav and active styling.

Purpose: Provide one canonical 2–N option toggle that both Morphine (Linear/Compounding) and Formula (Breast milk/Formula) will consume in Plan 21-02. Eliminates the "two patterns for one decision" inconsistency from /critique P1.

Output: Component file, test file, export line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-shared-segmentedtoggle/21-RESEARCH.md
@src/lib/shared/components/SelectPicker.svelte
@src/lib/shared/components/SelectPicker.test.ts
@src/lib/shared/index.ts
@src/lib/morphine/MorphineWeanCalculator.svelte

<interfaces>
SelectPicker prop shape (mirror this for SegmentedToggle):
```ts
let { label, value = $bindable(), options, class: className = '' } = $props();
```

Lift source — MorphineWeanCalculator.svelte lines 187-209 (markup), 105-148 (handlers).

Component API to implement (from RESEARCH §Component API, copy verbatim):
```ts
<script lang="ts" generics="T extends string">
  interface SegmentedOption { value: T; label: string; }
  let {
    label,
    value = $bindable(),
    options,
    ariaLabel,
    class: className = '',
  }: {
    label: string;
    value: T;
    options: SegmentedOption[];
    ariaLabel?: string;
    class?: string;
  } = $props();
</script>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SegmentedToggle.svelte component</name>
  <files>src/lib/shared/components/SegmentedToggle.svelte, src/lib/shared/index.ts</files>
  <read_first>
    - .planning/phases/21-shared-segmentedtoggle/21-RESEARCH.md (§Component API, §Markup, §Anti-Patterns, §Pitfalls 1, 4, 5)
    - src/lib/shared/components/SelectPicker.svelte (lines 7-26 — prop shape + uid pattern to mirror)
    - src/lib/morphine/MorphineWeanCalculator.svelte (lines 8-12, 105-148, 187-209 — extraction source)
    - src/lib/shared/index.ts (current export list)
  </read_first>
  <behavior>
    - Renders one button per option with role="tab"
    - Active button has aria-selected="true", tabindex=0, and class fragment text-[var(--color-identity)]
    - Inactive buttons have aria-selected="false", tabindex=-1
    - Container has role="tablist" and aria-label (from ariaLabel ?? label)
    - Click on a button writes value via $bindable
    - ArrowRight/ArrowLeft cycle (wrap), Home -> first, End -> last; new tab gets focus + becomes active
    - Space/Enter on a focused tab activates it
    - No aria-controls (intentionally dropped per Pitfall 1)
  </behavior>
  <action>
    Create `src/lib/shared/components/SegmentedToggle.svelte` as a generic Svelte 5 component using `<script lang="ts" generics="T extends string">`.

    Copy the script body and markup VERBATIM from RESEARCH.md §Component API (lines 62-122) and §Markup (lines 135-157). Do not re-derive — RESEARCH is prescriptive.

    Key requirements (per RESEARCH):
    1. Define inline `interface SegmentedOption { value: T; label: string; }`
    2. Props: `label: string`, `value: T` (with `= $bindable()`), `options: SegmentedOption[]`, `ariaLabel?: string`, `class?: string` (aliased as `className`)
    3. `const uid = crypto.randomUUID();` (matches SelectPicker.svelte:26)
    4. `function activate(next: T) { value = next; }`
    5. `handleKeydown(event, current)`: ArrowRight/ArrowLeft wrap-around via modulo, Home -> 0, End -> length-1, Space/Enter -> activate(current). Use `event.preventDefault()` on each handled key. After cycling, call `activate(nextValue)` then `document.getElementById(\`${uid}-tab-${nextValue}\`)?.focus()`.
    6. Markup: outer `<div role="tablist" aria-label={ariaLabel ?? label}>` with classes `flex p-1 bg-[var(--color-surface-alt)] rounded-2xl shadow-inner border border-[var(--color-border)] {className}`
    7. Inside `{#each options as option (option.value)}` with `{@const active = value === option.value}`
    8. Each `<button type="button" role="tab" aria-selected={active} id="{uid}-tab-{option.value}" tabindex={active ? 0 : -1}>` with classes: `flex-1 flex items-center justify-center gap-2 py-3 rounded-xl transition-all font-semibold text-ui outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-identity)] {active ? 'bg-[var(--color-surface-card)] text-[var(--color-identity)] shadow-sm' : 'text-[var(--color-text-secondary)] hover:text-[var(--color-identity)] hover:bg-[var(--color-surface)]'}`
    9. Button content: `<span>{option.label}</span>`
    10. Handlers: `onclick={() => activate(option.value)}` and `onkeydown={(e) => handleKeydown(e, option.value)}`

    DO NOT include `aria-controls` (Pitfall 1 — Morphine's current dangling reference is fixed by omission).
    DO NOT switch ARIA semantics to role=group/aria-pressed (Anti-pattern — TOG-06 risk).

    After creating the component, add the export to `src/lib/shared/index.ts` after the existing `SelectPicker` export line (around line 7):
    ```
    export { default as SegmentedToggle } from './components/SegmentedToggle.svelte';
    ```
    Place it alphabetically or grouped with other component exports — match the existing file's convention.
  </action>
  <acceptance_criteria>
    - `test -f src/lib/shared/components/SegmentedToggle.svelte` exits 0
    - `grep -q 'role="tablist"' src/lib/shared/components/SegmentedToggle.svelte`
    - `grep -q 'role="tab"' src/lib/shared/components/SegmentedToggle.svelte`
    - `grep -q 'generics="T extends string"' src/lib/shared/components/SegmentedToggle.svelte`
    - `grep -q '\$bindable()' src/lib/shared/components/SegmentedToggle.svelte`
    - `grep -q 'crypto.randomUUID' src/lib/shared/components/SegmentedToggle.svelte`
    - `grep -q 'var(--color-identity)' src/lib/shared/components/SegmentedToggle.svelte`
    - `grep -c 'ArrowRight\|ArrowLeft\|Home\|End' src/lib/shared/components/SegmentedToggle.svelte` returns >= 4
    - `grep -qv 'aria-controls' src/lib/shared/components/SegmentedToggle.svelte` (must NOT contain aria-controls)
    - `grep -q 'SegmentedToggle' src/lib/shared/index.ts`
    - `npx svelte-check` reports no new errors in the new file
  </acceptance_criteria>
  <verify>
    <automated>npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep -i "SegmentedToggle" || echo "no segmentedtoggle errors"</automated>
  </verify>
  <done>Component file exists with verbatim lift, exports from $lib/shared/index.ts, svelte-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write SegmentedToggle component tests (T-01..T-07)</name>
  <files>src/lib/shared/components/SegmentedToggle.test.ts</files>
  <read_first>
    - .planning/phases/21-shared-segmentedtoggle/21-RESEARCH.md (§Validation Architecture — Phase Requirements → Test Map, Test patterns to mirror)
    - src/lib/shared/components/SelectPicker.test.ts (full file — mirror T-05/T-06 keyboard pattern)
    - src/lib/shared/components/SegmentedToggle.svelte (the component just created)
  </read_first>
  <behavior>
    - T-01: Renders a tablist with N option buttons; each button has correct label text
    - T-02: Active option's button className contains `text-[var(--color-identity)]`; inactive does not
    - T-03: ArrowRight on focused active tab moves selection + focus to next tab; wraps from last to first
    - T-04: ArrowLeft cycles backwards; wraps from first to last
    - T-05: Home jumps selection + focus to first; End jumps to last
    - T-06: Space and Enter activate the focused tab (no-op if already active, but handler runs)
    - T-07: Selecting via click writes value back to a parent $state via bind:value
  </behavior>
  <action>
    Create `src/lib/shared/components/SegmentedToggle.test.ts` using Vitest 4 + @testing-library/svelte. Mirror the structure of `SelectPicker.test.ts` (imports, describe block, test naming `T-NN`).

    Use a small wrapper component pattern (or `mount` with reactive props) to test `bind:value` writeback. Two-option fixture is sufficient for most tests; use a three-option fixture for the wrap-around tests to make ArrowLeft from index 0 -> index 2 unambiguous.

    Test list (mirrors RESEARCH §Phase Requirements → Test Map):

    - T-01: Render with `options=[{value:'a',label:'Alpha'},{value:'b',label:'Beta'}]` and `value='a'`. Assert: `getByRole('tablist')` exists, `getAllByRole('tab')` length === 2, button text matches labels.
    - T-02: Same setup. Assert active button's `className` string contains `text-[var(--color-identity)]`; inactive button's className does NOT contain that fragment. Use string match on `className`, NOT computed style (JSDOM doesn't resolve CSS custom properties — see RESEARCH §Test patterns).
    - T-03: Three-option fixture `[a,b,c]`, value=`a`. Get the active tab, fire `keyDown` with `{ key: 'ArrowRight' }`. Assert: tab `b` now has `aria-selected="true"` and is `document.activeElement`. Fire ArrowRight twice more; assert wrap to `a`.
    - T-04: Three-option fixture, value=`a`. Fire ArrowLeft on active tab. Assert wrap to `c`.
    - T-05: Three-option fixture, value=`b`. Fire Home -> assert `a` active + focused. Fire End -> assert `c` active + focused.
    - T-06: Two-option fixture, value=`a`. Focus tab `b` programmatically (or fire keyDown on it). Fire keyDown with `{ key: ' ' }` then `{ key: 'Enter' }`. Assert `b` becomes active.
    - T-07: Use a wrapper Svelte component with `let value = $state('a')` bound via `bind:value` to SegmentedToggle. Click the second tab. Assert wrapper's value updated to `'b'` (read via exposed getter or assert on rendered DOM).

    Use `fireEvent.keyDown(element, { key: 'ArrowRight' })` from `@testing-library/svelte`. Assert `document.activeElement` for focus checks.

    Do NOT mock `crypto.randomUUID` — confirmed working in JSDOM per Pitfall 5.
  </action>
  <acceptance_criteria>
    - `test -f src/lib/shared/components/SegmentedToggle.test.ts` exits 0
    - `grep -c "test\|it(" src/lib/shared/components/SegmentedToggle.test.ts` returns >= 7
    - `grep -q "T-01" src/lib/shared/components/SegmentedToggle.test.ts`
    - `grep -q "T-07" src/lib/shared/components/SegmentedToggle.test.ts`
    - `grep -q "ArrowRight" src/lib/shared/components/SegmentedToggle.test.ts`
    - `grep -q "Home\|End" src/lib/shared/components/SegmentedToggle.test.ts`
    - `npx vitest run src/lib/shared/components/SegmentedToggle.test.ts` — all tests pass
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/shared/components/SegmentedToggle.test.ts</automated>
  </verify>
  <done>Test file exists with T-01..T-07, all tests pass, covers TOG-01/02/03 + A11Y-03.</done>
</task>

</tasks>

<verification>
- `npx svelte-check` clean
- `npx vitest run src/lib/shared/components/SegmentedToggle.test.ts` green
- `grep -q 'SegmentedToggle' src/lib/shared/index.ts`
</verification>

<success_criteria>
- SegmentedToggle.svelte exists, exports from $lib/shared, generic-typed
- Active state uses var(--color-identity)
- Keyboard nav (←/→/Home/End/Space/Enter) works and is tested
- Component tests T-01..T-07 all pass
- TOG-01, TOG-02, TOG-03, A11Y-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/21-shared-segmentedtoggle/21-01-SUMMARY.md`
</output>
</content>
</invoke>