---
phase: 15-shell-nav-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/shell/NavShell.svelte
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03]
must_haves:
  truths:
    - "Title bar matches .impeccable.md: refined spacing, app name weight, icon-button visible focus"
    - "Bottom tab bar and desktop top nav have refined active/hover/focus-visible states via OKLCH tokens"
    - "All interactive surfaces have ≥48px touch targets and safe-area-aware padding"
    - "NavShell.test.ts continues to pass (source greps unchanged)"
  artifacts:
    - path: "src/lib/shell/NavShell.svelte"
      provides: "Polished shell chrome (title bar + desktop nav + mobile tab bar)"
      contains: "focus-visible:ring"
---

<objective>
Polish the app shell: title bar, desktop top nav, and mobile bottom tab bar. Refine spacing, typography, active/hover/focus-visible states, and ensure every interactive surface hits ≥48px with OKLCH tokens only. AboutSheet markup itself is NOT touched (still uses bits-ui Dialog; full port is deferred per v1.3 decision) — we only harmonize how it's invoked from the shell.

No edits to: `registry.ts`, `theme.svelte.ts`, `AboutSheet.svelte`, `DisclaimerModal.svelte`.
</objective>

<context>
@src/lib/shell/NavShell.svelte
@src/lib/shell/NavShell.test.ts
@src/lib/shell/registry.ts
@src/lib/shared/theme.svelte.ts
@.impeccable.md

Test contract from NavShell.test.ts (all assertions are source-string greps):
- Source contains `sticky top-0`, `z-10`, `NICU Assist`, `fixed bottom-0`, `hidden md:flex`
- The HEADER section (before `<!-- Mobile bottom tab bar -->`) contains `About this calculator` and `theme.toggle`
- The BOTTOM NAV section (after `<!-- Mobile bottom tab bar -->`) does NOT contain `About this calculator` or `theme.toggle`
- Bottom nav section contains `flex-1` and `z-10`

All of these substrings must be preserved verbatim.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Title bar polish — spacing, typography, icon-button focus-visible</name>
  <files>src/lib/shell/NavShell.svelte</files>
  <read_first>
    - src/lib/shell/NavShell.svelte (lines 16-74 — header block)
    - src/lib/shell/NavShell.test.ts (source-grep assertions)
    - src/app.css (`.icon-btn` base class — has hover but no focus-visible ring)
  </read_first>
  <action>
In the `<header>` block (lines 17-74):

1. Change the `<header>` padding and min-height:
   - Add `min-h-14` to the existing classes (ensures ~56px bar, well above 48px touch targets without crowding icon buttons).
   - Keep `sticky top-0 left-0 right-0 z-10 flex items-center px-4 gap-2 border-b border-[var(--color-border)] bg-[var(--color-surface)]`.

2. Change the app name span:
   - From: `class="font-semibold text-base text-[var(--color-text-primary)]"`
   - To:   `class="font-semibold text-base tracking-tight text-[var(--color-text-primary)]"`
   - Keep the literal text `NICU Assist` (NavShell.test.ts line 19).

3. Desktop calculator tab anchors (inside `<nav class="hidden md:flex ...">`):
   - Append `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--color-accent)] rounded-t-lg` to the class list (rounded top for the focus ring to look crisp with the bottom border).
   - Keep existing `border-b-2 transition-colors` and the active / inactive ternary untouched.

4. Icon buttons (Info + theme toggle):
   - Keep `icon-btn min-h-[48px] min-w-[48px]` base.
   - Wrap each icon button in NOTHING new — just ensure the `icon-btn` class (in `app.css`) already has a focus-visible state. It does (box-shadow ring via `--color-accent-light`). No JS change needed.
   - DO NOT remove the `onclick={() => aboutOpen = true}` handler or the literal string `About this calculator` — the test greps for it.
   - DO NOT remove `theme.toggle` literal source.

5. The `<div class="flex items-center gap-0.5">` wrapping the two icon buttons stays as-is.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm vitest run src/lib/shell/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'min-h-14' src/lib/shell/NavShell.svelte` matches the header element
    - `grep -n 'NICU Assist' src/lib/shell/NavShell.svelte` still returns exactly 1 match
    - `grep -n 'tracking-tight' src/lib/shell/NavShell.svelte` matches the app-name span
    - `grep -c 'About this calculator' src/lib/shell/NavShell.svelte` equals 1
    - `grep -c 'theme.toggle' src/lib/shell/NavShell.svelte` equals 1
    - `grep -n 'focus-visible:outline-\[var(--color-accent)\]' src/lib/shell/NavShell.svelte` matches on desktop nav anchor
    - `pnpm vitest run src/lib/shell/` — all tests pass
  </acceptance_criteria>
  <done>Title bar has calmer spacing and visible focus on desktop tab anchors; all shell tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile bottom tab bar polish — active weight, hover/focus, min-h-14 rows</name>
  <files>src/lib/shell/NavShell.svelte</files>
  <read_first>
    - src/lib/shell/NavShell.svelte (lines 76-102 — bottom nav block)
    - src/lib/shell/NavShell.test.ts (bottom nav contract: `fixed bottom-0`, `z-10`, `flex-1`, no `About this calculator`, no `theme.toggle`)
  </read_first>
  <action>
Within the mobile `<nav>` block (lines 77-102):

1. Keep existing outer `<nav>` classes verbatim (test asserts `fixed bottom-0`, `z-10`, `md:hidden`, `pb-[env(safe-area-inset-bottom,0px)]`).

2. The inner anchor class list currently reads:
   ```
   flex flex-col items-center justify-center flex-1 gap-1
   min-h-[48px] py-2 text-ui font-medium
   {isActive ? 'text-[var(--color-accent)]' : 'text-[var(--color-text-secondary)]'}
   ```
   Change to:
   ```
   flex flex-col items-center justify-center flex-1 gap-1
   min-h-14 py-2 text-ui font-medium transition-colors
   focus-visible:outline-2 focus-visible:outline-offset-[-2px] focus-visible:outline-[var(--color-accent)]
   {isActive ? 'text-[var(--color-accent)] font-semibold' : 'text-[var(--color-text-secondary)] hover:text-[var(--color-text-primary)]'}
   ```
   - `min-h-14` (56px) gives more bedside thumb room while staying token-driven.
   - `transition-colors` calms the active-state handoff.
   - `focus-visible:outline-…` adds a visible keyboard focus ring via OKLCH accent.
   - Active state gets `font-semibold` on top of the accent color (clearer active state, matches the morphine mode tab style).
   - Inactive state gets `hover:text-[var(--color-text-primary)]` (desktop mouse users interacting via devtools or pointer coarse testing still get a hover affordance).

3. DO NOT remove `flex-1` (test asserts it).
4. DO NOT add `About this calculator` or `theme.toggle` to the bottom nav section (test asserts their absence).
5. DO NOT restructure the `{#each CALCULATOR_REGISTRY as calc}` loop.
  </action>
  <verify>
    <automated>cd /mnt/data/src/nicu-assistant && pnpm vitest run src/lib/shell/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'min-h-14' src/lib/shell/NavShell.svelte` returns ≥ 2 matches (header + bottom nav anchor)
    - `grep -n 'font-semibold' src/lib/shell/NavShell.svelte` matches the bottom nav active-state ternary
    - `grep -n 'focus-visible:outline-offset-\[-2px\]' src/lib/shell/NavShell.svelte` matches the bottom nav anchor
    - `grep -c 'flex-1' src/lib/shell/NavShell.svelte` ≥ 2 (header spacer + bottom nav anchors)
    - Bottom nav section (after `<!-- Mobile bottom tab bar -->` comment) does NOT contain `About this calculator` or `theme.toggle`
    - `pnpm vitest run src/lib/shell/` — all tests pass
    - `grep -Eq "#[0-9a-fA-F]{3,6}|rgb\(|rgba\(" src/lib/shell/NavShell.svelte` returns NON-ZERO (no hardcoded colors)
  </acceptance_criteria>
  <done>Mobile tab bar has calmer rhythm, explicit focus ring, and an active-state weight bump — all source-grep tests still pass.</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run src/lib/shell/` — green
2. `pnpm vitest run` — full suite green
3. `pnpm build` — green
4. `git diff --stat` — only `NavShell.svelte` modified
5. `grep -rn "from 'bits-ui'" src/` — unchanged (AboutSheet + DisclaimerModal still use Dialog)
</verification>

<success_criteria>
- SHELL-01: Title bar refined spacing, typography (`tracking-tight`), and icon-button focus — matches `.impeccable.md`.
- SHELL-02: Desktop top nav + mobile bottom tab bar have refined active, hover, and focus-visible states via OKLCH tokens. No hardcoded colors.
- SHELL-03: AboutSheet and theme toggle are invoked from a cohesive title bar; their internal markup is unchanged (deferred per v1.3 bits-ui decision).
- All shell tests pass unchanged.
- ≥48 px touch targets preserved (min-h-14 = 56 px, well above).
</success_criteria>

<output>
After completion, create `.planning/phases/15-shell-nav-polish/15-01-SUMMARY.md`.
</output>
