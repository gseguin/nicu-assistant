---
phase: 4
plan: 5
type: execute
wave: 5
workstream: pert
milestone: v1.15
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - src/lib/pert/PertCalculator.svelte
  - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
requirements:
  - PERT-DESIGN-02
  - PERT-DESIGN-03
  - PERT-DESIGN-04
  - PERT-DESIGN-05
autonomous: false
gap_closure: true
tags: [pert, design-polish, gap-closure, wave-5, oral-hierarchy-inversion, prescribing-artifact-leads, d-12, d-13]
must_haves:
  truths:
    - "D-12 amendment of Phase 2 D-09 shipped (visual-hierarchy aspect only): in `src/lib/pert/PertCalculator.svelte` the Oral-mode `Estimated daily total (3 meals/day)` tertiary row at lines ~211-227 in HEAD (verify exact line numbers fresh via `grep -n 'Estimated daily total' src/lib/pert/PertCalculator.svelte`) gets a 3-token swap that promotes it to the prescribing-artifact treatment: line ~216 eyebrow class `tracking-wide` -> `tracking-wider`; line ~221 numeral class `text-base` -> `text-title`; line ~221 numeral class `font-medium` -> `font-extrabold`. The HTML comment at line ~211 is updated to cite D-12 (supersedes Phase 2 D-09 visual treatment) + D-13 (Prescribing-Artifact-Leads Rule cross-mode codification) instead of the original `D-09 verbatim` framing. The calculation, the visible text `Estimated daily total (3 meals/day)`, the numeral expression `{oralResult.estimatedDailyTotal}`, the suffix `capsules`, the row's structural placement (below the 2 secondary rows, above advisories) are ALL byte-identical -- the change is purely typographic on the row's eyebrow + numeral classes."
    - "D-13 codification of the Prescribing-Artifact-Leads Rule: this plan ships the Oral-mode half of the cross-mode treatment that Wave 4 shipped for Tube-Feed. After Wave 5, both prescribing artifacts (Tube-Feed `Capsules per month` line 288/293 + Oral `Estimated daily total` line ~216/~221) carry the SAME both-vectors typographic delta (eyebrow `tracking-wider` AND numeral `font-extrabold`). The cross-mode parity is the structural correction encoded by D-13 (workstream-local rule for v1.15; DESIGN.md update deferred to v1.16 design-contract-refinement phase). Watch Item 5 from 04-UI-SPEC.md generalizes from Tube-Feed only to BOTH PERT modes."
    - "Identity-Inside Rule (DESIGN.md line 186 + Phase 1 D-03 + Phase 1 D-17) honored: the Oral tertiary eyebrow color stays `text-[var(--color-identity)]` (NEVER changed); the numeral color stays `text-[var(--color-text-primary)]` (NEVER changed). The 3-token swap is purely typographic (letter-spacing + font-size + font-weight on existing class slots) -- NOT chromatic. The eyebrow stays identity-purple inside the route; chrome (top tabs, bottom nav, hamburger) is untouched."
    - "Pitfall 4 selector preservation honored: the visible text `Estimated daily total (3 meals/day)` is preserved verbatim (no rename, no abbreviation, no parenthetical change). The numeral expression `{oralResult.estimatedDailyTotal}` is preserved verbatim (no format change, no rounding tweak, no `.toLocaleString` insertion). The suffix `capsules` is preserved verbatim. PertCalculator.test.ts asserts on the visible text via `screen.getByText('Estimated daily total (3 meals/day)')` at lines 65 and 77 -- those selectors continue to match without any test-side change. e2e/pert.spec.ts does NOT assert on this row's value; no e2e selector update needed."
    - "D-08 PERT-route allowlist honored: the only file edits in this plan's commits are `src/lib/pert/PertCalculator.svelte` (allowed) and the planning artifact `04-05-SUMMARY.md` at the phase directory. Strictly forbidden per D-08b: shared components (`src/lib/shared/components/**` including HeroResult, SegmentedToggle, SelectPicker, NumericInput, RangedNumericInput, InputDrawer, DisclaimerBanner, AboutSheet), shell (`src/lib/shell/NavShell.svelte`, `src/lib/shell/HamburgerMenu.svelte`), other calculators (`src/lib/{morphine-wean,formula,gir,feeds,uac-uvc}/**`), `DESIGN.md`, `DESIGN.json`, calc-layer (`src/lib/pert/calculations.ts`, `src/lib/pert/state.svelte.ts`, `src/lib/pert/types.ts`, `src/lib/pert/config.ts`), `src/lib/pert/PertInputs.svelte` (Phase 3.1 frozen), `src/lib/pert/pert-config.json` (Phase 4 commit 2dc7ae2 frozen), `e2e/pert.spec.ts` and `e2e/pert-a11y.spec.ts` (Pitfall 4 selector preservation means no e2e edit needed), any test file (PertCalculator.test.ts asserts on the verbatim visible text which is preserved; no test edit needed)."
    - "Phase 3.1 invariants preserved per UI-SPEC §'PERT Component Invariants': function-binding bridges intact at 3 hits in `src/lib/pert/PertInputs.svelte` (medication / strength / formula at the post-Phase-3.1 multi-line `bind:value={` shape; this plan does NOT touch PertInputs.svelte); zero string-bridge proxies (`grep -cE 'let \\w+(Bridge|Proxy) = \\$state' src/lib/pert/PertInputs.svelte` returns 0); em-dash count 0 (`grep -cP '\\x{2014}' src/lib/pert/PertCalculator.svelte` returns 0); en-dash count 0 (`grep -cP '\\x{2013}' src/lib/pert/PertCalculator.svelte` returns 0); STOP-red advisory carve-out preserved (`border-2 border-[var(--color-error)]` + `OctagonAlert` + `role=\"alert\"` block at lines ~310-322 byte-identical -- Wave 5's edit is at lines ~211-227 which is upstream of the STOP-red block in the divide-y stack)."
    - "Wave 4 Tube-Feed Approach C treatment preserved byte-identical: the Tube-Feed `Capsules per month` row eyebrow at line 288 stays `tracking-wider` (Wave 4 commit `d82aa30`); the numeral at line 293 stays `font-extrabold` (Wave 2 commit `29306e7`). After Wave 5 ships, `grep -c 'tracking-wider'` returns exactly 2 (the Tube-Feed eyebrow at line 288 + the new Oral eyebrow at line ~216) and `grep -c 'font-extrabold'` returns exactly 2 (the Tube-Feed numeral at line 293 + the new Oral numeral at line ~221). The Wave 4 Approach C COMBINED RECIPE on the Tube-Feed prescribing artifact is byte-identical -- this plan ships the parallel COMBINED RECIPE on the Oral prescribing artifact."
    - "Human-eye visual UAT gate (D-13 audit-both-modes corrective process insertion) replaces the LLM-Design-Review fallback verdict from Phase 4 closure. The 04-VERIFICATION.md fallback overlooked the Oral hierarchy inversion entirely; Wave 4's checkpoint:human-verify only inspected Tube-Feed (because that's where F-03 originated) and never explicitly looked at Oral. This plan's Task 3 is the corrective gate: human-eye review of the rendered Oral mode at `/pert` after the fix ships, with the D-13 audit-both-modes principle called out explicitly so future re-runs propagate it. Minimum coverage: light-desktop-oral (the surfaced-issue context from 04-UAT.md test #4). Recommended coverage: light + dark x mobile + desktop x oral (4 contexts). The user must explicitly type `approved` or describe residual issues at the checkpoint task. NO automated DOM inspection alone, NO LLM-generated /impeccable score, NO substitute LLM-Design-Review pass can replace the human-eye gate. This is the lesson from 04-UAT.md test #4 + the Wave 4 SUMMARY's audit-trail observation that the Wave 4 human-verify only checked one mode encoded as a hard process gate."
    - "Quality gates: `pnpm run check` returns 0 errors / 0 warnings (Phase 3.1 + Phase 4 baseline preserved, ~4586 files); `pnpm exec vitest run src/lib/pert/` returns 81/81 (the PERT vitest subset: PertCalculator.test.ts 10 + PertInputs.test.ts 9 + calculations.test.ts 45 + config.test.ts 17 = 81; matches Phase 3.1 + Phase 4 baseline because no test files are touched and the edit is a Tailwind class-string change that does not affect any DOM-asserted selector -- PertCalculator.test.ts:58-77 asserts on the VISIBLE TEXT `Estimated daily total (3 meals/day)` which is byte-identical, NOT on the class string); em-dash + en-dash sweep on PertCalculator.svelte returns 0 hits each via `grep -cP '\\x{2014}'` and `grep -cP '\\x{2013}'`; `grep -cE 'class=\"[^\"]*\\bnum\\b' src/lib/pert/PertCalculator.svelte` returns at least 5 (UI-SPEC Gate 12; baseline = 9, unchanged because the `.num` class slot on the Oral tertiary numeral `<span>` is preserved -- only the size/weight tokens within the same class string change)."
    - "Negative-space audit at plan close: `git diff --name-only HEAD~1..HEAD -- src/ e2e/` returns ONLY `src/lib/pert/PertCalculator.svelte` (the D-12 commit). No other source/test/config touched. The PERT-route allowlist boundary (`src/lib/pert/**` + `src/routes/pert/**`) holds. After this plan ships, the cumulative phase diff (`git diff --name-only $PHASE_BASELINE..HEAD -- src/ e2e/` where $PHASE_BASELINE is the parent of Wave 1 commit `eec18c2`) returns: `src/lib/pert/PertCalculator.svelte` (Wave 2 F-03 numeral + Wave 4 Approach C eyebrow + Wave 5 D-12 Oral 3-token swap, all font-weight/tracking/size only) + `src/lib/pert/pert-config.json` (commit 2dc7ae2 weight-range alignment) + `src/lib/shared/about-content.ts` (commit 5cd3386 PERT-entry-only xlsx removal, D-08a-style amendment). All within D-08 allowlist or pre-acknowledged D-08a-style amendments."
    - "PERT-DESIGN-02..05 re-validation handled by re-running `/gsd-verify-work 4 --ws pert` after this plan ships (NOT by flipping the REQUIREMENTS.md traceability table back to Active in this plan). The verification command will re-check the existing 04-UAT.md test #4 gap row against the post-D-12 UI; if visual UAT passes, the gap row is marked resolved and PERT-DESIGN-02..05 stay Validated. This plan does NOT touch REQUIREMENTS.md."
    - "D-13 future-audit forward note: any post-Phase-4 design-polish sweep (or any future calculator polish phase) MUST explicitly inspect the prescribing artifact in BOTH modes side-by-side. The 8-context critique sweep already covers this combinatorially in principle, but human-verify checkpoints must explicitly call out both rows by name (`Capsules per month` AND `Estimated daily total (3 meals/day)`). The 04-05-SUMMARY.md Task 4 records this rule verbatim so the next gap-closure pickup propagates it."
    - "Verbatim user-visual verdict captured in 04-05-SUMMARY.md (D-13 audit-trail requirement): the SUMMARY's `Human-visual UAT outcome` section records the user's resume signal verbatim (single-token `approved` OR description of residual issues), the contexts the user reviewed (minimum: light-desktop-oral; recommended: 4 contexts including dark + mobile), and the outcome's effect on the 04-UAT.md gap row test #4 (resolved if `approved`; escalation-required note + next-plan recommendation if not)."
  artifacts:
    - path: src/lib/pert/PertCalculator.svelte
      provides: "Post-D-12-amendment component: Oral-mode `Estimated daily total (3 meals/day)` tertiary row eyebrow `<div>` (line ~216) bumped from `tracking-wide` to `tracking-wider` AND numeral `<span>` (line ~221) bumped from `text-base font-medium` to `text-title font-extrabold`. The 3-token swap promotes the Oral prescribing artifact to the same both-vectors typographic treatment as the Tube-Feed `Capsules per month` row shipped by Wave 4. The HTML comment at line ~211 is updated from `Tertiary line per D-09 verbatim. text-2xs eyebrow + text-base value (smaller than secondaries).` to a citation that supersedes Phase 2 D-09 (visual treatment only) per Phase 4 D-12 + D-13 (Prescribing-Artifact-Leads Rule). The 7 sibling `tracking-wide` eyebrows BELOW are untouched (page subtitle line 149 + the 2 Oral secondary-row eyebrows at lines 181 + 198 + the 4 Tube-Feed eyebrows at lines 237/254/271/288 of which line 288 was already flipped to `tracking-wider` by Wave 4). Verified by `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` returning exactly 2 (Tube-Feed eyebrow line 288 from Wave 4 + Oral eyebrow line ~216 from Wave 5) AND `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` returning exactly 2 (Tube-Feed numeral line 293 from Wave 2 + Oral numeral line ~221 from Wave 5) AND `grep -c 'text-title' src/lib/pert/PertCalculator.svelte` increasing from 7 to 8 (Oral numeral added) AND `grep -c 'text-base'` dropping from 2 to 1 AND `grep -c 'font-medium'` dropping from 2 to 1."
      contains: "Estimated daily total (3 meals/day)"
      contains: "oralResult.estimatedDailyTotal"
      contains: "tracking-wider"
      contains: "font-extrabold"
      contains: "text-title"
      contains: "D-12"
      contains: "D-13"
    - path: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      provides: "Wave 5 (gap-closure) closure record: D-12 Oral hierarchy promotion shipped; before/after class strings for the eyebrow `<div>` AND numeral `<span>` pasted verbatim; D-12 amendment of Phase 2 D-09 documented (visual-treatment-aspect-only supersedure rationale; original D-09 carried under different assumption per 04-UAT.md test #4 conflict explanation); D-13 Prescribing-Artifact-Leads Rule codified for v1.15 (workstream-local; DESIGN.md update deferred to v1.16); cross-mode parity confirmed (Tube-Feed `Capsules per month` Wave 4 treatment + Oral `Estimated daily total` Wave 5 treatment now structurally identical); F-03 numeral preservation confirmed (grep count 1 -> 2; the original F-03 numeral fix at line 293 byte-identical and joined by the new Oral numeral at line ~221); Wave 4 Approach C eyebrow preservation confirmed (line 288 byte-identical; joined by the new Oral eyebrow at line ~216); plan-level negative-space audit clean (only PertCalculator.svelte modified); Phase 3.1 invariants regression-guard pasted (function-binding 3 hits, em + en-dash 0/0, tabular-numerals 9 unchanged); user-visual-UAT outcome recorded (verbatim resume signal `approved` OR escalation-required note); D-13 audit-both-modes forward-context note recorded; PERT-DESIGN-02..05 re-validation note (re-run /gsd-verify-work 4 --ws pert after this plan ships; do NOT flip REQUIREMENTS.md from this plan)."
      contains: "D-12"
      contains: "D-13"
      contains: "Prescribing-Artifact-Leads"
      contains: "Estimated daily total"
      contains: "Capsules per month"
      contains: "tracking-wider"
      contains: "font-extrabold"
      contains: "text-title"
      contains: "Pitfall 4"
      contains: "Identity-Inside"
      contains: "approved"
      min_lines: 50
  key_links:
    - from: src/lib/pert/PertCalculator.svelte
      to: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-UAT.md
      via: "the gap row in 04-UAT.md test #4 (`Oral mode prescribing-artifact hierarchy parity with Tube-Feed`) cites the 3-token swap as the canonical recipe; this plan applies that exact fix; the post-fix verification command `/gsd-verify-work 4 --ws pert` re-checks the gap row against the rendered UI"
      pattern: "Estimated daily total"
    - from: src/lib/pert/PertCalculator.svelte
      to: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-CONTEXT.md
      via: "04-CONTEXT.md D-12 (gap-closure decision; lines 124-125) authorizes this plan to amend Phase 2 D-09 for the visual-hierarchy aspect only; D-13 (lines 127-128) codifies the Prescribing-Artifact-Leads Rule that this plan ships in code"
      pattern: "D-12"
    - from: src/lib/pert/PertCalculator.svelte
      to: .planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-CONTEXT.md
      via: "the Oral tertiary row's HTML comment is updated from `D-09 verbatim` to a D-12/D-13 citation; the calculation, label, value, and structural placement defined by Phase 2 D-09 are byte-identical (only the visual treatment is amended per D-12)"
      pattern: "D-09"
    - from: src/lib/pert/PertCalculator.svelte
      to: src/lib/pert/PertCalculator.svelte
      via: "cross-mode visual parity: post-Wave-5, the Oral row at lines ~213-227 and the Tube-Feed row at lines ~285-299 share the same both-vectors typographic treatment (eyebrow `tracking-wider` + numeral `text-title font-extrabold`) -- both prescribing artifacts read at the same hierarchy weight regardless of mode; the 3 derived rows in each mode (Total lipase needed + Lipase per dose for Oral; Total fat + Total lipase needed + Lipase per kg for Tube-Feed) recede with the lighter `tracking-wide` + `text-title font-bold` treatment"
      pattern: "tracking-wider"
    - from: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      to: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-UAT.md
      via: "SUMMARY's user-visual-UAT outcome section records the approval (or escalation-required note) verbatim; the verifier reads SUMMARY to know whether to mark the 04-UAT.md test #4 gap row resolved or open the next escalation rung"
      pattern: "user-visual-UAT"
    - from: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      to: DESIGN.md
      via: "SUMMARY records that D-13 (Prescribing-Artifact-Leads Rule) lives as a workstream-local rule for v1.15; DESIGN.md update is deferred to v1.16 design-contract-refinement phase per the existing Deferred Ideas section in 04-CONTEXT.md (so DESIGN.md.line 186 Identity-Inside Rule paragraph stays byte-identical for now); the SUMMARY records the future-DESIGN.md-update note for the next planner pickup"
      pattern: "D-13"
commit_message: "fix(pert/04-05): D-12 Oral daily-total promoted to prescribing-artifact treatment (D-13 cross-mode rule)"
---

<objective>
**Wave 5 of Phase 4 -- gap closure for the Oral hierarchy inversion surfaced by human UAT test #4.**

Phase 4 was certified Complete on 2026-04-26 (Wave 3 plan 04-03 closed; PERT-DESIGN-01..06 flipped Active -> Validated; aggregate score 36.25/40 across 8 LLM-Design-Review FINAL critique transcripts). Wave 4 then shipped the F-03 Approach C eyebrow `tracking-wider` bump on Tube-Feed `Capsules per month` after human UAT test #1 contradicted the LLM verdict (commits `d82aa30` + `78686f9`; user resume signal `approved`). However, when the user ran human UAT test #4 against the Oral-mode UI on 2026-04-26, they reported a SECOND clinical-trust regression: the Oral `Estimated daily total (3 meals/day)` tertiary row reads SMALLER and LIGHTER (`text-base font-medium`, 16px / weight 500) than the per-dose derived figures above it (Total lipase needed + Lipase per dose, both `text-title font-bold`, 22px / weight 700) -- a hierarchy INVERSION where the prescribing artifact for daily-script + parent-education workflows is visually de-emphasized below its own derived per-dose figures.

**This issue was missed by:**
1. **Wave 1 LLM-Design-Review fallback** (8 critique transcripts; 4 in Oral mode contexts) -- never flagged the Oral tertiary as a prescribing-artifact-hierarchy issue. The screenshot-driven LLM critique reported DOM-level structural correctness without registering the perceptual gap.
2. **Wave 4 checkpoint:human-verify** (light-desktop-tube-feed coverage) -- the corrective process insertion DID work for Tube-Feed F-03 but only inspected ONE mode. Oral mode was never explicitly looked at because that's not where F-03 originated.

**The gap-closure decision was authored 2026-04-26 in 04-CONTEXT.md as D-12 + D-13:**

- **D-12** authorizes amending Phase 2 LOCKED decision D-09 (which prescribed `text-2xs uppercase eyebrow + text-base value with class="num"`, "smaller than secondary outputs which are text-title") for the visual-hierarchy aspect ONLY. The calculation (`oralResult.estimatedDailyTotal`), the label (`Estimated daily total (3 meals/day)`), the cardinality (the row is the third / tertiary line in the Oral secondaries section), and the structural placement (below the 2 secondary rows, above advisories) are all UNCHANGED. The amendment is purely the typographic treatment: numeral `text-title font-extrabold` (22px / weight 800) + eyebrow `tracking-wider` (0.55px) -- mirroring the Tube-Feed `Capsules per month` Wave 4 treatment.

- **D-13** codifies the Prescribing-Artifact-Leads Rule as a Phase 4 cross-mode design rule for v1.15: any PERT secondary-card stack's prescribing artifact (the value a clinician orders against -- `Capsules per month` in Tube-Feed; `Estimated daily total` in Oral) MUST read visually distinct from derived siblings via the both-vectors typographic delta (eyebrow `tracking-wider` + numeral `font-extrabold`; numeral size matches secondaries at `text-title`, NOT the hero `text-display`). The audit gate: any post-Phase-4 design-polish sweep MUST explicitly inspect the prescribing artifact in BOTH Oral and Tube-Feed modes. The rule lives as a workstream-local rule for now; DESIGN.md update is deferred to v1.16 design-contract-refinement phase per the existing Deferred Ideas in 04-CONTEXT.md.

**Why a Wave-5 gap-closure plan inside Phase 4 (not a Phase 4.2 / Phase 6):** the fix scope is identical to Wave 4's structural template (single-token-class swap on the same source file; PERT-route only; ~3 LOC change vs Wave 4's ~1 LOC; D-08 allowlist honored; no e2e selector change; no test edit; Phase 3.1 + Phase 4 invariants preserved). It is the literal Oral-mode parallel of the Wave 4 Tube-Feed treatment. Phase 5 release-readiness is unblocked by this fix landing; Phase 4 closure record (04-VERIFICATION.md) acknowledged the LLM-Design-Review fallback overlooked Wave 4's issue, and now the human-UAT loop has surfaced the second issue that was missed by both the LLM fallback AND Wave 4's mode-asymmetric human-verify -- D-13's audit-both-modes principle is the structural correction.

**This plan ships exactly two things:**

1. **D-12 Oral hierarchy promotion** (Task 1 + Task 2 + Task 3): a 3-token swap on the Oral tertiary row of `src/lib/pert/PertCalculator.svelte` (Oral branch, lines ~213-227). The 3 token swaps are: line ~216 eyebrow class `tracking-wide` -> `tracking-wider`; line ~221 numeral class `text-base` -> `text-title`; line ~221 numeral class `font-medium` -> `font-extrabold`. The HTML comment at line ~211 is updated to cite D-12 (supersedes Phase 2 D-09 visual treatment) + D-13 (Prescribing-Artifact-Leads Rule cross-mode codification) instead of the original `Tertiary line per D-09 verbatim. text-2xs eyebrow + text-base value (smaller than secondaries).` framing. The bump is typographic only (letter-spacing + font-size + font-weight) -- NEVER chromatic (Identity-Inside Rule reserves eyebrow color for `text-[var(--color-identity)]`). The visible text `Estimated daily total (3 meals/day)`, the numeral expression `{oralResult.estimatedDailyTotal}`, and the `capsules` suffix are all PRESERVED -- PertCalculator.test.ts:65 + 77 selectors via `screen.getByText('Estimated daily total (3 meals/day)')` and e2e selectors still match (Pitfall 4).

2. **Human-eye visual UAT checkpoint** (Task 3): a `checkpoint:human-verify` task that asks the user to navigate to `/pert` in the rendered Oral mode and confirm by eye whether the new typographic treatment (extrabold + text-title + tracking-wider) makes the Estimated daily total row read as the visually-leading row of the Oral secondaries section -- distinct from and ABOVE in hierarchy weight the 2 derived per-dose figures (Total lipase needed + Lipase per dose). The user types `approved` if the gap is closed, OR describes residual issues if more escalation is needed. Per D-13, the checkpoint's `<how-to-verify>` block calls out the audit-both-modes principle explicitly so the user (and any future executor reading this plan) propagates it forward: review BOTH Oral `Estimated daily total` AND Tube-Feed `Capsules per month` side-by-side in the same UAT pass.

3. **04-05-SUMMARY.md** (Task 4): the Wave 5 closure record. Documents (a) the D-12 amendment line range + exact before/after class strings + verify-gate output; (b) the D-13 Prescribing-Artifact-Leads Rule codification (workstream-local for v1.15; DESIGN.md update deferred to v1.16); (c) the cross-mode parity confirmation (Tube-Feed Wave 4 + Oral Wave 5 prescribing-artifact treatments structurally identical post-fix); (d) the human-UAT outcome (`approved` or escalation-required note); (e) the verbatim user-visual verdict (D-13 audit-trail requirement); (f) the D-13 future-audit forward note (next sweep MUST inspect both modes); (g) the PERT-DESIGN-02..05 re-validation note (re-run `/gsd-verify-work 4 --ws pert` after this plan ships; do NOT flip REQUIREMENTS.md from this plan).

**This plan does NOT:**

- Touch `src/lib/pert/PertInputs.svelte` (Phase 3.1 frozen; function-binding bridges + em-dash + en-dash count 0 + zero string-bridge proxies must hold).
- Touch `src/lib/pert/pert-config.json` (Phase 4 commit `2dc7ae2` frozen).
- Touch `src/lib/pert/calculations.ts`, `src/lib/pert/state.svelte.ts`, `src/lib/pert/types.ts`, `src/lib/pert/config.ts` (calc-layer + state shape are Phase 2/3.1 frozen).
- Touch `src/lib/pert/PertCalculator.test.ts` (the test asserts on the verbatim visible text `Estimated daily total (3 meals/day)` which is byte-identical post-fix; Pitfall 4 selector preservation makes the test pass without modification).
- Touch any shared component, NavShell, HamburgerMenu, other calculators, DESIGN.md, DESIGN.json (D-08b strictly forbidden). DESIGN.md update for the Prescribing-Artifact-Leads Rule is explicitly deferred to v1.16 per 04-CONTEXT.md Deferred Ideas + D-13.
- Touch `e2e/pert.spec.ts` or `e2e/pert-a11y.spec.ts` (Pitfall 4: D-12 changes letter-spacing + font-size + font-weight only, not text content; e2e doesn't even assert on the daily-total value; ZERO selector update permitted).
- Re-touch the Wave 4 Tube-Feed Approach C treatment (line 288 eyebrow `tracking-wider` from commit `d82aa30` byte-identical; line 293 numeral `font-extrabold` from commit `29306e7` byte-identical). This plan ADDS the Oral parallel; it does not modify the Tube-Feed treatment.
- Re-run the 8-context `/impeccable` critique sweep -- the LLM-Design-Review fallback was the source of the false-PASS at Phase 4 closure for both F-03 and the Oral hierarchy inversion; this plan replaces that gate with human-eye UAT (the same corrective process Wave 4 instituted, now extended to BOTH modes per D-13).
- Update STATE.md / ROADMAP.md / REQUIREMENTS.md -- those updates are the orchestrator's responsibility after `/gsd-verify-work 4 --ws pert` re-runs and confirms the gap closed.
- Apply any visual-isolation escalation (background tint, top divider, icon prefix) on the Oral row. The 3-token swap to mirror Tube-Feed's both-vectors typographic delta is the canonical recipe per 04-UAT.md test #4 options + D-12 + D-13. If post-Wave-5 human UAT records the typographic delta is STILL insufficient (Approach C-equivalent for Oral didn't bridge the perceptual gap as it did for Tube-Feed), the next planner pickup runs `/gsd-plan-phase 4 --gaps --ws pert` for an Approach D / E equivalent on the Oral row -- one approach at a time per Wave-N gap-closure plan, NOT chained.

**Per 04-UAT.md test #4 acceptance:** post-fix, the Oral `Estimated daily total (3 meals/day)` row reads with the same prescribing-artifact treatment as Tube-Feed `Capsules per month` (numeral `text-title font-extrabold` + eyebrow `tracking-wider`); the hierarchy inversion is eliminated; the prescribing artifact reads at hierarchy weight EQUAL TO OR ABOVE the 2 derived per-dose figures (Total lipase needed + Lipase per dose) above it. Human UAT either confirms PASS (gap closed) OR records that the typographic delta is still insufficient -- in which case the next escalation is authored as plan 04-06 by re-running `/gsd-plan-phase 4 --gaps --ws pert`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/pert/STATE.md
@.planning/workstreams/pert/ROADMAP.md
@.planning/workstreams/pert/REQUIREMENTS.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-CONTEXT.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-DISCUSSION-LOG.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-UAT.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-UI-SPEC.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-VERIFICATION.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-PLAN.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
@.planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-CONTEXT.md
@DESIGN.md
@CLAUDE.md
@src/lib/pert/PertCalculator.svelte
@src/lib/pert/PertCalculator.test.ts

<interfaces>
<!-- Pre-extracted code excerpts for the D-12 edit site. Captured from HEAD post-Wave-4 (commit `d82aa30` shipped Approach C eyebrow on Tube-Feed; this plan ships the parallel COMBINED RECIPE on the Oral tertiary row). The executor reads PertCalculator.svelte fresh and verifies line numbers at edit time, since pre-edit line numbers may shift if any unrelated commit lands first. -->

CURRENT (Oral `Estimated daily total (3 meals/day)` tertiary row at lines 211-227 in src/lib/pert/PertCalculator.svelte HEAD; comment at lines 211-212; eyebrow `<div>` at lines 215-219; numeral `<span>` at lines 221-223):
```svelte
				<!-- Tertiary line per D-09 verbatim.
				     text-2xs eyebrow + text-base value (smaller than secondaries). -->
				<div class="flex items-baseline justify-between px-5 py-4">
					<div>
						<div
							class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase"
						>
							Estimated daily total (3 meals/day)
						</div>
						<div class="flex items-baseline gap-2">
							<span class="num text-base font-medium text-[var(--color-text-primary)]">
								{oralResult.estimatedDailyTotal}
							</span>
							<span class="text-ui text-[var(--color-text-secondary)]">capsules</span>
						</div>
					</div>
				</div>
```

Note the current state is byte-identical with Phase 2 D-09 verbatim treatment. No prior Phase 4 wave touched this row -- Wave 1 (verification only), Wave 2 (Tube-Feed numeral only), Wave 3 (verification only), Wave 4 (Tube-Feed eyebrow only) all left it alone. This plan is the FIRST edit to this row in Phase 4.

POST-FIX (D-12: 3-token swap on the Oral tertiary row; comment updated to cite D-12 + D-13):
```svelte
				<!-- Prescribing-artifact line per D-12 (supersedes Phase 2 D-09 visual treatment).
				     Same shape as Tube-Feed Capsules per month per D-13 Prescribing-Artifact-Leads Rule. -->
				<div class="flex items-baseline justify-between px-5 py-4">
					<div>
						<div
							class="text-2xs font-semibold tracking-wider text-[var(--color-identity)] uppercase"
						>
							Estimated daily total (3 meals/day)
						</div>
						<div class="flex items-baseline gap-2">
							<span class="num text-title font-extrabold text-[var(--color-text-primary)]">
								{oralResult.estimatedDailyTotal}
							</span>
							<span class="text-ui text-[var(--color-text-secondary)]">capsules</span>
						</div>
					</div>
				</div>
```

Diff: 3 token swaps inside the class attributes + 2 comment lines updated.
- Eyebrow `<div>` class: `tracking-wide` -> `tracking-wider` (1 token swap; letter-spacing 0.275px -> 0.55px).
- Numeral `<span>` class: `text-base` -> `text-title` (1 token swap; font-size 16px -> 22px).
- Numeral `<span>` class: `font-medium` -> `font-extrabold` (1 token swap; font-weight 500 -> 800).
- Comment lines 211-212: updated to cite D-12 + D-13 instead of D-09.

The visible text `Estimated daily total (3 meals/day)`, the numeral expression `{oralResult.estimatedDailyTotal}`, the `capsules` suffix, the `class="num"` slot on the numeral `<span>`, the `text-[var(--color-identity)]` color slot on the eyebrow, and the `text-[var(--color-text-primary)]` color slot on the numeral are ALL byte-identical -- only the size + weight + tracking tokens within the existing class strings change.

The Tube-Feed `Capsules per month` row at lines 284-299 is BYTE-IDENTICAL post-Wave-5 (Wave 4 commit `d82aa30` eyebrow `tracking-wider` at line 288 + Wave 2 commit `29306e7` numeral `font-extrabold` at line 293). The 7 sibling `tracking-wide` eyebrows (page subtitle line 149 + 2 Oral secondary-row eyebrows lines 181 + 198 + 4 Tube-Feed sibling-row eyebrows lines 237/254/271 -- 6 total post-Wave-5 because Wave 5 flips line ~216 from `tracking-wide` to `tracking-wider`) are UNTOUCHED. The bump is asymmetric BY DESIGN: only the prescribing artifacts (Tube-Feed `Capsules per month` line 288 + Oral `Estimated daily total` line ~216) get the wider tracking; the page subtitle and the 6 derived-row eyebrows stay at `tracking-wide`.

FORBIDDEN: any change to the eyebrow's `text-[var(--color-identity)]` color token (Identity-Inside Rule per DESIGN.md line 186 + Phase 1 D-03 + Phase 1 D-17). FORBIDDEN: any change to the numeral's `text-[var(--color-text-primary)]` color token. FORBIDDEN: any change to the visible text `Estimated daily total (3 meals/day)` (PertCalculator.test.ts:65 + 77 use `screen.getByText('Estimated daily total (3 meals/day)')`; e2e selectors stay match-stable per Pitfall 4). FORBIDDEN: any change to the numeral expression `{oralResult.estimatedDailyTotal}` (preserved verbatim; this is the same expression rendered in the Oral happy path -- though e2e/pert.spec.ts does NOT assert on this row's value, the calculation contract is byte-identical with Phase 2 D-15..D-18 + Phase 3 calculations.test.ts parity matrix). FORBIDDEN: any change to the `class="num"` slot on the numeral `<span>` (UI-SPEC Gate 12 tabular-numerals contract; the slot stays exactly `class="num text-title font-extrabold ..."` post-fix). FORBIDDEN: re-touching the Tube-Feed `Capsules per month` row (lines 284-299 byte-identical; Wave 4 + Wave 2 treatments preserved).

Pre-fix grep counts (verify before editing):
- `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` returns 1 (the Tube-Feed eyebrow at line 288 from Wave 4)
- `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` returns 1 (the Tube-Feed numeral at line 293 from Wave 2)
- `grep -c 'text-title' src/lib/pert/PertCalculator.svelte` returns 7 (the page subtitle "PERT Dosing" header at line 144 uses `text-title font-black`; the 6 secondary-row numerals at lines 186/203/242/259/276/293 use `text-title font-bold` or `font-extrabold`)
- `grep -c 'text-base' src/lib/pert/PertCalculator.svelte` returns 2 (Oral tertiary numeral at line 221 with `text-base font-medium` AND one other internal usage; the executor confirms by reading the file fresh)
- `grep -c 'font-medium' src/lib/pert/PertCalculator.svelte` returns 2 (Oral tertiary numeral at line 221 + one secondary-text utility usage; executor confirms by reading the file fresh)
- `grep -oE 'tracking-wide(r|[^a-z]|"|$)' src/lib/pert/PertCalculator.svelte | grep -v 'tracking-wider' | wc -l` returns 7 (page subtitle line 149 + 2 Oral secondary-row eyebrows lines 181 + 198 + Oral tertiary eyebrow line ~216 -- the row Wave 5 will flip + 3 Tube-Feed sibling-row eyebrows lines 237 + 254 + 271; Wave 4 already flipped line 288 to `tracking-wider`)

Post-fix grep counts (verify after editing):
- `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` returns 2 (Tube-Feed eyebrow line 288 from Wave 4 byte-identical + new Oral eyebrow line ~216 from Wave 5)
- `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` returns 2 (Tube-Feed numeral line 293 from Wave 2 byte-identical + new Oral numeral line ~221 from Wave 5)
- `grep -c 'text-title' src/lib/pert/PertCalculator.svelte` returns 8 (was 7 pre-fix; +1 because the Oral tertiary numeral was `text-base` and is now `text-title`)
- `grep -c 'text-base' src/lib/pert/PertCalculator.svelte` drops by 1 (the Oral tertiary numeral was `text-base` and is now `text-title`; the count goes from 2 to 1)
- `grep -c 'font-medium' src/lib/pert/PertCalculator.svelte` drops by 1 (the Oral tertiary numeral was `font-medium` and is now `font-extrabold`; the count goes from 2 to 1)
- `grep -oE 'tracking-wide(r|[^a-z]|"|$)' src/lib/pert/PertCalculator.svelte | grep -v 'tracking-wider' | wc -l` returns 6 (was 7 pre-fix; -1 because the Oral tertiary eyebrow was `tracking-wide` and is now `tracking-wider`)
- `grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte` returns 9 (UNCHANGED; the Oral tertiary numeral `<span>` keeps `class="num ..."`; only the size/weight tokens within the same class string change)

NOTE: `tracking-wide` is a substring of `tracking-wider`; the bare `grep -c 'tracking-wide'` form would over-count (returns post-fix tracking-wider hits + tracking-wide-only hits = 2 + 6 = 8 for Wave 5, vs post-Wave-4 baseline 1 + 7 = 8 -- same total, masking the swap). Use the disambiguating word-boundary form `grep -oE 'tracking-wide(r|[^a-z]|"|$)' file | grep -v 'tracking-wider' | wc -l` for tracking-wide-only counts (the Wave 4 plan documented this anti-pattern at its `<interfaces>` lines 192-196; this plan re-uses the same disambiguating form).

The verification commands in Task 1 use `grep -oE 'tracking-wide(r|[^a-z]|"|$)' | grep -v "tracking-wider" | wc -l` to disambiguate the two tokens cleanly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply D-12 Oral hierarchy promotion -- 3-token swap on Estimated daily total row in PertCalculator.svelte</name>
  <read_first>
    - src/lib/pert/PertCalculator.svelte (read in full; verify the actual line range for the Oral `Estimated daily total (3 meals/day)` tertiary row -- citing target lines 211-227 from HEAD with the comment at lines 211-212, the eyebrow `<div>` opening at line 215 + class line at line 216 + visible text at line 218 + closing `</div>` at line 219, and the numeral `<span>` opening at line 221 + numeral expression at line 222; the executor must confirm at edit time since the file may have shifted if any unrelated commit landed since this plan was authored)
    - DESIGN.md (offset 180, limit 35 -- "The Identity-Inside Rule" section at line 186; the eyebrow color token `var(--color-identity)` is reserved for inside-the-route surfaces and the D-12 amendment must NOT alter the eyebrow color)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-CONTEXT.md (offset 91, limit 11 -- D-08 allowlist; the only allowed source/test edit in this task is `src/lib/pert/PertCalculator.svelte`. ALSO offset 123, limit 10 -- D-12 + D-13 gap-closure decisions that authorize this plan)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-UAT.md (read in full; the Gaps section test #4 documents the Oral hierarchy inversion + the 3-token-swap canonical recipe; the verbatim user request is the contract this plan implements)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-PLAN.md (offset 130, limit 70 -- the Wave 4 `<interfaces>` block documents Approach C verbatim for the Tube-Feed row; this Wave 5 plan ships the parallel both-vectors recipe on the Oral row; the verb-by-verb structural mirror is the safety net)
    - .planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-CONTEXT.md (offset 92, limit 8 -- Phase 2 D-09 source decision being amended; show this is a deliberate locked-decision change per D-12, not a silent override)
    - src/lib/pert/PertCalculator.test.ts (offset 55, limit 25 -- the Oral tertiary `getByText` selectors at lines 65 + 77 verifying that the visible text `Estimated daily total (3 meals/day)` is the assertion target; Pitfall 4 selector preservation requires the visible text byte-identical; the executor confirms the test is class-string-blind so the typographic swap doesn't break the test)
    - e2e/pert.spec.ts (skim the Oral happy-path test; confirm there is NO selector assertion on the daily-total row's value -- it asserts on the hero `capsulesPerDose=4` numeral. Pitfall 4 protection on this row lives in PertCalculator.test.ts only)
  </read_first>
  <files>src/lib/pert/PertCalculator.svelte</files>
  <action>
**STEP A -- Re-read the actual line range.** The plan-cited range is lines 211-227 for the Oral tertiary row but the executor must confirm by reading PertCalculator.svelte fresh. Use:

```
grep -n "Estimated daily total" src/lib/pert/PertCalculator.svelte
```

Expected: 1 hit on the visible-text line (line 218 in HEAD pre-fix). The eyebrow `<div>` opening tag with the class string is at line 215; the class line is at line 216 (the line carrying `tracking-wide`); the visible text is at line 218; the numeral `<span>` opening with the class string is at line 221 (the line carrying `text-base font-medium`). If line numbers have shifted, proceed with the actual range -- the task targets:
- The eyebrow `<div>` containing the class string `text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase` immediately above the `Estimated daily total (3 meals/day)` visible text.
- The numeral `<span>` containing the class string `num text-base font-medium text-[var(--color-text-primary)]` immediately above the `{oralResult.estimatedDailyTotal}` expression.
- The HTML comment `Tertiary line per D-09 verbatim.` (and its continuation line `text-2xs eyebrow + text-base value (smaller than secondaries).`) immediately above the row's outer `<div class="flex items-baseline justify-between px-5 py-4">` opening tag.

**STEP B -- Verify the pre-edit baseline.** Run these greps to confirm the file is in the expected state before editing:

```
grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte
```
Expected: `1` (Wave 4's Tube-Feed eyebrow at line 288 from commit `d82aa30`). If 0, Wave 4's Approach C has been reverted -- STOP and escalate; do NOT proceed. If 2+, this plan has already partly run -- STOP and inspect; the executor may have re-entered after a partial edit.

```
grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte
```
Expected: `1` (Wave 2's Tube-Feed numeral at line 293 from commit `29306e7`). If 0, Wave 2's F-03 fix has been reverted -- STOP and escalate. If 2+, this plan has already partly run -- STOP and inspect.

```
grep -oE 'tracking-wide(r|[^a-z]|"|$)' src/lib/pert/PertCalculator.svelte | grep -v "tracking-wider" | wc -l
```
Expected: `7` (7 eyebrow `tracking-wide` hits across the file: 1 page subtitle at line ~149 + 6 secondary-row eyebrows at lines ~181, 198, 216, 237, 254, 271). If different, the file has drifted from the plan-authoring baseline -- pause and reconcile.

```
grep -c 'text-base' src/lib/pert/PertCalculator.svelte
```
Expected: `2` (the Oral tertiary numeral on line 221 + one other usage). The exact count matters less than the post-fix delta (must drop by exactly 1). Capture the pre-fix value for the post-fix delta check.

```
grep -c 'font-medium' src/lib/pert/PertCalculator.svelte
```
Expected: `2` (the Oral tertiary numeral on line 221 + one other usage). Capture the pre-fix value for the post-fix delta check.

```
grep -c 'text-title' src/lib/pert/PertCalculator.svelte
```
Expected: `7`. Capture the pre-fix value for the post-fix delta check (must increase by exactly 1).

**STEP C -- Apply the edit.** Use the Edit tool. The specific edit replaces the entire Oral tertiary block (comment + outer div) so the 3 token swaps + 2 comment-line updates land atomically:

Old text (must match exactly; the disambiguating context is the visible text `Estimated daily total (3 meals/day)` and the numeral expression `oralResult.estimatedDailyTotal` -- both unique in the file):

```
				<!-- Tertiary line per D-09 verbatim.
				     text-2xs eyebrow + text-base value (smaller than secondaries). -->
				<div class="flex items-baseline justify-between px-5 py-4">
					<div>
						<div
							class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase"
						>
							Estimated daily total (3 meals/day)
						</div>
						<div class="flex items-baseline gap-2">
							<span class="num text-base font-medium text-[var(--color-text-primary)]">
								{oralResult.estimatedDailyTotal}
							</span>
							<span class="text-ui text-[var(--color-text-secondary)]">capsules</span>
						</div>
					</div>
				</div>
```

New text (D-12 amendment + D-13 codification):

```
				<!-- Prescribing-artifact line per D-12 (supersedes Phase 2 D-09 visual treatment).
				     Same shape as Tube-Feed Capsules per month per D-13 Prescribing-Artifact-Leads Rule. -->
				<div class="flex items-baseline justify-between px-5 py-4">
					<div>
						<div
							class="text-2xs font-semibold tracking-wider text-[var(--color-identity)] uppercase"
						>
							Estimated daily total (3 meals/day)
						</div>
						<div class="flex items-baseline gap-2">
							<span class="num text-title font-extrabold text-[var(--color-text-primary)]">
								{oralResult.estimatedDailyTotal}
							</span>
							<span class="text-ui text-[var(--color-text-secondary)]">capsules</span>
						</div>
					</div>
				</div>
```

Diff:
- Comment line 1: `Tertiary line per D-09 verbatim.` -> `Prescribing-artifact line per D-12 (supersedes Phase 2 D-09 visual treatment).`
- Comment line 2: `text-2xs eyebrow + text-base value (smaller than secondaries).` -> `Same shape as Tube-Feed Capsules per month per D-13 Prescribing-Artifact-Leads Rule.`
- Eyebrow `<div>` class: `tracking-wide` -> `tracking-wider` (1 character added: `r`).
- Numeral `<span>` class: `text-base` -> `text-title` (5 characters changed within the same class slot).
- Numeral `<span>` class: `font-medium` -> `font-extrabold` (5 characters changed within the same class slot; net +3 characters in the class string).

The visible text `Estimated daily total (3 meals/day)`, the numeral expression `{oralResult.estimatedDailyTotal}`, the suffix `capsules`, the `class="num"` token presence, the `text-[var(--color-identity)]` color token, and the `text-[var(--color-text-primary)]` color token are ALL byte-identical.

If the Edit tool reports the old_string match failed, re-read the file to confirm the current shape; the most likely cause is whitespace drift (tabs vs spaces) or a comment-line reflow during a prior unrelated commit. If so, either edit the matching block as it actually exists in the file (preserving its leading whitespace) or use 2 separate Edit calls (one for the comment lines, one for the row's `<div>` block).

**STEP D -- Verify the change is scoped correctly.**

After editing, run:

```
grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte
```
Expected: exactly 2 (Wave 4 Tube-Feed eyebrow line 288 byte-identical + new Oral eyebrow line ~216). If 1, the eyebrow swap didn't land. If 3+, the executor accidentally changed a sibling row -- revert and re-edit with disambiguating context.

```
grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte
```
Expected: exactly 2 (Wave 2 Tube-Feed numeral line 293 byte-identical + new Oral numeral line ~221). If 1, the numeral weight swap didn't land. If 3+, the executor over-edited.

```
grep -c 'text-title' src/lib/pert/PertCalculator.svelte
```
Expected: exactly 8 (pre-fix baseline + 1; the Oral tertiary numeral was `text-base` and is now `text-title`).

```
grep -c 'text-base' src/lib/pert/PertCalculator.svelte
```
Expected: pre-fix value minus 1 (typically 1; was 2 pre-fix because the Oral tertiary numeral was `text-base` and is now `text-title`).

```
grep -c 'font-medium' src/lib/pert/PertCalculator.svelte
```
Expected: pre-fix value minus 1 (typically 1; was 2 pre-fix because the Oral tertiary numeral was `font-medium` and is now `font-extrabold`).

```
grep -oE 'tracking-wide(r|[^a-z]|"|$)' src/lib/pert/PertCalculator.svelte | grep -v "tracking-wider" | wc -l
```
Expected: `6` (count drops by exactly 1 from the pre-edit baseline of 7 to 6; the 6 remaining eyebrows are the page subtitle + 2 Oral secondary-row eyebrows + 3 Tube-Feed sibling-row eyebrows; only the Oral tertiary eyebrow flipped to `tracking-wider`).

```
grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte
```
Expected: exactly 9 (UI-SPEC Gate 12; UNCHANGED -- the Oral tertiary numeral kept `class="num"` and only the size/weight tokens within the same class string changed).

```
grep -q 'tracking-wider text-\[var(--color-identity)\] uppercase' src/lib/pert/PertCalculator.svelte
```
Expected: exit 0 (the new Oral eyebrow class string preserves Identity-Inside Rule color token unchanged; the Tube-Feed eyebrow at line 288 also matches this pattern from Wave 4).

```
grep -q 'class="num text-title font-extrabold text-\[var(--color-text-primary)\]' src/lib/pert/PertCalculator.svelte
```
Expected: exit 0 at least 2 hits (the new Oral numeral class string at line ~221 + the existing Tube-Feed numeral class string at line 293 from Wave 2; they're now structurally identical).

**STEP E -- Em-dash + en-dash sweep on the modified file.**

Run:
```
grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte
grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte
```
Expected: both return 0. If either returns > 0, the executor introduced a forbidden character (the workstream Q1 broad ban + Phase 2 D-19 + Phase 3.1 D-08 require both U+2014 and U+2013 = 0 across all PERT-route files); replace with ASCII hyphens or periods and re-run the sweep.

**STEP F -- Visible text + Pitfall 4 selector preservation check.**

```
grep -c 'Estimated daily total (3 meals/day)' src/lib/pert/PertCalculator.svelte
```
Expected: exactly 1 (the visible text in the Oral tertiary row; PertCalculator.test.ts:65 + 77 + future e2e selectors all assert on this string).

```
grep -c 'oralResult.estimatedDailyTotal' src/lib/pert/PertCalculator.svelte
```
Expected: exactly 1 (the numeral expression preserved verbatim; calculation contract from Phase 2 D-15..D-18 byte-identical).

```
grep -c 'D-12' src/lib/pert/PertCalculator.svelte
```
Expected: at least 1 (the comment update cites D-12). Comment cites are the audit trail for the supersedure of Phase 2 D-09.

```
grep -c 'D-13' src/lib/pert/PertCalculator.svelte
```
Expected: at least 1 (the comment update cites D-13). Comment cites are the codification of the Prescribing-Artifact-Leads Rule.

**STEP G -- Commit.** Stage `src/lib/pert/PertCalculator.svelte`. Commit with the plan-frontmatter `commit_message`:

```
fix(pert/04-05): D-12 Oral daily-total promoted to prescribing-artifact treatment (D-13 cross-mode rule)
```

The commit body should record:
- The line range edited (Oral tertiary row at lines ~211-227; comment lines + eyebrow class line + numeral class line).
- The diff: 3 token swaps + 2 comment-line updates.
  - Eyebrow `tracking-wide` -> `tracking-wider`.
  - Numeral `text-base` -> `text-title`.
  - Numeral `font-medium` -> `font-extrabold`.
  - Comment cites D-12 + D-13 instead of D-09.
- Cross-reference: this ships the COMBINED RECIPE parallel to Wave 4's Tube-Feed Approach C; the Oral row now matches the Tube-Feed row's prescribing-artifact treatment.
- D-12 amendment of Phase 2 D-09 (visual-hierarchy aspect ONLY; calculation/label/cardinality/structure unchanged).
- D-13 codification of the Prescribing-Artifact-Leads Rule (workstream-local for v1.15; DESIGN.md update deferred to v1.16).
- Identity-Inside Rule preserved (eyebrow + numeral color tokens unchanged).
- Pitfall 4 selector preservation honored (visible text `Estimated daily total (3 meals/day)` and numeral expression `oralResult.estimatedDailyTotal` byte-identical; PertCalculator.test.ts:65 + 77 selectors continue to match).
- Reference to gap closure: 04-UAT.md test #4.

**FORBIDDEN:**
- Editing any other file in this task. The only allowed file is `src/lib/pert/PertCalculator.svelte`.
- Changing the eyebrow color token `text-[var(--color-identity)]` to anything else (Identity-Inside Rule).
- Changing the numeral color token `text-[var(--color-text-primary)]` to anything else (Identity-Inside Rule + the Eyebrow-Above-Numeral pattern uses neutral primary text on the numeral).
- Changing the visible text `Estimated daily total (3 meals/day)` (Pitfall 4 selector preservation; PertCalculator.test.ts:65 + 77).
- Changing the numeral expression `{oralResult.estimatedDailyTotal}` (Pitfall 4 selector preservation; calculation contract).
- Changing the suffix `capsules` (visible text consistency with Tube-Feed row).
- Removing the `class="num"` slot on the numeral `<span>` (UI-SPEC Gate 12 tabular-numerals contract).
- Re-touching the Tube-Feed `Capsules per month` row at lines 284-299 (Wave 4 + Wave 2 treatments byte-identical; Wave 5 ships the Oral parallel ONLY).
- Editing the 6 sibling `tracking-wide` eyebrows (lines ~149, 181, 198, 237, 254, 271). The hierarchy bump is asymmetric BY DESIGN: only the prescribing artifacts (Oral `Estimated daily total` line ~216 + Tube-Feed `Capsules per month` line 288) get the wider tracking.
- Editing the Oral secondary-row numerals at lines 186 + 203 (Total lipase needed + Lipase per dose; they keep `text-title font-bold`; the prescribing artifact at line ~221 differentiates with `font-extrabold` which is heavier).
- Editing `src/lib/pert/PertInputs.svelte` (Phase 3.1 frozen).
- Editing `src/lib/pert/pert-config.json` (Phase 4 commit `2dc7ae2` frozen).
- Editing `src/lib/pert/PertCalculator.test.ts` or any other test file (Pitfall 4 selector preservation means no test edit needed).
- Editing any e2e spec (Pitfall 4 reminder applied: D-12 changes letter-spacing + font-size + font-weight only, not text content; selectors still match).
- Touching any shared component or shell file (D-08b strictly forbidden).
- Updating DESIGN.md to codify the Prescribing-Artifact-Leads Rule (D-13 explicitly defers the DESIGN.md update to v1.16; workstream-local rule is recorded in 04-CONTEXT.md + 04-05-SUMMARY.md only).
  </action>
  <verify>
    <automated>
      # 1. Both prescribing artifacts now carry the wider-tracking eyebrow (Tube-Feed line 288 from Wave 4 + Oral line ~216 from Wave 5).
      test "$(grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte)" -eq 2

      # 2. Both prescribing artifacts now carry the extrabold numeral (Tube-Feed line 293 from Wave 2 + Oral line ~221 from Wave 5).
      test "$(grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte)" -eq 2

      # 3. text-title count increased by exactly 1 (the Oral tertiary numeral was text-base and is now text-title; baseline 7 -> 8).
      test "$(grep -c 'text-title' src/lib/pert/PertCalculator.svelte)" -eq 8

      # 4. tracking-wide-only count drops by exactly 1 (the Oral tertiary eyebrow was tracking-wide and is now tracking-wider; baseline 7 -> 6).
      test "$(grep -oE 'tracking-wide(r|[^a-z]|\"|$)' src/lib/pert/PertCalculator.svelte | grep -v 'tracking-wider' | wc -l)" -eq 6

      # 5. Eyebrow color token preserved on the new Oral eyebrow (Identity-Inside Rule).
      grep -q 'text-2xs font-semibold tracking-wider text-\[var(--color-identity)\] uppercase' src/lib/pert/PertCalculator.svelte

      # 6. Numeral class string structurally identical to Tube-Feed Wave 2 + Wave 4 numeral (cross-mode parity for the prescribing artifact).
      test "$(grep -c 'class="num text-title font-extrabold text-\[var(--color-text-primary)\]' src/lib/pert/PertCalculator.svelte)" -ge 2

      # 7. Visible text "Estimated daily total (3 meals/day)" preserved (Pitfall 4; PertCalculator.test.ts:65 + 77).
      grep -q 'Estimated daily total (3 meals/day)' src/lib/pert/PertCalculator.svelte

      # 8. Numeral expression {oralResult.estimatedDailyTotal} preserved (Pitfall 4; calculation contract).
      grep -q 'oralResult\.estimatedDailyTotal' src/lib/pert/PertCalculator.svelte

      # 9. Comment update cites D-12 + D-13 (audit trail for the supersedure of Phase 2 D-09).
      grep -q 'D-12' src/lib/pert/PertCalculator.svelte
      grep -q 'D-13' src/lib/pert/PertCalculator.svelte

      # 10. Em-dash + en-dash sweep on the modified file: both 0.
      test "$(grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte)" -eq 0
      test "$(grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte)" -eq 0

      # 11. Tabular-numerals (.num) class still on hero + secondaries (UI-SPEC Gate 12; baseline 9, unchanged).
      test "$(grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte)" -eq 9

      # 12. Wave 4 + Wave 2 Tube-Feed treatment byte-identical (line 288 eyebrow + line 293 numeral preserved).
      grep -q 'class="text-2xs font-semibold tracking-wider text-\[var(--color-identity)\] uppercase"' src/lib/pert/PertCalculator.svelte
      # The Capsules per month row is on the line directly below this class line; verify by looking up the visible text:
      test "$(grep -c 'Capsules per month' src/lib/pert/PertCalculator.svelte)" -ge 2

      # 13. The plan only touches the allowed file (no shell, no shared, no other calculator, no e2e, no test).
      test "$(git diff --name-only HEAD -- src/ e2e/ | grep -vE '^src/lib/pert/PertCalculator\.svelte$' | wc -l)" -eq 0

      # 14. PertInputs.svelte byte-identical (Phase 3.1 frozen).
      test -z "$(git diff HEAD -- src/lib/pert/PertInputs.svelte)"

      # 15. pert-config.json byte-identical against HEAD (Phase 4 commit 2dc7ae2 frozen).
      test -z "$(git diff HEAD -- src/lib/pert/pert-config.json)"

      # 16. Calc-layer files byte-identical (Phase 2/3.1 frozen).
      test -z "$(git diff HEAD -- src/lib/pert/calculations.ts)"
      test -z "$(git diff HEAD -- src/lib/pert/state.svelte.ts)"
      test -z "$(git diff HEAD -- src/lib/pert/types.ts)"
      test -z "$(git diff HEAD -- src/lib/pert/config.ts)"

      # 17. Test file byte-identical (Pitfall 4 selector preservation means no test edit needed).
      test -z "$(git diff HEAD -- src/lib/pert/PertCalculator.test.ts)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` returns exactly 2 (Wave 4 Tube-Feed eyebrow line 288 byte-identical + new Oral eyebrow line ~216).
    - `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` returns exactly 2 (Wave 2 Tube-Feed numeral line 293 byte-identical + new Oral numeral line ~221).
    - `grep -c 'text-title' src/lib/pert/PertCalculator.svelte` returns exactly 8 (pre-fix baseline 7 + 1 from the Oral tertiary numeral promotion).
    - `grep -c 'text-base' src/lib/pert/PertCalculator.svelte` drops by exactly 1 from the pre-fix baseline (the Oral tertiary numeral was `text-base` and is now `text-title`).
    - `grep -c 'font-medium' src/lib/pert/PertCalculator.svelte` drops by exactly 1 from the pre-fix baseline (the Oral tertiary numeral was `font-medium` and is now `font-extrabold`).
    - `grep -oE 'tracking-wide(r|[^a-z]|"|$)' src/lib/pert/PertCalculator.svelte | grep -v 'tracking-wider' | wc -l` returns exactly 6 (was 7 pre-fix; one flipped to `tracking-wider`).
    - The eyebrow on the Oral tertiary row preserves the Identity-Inside Rule: `class="text-2xs font-semibold tracking-wider text-[var(--color-identity)] uppercase"` (eyebrow color token byte-identical).
    - The numeral on the Oral tertiary row preserves the primary-text color token: `class="num text-title font-extrabold text-[var(--color-text-primary)]"` (numeral color token byte-identical).
    - The visible string `Estimated daily total (3 meals/day)` is preserved verbatim (Pitfall 4 selector preservation; PertCalculator.test.ts:65 + 77 selectors continue to match without test-side change).
    - The numeral expression `{oralResult.estimatedDailyTotal}` is preserved verbatim.
    - The suffix `capsules` is preserved verbatim.
    - The HTML comment lines 211-212 cite D-12 + D-13 (audit trail for the supersedure of Phase 2 D-09).
    - `grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte` returns 0 (em-dash count).
    - `grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte` returns 0 (en-dash count).
    - `grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte` returns exactly 9 (UI-SPEC Gate 12; baseline unchanged because only size/weight tokens within the existing class string changed).
    - The Wave 4 Tube-Feed Approach C treatment (line 288 eyebrow `tracking-wider`) and Wave 2 F-03 fix (line 293 numeral `font-extrabold`) are byte-identical.
    - `git diff --name-only HEAD -- src/ e2e/` returns ONLY `src/lib/pert/PertCalculator.svelte` (no other source/test/config changed).
    - `git diff HEAD -- src/lib/pert/PertInputs.svelte` is empty (Phase 3.1 frozen).
    - `git diff HEAD -- src/lib/pert/pert-config.json` is empty (Phase 4 commit `2dc7ae2` frozen).
    - All calc-layer files (`calculations.ts`, `state.svelte.ts`, `types.ts`, `config.ts`) byte-identical against HEAD.
    - `src/lib/pert/PertCalculator.test.ts` byte-identical against HEAD (Pitfall 4 selector preservation makes the test pass without modification).
  </acceptance_criteria>
  <done>
    - The Oral `Estimated daily total (3 meals/day)` tertiary row in `src/lib/pert/PertCalculator.svelte` renders with the prescribing-artifact treatment: eyebrow `tracking-wider` + numeral `text-title font-extrabold`. Cross-mode parity with Tube-Feed `Capsules per month` is structurally complete.
    - The eyebrow + numeral color tokens are unchanged (Identity-Inside Rule honored); only typographic tracking + size + weight changed.
    - The Wave 4 Tube-Feed Approach C eyebrow (line 288) + Wave 2 F-03 numeral (line 293) are preserved byte-identical (this plan ADDS the Oral parallel; it does not modify the Tube-Feed treatment).
    - The HTML comment at lines 211-212 cites D-12 + D-13 (the audit trail for the supersedure of Phase 2 D-09 visual treatment + the codification of the Prescribing-Artifact-Leads Rule).
    - The PertCalculator.test.ts:65 + 77 selectors still match (visible text byte-identical; Pitfall 4).
    - The change is staged + committed with the plan-frontmatter `commit_message`.
    - Em-dash + en-dash count on PertCalculator.svelte = 0 each.
    - Negative-space audit clean: only `src/lib/pert/PertCalculator.svelte` is in `git diff --name-only HEAD~1..HEAD`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run quality gates (svelte-check 0/0; vitest src/lib/pert/ 81/81; em + en-dash sweep on PertCalculator.svelte = 0; D-12 invariants landed; Wave 4 + Wave 2 treatments preserved)</name>
  <read_first>
    - .planning/workstreams/pert/STATE.md (offset 28, limit 30 -- the Phase 3.1 + Phase 4 outcomes paragraphs documenting the gate baselines: svelte-check 0/0; vitest 425/425 with PERT subset 81/81; pnpm build OK; pert-a11y 4/4; pert.spec 12/12; full Playwright 117 + 1 baseline flake)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-PLAN.md (offset 405, limit 175 -- Wave 4 Task 2's 9-gate sequence; this Wave 5 plan mirrors the same gate structure with adjusted expected counts for the D-12 swap)
    - src/lib/pert/PertCalculator.svelte (post-Task-1; verify the D-12 amendment landed)
  </read_first>
  <files>(none -- this task runs commands and captures their output to /tmp/04-05-gates/; no source edits)</files>
  <action>
This task is verification-only. It runs the quality gates that re-validate the Phase 3.1 + Phase 4 baselines after the D-12 amendment lands; the goal is to fail-fast at Wave 5 if Task 1 regressed any baseline.

**STEP A -- Create the gate-output directory.**

```
mkdir -p /tmp/04-05-gates
```

**STEP B -- Gate 1: svelte-check 0/0.**

Use `pnpm run check` (the project's actual script entry):

```
pnpm run check 2>&1 | tee /tmp/04-05-gates/gate-01-svelte-check.log
```

Acceptance: exit 0 AND output reports `0 errors` AND `0 warnings`. The Phase 3.1 + Phase 4 baseline is `~4586 files, 0 errors, 0 warnings`. If errors > 0 OR warnings > 0, Task 1 introduced a syntax or type regression -- revert Task 1's edit, investigate, re-apply with the fix.

If the worktree has no `node_modules` (per the 04-02-PLAN.md Wave 2 deviation pattern + 04-04-PLAN.md Wave 4 same pattern), run `pnpm install --frozen-lockfile` first. The `node_modules` directory is gitignored; the install does NOT pollute the diff.

**STEP C -- Gate 2: vitest src/lib/pert/ 81/81.**

Use `pnpm exec vitest run src/lib/pert/` directly (bypassing the npm-script wrapper; per 04-02-PLAN.md + 04-04-PLAN.md deviation notes, the `pnpm test:run -- src/lib/pert/` form has an argument-passing quirk that runs the full 425 suite instead of the PERT subset):

```
pnpm exec vitest run src/lib/pert/ 2>&1 | tee /tmp/04-05-gates/gate-02-vitest-pert.log
```

Acceptance: exit 0 AND test count = 81 (PertCalculator.test.ts 10 + PertInputs.test.ts 9 + calculations.test.ts 45 + config.test.ts 17 = 81; matches Phase 3.1 + Phase 4 PERT-subset baseline). If count != 81 OR any failure, Task 1 broke a test. The most likely culprit if a test breaks is PertCalculator.test.ts:65 + 77 if the visible text changed (it MUST be byte-identical post-fix; if the test fails, revert the visible-text change and re-run). PertCalculator.test.ts:58 ("renders Oral tertiary `Estimated daily total (3 meals/day)` eyebrow") asserts the eyebrow EXISTS in Oral mode and is HIDDEN in Tube-Feed mode -- this assertion is class-string-blind and continues to pass post-D-12.

**STEP D -- Gate 3: em-dash + en-dash sweep on PertCalculator.svelte = 0/0.**

```
EMS=$(grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte)
ENS=$(grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte)
echo "em-dash count: $EMS" | tee /tmp/04-05-gates/gate-03-dash-sweep.log
echo "en-dash count: $ENS" | tee -a /tmp/04-05-gates/gate-03-dash-sweep.log
test "$EMS" -eq 0
test "$ENS" -eq 0
```

Acceptance: both 0. If either is non-zero, identify the offender (most likely an inadvertent comment introduced by Task 1's edit), replace with ASCII hyphen or period, and re-commit (amend in this case is fine since the only commit so far in this plan is Task 1's).

**STEP E -- Gate 4: tabular-numerals preserved (UI-SPEC Gate 12).**

```
NUM_HITS=$(grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte)
echo "tabular-numerals .num class hits: $NUM_HITS" | tee /tmp/04-05-gates/gate-04-tabular-numerals.log
test "$NUM_HITS" -eq 9
```

Acceptance: count = 9 (Phase 2 baseline; UNCHANGED because the D-12 swap only changes size/weight tokens within the existing `class="num ..."` slot on the Oral tertiary numeral; the `class="num"` token itself is preserved).

**STEP F -- Gate 5: Wave 4 + Wave 2 Tube-Feed prescribing-artifact treatment preserved (regression guard).**

```
TF_EYEBROW_LINE=$(grep -nE 'tracking-wider text-\[var\(--color-identity\)\] uppercase' src/lib/pert/PertCalculator.svelte | head -1 | cut -d: -f1)
TF_NUMERAL_LINE=$(grep -nE 'class="num text-title font-extrabold' src/lib/pert/PertCalculator.svelte | grep -v 'oralResult' | head -1 | cut -d: -f1)
echo "Tube-Feed eyebrow line (Wave 4 commit d82aa30): $TF_EYEBROW_LINE" | tee /tmp/04-05-gates/gate-05-tf-preserved.log
echo "Tube-Feed numeral line (Wave 2 commit 29306e7): $TF_NUMERAL_LINE" | tee -a /tmp/04-05-gates/gate-05-tf-preserved.log
test "$(grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte)" -eq 2
test "$(grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte)" -eq 2
test "$(grep -c 'Capsules per month' src/lib/pert/PertCalculator.svelte)" -ge 2
```

Acceptance: tracking-wider count = 2 (Wave 4 line 288 + new Wave 5 line ~216). font-extrabold count = 2 (Wave 2 line 293 + new Wave 5 line ~221). `Capsules per month` visible-text + comment hits >= 2 (the row is preserved). If the Wave 4 / Wave 2 Tube-Feed treatment was accidentally clobbered by Task 1, the counts diverge -- STOP, revert, escalate.

**STEP G -- Gate 6: D-12 Oral hierarchy promotion landed (this-plan-specific assertion).**

```
ORAL_EYEBROW=$(grep -A2 'tracking-wider text-\[var(--color-identity)\] uppercase' src/lib/pert/PertCalculator.svelte | grep -c 'Estimated daily total (3 meals/day)')
ORAL_NUMERAL=$(grep -A1 'class="num text-title font-extrabold' src/lib/pert/PertCalculator.svelte | grep -c 'oralResult.estimatedDailyTotal')
echo "Oral eyebrow + visible-text co-located: $ORAL_EYEBROW (expected: 1)" | tee /tmp/04-05-gates/gate-06-d12-landed.log
echo "Oral numeral class + expression co-located: $ORAL_NUMERAL (expected: 1)" | tee -a /tmp/04-05-gates/gate-06-d12-landed.log
test "$ORAL_EYEBROW" -eq 1
test "$ORAL_NUMERAL" -eq 1
test "$(grep -c 'D-12' src/lib/pert/PertCalculator.svelte)" -ge 1
test "$(grep -c 'D-13' src/lib/pert/PertCalculator.svelte)" -ge 1
```

Acceptance: ORAL_EYEBROW = 1 AND ORAL_NUMERAL = 1 AND comment cites D-12 AND D-13. If either co-location check fails, the D-12 swap landed in the wrong row (e.g. on a sibling Oral secondary row instead of the tertiary row) -- revert Task 1's edit, re-read the file, re-apply the edit on the correct row.

**STEP H -- Gate 7: function-binding bridges Phase 3.1 D-01 regression guard.**

```
BINDS=$(grep -cE 'bind:value=\{$' src/lib/pert/PertInputs.svelte)
PROXIES=$(grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte)
echo "function-binding bridges: $BINDS" | tee /tmp/04-05-gates/gate-07-bind-bridges.log
echo "string-bridge proxies: $PROXIES" | tee -a /tmp/04-05-gates/gate-07-bind-bridges.log
test "$BINDS" -ge 3
test "$PROXIES" -eq 0
```

Acceptance: BINDS >= 3 (Phase 3.1 baseline; one per medication / strength / formula picker) AND PROXIES = 0 (no string-bridge proxies re-introduced). Wave 5 does NOT touch PertInputs.svelte so this should pass trivially; the gate is a safety check.

**STEP I -- Gate 8: pert-config.json byte-identical against HEAD (Phase 4 commit `2dc7ae2` frozen).**

```
git log --oneline -1 src/lib/pert/pert-config.json | tee /tmp/04-05-gates/gate-08-config-history.log
test -z "$(git diff HEAD -- src/lib/pert/pert-config.json)"
```

Acceptance: the most recent commit touching `pert-config.json` is `2dc7ae2`; `git diff HEAD -- src/lib/pert/pert-config.json` is empty.

**STEP J -- Gate 9: plan-level negative-space audit.**

```
git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | tee /tmp/04-05-gates/gate-09-neg-space-plan.log
test "$(git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | wc -l)" -eq 1
git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | grep -q '^src/lib/pert/PertCalculator\.svelte$'
```

Acceptance: exactly 1 file in the plan's diff (Task 1's commit), and that file is `src/lib/pert/PertCalculator.svelte`. Anything else (e.g. PertInputs.svelte, pert-config.json, e2e specs, shell, shared, tests) is a D-08 boundary violation -- STOP and escalate to the user.

**Note on running pnpm build / Playwright / pert-a11y / pert.spec / full Playwright:** these heavy gates are NOT re-run in this Wave 5 plan -- they were already green at Phase 4 closure (Wave 3 plan 04-03; commits `95e31b0`, `3001c2a`, `33695e1`) and re-validated implicitly by Wave 4's identical class-string-only edit profile. The D-12 edit is a Tailwind class-string change with no behavioral or layout impact (font-size + font-weight + letter-spacing only); re-running the heavy gates in the same gsd-execute pass would consume context for marginal additional safety. The user's `/gsd-verify-work 4 --ws pert` re-run after this plan ships will include the heavy gates as part of the standard verification command sequence.

**STEP K -- Capture outputs.** All `/tmp/04-05-gates/gate-NN-*.log` files are referenced verbatim in Task 4's 04-05-SUMMARY.md as evidence pastes. Do NOT commit these tmp logs to git -- they are transient verification artifacts.
  </action>
  <verify>
    <automated>
      # Gate 1: svelte-check 0/0
      test -f /tmp/04-05-gates/gate-01-svelte-check.log
      grep -qE '0 errors' /tmp/04-05-gates/gate-01-svelte-check.log
      grep -qE '0 warnings' /tmp/04-05-gates/gate-01-svelte-check.log

      # Gate 2: vitest src/lib/pert/ 81/81
      test -f /tmp/04-05-gates/gate-02-vitest-pert.log
      grep -qE 'Tests[[:space:]]+81 passed' /tmp/04-05-gates/gate-02-vitest-pert.log

      # Gate 3: em-dash + en-dash 0/0 on PertCalculator.svelte
      test "$(grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte)" -eq 0
      test "$(grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte)" -eq 0

      # Gate 4: tabular-numerals = 9 hits (UNCHANGED; UI-SPEC Gate 12 contract)
      test "$(grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte)" -eq 9

      # Gate 5: Wave 4 + Wave 2 Tube-Feed treatment preserved
      test "$(grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte)" -eq 2
      test "$(grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte)" -eq 2
      test "$(grep -c 'Capsules per month' src/lib/pert/PertCalculator.svelte)" -ge 2

      # Gate 6: D-12 landed on the correct row (eyebrow above Estimated daily total visible text; numeral above oralResult.estimatedDailyTotal expression)
      test "$(grep -A2 'tracking-wider text-\[var(--color-identity)\] uppercase' src/lib/pert/PertCalculator.svelte | grep -c 'Estimated daily total (3 meals/day)')" -eq 1
      test "$(grep -A1 'class="num text-title font-extrabold' src/lib/pert/PertCalculator.svelte | grep -c 'oralResult\.estimatedDailyTotal')" -eq 1
      test "$(grep -c 'D-12' src/lib/pert/PertCalculator.svelte)" -ge 1
      test "$(grep -c 'D-13' src/lib/pert/PertCalculator.svelte)" -ge 1

      # Gate 7: function-binding bridges >=3, string-bridge proxies = 0
      test "$(grep -cE 'bind:value=\{$' src/lib/pert/PertInputs.svelte)" -ge 3
      test "$(grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte)" -eq 0

      # Gate 8: pert-config.json byte-identical against HEAD
      test -z "$(git diff HEAD -- src/lib/pert/pert-config.json)"

      # Gate 9: plan-level negative-space audit -- exactly 1 file (PertCalculator.svelte)
      test "$(git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | wc -l)" -eq 1
      git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | grep -q '^src/lib/pert/PertCalculator\.svelte$'
    </automated>
  </verify>
  <acceptance_criteria>
    - Gate 1 (svelte-check): exit 0; output reports 0 errors AND 0 warnings (Phase 3.1 + Phase 4 baseline preserved).
    - Gate 2 (vitest src/lib/pert/): exit 0; 81 tests pass; 0 fail. Matches Phase 3.1 + Phase 4 baseline. PertCalculator.test.ts:65 + 77 selectors continue to match (visible text byte-identical).
    - Gate 3 (em-dash + en-dash sweep): both `grep -cP '\x{2014}'` and `grep -cP '\x{2013}'` return 0 on `src/lib/pert/PertCalculator.svelte`.
    - Gate 4 (tabular-numerals): `grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte` returns exactly 9 (UI-SPEC Gate 12; baseline unchanged).
    - Gate 5 (Wave 4 + Wave 2 preserved): tracking-wider count = 2 AND font-extrabold count = 2 AND `Capsules per month` hits >= 2.
    - Gate 6 (D-12 landed): the new tracking-wider eyebrow co-locates with `Estimated daily total (3 meals/day)`; the new text-title font-extrabold numeral co-locates with `oralResult.estimatedDailyTotal`; comment cites D-12 + D-13.
    - Gate 7 (function-binding bridges): `grep -cE 'bind:value=\{$' src/lib/pert/PertInputs.svelte` returns >= 3; `grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte` returns 0.
    - Gate 8 (pert-config.json): `git diff HEAD -- src/lib/pert/pert-config.json` returns empty (Phase 4 commit `2dc7ae2` frozen).
    - Gate 9 (plan-level negative-space audit): `git diff --name-only HEAD~1..HEAD -- src/ e2e/` returns exactly 1 line: `src/lib/pert/PertCalculator.svelte`.
    - All 9 gate logs captured at `/tmp/04-05-gates/gate-NN-*.log` (transient; not committed; referenced in Task 4's SUMMARY).
  </acceptance_criteria>
  <done>
    - All 9 quality gates pass; outputs captured at `/tmp/04-05-gates/`.
    - Phase 3.1 invariants verified intact (function-binding bridges 3+ hits, zero string-bridge proxies, em-dash + en-dash 0/0 on PertCalculator.svelte).
    - Phase 4 invariants verified intact (Wave 4 Tube-Feed eyebrow `tracking-wider` line 288 + Wave 2 Tube-Feed numeral `font-extrabold` line 293 byte-identical; tabular-numerals = 9; pert-config.json byte-identical).
    - D-12 Oral hierarchy promotion verified landed on the correct row (eyebrow + visible text + numeral expression all co-located; comment cites D-12 + D-13).
    - PERT-subset vitest count 81/81 unchanged (Phase 3.1 + Phase 4 baseline preserved; PertCalculator.test.ts:65 + 77 visible-text selectors continue to match).
    - Plan-level negative-space audit clean (only PertCalculator.svelte modified in Wave 5's commit).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-eye visual UAT -- confirm D-12 closes the Oral hierarchy inversion AND audit BOTH modes per D-13</name>
  <what-built>
    Wave 5 ships the D-12 amendment of Phase 2 D-09 (visual-treatment aspect only) plus the D-13 codification of the Prescribing-Artifact-Leads Rule: the Oral-mode `Estimated daily total (3 meals/day)` tertiary row in `src/lib/pert/PertCalculator.svelte` (lines ~211-227) was promoted to the prescribing-artifact treatment via a 3-token swap. Eyebrow class `tracking-wide` -> `tracking-wider`. Numeral class `text-base` -> `text-title`. Numeral class `font-medium` -> `font-extrabold`. Comment lines 211-212 updated to cite D-12 (supersedure of Phase 2 D-09 visual treatment) + D-13 (cross-mode rule codification).

    Cross-mode parity: post-Wave-5, the Oral tertiary row at lines ~211-227 and the Tube-Feed `Capsules per month` row at lines 284-299 share the same both-vectors typographic treatment. Both prescribing artifacts now read at the same hierarchy weight regardless of mode. The 7 sibling `tracking-wide` eyebrows below are byte-identical (page subtitle line 149 + 2 Oral secondary-row eyebrows lines 181 + 198 + 3 Tube-Feed sibling-row eyebrows lines 237/254/271). Identity-Inside Rule honored (eyebrow + numeral color tokens unchanged). Pitfall 4 selector preservation honored (visible text `Estimated daily total (3 meals/day)`, numeral expression `oralResult.estimatedDailyTotal`, suffix `capsules` all byte-identical; PertCalculator.test.ts:65 + 77 selectors continue to match).

    All 9 plan-level quality gates from Task 2 are green: svelte-check 0/0; vitest src/lib/pert/ 81/81; em-dash + en-dash 0/0; tabular-numerals 9 (unchanged); Wave 4 + Wave 2 Tube-Feed treatment preserved (tracking-wider count = 2 + font-extrabold count = 2); D-12 landed on the correct Oral row (co-location checks pass); function-binding bridges 3+; pert-config.json byte-identical; negative-space audit clean (only PertCalculator.svelte modified).

    THIS CHECKPOINT IS THE D-13 AUDIT GATE. The D-13 Prescribing-Artifact-Leads Rule codifies that any post-Phase-4 design-polish sweep MUST explicitly inspect the prescribing artifact in BOTH Oral and Tube-Feed modes. The original Wave 1 LLM-Design-Review fallback missed both F-03 (Tube-Feed) AND the Oral hierarchy inversion. Wave 4's checkpoint:human-verify caught F-03 but only inspected Tube-Feed (where the originating finding was). This Wave 5 checkpoint corrects the audit gap by EXPLICITLY auditing both modes in the same UAT pass: the user reviews Oral first (the surfaced-issue context for test #4) AND Tube-Feed second (the cross-mode parity verification context). NO automated DOM inspection, NO LLM-Design-Review pass, NO substitute LLM gate can replace the human-eye review of both modes.
  </what-built>
  <how-to-verify>
    1. **Start the dev server.** From the worktree root, run `pnpm dev`. Vite will start on a port (typically 5173 unless taken). Note the URL printed in the terminal.

    2. **Open `/pert` in a browser.** Navigate to `http://localhost:{port}/pert`. The route renders the PERT calculator. Default mode is Oral.

    3. **Stay in Oral mode (the surfaced-issue context for test #4).** Confirm the Oral / Tube-Feed segmented toggle has Oral highlighted with the identity-purple active state. The first secondaries-section context to inspect is Oral.

    4. **Populate the Oral inputs to render the secondaries section.** Enter:
       - Weight: `10` kg (or any value in the 0.3-10 kg range; the slider max is 10 kg per Wave 2 commit `2dc7ae2`).
       - Fat per meal: `25` g.
       - Lipase per gram of fat: `2000` units/g.
       - Medication: pick any (e.g. `Creon`).
       - Strength: pick any from the filtered list (e.g. `12,000 units`).

    5. **Locate the Oral secondaries card.** Below the hero (Capsules per dose numeral), the Oral mode shows a `divide-y` stack of 3 rows:
       - Row 1: `Total lipase needed` ... `units/meal`
       - Row 2: `Lipase per dose` ... `units/dose`
       - Row 3: `Estimated daily total (3 meals/day)` ... `capsules` <- the prescribing artifact, the D-12 target

    6. **Read the 3 Oral rows by eye (the test #4 acceptance check).** The question is: does the `Estimated daily total (3 meals/day)` row (Row 3) read as the visually-leading row of the secondaries section -- distinct from and AT or ABOVE in hierarchy weight the 2 derived per-dose figures (Total lipase needed + Lipase per dose) above it? Specifically:
       - The eyebrow `ESTIMATED DAILY TOTAL (3 MEALS/DAY)` should have wider letter-spacing than `TOTAL LIPASE NEEDED` and `LIPASE PER DOSE` (those stay `tracking-wide`).
       - The numeral should look heavier (extrabold = weight 800) than the 2 sibling numerals (bold = weight 700) AND larger ... wait, no -- the numeral size matches the secondaries at `text-title` (22px), NOT the hero `text-display`. So the numeral SIZE is the same as the 2 derived siblings, but the WEIGHT is heavier (extrabold vs bold). Combined with the wider-tracking eyebrow above it, the row should clearly stand out without screaming.
       - PRE-FIX the row was `text-base font-medium` (16px / weight 500) AND `tracking-wide` -- visibly SMALLER and LIGHTER than the 2 derived siblings. POST-FIX it should be visibly HEAVIER (and same size as the siblings, NOT smaller).

    7. **Switch to Tube-Feed mode (the cross-mode parity verification per D-13).** Click the `Tube-Feed` segmented-toggle pill. Populate the Tube-Feed inputs:
       - Weight: `7` kg (already populated if you set it in step 4; weight is shared across modes per PERT-MODE-03).
       - Formula: pick any pediatric formula (e.g. `Kate Farms Pediatric Standard 1.2`).
       - Volume per day: `1500` mL.
       - Lipase units/kg/day: leave at default `1000`.
       - Medication + Strength: shared with Oral mode; should already be populated.

    8. **Locate the Tube-Feed secondaries card.** It shows a `divide-y` stack of 4 rows:
       - Row 1: `Total fat` ... `g/day`
       - Row 2: `Total lipase needed` ... `units/day`
       - Row 3: `Lipase per kg` ... `units/kg/day`
       - Row 4: `Capsules per month` ... `capsules` <- the Tube-Feed prescribing artifact (Wave 2 + Wave 4 treatment)

    9. **D-13 cross-mode parity check (the audit gate).** Compare the visual weight of:
       - Oral `Estimated daily total (3 meals/day)` row (Wave 5 D-12 treatment).
       - Tube-Feed `Capsules per month` row (Wave 2 F-03 + Wave 4 Approach C combined treatment).
       Both should render at the SAME hierarchy weight. The eyebrow letter-spacing should match (`tracking-wider`). The numeral size + weight should match (`text-title font-extrabold`). The eyebrow color should match (identity-purple). If one row reads heavier or lighter than the other, the cross-mode parity per D-13 is broken -- record this in the resume signal.

    10. **Optional: switch to dark theme + check mobile width.** The user's reported issue surfaced at light-desktop-oral; for completeness:
       - Toggle theme via the theme-toggle button in the header (or set `localStorage.theme = 'dark'` and reload).
       - Resize the browser to mobile width (~375px) or use DevTools responsive mode.
       - Re-read both Oral and Tube-Feed secondaries sections in dark + mobile + each mode = 4 contexts total.

    11. **Type the resume signal.** If:
       - The Oral `Estimated daily total` row reads at hierarchy weight EQUAL TO OR ABOVE the 2 derived per-dose figures (Total lipase needed + Lipase per dose) above it -- the test #4 inversion is eliminated. AND
       - The Oral row reads at the SAME hierarchy weight as the Tube-Feed `Capsules per month` row -- D-13 cross-mode parity is structurally complete.
       Then type `approved` to close the gap.

       If either condition fails:
       - The Oral row STILL reads as smaller / lighter than the derived figures (D-12 typographic delta insufficient): describe the residual issue (e.g. "Oral row still reads lighter; recommend numeral text-display escalation"). The next planner pickup runs `/gsd-plan-phase 4 --gaps --ws pert` for an escalation rung.
       - The Oral row reads heavier than the Tube-Feed row (or vice versa) -- cross-mode parity broken: describe the asymmetry. The next planner pickup investigates whether the discrepancy is rendering-engine drift (different glyph metrics for `(3 meals/day)` vs `Capsules per month`) or a class-string mismatch (re-verify both rows' class strings byte-by-byte).

    **What to look for / what NOT to look for:**
    - LOOK FOR (Oral): a perceptible hierarchy bump on the Estimated daily total row vs the 2 derived rows. Combined with the wider-tracking eyebrow, the row should clearly read as "the secondary that matters most" -- the prescribing artifact for daily-script + parent-education workflows.
    - LOOK FOR (Cross-mode): visual parity between the Oral `Estimated daily total` row and the Tube-Feed `Capsules per month` row. They should both stand out as their respective secondaries' prescribing artifacts at matching hierarchy weight.
    - DO NOT LOOK FOR: a hero-equivalent visual treatment on the Oral row -- the row is a tertiary in the divide-y stack, not the hero. Numeral size matches secondaries (`text-title` = 22px), NOT the hero (`text-display` = 44px). If the row screams as loud as the hero, the post-fix size token may have been over-applied (verify it's `text-title`, not `text-display`).
    - DO NOT LOOK FOR: any color change on the eyebrow -- it stays identity-purple (`text-[var(--color-identity)]`); the bump is typographic only (extrabold weight + larger size + wider tracking).
    - DO NOT LOOK FOR: any change to the visible text `Estimated daily total (3 meals/day)` -- it's preserved verbatim per Pitfall 4 (PertCalculator.test.ts:65 + 77 selectors).
    - DO NOT LOOK FOR: any change to the suffix `capsules` -- preserved verbatim.

    **Why this checkpoint exists (the D-13 audit-both-modes corrective process insertion):** Phase 4 closure (Wave 3 plan 04-03) used the LLM-Design-Review fallback to certify all P1/P2 findings as resolved at the >=35/40 score bar. The fallback's screenshot-driven critique transcripts did NOT surface either F-03 (caught by Wave 4 human UAT test #1) OR the Oral hierarchy inversion (caught by Wave 4 human UAT test #4). Wave 4's human-verify checkpoint caught F-03 but only inspected Tube-Feed because that's where the originating finding was -- it never explicitly looked at Oral. This Wave 5 checkpoint encodes the D-13 corrective process insertion: human-eye review must explicitly cover BOTH modes by name. After this plan ships and the user runs `/gsd-verify-work 4 --ws pert`, the existing 04-UAT.md test #4 gap row is re-checked against the post-D-12 UI; PASS marks the row resolved + cross-mode parity confirmed; FAIL re-opens for an escalation rung via `/gsd-plan-phase 4 --gaps --ws pert`.

    **FORBIDDEN:** any LLM-Design-Review or `/impeccable` substitute pass to close this gate. The corrective process insertion is precisely human-eye review of both modes; substituting another LLM gate would re-introduce the failure mode this checkpoint exists to fix (Wave 1 LLM-Design-Review missed both findings; the LLM is structurally unable to register the perceptual-hierarchy-bump issue at the rendered Plus Jakarta Sans scale).
  </how-to-verify>
  <resume-signal>Type `approved` if (a) the Oral `Estimated daily total (3 meals/day)` row's typographic treatment closes the test #4 hierarchy inversion gap by eye AND (b) the Oral row reads at the same hierarchy weight as the Tube-Feed `Capsules per month` row (D-13 cross-mode parity structurally complete). If either condition fails, type a description of the residual issue + the recommended next escalation rung (e.g. numeral text-title -> text-[28px] / text-display, OR visual isolation via background tint, OR cross-mode rendering drift to investigate). The next planner pickup will run `/gsd-plan-phase 4 --gaps --ws pert` to author the next gap-closure plan based on the residual-issue description.</resume-signal>
  <files>(none -- this is a checkpoint:human-verify task; no source files modified; the human reviews the rendered UI from Task 1's edit)</files>
  <action>
This task is a checkpoint:human-verify gate. The full instructions for the human are in the `<what-built>`, `<how-to-verify>`, and `<resume-signal>` blocks above. The orchestrator should:

1. Pause execution after Task 2 completes (all 9 quality gates green).
2. Surface the `<what-built>` and `<how-to-verify>` content to the user verbatim. Highlight the D-13 audit-both-modes principle: the user MUST review BOTH Oral AND Tube-Feed secondaries sections in the same UAT pass (NOT just Oral; that's the audit gap that surfaced this plan).
3. Wait for the user's resume signal per `<resume-signal>` (either `approved` or an escalation-required description).
4. Capture the user's signal verbatim for Task 4's SUMMARY authoring (D-13 audit-trail requirement: verbatim verdict goes in 04-05-SUMMARY.md).
5. If `approved`: proceed to Task 4. If escalation-required: proceed to Task 4 (the SUMMARY records the escalation outcome; the next planner pickup runs `/gsd-plan-phase 4 --gaps --ws pert` for the next escalation rung based on the residual-issue description).

NO automated action is taken in this task; the entire purpose is the D-13 human-eye visual UAT gate replacing both the LLM-Design-Review fallback verdict from Phase 4 closure AND the mode-asymmetric Wave 4 human-verify (which only inspected Tube-Feed).

FORBIDDEN:
- Any source-code edit in this task (this is a verification-only checkpoint).
- Any automated /impeccable critique pass or screenshot-driven LLM-Design-Review (the corrective process insertion is precisely human-eye review of BOTH modes; substituting another LLM gate would re-introduce the failure mode this checkpoint exists to fix).
- Auto-resolving the checkpoint without the user's explicit resume signal.
- Inspecting only Oral mode (the D-13 audit gate REQUIRES both Oral AND Tube-Feed inspection; the user's resume signal must address both).
  </action>
  <verify>
    <automated>
      # Checkpoint:human-verify tasks have no automated verification gate -- the gate is the user's resume signal.
      # The orchestrator's checkpoint handling captures the resume signal and writes it to the execution log.
      # This <automated> block is present to satisfy the plan-structure validator's <verify> requirement; it asserts
      # the checkpoint task type is correctly declared in the task header (gate is the human's signal) AND that the
      # plan explicitly invokes the D-13 audit-both-modes principle (cross-mode parity is the structural correction).
      grep -q 'type="checkpoint:human-verify"' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-PLAN.md
      grep -q 'D-13' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-PLAN.md
      grep -q 'BOTH' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-PLAN.md
    </automated>
  </verify>
  <done>
    - The user has reviewed BOTH the rendered Oral mode AND the rendered Tube-Feed mode at `/pert` post-D-12-amendment by eye in at least the light-desktop context per mode (the surfaced-issue context for test #4 + the cross-mode parity verification context per D-13); recommended coverage is light + dark x mobile + desktop x both modes = 8 contexts (or at minimum 4 contexts: light + dark x desktop x both modes).
    - The user's resume signal has been captured verbatim (either `approved` or an escalation-required description with the residual-issue context).
    - If `approved`: the test #4 Oral hierarchy inversion gap from 04-UAT.md is closed by human verdict; D-13 cross-mode parity is structurally complete; PERT-DESIGN-02..05 stay Validated; Phase 4 closure is re-confirmed after the user re-runs `/gsd-verify-work 4 --ws pert`; Phase 5 (Release) is unblocked.
    - If escalation-required: the residual issue + the recommended next escalation rung is captured verbatim for the next planner pickup; the user runs `/gsd-plan-phase 4 --gaps --ws pert` to author the next gap-closure plan after this plan's SUMMARY ships.
  </done>
</task>

<task type="auto">
  <name>Task 4: Author 04-05-SUMMARY.md (D-12 amendment record + D-13 codification + cross-mode parity confirmation + human-UAT outcome + verbatim user-visual verdict)</name>
  <read_first>
    - src/lib/pert/PertCalculator.svelte (post-Task-1; cite the actual line range edited and the before/after class strings for both the eyebrow and the numeral)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-UAT.md (read in full; cite the test #4 gap row closure status; D-13 audit-both-modes outcome)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-CONTEXT.md (offset 123, limit 6 -- D-12 + D-13 source decisions; cite verbatim into the SUMMARY's "Source decisions" subsection)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md (read for the structural template -- Wave 5's SUMMARY mirrors Wave 4's structure: header, what shipped, gap-closure rationale, quality gates, human-UAT outcome, escalation ladder forward note, REQUIREMENTS.md re-validation note, Phase 3.1 + Phase 4 invariants, negative-space audit, deviations, PERT-DESIGN satisfaction mapping, self-check, next step. Wave 5 ADDS a "D-13 cross-mode parity confirmation" subsection unique to this gap closure.)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-VERIFICATION.md (read for the LLM-Design-Review fallback acknowledgment context; Wave 5's SUMMARY records the audit-both-modes-as-corrective-process insertion against both the fallback AND the Wave 4 mode-asymmetric human-verify)
    - All 9 gate logs at /tmp/04-05-gates/gate-NN-*.log (Task 2's outputs; paste verbatim into the SUMMARY's "Quality gates" section)
    - The output of Task 3's checkpoint (the user's `approved` or escalation-required signal; capture verbatim in the SUMMARY's "Human-visual UAT outcome" section -- the verbatim verdict is the D-13 audit-trail requirement)
    - git log --oneline -5 (capture the SHAs of: (a) Task 1's commit just shipped; (b) Wave 4 commits `d82aa30` Task 1 + `78686f9` Task 4; (c) Wave 2 commit `29306e7`; (d) Wave 3 plan 04-03 closure commit)
  </read_first>
  <files>.planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md</files>
  <action>
Compose `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md` with the following structure (>=50 lines target). Use Wave 4's SUMMARY (`04-04-SUMMARY.md`) as a structural reference but Wave 5 adds the cross-mode parity confirmation subsection + the D-13 codification subsection unique to this gap closure.

**Required sections (in order):**

1. **Frontmatter** with: `phase: 4`, `plan: 5`, `subsystem: pert/design-polish-impeccable`, `workstream: pert`, `milestone: v1.15`, `wave: 5`, `gap_closure: true`, `tags: [pert, design-polish, gap-closure, wave-5, oral-hierarchy-inversion, prescribing-artifact-leads, d-12, d-13]`, `dependency_graph` (requires Wave 1 + Wave 2 + Wave 3 + Wave 4 + 04-UAT.md test #4; provides the D-12 amendment + D-13 codification + this SUMMARY), `key_files.modified: [src/lib/pert/PertCalculator.svelte]`, `decisions` (cite D-12 amendment of Phase 2 D-09 visual-treatment-aspect-only + D-13 Prescribing-Artifact-Leads Rule codification workstream-local + DESIGN.md update deferred to v1.16 + the cross-mode parity confirmation as the structural correction encoded by D-13), `metrics` (lines edited, gate counts, commit count, human-UAT outcome, cross-mode parity confirmation status).

2. **Header**: `# Phase 4 Plan 05 Summary: D-12 Oral Hierarchy Promotion + D-13 Prescribing-Artifact-Leads Rule Codification (Wave 5 Gap Closure)` + `**Phase:** 04-design-polish-impeccable` + `**Plan:** 04-05 (Wave 5 -- gap closure from 04-UAT.md test #4)` + `**Generated:** {today}` + 1-paragraph executive summary citing the 04-UAT.md test #4 gap closure + D-12 amendment of Phase 2 D-09 visual-treatment-aspect-only + D-13 codification of the Prescribing-Artifact-Leads Rule + the cross-mode parity confirmation + the human-UAT-as-corrective-process insertion.

3. **What this plan shipped** (numbered list):
   - D-12 Oral hierarchy promotion: line range edited (~211-227); 3-token swap recipe applied (eyebrow `tracking-wide` -> `tracking-wider`; numeral `text-base` -> `text-title`; numeral `font-medium` -> `font-extrabold`; comment lines updated to cite D-12 + D-13); commit SHA from Task 1; before/after class strings pasted verbatim for both the eyebrow `<div>` and the numeral `<span>`.
   - D-13 codification: the Prescribing-Artifact-Leads Rule lives as a workstream-local rule for v1.15; DESIGN.md update deferred to v1.16 design-contract-refinement phase per 04-CONTEXT.md Deferred Ideas; the rule's audit gate (any post-Phase-4 design-polish sweep MUST explicitly inspect the prescribing artifact in BOTH Oral and Tube-Feed modes) is encoded in this SUMMARY as the future-audit forward note for the next planner pickup.
   - Cross-mode parity confirmation: post-Wave-5, the Oral `Estimated daily total` row at lines ~211-227 and the Tube-Feed `Capsules per month` row at lines 284-299 share the same both-vectors typographic treatment (eyebrow `tracking-wider` + numeral `text-title font-extrabold`); both prescribing artifacts read at the same hierarchy weight regardless of mode; D-13 cross-mode parity is structurally complete.
   - Human-eye visual UAT checkpoint result (Task 3): user's resume signal verbatim (`approved` or escalation-required note); contexts reviewed (minimum: light-desktop-oral + light-desktop-tube-feed; recommended: 8 contexts including dark + mobile).
   - 04-05-SUMMARY.md (this file).

4. **Source decisions** -- a dedicated subsection citing 04-CONTEXT.md verbatim:
   - **D-12 [auto-recommended]:** [paste the verbatim D-12 text from 04-CONTEXT.md lines 124-125].
   - **D-13 [auto-recommended]:** [paste the verbatim D-13 text from 04-CONTEXT.md lines 127-128].
   - Note that both decisions were authored 2026-04-26 in `/gsd-discuss-phase 4 --gaps --ws pert --auto` per the 04-DISCUSSION-LOG.md "Gap-closure pass -- 2026-04-26" section.

5. **Why this plan exists (the gap-closure rationale)** -- a dedicated subsection:
   - Cite Phase 4 closure context: Wave 3 plan 04-03 (commits `95e31b0`, `3001c2a`, `33695e1`, `a6d9469`) certified PERT-DESIGN-01..06 as Validated at the >=35/40 score bar based on 8 LLM-Design-Review FINAL critique transcripts.
   - Cite Wave 4 closure: plan 04-04 (commits `d82aa30` + `78686f9`) shipped F-03 Approach C eyebrow `tracking-wider` on Tube-Feed `Capsules per month` after human UAT test #1; user resume signal `approved` for that gap.
   - Cite the 04-UAT.md test #4 gap: human UAT recorded the Oral `Estimated daily total (3 meals/day)` row reads SMALLER and LIGHTER than the per-dose derived figures above it -- a hierarchy INVERSION where the prescribing artifact is visually de-emphasized below its own derived per-dose figures.
   - Frame the audit-both-modes-as-corrective-process insertion: Wave 1 LLM-Design-Review fallback missed both F-03 AND the Oral hierarchy inversion. Wave 4 human-verify caught F-03 but only inspected Tube-Feed (mode-asymmetric audit gap). D-13 codifies the audit-both-modes principle as the structural correction; this Wave 5 checkpoint:human-verify is the first audit gate to enforce it explicitly.
   - Note the D-12 amendment is a deliberate locked-decision change (not a silent override): Phase 2 D-09 was authored under the assumption that the per-dose figures are the prescribing artifact and the daily total is informational/derived; the user's clarification (the daily total IS the prescribing artifact for daily-script + parent-education workflows) is the rationale for the visual-treatment-aspect-only supersedure.

6. **Quality gates pasted verbatim** from /tmp/04-05-gates/gate-NN-*.log (all 9):
   - Gate 1 svelte-check tail: `0 errors, 0 warnings`.
   - Gate 2 vitest src/lib/pert/: `Tests 81 passed (81)`.
   - Gate 3 em-dash + en-dash sweep: both 0.
   - Gate 4 tabular-numerals: count = 9 (UNCHANGED; baseline preserved).
   - Gate 5 Wave 4 + Wave 2 Tube-Feed treatment preserved: `tracking-wider` count = 2; `font-extrabold` count = 2; `Capsules per month` hits >= 2.
   - Gate 6 D-12 landed on the correct row: eyebrow + visible-text co-located = 1; numeral + expression co-located = 1; D-12 + D-13 cited in comment.
   - Gate 7 function-binding bridges + string-bridge proxies: counts (>= 3 / = 0).
   - Gate 8 pert-config.json byte-identical against HEAD: empty diff confirmed.
   - Gate 9 plan-level negative-space audit: `src/lib/pert/PertCalculator.svelte` only.

7. **Cross-mode parity confirmation** -- a dedicated subsection unique to this gap closure:
   - Post-Wave-5 the Oral row at lines ~211-227 and the Tube-Feed row at lines 284-299 share the same both-vectors typographic treatment.
   - Side-by-side class-string comparison (paste verbatim):
     - Oral eyebrow: `class="text-2xs font-semibold tracking-wider text-[var(--color-identity)] uppercase"`
     - Tube-Feed eyebrow: `class="text-2xs font-semibold tracking-wider text-[var(--color-identity)] uppercase"` (byte-identical from Wave 4 commit `d82aa30` line 288)
     - Oral numeral: `class="num text-title font-extrabold text-[var(--color-text-primary)]"`
     - Tube-Feed numeral: `class="num text-title font-extrabold text-[var(--color-text-primary)]"` (byte-identical from Wave 2 commit `29306e7` line 293)
   - The 6 derived-row eyebrows (page subtitle line 149 + Oral secondary lines 181 + 198 + Tube-Feed sibling lines 237 + 254 + 271) keep `tracking-wide`; the bump is asymmetric BY DESIGN.
   - The 5 derived-row numerals (Oral secondary lines 186 + 203 + Tube-Feed sibling lines 242 + 259 + 276) keep `text-title font-bold`; the bump is asymmetric BY DESIGN.
   - D-13 cross-mode parity per the Prescribing-Artifact-Leads Rule: structurally complete.

8. **Human-visual UAT outcome** -- a dedicated subsection (D-13 audit-trail requirement: verbatim verdict):
   - The user's resume signal verbatim from Task 3 (`approved` OR escalation-required note).
   - Coverage actually reviewed: which contexts the user verified by eye for Oral AND Tube-Feed (minimum: light-desktop-oral + light-desktop-tube-feed; recommended: 8 contexts including dark + mobile per mode).
   - If `approved`: 04-UAT.md test #4 gap row is closed; D-13 cross-mode parity confirmed; PERT-DESIGN-02..05 stay Validated after the user re-runs `/gsd-verify-work 4 --ws pert`.
   - If escalation-required: cite the residual issue verbatim + the recommended next escalation rung; defer escalation plan to the next planner pickup.

9. **Escalation ladder forward note (for the next planner pickup if D-12 + D-13 are still insufficient)** -- a dedicated subsection:
   - **Numeral size escalation:** bump the Oral tertiary numeral from `text-title` (22px) to `text-[28px]` (less invasive) OR `text-display` (44px; matches the hero numeral; potentially too loud). Mirror the Tube-Feed equivalent if cross-mode parity must hold. May require a row-padding adjustment.
   - **Visual isolation:** add a row-level treatment to the Oral tertiary `<div class="flex items-baseline justify-between px-5 py-4">` -- `bg-[var(--color-identity-hero)]/30` background tint, `border-t-2 border-[var(--color-identity)]` top divider, OR icon prefix (e.g. lucide `Pill` glyph). Mirror Tube-Feed if cross-mode parity must hold.
   - **Cross-mode rendering drift investigation:** if Approach C+D both pass on Tube-Feed but fail on Oral, investigate glyph-metric differences between `(3 meals/day)` parenthetical text and `Capsules per month` plain text; the `(3 meals/day)` parenthetical may visually push down the eyebrow's left-aligned weight despite identical class strings.
   - Recommendation: typographic deltas before container-level cues; one approach at a time per Wave-N gap-closure plan; do NOT chain.
   - Replan command if needed: `/gsd-plan-phase 4 --gaps --ws pert` after re-running UAT.

10. **D-13 future-audit forward note** -- a dedicated subsection:
    - The Prescribing-Artifact-Leads Rule lives as a workstream-local rule for v1.15.
    - Any post-Phase-4 design-polish sweep MUST explicitly inspect the prescribing artifact in BOTH Oral and Tube-Feed modes -- by name, side-by-side, in the same UAT pass.
    - The DESIGN.md update is deferred to v1.16 design-contract-refinement phase per 04-CONTEXT.md Deferred Ideas.
    - The next planner pickup that touches PERT typography (or any new calculator's secondary-card stack) MUST consult this rule and its audit gate.

11. **REQUIREMENTS.md re-validation note** -- a dedicated subsection:
    - This plan does NOT touch REQUIREMENTS.md.
    - PERT-DESIGN-02..05 are currently Validated (per Phase 4 closure record at Wave 3); the gap row in 04-UAT.md test #4 re-opens the question.
    - After this plan ships, the user re-runs `/gsd-verify-work 4 --ws pert`. The verifier re-checks the existing 04-UAT.md test #4 gap row against the post-D-12 UI:
      - If user-visual-UAT passes (Task 3 resume signal = `approved`): the gap row is marked resolved; D-13 cross-mode parity confirmed; PERT-DESIGN-02..05 stay Validated; Phase 4 closure is re-confirmed; the workstream is unblocked for Phase 5 (Release).
      - If user-visual-UAT fails (Task 3 resume signal = escalation-required note): the gap row stays open; PERT-DESIGN-02..05 STAY Validated (the validated state was Phase 4's own LLM-driven verdict; re-opening would require a deliberate flip), but the gap row provides the audit trail; the user re-runs `/gsd-plan-phase 4 --gaps --ws pert` for the next escalation rung.
    - The verification loop is: ship plan -> human UAT in checkpoint -> commit SUMMARY with outcome -> user runs `/gsd-verify-work 4 --ws pert` -> verifier re-checks gap row -> orchestrator updates STATE.md / ROADMAP.md / REQUIREMENTS.md as needed (ALL outside this plan's scope).

12. **Phase 3.1 + Phase 4 invariants regression guard** (paste from Gates 4, 5, 7):
    - Wave 4 + Wave 2 Tube-Feed treatment: `tracking-wider` count = 2 (1 from Wave 4 line 288 + 1 from new Wave 5 line ~216); `font-extrabold` count = 2 (1 from Wave 2 line 293 + 1 from new Wave 5 line ~221).
    - Function-binding bridges: 3+ hits in PertInputs.svelte.
    - String-bridge proxies: 0 hits in PertInputs.svelte.
    - Em-dash count on PertCalculator.svelte: 0.
    - En-dash count on PertCalculator.svelte: 0.
    - Tabular-numerals on PertCalculator.svelte: = 9 hits (baseline; UNCHANGED because the `class="num"` slot on the Oral tertiary numeral is preserved -- only size/weight tokens within the same class string changed).
    - STOP-red advisory carve-out preserved (`border-2 border-[var(--color-error)]` + `OctagonAlert` + `role="alert"` block at lines ~310-322 untouched per Wave 5's plan-level negative-space audit).
    - PertCalculator.test.ts:65 + 77 selectors continue to match (visible text byte-identical; Pitfall 4).

13. **Negative-space audit** (paste from Gate 9):
    - `git diff --name-only HEAD~1..HEAD -- src/ e2e/` returns exactly: `src/lib/pert/PertCalculator.svelte`.
    - Phase-level audit forward note: when the user runs the Phase 4 cumulative audit (`git diff --name-only $PHASE_BASELINE..HEAD -- src/ e2e/` where `$PHASE_BASELINE` is the parent of Wave 1 commit `eec18c2`), the result will be: `src/lib/pert/PertCalculator.svelte` (Wave 2 F-03 numeral + Wave 4 Approach C eyebrow + Wave 5 D-12 Oral 3-token swap; all font-weight/tracking/size only) + `src/lib/pert/pert-config.json` (commit `2dc7ae2` weight-range alignment) + `src/lib/shared/about-content.ts` (commit `5cd3386` PERT-entry-only xlsx removal, D-08a-style amendment per Phase 4 VERIFICATION.md acknowledgment). All within D-08 allowlist or pre-acknowledged D-08a-style amendments.

14. **Deviations from Plan** (mirroring Wave 4 SUMMARY's "Deviations" section):
    - List any deviation from the recipe (e.g. "the executor needed to install node_modules in a fresh worktree per the Wave 2 + Wave 4 deviation pattern; pnpm install --frozen-lockfile completed in Xs").
    - Capture any auto-fixes during execution (svelte-check warnings introduced + reverted, etc.).
    - Auth gates: none expected (the workflow is fully agent-runnable until Task 3's human checkpoint).

15. **PERT-DESIGN-02..05 satisfaction mapping** (which requirement closure each artifact contributes to):
    - PERT-DESIGN-02 (P1 fixes ship before merge; cheap P2/P3 inline) -- D-12 Oral hierarchy promotion is a cheap-inline P2 follow-up to the Oral hierarchy inversion gap caught at human UAT test #4; satisfied by Task 1 + the human-UAT confirmation.
    - PERT-DESIGN-03 (DESIGN.md / DESIGN.json contract enforced -- Identity-Inside Rule, Eyebrow-Above-Numeral, Tabular-Numbers, etc.) -- Identity-Inside preserved (eyebrow + numeral color tokens unchanged); Tabular-Numbers preserved (`.num` class hits = 9, unchanged); Eyebrow-Above-Numeral preserved (eyebrow `<div>` above numeral `<span>`); Five-Roles-Only preserved (the new size/weight tokens reuse existing typographic role slots, no new role introduced; D-13 codifies the Prescribing-Artifact-Leads Rule WITHIN the existing role taxonomy).
    - PERT-DESIGN-04 (HeroResult above-the-fold + sticky InputDrawer -- Phase 42.1 inheritance; F-04 P3 deferred) -- already satisfied by inheritance; F-04 deferred record stays in 04-FINDINGS.md.
    - PERT-DESIGN-05 (SegmentedToggle visual integration with identity hue) -- Phase 1 + Phase 2 already shipped; Wave 1 + Wave 3 critique surfaced no SegmentedToggle finding; satisfied by inheritance.
    - Note: PERT-DESIGN-01 (8 critique transcripts captured) was satisfied by Wave 1 (8 baseline + 8 FINAL transcripts). PERT-DESIGN-06 (>=35/40 score target) was satisfied by Wave 3 (aggregate 36.25/40 at LLM-Design-Review fallback); the human-UAT gap closures on F-03 + Oral hierarchy inversion may incrementally improve the score on a future re-run but are NOT required for the score bar (the >=35/40 target was met at Wave 3; these gap closures are quality-bar follow-ups, not score-target re-validations).

16. **Self-Check** section (mirrors Wave 4 SUMMARY's "Self-Check" section): all artifacts on disk, all gates green, all SUMMARY checklist items checked, human-UAT outcome captured verbatim, D-13 audit-both-modes principle propagated to the SUMMARY.

17. **Next step (the verification re-run + future escalation transition)**: the user runs `/gsd-verify-work 4 --ws pert` to re-check the 04-UAT.md test #4 gap row against the post-D-12 UI. If `approved`, Phase 4 closure is re-confirmed and Phase 5 is unblocked. If escalation-required, the user runs `/gsd-plan-phase 4 --gaps --ws pert` for the next escalation rung based on the residual-issue description.

**Em-dash + en-dash hygiene on the SUMMARY itself:** the workstream Q1 broad ban applies to this file too. After authoring, run:

```
grep -cP '\x{2014}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
grep -cP '\x{2013}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
```

Both must return 0. Use `--` (double ASCII hyphen) for any em-dash equivalent in prose; use `-` (ASCII hyphen) for any en-dash equivalent.

**Stage + commit:** stage `04-05-SUMMARY.md` and create a separate commit `docs(pert/04-05): summary -- D-12 Oral hierarchy promotion + D-13 cross-mode rule + verbatim user-visual verdict`. Do NOT amend Task 1's commit (the source-edit commit and the SUMMARY commit are conceptually separable; keeping them as 2 commits matches the Wave 4 pattern of separate Task 1 + Task 4 commits).
  </action>
  <verify>
    <automated>
      # 1. SUMMARY file exists and is >= 50 lines.
      test -f .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      test "$(wc -l < .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md)" -ge 50

      # 2. Required content markers present.
      grep -q "D-12" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "D-13" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "Prescribing-Artifact-Leads" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "Estimated daily total" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "Capsules per month" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "tracking-wider" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "font-extrabold" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "text-title" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "Pitfall 4" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "Identity-Inside" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "PertCalculator.svelte" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "/gsd-verify-work" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "PERT-DESIGN-02" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "Phase 3.1" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -qE "(cross-mode|both modes|both-mode)" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "human" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -qE "(audit|gate)" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "Phase 2 D-09" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -q "v1.16" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md

      # 3. Em-dash + en-dash sweep on the SUMMARY itself: both 0.
      test "$(grep -cP '\x{2014}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md)" -eq 0
      test "$(grep -cP '\x{2013}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md)" -eq 0

      # 4. Quality gate evidence pasted.
      grep -qE "(0 errors|0 warnings)" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md
      grep -qE "81 passed" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md

      # 5. Verbatim user-visual verdict captured (D-13 audit-trail requirement).
      # The SUMMARY MUST contain either the literal token "approved" OR an escalation-required note section header (the latter is also a valid outcome).
      grep -qE "(approved|escalation-required|residual issue)" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md

      # 6. SUMMARY committed (separate commit, NOT amended into Task 1's commit per the Wave 4 2-commit pattern).
      test -n "$(git log --oneline -3 -- .planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md)"
    </automated>
  </verify>
  <acceptance_criteria>
    - 04-05-SUMMARY.md exists at `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md` with >= 50 lines.
    - D-12 amendment record present: line range, before/after class strings (eyebrow + numeral), commit SHA, Wave 4 + Wave 2 Tube-Feed treatment byte-identity confirmation.
    - D-13 codification record present: workstream-local rule for v1.15; DESIGN.md update deferred to v1.16; audit-both-modes principle propagated as the future-audit forward note.
    - Cross-mode parity confirmation present: side-by-side class-string comparison of the Oral and Tube-Feed prescribing-artifact rows; structural parity confirmed.
    - Human-visual UAT outcome captured (resume signal verbatim from Task 3; coverage of contexts reviewed for BOTH modes).
    - Escalation ladder forward note present: numeral size escalation + visual isolation + cross-mode rendering drift investigation + replan command.
    - REQUIREMENTS.md re-validation note present: do NOT touch REQUIREMENTS.md from this plan; user re-runs `/gsd-verify-work 4 --ws pert` after this plan ships.
    - Phase 3.1 + Phase 4 invariants regression-guard pasted (Wave 4 + Wave 2 Tube-Feed treatment; function-binding 3+ hits; em-dash + en-dash 0/0; tabular-numerals = 9; PertCalculator.test.ts:65 + 77 selectors stable).
    - All 9 Task-2 gate outputs pasted verbatim (svelte-check, vitest, em+en-dash, tabular, Wave 4 + Wave 2 preservation, D-12 landed, function-bindings, config-history, neg-space).
    - PERT-DESIGN-02..05 satisfaction mapping documented.
    - Em-dash + en-dash count on the SUMMARY itself: 0 each (workstream Q1 broad ban honored).
    - SUMMARY committed as a separate commit (NOT amended into Task 1's commit; matches Wave 4 2-commit pattern).
  </acceptance_criteria>
  <done>
    - 04-05-SUMMARY.md authored with all 17 required sections (frontmatter, header, what shipped, source decisions, gap-closure rationale, quality gates, cross-mode parity confirmation, human-UAT outcome, escalation ladder forward note, D-13 future-audit forward note, REQUIREMENTS.md re-validation note, Phase 3.1 + Phase 4 invariants, negative-space audit, deviations, PERT-DESIGN satisfaction mapping, self-check, next step).
    - Em-dash + en-dash count on SUMMARY = 0 each.
    - Verbatim user-visual verdict captured (D-13 audit-trail requirement).
    - Commit landed; `git log -- 04-05-SUMMARY.md` returns at least 1 commit.
    - Verifier (`/gsd-verify-work 4 --ws pert`) is unblocked: it will read 04-05-SUMMARY.md as the audit trail for the D-12 amendment + D-13 codification + cross-mode parity confirmation + human-UAT outcome + the future-audit forward note for the next planner pickup if needed.
  </done>
</task>

</tasks>

<verification>
**Phase-level re-verification is the user's `/gsd-verify-work 4 --ws pert` re-run after this plan ships.** Wave 5's verification scope is the 9 fast-feedback gates run in Task 2 + the human-eye visual UAT in Task 3 (BOTH modes per D-13) + the SUMMARY content-marker checks run in Task 4.

The user's `/gsd-verify-work 4 --ws pert` re-run will:

1. Re-check the 04-UAT.md test #4 gap row against the post-D-12 UI.
2. Re-check the 04-UAT.md test #1 gap row (Wave 4 closure) is still resolved (the Tube-Feed treatment must remain byte-identical post-Wave-5; Gate 5 in Task 2 verifies this).
3. If the user's Task 3 resume signal in 04-05-SUMMARY.md was `approved`: the test #4 gap row is marked resolved; D-13 cross-mode parity is structurally complete; PERT-DESIGN-02..05 stay Validated; Phase 4 closure is re-confirmed; the workstream is unblocked for Phase 5 (Release).
4. If the user's Task 3 resume signal was escalation-required: the test #4 gap row stays open; PERT-DESIGN-02..05 STAY Validated (the validated state was Phase 4's own LLM-driven verdict; re-opening would require a deliberate flip), but the gap row provides the audit trail.
5. The verifier may re-run the heavy gates (pnpm build + pert-a11y + pert.spec + full Playwright + AUDIT.sh) at its discretion based on the standard /gsd-verify-work command sequence; this Wave 5 plan does NOT re-run them in-scope to conserve context (the D-12 edit is a class-string change with no behavioral or layout impact).

If Wave 5's plan-level gates fail (e.g. svelte-check non-zero or vitest count != 81 or em-dash + en-dash sweep > 0 or negative-space audit dirty or Wave 4 + Wave 2 Tube-Feed preservation broken or D-12 landed in the wrong row), the verifier cannot re-confirm closure until Wave 5 re-passes. The Task 2 gate sequence is fail-fast for exactly this reason.

If the human-UAT in Task 3 records escalation-required, the next planner pickup runs `/gsd-plan-phase 4 --gaps --ws pert` to author the next escalation rung; the verification loop continues from there.
</verification>

<success_criteria>
- D-12 amendment shipped: 3-token swap on the Oral `Estimated daily total (3 meals/day)` tertiary row in `src/lib/pert/PertCalculator.svelte` (lines ~211-227); eyebrow `tracking-wide` -> `tracking-wider`; numeral `text-base` -> `text-title`; numeral `font-medium` -> `font-extrabold`; comment lines updated to cite D-12 + D-13. Wave 4 + Wave 2 Tube-Feed treatment byte-identical (line 288 eyebrow + line 293 numeral). Identity-Inside Rule honored. Pitfall 4 selector preservation honored. 1 commit landed with the plan-frontmatter `commit_message`.
- D-13 codification shipped: the Prescribing-Artifact-Leads Rule lives as a workstream-local rule for v1.15 documented in 04-05-SUMMARY.md; DESIGN.md update deferred to v1.16. The audit-both-modes principle is the future-audit forward note for the next planner pickup.
- Cross-mode parity structurally complete: post-Wave-5, the Oral and Tube-Feed prescribing-artifact rows share the same both-vectors typographic treatment (eyebrow `tracking-wider` + numeral `text-title font-extrabold` + Identity-purple eyebrow color preserved on both).
- All 9 plan-level quality gates green: svelte-check 0/0; vitest src/lib/pert/ 81/81; em-dash + en-dash sweep 0/0 on PertCalculator.svelte; tabular-numerals = 9 hits (UNCHANGED); Wave 4 + Wave 2 Tube-Feed treatment preserved (tracking-wider count = 2; font-extrabold count = 2; `Capsules per month` hits >= 2); D-12 landed on the correct Oral row (eyebrow + visible-text co-located; numeral + expression co-located; comment cites D-12 + D-13); function-binding bridges >= 3 hits; string-bridge proxies = 0; pert-config.json byte-identical against HEAD; plan-level negative-space audit clean (only `src/lib/pert/PertCalculator.svelte` modified).
- Human-eye visual UAT checkpoint completed (Task 3); user's resume signal captured verbatim in 04-05-SUMMARY.md; D-13 audit-both-modes principle followed (user inspected BOTH Oral AND Tube-Feed prescribing-artifact rows in the same UAT pass).
- 04-05-SUMMARY.md authored at `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md` with all 17 required sections including the source decisions (D-12 + D-13 verbatim cites), the gap-closure rationale, the cross-mode parity confirmation, the human-UAT outcome, the escalation ladder forward note, the D-13 future-audit forward note, and the REQUIREMENTS.md re-validation note.
- D-08 PERT-route allowlist honored: only `src/lib/pert/PertCalculator.svelte` (and the planning artifact 04-05-SUMMARY.md) modified by this plan.
- D-08b strictly-forbidden boundary honored: zero shared component / NavShell / HamburgerMenu / other-calculator / DESIGN.md / DESIGN.json / calc-layer / PertInputs.svelte / pert-config.json / e2e / test edits.
- Phase 3.1 + Phase 4 invariants preserved: function-binding bridges intact, zero string-bridge proxies, em-dash + en-dash 0/0 across PERT-route files, STOP-red advisory carve-out untouched, Wave 4 + Wave 2 Tube-Feed treatment byte-identical.
- Verifier (`/gsd-verify-work 4 --ws pert`) is unblocked and ready to re-check the 04-UAT.md test #4 gap row against the post-D-12 UI.
</success_criteria>

<output>
This plan ships:

1. `src/lib/pert/PertCalculator.svelte` -- D-12 Oral hierarchy promotion: 3-token swap on the Oral `Estimated daily total (3 meals/day)` tertiary row at lines ~211-227 (eyebrow `tracking-wide` -> `tracking-wider`; numeral `text-base` -> `text-title`; numeral `font-medium` -> `font-extrabold`; comment lines updated to cite D-12 + D-13). The Wave 4 Tube-Feed Approach C eyebrow at line 288 from commit `d82aa30` is preserved byte-identical. The Wave 2 F-03 numeral at line 293 from commit `29306e7` is preserved byte-identical. PERT-route only; Identity-Inside Rule honored; Pitfall 4 selector preservation honored.

2. `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-05-SUMMARY.md` -- Wave 5 (gap-closure) closure record covering the D-12 amendment of Phase 2 D-09 (visual-treatment-aspect-only supersedure) + the D-13 codification of the Prescribing-Artifact-Leads Rule (workstream-local for v1.15; DESIGN.md update deferred to v1.16) + the cross-mode parity confirmation (Tube-Feed Wave 4 + Wave 2 + Oral Wave 5 prescribing-artifact treatments structurally identical post-fix) + the gap-closure rationale (LLM-Design-Review fallback overlooked the Oral hierarchy inversion at Phase 4 closure; Wave 4 human-verify only inspected Tube-Feed; D-13 audit-both-modes principle is the structural correction) + the human-eye visual UAT outcome (verbatim user-visual verdict per D-13 audit-trail requirement) + the escalation ladder forward note + the D-13 future-audit forward note + the REQUIREMENTS.md re-validation note (re-run `/gsd-verify-work 4 --ws pert` after this plan ships; do NOT flip REQUIREMENTS.md from this plan).

After commit, the user runs `/gsd-verify-work 4 --ws pert` to re-check the 04-UAT.md test #4 gap row against the post-D-12 UI. If the human-eye UAT in Task 3 recorded `approved`, the gap is closed, D-13 cross-mode parity is structurally complete, and Phase 5 (Release) is unblocked. If escalation-required, the user runs `/gsd-plan-phase 4 --gaps --ws pert` to author the next escalation rung based on the residual-issue description.
</output>

<self_check>
Mirroring the 10-dimension Self-Check structure of 04-04-PLAN.md:

1. **Frontmatter completeness:** `phase`, `plan`, `type`, `wave`, `workstream`, `milestone`, `depends_on`, `files_modified`, `requirements`, `autonomous`, `gap_closure`, `tags`, `must_haves` (truths + artifacts + key_links), `commit_message` -- all present. `gap_closure: true` is set per the orchestrator's `--gaps-only` filter requirement. `requirements` lists PERT-DESIGN-02..05 (the IDs Wave 2 + Wave 4 originally satisfied per 04-02-PLAN.md + 04-04-PLAN.md mapping; Wave 5 is the Oral parallel of Wave 4's Tube-Feed treatment and addresses the same hierarchy-bump quality bar in the Oral mode). `wave: 5` is correct (after 1/2/3/4). `depends_on` lists 04-01..04-04 (all prior plans).

2. **Task atomicity:** 4 tasks. Task 1 (source edit on PertCalculator.svelte; ~5-7% context, 3 token swaps + 2 comment-line updates on a single 17-line block). Task 2 (verification gates; ~5% context). Task 3 (human-eye visual UAT checkpoint; pauses for user input; D-13 audit-both-modes scope). Task 4 (SUMMARY authoring with 17 required sections; ~10-12% context). Total ~25-30% context; well under the 50% target. Each task is single-concern; no task touches more than 1 production file (Task 1 only) or only the planning artifact (Task 4 only).

3. **Verify gates:** every task has a `<verify>` block with `<automated>` commands. Task 1 has 17 grep-based and git-diff-based assertions (the 3-token swap leaves more verifiable invariants than Wave 4's 1-token swap). Task 2 has 9 gate-log-based and grep-based assertions. Task 3 has the human checkpoint resume signal (no automated verify; gate is the user typing) plus a self-referential check that the plan invokes the D-13 audit-both-modes principle. Task 4 has 6 grep-based assertions on the SUMMARY content + commit existence + the verbatim-verdict capture check.

4. **Acceptance criteria:** every task has an `<acceptance_criteria>` block enumerating the testable conditions. Task 1's criteria specify exact grep counts (`tracking-wider` = 2; `font-extrabold` = 2; `text-title` = 8; `tracking-wide`-only = 6; em + en-dash = 0; `.num` = 9; etc.). Task 2's criteria mirror the 9 gates. Task 4's criteria specify the 17 SUMMARY sections + the SUMMARY-level em + en-dash sweep + the verbatim-verdict capture.

5. **Done criteria:** every task has a `<done>` block with the post-task state of completion. Phrased in observable terms (file modified, commit landed, gates green, etc.).

6. **Identity-Inside Rule preservation:** explicitly enforced at multiple layers -- in the must_haves truths (truth #3), in the <interfaces> block (FORBIDDEN list), in Task 1's STEP B baseline check + STEP C edit + STEP D scope verification, in Task 1's verify gate #5 + #6 (eyebrow + numeral color tokens preserved), in Task 1's FORBIDDEN list, in Task 3's "DO NOT LOOK FOR" guidance, in Task 4's SUMMARY required sections (PERT-DESIGN-03 satisfaction mapping subsection).

7. **Pitfall 4 selector preservation:** explicitly enforced at multiple layers -- in the must_haves truths (truth #4), in the <interfaces> block (FORBIDDEN list), in Task 1's verify gates #7 + #8 (visible text + numeral expression preserved), in Task 1's FORBIDDEN list, in Task 1's STEP G commit body fields, in Task 4's SUMMARY required sections (Pitfall 4 selector preservation marker checked by verify gate). PertCalculator.test.ts:65 + 77 selectors are explicitly cited as the protection target throughout.

8. **D-08 PERT-route allowlist + D-08b strictly-forbidden boundary:** explicitly enforced at multiple layers -- in the must_haves truths (truth #5), in the <interfaces> block, in Task 1's FORBIDDEN list (12 explicit forbidden file/file-group entries including the new explicit ban on test-file edits), in Task 1's verify gates #13, #14, #15, #16, #17 (only PertCalculator.svelte modified; PertInputs / pert-config / calc-layer / test all byte-identical), in Task 2's Gate 8 + Gate 9, in Task 4's negative-space audit subsection.

9. **Wave 4 + Wave 2 Tube-Feed treatment preservation (regression guard for prior gap closures):** explicitly enforced at multiple layers -- in the must_haves truths (truth #7: "Wave 4 Tube-Feed Approach C treatment preserved byte-identical"), in the <interfaces> block (POST-FIX section shows Tube-Feed eyebrow + numeral byte-identical; FORBIDDEN list explicitly forbids re-touching the Tube-Feed row), in Task 1's STEP B baseline check (`tracking-wider` = 1 expected pre-edit from Wave 4; `font-extrabold` = 1 expected pre-edit from Wave 2), in Task 1's STEP D scope verification (counts must reach 2 each; the original 1 hit each from Wave 4 + Wave 2 must be preserved), in Task 1's verify gate #12, in Task 1's FORBIDDEN list, in Task 2's Gate 5 (regression guard: Tube-Feed treatment preserved), in Task 4's SUMMARY required sections (cross-mode parity confirmation subsection cites both rows side-by-side; Phase 3.1 + Phase 4 invariants regression-guard cites Wave 4 + Wave 2 preservation).

10. **D-13 audit-both-modes corrective process insertion (the core purpose of Wave 5):** explicitly framed at multiple layers -- in the must_haves truths (truth #2: "D-13 codification of the Prescribing-Artifact-Leads Rule"; truth #8: "Human-eye visual UAT gate (D-13 audit-both-modes corrective process insertion) replaces the LLM-Design-Review fallback verdict from Phase 4 closure"; truth #12: "D-13 future-audit forward note"; truth #13: "Verbatim user-visual verdict captured in 04-05-SUMMARY.md (D-13 audit-trail requirement)"), in the <objective> (multi-paragraph framing of the LLM-Design-Review fallback overlooking the Oral hierarchy inversion AND the Wave 4 mode-asymmetric human-verify NOT inspecting Oral; D-13 codifies the audit-both-modes principle), in Task 3 (the entire checkpoint task is this gate; <what-built> + <how-to-verify> + <resume-signal> are all structured around the human-eye review of BOTH Oral AND Tube-Feed in the same UAT pass; Task 3 verify gate explicitly checks the plan invokes "BOTH" + "D-13"), in Task 4's SUMMARY required sections (source decisions subsection cites D-12 + D-13 verbatim from 04-CONTEXT.md; gap-closure rationale subsection frames the audit-both-modes-as-corrective-process insertion; cross-mode parity confirmation subsection structurally proves D-13 is satisfied; D-13 future-audit forward note propagates the rule for the next planner pickup).

All 10 dimensions PASSED.

**Additional self-check items per the orchestrator's gap-closure planner constraints:**

- `gap_closure: true` in frontmatter: PRESENT.
- Plan numbering 04-05 (next sequential after 04-01, 04-02, 04-03, 04-04): CORRECT.
- Wave assignment `wave: 5`: CORRECT.
- Single plan, not multiple (no chained escalation rungs in this plan; the next rung is the next planner pickup if needed): CORRECT.
- D-08 allowlist + D-08b strictly-forbidden: ENFORCED (see dimension 8 above).
- Identity-Inside Rule (3 token swaps are typographic only -- letter-spacing + font-size + font-weight; NEVER chromatic): ENFORCED (see dimension 6 above).
- Pitfall 4 selector preservation: ENFORCED (see dimension 7 above; PertCalculator.test.ts:65 + 77 visible-text selectors stable).
- Em + en-dash sweep on PertCalculator.svelte = 0: ENFORCED at Task 1 STEP E + Task 2 Gate 3 + Task 1 verify gate #10.
- Human visual UAT as verification gate (D-13 audit-both-modes scope): ENFORCED at Task 3 (checkpoint:human-verify; the entire task is the human-eye review gate; Task 3 verify gate explicitly checks the plan invokes "BOTH" + "D-13").
- Commit message form `fix(pert/04-XX):`: PRESENT (`fix(pert/04-05): D-12 Oral daily-total promoted to prescribing-artifact treatment (D-13 cross-mode rule)`).
- REQUIREMENTS.md NOT touched: ENFORCED (the must_haves truth #11 documents the loop; Task 4's SUMMARY required sections include the REQUIREMENTS.md re-validation note explicitly stating "this plan does NOT touch REQUIREMENTS.md").
- Tasks structure mirrors 04-04-PLAN.md: TASK 1 (source edit) + TASK 2 (gates) + TASK 3 (human checkpoint, D-13 audit-both-modes scope) + TASK 4 (SUMMARY).
- Task-level <verify> blocks include grep assertions: PRESENT throughout.
- <interfaces> block with code excerpts (CURRENT vs POST-FIX): PRESENT (the eyebrow `<div>` + numeral `<span>` + comment lines before/after; reference to Tube-Feed treatment byte-identity).
- must_haves block covers (a) D-12 supersedes Phase 2 D-09 for visual-hierarchy aspect only (truth #1), (b) D-13 codifies the cross-mode rule (truth #2), (c) Identity-Inside preserved (truth #3), (d) Pitfall 4 e2e + test selectors preserved (truth #4), (e) D-08 allowlist honored (truth #5), (f) Phase 3.1 invariants preserved (truth #6), (g) Wave 4 + Wave 2 Tube-Feed treatment byte-identical (truth #7), (h) human-verify gate is non-substitutable -- D-13 audit-both-modes scope (truth #8), (i) verbatim user-visual verdict captured in SUMMARY (truth #13): ALL PRESENT.
- Self-Check at the end of the plan with 10-dimension structure: PRESENT (this section).

Self-Check: PASSED.
</self_check>
</content>
