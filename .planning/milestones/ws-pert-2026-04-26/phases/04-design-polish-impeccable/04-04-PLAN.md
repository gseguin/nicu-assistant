---
phase: 4
plan: 4
type: execute
wave: 4
workstream: pert
milestone: v1.15
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - src/lib/pert/PertCalculator.svelte
  - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
requirements:
  - PERT-DESIGN-02
  - PERT-DESIGN-03
  - PERT-DESIGN-04
  - PERT-DESIGN-05
autonomous: false
gap_closure: true
tags: [pert, design-polish, gap-closure, wave-4, f-03, uat-escalation, approach-c]
must_haves:
  truths:
    - "F-03 escalation shipped (Approach C, plan-anticipated fallback per 04-02-PLAN.md): in src/lib/pert/PertCalculator.svelte the Tube-Feed `Capsules per month` row's eyebrow `<div>` (currently at lines 286-289 in HEAD; verify exact line range at edit time via `grep -n 'Capsules per month' src/lib/pert/PertCalculator.svelte`) gets a 1-token swap from `tracking-wide` to `tracking-wider`. The numeral on line 293 (already `font-extrabold` from F-03 fix at commit 29306e7) is NOT touched -- it stays byte-identical. The bump scope is the SINGLE eyebrow `<div>` in the `Capsules per month` block; the 7 sibling `tracking-wide` eyebrows above it (the page subtitle eyebrow at line 149 + the 6 other secondary-row eyebrows at lines 181/198/216/237/254/271) are untouched. Asymmetric BY DESIGN: only the prescribing artifact (Capsules per month) gets the both-vectors typographic bump (extrabold weight + wider tracking) so it reads visually distinct from the 3 derived sibling rows (Total fat / Total lipase / Lipase per kg)."
    - "Identity-Inside Rule (DESIGN.md line 186 + Phase 1 D-03 + Phase 1 D-17) honored: the eyebrow color stays `text-[var(--color-identity)]` (NEVER changed); the numeral color stays `text-[var(--color-text-primary)]` (NEVER changed). The escalation is a TRACKING change (letter-spacing only), NOT a color change. The eyebrow stays identity-purple inside the route; chrome (top tabs, bottom nav, hamburger) is untouched."
    - "Pitfall 4 selector preservation honored: the visible text `Capsules per month` is preserved verbatim (no rename, no abbreviation). The numeral expression `{tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}` is preserved verbatim (no format change). The e2e selector at e2e/pert.spec.ts that grep-finds these strings continues to match. ZERO e2e selector update permitted."
    - "D-08 PERT-route allowlist honored: the only file edits in this plan's commits are `src/lib/pert/PertCalculator.svelte` (allowed) and the planning artifact `04-04-SUMMARY.md` at the phase directory. Strictly forbidden per D-08b: shared components (`src/lib/shared/components/**` including HeroResult, SegmentedToggle, SelectPicker, NumericInput, RangedNumericInput, InputDrawer, DisclaimerBanner, AboutSheet), shell (`src/lib/shell/NavShell.svelte`, `src/lib/shell/HamburgerMenu.svelte`), other calculators (`src/lib/{morphine-wean,formula,gir,feeds,uac-uvc}/**`), `DESIGN.md`, `DESIGN.json`, calc-layer (`src/lib/pert/calculations.ts`, `src/lib/pert/state.svelte.ts`, `src/lib/pert/types.ts`, `src/lib/pert/config.ts`), `src/lib/pert/PertInputs.svelte` (Phase 3.1 frozen), `src/lib/pert/pert-config.json` (Phase 4 commit 2dc7ae2 frozen), `e2e/pert.spec.ts` and `e2e/pert-a11y.spec.ts` (Pitfall 4 selector preservation means no e2e edit needed)."
    - "Phase 3.1 invariants preserved per UI-SPEC §'PERT Component Invariants': function-binding bridges intact at 3 hits in `src/lib/pert/PertInputs.svelte` (medication / strength / formula at the post-Phase-3.1 multi-line `bind:value={` shape; this plan does NOT touch PertInputs.svelte); zero string-bridge proxies (`grep -cE 'let \\w+(Bridge|Proxy) = \\$state' src/lib/pert/PertInputs.svelte` returns 0); em-dash count 0 (`grep -cP '\\x{2014}' src/lib/pert/PertCalculator.svelte` returns 0); en-dash count 0 (`grep -cP '\\x{2013}' src/lib/pert/PertCalculator.svelte` returns 0); STOP-red advisory carve-out preserved (`border-2 border-[var(--color-error)]` + `OctagonAlert` + `role=\"alert\"` block at lines ~310-322 byte-identical -- this plan's edit is at line ~288 which is upstream of the STOP-red block); F-03 numeral `font-extrabold` from commit 29306e7 preserved (`grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` still returns exactly 1)."
    - "User-visual UAT gate replaces the LLM-Design-Review verdict from Phase 4 closure (Plan 04-03's 04-VERIFICATION.md certified F-03 as PASS based on LLM-Design-Review fallback transcripts; human UAT recorded in 04-UAT.md contradicted that verdict). This plan's verification GATE is human-eye review of the rendered Tube-Feed mode at `/pert` after the fix ships -- minimum coverage: light-desktop-tube-feed (the surfaced-issue context). Recommended coverage: light + dark x mobile + desktop x tube-feed (4 contexts). The user must explicitly type `approved` or describe residual issues at the checkpoint task. NO automated DOM inspection alone, NO LLM-generated /impeccable score, can substitute for the human-eye gate. This is the lesson from the 04-VERIFICATION.md fallback-overlooked-the-issue postmortem encoded as a hard process gate."
    - "Quality gates: `pnpm exec svelte-check` returns 0 errors / 0 warnings (Phase 3.1 baseline preserved, ~4586 files); `pnpm exec vitest run src/lib/pert/` returns 81/81 (the PERT vitest subset: PertCalculator.test.ts 10 + PertInputs.test.ts 9 + calculations.test.ts 45 + config.test.ts 17 = 81; matches Phase 3.1 + Phase 4 baseline because no test files are touched and the edit is a Tailwind class-string change that does not affect any DOM-asserted selector); em-dash + en-dash sweep on PertCalculator.svelte returns 0 hits each via `grep -cP '\\x{2014}'` and `grep -cP '\\x{2013}'`; `grep -cE 'class=\"[^\"]*\\bnum\\b' src/lib/pert/PertCalculator.svelte` returns at least 5 (UI-SPEC Gate 12; baseline = 9, unchanged because tracking-wider edit is on the eyebrow `<div>`, not on a `.num` numeral)."
    - "Negative-space audit at plan close: `git diff --name-only HEAD~1..HEAD -- src/ e2e/` returns ONLY `src/lib/pert/PertCalculator.svelte` (the Approach C commit). No other source/test/config touched. The PERT-route allowlist boundary (`src/lib/pert/**` + `src/routes/pert/**`) holds. After this plan ships, the cumulative phase diff (`git diff --name-only $PHASE_BASELINE..HEAD -- src/ e2e/` where $PHASE_BASELINE is the parent of Wave 1 commit `eec18c2`) returns: `src/lib/pert/PertCalculator.svelte` (Wave 2 F-03 + Wave 4 Approach C escalation, both font-weight/tracking only) + `src/lib/pert/pert-config.json` (commit 2dc7ae2 weight-range alignment) + `src/lib/shared/about-content.ts` (commit 5cd3386 PERT-entry-only xlsx removal, D-08a-style amendment). All within D-08 allowlist or pre-acknowledged D-08a-style amendments."
    - "F-03 acceptance gate per 04-UAT.md: post-fix, the Capsules-per-month row reads visually distinct from the 3 derived rows (Total fat / Total lipase / Lipase per kg) when viewed BY HUMAN EYE in the rendered Tube-Feed mode at `/pert`. The combined typographic delta (extrabold weight from F-03 + wider tracking from Approach C) creates a sufficient hierarchy bump that the prescribing artifact is findable faster than the 3 derived figures. Human UAT either confirms PASS (gap closed) OR records that Approach C is still insufficient -- in which case the next escalation is Approach D (text-title 22px -> text-[28px] or text-display 44px on the Capsules per month numeral) authored as plan 04-05 by re-running `/gsd-plan-phase 4 --gaps --ws pert`."
    - "PERT-DESIGN-02..05 re-validation handled by re-running `/gsd-verify-work 4 --ws pert` after this plan ships (NOT by flipping the REQUIREMENTS.md traceability table back to Active in this plan). The verification command will re-check the existing 04-UAT.md row against the post-Approach-C UI; if visual UAT passes, the gap row in 04-UAT.md is marked resolved and PERT-DESIGN-02..05 stay Validated. If visual UAT still fails, the user re-runs `/gsd-plan-phase 4 --gaps --ws pert` for Approach D escalation. This plan does NOT touch REQUIREMENTS.md."
  artifacts:
    - path: src/lib/pert/PertCalculator.svelte
      provides: "Post-Approach-C-escalation component: Tube-Feed `Capsules per month` row eyebrow `<div>` (line ~288) bumped from `tracking-wide` to `tracking-wider`. The numeral `<span>` on line ~293 (already `font-extrabold` from F-03 commit 29306e7) is byte-identical. The 7 sibling `tracking-wide` eyebrows are byte-identical. Combined visual effect: the prescribing artifact gets BOTH heavier weight (font-extrabold) AND wider letter-spacing (tracking-wider) on its label/value pair, creating a stronger hierarchy bump than F-03's weight-only Approach A. Verified by `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` returning exactly 1 AND `grep -c 'tracking-wide' src/lib/pert/PertCalculator.svelte` returning exactly 7 (drops by 1 from the pre-edit baseline of 8) AND `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` still returning exactly 1 (F-03 fix preserved)."
      contains: "Capsules per month"
      contains: "tubeFeedResult.capsulesPerMonth"
      contains: "tracking-wider"
      contains: "font-extrabold"
    - path: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      provides: "Wave 4 (gap-closure) closure record: F-03 Approach C escalation shipped; before/after class strings for the eyebrow `<div>` pasted verbatim; F-03 numeral preservation confirmed (grep count stays at 1 hit); plan-level negative-space audit clean (only PertCalculator.svelte modified); Phase 3.1 invariants regression-guard pasted (function-binding 3 hits, em + en-dash 0/0, tabular-numerals >= 5); Phase 4 invariants regression-guard pasted (font-extrabold 1 hit preserved); user-visual-UAT outcome recorded (`approved` OR escalation-required note); next-step note documenting the future escalation path (Approach D = numeral text-title -> text-[28px] or text-display; Approach E = visual isolation via background tint or top divider) for the next planner pickup if needed; PERT-DESIGN-02..05 re-validation note (re-run /gsd-verify-work 4 --ws pert after this plan ships; do NOT flip REQUIREMENTS.md from this plan)."
      contains: "Approach C"
      contains: "tracking-wider"
      contains: "font-extrabold"
      contains: "Pitfall 4"
      contains: "Identity-Inside"
      contains: "approved"
      min_lines: 50
  key_links:
    - from: src/lib/pert/PertCalculator.svelte
      to: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-UAT.md
      via: "the gap row in 04-UAT.md (`F-03 visual hierarchy on Tube-Feed Capsules per month` test #1) cites Approach C as the plan-anticipated fallback; this plan applies that exact fix; the post-fix verification command `/gsd-verify-work 4 --ws pert` re-checks the gap row against the rendered UI"
      pattern: "Approach C"
    - from: src/lib/pert/PertCalculator.svelte
      to: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-PLAN.md
      via: "the 04-02-PLAN.md `<interfaces>` block at lines 112-155 documents Approach C verbatim as the COMBINED RECIPE (eyebrow tracking-wide -> tracking-wider AND numeral font-bold -> font-extrabold); F-03 commit 29306e7 shipped only the numeral half (Approach A); this Wave 4 plan ships the eyebrow half to complete Approach C"
      pattern: "Approach C"
    - from: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      to: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-UAT.md
      via: "SUMMARY's user-visual-UAT outcome section records the approval (or escalation-required note); the verifier reads SUMMARY to know whether to mark the 04-UAT.md gap row resolved or open Approach D"
      pattern: "user-visual-UAT"
commit_message: "fix(pert/04-04): F-03 escalation to Approach C -- eyebrow tracking-wider bump"
---

<objective>
**Wave 4 of Phase 4 -- gap closure for the F-03 visual hierarchy issue surfaced by human UAT.**

Phase 4 was certified Complete on 2026-04-26 (Wave 3 plan 04-03 closed; PERT-DESIGN-01..06 flipped Active -> Validated; aggregate score 36.25/40 across 8 LLM-Design-Review critique transcripts). However, when the user ran human UAT against the post-Wave-2 Tube-Feed UI, they reported the F-03 fix (Approach A: numeral `font-bold` -> `font-extrabold` at commit `29306e7`) is NOT visually distinguishable -- "the font-weight 700 -> 800 delta on Plus Jakarta Sans at text-title (22px) does not produce a perceptible hierarchy bump. All 4 numerals read at equivalent weight by eye."

The 04-02-PLAN.md `<interfaces>` block at lines 112-155 anticipated this exact failure mode and documented Approach C (combined: numeral font-extrabold + eyebrow tracking-wide -> tracking-wider) as the plan-anticipated fallback. This Wave 4 plan ships the eyebrow half of Approach C to complete the typographic escalation.

**Why a gap-closure plan inside Phase 4 (not a Phase 4.1 / Phase 6):** the fix scope is identical to Wave 2 (single-token swap on PertCalculator.svelte; PERT-route only; ~1 LOC; D-08 allowlist honored; no e2e selector change; Phase 3.1 invariants preserved). It is the literal next step in the plan-anticipated escalation ladder authored at 04-02-PLAN.md `<interfaces>`. Phase 5 release-readiness is unblocked by this fix landing; Phase 4 closure record (04-VERIFICATION.md) acknowledges the LLM-Design-Review fallback overlooked the issue and the human-UAT loop is the corrective process gate.

**This plan ships exactly two things:**

1. **F-03 Approach C escalation** (Task 1 + Task 2 + Task 3): a 1-LOC eyebrow tracking bump on the Capsules per month row of `src/lib/pert/PertCalculator.svelte` (Tube-Feed branch, line ~288). The single token swap is `tracking-wide` -> `tracking-wider` on the eyebrow `<div class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase">`. The bump is typographic (letter-spacing only) -- NEVER chromatic (Identity-Inside Rule reserves eyebrow color for `text-[var(--color-identity)]`). The visible text `Capsules per month` and the numeral display format are PRESERVED -- e2e selectors at `e2e/pert.spec.ts` still match (Pitfall 4 reminder). The numeral on line ~293 already has `font-extrabold` from F-03 commit 29306e7 and stays byte-identical -- this Wave 4 plan adds the second vector (wider tracking) to amplify the hierarchy bump beyond what extrabold alone produced.

2. **Human-eye visual UAT checkpoint** (Task 3): a `checkpoint:human-verify` task that asks the user to navigate to `/pert` in the rendered Tube-Feed mode and confirm by eye whether the combined typographic delta (extrabold weight + wider tracking) makes the Capsules per month row read visually distinct from the 3 sibling derived rows (Total fat / Total lipase / Lipase per kg). The user types `approved` if the gap is closed, OR describes residual issues if Approach C is still insufficient. This human-eye gate is the corrective process insertion against the LLM-Design-Review fallback that overlooked the issue at Phase 4 closure.

3. **04-04-SUMMARY.md** (Task 4): the Wave 4 closure record. Documents (a) the Approach C escalation line range + exact before/after class string + verify-gate output; (b) the human-UAT outcome (`approved` or escalation-required note); (c) the future escalation path (Approach D = numeral text-title -> text-[28px] or text-display; Approach E = visual isolation via background tint or top divider) for the next planner pickup if Approach C also proves insufficient; (d) the PERT-DESIGN-02..05 re-validation note (re-run `/gsd-verify-work 4 --ws pert` after this plan ships; do NOT flip REQUIREMENTS.md from this plan).

**This plan does NOT:**

- Touch `src/lib/pert/PertInputs.svelte` (Phase 3.1 frozen; function-binding bridges + em-dash + en-dash count 0 + zero string-bridge proxies must hold).
- Touch `src/lib/pert/pert-config.json` (Phase 4 commit 2dc7ae2 frozen; re-editing would either revert the weight-range alignment or no-op + dirty the diff).
- Touch `src/lib/pert/calculations.ts`, `src/lib/pert/state.svelte.ts`, `src/lib/pert/types.ts`, `src/lib/pert/config.ts` (calc-layer + state shape are Phase 2/3.1 frozen).
- Touch any shared component, NavShell, HamburgerMenu, other calculators, DESIGN.md, DESIGN.json (D-08b strictly forbidden).
- Touch `e2e/pert.spec.ts` or `e2e/pert-a11y.spec.ts` (Pitfall 4 reminder applied: Approach C changes letter-spacing only, not text content; selectors still match -- ZERO selector update permitted).
- Re-touch the F-03 numeral on line ~293 (already `font-extrabold` from commit 29306e7; this plan extends the fix on the eyebrow, NOT on the numeral; the numeral stays byte-identical).
- Re-run the 8-context `/impeccable` critique sweep -- the LLM-Design-Review fallback was the source of the false-PASS at Phase 4 closure; this plan replaces that gate with human-eye UAT.
- Update STATE.md / ROADMAP.md / REQUIREMENTS.md -- those updates are the orchestrator's responsibility after `/gsd-verify-work 4 --ws pert` re-runs and confirms the gap closed.
- Apply Approach D (text-title -> text-[28px] / text-display on the numeral) OR Approach E (visual isolation: background tint, top divider, icon prefix). Per the gap-closure planner's recommendation: ship Approach C alone, re-run UAT, escalate to D or E only if C is still insufficient. This avoids ratcheting up multiple typographic vectors simultaneously and over-shooting the hierarchy.

**Escalation ladder if Approach C is still insufficient (forward note for the next planner pickup):**

If the user-visual-UAT checkpoint in Task 3 records that the combined Approach A + C delta (extrabold + tracking-wider) STILL reads as insufficient hierarchy by eye, the next planner pickup should author a Wave 5 plan applying:

- **Approach D (typographic escalation):** bump the Capsules per month numeral from `text-title` (22px) to a custom `text-[28px]` (less invasive) OR `text-display` (44px; matches the hero numeral; potentially too loud). Larger size + extrabold weight together creates an unambiguous hero-secondary that visually leads. Slightly more invasive (changes line-height + row spacing in the divide-y stack); may require a row-padding adjustment.
- **Approach E (visual isolation, no further typographic change):** add a row-level treatment to the Capsules per month `<div class="flex items-baseline justify-between px-5 py-4">` -- e.g. `bg-[var(--color-identity-hero)]/30` background tint, OR `border-t-2 border-[var(--color-identity)]` top divider, OR icon prefix (e.g. lucide `Pill` glyph). Restores hierarchy via container-level cue rather than typographic delta.

Recommend Approach D over Approach E if the typographic deltas are exhausted; recommend Approach E if Approach D feels too loud or breaks row alignment. The escalation should be one approach at a time per Wave-N gap-closure plan, NOT chained into the same plan.

**Per 04-UAT.md acceptance:** post-fix, the Capsules-per-month row reads visually distinct from the 3 derived rows in the rendered Tube-Feed mode at `/pert`, confirmed by HUMAN EYE in the user-visual-UAT checkpoint task. The combined Approach A + C delta is the v1.13-precedent-grounded smallest-diff escalation; ship and re-test before climbing the ladder further.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/pert/STATE.md
@.planning/workstreams/pert/ROADMAP.md
@.planning/workstreams/pert/REQUIREMENTS.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-CONTEXT.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-RESEARCH.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-UI-SPEC.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-FINDINGS.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-VERIFICATION.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-UAT.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-PLAN.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
@DESIGN.md
@CLAUDE.md
@src/lib/pert/PertCalculator.svelte

<interfaces>
<!-- Pre-extracted code excerpts for the Approach C edit site. Captured from HEAD post-F-03-fix (commit 29306e7 shipped Approach A on the numeral; this plan adds the Approach C eyebrow half). The executor reads PertCalculator.svelte fresh and verifies line numbers at edit time, since pre-edit line numbers may shift if any unrelated commit lands first. -->

CURRENT (Tube-Feed `Capsules per month` row at lines 284-299 in src/lib/pert/PertCalculator.svelte HEAD; eyebrow `<div>` is at lines 286-289; numeral `<span>` is at line 293):
```svelte
				<!-- Capsules per month (F-03: extra-bold to elevate prescribing artifact above derived siblings; Identity-Inside preserved). -->
				<div class="flex items-baseline justify-between px-5 py-4">
					<div>
						<div
							class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase"
						>
							Capsules per month
						</div>
						<div class="flex items-baseline gap-2">
							<span class="num text-title font-extrabold text-[var(--color-text-primary)]">
								{tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}
							</span>
							<span class="text-ui text-[var(--color-text-secondary)]">capsules</span>
						</div>
					</div>
				</div>
```

Note the F-03 numeral fix from commit 29306e7 is already in place at line 293 (`font-extrabold` instead of `font-bold`). This plan adds the eyebrow half on line 288.

POST-FIX (Approach C: eyebrow tracking bump; numeral byte-identical):
```svelte
				<!-- Capsules per month (F-03 + Approach C escalation: extra-bold numeral + tracking-wider eyebrow to elevate prescribing artifact above derived siblings; Identity-Inside preserved). -->
				<div class="flex items-baseline justify-between px-5 py-4">
					<div>
						<div
							class="text-2xs font-semibold tracking-wider text-[var(--color-identity)] uppercase"
						>
							Capsules per month
						</div>
						<div class="flex items-baseline gap-2">
							<span class="num text-title font-extrabold text-[var(--color-text-primary)]">
								{tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}
							</span>
							<span class="text-ui text-[var(--color-text-secondary)]">capsules</span>
						</div>
					</div>
				</div>
```

Diff: `tracking-wide` -> `tracking-wider` on the eyebrow `<div>` (1 token swap). The HTML comment may OPTIONALLY be expanded from `<!-- Capsules per month (F-03: extra-bold to elevate prescribing artifact above derived siblings; Identity-Inside preserved). -->` to `<!-- Capsules per month (F-03 + Approach C escalation: extra-bold numeral + tracking-wider eyebrow to elevate prescribing artifact above derived siblings; Identity-Inside preserved). -->` to record the escalation rationale inline. Comment update is OPTIONAL; the functional change is the `tracking-wide` -> `tracking-wider` swap on the eyebrow `<div>`.

The 7 sibling `tracking-wide` eyebrows in this file (verified by `grep -nE "tracking-wide" src/lib/pert/PertCalculator.svelte` -- 8 hits pre-fix at lines 149, 181, 198, 216, 237, 254, 271, 288; post-fix should be 7 hits at lines 149, 181, 198, 216, 237, 254, 271 with line 288 flipped to tracking-wider) are UNTOUCHED. The bump is asymmetric BY DESIGN: only the prescribing artifact (Capsules per month, line 288) gets the wider tracking; the page subtitle (line 149) and the 6 other secondary-row eyebrows stay at `tracking-wide`.

The numeral `<span>` on line 293 is byte-identical (already `font-extrabold` from commit 29306e7).

FORBIDDEN: any change to the eyebrow's `text-[var(--color-identity)]` color token (Identity-Inside Rule per DESIGN.md line 186 + Phase 1 D-03 + Phase 1 D-17). FORBIDDEN: any change to the visible text `Capsules per month` (e2e selector at e2e/pert.spec.ts would break per Pitfall 4). FORBIDDEN: any change to the numeral display (`{tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}` -- this is the same expression e2e/pert.spec.ts resolves via `getByText('150', { exact: true })` for the Tube-Feed happy path). FORBIDDEN: any change to the numeral's `font-extrabold` weight (F-03 fix from commit 29306e7 must be preserved).

Pre-fix grep counts (verify before editing):
- `grep -c 'tracking-wide' src/lib/pert/PertCalculator.svelte` returns 8
- `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` returns 0
- `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` returns 1 (F-03 fix, line 293)

Post-fix grep counts (verify after editing):
- `grep -c 'tracking-wide' src/lib/pert/PertCalculator.svelte` returns 7 (one flipped to tracking-wider)
- `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` returns 1 (the Capsules per month eyebrow at line 288)
- `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` returns 1 (F-03 fix preserved, byte-identical)

NOTE: `tracking-wide` is a substring of `tracking-wider`; the grep counts above use bare-token form which would over-count. Use word-boundary-aware grep for the precise count:
- `grep -cE 'tracking-wide(\s|"|\b)' src/lib/pert/PertCalculator.svelte` for tracking-wide-only count (excludes tracking-wider).
- Equivalently, `grep -oE 'tracking-wide[^r]' src/lib/pert/PertCalculator.svelte | wc -l` then check that no line has `tracking-wide"` followed by a non-`r` character.

The verification commands in Task 1 use `grep -oE 'tracking-wide(r|[^a-z])'` to disambiguate the two tokens cleanly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply F-03 Approach C escalation -- eyebrow tracking-wide -> tracking-wider on the Capsules per month row in PertCalculator.svelte</name>
  <read_first>
    - src/lib/pert/PertCalculator.svelte (read in full; verify the actual line range for the Tube-Feed `Capsules per month` row -- citing target ~284-299 from HEAD with the eyebrow `<div>` at lines 286-289; the executor must confirm at edit time since the file may have shifted if any unrelated commit landed since this plan was authored)
    - DESIGN.md (offset 180, limit 35 -- "The Identity-Inside Rule" section starting at line 186; the eyebrow color token `var(--color-identity)` is reserved for inside-the-route surfaces and the Approach C bump must NOT alter the eyebrow color)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-CONTEXT.md (offset 91, limit 11 -- D-08 allowlist; the only allowed source/test edit in this task is `src/lib/pert/PertCalculator.svelte`)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-UAT.md (read in full; the Gaps section documents the F-03 escalation requirement and lists Approach C/D/E options in escalation order)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-PLAN.md (offset 112, limit 50 -- the `<interfaces>` block documents Approach C verbatim as the COMBINED RECIPE; this Wave 4 plan ships the eyebrow half of that recipe)
    - e2e/pert.spec.ts (skim the Tube-Feed happy-path test; confirm the selector for `Capsules per month` -- the executor must NOT change strings these selectors target; Pitfall 4)
  </read_first>
  <files>src/lib/pert/PertCalculator.svelte</files>
  <action>
**STEP A -- Re-read the actual line range.** The plan-cited range is lines 286-289 for the eyebrow `<div>` but the executor must confirm by reading PertCalculator.svelte fresh. Use:

```
grep -n "Capsules per month" src/lib/pert/PertCalculator.svelte
```

Expected: 2 hits -- one in the HTML comment line (line ~284 in HEAD) and one in the visible-text line inside the eyebrow `<div>` (line ~290 in HEAD). The eyebrow `<div>` opening tag with the class string is the line directly above the visible-text line (line ~287 with the `<div` opening; line ~288 with the `class="..."` attribute). If line numbers have shifted, proceed with the actual range -- the task targets the eyebrow `<div>` containing the class string `text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase` immediately above the `Capsules per month` visible text, regardless of exact line number.

**STEP B -- Verify the pre-edit baseline.** Run these greps to confirm the file is in the expected state before editing:

```
grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte
```
Expected: `1` (the F-03 numeral fix from commit 29306e7 is in place). If 0, the F-03 fix has been reverted -- STOP and escalate to the user; do NOT proceed (this plan extends the fix; it does not re-apply F-03).

```
grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte
```
Expected: `0` (Approach C has not yet been applied). If 1+, this plan has already partly run -- STOP and inspect; the executor may have re-entered after a partial edit.

```
grep -oE 'tracking-wide(r|[^a-z]|"|$)' src/lib/pert/PertCalculator.svelte | grep -v "tracking-wider" | wc -l
```
Expected: `8` (8 eyebrow `tracking-wide` hits across the file: 1 page subtitle at line ~149 + 7 secondary-row eyebrows at lines ~181, 198, 216, 237, 254, 271, 288). If different, the file has drifted from the plan-authoring baseline -- pause and reconcile.

**STEP C -- Apply the edit.** Use the Edit tool. The specific edit is:

Old text (must match exactly; verify uniqueness via the surrounding context including the `Capsules per month` visible-text line below the class line, since the bare class string `text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase` appears 7 times in the file):

```
					<div
						class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase"
					>
						Capsules per month
					</div>
```

(Use the surrounding `Capsules per month` text inside the `<div>` content as the disambiguating anchor. The `Edit` tool's `old_string` must include the visible-text line below the class line so the match is unique to the Capsules per month row.)

New text (Approach C eyebrow bump):

```
					<div
						class="text-2xs font-semibold tracking-wider text-[var(--color-identity)] uppercase"
					>
						Capsules per month
					</div>
```

Diff: 1 character change inside the class attribute (`tracking-wide` -> `tracking-wider` on the eyebrow `<div>` of the Capsules per month row). The visible text `Capsules per month` is preserved verbatim.

OPTIONALLY, also expand the HTML comment line above the row to record the Approach C escalation:

Old comment (line ~284, optional):
```
				<!-- Capsules per month (F-03: extra-bold to elevate prescribing artifact above derived siblings; Identity-Inside preserved). -->
```

New comment (Approach C annotation):
```
				<!-- Capsules per month (F-03 + Approach C escalation: extra-bold numeral + tracking-wider eyebrow to elevate prescribing artifact above derived siblings; Identity-Inside preserved). -->
```

The comment update is optional; the executor may keep the original comment if preferred. The functional change is the `tracking-wide` -> `tracking-wider` swap on the eyebrow `<div>`.

**STEP D -- Verify the change is scoped correctly.**

After editing, run:

```
grep -n "tracking-wider" src/lib/pert/PertCalculator.svelte
```
Expected: exactly 1 hit, on the `Capsules per month` eyebrow `<div>` (line ~288 in HEAD). If 0 hits, the edit didn't land. If 2+ hits, the executor accidentally changed sibling rows -- revert and re-edit with disambiguating context.

```
grep -oE 'tracking-wide(r|[^a-z]|"|$)' src/lib/pert/PertCalculator.svelte | grep -v "tracking-wider" | wc -l
```
Expected: `7` (count drops by exactly 1 from the pre-edit baseline of 8 to 7; the 7 remaining eyebrows are the page subtitle + 6 sibling secondary-row eyebrows; only the Capsules per month eyebrow flipped).

```
grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte
```
Expected: exactly 1 (F-03 numeral fix from commit 29306e7 preserved byte-identical; this plan extends the fix on the eyebrow, NOT on the numeral).

**STEP E -- Em-dash + en-dash sweep on the modified file.**

Run:
```
grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte
grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte
```
Expected: both return 0. If either returns > 0, the executor introduced a forbidden character (the workstream Q1 broad ban + Phase 2 D-19 + Phase 3.1 D-08 require both U+2014 and U+2013 = 0 across all PERT-route files); replace with ASCII hyphens or periods and re-run the sweep.

**STEP F -- Commit.** Stage `src/lib/pert/PertCalculator.svelte`. Commit with the plan-frontmatter `commit_message`:

```
fix(pert/04-04): F-03 escalation to Approach C -- eyebrow tracking-wider bump
```

The commit body should record:
- The line range edited (eyebrow `<div>` at line ~288).
- The diff: `tracking-wide` -> `tracking-wider` on the Capsules per month eyebrow only.
- Cross-reference: this completes the COMBINED RECIPE (Approach C) anticipated at 04-02-PLAN.md `<interfaces>` lines 147-150; the F-03 numeral half (Approach A) was shipped at commit 29306e7.
- Identity-Inside Rule preserved (eyebrow color token `text-[var(--color-identity)]` unchanged).
- Pitfall 4 selector preservation honored (visible text `Capsules per month` and numeral expression unchanged).
- Reference to gap closure: 04-UAT.md gap row test #1 (F-03 visual hierarchy).

**FORBIDDEN:**
- Editing any other file in this task. The only allowed file is `src/lib/pert/PertCalculator.svelte`.
- Changing the eyebrow color token `text-[var(--color-identity)]` to anything else (Identity-Inside Rule).
- Changing the visible text `Capsules per month` (Pitfall 4 selector preservation).
- Changing the numeral expression `{tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}` (Pitfall 4 selector preservation; e2e/pert.spec.ts asserts on the rendered value).
- Re-touching the F-03 numeral on line ~293 (already `font-extrabold` from commit 29306e7; must stay byte-identical).
- Editing the 7 sibling `tracking-wide` eyebrows (lines ~149, 181, 198, 216, 237, 254, 271). The hierarchy bump is asymmetric BY DESIGN: only the prescribing artifact (Capsules per month) gets the wider tracking.
- Editing `src/lib/pert/PertInputs.svelte` (Phase 3.1 frozen).
- Editing `src/lib/pert/pert-config.json` (Phase 4 commit 2dc7ae2 frozen).
- Editing any e2e spec (Pitfall 4 selector preservation means no e2e change is needed).
- Touching any shared component or shell file (D-08b strictly forbidden).
- Apply Approach D (numeral text-title -> text-[28px] / text-display) OR Approach E (visual isolation: background tint, top divider, icon prefix) in this task. Those are documented as the next escalation rungs in the plan's <objective> for the future planner pickup IF Approach C also proves insufficient at the human-UAT checkpoint.
  </action>
  <verify>
    <automated>
      # 1. The Approach C eyebrow bump landed exactly once.
      test "$(grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte)" -eq 1

      # 2. The bump is scoped to the Capsules per month row eyebrow -- siblings untouched.
      # tracking-wide-only count drops by exactly 1 (from 8 to 7).
      test "$(grep -oE 'tracking-wide(r|[^a-z]|\"|$)' src/lib/pert/PertCalculator.svelte | grep -v 'tracking-wider' | wc -l)" -eq 7

      # 3. The F-03 numeral fix is preserved byte-identical.
      test "$(grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte)" -eq 1

      # 4. The eyebrow color token was preserved (Identity-Inside Rule).
      grep -q 'text-2xs font-semibold tracking-wider text-\[var(--color-identity)\] uppercase' src/lib/pert/PertCalculator.svelte

      # 5. The visible text "Capsules per month" was preserved (Pitfall 4 selector).
      grep -q 'Capsules per month' src/lib/pert/PertCalculator.svelte

      # 6. The numeral expression was preserved (Pitfall 4 selector).
      grep -q 'tubeFeedResult.capsulesPerMonth.toLocaleString' src/lib/pert/PertCalculator.svelte

      # 7. Em-dash + en-dash sweep on the modified file: both 0.
      test "$(grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte)" -eq 0
      test "$(grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte)" -eq 0

      # 8. Tabular-numerals (.num) class still on hero + secondaries (>=5 per UI-SPEC Gate 12).
      test "$(grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte)" -ge 5

      # 9. The plan only touches the allowed file (no shell, no shared, no other calculator, no e2e).
      test "$(git diff --name-only HEAD -- src/ e2e/ | grep -vE '^src/lib/pert/PertCalculator\.svelte$' | wc -l)" -eq 0

      # 10. PertInputs.svelte byte-identical (Phase 3.1 frozen).
      test -z "$(git diff HEAD -- src/lib/pert/PertInputs.svelte)"

      # 11. pert-config.json byte-identical against HEAD (Phase 4 commit 2dc7ae2 frozen).
      test -z "$(git diff HEAD -- src/lib/pert/pert-config.json)"

      # 12. Calc-layer files byte-identical (Phase 2/3.1 frozen).
      test -z "$(git diff HEAD -- src/lib/pert/calculations.ts)"
      test -z "$(git diff HEAD -- src/lib/pert/state.svelte.ts)"
      test -z "$(git diff HEAD -- src/lib/pert/types.ts)"
      test -z "$(git diff HEAD -- src/lib/pert/config.ts)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` returns exactly 1 (the Capsules per month eyebrow; not present anywhere else in the file pre-fix).
    - tracking-wide-only count (excluding tracking-wider) drops by exactly 1 from the pre-edit baseline of 8 to 7 (`grep -oE 'tracking-wide(r|[^a-z]|"|$)' src/lib/pert/PertCalculator.svelte | grep -v 'tracking-wider' | wc -l` returns 7).
    - The F-03 numeral fix is preserved: `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` returns exactly 1 (byte-identical with commit 29306e7).
    - The eyebrow on the Capsules per month row preserves the Identity-Inside Rule: `class="text-2xs font-semibold tracking-wider text-[var(--color-identity)] uppercase"` (the only change is `tracking-wide` -> `tracking-wider`; color token must be `text-[var(--color-identity)]` unchanged).
    - The visible string `Capsules per month` is preserved verbatim (no rename, no abbreviation).
    - The numeral expression `{tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}` is preserved verbatim (no format change, no rounding tweak).
    - `grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte` returns 0 (em-dash count).
    - `grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte` returns 0 (en-dash count).
    - `grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte` returns >= 5 (UI-SPEC Gate 12; baseline 9, unchanged).
    - `git diff --name-only HEAD -- src/ e2e/` returns ONLY `src/lib/pert/PertCalculator.svelte` (no other source/test/config changed).
    - `git diff HEAD -- src/lib/pert/PertInputs.svelte` is empty (Phase 3.1 frozen).
    - `git diff HEAD -- src/lib/pert/pert-config.json` is empty (Phase 4 commit 2dc7ae2 frozen).
    - All calc-layer files (`calculations.ts`, `state.svelte.ts`, `types.ts`, `config.ts`) byte-identical against HEAD.
  </acceptance_criteria>
  <done>
    - The Capsules per month eyebrow `<div>` in `src/lib/pert/PertCalculator.svelte` Tube-Feed branch renders with `tracking-wider` (wider letter-spacing) -- combining with the F-03 numeral `font-extrabold` (commit 29306e7) to give the prescribing artifact a both-vectors typographic bump above the 3 sibling derived rows.
    - The eyebrow color token is unchanged (Identity-Inside Rule honored); only typographic tracking changed.
    - The F-03 numeral `font-extrabold` is preserved byte-identical (this plan extends the fix on the eyebrow, NOT on the numeral).
    - The e2e selectors at `e2e/pert.spec.ts` still match (Pitfall 4 reminder honored; `Capsules per month` and the numeral expression both preserved).
    - The change is staged + committed with the plan-frontmatter `commit_message`.
    - Em-dash + en-dash count on PertCalculator.svelte = 0 each.
    - Negative-space audit clean: only `src/lib/pert/PertCalculator.svelte` is in `git diff --name-only HEAD~1..HEAD`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run quality gates (svelte-check 0/0; vitest src/lib/pert/ 81/81; em + en-dash sweep on PertCalculator.svelte = 0; F-03 + Approach C invariants preserved)</name>
  <read_first>
    - .planning/workstreams/pert/STATE.md (offset 28, limit 25 -- the Phase 3.1 Wave 3 outcomes paragraph documenting the 7-gate clinical baseline: svelte-check 0/0; vitest 425/425 with PERT subset 81/81; pnpm build OK; pert-a11y 4/4; pert.spec 12/12; full Playwright 117 + 1 baseline flake)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-PLAN.md (offset 326, limit 140 -- Task 2's 7-gate sequence; this Wave 4 plan mirrors the same gate structure with one additional gate for the Approach C-specific assertion that tracking-wider exists exactly once)
    - src/lib/pert/PertCalculator.svelte (post-Task-1; verify the Approach C eyebrow bump landed)
  </read_first>
  <files>(none -- this task runs commands and captures their output to /tmp/04-04-gates/; no source edits)</files>
  <action>
This task is verification-only. It runs the quality gates that re-validate the Phase 3.1 + Phase 4 baselines after the Approach C escalation lands; the goal is to fail-fast at Wave 4 if Task 1 regressed any baseline.

**STEP A -- Create the gate-output directory.**

```
mkdir -p /tmp/04-04-gates
```

**STEP B -- Gate 1: svelte-check 0/0.**

Use `pnpm run check` (the project's actual script entry; the `pnpm exec svelte-check` form caused a worktree binary-path issue in 04-02-PLAN.md execution per its deviation note):

```
pnpm run check 2>&1 | tee /tmp/04-04-gates/gate-01-svelte-check.log
```

Acceptance: exit 0 AND output reports `0 errors` AND `0 warnings`. The Phase 3.1 + Phase 4 baseline is `~4586 files, 0 errors, 0 warnings`. If errors > 0 OR warnings > 0, Task 1 introduced a syntax or type regression -- revert Task 1's edit, investigate, re-apply with the fix.

If the worktree has no `node_modules` (per the 04-02-PLAN.md Wave 2 deviation pattern), run `pnpm install --frozen-lockfile` first. The `node_modules` directory is gitignored; the install does NOT pollute the diff.

**STEP C -- Gate 2: vitest src/lib/pert/ 81/81.**

Use `pnpm exec vitest run src/lib/pert/` directly (bypassing the npm-script wrapper; per 04-02-PLAN.md Wave 2 deviation note, the `pnpm test:run -- src/lib/pert/` form has an argument-passing quirk that runs the full 425 suite instead of the PERT subset):

```
pnpm exec vitest run src/lib/pert/ 2>&1 | tee /tmp/04-04-gates/gate-02-vitest-pert.log
```

Acceptance: exit 0 AND test count = 81 (PertCalculator.test.ts 10 + PertInputs.test.ts 9 + calculations.test.ts 45 + config.test.ts 17 = 81; matches Phase 3.1 + Phase 4 PERT-subset baseline). If count != 81 OR any failure, Task 1 broke a test (most likely PertCalculator.test.ts which DOM-asserts on the secondary section). The likely culprit is a class-string change that broke a `getByText` or `querySelector` assertion. Read the failing test's selector; if it queries `tracking-wide` literally, that's a Phase 3.1 test bug surfaced by Approach C -- escalate to the user via Rule 4 (do NOT auto-edit the test; D-08b protects test infrastructure as a Phase 3.1 frozen artifact).

**STEP D -- Gate 3: em-dash + en-dash sweep on PertCalculator.svelte = 0/0.**

```
EMS=$(grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte)
ENS=$(grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte)
echo "em-dash count: $EMS" | tee /tmp/04-04-gates/gate-03-dash-sweep.log
echo "en-dash count: $ENS" | tee -a /tmp/04-04-gates/gate-03-dash-sweep.log
test "$EMS" -eq 0
test "$ENS" -eq 0
```

Acceptance: both 0. The workstream Q1 broad ban + Phase 2 D-19 + Phase 3.1 D-08 carve forbid both U+2014 and U+2013 in any PERT-route file. If either is non-zero, identify the offender (most likely an inadvertent comment introduced by Task 1's edit), replace with ASCII hyphen or period, and re-commit (amend in this case is fine since the only commit so far in this plan is Task 1's).

**STEP E -- Gate 4: tabular-numerals preserved (UI-SPEC Gate 12 forward check).**

```
NUM_HITS=$(grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte)
echo "tabular-numerals .num class hits: $NUM_HITS" | tee /tmp/04-04-gates/gate-04-tabular-numerals.log
test "$NUM_HITS" -ge 5
```

Acceptance: count >= 5 (Phase 2 baseline is 9 hits; Wave 4 should not have changed `.num` class assignment, only `tracking-wide` -> `tracking-wider` on one eyebrow `<div>`, so the count remains 9).

**STEP F -- Gate 5: F-03 numeral fix preserved byte-identical (Wave 2 invariant).**

```
EXTRABOLD=$(grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte)
echo "font-extrabold count: $EXTRABOLD (expected: 1; F-03 numeral preserved from commit 29306e7)" | tee /tmp/04-04-gates/gate-05-f03-preserved.log
test "$EXTRABOLD" -eq 1
```

Acceptance: count = 1 (the F-03 numeral fix on the Capsules per month numeral `<span>` from commit 29306e7 is preserved byte-identical; this Wave 4 plan extends the fix on the eyebrow, NOT on the numeral). If 0, F-03 has been reverted -- escalate; do NOT proceed. If 2+, this plan accidentally added font-extrabold to a sibling row -- revert and re-edit.

**STEP G -- Gate 6: Approach C eyebrow bump landed exactly once (this-plan-specific assertion).**

```
TRACKING_WIDER=$(grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte)
TRACKING_WIDE_ONLY=$(grep -oE 'tracking-wide(r|[^a-z]|"|$)' src/lib/pert/PertCalculator.svelte | grep -v 'tracking-wider' | wc -l)
echo "tracking-wider count: $TRACKING_WIDER (expected: 1; Approach C eyebrow bump on Capsules per month)" | tee /tmp/04-04-gates/gate-06-approach-c.log
echo "tracking-wide-only count: $TRACKING_WIDE_ONLY (expected: 7; was 8 pre-fix; one flipped to tracking-wider)" | tee -a /tmp/04-04-gates/gate-06-approach-c.log
test "$TRACKING_WIDER" -eq 1
test "$TRACKING_WIDE_ONLY" -eq 7
```

Acceptance: tracking-wider = 1 AND tracking-wide-only = 7. If either differs, the executor either over-edited (multiple eyebrows flipped) or under-edited (the Capsules per month eyebrow was missed). Read the file at the eyebrow line range and confirm; revert + re-edit if needed.

**STEP H -- Gate 7: function-binding bridges Phase 3.1 D-01 regression guard.**

```
BINDS=$(grep -cE 'bind:value=\{$' src/lib/pert/PertInputs.svelte)
PROXIES=$(grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte)
echo "function-binding bridges: $BINDS" | tee /tmp/04-04-gates/gate-07-bind-bridges.log
echo "string-bridge proxies: $PROXIES" | tee -a /tmp/04-04-gates/gate-07-bind-bridges.log
test "$BINDS" -ge 3
test "$PROXIES" -eq 0
```

Acceptance: BINDS >= 3 (Phase 3.1 baseline; one per medication / strength / formula picker) AND PROXIES = 0 (no string-bridge proxies re-introduced). Wave 4 does NOT touch PertInputs.svelte so this should pass trivially; the gate is a safety check that no rogue edit happened.

**STEP I -- Gate 8: pert-config.json byte-identical against HEAD (Phase 4 commit 2dc7ae2 frozen).**

```
git log --oneline -1 src/lib/pert/pert-config.json | tee /tmp/04-04-gates/gate-08-config-history.log
test -z "$(git diff HEAD -- src/lib/pert/pert-config.json)"
```

Acceptance: the most recent commit touching `pert-config.json` is `2dc7ae2` (the out-of-band weight-range fix from Wave 2 acknowledgment); `git diff HEAD -- src/lib/pert/pert-config.json` is empty (Wave 4 does NOT re-edit).

**STEP J -- Gate 9: plan-level negative-space audit.**

```
git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | tee /tmp/04-04-gates/gate-09-neg-space-plan.log
test "$(git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | wc -l)" -eq 1
git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | grep -q '^src/lib/pert/PertCalculator\.svelte$'
```

Acceptance: exactly 1 file in the plan's diff (Task 1's commit), and that file is `src/lib/pert/PertCalculator.svelte`. Anything else (e.g. PertInputs.svelte, pert-config.json, e2e specs, shell, shared) is a D-08 boundary violation -- STOP and escalate to the user.

**Note on running pnpm build / Playwright / pert-a11y / pert.spec / full Playwright:** these heavy gates are NOT re-run in this Wave 4 plan -- they were already green at Phase 4 closure (Wave 3 plan 04-03; commits `95e31b0`, `3001c2a`, `33695e1`). The Approach C edit is a Tailwind class-string change with no behavioral or layout impact (letter-spacing only); re-running the heavy gates in the same gsd-execute pass would consume context for marginal additional safety. The user's `/gsd-verify-work 4 --ws pert` re-run after this plan ships will include the heavy gates as part of the standard verification command sequence.

**STEP K -- Capture outputs.** All `/tmp/04-04-gates/gate-NN-*.log` files are referenced verbatim in Task 4's 04-04-SUMMARY.md as evidence pastes. Do NOT commit these tmp logs to git -- they are transient verification artifacts.
  </action>
  <verify>
    <automated>
      # Gate 1: svelte-check 0/0
      test -f /tmp/04-04-gates/gate-01-svelte-check.log
      grep -qE '0 errors' /tmp/04-04-gates/gate-01-svelte-check.log
      grep -qE '0 warnings' /tmp/04-04-gates/gate-01-svelte-check.log

      # Gate 2: vitest src/lib/pert/ 81/81
      test -f /tmp/04-04-gates/gate-02-vitest-pert.log
      grep -qE 'Tests[[:space:]]+81 passed' /tmp/04-04-gates/gate-02-vitest-pert.log

      # Gate 3: em-dash + en-dash 0/0 on PertCalculator.svelte
      test "$(grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte)" -eq 0
      test "$(grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte)" -eq 0

      # Gate 4: tabular-numerals >=5 hits
      test "$(grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte)" -ge 5

      # Gate 5: F-03 numeral preserved (font-extrabold count = 1)
      test "$(grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte)" -eq 1

      # Gate 6: Approach C bump landed exactly once
      test "$(grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte)" -eq 1
      test "$(grep -oE 'tracking-wide(r|[^a-z]|\"|$)' src/lib/pert/PertCalculator.svelte | grep -v 'tracking-wider' | wc -l)" -eq 7

      # Gate 7: function-binding bridges >=3, string-bridge proxies = 0
      test "$(grep -cE 'bind:value=\{$' src/lib/pert/PertInputs.svelte)" -ge 3
      test "$(grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte)" -eq 0

      # Gate 8: pert-config.json byte-identical against HEAD
      test -z "$(git diff HEAD -- src/lib/pert/pert-config.json)"

      # Gate 9: plan-level negative-space audit -- exactly 1 file (PertCalculator.svelte)
      test "$(git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | wc -l)" -eq 1
      git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | grep -q '^src/lib/pert/PertCalculator\.svelte$'
    </automated>
  </verify>
  <acceptance_criteria>
    - Gate 1 (svelte-check): exit 0; output reports 0 errors AND 0 warnings (Phase 3.1 + Phase 4 baseline preserved).
    - Gate 2 (vitest src/lib/pert/): exit 0; 81 tests pass; 0 fail. Matches Phase 3.1 + Phase 4 baseline.
    - Gate 3 (em-dash + en-dash sweep): both `grep -cP '\x{2014}'` and `grep -cP '\x{2013}'` return 0 on `src/lib/pert/PertCalculator.svelte`.
    - Gate 4 (tabular-numerals): `grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte` returns >= 5 (UI-SPEC Gate 12).
    - Gate 5 (F-03 preserved): `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` returns exactly 1 (Wave 2 invariant from commit 29306e7).
    - Gate 6 (Approach C landed): `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` returns exactly 1 AND tracking-wide-only count returns 7.
    - Gate 7 (function-binding bridges): `grep -cE 'bind:value=\{$' src/lib/pert/PertInputs.svelte` returns >= 3; `grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte` returns 0.
    - Gate 8 (pert-config.json): `git diff HEAD -- src/lib/pert/pert-config.json` returns empty (Wave 2 commit 2dc7ae2 frozen).
    - Gate 9 (plan-level negative-space audit): `git diff --name-only HEAD~1..HEAD -- src/ e2e/` returns exactly 1 line: `src/lib/pert/PertCalculator.svelte`.
    - All 9 gate logs captured at `/tmp/04-04-gates/gate-NN-*.log` (transient; not committed; referenced in Task 4's SUMMARY).
  </acceptance_criteria>
  <done>
    - All 9 quality gates pass; outputs captured at `/tmp/04-04-gates/`.
    - Phase 3.1 invariants verified intact (function-binding bridges 3+ hits, zero string-bridge proxies, em-dash + en-dash 0/0 on PertCalculator.svelte).
    - Phase 4 invariants verified intact (F-03 numeral `font-extrabold` preserved at exactly 1 hit; tabular-numerals >= 5).
    - Approach C eyebrow bump verified at exactly 1 hit on the Capsules per month eyebrow (siblings untouched; tracking-wide-only count drops by exactly 1 from 8 to 7).
    - PERT-subset vitest count 81/81 unchanged (Phase 3.1 + Phase 4 baseline preserved).
    - Plan-level negative-space audit clean (only PertCalculator.svelte modified in Wave 4's commit).
    - pert-config.json byte-identical against HEAD (Phase 4 commit 2dc7ae2 frozen; not re-edited).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-eye visual UAT -- confirm Approach C closes the F-03 hierarchy gap</name>
  <what-built>
    Wave 4 ships the F-03 Approach C escalation: the Tube-Feed `Capsules per month` row eyebrow `<div>` in `src/lib/pert/PertCalculator.svelte` (line ~288) was bumped from `tracking-wide` to `tracking-wider`, combining with the Wave 2 F-03 numeral fix (`font-extrabold` from commit 29306e7) to give the prescribing artifact a both-vectors typographic delta (heavier weight + wider letter-spacing) above the 3 sibling derived rows (Total fat / Total lipase / Lipase per kg).

    The 7 sibling `tracking-wide` eyebrows in the file (page subtitle + 6 other secondary-row eyebrows) are byte-identical. The numeral on line ~293 (already `font-extrabold` from Wave 2) is byte-identical. Identity-Inside Rule honored (eyebrow color token `text-[var(--color-identity)]` unchanged). Pitfall 4 selector preservation honored (visible text `Capsules per month` and numeral expression unchanged; e2e selectors still match).

    All 9 plan-level quality gates from Task 2 are green: svelte-check 0/0; vitest src/lib/pert/ 81/81; em-dash + en-dash 0/0; tabular-numerals 9; F-03 numeral preserved (font-extrabold count = 1); Approach C eyebrow bump (tracking-wider count = 1; tracking-wide-only drops 8 -> 7); function-binding bridges 3+; string-bridge proxies 0; pert-config.json byte-identical; negative-space audit clean (only PertCalculator.svelte modified).

    THIS CHECKPOINT IS THE CORE PURPOSE OF WAVE 4 -- replacing the LLM-Design-Review fallback gate (which certified F-03 as PASS at Phase 4 closure based on screenshot transcripts that did not surface the perceptible-hierarchy-bump issue) with human-eye review. The user's UAT contradicted the LLM verdict; this checkpoint encodes the corrective process insertion.
  </what-built>
  <how-to-verify>
    1. **Start the dev server.** From the worktree root, run `pnpm dev`. Vite will start on a port (typically 5173 unless taken). Note the URL printed in the terminal.

    2. **Open `/pert` in a browser.** Navigate to `http://localhost:{port}/pert`. The route renders the PERT calculator.

    3. **Switch to Tube-Feed mode.** Click the `Tube-Feed` segmented-toggle pill at the top of the calculator (Oral / Tube-Feed). The mode-toggle should highlight `Tube-Feed` with the identity-purple active state.

    4. **Populate the Tube-Feed inputs to render the secondaries section.** Enter:
       - Weight: `7` (or any value in the 0.3-10 kg range; Wave 2 commit 2dc7ae2 set the slider max to 10 kg).
       - Formula: pick any pediatric formula from the picker (e.g. `Kate Farms Pediatric Standard 1.2`).
       - Volume per day: `1500` mL (or any value that produces a non-zero result).
       - Lipase units/kg/day: leave at default `1000`.
       - Medication: pick any (e.g. `Pancreaze`).
       - Strength: pick any from the filtered list (e.g. `37,000 units`).

    5. **Locate the secondaries card.** Below the hero (Capsules per day numeral), the Tube-Feed mode shows a `divide-y` stack of 4 rows:
       - Row 1: `Total fat` ... `g`
       - Row 2: `Total lipase needed` ... `units/day`
       - Row 3: `Lipase per kg` ... `units/kg/day`
       - Row 4: `Capsules per month` ... `capsules` <- the prescribing artifact, the F-03 + Approach C target

    6. **Read the 4 rows by eye.** The question is: does the `Capsules per month` row (Row 4) read visually distinct from the 3 derived rows above it (Rows 1-3)? Specifically:
       - The eyebrow `CAPSULES PER MONTH` should have wider letter-spacing than `TOTAL FAT`, `TOTAL LIPASE NEEDED`, `LIPASE PER KG`. The wider-tracking eyebrow sits above the bolder numeral; the combined effect should make the row stand out without being loud.
       - The numeral should look heavier (extrabold) than the 3 sibling numerals (bold).

    7. **Optional: switch to dark theme + check mobile width.** The user's reported issue surfaced at light-desktop-tube-feed; for completeness:
       - Toggle theme via the theme-toggle button in the header (or set `localStorage.theme = 'dark'` and reload).
       - Resize the browser to mobile width (~375px) or use DevTools responsive mode.
       - Re-read the same Tube-Feed secondaries section in dark + mobile + tube-feed.

    8. **Type the resume signal.** If the combined Approach A + C delta makes the `Capsules per month` row read visually distinct from the 3 derived rows, type `approved` to close the gap. If the row STILL reads at equivalent hierarchy by eye (Approach C insufficient), type a description of the residual issue (e.g. "still equivalent; recommend Approach D") -- the next planner pickup will author Wave 5 applying Approach D (numeral text-title -> text-[28px] or text-display) per the escalation ladder documented in this plan's <objective>.

    **What to look for / what NOT to look for:**
    - LOOK FOR: a perceptible hierarchy bump on the Capsules per month row vs the 3 derived rows. The bump should be obvious-but-not-loud; clinicians should be able to find the prescribing artifact at a glance without it shouting.
    - DO NOT LOOK FOR: a hero-equivalent visual treatment on Capsules per month -- the row is a SECONDARY in the divide-y stack, not the hero. It should clearly read as "the secondary that matters most" while the 3 derived rows recede below it.
    - DO NOT LOOK FOR: any color change on the eyebrow -- the eyebrow stays identity-purple (`text-[var(--color-identity)]`); the bump is typographic only (extrabold weight + wider tracking). If the eyebrow color shifted, that's a regression -- escalate.
    - DO NOT LOOK FOR: any change to the visible text `Capsules per month` -- it's preserved verbatim per Pitfall 4 (e2e selector preservation).

    **Why this checkpoint exists:** Phase 4 closure (Wave 3 plan 04-03) used the LLM-Design-Review fallback to certify all P1/P2 findings as resolved at the >=35/40 score bar. The fallback's screenshot-driven critique transcripts did NOT surface the perceptible-hierarchy-bump issue that human UAT immediately caught (per 04-UAT.md). This checkpoint encodes the lesson: human-eye review is non-substitutable for typographic hierarchy bumps where the LLM may report DOM-level structural correctness without registering the perceptual gap. After this plan ships and the user runs `/gsd-verify-work 4 --ws pert`, the existing 04-UAT.md gap row is re-checked against the post-Approach-C UI; PASS marks the row resolved, FAIL re-opens for Approach D escalation via `/gsd-plan-phase 4 --gaps --ws pert`.
  </how-to-verify>
  <resume-signal>Type `approved` if the combined Approach A + C delta closes the F-03 hierarchy gap by eye. Type a description of residual issues if Approach C is still insufficient (the next planner pickup will escalate to Approach D per the ladder in this plan's `<objective>`).</resume-signal>
  <files>(none -- this is a checkpoint:human-verify task; no source files modified; the human reviews the rendered UI from Task 1's edit)</files>
  <action>
This task is a checkpoint:human-verify gate. The full instructions for the human are in the `<what-built>`, `<how-to-verify>`, and `<resume-signal>` blocks above. The orchestrator should:

1. Pause execution after Task 2 completes (all 9 quality gates green).
2. Surface the `<what-built>` and `<how-to-verify>` content to the user verbatim.
3. Wait for the user's resume signal per `<resume-signal>` (either `approved` or an escalation-required description).
4. Capture the user's signal verbatim for Task 4's SUMMARY authoring.
5. If `approved`: proceed to Task 4. If escalation-required: proceed to Task 4 (the SUMMARY records the escalation outcome; the next planner pickup runs `/gsd-plan-phase 4 --gaps --ws pert` for Approach D plan authoring per the ladder documented in this plan's `<objective>`).

NO automated action is taken in this task; the entire purpose is the human-eye visual UAT gate replacing the LLM-Design-Review fallback verdict from Phase 4 closure.

FORBIDDEN:
- Any source-code edit in this task (this is a verification-only checkpoint).
- Any automated /impeccable critique pass or screenshot-driven LLM-Design-Review (the corrective process insertion is precisely human-eye review; substituting another LLM gate would re-introduce the failure mode this checkpoint exists to fix).
- Auto-resolving the checkpoint without the user's explicit resume signal.
  </action>
  <verify>
    <automated>
      # Checkpoint:human-verify tasks have no automated verification gate -- the gate is the user's resume signal.
      # The orchestrator's checkpoint handling captures the resume signal and writes it to the execution log.
      # This <automated> block is present to satisfy the plan-structure validator's <verify> requirement; it asserts
      # the checkpoint task type is correctly declared in the task header (gate is the human's signal).
      grep -q 'type="checkpoint:human-verify"' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-PLAN.md
    </automated>
  </verify>
  <done>
    - The user has reviewed the rendered Tube-Feed mode at `/pert` post-Approach-C-escalation by eye in at least the light-desktop-tube-feed context (the surfaced-issue context); recommended coverage is light + dark x mobile + desktop x tube-feed = 4 contexts.
    - The user's resume signal has been captured verbatim (either `approved` or an escalation-required description).
    - If `approved`: the F-03 hierarchy gap from 04-UAT.md is closed by human verdict; PERT-DESIGN-02..05 stay Validated; Phase 4 closure is re-confirmed after the user re-runs `/gsd-verify-work 4 --ws pert`; Phase 5 (Release) is unblocked.
    - If escalation-required: the residual issue is captured for the next planner pickup; the user runs `/gsd-plan-phase 4 --gaps --ws pert` to author Wave 5 (Approach D escalation per the documented ladder) after this plan's SUMMARY ships.
  </done>
</task>

<task type="auto">
  <name>Task 4: Author 04-04-SUMMARY.md (Approach C escalation record + human-UAT outcome + escalation-ladder forward note)</name>
  <read_first>
    - src/lib/pert/PertCalculator.svelte (post-Task-1; cite the actual line range edited and the before/after class string)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-UAT.md (read in full; cite the gap row test #1 closure status and the Approach D/E escalation options for the forward note)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md (read for the audit-trail format -- Wave 4's SUMMARY mirrors Wave 2's structure: header, what shipped, deviations, pitfall encounter notes, next step)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-VERIFICATION.md (read for the LLM-Design-Review fallback acknowledgment context; Wave 4's SUMMARY records the human-UAT-as-corrective-process insertion against that fallback)
    - All 9 gate logs at /tmp/04-04-gates/gate-NN-*.log (Task 2's outputs; paste verbatim into the SUMMARY's "Quality gates" section)
    - The output of Task 3's checkpoint (the user's `approved` or escalation-required signal; capture verbatim in the SUMMARY's "Human-visual UAT outcome" section)
    - git log --oneline -3 (capture the SHAs of: (a) Task 1's commit just shipped; (b) Wave 2 commit 29306e7; (c) Wave 3 plan 04-03 closure commit)
  </read_first>
  <files>.planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md</files>
  <action>
Compose `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md` with the following structure (>=50 lines target). Use Wave 2's SUMMARY (`04-02-SUMMARY.md`) as a structural reference but Wave 4 also includes the human-UAT outcome section + the escalation-ladder forward note.

**Required sections (in order):**

1. **Frontmatter** with: `phase: 4`, `plan: 4`, `subsystem: pert/design-polish-impeccable`, `workstream: pert`, `milestone: v1.15`, `wave: 4`, `tags: [pert, design-polish, gap-closure, wave-4, f-03, uat-escalation, approach-c]`, `dependency_graph` (requires Wave 1 + Wave 2 + Wave 3 + 04-UAT.md; provides the Approach C escalation + this SUMMARY), `key_files.modified: [src/lib/pert/PertCalculator.svelte]`, `decisions` (cite Approach C as the plan-anticipated fallback per 04-02-PLAN.md `<interfaces>` lines 147-150 + the Approach D/E reservation as the next escalation rungs + the human-UAT-as-corrective-process insertion against the LLM-Design-Review fallback), `metrics` (lines edited, gate counts, commit count, human-UAT outcome).

2. **Header**: `# Phase 4 Plan 04 Summary: F-03 Approach C Escalation -- Eyebrow Tracking-Wider Bump (Wave 4 Gap Closure)` + `**Phase:** 04-design-polish-impeccable` + `**Plan:** 04-04 (Wave 4 -- gap closure from 04-UAT.md)` + `**Generated:** {today}` + 1-paragraph executive summary citing the 04-UAT.md gap closure (test #1) + the Approach C escalation as the plan-anticipated fallback + the human-UAT-as-corrective-process insertion + the future-escalation-rung note.

3. **What this plan shipped** (numbered list):
   - Approach C eyebrow bump: line range edited (~288); recipe applied (eyebrow `tracking-wide` -> `tracking-wider`); commit SHA from Task 1; before/after class strings pasted verbatim.
   - Human-eye visual UAT checkpoint result (Task 3): user's resume signal verbatim (`approved` or escalation-required note).
   - 04-04-SUMMARY.md (this file).

4. **Why this plan exists (the gap-closure rationale)** -- a dedicated subsection:
   - Cite Phase 4 closure context: Wave 3 plan 04-03 (commit `33695e1` and Task 3 commit) certified PERT-DESIGN-01..06 as Validated at the >=35/40 score bar based on 8 LLM-Design-Review FINAL critique transcripts.
   - Cite the 04-UAT.md gap: human UAT recorded the F-03 fix (Approach A: numeral `font-bold` -> `font-extrabold`) is NOT visually distinguishable at the rendered text-title (22px) Plus Jakarta Sans scale. The user reported "all 4 numerals read at equivalent weight by eye" in the Tube-Feed secondaries section.
   - Cite the plan-anticipated escalation: 04-02-PLAN.md `<interfaces>` lines 147-150 documented Approach C (combined: extrabold numeral + tracking-wider eyebrow) as the COMBINED RECIPE fallback. Wave 2 chose Approach A as the smallest-diff recipe; Wave 4 ships the eyebrow half to complete Approach C.
   - Frame the human-UAT-as-corrective-process insertion: the LLM-Design-Review fallback's screenshot transcripts at Phase 4 closure did not surface the perceptible-hierarchy-bump issue. This Wave 4 checkpoint encodes the lesson that human-eye review is non-substitutable for typographic hierarchy assertions.

5. **Quality gates pasted verbatim** from /tmp/04-04-gates/gate-NN-*.log (all 9):
   - Gate 1 svelte-check tail: `0 errors, 0 warnings`.
   - Gate 2 vitest src/lib/pert/: `Tests 81 passed (81)`.
   - Gate 3 em-dash + en-dash sweep: both 0.
   - Gate 4 tabular-numerals: count (>= 5; baseline 9).
   - Gate 5 F-03 numeral preserved: `font-extrabold` count = 1 (byte-identical with commit 29306e7).
   - Gate 6 Approach C landed: `tracking-wider` count = 1, tracking-wide-only count = 7 (was 8 pre-fix).
   - Gate 7 function-binding bridges + string-bridge proxies: counts (>= 3 / = 0).
   - Gate 8 pert-config.json byte-identical against HEAD: empty diff confirmed.
   - Gate 9 plan-level negative-space audit: `src/lib/pert/PertCalculator.svelte` only.

6. **Human-visual UAT outcome** -- a dedicated subsection:
   - The user's resume signal verbatim from Task 3 (`approved` OR escalation-required note).
   - Coverage actually reviewed: which contexts the user verified by eye (minimum: light-desktop-tube-feed; recommended: light + dark x mobile + desktop x tube-feed = 4 contexts).
   - If `approved`: 04-UAT.md gap row test #1 is closed; PERT-DESIGN-02..05 stay Validated after the user re-runs `/gsd-verify-work 4 --ws pert`.
   - If escalation-required: cite the residual issue verbatim; defer Approach D / E plan to the next planner pickup.

7. **Escalation ladder forward note (for the next planner pickup if Approach C is still insufficient)** -- a dedicated subsection:
   - **Approach D (typographic escalation):** bump the Capsules per month numeral from `text-title` (22px) to a custom `text-[28px]` (less invasive) OR `text-display` (44px; matches the hero numeral; potentially too loud). Larger size + extrabold weight together creates an unambiguous hero-secondary; may require a row-padding adjustment to maintain divide-y alignment.
   - **Approach E (visual isolation, no further typographic change):** add a row-level treatment to the Capsules per month `<div class="flex items-baseline justify-between px-5 py-4">` -- e.g. `bg-[var(--color-identity-hero)]/30` background tint, OR `border-t-2 border-[var(--color-identity)]` top divider, OR icon prefix (e.g. lucide `Pill` glyph). Restores hierarchy via container-level cue rather than typographic delta.
   - Recommendation: Approach D over Approach E if typographic deltas are exhausted; Approach E if Approach D feels too loud or breaks row alignment. One approach at a time per Wave-N gap-closure plan; do NOT chain.
   - Replan command if needed: `/gsd-plan-phase 4 --gaps --ws pert` after re-running UAT.

8. **REQUIREMENTS.md re-validation note** -- a dedicated subsection:
   - This plan does NOT touch REQUIREMENTS.md.
   - PERT-DESIGN-02..05 are currently Validated (per Phase 4 closure record); the gap row in 04-UAT.md re-opens the question.
   - After this plan ships, the user re-runs `/gsd-verify-work 4 --ws pert`. The verifier re-checks the existing 04-UAT.md gap row against the post-Approach-C UI:
     - If user-visual-UAT passes (Task 3 resume signal = `approved`): the gap row is marked resolved; PERT-DESIGN-02..05 stay Validated; Phase 4 closure is re-confirmed; the workstream is unblocked for Phase 5 (Release).
     - If user-visual-UAT fails (Task 3 resume signal = escalation-required note): the gap row stays open; PERT-DESIGN-02..05 STAY Validated (the validated state was Phase 4's own LLM-driven verdict; re-opening would require a deliberate flip), but the gap row provides the audit trail; the user re-runs `/gsd-plan-phase 4 --gaps --ws pert` for Approach D escalation.
   - The verification loop is: ship plan -> human UAT in checkpoint -> commit SUMMARY with outcome -> user runs `/gsd-verify-work 4 --ws pert` -> verifier re-checks gap row -> orchestrator updates STATE.md / ROADMAP.md / REQUIREMENTS.md as needed (ALL outside this plan's scope).

9. **Phase 3.1 + Phase 4 invariants regression guard** (paste from Gates 5, 7):
   - F-03 numeral fix: `font-extrabold` count = 1 (commit 29306e7 preserved byte-identical).
   - Function-binding bridges: 3+ hits in PertInputs.svelte.
   - String-bridge proxies: 0 hits in PertInputs.svelte.
   - Em-dash count on PertCalculator.svelte: 0.
   - En-dash count on PertCalculator.svelte: 0.
   - Tabular-numerals on PertCalculator.svelte: >= 5 hits (baseline 9; unchanged because Approach C edit is on the eyebrow `<div>`, not on a `.num` numeral).
   - STOP-red advisory carve-out preserved (`border-2 border-[var(--color-error)]` + `OctagonAlert` + `role="alert"` block at lines ~310-322 untouched per Wave 4's plan-level negative-space audit).

10. **Negative-space audit** (paste from Gate 9):
    - `git diff --name-only HEAD~1..HEAD -- src/ e2e/` returns exactly: `src/lib/pert/PertCalculator.svelte`.
    - Phase-level audit forward note: when the user runs the Phase 4 cumulative audit (`git diff --name-only $PHASE_BASELINE..HEAD -- src/ e2e/` where `$PHASE_BASELINE` is the parent of Wave 1 commit `eec18c2`), the result will be: `src/lib/pert/PertCalculator.svelte` (Wave 2 F-03 + Wave 4 Approach C escalation; both font-weight/tracking only, both within the Capsules per month row block) + `src/lib/pert/pert-config.json` (commit `2dc7ae2` weight-range alignment) + `src/lib/shared/about-content.ts` (commit `5cd3386` PERT-entry-only xlsx removal, D-08a-style amendment per Phase 4 VERIFICATION.md acknowledgment). All within D-08 allowlist or pre-acknowledged D-08a-style amendments.

11. **Deviations from Plan** (mirroring Wave 2 SUMMARY's "Deviations" section):
    - List any deviation from the recipe (e.g. "the executor needed to install node_modules in a fresh worktree per the Wave 2 deviation pattern; pnpm install --frozen-lockfile completed in Xs").
    - Capture any auto-fixes during execution (svelte-check warnings introduced + reverted, etc.).
    - Auth gates: none expected (the workflow is fully agent-runnable until Task 3's human checkpoint).

12. **PERT-DESIGN-02..05 satisfaction mapping** (which requirement closure each artifact contributes to):
    - PERT-DESIGN-02 (P1 fixes ship before merge; cheap P2/P3 inline) -- the Approach C escalation is a cheap-inline P2 follow-up to the F-03 P2 fix-now from Wave 2; satisfied by Task 1 + the human-UAT confirmation.
    - PERT-DESIGN-03 (DESIGN.md / DESIGN.json contract enforced -- Identity-Inside Rule, Eyebrow-Above-Numeral, Tabular-Numbers, etc.) -- Identity-Inside preserved (eyebrow color unchanged); Tabular-Numbers preserved (`.num` class hits >= 5); Eyebrow-Above-Numeral preserved (eyebrow `<div>` above numeral `<span>`); Five-Roles-Only preserved (no new typographic role introduced; tracking-wider is a Tailwind utility on the existing Label role).
    - PERT-DESIGN-04 (HeroResult above-the-fold + sticky InputDrawer -- Phase 42.1 inheritance; F-04 P3 deferred per D-03 default) -- already satisfied by inheritance; F-04 deferred record stays in 04-FINDINGS.md.
    - PERT-DESIGN-05 (SegmentedToggle visual integration with identity hue) -- Phase 1 + Phase 2 already shipped; Wave 1 + Wave 3 critique surfaced no SegmentedToggle finding; satisfied by inheritance.
    - Note: PERT-DESIGN-01 was satisfied by Wave 1 (critique-skill-run requirement; 8 transcripts captured + 8 FINAL transcripts captured at Wave 3). PERT-DESIGN-06 (>=35/40 score target) was satisfied by Wave 3 (aggregate 36.25/40 at LLM-Design-Review fallback); the human-UAT gap closure on F-03 may incrementally improve the score on a Wave 5 re-run if needed but is NOT required for the score bar (the >=35/40 target was met at Wave 3; this gap closure is a quality-bar follow-up, not a score-target re-validation).

13. **Self-Check** section (mirrors Wave 2 SUMMARY's "Self-Check" section): all artifacts on disk, all gates green, all SUMMARY checklist items checked, human-UAT outcome captured.

14. **Next step (the verification re-run + future escalation transition)**: the user runs `/gsd-verify-work 4 --ws pert` to re-check the 04-UAT.md gap row against the post-Approach-C UI. If `approved`, Phase 4 closure is re-confirmed and Phase 5 is unblocked. If escalation-required, the user runs `/gsd-plan-phase 4 --gaps --ws pert` for Approach D plan authoring.

**Em-dash + en-dash hygiene on the SUMMARY itself:** the workstream Q1 broad ban applies to this file too. After authoring, run:

```
grep -cP '\x{2014}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
grep -cP '\x{2013}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
```

Both must return 0. Use `--` (double ASCII hyphen) for any em-dash equivalent in prose; use `-` (ASCII hyphen) for any en-dash equivalent.

**Stage + commit:** stage `04-04-SUMMARY.md` and create a separate commit `docs(pert/04-04): summary -- F-03 Approach C escalation + human-UAT outcome + escalation-ladder forward note`. Do NOT amend Task 1's commit (the source-edit commit and the SUMMARY commit are conceptually separable; keeping them as 2 commits matches the Wave 2 pattern of separate Task 1 + Task 3 commits).
  </action>
  <verify>
    <automated>
      # 1. SUMMARY file exists and is >= 50 lines.
      test -f .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      test "$(wc -l < .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md)" -ge 50

      # 2. Required content markers present.
      grep -q "Approach C" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -q "tracking-wider" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -q "font-extrabold" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -q "F-03" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -q "29306e7" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -q "Identity-Inside" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -q "Pitfall 4" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -q "human" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -q "PertCalculator.svelte" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -qE "Capsules per month" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -qE "(Approach D|escalation ladder)" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -qE "(Approach E|visual isolation)" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -q "/gsd-verify-work" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -q "PERT-DESIGN-02" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -q "Phase 3.1" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md

      # 3. Em-dash + en-dash sweep on the SUMMARY itself: both 0.
      test "$(grep -cP '\x{2014}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md)" -eq 0
      test "$(grep -cP '\x{2013}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md)" -eq 0

      # 4. Quality gate evidence pasted.
      grep -qE "(0 errors|0 warnings)" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md
      grep -qE "81 passed" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md

      # 5. SUMMARY committed (separate commit, NOT amended into Task 1's commit per the Wave 2 2-commit pattern).
      test -n "$(git log --oneline -3 -- .planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md)"
    </automated>
  </verify>
  <acceptance_criteria>
    - 04-04-SUMMARY.md exists at `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md` with >= 50 lines.
    - Approach C escalation record present: line range, before/after class strings, commit SHA, F-03 numeral byte-identity confirmation.
    - Human-visual UAT outcome captured (resume signal verbatim from Task 3; coverage of contexts reviewed).
    - Escalation ladder forward note present: Approach D (text-title -> text-[28px] / text-display) + Approach E (visual isolation: background tint, top divider, icon prefix) + recommendation order + replan command.
    - REQUIREMENTS.md re-validation note present: do NOT touch REQUIREMENTS.md from this plan; user re-runs `/gsd-verify-work 4 --ws pert` after this plan ships.
    - Phase 3.1 + Phase 4 invariants regression-guard pasted (function-binding 3+ hits, em-dash + en-dash 0/0, tabular-numerals >= 5, F-03 numeral font-extrabold = 1).
    - All 9 Task-2 gate outputs pasted verbatim (svelte-check, vitest, em+en-dash, tabular, F-03 preservation, Approach C bump, function-bindings, config-history, neg-space).
    - PERT-DESIGN-02..05 satisfaction mapping documented.
    - Em-dash + en-dash count on the SUMMARY itself: 0 each (workstream Q1 broad ban honored).
    - SUMMARY committed as a separate commit (NOT amended into Task 1's commit; matches Wave 2 2-commit pattern).
  </acceptance_criteria>
  <done>
    - 04-04-SUMMARY.md authored with all 14 required sections (frontmatter, header, what shipped, gap-closure rationale, quality gates, human-UAT outcome, escalation ladder, REQUIREMENTS.md re-validation note, Phase 3.1 + Phase 4 invariants, negative-space audit, deviations, PERT-DESIGN satisfaction mapping, self-check, next step).
    - Em-dash + en-dash count on SUMMARY = 0 each.
    - Commit landed; `git log -- 04-04-SUMMARY.md` returns at least 1 commit.
    - Verifier (`/gsd-verify-work 4 --ws pert`) is unblocked: it will read 04-04-SUMMARY.md as the audit trail for the Approach C escalation + human-UAT outcome + the escalation-ladder forward note.
  </done>
</task>

</tasks>

<verification>
**Phase-level re-verification is the user's `/gsd-verify-work 4 --ws pert` re-run after this plan ships.** Wave 4's verification scope is the 9 fast-feedback gates run in Task 2 + the human-eye visual UAT in Task 3 + the SUMMARY content-marker checks run in Task 4.

The user's `/gsd-verify-work 4 --ws pert` re-run will:

1. Re-check the 04-UAT.md gap row test #1 against the post-Approach-C UI.
2. If the user's Task 3 resume signal in 04-04-SUMMARY.md was `approved`: the gap row is marked resolved; PERT-DESIGN-02..05 stay Validated; Phase 4 closure is re-confirmed; the workstream is unblocked for Phase 5 (Release).
3. If the user's Task 3 resume signal was escalation-required: the gap row stays open; PERT-DESIGN-02..05 STAY Validated (the validated state was Phase 4's own LLM-driven verdict; re-opening would require a deliberate flip), but the gap row provides the audit trail.
4. The verifier may re-run the heavy gates (pnpm build + pert-a11y + pert.spec + full Playwright + AUDIT.sh) at its discretion based on the standard /gsd-verify-work command sequence; this Wave 4 plan does NOT re-run them in-scope to conserve context (the Approach C edit is a class-string change with no behavioral or layout impact).

If Wave 4's plan-level gates fail (e.g. svelte-check non-zero or vitest count != 81 or em-dash + en-dash sweep > 0 or negative-space audit dirty or F-03 numeral preservation broken), the verifier cannot re-confirm closure until Wave 4 re-passes. The Task 2 gate sequence is fail-fast for exactly this reason.

If the human-UAT in Task 3 records escalation-required, the next planner pickup runs `/gsd-plan-phase 4 --gaps --ws pert` to author Wave 5 (Approach D); the verification loop continues from there.
</verification>

<success_criteria>
- F-03 Approach C escalation shipped: 1-LOC eyebrow `tracking-wide` -> `tracking-wider` swap on the Capsules per month row in `src/lib/pert/PertCalculator.svelte` (line ~288); F-03 numeral `font-extrabold` from commit 29306e7 preserved byte-identical; eyebrow color preserved (Identity-Inside Rule); visible text + numeral expression preserved (Pitfall 4 selector preservation); 1 commit landed with the plan-frontmatter `commit_message`.
- All 9 plan-level quality gates green: svelte-check 0/0; vitest src/lib/pert/ 81/81; em-dash + en-dash sweep 0/0 on PertCalculator.svelte; tabular-numerals >= 5 hits; F-03 numeral preserved (font-extrabold count = 1); Approach C bump landed (tracking-wider count = 1; tracking-wide-only drops 8 -> 7); function-binding bridges >= 3 hits; string-bridge proxies = 0; pert-config.json byte-identical against HEAD; plan-level negative-space audit clean (only `src/lib/pert/PertCalculator.svelte` modified).
- Human-eye visual UAT checkpoint completed (Task 3); user's resume signal captured verbatim in 04-04-SUMMARY.md.
- 04-04-SUMMARY.md authored at `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md` with all 14 required sections including the gap-closure rationale, the human-UAT outcome, the escalation ladder forward note (Approach D/E), and the REQUIREMENTS.md re-validation note.
- D-08 PERT-route allowlist honored: only `src/lib/pert/PertCalculator.svelte` (and the planning artifact 04-04-SUMMARY.md) modified by this plan.
- D-08b strictly-forbidden boundary honored: zero shared component / NavShell / HamburgerMenu / other-calculator / DESIGN.md / DESIGN.json / calc-layer / PertInputs.svelte / pert-config.json / e2e edits.
- Phase 3.1 + Phase 4 invariants preserved: function-binding bridges intact, zero string-bridge proxies, em-dash + en-dash 0/0 across PERT-route files, STOP-red advisory carve-out untouched, F-03 numeral `font-extrabold` byte-identical from commit 29306e7.
- Verifier (`/gsd-verify-work 4 --ws pert`) is unblocked and ready to re-check the 04-UAT.md gap row against the post-Approach-C UI.
</success_criteria>

<output>
This plan ships:

1. `src/lib/pert/PertCalculator.svelte` -- F-03 Approach C escalation: 1-LOC eyebrow `tracking-wide` -> `tracking-wider` swap on the Tube-Feed `Capsules per month` row (line ~288). The F-03 numeral `font-extrabold` from commit `29306e7` is preserved byte-identical. PERT-route only; Identity-Inside Rule honored; Pitfall 4 selector preservation honored.

2. `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-04-SUMMARY.md` -- Wave 4 (gap-closure) closure record covering the Approach C escalation + the gap-closure rationale (LLM-Design-Review fallback overlooked the issue at Phase 4 closure; human UAT corrected) + the human-eye visual UAT outcome + the escalation ladder forward note (Approach D = numeral text-title -> text-[28px] or text-display; Approach E = visual isolation via background tint, top divider, or icon prefix) + the REQUIREMENTS.md re-validation note (re-run `/gsd-verify-work 4 --ws pert` after this plan ships; do NOT flip REQUIREMENTS.md from this plan).

After commit, the user runs `/gsd-verify-work 4 --ws pert` to re-check the 04-UAT.md gap row test #1 against the post-Approach-C UI. If the human-eye UAT in Task 3 recorded `approved`, the gap is closed and Phase 5 (Release) is unblocked. If escalation-required, the user runs `/gsd-plan-phase 4 --gaps --ws pert` to author Wave 5 (Approach D escalation per the documented ladder).
</output>

<self_check>
Mirroring the 10-dimension Self-Check structure of 04-02-PLAN.md:

1. **Frontmatter completeness:** `phase`, `plan`, `type`, `wave`, `depends_on`, `files_modified`, `requirements`, `autonomous`, `gap_closure`, `tags`, `must_haves` (truths + artifacts + key_links), `commit_message` -- all present. `gap_closure: true` is set per the orchestrator's `--gaps-only` filter requirement. `requirements` lists PERT-DESIGN-02..05 (the IDs the original F-03 fix originally satisfied per 04-02-PLAN.md and 04-02-SUMMARY.md mapping).

2. **Task atomicity:** 4 tasks. Task 1 (source edit on PertCalculator.svelte; ~5% context). Task 2 (verification gates; ~5% context). Task 3 (human-eye visual UAT checkpoint; pauses for user input). Task 4 (SUMMARY authoring; ~10% context). Total ~20-25% context; well under the 50% target. Each task is single-concern; no task touches more than 1 production file (Task 1 only) or only the planning artifact (Task 4 only).

3. **Verify gates:** every task has a `<verify>` block with `<automated>` commands. Task 1 has 12 grep-based and git-diff-based assertions. Task 2 has 9 gate-log-based and grep-based assertions. Task 3 has the human checkpoint resume signal (no automated verify; gate is the user typing). Task 4 has 5 grep-based assertions on the SUMMARY content + commit existence.

4. **Acceptance criteria:** every task has an `<acceptance_criteria>` block enumerating the testable conditions. Task 1's criteria specify exact grep counts (`tracking-wider` = 1; `tracking-wide`-only = 7; `font-extrabold` = 1; em + en-dash = 0; etc.). Task 2's criteria mirror the 9 gates. Task 4's criteria specify the 14 SUMMARY sections + the SUMMARY-level em + en-dash sweep.

5. **Done criteria:** every task has a `<done>` block with the post-task state of completion. Phrased in observable terms (file modified, commit landed, gates green, etc.).

6. **Identity-Inside Rule preservation:** explicitly enforced at multiple layers -- in the must_haves truths (truth #2), in the <interfaces> block (FORBIDDEN list), in Task 1's STEP B baseline check + STEP C edit + STEP D scope verification, in Task 1's verify gate #4 (eyebrow color token preserved), in Task 1's FORBIDDEN list, in Task 3's "DO NOT LOOK FOR" guidance, in Task 4's SUMMARY required sections (PERT-DESIGN-03 satisfaction mapping subsection).

7. **Pitfall 4 selector preservation:** explicitly enforced at multiple layers -- in the must_haves truths (truth #3), in the <interfaces> block (FORBIDDEN list), in Task 1's verify gates #5 + #6 (visible text + numeral expression preserved), in Task 1's FORBIDDEN list, in Task 1's STEP F commit body fields, in Task 4's SUMMARY required sections (Pitfall 4 selector preservation marker checked by verify gate #2).

8. **D-08 PERT-route allowlist + D-08b strictly-forbidden boundary:** explicitly enforced at multiple layers -- in the must_haves truths (truth #4), in the <interfaces> block, in Task 1's FORBIDDEN list (10 explicit forbidden file/file-group entries), in Task 1's verify gates #9, #10, #11, #12 (only PertCalculator.svelte modified; PertInputs / pert-config / calc-layer all byte-identical), in Task 2's Gate 8 + Gate 9, in Task 4's negative-space audit subsection.

9. **F-03 numeral preservation (Wave 2 invariant):** explicitly enforced at multiple layers -- in the must_haves truths (truth #5: "F-03 numeral `font-extrabold` from commit 29306e7 preserved"), in the <interfaces> block (POST-FIX section shows `font-extrabold` byte-identical; FORBIDDEN list explicitly forbids re-touching the numeral), in Task 1's STEP B baseline check (`font-extrabold` count = 1 expected pre-edit), in Task 1's STEP D scope verification (`font-extrabold` count still = 1 post-edit), in Task 1's verify gate #3, in Task 1's FORBIDDEN list, in Task 2's Gate 5, in Task 4's SUMMARY required sections (F-03 byte-identity confirmation).

10. **Human-UAT-as-corrective-process insertion (the core purpose of Wave 4):** explicitly framed at multiple layers -- in the must_haves truths (truth #6: "User-visual UAT gate replaces the LLM-Design-Review verdict from Phase 4 closure"), in the <objective> (multi-paragraph framing of the LLM-Design-Review fallback overlooking the issue and the human-UAT-as-corrective-process insertion), in Task 3 (the entire checkpoint task is this gate; <what-built> + <how-to-verify> + <resume-signal> are all structured around the human-eye review), in Task 4's SUMMARY required sections (gap-closure rationale subsection + human-visual UAT outcome subsection + REQUIREMENTS.md re-validation note subsection + PERT-DESIGN satisfaction mapping subsection that notes PERT-DESIGN-06 score-target re-validation is NOT required by this plan since the Wave 3 LLM verdict already met the bar).

All 10 dimensions PASSED.

**Additional self-check items per the orchestrator's gap-closure planner constraints:**

- `gap_closure: true` in frontmatter: PRESENT (line 18).
- Plan numbering 04-04 (next sequential after 04-01, 04-02, 04-03): CORRECT.
- Wave assignment `wave: 4` (after 1/2/3): CORRECT.
- Single plan, not multiple (no chained Approach D + E): CORRECT.
- D-08 allowlist + D-08b strictly-forbidden: ENFORCED (see dimension 8 above).
- Identity-Inside Rule (TRACKING change, NOT color change): ENFORCED (see dimension 6 above).
- Pitfall 4 selector preservation: ENFORCED (see dimension 7 above).
- Em + en-dash sweep on PertCalculator.svelte = 0: ENFORCED at Task 1 STEP E + Task 2 Gate 3 + Task 1 verify gate #7.
- Human visual UAT as verification gate: ENFORCED at Task 3 (checkpoint:human-verify; the entire task is the human-eye review gate).
- Commit message form `fix(pert/04-XX):`: PRESENT (`fix(pert/04-04): F-03 escalation to Approach C -- eyebrow tracking-wider bump`).
- REQUIREMENTS.md NOT touched: ENFORCED (the must_haves truth #10 documents the loop; Task 4's SUMMARY required sections include the REQUIREMENTS.md re-validation note explicitly stating "this plan does NOT touch REQUIREMENTS.md").
- Tasks structure mirrors 04-02-PLAN.md: TASK 1 (source edit) + TASK 2 (gates) + TASK 3 (human checkpoint inserted; this is the Wave 4 escalation over Wave 2's structure) + TASK 4 (SUMMARY).
- Task-level <verify> blocks include grep assertions: PRESENT throughout.
- <interfaces> block with code excerpts (CURRENT vs POST-FIX): PRESENT (lines ~260-300 of this plan; eyebrow `<div>` before/after; reference to F-03 numeral byte-identity at line 293).
- must_haves block covers (a) Approach C scoped to ONLY Capsules per month eyebrow (truth #1), (b) Identity-Inside preserved (truth #2), (c) e2e selectors preserved (truth #3), (d) no D-08b violation (truth #4), (e) human-visual-UAT gate replaces LLM-Design-Review verdict (truth #6), (f) Phase 3.1 invariants preserved (truth #5): ALL PRESENT.
- Self-Check at the end of the plan with 10-dimension structure: PRESENT (this section).

Self-Check: PASSED.
</self_check>
</content>
</invoke>