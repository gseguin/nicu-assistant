---
phase: 3
plan: 5
type: execute
wave: 4
workstream: pert
milestone: v1.15
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified: []
requirements:
  - PERT-TEST-04
  - PERT-TEST-06
autonomous: true
must_haves:
  truths:
    - "All four clinical-gate commands run green: pnpm check 0/0; pnpm test:run all-green; pnpm build PWA bundle compiles; CI=1 pnpm exec playwright test full suite all-green except the pre-existing baseline flake (disclaimer-banner.spec.ts:28)."
    - "PERT-TEST-04 closure verified: Phase 1's src/lib/pert/config.test.ts continues to pass with all 11 tests green AND Plan 03-02's calculations.test.ts contains the 2-test 'PERT-TEST-04 config-to-calc integration (D-09 delta)' describe block green. The plan summary docstring cites: 'PERT-TEST-04 mostly covered by Phase 1 src/lib/pert/config.test.ts (11 tests); Phase 3 adds end-to-end wiring delta in calculations.test.ts'."
    - "PERT-TEST-06 closure verified: CI=1 pnpm exec playwright test pert-a11y --reporter=line returns exactly 4 passed (no new axe specs). The plan summary docstring cites: 'PERT-TEST-06 fully covered by Phase 1 e2e/pert-a11y.spec.ts (4 sweeps green); Phase 3 verifies no regression'."
    - "Vitest baseline preservation (Q7): the prior-phase 361 tests from Phase 2 STATE.md (38 files) all stay green; new Phase 3 tests (calculations.test.ts + PertCalculator.test.ts + PertInputs.test.ts) all green; total vitest count is locked in the verification report."
    - "Playwright total count: 105 prior baseline + N new from Plan 03-04's e2e/pert.spec.ts = total runs locked at execution time. The 1 baseline flake on disclaimer-banner.spec.ts:28 is allowed (Phase 1+2 verifier baseline; pre-existing)."
    - "No new files created in this plan (verification-only). git diff --name-only against the previous wave's commit shows zero changes outside .planning/."
    - "All Phase-1-frozen artifacts confirmed unchanged via git diff: src/lib/pert/config.test.ts, src/lib/pert/state.test.ts, e2e/pert-a11y.spec.ts, vitest.config.ts (or vite.config.ts vitest section), playwright.config.ts."
  artifacts: []
  key_links:
    - from: .planning/workstreams/pert/phases/03-tests/03-05-SUMMARY.md
      to: .planning/workstreams/pert/phases/03-tests/03-04-SUMMARY.md
      via: "Plan 03-05's exact-count assertions read the locked Playwright test count from Plan 03-04's summary (the new pert.spec.ts test total is decided when 03-04 ships; Plan 03-05 asserts 105 prior + that count)."
      pattern: "pert.spec.ts"
    - from: .planning/workstreams/pert/phases/03-tests/03-05-SUMMARY.md
      to: e2e/pert-a11y.spec.ts
      via: "Asserts CI=1 pnpm exec playwright test pert-a11y returns 4/4; PERT-TEST-06 complete-by-prior-phase per CONTEXT D-12"
      pattern: "pert-a11y"
    - from: .planning/workstreams/pert/phases/03-tests/03-05-SUMMARY.md
      to: src/lib/pert/config.test.ts
      via: "Asserts Phase-1's 11 config tests all green; PERT-TEST-04 mostly-Phase-1-covered + Plan 03-02 delta"
      pattern: "config.test"
commit_message: "docs(pert/03-05): verify clinical gate green and document PERT-TEST-04 + PERT-TEST-06 closure"
---

<objective>
Verification-only plan. NO code or test files are created or modified. The plan
runs the four clinical-gate commands, asserts the locked counts/states, and
documents the complete-by-prior-phase closure for PERT-TEST-04 (mostly Phase 1) and
PERT-TEST-06 (fully Phase 1).

Purpose: provide an auditable end-of-phase gate proving:
1. The full test pyramid green (svelte-check / vitest / build / Playwright).
2. Phase 1 + Phase 2's frozen artifacts not regressed (config.test.ts 11 green,
   state.test.ts 6 green, pert-a11y.spec.ts 4 green, full Playwright still 105
   prior + same baseline-flake state).
3. PERT-TEST-04 closure: docstring + grep proves Phase 1's 11 tests + Plan 03-02's
   2-test integration delta both deliver the requirement.
4. PERT-TEST-06 closure: docstring + Playwright sweep proves Phase 1's 4-sweep
   axe coverage stays green; no new axe specs needed (CONTEXT D-12).

Output: NO files modified; only `.planning/workstreams/pert/phases/03-tests/03-05-SUMMARY.md`
written at the end documenting the verification result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/pert/STATE.md
@.planning/workstreams/pert/phases/03-tests/03-CONTEXT.md
@.planning/workstreams/pert/phases/03-tests/03-RESEARCH.md
@.planning/workstreams/pert/phases/03-tests/03-01-SUMMARY.md
@.planning/workstreams/pert/phases/03-tests/03-02-SUMMARY.md
@.planning/workstreams/pert/phases/03-tests/03-03-SUMMARY.md
@.planning/workstreams/pert/phases/03-tests/03-04-SUMMARY.md
@CLAUDE.md
@DESIGN.md

<interfaces>
<!-- Locked baselines from STATE.md (Phase 2 outcomes) -->

- vitest baseline: 361 tests across 38 files (preserved through Phase 2; Phase 3 ships net new tests).
- Playwright baseline: 105/106 (the 1 baseline flake is `disclaimer-banner.spec.ts:28` pre-existing — STATE.md confirms it's pre-existing, not a Phase 2 regression).
- pert-a11y.spec.ts: 4/4 (Phase 1 frozen).
- svelte-check: 0/0 across 4582 files.

<!-- Plan 03-04's locked test count -->

The exact test count delta from `e2e/pert.spec.ts` is recorded in
`.planning/workstreams/pert/phases/03-tests/03-04-SUMMARY.md` after Plan 03-04
ships. Plan 03-05's count assertion reads that file at run time and verifies
105 + locked-delta against the actual full-suite run output.

<!-- Phase-1-frozen artifacts (must NOT be modified) -->

- src/lib/pert/config.test.ts (11 tests)
- src/lib/pert/state.test.ts (6 tests)
- e2e/pert-a11y.spec.ts (4 sweeps)
- vite.config.ts (vitest section inlined at lines 60-65)
- playwright.config.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full clinical gate; verify Phase-1-frozen artifacts unchanged; verify PERT-TEST-04 + PERT-TEST-06 closure docstrings; record results in 03-05-SUMMARY.md</name>
  <read_first>
    - .planning/workstreams/pert/phases/03-tests/03-04-SUMMARY.md (read the LOCKED Playwright test count delta for e2e/pert.spec.ts; the assertion below uses this number)
    - .planning/workstreams/pert/phases/03-tests/03-02-SUMMARY.md (read the test count + the 'PERT-TEST-04' docstring location in calculations.test.ts)
    - .planning/workstreams/pert/STATE.md (verify Phase 2 baseline counts: vitest 361/361, Playwright 105/106 with disclaimer-banner.spec.ts:28 baseline flake)
    - .planning/workstreams/pert/phases/03-tests/03-CONTEXT.md (D-09 PERT-TEST-04 mostly-Phase-1-covered; D-12 PERT-TEST-06 fully-Phase-1-covered)
  </read_first>
  <files></files>
  <action>
This is a verification-only plan; do NOT create or modify any production files,
test files, or fixtures. The only file written is the plan summary at the end.

**Step 1: Confirm no working-tree changes outside .planning/.**

```bash
git diff --name-only HEAD | grep -v '^.planning/' || echo "no source-tree changes"
```

If anything other than `.planning/` files appear, halt and investigate (Phase 3
should be at this point with all source-tree changes already committed via
Plans 03-01 through 03-04).

**Step 2: Confirm Phase-1-frozen artifacts unchanged across the entire phase.**

Identify the Phase 2 base commit (from STATE.md `055a1f1`). Run:

```bash
git diff 055a1f1..HEAD -- \
  src/lib/pert/config.test.ts \
  src/lib/pert/state.test.ts \
  e2e/pert-a11y.spec.ts \
  vite.config.ts \
  playwright.config.ts
```

Output MUST be empty. If anything appears, halt; a frozen file was modified
and must be reverted before proceeding.

Also verify no other production code was changed in Phase 3 (the only
production-tree changes should be the four NEW test/fixture files and zero
modifications to .svelte/.ts/.json production files):

```bash
git diff --name-only 055a1f1..HEAD -- src/ e2e/ | sort
```

Expected output exactly:
```
e2e/pert.spec.ts
src/lib/pert/PertCalculator.test.ts
src/lib/pert/PertInputs.test.ts
src/lib/pert/calculations.test.ts
src/lib/pert/pert-parity.fixtures.json
```

If any other path appears, halt and investigate.

**Step 3: Run pnpm check (svelte-check).**

```bash
pnpm check
```

Expected: 0 errors / 0 warnings. Record the exact line counts.

**Step 4: Run full vitest baseline + Phase 3 new tests.**

```bash
pnpm test:run
```

Expected (per Q7):
- All Phase 1 + Phase 2 prior 361 tests green (38 files baseline).
- Plan 03-02's calculations.test.ts green.
- Plan 03-03's PertCalculator.test.ts + PertInputs.test.ts green.
- Phase-1-frozen config.test.ts (11 tests) + state.test.ts (6 tests) green.
- Total green count = 361 + new (the new count from Plans 03-02 + 03-03 summaries).
- Zero failures.

Record the exact test/file counts.

**Step 5: Run pnpm build.**

```bash
pnpm build
```

Expected: PWA bundle compiles successfully (Phase 2's STATE.md confirms 50 entries / ~563 KiB; size may differ slightly with Phase 3 — that's fine; what matters is the build succeeds).

**Step 6: Run pert-a11y subset (PERT-TEST-06 closure).**

```bash
CI=1 pnpm exec playwright test pert-a11y --reporter=line
```

Expected: `4 passed`. PERT-TEST-06 closure: Phase 1 fully covers; Phase 3 verifies no regression. Record the exact count.

**Step 7: Run new pert.spec.ts (PERT-TEST-05 verification).**

```bash
CI=1 pnpm exec playwright test pert.spec.ts --reporter=line
```

Expected: all green (the count is the locked delta from Plan 03-04's summary). Record the exact count.

**Step 8: Run full Playwright regression.**

```bash
CI=1 pnpm exec playwright test --reporter=line
```

Expected:
- Total count = 105 prior baseline + locked-delta from 03-04 SUMMARY.
- The 1 baseline flake on `disclaimer-banner.spec.ts:28` is allowed (pre-existing per Phase 2 STATE.md).
- No NEW failures vs Phase 2 baseline (105/106).

Record the exact total count + pass/fail breakdown. If any NEW failure appears (anything other than the documented `disclaimer-banner.spec.ts:28` flake), halt and investigate.

**Step 9: Verify PERT-TEST-04 closure docstring.**

```bash
grep -A 2 "PERT-TEST-04 config-to-calc integration" src/lib/pert/calculations.test.ts
```

Expected: the docstring/comment cites Phase 1's `config.test.ts` 11 tests + Phase 3's integration delta. Record the exact substring matched.

**Step 10: Verify PERT-TEST-06 closure docstring (in this plan's summary, not a code file).**

The Plan 03-05 summary itself carries the PERT-TEST-06 closure docstring per the requirement-claim contract. The summary text will read:
"PERT-TEST-06 fully covered by Phase 1 e2e/pert-a11y.spec.ts (4 sweeps green); Phase 3 verifies no regression. Verification: `CI=1 pnpm exec playwright test pert-a11y --reporter=line` returns 4 passed."

**Step 11: Em-dash ban verification across ALL Phase 3 NEW files.**

```bash
grep -c "—" src/lib/pert/pert-parity.fixtures.json src/lib/pert/calculations.test.ts src/lib/pert/PertCalculator.test.ts src/lib/pert/PertInputs.test.ts e2e/pert.spec.ts
grep -c "–" src/lib/pert/pert-parity.fixtures.json src/lib/pert/calculations.test.ts src/lib/pert/PertCalculator.test.ts src/lib/pert/PertInputs.test.ts e2e/pert.spec.ts
```

Expected: every line in both outputs ends in `:0`. (Q1 broad ban; Phase 3 honors the Phase 1 + Phase 2 convention.)

**Step 12: Write `.planning/workstreams/pert/phases/03-tests/03-05-SUMMARY.md`.**

Required content:

1. Header noting Plan 03-05 is verification-only (no source files modified).
2. Step-by-step verification results table:
   - Step 1: working-tree clean (PASS/FAIL)
   - Step 2: Phase-1-frozen artifacts unchanged (PASS/FAIL; cite the exact `git diff 055a1f1..HEAD -- src/ e2e/` output as the 5 expected paths)
   - Step 3: `pnpm check` (0 errors / 0 warnings)
   - Step 4: `pnpm test:run` (X tests across Y files — record exact)
   - Step 5: `pnpm build` (PASS/FAIL)
   - Step 6: `CI=1 pnpm exec playwright test pert-a11y --reporter=line` (4/4)
   - Step 7: `CI=1 pnpm exec playwright test pert.spec.ts --reporter=line` (record exact count)
   - Step 8: `CI=1 pnpm exec playwright test --reporter=line` (105 + locked-delta = total; baseline flake state)
   - Step 11: em-dash + en-dash grep (zero across all 5 new Phase 3 files)
3. Requirement closure section:
   - PERT-TEST-01: closed by Plan 03-01 fixtures + Plan 03-02 oral parity describe block (9 tests).
   - PERT-TEST-02: closed by Plan 03-01 fixtures + Plan 03-02 tube-feed parity describe block (18 tests).
   - PERT-TEST-03: closed by Plan 03-03 PertCalculator.test.ts (10 tests) + PertInputs.test.ts (7 tests).
   - **PERT-TEST-04**: "PERT-TEST-04 mostly covered by Phase 1 src/lib/pert/config.test.ts (11 tests); Phase 3 adds end-to-end wiring delta via Plan 03-02's 'PERT-TEST-04 config-to-calc integration (D-09 delta)' describe block (2 tests)." Cite the grep result from Step 9.
   - PERT-TEST-05: closed by Plan 03-04 e2e/pert.spec.ts (record exact count from Step 7).
   - **PERT-TEST-06**: "PERT-TEST-06 fully covered by Phase 1 e2e/pert-a11y.spec.ts (4 sweeps green); Phase 3 verifies no regression. Verification: `CI=1 pnpm exec playwright test pert-a11y --reporter=line` returns 4 passed." Cite the Step 6 result.
4. ROADMAP success-criteria mapping (Phase 3 has 4 success criteria from ROADMAP):
   - SC1 (parity within 1% epsilon both modes including xlsx defaults): closed by 03-01 + 03-02. Note: ROADMAP's "weight 22 lbs ~= 9.98 kg, fat 25 g, lipase 1000/kg, Creon 12000" is stale per CONTEXT D-04 + D-05; row 0 actual is weight=10 kg / fat=25 / lipasePerG=2000 / Creon 12000 (xlsx live read). ROADMAP wording cleanup is Phase 5.
   - SC2 (component tests + config shape): closed by 03-03 + Phase-1-frozen config.test.ts + 03-02 integration delta.
   - SC3 (Playwright E2E happy-path mobile + desktop both modes + inputmode + favorites + sessionStorage): closed by 03-04. Note: ROADMAP says sessionStorage; reality is localStorage `nicu_pert_state` per Phase 1 D-09 + CONTEXT D-11. ROADMAP wording cleanup is Phase 5.
   - SC4 (axe suite extended): closed-by-prior-phase per CONTEXT D-12 + the 4/4 verification above. Note: ROADMAP wording "33/33 -> 35/35" is stale per CONTEXT D-12; Phase 1 added 4 sweeps not 2; cleanup is Phase 5.
5. Doc-drift items confirmed for Phase 5 cleanup (already in STATE.md; this summary just confirms they remain and adds any new ones surfaced during Phase 3).
6. Em-dash ban honored throughout: zero matches across all 5 new Phase 3 files.

Em-dash ban applies to this summary file too. Use ASCII punctuation only. `grep -c "—" .planning/workstreams/pert/phases/03-tests/03-05-SUMMARY.md` must return 0.
  </action>
  <verify>
    <automated>
      # The action steps ARE the verification. The full clinical gate is run inline above.

      # Final sanity: Plan 03-05 produces zero src/ or e2e/ changes (verification-only).
      git diff --name-only HEAD~0 HEAD -- src/ e2e/ vite.config.ts playwright.config.ts package.json | wc -l | grep -qx 0 && echo "verification plan made no source changes"

      # The Plan 03-05 summary file MUST exist.
      test -f .planning/workstreams/pert/phases/03-tests/03-05-SUMMARY.md

      # Em-dash ban honored in the summary too.
      grep -c "—" .planning/workstreams/pert/phases/03-tests/03-05-SUMMARY.md | grep -qx 0
      grep -c "–" .planning/workstreams/pert/phases/03-tests/03-05-SUMMARY.md | grep -qx 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check` returns 0 errors / 0 warnings.
    - `pnpm test:run` returns all-green (361 prior-phase + new Phase-3 tests; exact count recorded in 03-05-SUMMARY).
    - `pnpm build` succeeds (PWA bundle compiles).
    - `CI=1 pnpm exec playwright test pert-a11y --reporter=line` returns exactly `4 passed` (PERT-TEST-06 closure).
    - `CI=1 pnpm exec playwright test pert.spec.ts --reporter=line` returns the locked count from 03-04-SUMMARY all-green.
    - `CI=1 pnpm exec playwright test --reporter=line` returns 105 + locked-delta total with NO new failures (only the documented `disclaimer-banner.spec.ts:28` baseline flake allowed).
    - `git diff 055a1f1..HEAD -- src/ e2e/` shows EXACTLY 5 paths: pert-parity.fixtures.json + calculations.test.ts + PertCalculator.test.ts + PertInputs.test.ts + e2e/pert.spec.ts.
    - `git diff 055a1f1..HEAD -- src/lib/pert/config.test.ts src/lib/pert/state.test.ts e2e/pert-a11y.spec.ts vite.config.ts playwright.config.ts` returns empty (Phase-1-frozen artifacts untouched).
    - `grep -c "—"` AND `grep -c "–"` both return 0 for every Phase 3 NEW file (5 files) AND for the 03-05-SUMMARY.md.
    - `grep "PERT-TEST-04 config-to-calc integration" src/lib/pert/calculations.test.ts` returns a match (the closure docstring is in place from Plan 03-02).
    - `.planning/workstreams/pert/phases/03-tests/03-05-SUMMARY.md` exists, documents all 12 verification steps, declares PERT-TEST-04 + PERT-TEST-06 closure with the required docstrings.
  </acceptance_criteria>
  <done>
    - All 12 verification steps run + recorded.
    - PERT-TEST-04 + PERT-TEST-06 closure docstrings published.
    - 03-05-SUMMARY.md written.
    - Phase 3 ready to be marked complete in workstream STATE.md (handled by Phase 3's verifier or by the next /gsd-phase-verify run).
  </done>
</task>

</tasks>

<verification>
**Wave gate (Wave 4 / Plan 03-05) = Phase 3 exit gate:**

1. `pnpm check` 0/0.
2. `pnpm test:run` all-green (361 + new).
3. `pnpm build` succeeds.
4. `CI=1 pnpm exec playwright test pert-a11y` 4/4.
5. `CI=1 pnpm exec playwright test pert.spec.ts` all-green at locked count.
6. `CI=1 pnpm exec playwright test` (full suite) at 105 + locked-delta with the 1 documented baseline flake allowed; NO new failures.
7. Phase-1-frozen artifacts (`config.test.ts`, `state.test.ts`, `pert-a11y.spec.ts`, `vite.config.ts`, `playwright.config.ts`) unchanged across the entire phase (`git diff 055a1f1..HEAD` returns empty for those paths).
8. Source-tree diff shows EXACTLY 5 new files (Plans 03-01 through 03-04 outputs).
9. PERT-TEST-04 closure docstring present in `calculations.test.ts`.
10. PERT-TEST-06 closure docstring present in `03-05-SUMMARY.md`.
11. Em-dash + en-dash grep returns 0 across all 5 new Phase 3 files AND across the summary.
</verification>

<success_criteria>
- All 4 clinical-gate commands green (svelte-check / vitest / build / Playwright).
- PERT-TEST-04 closure: Phase-1 11 config tests + Plan 03-02's 2-test integration delta both green; docstring cites the Phase-1-mostly-covered status.
- PERT-TEST-06 closure: Phase-1 4 axe sweeps still green; no new axe specs added; docstring cites the Phase-1-fully-covered status.
- Vitest baseline (361 prior-phase tests) preserved across Phase 3.
- Playwright baseline (105 prior-phase + the 1 documented flake) preserved across Phase 3.
- All 6 PERT-TEST-* requirements claimed by at least one Plan in Phase 3.
- 03-05-SUMMARY.md documents the verification result + per-requirement closure mapping.
- Em-dash ban honored uniformly.
</success_criteria>

<output>
Create `.planning/workstreams/pert/phases/03-tests/03-05-SUMMARY.md` with the
verification results table, requirement closure section (PERT-TEST-01..06 each
mapped to its closing plan/phase), ROADMAP success-criteria mapping with the 4
documented doc-drift annotations (cell labels / Kate Farms 48 not 40 /
sessionStorage->localStorage / "33/33 -> 35/35" arithmetic — all already pre-
authorized for Phase 5 cleanup), and a one-line "Phase 3 ready for /gsd-phase-verify"
green-light statement.
</output>
