---
phase: 2
plan: 1
type: execute
wave: 0
workstream: pert
milestone: v1.15
depends_on: []
files_modified:
  - src/lib/pert/pert-config.json
requirements:
  - PERT-SAFE-01
  - PERT-SAFE-02
  - PERT-SAFE-03
autonomous: true
must_haves:
  truths:
    - "All four advisory message strings in pert-config.json are em-dash-free."
    - "Strings end with a period (or follow ASCII sentence punctuation) per DESIGN.md em-dash ban."
    - "Phase-1-frozen state.test.ts and config.test.ts continue to pass with zero changes."
    - "max-lipase-cap STOP-red advisory now has a clinically clear, em-dash-free string ready for Wave 2 rendering."
  artifacts:
    - path: src/lib/pert/pert-config.json
      provides: "Em-dash-clean advisory message strings (lines 99, 108, 117, 126)."
      contains: "Exceeds 10,000 units/kg/day cap. Verify with prescriber."
  key_links:
    - from: src/lib/pert/pert-config.json
      to: src/lib/pert/config.ts
      via: "JSON import unchanged; advisories array shape unchanged."
      pattern: "advisories: Advisory\\[\\] = config.advisories as Advisory\\[\\]"
    - from: src/lib/pert/pert-config.json
      to: DESIGN.md
      via: "Em-dash ban compliance — Don't list line 312"
      pattern: "[^—]"
commit_message: "fix(pert/02-01): replace em-dashes in advisory messages per D-19"
---

<objective>
Wave 0 mechanical fix per **CONTEXT D-19** (user-locked 2026-04-25 via UI-SPEC):
remove the four em-dashes (`—`) currently shipped in `src/lib/pert/pert-config.json`
advisory `message` strings (lines 99, 108, 117, 126). DESIGN.md line 312 bans em-dash
in user-rendered strings; Phase 1 missed this because the strings were never rendered.
Phase 2 Wave 2 will RENDER these strings — fixing them now keeps Wave 2 contract clean
and avoids a Phase 4 polish-rework cycle.

**Purpose:** unblock STOP-red and warning-advisory rendering with copy that satisfies
DESIGN.md from the moment it appears on screen.

**Output:** `pert-config.json` modified in 4 places; no other file changes; Phase 1
tests stay green.

**No behavior change.** Phase-1 `state.test.ts` and `config.test.ts` do NOT assert these
strings (config.test.ts only asserts shape + count + STOP severity).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/pert/STATE.md
@.planning/workstreams/pert/ROADMAP.md
@.planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-CONTEXT.md
@.planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-UI-SPEC.md
@DESIGN.md
@CLAUDE.md

<interfaces>
<!-- The pert-config.json shape is Phase-1-frozen for advisory entries. Only the
     `message` strings change in this plan. -->

From src/lib/pert/types.ts:
```typescript
export type AdvisorySeverity = 'warning' | 'stop';

export interface Advisory {
  id: string;
  field: string;
  comparator: 'gt' | 'gte' | 'lt' | 'lte' | 'outside';
  value: number | { min: number; max: number };
  message: string;          // <-- THIS is what changes (4 entries)
  severity: AdvisorySeverity;
  mode?: PertMode | 'both';
}
```

From src/lib/pert/config.ts (read-only consumer; no edit):
```typescript
export const advisories: Advisory[] = config.advisories as Advisory[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace em-dashes in 4 advisory message strings</name>
  <files>src/lib/pert/pert-config.json</files>
  <read_first>
    <file>src/lib/pert/pert-config.json</file>
    <file>DESIGN.md</file>
    <file>src/lib/pert/state.test.ts</file>
    <file>src/lib/pert/config.test.ts</file>
  </read_first>
  <action>
Open `src/lib/pert/pert-config.json`. Replace EXACTLY these four `message` field values
(line numbers reflect the current file from Phase 1):

1. **Line 99** (`max-lipase-cap.message`):
   - Before: `"Exceeds 10,000 units/kg/day cap — verify with prescriber"`
   - After:  `"Exceeds 10,000 units/kg/day cap. Verify with prescriber."`

2. **Line 108** (`weight-out-of-range.message`):
   - Before: `"Outside expected pediatric range — verify"`
   - After:  `"Outside expected pediatric range. Verify."`

3. **Line 117** (`fat-out-of-range.message`):
   - Before: `"Outside expected fat range — verify"`
   - After:  `"Outside expected fat range. Verify."`

4. **Line 126** (`volume-out-of-range.message`):
   - Before: `"Outside expected volume range — verify"`
   - After:  `"Outside expected volume range. Verify."`

**Constraints:**
- Use the Edit tool, not a regex sweep — each replacement is a literal swap of one
  string field value. Keep all other JSON whitespace, indentation, and field order
  identical.
- Do **NOT** modify any other field in `pert-config.json` (no edits to advisory `id`,
  `field`, `comparator`, `value`, `severity`, or `mode`; no edits to `defaults`,
  `inputs`, `dropdowns`, `validationMessages`, or `fdaAllowlist`).
- The 4 messages are now **period-terminated, em-dash-free**, and match UI-SPEC
  §Copywriting Contract verbatim. The `Verify with prescriber` capitalization
  on line 99 starts a new sentence; the other 3 use `Verify.` as a one-word imperative
  sentence — same form as the existing `<NumericInput>` inline blur error
  (`"Outside expected range. Verify."`).
- Do NOT touch `src/lib/pert/state.svelte.ts`, `src/lib/pert/types.ts`, or
  `src/lib/pert/config.ts` (Phase-1-frozen per CONTEXT).
  </action>
  <verify>
    <automated>cd /home/ghislain/gsd-workspaces/pert/nicu-assistant &amp;&amp; ! grep -n "—" src/lib/pert/pert-config.json &amp;&amp; grep -c "Exceeds 10,000 units/kg/day cap. Verify with prescriber." src/lib/pert/pert-config.json &amp;&amp; pnpm test:run src/lib/pert/ &amp;&amp; pnpm svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "—" src/lib/pert/pert-config.json` returns no matches (em-dash absent).
    - `grep -c "Verify with prescriber\\." src/lib/pert/pert-config.json` returns 1 (max-lipase-cap fixed).
    - `grep -c "Outside expected pediatric range\\. Verify\\." src/lib/pert/pert-config.json` returns 1.
    - `grep -c "Outside expected fat range\\. Verify\\." src/lib/pert/pert-config.json` returns 1.
    - `grep -c "Outside expected volume range\\. Verify\\." src/lib/pert/pert-config.json` returns 1.
    - `pnpm test:run src/lib/pert/` passes 17/17 (Phase-1 baseline: 6 state + 11 config tests).
    - `pnpm svelte-check` reports 0 errors / 0 warnings.
    - File diff shows exactly 4 line-level message changes; no other JSON keys modified.
  </acceptance_criteria>
  <done>4 em-dashes removed from advisory messages; Phase 1 tests stay green; svelte-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pert-config.json → render layer | Static JSON read at build time; advisory strings will render in Wave 2 (`<PertCalculator />` advisory cards). No untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | pert-config.json advisory copy | accept | Static clinical copy. No PII; advisory strings are clinically reviewed and DESIGN.md-compliant. |
| T-02-02 | Tampering | pert-config.json field shape | mitigate | Phase 1 `config.test.ts` shape tests gate the JSON structure (4 advisories, ids, severity); they will catch any accidental shape change in this edit. |
</threat_model>

<verification>
Run all gates before commit:

1. `cd /home/ghislain/gsd-workspaces/pert/nicu-assistant && grep -n "—" src/lib/pert/pert-config.json` → must return no matches.
2. `cd /home/ghislain/gsd-workspaces/pert/nicu-assistant && pnpm test:run src/lib/pert/` → must report 17/17 passed (Phase-1 baseline).
3. `cd /home/ghislain/gsd-workspaces/pert/nicu-assistant && pnpm test:run` → must NOT regress 361/361 baseline.
4. `cd /home/ghislain/gsd-workspaces/pert/nicu-assistant && pnpm svelte-check` → must report 0 errors / 0 warnings.
</verification>

<success_criteria>
- [ ] All 4 advisory `message` strings in `src/lib/pert/pert-config.json` are em-dash-free.
- [ ] Each replacement matches CONTEXT D-19 verbatim.
- [ ] No other field in the JSON is modified (verified by `git diff --stat` showing 1 file with at most ~8 line changes — 4 deletions + 4 insertions, ignoring possible whitespace).
- [ ] `pnpm test:run` reports 361/361 (no regression).
- [ ] `pnpm svelte-check` reports 0/0.
- [ ] Commit message: `fix(pert/02-01): replace em-dashes in advisory messages per D-19`.
</success_criteria>

<output>
After completion, create `.planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-01-SUMMARY.md`
following the SUMMARY template. Capture: lines edited, before/after string pairs, test
results (361/361, svelte-check 0/0), and any deviations from the plan (none expected).
</output>
