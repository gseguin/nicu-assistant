---
phase: 2
plan: 4
type: execute
wave: 2
workstream: pert
milestone: v1.15
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - src/lib/pert/PertCalculator.svelte
  - src/routes/pert/+page.svelte
requirements:
  - PERT-ORAL-06
  - PERT-ORAL-07
  - PERT-ORAL-08
  - PERT-TUBE-06
  - PERT-TUBE-07
  - PERT-MODE-04
  - PERT-SAFE-01
  - PERT-SAFE-04
autonomous: true
must_haves:
  truths:
    - "PertCalculator.svelte body replaced — empty state shows the Phase-1 promoted PERT eyebrow + mode qualifier; valid state shows a text-display capsules numeral with mode-appropriate unit ('capsules/dose' for oral, 'capsules/day' for tube-feed)."
    - "Secondary outputs card renders all required rows per mode (oral: total lipase, lipase per dose, estimated daily total tertiary; tube-feed: total fat g, total lipase, lipase per kg, capsules per month)."
    - "STOP-red advisory card renders BELOW the secondaries with border-2 border-[var(--color-error)] + bg-[var(--color-surface-card)] + OctagonAlert icon + bold red message text per D-04 + D-20 + UI-SPEC §STOP-red."
    - "Neutral warning advisories render BELOW the STOP-red card with the existing feeds-pattern shape (border-[var(--color-border)] + bg-[var(--color-surface-alt)] + AlertTriangle icon + secondary message text)."
    - "Empty-state gate per D-08: when ANY required input is null/≤0, secondary outputs + STOP-red + warnings are HIDDEN; hero shows empty-state copy from validationMessages."
    - "Hero <section> aria-live='polite' (existing); STOP-red advisory <section> uses role='alert' aria-live='assertive' for clinical urgency per UI-SPEC."
    - "Tertiary 'Estimated daily total (3 meals/day)' line renders only in oral mode, with text-2xs eyebrow + text-base value (D-09 verbatim)."
    - "+page.svelte mounts <PertInputs /> in BOTH the desktop sticky aside AND the mobile <InputDrawer> (replacing both Phase-1 placeholder text blocks)."
    - "+page.svelte recapItems extends with mode-specific entries (Fat g for oral; Formula name + Volume for tube-feed) per UI-SPEC §Copywriting Contract."
    - "Existing 4/4 Playwright pert-a11y axe sweeps stay GREEN (Phase 1 baseline)."
    - "Phase 1 state.test.ts and config.test.ts continue to pass; no Phase-1-frozen file outside the explicit list is touched."
  artifacts:
    - path: src/lib/pert/PertCalculator.svelte
      provides: "Calculator body — hero + secondary outputs + STOP-red advisory + warnings + empty-state."
      contains: "OctagonAlert"
      contains: "AlertTriangle"
      contains: "computeOralResult"
      contains: "computeTubeFeedResult"
      contains: "getTriggeredAdvisories"
      min_lines: 200
    - path: src/routes/pert/+page.svelte
      provides: "Route shell with PertInputs mounted in desktop aside + mobile drawer; recapItems extended."
      contains: "<PertInputs />"
  key_links:
    - from: src/lib/pert/PertCalculator.svelte
      to: src/lib/pert/calculations.ts
      via: "imports computeOralResult, computeTubeFeedResult, getTriggeredAdvisories"
      pattern: "from './calculations"
    - from: src/lib/pert/PertCalculator.svelte
      to: src/lib/pert/state.svelte.ts
      via: "imports pertState; reads .current.* in $derived blocks"
      pattern: "from './state.svelte"
    - from: src/routes/pert/+page.svelte
      to: src/lib/pert/PertInputs.svelte
      via: "imports + mounts in desktop aside (line 67-74 area) and mobile drawer (line 84-89 area)"
      pattern: "import PertInputs"
    - from: src/lib/pert/PertCalculator.svelte
      to: src/lib/pert/pert-config.json (via config.ts)
      via: "reads validationMessages.emptyOral / emptyTubeFeed for empty-state hero"
      pattern: "validationMessages"
commit_message: "feat(pert/02-04): wire calculator body with results, advisories, and route placeholders"
---

<objective>
Land the **integration wave** that turns the Phase-1 placeholder into a working
PERT calculator. Two files change: `src/lib/pert/PertCalculator.svelte` (body
replaced; outer `<div class="space-y-6">` shell preserved) and
`src/routes/pert/+page.svelte` (Phase-1 placeholder text in BOTH the desktop sticky
aside and the mobile InputDrawer replaced with `<PertInputs />`; `recapItems`
extended with mode-specific entries).

**This is the visible-to-clinicians plan.** Plans 02 + 03 produced testable parts;
this plan composes them. The exit gate includes the existing 4/4 Playwright
pert-a11y axe sweeps staying green (Phase 1 baseline; Phase 2 must not regress).

**Decisions implemented (Plan 04 surface):**
- D-01 / D-04 / D-08 / D-09 / D-10 / D-20: hero + STOP-red advisory + neutral
  warnings + empty-state + tertiary line + severity-DESC ordering + OctagonAlert.
- D-15 / D-16 (math): consumed via `computeOralResult` / `computeTubeFeedResult` from
  calculations.ts.
- PERT-MODE-04: aria-live polite on hero (already present from Phase 1's
  `<HeroResult>`), assertive on STOP-red advisory.

**Out of scope:** Component / E2E / parity tests — Phase 3. Design polish
critique sweep — Phase 4.

**Output:**
- `src/lib/pert/PertCalculator.svelte`: body replaced (~ 200-280 lines).
- `src/routes/pert/+page.svelte`: `<PertInputs />` mounted in 2 places; recapItems
  extended; otherwise unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/pert/STATE.md
@.planning/workstreams/pert/ROADMAP.md
@.planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-CONTEXT.md
@.planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-UI-SPEC.md
@.planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-01-SUMMARY.md
@.planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-02-SUMMARY.md
@.planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-03-SUMMARY.md
@DESIGN.md
@CLAUDE.md
@src/lib/pert/types.ts
@src/lib/pert/state.svelte.ts
@src/lib/pert/config.ts
@src/lib/pert/pert-config.json
@src/lib/pert/PertCalculator.svelte
@src/routes/pert/+page.svelte
@src/lib/feeds/FeedAdvanceCalculator.svelte
@src/lib/gir/GirCalculator.svelte

<interfaces>
<!-- Calc layer (from Plan 02 — verified live by that plan's verification gate) -->
From src/lib/pert/calculations.ts:
```typescript
export const MAX_LIPASE_PER_KG_PER_DAY: 10000;

export interface OralCalcInputs {
  fatGrams: number;
  lipasePerKgPerMeal: number;
  strengthValue: number;
}

export interface TubeFeedCalcInputs {
  formulaFatGPerL: number;
  volumePerDayMl: number;
  lipasePerKgPerDay: number;
  weightKg: number;
  strengthValue: number;
}

export function computeOralResult(inputs: OralCalcInputs): PertOralResult;
export function computeTubeFeedResult(inputs: TubeFeedCalcInputs): PertTubeFeedResult;
export function getTriggeredAdvisories(
  mode: PertMode,
  state: PertStateData,
  result: PertOralResult | PertTubeFeedResult | null,
  advisoryConfig: Advisory[]
): TriggeredAdvisory[];
```

From src/lib/pert/types.ts (incl. Plan 02 addition):
```typescript
export interface PertOralResult {
  capsulesPerDose: number;
  totalLipase: number;
  lipasePerDose: number;
  estimatedDailyTotal: number;
}
export interface PertTubeFeedResult {
  capsulesPerDay: number;
  totalLipase: number;
  totalFatG: number;
  lipasePerKg: number;
  capsulesPerMonth: number;
}
export interface TriggeredAdvisory {
  id: string;
  message: string;
  severity: AdvisorySeverity;
}
```

From src/lib/pert/config.ts:
```typescript
export const advisories: Advisory[];           // 4 entries (Phase 1)
export const validationMessages: ValidationMessages;  // emptyOral, emptyTubeFeed
export function getFormulaById(id: string): Formula | undefined;
```

<!-- Phase 1 +page.svelte placeholders (current state) -->
Two placeholder blocks need replacement at:
- Line 69-72: Desktop sticky aside placeholder text
- Line 84-89: Mobile InputDrawer placeholder text
- Line 23-30: recapItems derivation (currently only weight)

Phase 1 PertCalculator.svelte (current placeholder body — to be REPLACED):
- Lines 25-51: outer `<div class="space-y-6">` + single `<HeroResult>` with empty-state snippet.
- The `$effect` persist hook at lines 19-22 STAYS verbatim per D-07.
</interfaces>

**STOP-red card EXACT classes (per UI-SPEC §STOP-red detail + D-04 + D-20):**
```
class="flex items-start gap-3 rounded-xl border-2 border-[var(--color-error)] bg-[var(--color-surface-card)] px-4 py-3"
role="alert" aria-live="assertive"
```
Inner `<OctagonAlert size={20} class="mt-0.5 shrink-0 text-[var(--color-error)]" aria-hidden="true" />`.
Inner message `<p class="text-ui font-bold text-[var(--color-error)]">{advisory.message}</p>`.

**Neutral warning card EXACT classes (per UI-SPEC + matches feeds analog):**
```
class="flex items-start gap-3 rounded-xl border border-[var(--color-border)] bg-[var(--color-surface-alt)] px-4 py-3"
role="note"
```
Inner `<AlertTriangle size={20} class="mt-0.5 shrink-0 text-[var(--color-text-secondary)]" aria-hidden="true" />`.
Inner message `<p class="text-ui font-semibold text-[var(--color-text-primary)]">{advisory.message}</p>`.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace PertCalculator.svelte body — derived results + hero + secondaries + advisories</name>
  <files>src/lib/pert/PertCalculator.svelte</files>
  <read_first>
    <file>src/lib/pert/PertCalculator.svelte</file>
    <file>src/lib/pert/calculations.ts</file>
    <file>src/lib/pert/state.svelte.ts</file>
    <file>src/lib/pert/config.ts</file>
    <file>src/lib/pert/types.ts</file>
    <file>src/lib/feeds/FeedAdvanceCalculator.svelte</file>
    <file>src/lib/gir/GirCalculator.svelte</file>
    <file>src/lib/shared/components/HeroResult.svelte</file>
    <file>.planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-UI-SPEC.md</file>
  </read_first>
  <action>
**Replace** the body of `src/lib/pert/PertCalculator.svelte` (currently 51 lines —
Phase-1 placeholder). Keep the file location, the outer `<div class="space-y-6">`
wrapper, and the persist `$effect` hook (D-07). Everything else inside the wrapper
is rewritten.

**Concrete final file shape:**

```svelte
<script lang="ts">
  import { pertState } from './state.svelte.js';
  import {
    advisories,
    validationMessages,
    getFormulaById
  } from './config.js';
  import {
    computeOralResult,
    computeTubeFeedResult,
    getTriggeredAdvisories
  } from './calculations.js';
  import HeroResult from '$lib/shared/components/HeroResult.svelte';
  import { OctagonAlert, AlertTriangle } from '@lucide/svelte';

  // ----- Empty-state gate per D-08 -----
  // Required-input set per mode. ANY null/≤0 hides secondaries + advisories
  // and shows the empty-state hero.
  let isOralValid = $derived.by(() => {
    const s = pertState.current;
    if (s.mode !== 'oral') return false;
    if (s.weightKg === null || s.weightKg <= 0) return false;
    if (s.medicationId === null) return false;
    if (s.strengthValue === null || s.strengthValue <= 0) return false;
    if (s.oral.fatGrams === null || s.oral.fatGrams <= 0) return false;
    if (s.oral.lipasePerKgPerMeal === null || s.oral.lipasePerKgPerMeal <= 0) return false;
    return true;
  });

  let isTubeFeedValid = $derived.by(() => {
    const s = pertState.current;
    if (s.mode !== 'tube-feed') return false;
    if (s.weightKg === null || s.weightKg <= 0) return false;
    if (s.medicationId === null) return false;
    if (s.strengthValue === null || s.strengthValue <= 0) return false;
    if (s.tubeFeed.formulaId === null) return false;
    if (s.tubeFeed.volumePerDayMl === null || s.tubeFeed.volumePerDayMl <= 0) return false;
    if (s.tubeFeed.lipasePerKgPerDay === null || s.tubeFeed.lipasePerKgPerDay <= 0) return false;
    // Formula must resolve to a known entry (defensive — stale localStorage edge).
    if (getFormulaById(s.tubeFeed.formulaId) === undefined) return false;
    return true;
  });

  // ----- Result derivation -----
  let oralResult = $derived.by(() => {
    if (!isOralValid) return null;
    const s = pertState.current;
    return computeOralResult({
      fatGrams: s.oral.fatGrams!,
      lipasePerKgPerMeal: s.oral.lipasePerKgPerMeal!,
      strengthValue: s.strengthValue!
    });
  });

  let tubeFeedResult = $derived.by(() => {
    if (!isTubeFeedValid) return null;
    const s = pertState.current;
    const formula = getFormulaById(s.tubeFeed.formulaId!)!;
    return computeTubeFeedResult({
      formulaFatGPerL: formula.fatGPerL,
      volumePerDayMl: s.tubeFeed.volumePerDayMl!,
      lipasePerKgPerDay: s.tubeFeed.lipasePerKgPerDay!,
      weightKg: s.weightKg!,
      strengthValue: s.strengthValue!
    });
  });

  // ----- Advisories (severity-DESC, empty when result is null per Plan 02) -----
  let triggeredAdvisories = $derived(
    getTriggeredAdvisories(
      pertState.current.mode,
      pertState.current,
      pertState.current.mode === 'oral' ? oralResult : tubeFeedResult,
      advisories
    )
  );

  let stopAdvisories = $derived(triggeredAdvisories.filter((a) => a.severity === 'stop'));
  let warningAdvisories = $derived(triggeredAdvisories.filter((a) => a.severity === 'warning'));

  // ----- Hero values -----
  let heroValue = $derived.by(() => {
    if (pertState.current.mode === 'oral' && oralResult) {
      return oralResult.capsulesPerDose;
    }
    if (pertState.current.mode === 'tube-feed' && tubeFeedResult) {
      return tubeFeedResult.capsulesPerDay;
    }
    return null;
  });

  let heroUnit = $derived(
    pertState.current.mode === 'tube-feed' ? 'capsules/day' : 'capsules/dose'
  );

  let modeQualifier = $derived(
    pertState.current.mode === 'tube-feed' ? 'Tube-feed dose' : 'Oral dose'
  );

  let emptyMessage = $derived(
    pertState.current.mode === 'tube-feed'
      ? validationMessages.emptyTubeFeed
      : validationMessages.emptyOral
  );

  // pulseKey: empty-${mode} when no result; ${mode}-${capsules} when computed.
  // The transition to a new key fires HeroResult's 200ms pulse on first valid
  // result + each subsequent change (Q-04 — fire on first valid is desired UX).
  let pulseKey = $derived.by(() => {
    if (heroValue === null) return `empty-${pertState.current.mode}`;
    return `${pertState.current.mode}-${heroValue}`;
  });

  // Persist (D-07 — kept verbatim from Phase 1 placeholder).
  $effect(() => {
    JSON.stringify(pertState.current);
    pertState.persist();
  });
</script>

<div class="space-y-6">
  <!-- HERO: empty state OR computed capsules numeral. The escape-hatch <children> snippet
       preserves the Phase 1 promoted PERT eyebrow + mode qualifier shape and adds
       the text-display numeral row when valid. -->
  <HeroResult
    eyebrow="PERT"
    value={heroValue !== null ? String(heroValue) : ''}
    {pulseKey}
    ariaLabel="PERT capsule dose"
    numericValue={heroValue}
  >
    {#snippet children()}
      <div class="flex flex-col gap-2">
        <div class="flex items-baseline gap-3">
          <span class="text-title font-black tracking-tight text-[var(--color-identity)] uppercase">
            PERT
          </span>
          <span class="text-2xs font-semibold tracking-wide text-[var(--color-text-secondary)] uppercase">
            {modeQualifier}
          </span>
        </div>
        {#if heroValue !== null}
          <div class="flex items-baseline gap-2">
            <span class="num text-display font-black text-[var(--color-text-primary)]">
              {heroValue}
            </span>
            <span class="text-ui font-medium text-[var(--color-text-secondary)]">
              {heroUnit}
            </span>
          </div>
        {:else}
          <p class="text-ui text-[var(--color-text-secondary)]">{emptyMessage}</p>
        {/if}
      </div>
    {/snippet}
  </HeroResult>

  <!-- SECONDARY OUTPUTS — visible only when a valid result exists (D-08 hides on empty). -->
  {#if pertState.current.mode === 'oral' && oralResult}
    <section class="card">
      <div class="flex flex-col divide-y divide-[var(--color-border)]">
        <!-- Total lipase needed -->
        <div class="flex items-baseline justify-between px-5 py-4">
          <div>
            <div class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase">
              Total lipase needed
            </div>
            <div class="flex items-baseline gap-2">
              <span class="num text-title font-bold text-[var(--color-text-primary)]">
                {oralResult.totalLipase.toLocaleString('en-US')}
              </span>
              <span class="text-ui text-[var(--color-text-secondary)]">units/dose</span>
            </div>
          </div>
        </div>

        <!-- Lipase per dose -->
        <div class="flex items-baseline justify-between px-5 py-4">
          <div>
            <div class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase">
              Lipase per dose
            </div>
            <div class="flex items-baseline gap-2">
              <span class="num text-title font-bold text-[var(--color-text-primary)]">
                {oralResult.lipasePerDose.toLocaleString('en-US')}
              </span>
              <span class="text-ui text-[var(--color-text-secondary)]">units/dose</span>
            </div>
          </div>
        </div>

        <!-- Tertiary: Estimated daily total (3 meals/day) — D-09 verbatim -->
        <div class="flex items-baseline justify-between px-5 py-4">
          <div>
            <div class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase">
              Estimated daily total (3 meals/day)
            </div>
            <div class="flex items-baseline gap-2">
              <span class="num text-base font-medium text-[var(--color-text-primary)]">
                {oralResult.estimatedDailyTotal}
              </span>
              <span class="text-ui text-[var(--color-text-secondary)]">capsules</span>
            </div>
          </div>
        </div>
      </div>
    </section>
  {:else if pertState.current.mode === 'tube-feed' && tubeFeedResult}
    <section class="card">
      <div class="flex flex-col divide-y divide-[var(--color-border)]">
        <!-- Total fat -->
        <div class="flex items-baseline justify-between px-5 py-4">
          <div>
            <div class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase">
              Total fat
            </div>
            <div class="flex items-baseline gap-2">
              <span class="num text-title font-bold text-[var(--color-text-primary)]">
                {tubeFeedResult.totalFatG.toFixed(1)}
              </span>
              <span class="text-ui text-[var(--color-text-secondary)]">g/day</span>
            </div>
          </div>
        </div>

        <!-- Total lipase needed -->
        <div class="flex items-baseline justify-between px-5 py-4">
          <div>
            <div class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase">
              Total lipase needed
            </div>
            <div class="flex items-baseline gap-2">
              <span class="num text-title font-bold text-[var(--color-text-primary)]">
                {tubeFeedResult.totalLipase.toLocaleString('en-US')}
              </span>
              <span class="text-ui text-[var(--color-text-secondary)]">units/day</span>
            </div>
          </div>
        </div>

        <!-- Lipase per kg -->
        <div class="flex items-baseline justify-between px-5 py-4">
          <div>
            <div class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase">
              Lipase per kg
            </div>
            <div class="flex items-baseline gap-2">
              <span class="num text-title font-bold text-[var(--color-text-primary)]">
                {Math.round(tubeFeedResult.lipasePerKg).toLocaleString('en-US')}
              </span>
              <span class="text-ui text-[var(--color-text-secondary)]">units/kg/day</span>
            </div>
          </div>
        </div>

        <!-- Capsules per month -->
        <div class="flex items-baseline justify-between px-5 py-4">
          <div>
            <div class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase">
              Capsules per month
            </div>
            <div class="flex items-baseline gap-2">
              <span class="num text-title font-bold text-[var(--color-text-primary)]">
                {tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}
              </span>
              <span class="text-ui text-[var(--color-text-secondary)]">capsules</span>
            </div>
          </div>
        </div>
      </div>
    </section>
  {/if}

  <!-- STOP-RED ADVISORIES — D-04 + D-20 + UI-SPEC §STOP-red. role="alert" + aria-live="assertive"
       interrupts the polite hero region for clinical urgency. Only the max-lipase-cap
       advisory has severity 'stop' in Phase 2 config. -->
  {#each stopAdvisories as advisory (advisory.id)}
    <section
      class="flex items-start gap-3 rounded-xl border-2 border-[var(--color-error)] bg-[var(--color-surface-card)] px-4 py-3"
      role="alert"
      aria-live="assertive"
    >
      <OctagonAlert
        size={20}
        class="mt-0.5 shrink-0 text-[var(--color-error)]"
        aria-hidden="true"
      />
      <p class="text-ui font-bold text-[var(--color-error)]">{advisory.message}</p>
    </section>
  {/each}

  <!-- NEUTRAL WARNING ADVISORIES — matches feeds FeedAdvanceCalculator.svelte:311-322 verbatim. -->
  {#each warningAdvisories as advisory (advisory.id)}
    <div
      class="flex items-start gap-3 rounded-xl border border-[var(--color-border)] bg-[var(--color-surface-alt)] px-4 py-3"
      role="note"
    >
      <AlertTriangle
        size={20}
        class="mt-0.5 shrink-0 text-[var(--color-text-secondary)]"
        aria-hidden="true"
      />
      <p class="text-ui font-semibold text-[var(--color-text-primary)]">{advisory.message}</p>
    </div>
  {/each}
</div>
```

**Constraints:**
- The outer `<div class="space-y-6">` is preserved verbatim from Phase 1 (UI-SPEC
  §Spacing — top-level cadence between hero / secondaries / advisories).
- The `$effect` persist hook is preserved verbatim (D-07 — same shape as Phase 1).
- HeroResult is the SHARED component; do not write a custom hero. The `children`
  snippet is the escape hatch UI-SPEC §Typography mandates.
- The OctagonAlert import is `import { OctagonAlert } from '@lucide/svelte'` — NOT
  `AlertOctagon` (D-20 — verified against `src/lib/gir/GirCalculator.svelte:8`).
- Tertiary line uses `text-2xs` eyebrow + `text-base font-medium` for the numeral
  (D-09 — smaller than secondaries' `text-title font-bold`). NOT italic, NOT a
  separate card.
- Numeric formatting:
  - Total lipase + lipase per dose + capsules per month: `toLocaleString('en-US')`
    (e.g., `12,000`, `30,000`).
  - Total fat (g): `toFixed(1)` (one decimal).
  - Lipase per kg: rounded integer + `toLocaleString('en-US')`.
  - Hero capsules numeral: integer (already rounded by `Math.round` in Plan 02);
    no formatting beyond `String(heroValue)`.
- **DO NOT** introduce identity-purple (`var(--color-identity)`) on advisory cards,
  hero numeral text, or any chrome — Identity-Inside Rule restricts identity to the
  10 surfaces listed in UI-SPEC §Color "reservation list". Identity stays on hero
  background tint (handled by HeroResult), eyebrow text, and SegmentedToggle (which
  is in PertInputs, not here).
- **DO NOT** add `class="italic"`, `<em>`, or em-dashes anywhere.
- **DO NOT** modify `src/lib/pert/state.svelte.ts`, `types.ts`, `config.ts`, or
  `pert-config.json`.
- **DO NOT** add new shared components.
- The `as` casts on `heroValue !== null ? String(heroValue) : ''` for the
  HeroResult `value` prop are intentional: the shared component requires a string
  `value`, and the children-snippet escape hatch holds the actual numeral. We pass
  the string anyway so HeroResult's contract test (line 14: "must contain at least
  one text-display element") doesn't fire — the children snippet provides the
  text-display numeral when `heroValue !== null`.
  </action>
  <verify>
    <automated>cd /home/ghislain/gsd-workspaces/pert/nicu-assistant &amp;&amp; grep -c "import \\{ OctagonAlert" src/lib/pert/PertCalculator.svelte &amp;&amp; grep -c "import \\{ AlertTriangle" src/lib/pert/PertCalculator.svelte &amp;&amp; grep -c "from './calculations" src/lib/pert/PertCalculator.svelte &amp;&amp; grep -c "computeOralResult" src/lib/pert/PertCalculator.svelte &amp;&amp; grep -c "computeTubeFeedResult" src/lib/pert/PertCalculator.svelte &amp;&amp; grep -c "getTriggeredAdvisories" src/lib/pert/PertCalculator.svelte &amp;&amp; grep -c "Estimated daily total (3 meals/day)" src/lib/pert/PertCalculator.svelte &amp;&amp; grep -c "border-2 border-\\[var(--color-error)\\]" src/lib/pert/PertCalculator.svelte &amp;&amp; grep -c 'role="alert"' src/lib/pert/PertCalculator.svelte &amp;&amp; ! grep -q "AlertOctagon" src/lib/pert/PertCalculator.svelte &amp;&amp; ! grep "—" src/lib/pert/PertCalculator.svelte &amp;&amp; ! grep "italic" src/lib/pert/PertCalculator.svelte &amp;&amp; pnpm svelte-check &amp;&amp; pnpm test:run src/lib/pert/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import \\{ OctagonAlert" src/lib/pert/PertCalculator.svelte` returns 1 (D-20).
    - `grep -c "import \\{ AlertTriangle" src/lib/pert/PertCalculator.svelte` returns 1.
    - `grep -c "AlertOctagon" src/lib/pert/PertCalculator.svelte` returns 0 (must NOT use the wrong import name).
    - `grep -c "from './calculations" src/lib/pert/PertCalculator.svelte` returns 1.
    - `grep -c "computeOralResult" src/lib/pert/PertCalculator.svelte` returns ≥ 1.
    - `grep -c "computeTubeFeedResult" src/lib/pert/PertCalculator.svelte` returns ≥ 1.
    - `grep -c "getTriggeredAdvisories" src/lib/pert/PertCalculator.svelte` returns ≥ 1.
    - `grep -c "Estimated daily total (3 meals/day)" src/lib/pert/PertCalculator.svelte` returns 1 (D-09 verbatim).
    - `grep -c "border-2 border-\\[var(--color-error)\\]" src/lib/pert/PertCalculator.svelte` returns 1 (STOP-red card).
    - `grep -c 'role="alert"' src/lib/pert/PertCalculator.svelte` returns 1 (STOP-red).
    - `grep -c 'aria-live="assertive"' src/lib/pert/PertCalculator.svelte` returns 1 (STOP-red).
    - `grep -c 'role="note"' src/lib/pert/PertCalculator.svelte` returns 1 (warning advisory).
    - `grep -c "validationMessages.emptyOral" src/lib/pert/PertCalculator.svelte` returns 1.
    - `grep -c "validationMessages.emptyTubeFeed" src/lib/pert/PertCalculator.svelte` returns 1.
    - `grep "—" src/lib/pert/PertCalculator.svelte` returns no matches (em-dash ban).
    - `grep "italic" src/lib/pert/PertCalculator.svelte` returns no matches (D-09 ban).
    - `grep -c '\$effect(() => {' src/lib/pert/PertCalculator.svelte` returns ≥ 1 (persist hook preserved).
    - `pnpm svelte-check` reports 0 errors / 0 warnings.
    - `pnpm test:run src/lib/pert/` reports 17/17 (Phase-1 baseline preserved — config + state untouched).
    - `pnpm test:run` reports 361/361 (no regression).
  </acceptance_criteria>
  <done>PertCalculator body replaced; hero + secondaries + STOP-red + warnings render; empty-state gate enforced; svelte-check + tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mount PertInputs in +page.svelte (desktop aside + mobile drawer) and extend recapItems</name>
  <files>src/routes/pert/+page.svelte</files>
  <read_first>
    <file>src/routes/pert/+page.svelte</file>
    <file>src/lib/pert/PertInputs.svelte</file>
    <file>src/lib/pert/state.svelte.ts</file>
    <file>src/lib/pert/config.ts</file>
    <file>src/lib/feeds/+page.svelte (analog — same drawer + sticky-aside dual-mount)</file>
    <file>src/lib/shared/components/InputsRecap.svelte (RecapItem type)</file>
  </read_first>
  <action>
Modify `src/routes/pert/+page.svelte` in three discrete spots. Use the Edit tool, NOT
a full rewrite — Phase 1 wired the route shell + identity-pert wrapper + InputsRecap
+ InputDrawer + sticky aside structure, all of which is Phase-1-frozen (CONTEXT
"Phase 2 explicitly does NOT touch ... Everything else (header, identity-pert
wrapper, InputsRecap, sticky aside container) is Phase-1-frozen").

**Edit 1: Add the PertInputs import (after line 7's existing imports).**

Locate the existing imports block (lines 1-9 of +page.svelte). Add ONE new import
line directly after the `import InputsRecap, ...` line:

```typescript
import PertInputs from '$lib/pert/PertInputs.svelte';
```

Order: keep alongside the other `$lib/pert/*` imports (PertCalculator). The result
should look like:

```typescript
import PertCalculator from '$lib/pert/PertCalculator.svelte';
import PertInputs from '$lib/pert/PertInputs.svelte';
import InputDrawer from '$lib/shared/components/InputDrawer.svelte';
```

**Edit 2: Extend `recapItems` derivation with mode-specific entries (lines 23-30).**

Replace the current `recapItems` block:

```typescript
const recapItems = $derived.by<RecapItem[]>(() => [
  {
    label: 'Weight',
    value: pertState.current.weightKg === null ? null : `${pertState.current.weightKg}`,
    unit: 'kg',
    fullRow: true
  }
]);
```

With this expanded version that adds mode-specific items per UI-SPEC §Copywriting
Contract (Mobile InputsRecap label):

```typescript
const recapItems = $derived.by<RecapItem[]>(() => {
  const items: RecapItem[] = [
    {
      label: 'Weight',
      value: pertState.current.weightKg === null ? null : `${pertState.current.weightKg}`,
      unit: 'kg',
      fullRow: true
    }
  ];

  if (pertState.current.mode === 'oral') {
    items.push({
      label: 'Fat',
      value:
        pertState.current.oral.fatGrams === null
          ? null
          : `${pertState.current.oral.fatGrams}`,
      unit: 'g'
    });
  } else {
    const formula =
      pertState.current.tubeFeed.formulaId === null
        ? null
        : getFormulaById(pertState.current.tubeFeed.formulaId);
    items.push({
      label: 'Formula',
      value: formula?.name ?? null,
      fullRow: true
    });
    items.push({
      label: 'Volume',
      value:
        pertState.current.tubeFeed.volumePerDayMl === null
          ? null
          : `${pertState.current.tubeFeed.volumePerDayMl}`,
      unit: 'mL'
    });
  }

  return items;
});
```

This requires importing `getFormulaById` from `$lib/pert/config.js` — add to the
existing imports block (alongside `pertState`):

```typescript
import { getFormulaById } from '$lib/pert/config.js';
```

**Edit 3: Replace the desktop sticky aside placeholder (lines 67-74).**

Current Phase-1 placeholder:
```svelte
<aside class="hidden md:block" aria-label="PERT inputs">
  <div class="sticky top-20">
    <!-- Phase 2 will render <PertInputs /> here. Phase 1 placeholder. -->
    <div class="text-ui text-[var(--color-text-secondary)]">
      PERT inputs — coming in Phase 2.
    </div>
  </div>
</aside>
```

Replace the `<div>` body inside `<div class="sticky top-20">` with `<PertInputs />`:

```svelte
<aside class="hidden md:block" aria-label="PERT inputs">
  <div class="sticky top-20">
    <PertInputs />
  </div>
</aside>
```

**Edit 4: Replace the mobile InputDrawer placeholder (lines 84-89).**

Current Phase-1 placeholder children snippet:
```svelte
{#snippet children()}
  <!-- Phase 2 will render <PertInputs /> here. Phase 1 placeholder. -->
  <div class="text-ui text-[var(--color-text-secondary)]">
    PERT inputs — coming in Phase 2.
  </div>
{/snippet}
```

Replace the body with `<PertInputs />`:

```svelte
{#snippet children()}
  <PertInputs />
{/snippet}
```

**Constraints:**
- **DO NOT** modify the `<svelte:head>`, the `identity-pert` wrapper, the
  `<header>`, the `<InputsRecap />` placement, the route grid (`md:grid-cols-[minmax(0,1fr)_22rem]`),
  the `<InputDrawer>` props (title="PERT inputs", bind:expanded, onClear=reset),
  or the `onMount(setCalculatorContext + pertState.init)`. All Phase-1-frozen.
- **DO NOT** add identity-purple, em-dashes, italics, or new strings to recap items
  beyond what the UI-SPEC §Copywriting Contract enumerates. The labels are
  exactly: `'Weight'`, `'Fat'`, `'Formula'`, `'Volume'` (each is sentence-case
  short — same shape as the existing `'Weight'`).
- The `getFormulaById` import is a Phase-1 export from `config.ts` (read-only;
  this is consumption, not modification).
- The em-dash already in the placeholder text (`PERT inputs — coming in Phase 2.`)
  gets removed when the placeholder text is replaced — no separate audit needed.
  After this edit, `grep "—" src/routes/pert/+page.svelte` should return no matches.
- The mobile drawer mounts `<PertInputs />` AND the desktop aside mounts
  `<PertInputs />` — both instances bind to the SAME `pertState` singleton (the
  Svelte 5 module-scope `$state` rune is shared across all importers — verified
  by Phase-1 state.test.ts). UI-SPEC §IA "Mobile vs. desktop layout" confirms
  this is the intended pattern: ONE component instance per render path, two
  render paths, one shared state.
  </action>
  <verify>
    <automated>cd /home/ghislain/gsd-workspaces/pert/nicu-assistant &amp;&amp; grep -c "import PertInputs" src/routes/pert/+page.svelte &amp;&amp; grep -c "<PertInputs />" src/routes/pert/+page.svelte &amp;&amp; grep -c "import \\{ getFormulaById \\}" src/routes/pert/+page.svelte &amp;&amp; grep -c "label: 'Fat'" src/routes/pert/+page.svelte &amp;&amp; grep -c "label: 'Formula'" src/routes/pert/+page.svelte &amp;&amp; grep -c "label: 'Volume'" src/routes/pert/+page.svelte &amp;&amp; ! grep "—" src/routes/pert/+page.svelte &amp;&amp; ! grep "coming in Phase 2" src/routes/pert/+page.svelte &amp;&amp; pnpm svelte-check &amp;&amp; pnpm test:run &amp;&amp; CI=1 pnpm exec playwright test pert-a11y</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import PertInputs" src/routes/pert/+page.svelte` returns 1.
    - `grep -c "<PertInputs />" src/routes/pert/+page.svelte` returns 2 (one in desktop aside, one in mobile drawer).
    - `grep -c "import \{ getFormulaById \}" src/routes/pert/+page.svelte` returns 1.
    - `grep -c "label: 'Fat'" src/routes/pert/+page.svelte` returns 1.
    - `grep -c "label: 'Formula'" src/routes/pert/+page.svelte` returns 1.
    - `grep -c "label: 'Volume'" src/routes/pert/+page.svelte` returns 1.
    - `grep "—" src/routes/pert/+page.svelte` returns no matches (em-dash ban; placeholder em-dash gone).
    - `grep "coming in Phase 2" src/routes/pert/+page.svelte` returns no matches (placeholder text removed).
    - `pnpm svelte-check` reports 0 errors / 0 warnings.
    - `pnpm test:run` reports 361/361 (no regression).
    - `CI=1 pnpm exec playwright test pert-a11y` reports 4/4 pass (Phase 1 axe sweep contract preserved).
  </acceptance_criteria>
  <done>+page.svelte mounts PertInputs in both render paths; recapItems extends with mode-specific items; em-dash placeholder gone; svelte-check + tests + axe sweeps green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pertState.current.* → render layer | State (potentially restored from localStorage) drives the empty-state gate, hero numeral, secondary outputs, and advisory triggers. Defensive empty-state gate hides results entirely if any required input is null/≤0 or a formula id is stale. |
| getTriggeredAdvisories → STOP-red render | The `aria-live="assertive"` STOP-red region interrupts the polite hero region. Render-layer branch only fires when calc-layer math + 10,000 cap predicate (Plan 02) flag the violation. |
| pertState.current.tubeFeed.formulaId (string) → getFormulaById lookup | Stale formulaId from older localStorage may not resolve. Empty-state gate's defensive `getFormulaById(...) === undefined` check (Task 1) prevents NaN math + missing-formula crashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information disclosure | hero numeral mid-edit | accept | Hero is read-only; aria-live polite ensures screen-reader users hear updates as they type. |
| T-02-12 | Repudiation / clinical safety | STOP-red advisory hidden when math is wrong | mitigate | Calc-layer 10,000 cap predicate (Plan 02 D-03) is independent of render-layer; getTriggeredAdvisories returns the advisory whenever predicate fires; render filter `severity === 'stop'` always picks it up. Phase 3 component test (PERT-TEST-03) will assert this firing. |
| T-02-13 | Tampering / clinical safety | Stale `formulaId` in localStorage from a renamed formula | mitigate | Task 1 empty-state gate's `getFormulaById(s.tubeFeed.formulaId) === undefined` check returns false (no result); hero shows empty-state copy + secondaries hidden. No NaN render. |
| T-02-14 | Tampering | Two `<PertInputs />` instances binding to the same singleton | accept | UI-SPEC §IA "Mobile vs. desktop layout" confirms this is the intended pattern. Phase-1 state.test.ts asserts singleton behavior. Module-scope $state shares across all importers. |
| T-02-15 | Denial of service | Pathological recapItems on stale formulaId | mitigate | recapItems uses optional chaining (`formula?.name ?? null`) — null falls through to RecapItem placeholder ("·"). |
| T-02-16 | Information disclosure | Identity-purple bleeding into chrome (advisory cards, secondary numerals) | mitigate | UI-SPEC §Color reservation list locked; STOP-red and warning advisory cards use neutral surfaces explicitly; secondary numerals use `text-text-primary` (eyebrows carry identity). svelte-check passes verifies the classes compile. |
</threat_model>

<verification>
Run all gates after BOTH tasks complete:

1. `cd /home/ghislain/gsd-workspaces/pert/nicu-assistant && pnpm svelte-check` → 0 errors / 0 warnings.
2. `cd /home/ghislain/gsd-workspaces/pert/nicu-assistant && pnpm test:run` → 361/361 (Phase-1 baseline).
3. `cd /home/ghislain/gsd-workspaces/pert/nicu-assistant && pnpm test:run src/lib/pert/` → 17/17 (Phase-1 config + state still green).
4. `cd /home/ghislain/gsd-workspaces/pert/nicu-assistant && CI=1 pnpm exec playwright test pert-a11y` → 4/4 axe sweeps green (Phase-1 baseline; Phase 2 must not regress).
5. `cd /home/ghislain/gsd-workspaces/pert/nicu-assistant && CI=1 pnpm exec playwright test` → must not regress beyond the pre-existing 1 unrelated `disclaimer-banner.spec.ts:28` baseline flake from Phase 1 verification (105/106 was the Phase 1 baseline).
6. **Manual smoke test (clinical gate, optional but recommended before commit):**
   a. Run `pnpm dev`; open `http://localhost:5173/pert`.
   b. Confirm empty-state hero shows "PERT" + "Oral dose" + the empty message from `validationMessages.emptyOral`.
   c. Toggle to Tube-Feed; mode persists; empty message switches to `emptyTubeFeed`.
   d. Toggle back; enter weight 10, fat 25, lipase 1000, Creon 12000 → hero shows `2 capsules/dose`; tertiary "Estimated daily total (3 meals/day)" shows `6 capsules`.
   e. With weight 10 + fat 25 + lipase 12000 (high) + Creon 3000 → STOP-red card fires below the secondaries (10 × 12000 × 3 / 3000 × 3000 = 360,000 daily lipase, cap is 10 × 10000 = 100,000 — exceeds; STOP-red message appears with bold red text + OctagonAlert icon).
   f. Set weight 100 (out of range 0.3–80) → blur fires inline NumericInput "Outside expected range. Verify." AND the warning advisory card appears below.
   g. Resize to mobile viewport; confirm `<PertInputs />` lives in the InputDrawer (tap recap to open); desktop `<aside>` is hidden.
   h. Reload — state persists from localStorage; mode + weight + fat (or formula+volume) still set.

Smoke (f) is the most important sanity check before the Phase 3 fixture phase locks
the math to xlsx ±1%. Smoke (e) verifies PERT-SAFE-01 STOP-red advisory.
</verification>

<success_criteria>
- [ ] `src/lib/pert/PertCalculator.svelte` body replaced; outer wrapper + persist effect preserved.
- [ ] HeroResult fed by `heroValue` (capsulesPerDose for oral, capsulesPerDay for tube-feed); empty-state hero shows promoted PERT eyebrow + mode qualifier + `validationMessages.empty*` copy.
- [ ] Tertiary "Estimated daily total (3 meals/day)" line renders verbatim in oral mode (D-09).
- [ ] STOP-red advisory card uses `border-2 border-[var(--color-error)] bg-[var(--color-surface-card)]` + `OctagonAlert` + bold red text + `role="alert"` + `aria-live="assertive"` (D-04 + D-20 + UI-SPEC).
- [ ] Neutral warning advisory card matches feeds analog verbatim (border + surface-alt + AlertTriangle + secondary-text).
- [ ] Empty-state gate hides secondaries + advisories entirely when required inputs missing (D-08).
- [ ] severity-DESC ordering: stopAdvisories rendered before warningAdvisories (D-10 — already enforced by Plan 02's sort, but render layer keeps the two `{#each}` blocks in stop-then-warning order for clarity).
- [ ] `src/routes/pert/+page.svelte` imports + mounts `<PertInputs />` in BOTH desktop aside (line ~71) and mobile drawer (line ~85).
- [ ] `recapItems` extended with mode-specific items (Fat oral; Formula + Volume tube-feed); `getFormulaById` imported.
- [ ] `pnpm svelte-check` 0/0; `pnpm test:run` 361/361; `CI=1 pnpm exec playwright test pert-a11y` 4/4.
- [ ] No em-dashes in either modified file; no italics; no `text-xs` / `text-sm` overrides; no `AlertOctagon` (must be `OctagonAlert` per D-20).
- [ ] Commit message: `feat(pert/02-04): wire calculator body with results, advisories, and route placeholders`.
</success_criteria>

<output>
After completion, create
`.planning/workstreams/pert/phases/02-calculator-core-both-modes-safety/02-04-SUMMARY.md`
following the SUMMARY template. Capture: file lines added/replaced, hero-value
derivation, STOP-red render branch, recapItems extension, verification results
(svelte-check, test counts, axe sweeps 4/4), any deviations from plan (none expected
— UI-SPEC + interfaces inline above are the contract).

Phase-level Quality Gate (also run before declaring Phase 2 complete):
1. svelte-check 0/0
2. pnpm test:run 361/361 (Phase 2 ships testable code; Phase 3 owns the new tests)
3. pnpm exec playwright test pert-a11y 4/4 (Phase 1 baseline preserved)
4. pnpm build OK (PWA bundle compiles)
5. Manual smoke test §verification step 6 above (oral + tube-feed + STOP-red firing).
</output>
