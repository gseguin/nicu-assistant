---
phase: 1
plan: 02
title: "Wave 1 — `.identity-pert` OKLCH tokens (light + dark) with pre-PR axe sweep gate"
wave: 1
depends_on: [01-01]
files_modified:
  - src/app.css
  - tests/playwright/extended-axe.spec.ts
requirements:
  - PERT-HUE-01
  - PERT-HUE-02
  - PERT-HUE-03
autonomous: true
---

<objective>
Add the `.identity-pert` OKLCH token pair (light + dark) to `src/app.css` matching GIR/Feeds tuning constants at hue=285, then add the pre-PR axe-core sweep for `/pert` light + dark to the extended axe Playwright suite. The sweep must pass on first run (research-before-PR contract per v1.8 decision and PERT-HUE-03). If the sweep fails, retune L/C constants and re-run before this plan is considered complete.

This plan is independent of Plan 01-03 (config) and 01-04 (state+shell) — they touch different files. But it depends on Plan 01-01 (Wave 0) because the registry entry needs `'identity-pert'` in its union before the CSS class is referenced.
</objective>

<must_haves>
- `src/app.css` contains `.identity-pert` (light) and `.dark .identity-pert, [data-theme='dark'] .identity-pert` (dark) blocks, each defining `--color-identity` and `--color-identity-hero`.
- Both light and dark `--color-identity` clear WCAG 4.5:1 contrast against the route surface backgrounds (white-ish in light, dark-slate in dark) when verified by axe-core.
- The extended axe Playwright suite covers `/pert` in both light and dark themes, on first run, no `disableRules` escape hatches.
- `pnpm svelte-check` reports 0 errors / 0 warnings.
</must_haves>

<canonical_refs>
- `.planning/workstreams/pert/phases/01-architecture-identity-hue-clinical-data/01-CONTEXT.md` D-01..D-04
- `src/app.css` lines 240–283 (existing `.identity-*` blocks, GIR/Feeds tuning reference)
- `tests/playwright/extended-axe.spec.ts` (current axe suite covering 33/33 sweeps)
- DESIGN.md "Identity-Inside Rule" section
</canonical_refs>

<tasks>

<task id="01-02-01">
<title>Append `.identity-pert` light + dark blocks to src/app.css</title>
<read_first>
- src/app.css (read lines 240–283 to confirm the existing `.identity-*` block pattern)
- .planning/workstreams/pert/phases/01-architecture-identity-hue-clinical-data/01-CONTEXT.md (D-01, D-02)
- DESIGN.md
</read_first>
<action>
At the end of the layer block that contains the existing `.identity-*` declarations (immediately after the closing brace of the `.dark .identity-uac, [data-theme='dark'] .identity-uac { ... }` block — currently line 282 closes that block, line 283 closes the layer), insert the following CSS, mirroring the GIR/Feeds tuning shape with hue=285:

```css
  .identity-pert {
    --color-identity: oklch(42% 0.12 285);
    --color-identity-hero: oklch(96% 0.03 285);
  }
  .dark .identity-pert,
  [data-theme='dark'] .identity-pert {
    --color-identity: oklch(80% 0.10 285);
    --color-identity-hero: oklch(22% 0.045 285);
  }
```

Insert these two blocks BEFORE the closing `}` of the surrounding `@layer` (or `@theme`/`:root` parent — read the file structure to confirm the exact insertion point). Do not modify any of the existing `.identity-*` blocks.
</action>
<acceptance_criteria>
- `grep -F ".identity-pert" src/app.css` returns at least 3 matches (light selector + dark selector pair).
- `grep -F "oklch(42% 0.12 285)" src/app.css` returns one match.
- `grep -F "oklch(80% 0.10 285)" src/app.css` returns one match.
- `grep -F "oklch(96% 0.03 285)" src/app.css` returns one match.
- `grep -F "oklch(22% 0.045 285)" src/app.css` returns one match.
- `pnpm build` succeeds (Tailwind v4 parses the new tokens).
</acceptance_criteria>
</task>

<task id="01-02-02">
<title>Add `/pert` light + dark axe sweeps to the extended axe Playwright suite</title>
<read_first>
- tests/playwright/extended-axe.spec.ts (full file — locate the existing 33 sweep entries to mirror exactly; if path differs, find with `find tests -name "*axe*.spec.ts"`)
- .planning/workstreams/pert/phases/01-architecture-identity-hue-clinical-data/01-CONTEXT.md (D-04)
</read_first>
<action>
Locate the extended axe Playwright spec (search: `find tests -name "*axe*.spec.ts" -o -name "*a11y*.spec.ts" 2>/dev/null`).

In that spec, find the existing UAC/UVC light + dark sweep pair (search for `'/uac-uvc'` inside the file). Mirror that pair exactly to add two new sweeps for `/pert`:

1. **`/pert` light theme** — same `test('axe sweep — /pert (light theme)', ...)` shape as the existing `/uac-uvc` light test.
2. **`/pert` dark theme** — same shape as the existing `/uac-uvc` dark test.

Concretely (adjust to match the existing file's exact API — read the existing UAC test first):
- Both tests must use `page.goto('/pert')` (the route exists as a placeholder shell after Plan 01-04 lands; this plan runs after 01-04 in the same wave or as part of Wave 3 verification — see verification block below).
- Both tests must run `AxeBuilder().analyze()` (or the project's wrapper) with NO `disableRules`.
- The dark-theme test must set the theme via the same mechanism the existing UAC dark test uses (e.g. `page.evaluate(() => document.documentElement.classList.add('dark'))` or whatever the established pattern is — mirror exactly).
- Both tests must assert `expect(violations).toHaveLength(0)`.

If the existing pattern is data-driven (a `routes` array with light/dark variants), append `/pert` to that array — do NOT add a hand-written test pair if a data-driven pattern exists.

Run `pnpm exec playwright test extended-axe` (or the equivalent for the located spec) and confirm both new `/pert` sweeps pass on first run. **If they fail with contrast violations on `--color-identity`:**
- Open `src/app.css`, retune the L/C constants (try light `L=40%`, dark `L=82%`, or adjust C to `0.10` light / `0.08` dark) and re-run.
- Document the final tuning in this plan's commit message.
- Do NOT add `disableRules` — research-before-PR contract forbids it.
</action>
<acceptance_criteria>
- `grep -F "/pert" tests/playwright/extended-axe.spec.ts` (or located file) returns at least 2 matches (light + dark) OR `/pert` is included in a route-list used by parameterized tests.
- `grep -F "disableRules" tests/playwright/extended-axe.spec.ts` returns 0 NEW occurrences (any pre-existing `disableRules` for other routes are unrelated and stay).
- `pnpm exec playwright test extended-axe` (or located spec name) runs and the `/pert` light + `/pert` dark sweeps PASS.
- The total green sweep count is ≥ 35 (was 33; added 2 for `/pert`).
</acceptance_criteria>
</task>

</tasks>

<verification>
After all tasks complete, run from repo root:

```bash
pnpm svelte-check                                              # MUST report 0/0
pnpm build                                                     # MUST succeed
pnpm exec playwright test extended-axe --reporter=line         # MUST be all green; /pert light + /pert dark MUST pass
```

Manual check: open the dev server, navigate to `/pert` (route is a placeholder after Plan 01-04 — verification of this plan can be deferred to the Wave-3 gate if 01-02 lands before 01-04), inspect the result hero / focus rings / eyebrows / active nav indicator and confirm visually that they pick up the purple/violet hue. The hue should be visibly distinct from Morphine blue (220), Formula teal (195), GIR green (145), Feeds terracotta (30), and UAC rose (350) at a glance.

If the axe sweep fails on first run, retune L/C constants in `src/app.css` (do not weaken with `disableRules`) and re-run until both sweeps are green. Document final tuning in the commit message.
</verification>

<commit_message>
feat(pert/01-02): identity-pert OKLCH tokens + axe sweep gate

Adds .identity-pert light + dark CSS variable pairs at OKLCH hue 285
(purple/violet), matching GIR/Feeds tuning constants:
- Light: L=42% C=0.12 (identity), L=96% C=0.03 (hero tint)
- Dark:  L=80% C=0.10 (identity), L=22% C=0.045 (hero tint)

Adds /pert light + /pert dark axe sweeps to the extended Playwright
axe suite. Both pass on first run (research-before-PR contract per
v1.8 decision; no disableRules escape hatches). Extended axe suite
goes 33/33 → 35/35 green.

Requirements: PERT-HUE-01, PERT-HUE-02, PERT-HUE-03
</commit_message>
