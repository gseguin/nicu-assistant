---
phase: 44-kendamil-formula-family
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - src/lib/fortification/fortification-config.test.ts
autonomous: true
requirements:
  - KEND-TEST-02
user_setup: []

must_haves:
  truths:
    - "The formula-count assertion in fortification-config.test.ts reflects the new Kendamil-extended total of 33 (30 xlsx + 3 HCP)"
    - "A test asserts exactly 3 entries with manufacturer === 'Kendamil'"
    - "A test asserts each Kendamil id resolves via getFormulaById"
    - "A test asserts no Kendamil variant sets packetsSupported (D-12 default-false via omission)"
    - "A test asserts the Kendamil group sorts between Abbott and Mead Johnson under localeCompare"
  artifacts:
    - path: "src/lib/fortification/fortification-config.test.ts"
      provides: "Updated count assertion (30 → 33) + new 'Kendamil grouping (KEND-04 / KEND-TEST-02)' describe block"
      contains: "describe('Kendamil grouping (KEND-04 / KEND-TEST-02)'"
  key_links:
    - from: "src/lib/fortification/fortification-config.test.ts"
      to: "src/lib/fortification/fortification-config.json"
      via: "getFortificationFormulas() reads the JSON; the new describe block filters by manufacturer === 'Kendamil'"
      pattern: "manufacturer === 'Kendamil'"
---

<objective>
Bump the existing formula-count assertion at `fortification-config.test.ts:17` from 30 → 33 (with an updated xlsx-rationale comment noting the 3 Kendamil entries are HCP-sourced, per D-11), and append a new `describe('Kendamil grouping (KEND-04 / KEND-TEST-02)')` block with five assertions that verify the Kendamil group's count, name-order, id-resolvability, packets-default-false, and alphabetical position between Abbott and Mead Johnson.

Purpose: KEND-TEST-02 — the SelectPicker grouping test. Since the SelectPicker auto-groups by `manufacturer` via `localeCompare` at the data-shape level, asserting the data shape (here, in `fortification-config.test.ts`) is the equivalent test surface per D-10. The grouping behavior is enforced by the data, not by component code.

Output: Updated count assertion + new describe block at the bottom of the existing test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-kendamil-formula-family/44-CONTEXT.md
@.planning/phases/44-kendamil-formula-family/44-RESEARCH.md
@.planning/phases/44-kendamil-formula-family/44-PATTERNS.md
@.planning/phases/44-kendamil-formula-family/44-01-SUMMARY.md
@src/lib/fortification/fortification-config.test.ts
@src/lib/fortification/fortification-config.ts

<interfaces>
<!-- The current test file shape and the loader exports the new describe block consumes. -->

From src/lib/fortification/fortification-config.ts (after Plan 44-01 lands):
```typescript
export function getFortificationFormulas(): FortificationFormula[];
export function getFormulaById(id: string): FortificationFormula | undefined;
export function formulaSupportsPackets(id: string): boolean;
```

Existing imports in the test file (lines 1-2):
```typescript
import { describe, it, expect } from 'vitest';
import { getFortificationFormulas, getFormulaById } from './fortification-config.js';
```

Existing count assertion BEFORE this plan (lines 13-18 — to be replaced):
```typescript
describe('fortification-config loader', () => {
  const formulas = getFortificationFormulas();

  it('contains exactly 30 formulas (xlsx Calculator A3:D35 row count)', () => {
    expect(formulas).toHaveLength(30);
  });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump count assertion 30 → 33 with updated xlsx-rationale comment (D-11)</name>
  <files>src/lib/fortification/fortification-config.test.ts</files>
  <read_first>
    - src/lib/fortification/fortification-config.test.ts (entire 80-line file — confirm line 17 is the count assertion and no later test in the file separately asserts a count of 30)
    - src/lib/fortification/fortification-config.json (confirm `formulas.length` is now 33 after Plan 44-01)
    - .planning/phases/44-kendamil-formula-family/44-PATTERNS.md (§"fortification-config.test.ts" — verbatim BEFORE/AFTER excerpts)
    - .planning/phases/44-kendamil-formula-family/44-CONTEXT.md (D-11 — the count + comment must mention the HCP-sourced divergence so a future contributor doesn't try to reconcile against xlsx)
    - .planning/phases/44-kendamil-formula-family/44-01-SUMMARY.md (Plan 44-01's output — confirms 3 Kendamil entries are in place before this assertion runs)
  </read_first>
  <action>
    Open `src/lib/fortification/fortification-config.test.ts`. Find lines 13-18 which read EXACTLY:

    ```typescript
    describe('fortification-config loader', () => {
      const formulas = getFortificationFormulas();

      it('contains exactly 30 formulas (xlsx Calculator A3:D35 row count)', () => {
        expect(formulas).toHaveLength(30);
      });
    ```

    Replace with EXACTLY this verbatim text (preserves the existing `describe(...)` opener and `formulas` constant; replaces the `it(...)` block with the new title and adds the multi-line xlsx-rationale comment per D-11):

    ```typescript
    describe('fortification-config loader', () => {
      const formulas = getFortificationFormulas();

      it('contains exactly 33 formulas (30 from xlsx Calculator A3:D35 + 3 Kendamil HCP)', () => {
        // 30 entries transcribe recipe-calculator.xlsx Calculator tab A3:D35.
        // 3 Kendamil entries (Organic, Classic, Goat) extend beyond xlsx —
        // sourced from hcp.kendamil.com per Phase 44 PLAN.md audit trail.
        expect(formulas).toHaveLength(33);
      });
    ```

    Critical implementation rules:
    1. The `describe(...)` line and `const formulas = getFortificationFormulas();` line and the closing brace structure of the surrounding describe block MUST stay exactly as they are. Only the `it(...)` block changes.
    2. The new `it(...)` title is exactly `'contains exactly 33 formulas (30 from xlsx Calculator A3:D35 + 3 Kendamil HCP)'` — with the parenthetical breakdown matching D-11's wording (xlsx 30 + Kendamil HCP 3 = 33).
    3. The 3-line xlsx-rationale comment MUST appear above `expect(formulas).toHaveLength(33);` and uses the en-dash `—` (U+2014) on the second line, matching the existing project comment style. The comment lines must be indented with 4 spaces relative to the `describe(` opener (matching the surrounding `it(...)` body indentation).
    4. Do NOT touch the per-formula REQUIRED_KEYS loop at lines 20-31, the type assertions at lines 33-44, the inputs assertions at lines 46-58, or the existing `getFormulaById` describe block at lines 60-80. Only the count assertion changes in Task 1; the new Kendamil describe block is added in Task 2 below.

    After editing: `pnpm exec vitest run src/lib/fortification/fortification-config.test.ts -t "contains exactly 33 formulas"` exits 0.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/fortification/fortification-config.test.ts -t "contains exactly 33 formulas"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "expect(formulas).toHaveLength(33)" src/lib/fortification/fortification-config.test.ts` matches exactly once
    - `grep "expect(formulas).toHaveLength(30)" src/lib/fortification/fortification-config.test.ts` matches `0` lines (the old assertion is gone)
    - `grep "contains exactly 33 formulas (30 from xlsx Calculator A3:D35 + 3 Kendamil HCP)" src/lib/fortification/fortification-config.test.ts` matches exactly once
    - `grep "3 Kendamil entries (Organic, Classic, Goat) extend beyond xlsx" src/lib/fortification/fortification-config.test.ts` matches exactly once
    - `grep "sourced from hcp.kendamil.com per Phase 44 PLAN.md audit trail" src/lib/fortification/fortification-config.test.ts` matches exactly once
    - `pnpm exec vitest run src/lib/fortification/fortification-config.test.ts -t "contains exactly 33 formulas"` exits 0
  </acceptance_criteria>
  <done>Count assertion bumped to 33; xlsx-rationale comment updated to note Kendamil entries are HCP-sourced; the targeted vitest run passes.</done>
</task>

<task type="auto">
  <name>Task 2: Append 'Kendamil grouping (KEND-04 / KEND-TEST-02)' describe block (KEND-TEST-02)</name>
  <files>src/lib/fortification/fortification-config.test.ts</files>
  <read_first>
    - src/lib/fortification/fortification-config.test.ts (entire file — confirm the existing last describe block is `describe('getFormulaById', ...)` ending at line 80, and the file ends with a trailing newline — the new block appends at the bottom)
    - src/lib/fortification/fortification-config.json (confirm three Kendamil entries exist after Plan 44-01)
    - .planning/phases/44-kendamil-formula-family/44-PATTERNS.md (§"fortification-config.test.ts" — verbatim "Recommended new Kendamil grouping describe block" — copy-paste source)
    - .planning/phases/44-kendamil-formula-family/44-RESEARCH.md (§"E. REQUIRED_KEYS shape loop — extend with KEND-TEST-02 grouping assertions" — verbatim 5-assertion block)
    - .planning/phases/44-kendamil-formula-family/44-CONTEXT.md (D-10, D-12 — grouping test lives at config-shape level, not in component test; packets-default-false via omission)
  </read_first>
  <action>
    Append the following describe block VERBATIM at the end of `src/lib/fortification/fortification-config.test.ts`, after the existing closing `});` of the `describe('getFormulaById', ...)` block at line 80. The new block is a sibling of the existing describes, NOT nested inside `getFormulaById`:

    ```typescript

    describe('Kendamil grouping (KEND-04 / KEND-TEST-02)', () => {
      it('exposes exactly 3 Kendamil entries', () => {
        const kendamils = getFortificationFormulas().filter((f) => f.manufacturer === 'Kendamil');
        expect(kendamils).toHaveLength(3);
      });

      it('Kendamil entries appear in alphabetical order by name (Classic, Goat, Organic) after the picker sort', () => {
        const names = getFortificationFormulas()
          .filter((f) => f.manufacturer === 'Kendamil')
          .slice()
          .sort((a, b) => a.name.localeCompare(b.name))
          .map((f) => f.name);
        expect(names).toEqual(['Kendamil Classic', 'Kendamil Goat', 'Kendamil Organic']);
      });

      it('each Kendamil variant resolvable by id (KEND-01/02/03)', () => {
        expect(getFormulaById('kendamil-organic')?.name).toBe('Kendamil Organic');
        expect(getFormulaById('kendamil-classic')?.name).toBe('Kendamil Classic');
        expect(getFormulaById('kendamil-goat')?.name).toBe('Kendamil Goat');
      });

      it('no Kendamil variant supports packets — default-false via omitted field (KEND-05)', () => {
        for (const f of getFortificationFormulas().filter((x) => x.manufacturer === 'Kendamil')) {
          expect(f.packetsSupported).toBeUndefined();
        }
      });

      it('Kendamil group sorts between Abbott and Mead Johnson (alphabetical localeCompare)', () => {
        const manufacturers = Array.from(
          new Set(
            getFortificationFormulas()
              .slice()
              .sort((a, b) => a.manufacturer.localeCompare(b.manufacturer))
              .map((f) => f.manufacturer)
          )
        );
        const idxAbbott = manufacturers.indexOf('Abbott');
        const idxKendamil = manufacturers.indexOf('Kendamil');
        const idxMeadJohnson = manufacturers.indexOf('Mead Johnson');
        expect(idxAbbott).toBeGreaterThanOrEqual(0);
        expect(idxKendamil).toBe(idxAbbott + 1);
        expect(idxMeadJohnson).toBe(idxKendamil + 1);
      });
    });
    ```

    Critical implementation rules:
    1. Append after the closing `});` of `describe('getFormulaById', ...)`. Leave one blank line between the previous closing brace and the new `describe(...)` opener.
    2. The describe title MUST be exactly `'Kendamil grouping (KEND-04 / KEND-TEST-02)'` — the parenthetical traceability is mandatory so the verifier can grep for the requirement IDs.
    3. All five `it(...)` blocks must appear in the order shown: count → name-order → id-resolvability → packets-undefined → group-position. The group-position test asserts the alphabetical-localeCompare ordering Abbott → Kendamil → Mead Johnson with no manufacturers between Abbott and Kendamil and no manufacturers between Kendamil and Mead Johnson — this is the verifiable shape of D-07 (`Kendamil` lands cleanly between `Abbott` and `Mead Johnson` because `A < K < M` in lexicographic order, with no intervening manufacturer in the existing 30 entries).
    4. The packets-undefined assertion uses `expect(f.packetsSupported).toBeUndefined()` (NOT `.toBe(false)`) — per D-12, the field is OMITTED, so it reads as `undefined`, not `false`. This makes the test catch any future regression where someone adds `packetsSupported: false` explicitly (which would fail the assertion as it should — pattern divergence is the regression).
    5. No imports need to be added — `getFortificationFormulas` and `getFormulaById` are already imported at the top of the file (line 2).

    After editing: `pnpm exec vitest run src/lib/fortification/fortification-config.test.ts` exits 0 with all describes green (loader / shape loop / inputs / getFormulaById / Kendamil grouping).
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/fortification/fortification-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('Kendamil grouping (KEND-04 / KEND-TEST-02)'" src/lib/fortification/fortification-config.test.ts` returns `1`
    - `grep "exposes exactly 3 Kendamil entries" src/lib/fortification/fortification-config.test.ts` matches exactly once
    - `grep "Kendamil entries appear in alphabetical order by name (Classic, Goat, Organic) after the picker sort" src/lib/fortification/fortification-config.test.ts` matches exactly once
    - `grep "each Kendamil variant resolvable by id (KEND-01/02/03)" src/lib/fortification/fortification-config.test.ts` matches exactly once
    - `grep "no Kendamil variant supports packets" src/lib/fortification/fortification-config.test.ts` matches exactly once
    - `grep "Kendamil group sorts between Abbott and Mead Johnson" src/lib/fortification/fortification-config.test.ts` matches exactly once
    - `grep -c "expect(f.packetsSupported).toBeUndefined()" src/lib/fortification/fortification-config.test.ts` returns `1`
    - `grep "expect(idxKendamil).toBe(idxAbbott + 1)" src/lib/fortification/fortification-config.test.ts` matches exactly once
    - `grep "expect(idxMeadJohnson).toBe(idxKendamil + 1)" src/lib/fortification/fortification-config.test.ts` matches exactly once
    - `pnpm exec vitest run src/lib/fortification/fortification-config.test.ts` exits 0 with the new 5-test describe block reported as green
  </acceptance_criteria>
  <done>New 'Kendamil grouping (KEND-04 / KEND-TEST-02)' describe block contains the 5 specified assertions, asserts packetsSupported is `undefined` (not `false`), asserts the Abbott→Kendamil→Mead Johnson alphabetical adjacency, and the entire fortification-config.test.ts file passes vitest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → loader | Tests consume `getFortificationFormulas()` / `getFormulaById()` exports; no untrusted input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | Tampering (regression — pattern divergence) | fortification-config.json future Kendamil edits | mitigate | The "no Kendamil variant supports packets" test asserts `f.packetsSupported` is `toBeUndefined()` (not `toBe(false)`); a future contributor adding `packetsSupported: false` explicitly fails the assertion, catching pattern divergence. |
| T-44-05 | Tampering (transcription drift) | fortification-config.json count | mitigate | The bumped 33-formula assertion fails loudly on any accidental add/remove; the rationale comment names xlsx 30 + Kendamil 3 so future readers understand the breakdown. |
</threat_model>

<verification>
After both tasks complete:
- `pnpm exec vitest run src/lib/fortification/fortification-config.test.ts` exits 0 with all describes green
- `pnpm exec svelte-check --threshold error` exits 0
</verification>

<success_criteria>
- Count assertion is 33 with the xlsx + Kendamil HCP rationale comment
- New 'Kendamil grouping (KEND-04 / KEND-TEST-02)' describe block exists with 5 assertions
- All 5 assertions pass (count = 3, name-order matches Classic/Goat/Organic, ids resolve, packetsSupported is undefined, manufacturer adjacency Abbott → Kendamil → Mead Johnson)
- KEND-TEST-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/44-kendamil-formula-family/44-02-SUMMARY.md` documenting:
- Count assertion bumped 30 → 33 with updated xlsx-rationale comment
- 5-assertion Kendamil grouping describe block appended
- All fortification-config.test.ts describes green
- KEND-TEST-02 satisfied; KEND-04 and KEND-05 covered by id-resolution + packetsSupported-undefined assertions
</output>
