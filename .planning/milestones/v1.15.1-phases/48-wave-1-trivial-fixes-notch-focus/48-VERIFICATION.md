---
phase: 48-wave-1-trivial-fixes-notch-focus
verified: 2026-04-27T11:35:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 48: Wave-1 — Trivial Fixes (NOTCH + FOCUS) Verification Report

**Phase Goal:** On iPhone 14 Pro+ in standalone PWA mode, the title bar chrome (hamburger + wordmark + theme/info buttons) sits below the Dynamic Island / camera notch in both orientations; opening the input drawer never summons the iOS soft keyboard before a clinician chooses a field.

**Verified:** 2026-04-27T11:35:00Z
**Status:** passed
**Re-verification:** No (initial verification)

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth                                                                                                                                                                                                                                                                                                                                                          | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| SC-1 | iPhone 14 Pro+ standalone PWA portrait: hamburger + wordmark + theme/info sit fully below the Dynamic Island; `bg-[var(--color-surface)]` paints opaquely into the inset region                                                                                                                                                                                | VERIFIED (CI proxy)  | `NavShell.svelte:80` carries `pt-[env(safe-area-inset-top,0px)]`; existing `bg-[var(--color-surface)]` on line 79 paints into the new padded region by CSS box-model default (D-04 + LC-01); T-13 source-grep regression guard active in `NavShell.test.ts:256-266`. Real-iPhone visual confirmation correctly deferred to Phase 50 SMOKE-02 per CONTEXT.md D-19 + ROADMAP Phase 50 SC-1 (Playwright cannot render the Dynamic Island; PITFALLS.md P-19) |
| SC-2 | Landscape rotation respects `safe-area-inset-left/right`; browser-tab fallback preserves the existing `1rem` gutter                                                                                                                                                                                                                                            | VERIFIED (CI proxy)  | `NavShell.svelte:81` carries `px-[max(env(safe-area-inset-left,0px),1rem)]` replacing the previous `px-4`; `max()` floor of `1rem` matches design-floor 16px when env() returns 0 (browser-tab mode); T-13 covers both new tokens. Real-iPhone landscape visual confirmation correctly deferred to Phase 50 SMOKE-08 per CONTEXT.md D-19 + ROADMAP Phase 50 SC-7                                                                                          |
| SC-3 | Drawer-open: iOS soft keyboard does NOT appear; focus lands on close button (NOT input/select/textarea/[role=slider]); VoiceOver announces "Close inputs, button"                                                                                                                                                                                              | VERIFIED   | `InputDrawer.svelte:46-64` shows `queueMicrotask` block deleted in full; close button at line 112 carries `autofocus` attribute; `closeBtn?.focus()` at line 59 in `$effect` after `dialog.showModal()` runs deterministically (D-09 hybrid resolution). FOCUS-TEST-01 (T-07) passes in vitest (jsdom-fallback shim asserts non-input activeElement); FOCUS-TEST-03 12/12 Playwright cases pass live-browser, asserting `expect(close).toBeFocused()` + `tagName === 'BUTTON'` |
| SC-4 | Drawer-open-no-keyboard behavior holds on every one of the 6 calculators (Morphine, Formula, GIR, Feeds, UAC/UVC, PERT) — single source of truth in InputDrawer.svelte, no per-calculator divergence                                                                                                                                                          | VERIFIED   | FOCUS-TEST-03 spec at `e2e/drawer-no-autofocus.spec.ts:14-21` declares the explicit 6-element ROUTES list (PERT not omitted per Pitfall 3); `for (const path of ROUTES)` iterates each; runs under both `chromium` + `webkit-iphone` projects = 12 cases; all 12 pass. Code source-of-truth: `InputDrawer.svelte` is the only autofocus site (single shared component rendered through `+layout.svelte` for all 6 routes; verified — no per-route InputDrawer mutations)        |
| SC-5 | Existing 16/16 axe sweeps remain green; component + source-grep tests guard against the deleted `queueMicrotask` block reappearing OR the `pt-[env(safe-area-inset-top` Tailwind class being accidentally removed                                                                                                                                              | VERIFIED   | T-08 source-grep at `InputDrawer.test.ts:116-122` asserts `not.toContain('queueMicrotask')` + `not.toContain('[role="slider"]')` + positive `toContain('autofocus')`; T-13 source-grep at `NavShell.test.ts:256-266` asserts both new NOTCH tokens + canonical sticky-header tokens preserved. Full chromium Playwright suite (which includes axe sweeps) passes 128/128, 3 skipped. svelte-check 0 errors, 1 expected `a11y_autofocus` advisory warning per D-09  |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                                                | Expected                                                                                          | Status     | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/lib/shell/NavShell.svelte`                         | Header carrying `pt-[env(safe-area-inset-top,0px)]` + `px-[max(env(safe-area-inset-left,0px),1rem)]`; `px-4` token removed from header; `bg-[var(--color-surface)]` retained | VERIFIED   | Line 80: `pt-[env(safe-area-inset-top,0px)]` (1 occurrence); line 81: `px-[max(env(safe-area-inset-left,0px),1rem)]` (1 occurrence); existing `bg-[var(--color-surface)]` at line 79 unchanged; old `px-4` token removed from header (only remaining `px-4` at line 113 is on a desktop tab anchor, NOT the header). NO `max()` floor on `pt-` (D-02/P-10 honored). NO new wrapper element. NO `--header-h` custom property                                                                            |
| `src/lib/shell/NavShell.test.ts`                        | T-13 NOTCH-TEST-01 appended in third describe block ('NavShell — desktop full-nav divergence (Phase 45)') | VERIFIED   | Lines 256-266 contain T-13 with literal-token assertions for both new NOTCH tokens + `classAttrContainsAll` defense for canonical sticky-header tokens. T-13 reuses the existing `navShellSource` constant (line 22-25) and `classAttrContainsAll` helper (line 29-37) — no re-declaration. Total tests passing in this file: 19 (12 named T-01..T-12 + 6 unnamed structural + new T-13)                                                                                                       |
| `src/lib/shared/components/InputDrawer.svelte`          | `queueMicrotask` block + stale comment deleted; close button carries `autofocus`; closeBtn imperative focus added (D-09 hybrid resolution) | VERIFIED   | `queueMicrotask` literal absent; `[role="slider"]` literal absent; `firstInput` references absent. Close button at line 112 has `autofocus` attribute; new `let closeBtn = $state<HTMLButtonElement | null>(null)` at line 42; `bind:this={closeBtn}` at line 110; `closeBtn?.focus()` at line 59 inside `$effect` after `dialog.showModal()`. Stale lines-47-50 comment replaced with explanatory comment about Svelte 5 autofocus IDL behavior + hybrid resolution rationale (D-09 deviation override) |
| `src/lib/shared/components/InputDrawer.test.ts`         | T-07 (FOCUS-TEST-01 activeElement assertion with jsdom fallback) + T-08 (FOCUS-TEST-02 source-grep) appended | VERIFIED   | Lines 96-114 contain T-07 with `if (document.activeElement !== close) close.focus()` jsdom fallback before activeElement assertions; lines 116-122 contain T-08 source-grep `not.toContain('queueMicrotask')` + `not.toContain('[role="slider"]')` + positive `toContain('autofocus')`. `void InputDrawer;` retained as last statement before describe close (line 126). Total tests in file: 8 (T-01..T-08), all passing                                                                              |
| `e2e/drawer-no-autofocus.spec.ts`                       | New cross-calculator Playwright spec with explicit 6-element ROUTES list; runs under both projects = 12 cases | VERIFIED   | File exists; ROUTES at lines 14-21 lists all 6 calculator routes including `/pert` (Pitfall 3 honored); module-scope `test.use({ viewport: { width: 375, height: 667 } })` at line 28 overrides webkit-iphone descriptor for narrow-viewport drawer surface; `for (const path of ROUTES)` loop creates 6 tests; assertion uses `expect(close).toBeFocused()` + defense-in-depth `tagName === 'BUTTON'` + role !== 'slider'; NO `testMatch` filter, NO `test.skip`. Verified 12/12 passing (6×2)        |

### Key Link Verification

| From                                                       | To                                                                | Via                                                                                                       | Status     | Details                                                                                                                                                                                                                                                                                                              |
| ---------------------------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `NavShell.svelte` `<header>` className (line 76-82)        | platform-reported `safe-area-inset-*` values                       | Tailwind arbitrary-value bracket syntax `pt-[env(...)]` + `px-[max(env(...),1rem)]`                       | WIRED      | Both tokens present on the same `<header>` element; mirrors existing `pb-[env(safe-area-inset-bottom,0px)]` pattern at line 152 (mobile bottom nav) — same shape, same fallback default; verified via T-13 source-grep + visual inspection of compiled markup                                                       |
| `NavShell.svelte:152` (pre-existing bottom-nav inset pattern) | `NavShell.svelte:80-81` (NEW symmetric top-inset pattern)        | Identical Tailwind arbitrary-value bracket shape with `0px` fallback default                              | WIRED      | Pattern symmetry verified by inspection: bottom uses `pb-[env(safe-area-inset-bottom,0px)]`; top uses `pt-[env(safe-area-inset-top,0px)]`. Same atomic bracket-token shape. Single design vocabulary preserved (CONTEXT.md D-03)                                                                                  |
| `NavShell.test.ts` T-13 source-grep                        | `NavShell.svelte` source                                           | `fs.readFileSync` via existing `navShellSource` constant + `expect(navShellSource).toContain(...)`        | WIRED      | Test passes deterministically; 13 named tests (T-01..T-13) + 6 unnamed structural in NavShell.test.ts; full vitest run reports 454/454 passing across 43 files                                                                                                                                                       |
| `InputDrawer.svelte` close button (line 109-123)           | browser native `<dialog>.showModal()` autofocus resolution         | HTML `autofocus` attribute (declarative source-of-truth) + `closeBtn?.focus()` imperative call (jsdom + Svelte 5 IDL-only autofocus override) | WIRED      | Hybrid wiring per D-09 + executor deviation: declarative `autofocus` retained as documentation marker AND source-grep contract for T-08; imperative `closeBtn?.focus()` in `$effect` after `dialog.showModal()` ensures actual focus resolution in real browsers (Svelte 5 compiles `autofocus` to IDL-property only via `node_modules/svelte/.../autofocus.js:10-21` which is a no-op after showModal). Functionally equivalent to D-09's pure declarative intent (close button receives focus, no OSK summoned) |
| `e2e/drawer-no-autofocus.spec.ts`                          | `InputDrawer` rendered inside each calculator route                | `getByRole('button', { name: /tap to edit inputs/i }).click()` then `expect(close).toBeFocused()`         | WIRED      | 6 routes × 2 projects = 12 tests; all pass. Trigger label `/tap to edit inputs/i` is the shared `InputsRecap` accessible name across all 6 calculators (verified live by 12/12 pass). Close-button match `/Close /i` matches all 6 calculator titles                                                                |
| `InputDrawer.test.ts` T-08 source-grep                     | `InputDrawer.svelte` source                                       | `fs.readFileSync` + `not.toContain` assertions (regression guard)                                         | WIRED      | Test passes; verified via vitest run + manual grep (`queueMicrotask` 0 occurrences, `[role="slider"]` 0 occurrences, `autofocus` 1+ occurrence)                                                                                                                                                                      |

### Data-Flow Trace (Level 4)

| Artifact                                | Data Variable                                            | Source                                                                                                  | Produces Real Data | Status                                                                                                                                                                                                                                                                                              |
| --------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `NavShell.svelte` `<header>` padding     | `env(safe-area-inset-top)` + `env(safe-area-inset-left)` | Browser CSS engine reads platform-reported safe-area-inset values (iOS standalone PWA, Safari, Chrome) | Yes (platform-driven) | FLOWING — values are platform-injected at runtime by CSS engine; not application data. iOS standalone PWA reports ~47-59px top inset on iPhone 14 Pro+; Safari browser-tab reports 0 (URL bar consumes inset, per LC-02). Cannot be tested in CI; deferred to Phase 50 SMOKE-02/08 by design       |
| `InputDrawer.svelte` close button focus  | `closeBtn` (HTMLButtonElement state ref)                 | `bind:this={closeBtn}` (line 110) → `closeBtn?.focus()` (line 59) inside `$effect` after `showModal()`  | Yes                | FLOWING — verified by 12/12 Playwright cases asserting `expect(close).toBeFocused()` and `tagName === 'BUTTON'`. The state ref is non-null after `<dialog>` open + Svelte mount; `closeBtn?.focus()` actually moves focus in WebKit + Chromium                                                       |

### Behavioral Spot-Checks

| Behavior                                                                              | Command                                                                            | Result                                              | Status |
| ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | --------------------------------------------------- | ------ |
| svelte-check is clean (0 errors)                                                      | `pnpm svelte-check`                                                                | 4589 files, 0 errors, 1 expected `a11y_autofocus` warning at InputDrawer.svelte:112 | PASS   |
| Full vitest suite green (≥ 439 baseline + new T-07/T-08/T-13 = 454 expected)          | `pnpm test:run`                                                                    | 454 passed across 43 test files                     | PASS   |
| FOCUS-TEST-03 cross-calc Playwright runs 12 cases (6 routes × 2 projects)             | `pnpm exec playwright test drawer-no-autofocus`                                    | 6/6 chromium passing + 6/6 webkit-iphone passing = 12/12 | PASS   |
| Existing chromium suite remains green (≥ 99 baseline + 12 new = 128 expected, 3 skipped) | `pnpm exec playwright test --project=chromium`                                     | 128 passed, 3 skipped, 0 failed                     | PASS   |
| `queueMicrotask` literal absent from `InputDrawer.svelte` (FOCUS-01 source contract)  | `grep -n 'queueMicrotask' src/lib/shared/components/InputDrawer.svelte`            | 0 lines                                             | PASS   |
| `[role="slider"]` literal absent from `InputDrawer.svelte` (FOCUS-TEST-02 negative)   | `grep -n '\[role="slider"\]' src/lib/shared/components/InputDrawer.svelte`         | 0 lines                                             | PASS   |
| `pt-[env(safe-area-inset-top,0px)]` present once on `NavShell.svelte` (NOTCH-01)      | `grep -c 'pt-\[env(safe-area-inset-top,0px)\]' src/lib/shell/NavShell.svelte`      | 1                                                   | PASS   |
| `px-[max(env(safe-area-inset-left,0px),1rem)]` present once on `NavShell.svelte` (NOTCH-02) | `grep -c 'px-\[max(env(safe-area-inset-left,0px),1rem)\]' src/lib/shell/NavShell.svelte` | 1                                                   | PASS   |
| NO `max()` floor on `pt-` (D-02 / P-10 — must NOT match `max(env(safe-area-inset-top`)  | `grep -c 'max(env(safe-area-inset-top' src/lib/shell/NavShell.svelte`              | 0                                                   | PASS   |
| All 6 calculator routes still use `sticky top-20` (NOTCH-04 / D-05 audit)             | `grep -rn 'top-20' src/routes/`                                                    | 6 routes (feeds, formula, gir, morphine-wean, pert, uac-uvc) | PASS   |

### Requirements Coverage

| Requirement   | Source Plan      | Description                                                                                          | Status     | Evidence                                                                                                                              |
| ------------- | ---------------- | ---------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| NOTCH-01      | 48-02 (NOTCH)    | `<header>` carries `pt-[env(safe-area-inset-top,0px)]`                                              | SATISFIED  | `NavShell.svelte:80`; T-13 regression guard; flipped to Complete in REQUIREMENTS.md:106                                                |
| NOTCH-02      | 48-02 (NOTCH)    | `<header>` carries `px-[max(env(safe-area-inset-left,0px),1rem)]` (replaces `px-4`)                  | SATISFIED  | `NavShell.svelte:81`; replaces previous `px-4`; T-13 regression guard; REQUIREMENTS.md:107                                            |
| NOTCH-03      | 48-02 (NOTCH)    | `bg-[var(--color-surface)]` paints into the inset region                                            | SATISFIED  | Existing `bg-[var(--color-surface)]` on `NavShell.svelte:79` extends into new `pt-` padded region by CSS box-model default (D-04); REQUIREMENTS.md:108 |
| NOTCH-04      | 48-02 (NOTCH)    | Sticky-top consumers audited                                                                         | SATISFIED  | All 6 routes still use `sticky top-20` unchanged; D-05 inspection audit (56px header + 0px desktop inset = 56px clearance under 80px); REQUIREMENTS.md:109 |
| NOTCH-TEST-01 | 48-02 (NOTCH)    | Source-grep regression guard for `pt-[env(safe-area-inset-top` token                                | SATISFIED  | T-13 at `NavShell.test.ts:256-266`; passing in vitest; REQUIREMENTS.md:110                                                              |
| FOCUS-01      | 48-01 (FOCUS)    | Delete `queueMicrotask(() => firstInput?.focus())` block in full                                    | SATISFIED  | `InputDrawer.svelte` `queueMicrotask` literal absent (verified by grep + T-08); REQUIREMENTS.md:111                                  |
| FOCUS-02      | 48-01 (FOCUS)    | Add `autofocus` to close button so iOS soft keyboard does NOT appear                                | SATISFIED  | `InputDrawer.svelte:112` `autofocus` attribute present; FOCUS-TEST-03 12/12 cases verify focus is on close button (BUTTON, not INPUT/SELECT/TEXTAREA/[role=slider]); REQUIREMENTS.md:112 |
| FOCUS-03      | 48-01 (FOCUS)    | Single source of truth across 6 calculators (no per-calculator divergence)                          | SATISFIED  | `InputDrawer.svelte` is the only autofocus site; FOCUS-TEST-03 verifies same behavior on all 6 routes; REQUIREMENTS.md:113             |
| FOCUS-TEST-01 | 48-01 (FOCUS)    | Component test: after `<dialog>.showModal()`, `document.activeElement` is NOT input/select/textarea/[role="slider"] | SATISFIED  | T-07 at `InputDrawer.test.ts:96-114` with deterministic jsdom fallback; passing in vitest; REQUIREMENTS.md:114                       |
| FOCUS-TEST-02 | 48-01 (FOCUS)    | Source-grep test: source contains neither `queueMicrotask` nor `[role="slider"]`                    | SATISFIED  | T-08 at `InputDrawer.test.ts:116-122` with both negative assertions + positive `toContain('autofocus')`; passing in vitest; REQUIREMENTS.md:115 |
| FOCUS-TEST-03 | 48-01 (FOCUS)    | Cross-calculator Playwright: opens drawer on each of 6 routes, asserts close button focused         | SATISFIED  | `e2e/drawer-no-autofocus.spec.ts` lists all 6 routes explicitly; runs under both projects = 12 cases; all 12 passing live; REQUIREMENTS.md:116 |

**Coverage:** 11/11 requirements satisfied. No orphaned requirements (REQUIREMENTS.md traceability table maps NOTCH-01..04 + NOTCH-TEST-01 + FOCUS-01..03 + FOCUS-TEST-01..03 to Phase 48; all 11 IDs flipped to Complete).

### Anti-Patterns Found

| File                                          | Line  | Pattern                                                       | Severity | Impact                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------------------------- | ----- | ------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/lib/shared/components/InputDrawer.svelte` | 112   | Bare `autofocus` attribute on close button (svelte-check `a11y_autofocus` advisory warning, also `[vite-plugin-svelte] Avoid using autofocus`) | INFO     | This is a deliberate design contract per CONTEXT.md D-09 + UI-SPEC LC-03 + the 4-line in-source comment at lines 48-58 explaining the rationale. The bare attribute is required as a (a) source-grep contract for T-08 / FOCUS-TEST-02, (b) documentation marker for the focus-on-close-button intent, and (c) accessible-name vehicle for VoiceOver's "Close inputs, button" announcement. Suppressed-by-intent, not by code. |

No blocker or warning anti-patterns found. The single advisory warning is the deliberate marker that the D-09 contract is honored.

### Decision Honoring (D-01..D-21)

All 21 decisions traceable to shipped code or process. Notable:

- **D-01..D-07 (NOTCH):** All present in `NavShell.svelte` + `NavShell.test.ts` exactly as specified. Single edit on existing `<header>` (D-01); bare `env()` form on `pt-` (D-02 + P-10); Tailwind bracket syntax mirrors line 152 pattern (D-03); existing `bg-[var(--color-surface)]` retained (D-04); no per-route `top-20` edits (D-05 audit confirmed); NO `--header-h` custom property added (D-06 — `grep -c -- '--header-h'` returns 0); T-13 lives at `NavShell.test.ts` (D-07).
- **D-08 (FOCUS):** `queueMicrotask` block deleted in full + comment lines 47-50 deleted (D-15) — verified absent.
- **D-09 (FOCUS) — HYBRID DEVIATION DOCUMENTED:** Pure declarative `autofocus` directive does not work in Svelte 5 (`node_modules/svelte/.../autofocus.js:10-21` sets only IDL property and gates on `document.activeElement === body`, both no-ops after `dialog.showModal()`). Executor added `bind:this={closeBtn}` + imperative `closeBtn?.focus()` inside the `$effect` after `showModal()`. The declarative `autofocus` attribute is RETAINED on the close button (line 112) as the source-grep contract, documentation marker, and intent marker. Functionally equivalent to D-09's pure declarative intent (close button receives focus, no OSK summoned). The 1 svelte-check `a11y_autofocus` advisory warning is the deliberate marker that the contract is honored. Documented in 48-01-SUMMARY.md "Auto-fixed Issues" §1 + in-source comment at InputDrawer.svelte:48-58. **No override needed in this VERIFICATION because the implementation honors the contract — only the mechanism changed.**
- **D-10 (FOCUS):** Single source of truth — only `InputDrawer.svelte` carries `autofocus` for drawer close button; verified via FOCUS-TEST-03 12/12 cases.
- **D-11..D-15 (FOCUS):** All present (D-11 declarative attribute retained; D-12 T-07 with locked assertions; D-13 T-08 source-grep; D-14 cross-calc Playwright at `e2e/drawer-no-autofocus.spec.ts`; D-15 stale comment cleanup of original lines 47-50).
- **D-16..D-18 (build order):** Two sibling plans landed (48-01 FOCUS first per D-17, 48-02 NOTCH second). Worktree-based execution rather than single-branch sequential commits — see 48-01-SUMMARY.md "Worktree-mode discovery" — but resolved cleanly via merge to main (commits db88fee + 265bd59). All 8 plan commits + 2 merge commits + req-flip commit (857dcc0) on main HEAD. D-18 intent (atomic commits) honored.
- **D-19..D-21 (verification):** Phase 48 success criteria are CI-verifiable; real-iPhone visual confirmation correctly deferred to Phase 50 SMOKE-02/08; existing 16/16 axe sweeps re-run within the 128/128 chromium suite (which includes `*-a11y.spec.ts` for all 6 calculators in light + dark); existing chromium suite remains green; new `webkit-iphone` smoke spec from Phase 47 remains green.

### Pitfall Avoidance (5 in-phase pitfalls from RESEARCH.md)

| Pitfall | Description                                                                              | Status     |
| ------- | ---------------------------------------------------------------------------------------- | ---------- |
| P-1     | `NavShell.test.ts` already exists; do NOT create-new                                     | NEUTRALIZED — T-13 appended into existing file (12 named + 6 structural pre-existing tests intact)                |
| P-2     | jsdom does not auto-resolve `autofocus` on `showModal()`                                 | NEUTRALIZED — T-07 includes deterministic `if (document.activeElement !== close) close.focus()` fallback         |
| P-3     | `mobile-nav-clearance.spec.ts` only iterates 5 of 6 routes (omits PERT)                  | NEUTRALIZED — `e2e/drawer-no-autofocus.spec.ts:14-21` lists all 6 routes explicitly including `/pert`            |
| P-4     | Tailwind class-string format may shift on save                                           | NEUTRALIZED — T-13 uses literal-token assertions (`expect(src).toContain('pt-[env(safe-area-inset-top,0px)]')`) which are atomic to prettier-plugin-tailwindcss |
| P-5     | webkit-iphone runs ALL e2e specs by default                                              | NEUTRALIZED — `e2e/drawer-no-autofocus.spec.ts` has NO `testMatch`, NO `test.skip`; runs under both projects automatically per Phase 47 D-15; verified 6×2=12 cases |

### Cross-Plan Integration

NOTCH (`NavShell.svelte`) and FOCUS (`InputDrawer.svelte`) touch different files with no shared state. No interaction risk. Verified by:
- 454/454 vitest passing (no test from one plan breaks the other)
- 128/128 chromium passing (full e2e suite executes both file's contracts simultaneously)
- 12/12 drawer-no-autofocus passing under both Playwright projects after both plans landed on main

### Production-Ready Signals

- **Code:** All artifacts present, substantive, and wired with real data flow (Level 4)
- **Tests:** All new tests pass (3 vitest + 12 Playwright cases); all baselines green; no regressions
- **Type-check:** svelte-check 0 errors, 1 expected/intentional advisory warning
- **Documentation:** REQUIREMENTS.md traceability flipped (11 IDs); SUMMARYs document deviation; in-source comment explains hybrid resolution
- **Deferrals:** Phase 50 SMOKE-02/08 will close the real-iPhone visual confirmation half of SC-1 + SC-2 (per CONTEXT.md D-19 + ROADMAP); 31 pre-existing webkit-iphone failures in non-migrated specs are documented in `deferred-items.md` as Phase 47 follow-ups (NOT Phase 48 regressions per CONTEXT.md D-21)

### Human Verification Required

**None for this phase.** All Phase 48 success criteria are CI-verifiable by design (CONTEXT.md D-19). The visual half of SC-1 (notch-safe portrait) and SC-2 (landscape inset respected) are explicitly deferred to Phase 50 SMOKE-02 / SMOKE-08 per the milestone roadmap — those are roadmap-level verification surfaces, not Phase 48 verification gaps.

The single advisory warning (`a11y_autofocus` on InputDrawer.svelte:112) is documented as intentional in CONTEXT.md D-09 and in the in-source comment at lines 48-58; it does not require human verification.

### Gaps Summary

No gaps. The phase ships exactly what the goal demands within its CI-verifiable scope. The known deviations (D-09 hybrid declarative/imperative resolution + 31 pre-existing webkit-iphone failures in non-migrated specs) are:

1. **D-09 hybrid resolution:** functionally equivalent to the locked decision; close button still receives focus and OSK is still not summoned. The mechanism changed (added imperative `closeBtn?.focus()` in `$effect` because Svelte 5's `autofocus` directive is IDL-only and a no-op after `showModal()`), but the contract is honored and verified by 12/12 Playwright cases. Documented in 48-01-SUMMARY.md + in-source comment.

2. **31 pre-existing webkit-iphone failures:** NOT regressions from Phase 48; these specs (`feeds-a11y.spec.ts`, `gir-a11y.spec.ts`, etc.) were authored against chromium-only assumptions and predate Phase 48. The new `webkit-iphone` project added in Phase 47 D-15 surfaces them. Phase 47 D-15 deferred their migration; Phase 48 inherits the deferral per CONTEXT.md D-21 ("Phase 48 does NOT migrate the existing 6 calculator e2e specs to also run under webkit-iphone"). Logged in `deferred-items.md`. Phase 48's own `drawer-no-autofocus.spec.ts` runs cleanly under both projects.

Both items are documented and accepted by the milestone roadmap. They do not block Phase 48 closure or Phase 49 unblocking.

---

_Verified: 2026-04-27T11:35:00Z_
_Verifier: Claude (gsd-verifier, Opus 4.7 1M context)_
