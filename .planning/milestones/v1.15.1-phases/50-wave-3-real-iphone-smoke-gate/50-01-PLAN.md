---
phase: 50-wave-3-real-iphone-smoke-gate
plan: 01
type: execute
wave: 1
depends_on: [49]
files_modified:
  - .planning/v1.15.1-IPHONE-SMOKE.md
autonomous: false
requirements: [SMOKE-01, SMOKE-02, SMOKE-03, SMOKE-04, SMOKE-05, SMOKE-06, SMOKE-07, SMOKE-08, SMOKE-09, SMOKE-10]
tags: [smoke, ios, pwa, drawer, notch, focus, visual-viewport, milestone-gate, v1.15.1]

must_haves:
  truths:
    - "File `.planning/v1.15.1-IPHONE-SMOKE.md` exists at repo root planning directory (NOT under phase dir per D-02)."
    - "File contains exactly 10 `### SMOKE-` block headers, one per SMOKE-01..SMOKE-10."
    - "Every SMOKE block contains the six fields from D-04: title heading, **Closes:** line, **Setup:** line, **Action:** line, **Expected:** line, and a **Pass:** [ ] / **Fail:** [ ] — Notes line."
    - "Each SMOKE-XX block's **Expected:** text matches REQUIREMENTS.md SMOKE-XX acceptance text and ROADMAP.md Phase 50 success criterion XX verbatim (closure rubric per D-06)."
    - "Each SMOKE-XX block's **Closes:** line names the upstream NOTCH/FOCUS/DRAWER ID(s) it satisfies, matching REQUIREMENTS.md and 50-CONTEXT.md."
    - "SMOKE-10 contains 6 sub-bullets (Morphine, Formula, GIR, Feeds, UAC/UVC, PERT) with independent Pass/Fail boxes per D-12/D-13 — drawer-only behavior across the 6 calculators, NOT a 60-cell sweep."
    - "SMOKE-06 reproduction recipe lists BOTH Option A (Phone app + back) and Option B (Lock + 30s + unlock) per D-19/D-20."
    - "SMOKE-07 includes a 'DEFERRED — no hardware keyboard available' escape-hatch annotation per D-17/D-18 — does NOT block sign-off if 9/10 pass with one annotated DEFERRED route."
    - "SMOKE-09 explicitly routes failure to v1.15.2 (FOUC-script-toggled `<meta name=\"theme-color\">` mitigation per PITFALLS.md P-12) per D-15/D-16."
    - "File header references the v1.13 D-12 deferral and notes Phase 50 closes it (per ROADMAP Phase 50 goal + 50-CONTEXT.md domain text)."
    - "File header includes CI-proxy disclaimer referencing Phase 49 D-24 + PITFALLS.md P-19/P-20: Playwright cannot emulate iOS soft keyboard, Dynamic Island, or bfcache."
    - "File header includes tester instructions: install PWA via Safari → Share → Add to Home Screen (NOT Safari browser tab); ~15 minute session."
    - "File header explicitly states Pass-or-Documented-Defer for ALL 10 entries is the precondition for Phase 51 / REL-04 / milestone close."
    - "File contains a tester sign-off block at the bottom with stub fields for Tester, Date, iPhone Model, iOS Version, Calculator routes tested, and Overall verdict checkbox — empty until tester fills them in (D-11)."
    - "File contains a failure-routing footer paragraph explaining the three D-08 options (hotfix in v1.15.1 / defer to v1.15.2-v1.16 / re-test on different iPhone)."
    - "After Task 2 completes, the same file shows: every Pass/Fail/DEFERRED box checked, Notes filled where applicable, signature block fields populated with tester name + date + iPhone model + iOS version + routes tested + overall verdict."
  artifacts:
    - path: .planning/v1.15.1-IPHONE-SMOKE.md
      provides: "10-step real-iPhone smoke checklist artifact + tester sign-off block"
      contains: "10 `### SMOKE-` blocks; **Closes:** mapping per block; **Setup:**/**Action:**/**Expected:**/**Pass:**/**Fail:** fields per D-04; SMOKE-10 6-calculator sub-bullets; tester sign-off block per D-11; failure-routing footer per D-08"
      min_lines: 200
  key_links:
    - from: .planning/v1.15.1-IPHONE-SMOKE.md
      to: .planning/REQUIREMENTS.md
      via: "Each SMOKE-XX block's Expected: text matches REQUIREMENTS.md SMOKE-XX acceptance text verbatim"
      pattern: "verbatim Expected text from REQUIREMENTS.md SMOKE-01..10 lines 56-65"
    - from: .planning/v1.15.1-IPHONE-SMOKE.md
      to: .planning/ROADMAP.md
      via: "Each SMOKE-XX block aligns with ROADMAP.md Phase 50 success criterion XX (lines 162-172)"
      pattern: "1:1 SMOKE-XX → ROADMAP success-criterion XX correspondence"
    - from: .planning/v1.15.1-IPHONE-SMOKE.md
      to: ".planning/phases/49-wave-2-visualviewport-drawer-anchoring/49-CONTEXT.md (D-24 + D-25)"
      via: "CI-proxy disclaimer in file header explicitly cites Phase 49's CI gap (PITFALLS.md P-19/P-20) and names Phase 50 as primary verification surface"
      pattern: "header section names Phase 49 D-24 + D-25 + PITFALLS.md P-19/P-20"
    - from: .planning/v1.15.1-IPHONE-SMOKE.md
      to: PROJECT.md (v1.13 D-12 deferral text)
      via: "File header notes Phase 50 closes the v1.13 D-12 'real-iPhone smoke ... non-blocking per D-12' deferral by elevating real-iPhone testing to primary verification surface"
      pattern: "header references v1.13 D-12 closure"
---

<objective>
Phase 50 ships ONE artifact: `.planning/v1.15.1-IPHONE-SMOKE.md` — a 10-step blocking smoke checklist that the project owner runs on a real iPhone 14 Pro+ in standalone PWA mode and signs at the bedside. This phase **closes the v1.13 D-12 deferral** by elevating real-iPhone testing from "recommended post-deploy" to **primary verification surface** for v1.15.1.

CI cannot paint the Dynamic Island, emulate the iOS soft keyboard, trigger bfcache, or render `apple-mobile-web-app-status-bar-style: black-translucent`. Phase 49's Playwright spec (`e2e/drawer-visual-viewport.spec.ts`) explicitly admits this gap (PITFALLS.md P-19/P-20). This checklist is the only place those bedside behaviors get verified before milestone close.

This is a NON-CODING phase. No `src/` changes. No tests. No `pnpm` gates. The "build" is the markdown file; the "verification" is the human tester's signature.

**Purpose:** Provide a clinical-IFU-style sign-off artifact that closes v1.13 D-12, gates Phase 51 / REL-04, and lands in the milestone audit trail (`.planning/milestones/v1.15.1-…/`) at archive time.

**Output:** One file at `.planning/v1.15.1-IPHONE-SMOKE.md` — generated with all 10 boxes UNCHECKED in commit 1; edited inline by the tester at the bedside (boxes ticked, Notes filled, sign-off block populated) and committed in commit 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-wave-3-real-iphone-smoke-gate/50-CONTEXT.md
@.planning/phases/49-wave-2-visualviewport-drawer-anchoring/49-CONTEXT.md
@CLAUDE.md
</context>

<source_authority>
**Verbatim text sources for the 10 SMOKE blocks (executor MUST copy from these — no paraphrasing):**

- **SMOKE-01..10 acceptance text:** `.planning/REQUIREMENTS.md` lines 56–65 (the `- [ ] **SMOKE-XX**: …` bullets in §"Real-iPhone Smoke Gate (Wave-3) — closes v1.13 D-12 deferral")
- **SMOKE-01..10 Expected rubric:** `.planning/ROADMAP.md` lines 162–172 (the 10 numbered success-criteria items under Phase 50)
- **Closure mappings (which upstream IDs each SMOKE closes):** Both REQUIREMENTS.md and ROADMAP.md state these inline. The canonical mapping (per CONTEXT.md D-04 + REQUIREMENTS.md):
  - SMOKE-01 → (the artifact itself; "blocking gate" closure — does not close a NOTCH/FOCUS/DRAWER ID)
  - SMOKE-02 → NOTCH-01
  - SMOKE-03 → FOCUS-01 + FOCUS-02
  - SMOKE-04 → DRAWER-06 + DRAWER-07
  - SMOKE-05 → DRAWER-02 (iOS 26 #800125 regression survival)
  - SMOKE-06 → DRAWER-03 (bfcache rebind via `pageshow.persisted` + `visibilitychange`)
  - SMOKE-07 → DRAWER-09 (hardware-keyboard threshold guard)
  - SMOKE-08 → NOTCH-02 (landscape `safe-area-inset-left/right`)
  - SMOKE-09 → PITFALLS.md `black-translucent` legibility gap (no upstream NOTCH/FOCUS/DRAWER ID — verifies the existing `apple-mobile-web-app-status-bar-style: black-translucent` declaration in `src/app.html`)
  - SMOKE-10 → cross-calculator parity for FOCUS-03 + DRAWER-05 (single-source-of-truth contract)
- **v1.13 D-12 deferral text (for the header closure note):** `.planning/PROJECT.md` line 9 — "Real-iPhone smoke test (drawer, keyboard, STOP-red contrast in standalone PWA mode) recommended post-deploy as v1.13.1 hotfix fuel — non-blocking per D-12."
</source_authority>

<tasks>

<task type="auto">
  <name>Task 1: Generate `.planning/v1.15.1-IPHONE-SMOKE.md` from the SMOKE-XX template (all 10 boxes UNCHECKED)</name>
  <files>.planning/v1.15.1-IPHONE-SMOKE.md</files>
  <read_first>
    - /mnt/data/src/nicu-assistant/.planning/REQUIREMENTS.md (lines 54–65 — verbatim SMOKE-01..10 acceptance text + closure mappings — this is the SINGLE source of truth for the Expected text in each block)
    - /mnt/data/src/nicu-assistant/.planning/ROADMAP.md (lines 158–173 — Phase 50 entry + 10 numbered success-criteria items — verbatim Expected text per D-04)
    - /mnt/data/src/nicu-assistant/.planning/phases/50-wave-3-real-iphone-smoke-gate/50-CONTEXT.md (the 27 locked decisions — D-01 through D-27)
    - /mnt/data/src/nicu-assistant/.planning/phases/49-wave-2-visualviewport-drawer-anchoring/49-CONTEXT.md (D-24 + D-25 — for the CI-proxy disclaimer text in the header)
    - /mnt/data/src/nicu-assistant/.planning/PROJECT.md (line 9 — for the v1.13 D-12 deferral text the header cites)
  </read_first>
  <action>
Create `.planning/v1.15.1-IPHONE-SMOKE.md` (note: this path is OUTSIDE the phase directory per CONTEXT.md D-02 — it is a milestone artifact, NOT a phase-internal artifact). Use the Write tool. Do NOT use heredoc.

The file MUST contain the following sections in order. All bracketed values like `{verbatim from REQUIREMENTS.md SMOKE-XX}` are placeholders the executor fills in by reading the indicated source — do NOT leave the curly-brace text in the output.

═══════════════════════════════════════════════════════════════════════════════
SECTION 1 — File header (top of file, before the SMOKE blocks)
═══════════════════════════════════════════════════════════════════════════════

Write a top-level `# v1.15.1 Real-iPhone Smoke Checklist` H1 followed by a metadata block and 5 paragraphs of header content. Required header content (per orchestrator constraint #9 + D-11 + D-15/D-24/D-25/D-26):

1. **Phase + milestone reference:** "Phase 50 — Wave-3 Real-iPhone Smoke Gate, milestone v1.15.1 iOS Polish & Drawer Hardening." Cross-link to `.planning/phases/50-wave-3-real-iphone-smoke-gate/50-CONTEXT.md` and `.planning/ROADMAP.md` Phase 50.

2. **v1.13 D-12 closure paragraph:** State explicitly that this checklist closes the v1.13 D-12 deferral. Quote the deferral text from PROJECT.md line 9 verbatim: "Real-iPhone smoke test (drawer, keyboard, STOP-red contrast in standalone PWA mode) recommended post-deploy as v1.13.1 hotfix fuel — non-blocking per D-12." Note that v1.15.1 elevates this from "recommended post-deploy" to **primary verification surface** for the milestone (per Phase 49 D-25).

3. **CI-proxy disclaimer paragraph:** State that Playwright e2e (chromium + the new `webkit-iphone` project from Phase 47) CANNOT emulate the iOS soft keyboard, Dynamic Island safe-area insets, bfcache restore, or `apple-mobile-web-app-status-bar-style: black-translucent` rendering. Cite `.planning/research/PITFALLS.md` P-19 and P-20, and Phase 49 50-CONTEXT D-24. The DRAWER-TEST-03 spec at `e2e/drawer-visual-viewport.spec.ts` is a wiring/structural CI proxy — this checklist is the primary verification surface for SMOKE-04, SMOKE-05, SMOKE-06, SMOKE-08, and SMOKE-09.

4. **Tester instructions paragraph:** Explicit setup steps for the tester:
   - **Device required:** iPhone 14 Pro or newer (Dynamic Island device), iOS 17+ ideally iOS 18+ (covers the iOS 26 `visualViewport.height` post-dismiss regression — Apple Developer Forums #800125).
   - **Install via Safari, NOT browser-tab testing:** Open the deploy URL in Safari → tap Share → tap "Add to Home Screen". Launch the app from the Home Screen icon (standalone PWA mode). Browser-tab testing does NOT exercise the chrome / safe-area / bfcache code paths and is INVALID for this checklist.
   - **Expected duration:** ~15 minutes of focused bedside-style time per full smoke pass.
   - **Hardware keyboard:** SMOKE-07 requires a paired Bluetooth or Smart Connector keyboard. If unavailable, annotate SMOKE-07 as "DEFERRED — no hardware keyboard available" and route per D-08 option 3 (re-test pending). Does NOT block sign-off.
   - **Recording:** Tick each Pass/Fail/DEFERRED box inline in the markdown file. Fill the Notes field for any failure or deferral. Sign and date the bottom block. Commit the signed state.

5. **Sign-off precondition paragraph:** State explicitly that **Pass-or-Documented-Defer for ALL 10 entries is the precondition for Phase 51 / REL-04 / milestone close** (per REQUIREMENTS.md REL-04 + ROADMAP.md Phase 51 success criterion 5). Phase 51 release gate reads this file directly to verify all 10 boxes are checked Pass (or annotated DEFERRED with a documented route per D-08).

═══════════════════════════════════════════════════════════════════════════════
SECTION 2 — Block schema (used for each of SMOKE-01..10)
═══════════════════════════════════════════════════════════════════════════════

Each of the 10 SMOKE entries MUST follow this exact schema (per CONTEXT.md D-04). Use markdown `###` H3 for the block heading. Order the blocks SMOKE-01 through SMOKE-10 sequentially.

```markdown
### SMOKE-XX — {Title}
**Closes:** {Upstream NOTCH/FOCUS/DRAWER ID(s) — see <source_authority> mapping above}

**Setup:** {Preconditions and starting state — Claude's discretion under D-04, observable + clinical-IFU style}

**Action:** {The user gesture — Claude's discretion under D-04, single sentence imperative}

**Expected:** {Verbatim Expected text from REQUIREMENTS.md SMOKE-XX + ROADMAP.md success criterion XX. This is the rubric per D-06 — binary pass/fail. Do NOT paraphrase.}

**Pass:** [ ] / **Fail:** [ ] — Notes: ____
```

═══════════════════════════════════════════════════════════════════════════════
SECTION 3 — The 10 SMOKE blocks (concrete content per block)
═══════════════════════════════════════════════════════════════════════════════

Below is the concrete content for each block. The executor copies the **Expected** text from the indicated source verbatim; the **Setup** and **Action** wording is the executor's discretion under D-04, kept observable and clinical.

────────────────────────────────────────────────────────────
SMOKE-01 — Smoke checklist artifact created
────────────────────────────────────────────────────────────
**Closes:** SMOKE-01 itself (the artifact's existence; meta-requirement)
**Setup:** N/A — verified by the existence of this file at `.planning/v1.15.1-IPHONE-SMOKE.md` (committed in git).
**Action:** Confirm this file is committed alongside the milestone for clinical audit trail.
**Expected:** (Verbatim from REQUIREMENTS.md SMOKE-01) `.planning/v1.15.1-IPHONE-SMOKE.md` checklist artifact created and used as a blocking gate before milestone close. Tester signs off on each step on a real iPhone 14 Pro+ (or newer) in standalone PWA mode (not Safari browser tab).
**Pass:** [ ] / **Fail:** [ ] — Notes: ____

────────────────────────────────────────────────────────────
SMOKE-02 — Notch-safe chrome (portrait)
────────────────────────────────────────────────────────────
**Closes:** NOTCH-01
**Setup:** Launch NICU Assist from the Home Screen icon in portrait orientation. Land on any calculator route (default `/morphine-wean`).
**Action:** Look at the title bar at the top of the screen.
**Expected:** (Verbatim from ROADMAP.md Phase 50 success criterion 1) Hamburger / wordmark / theme button visible below the Dynamic Island in portrait orientation — closes NOTCH-01.
**Pass:** [ ] / **Fail:** [ ] — Notes: ____

────────────────────────────────────────────────────────────
SMOKE-03 — Drawer open without keyboard
────────────────────────────────────────────────────────────
**Closes:** FOCUS-01 + FOCUS-02
**Setup:** On `/morphine-wean` (or any calculator), with VoiceOver enabled (Settings → Accessibility → VoiceOver → On). Drawer is collapsed.
**Action:** Tap the "Tap to edit inputs" sticky drawer trigger to expand the drawer.
**Expected:** (Verbatim from ROADMAP.md Phase 50 success criterion 2) Drawer opens with no keyboard appearing, focus on close button, VoiceOver announces the drawer — closes FOCUS-01 + FOCUS-02.
**Pass:** [ ] / **Fail:** [ ] — Notes: ____

────────────────────────────────────────────────────────────
SMOKE-04 — Drawer above keyboard
────────────────────────────────────────────────────────────
**Closes:** DRAWER-06 + DRAWER-07
**Setup:** On `/morphine-wean` with the drawer expanded, no keyboard up.
**Action:** Tap the Weight input field.
**Expected:** (Verbatim from ROADMAP.md Phase 50 success criterion 3) Tapping a weight field summons the keyboard AND the drawer is anchored above the keyboard top with ≥ 8 px clearance; first input + eyebrow remain visible — closes DRAWER-06 + DRAWER-07.
**Pass:** [ ] / **Fail:** [ ] — Notes: ____

────────────────────────────────────────────────────────────
SMOKE-05 — Keyboard dismiss returns flush
────────────────────────────────────────────────────────────
**Closes:** DRAWER-02 (iOS 26 #800125 regression survival)
**Setup:** On `/morphine-wean` with the drawer expanded and the iOS soft keyboard summoned (continuing from SMOKE-04).
**Action:** Tap the iOS keyboard's "Done" button (or tap outside any input field) to dismiss the keyboard.
**Expected:** (Verbatim from ROADMAP.md Phase 50 success criterion 4) Dismissing the keyboard (Done button) returns the drawer smoothly to bottom-nav-top with `env(safe-area-inset-bottom)` clearance; no flicker, no stale offset — closes DRAWER-02 (iOS 26 #800125 regression survival).
**Pass:** [ ] / **Fail:** [ ] — Notes: ____

────────────────────────────────────────────────────────────
SMOKE-06 — bfcache restore renders flush
────────────────────────────────────────────────────────────
**Closes:** DRAWER-03
**Setup:** On `/morphine-wean` with the drawer expanded and at least one input populated. Tester picks ONE of the two reproductions below at the bedside (D-19/D-20 — both are equally valid):

- **Option A (Phone-app round-trip — PITFALLS.md P-04 worst case):** Swipe up to the App Switcher → open the Phone app → tap any recent contact's "FaceTime" tile (do NOT actually call) → swipe back to NICU Assist via the App Switcher.
- **Option B (Lock-then-wake — sleep/wake bfcache restore):** Press the side button to lock the device → wait 30 seconds → unlock with Face ID / passcode → return to NICU Assist by tapping the Home Screen icon.

**Action:** Observe the drawer state immediately on return — do NOT tap or scroll first.
**Expected:** (Verbatim from ROADMAP.md Phase 50 success criterion 5) Calling yourself / switching apps and returning renders the drawer flush without requiring a user gesture — closes DRAWER-03.
**Pass:** [ ] / **Fail:** [ ] — Notes (record which option used: A / B): ____

────────────────────────────────────────────────────────────
SMOKE-07 — Hardware keyboard does not lift drawer
────────────────────────────────────────────────────────────
**Closes:** DRAWER-09
**Setup:** Pair a Bluetooth keyboard (or attach Smart Connector keyboard) to the test iPhone. Confirm the keyboard is connected (Settings → Bluetooth → device shows Connected). Open NICU Assist from the Home Screen, navigate to `/morphine-wean`, expand the drawer.

**Hardware-keyboard escape hatch (per D-17/D-18):** If no Bluetooth/Smart Connector keyboard is available at smoke-time, annotate this entry as "DEFERRED — no hardware keyboard available" in the Notes field below and check the **DEFERRED** box. Routes per D-08 option 3 (re-test pending). This does NOT block overall sign-off — 9/10 Pass + 1 DEFERRED-with-route is acceptable for Phase 51 / REL-04.

**Action:** With the hardware keyboard connected, tap the Weight input field on screen.
**Expected:** (Verbatim from ROADMAP.md Phase 50 success criterion 6) Hardware-keyboard-paired iPhone does NOT lift the drawer — closes DRAWER-09.
**Pass:** [ ] / **Fail:** [ ] / **DEFERRED:** [ ] — Notes: ____

────────────────────────────────────────────────────────────
SMOKE-08 — Landscape inset respected
────────────────────────────────────────────────────────────
**Closes:** NOTCH-02
**Setup:** On any calculator route in portrait orientation.
**Action:** Rotate the iPhone to landscape (left or right). Observe the title bar chrome. Then rotate back to portrait and observe again.
**Expected:** (Verbatim from ROADMAP.md Phase 50 success criterion 7) Landscape rotation respects `safe-area-inset-left/right`; portrait re-rotation preserves notch-safe top — closes NOTCH-02.
**Pass:** [ ] / **Fail:** [ ] — Notes: ____

────────────────────────────────────────────────────────────
SMOKE-09 — Light-mode status-bar legibility
────────────────────────────────────────────────────────────
**Closes:** PITFALLS.md `black-translucent` legibility gap (no upstream NOTCH/FOCUS/DRAWER ID — verifies existing `<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">` in `src/app.html`)
**Setup:** Switch the app to light mode (theme toggle in title bar → light). Observe the iOS status bar at the top of the screen — clock, battery indicator, signal bars, Dynamic Island controls.

**Failure routing (per D-15/D-16):** If status-bar text is illegible against `var(--color-surface)` light value, this routes to **v1.15.2 — DEFER per D-08 option 2**. The mitigation is the FOUC-script-toggled `<meta name="theme-color">` (PITFALLS.md P-12) which requires non-trivial FOUC-script work and is out of v1.15.1 hotfix scope. Phase 50 only verifies whether the existing setup is acceptable — does NOT implement P-12. A FAIL here does NOT block v1.15.1 milestone close; it is documented as a v1.15.2 backlog item in PROJECT.md and the milestone ships without it.

**Action:** Read the time / battery percentage / Dynamic Island controls against the light-mode title-bar background.
**Expected:** (Verbatim from ROADMAP.md Phase 50 success criterion 8) Light mode + Dynamic Island shows status-bar text legible against `var(--color-surface)` light value (if poor, mitigation is light/dark `<meta name="theme-color">` toggled by the FOUC script) — closes the PITFALLS.md `black-translucent` legibility gap.
**Pass:** [ ] / **Fail (route to v1.15.2):** [ ] — Notes: ____

────────────────────────────────────────────────────────────
SMOKE-10 — Cross-calculator parity
────────────────────────────────────────────────────────────
**Closes:** Cross-calculator parity for FOCUS-03 + DRAWER-05 (single-source-of-truth contract — drawer-only behavior across the 6 calculators per D-13, NOT a 60-cell sweep)
**Setup:** From the Home Screen icon in standalone PWA mode, in portrait orientation. The same drawer contract verified in SMOKE-03 (drawer-open-no-keyboard) + SMOKE-04 (drawer-above-keyboard) is exercised on each of the six calculator routes.
**Action:** For EACH of the six calculators below, navigate to the route, expand the drawer, then tap an input field. Tick Pass/Fail per route. Per D-12: 6 sub-bullets, one per calculator, drawer-only behavior — independent Pass/Fail boxes.
**Expected:** (Verbatim from ROADMAP.md Phase 50 success criterion 9) All six calculators (Morphine, Formula, GIR, Feeds, UAC/UVC, PERT) smoke-tested for drawer + notch behavior — no per-calculator divergence found, or any divergence captured as a follow-up todo.

  - [ ] **Morphine Wean** (`/morphine-wean`) — drawer-open-no-keyboard + drawer-above-keyboard parity. **Pass:** [ ] / **Fail:** [ ] — Notes: ____
  - [ ] **Formula** (`/formula`) — drawer-open-no-keyboard + drawer-above-keyboard parity. **Pass:** [ ] / **Fail:** [ ] — Notes: ____
  - [ ] **GIR** (`/gir`) — drawer-open-no-keyboard + drawer-above-keyboard parity. **Pass:** [ ] / **Fail:** [ ] — Notes: ____
  - [ ] **Feeds** (`/feeds`) — drawer-open-no-keyboard + drawer-above-keyboard parity. **Pass:** [ ] / **Fail:** [ ] — Notes: ____
  - [ ] **UAC/UVC** (`/uac-uvc`) — drawer-open-no-keyboard + drawer-above-keyboard parity. **Pass:** [ ] / **Fail:** [ ] — Notes: ____
  - [ ] **PERT** (`/pert`) — drawer-open-no-keyboard + drawer-above-keyboard parity. **Pass:** [ ] / **Fail:** [ ] — Notes: ____

═══════════════════════════════════════════════════════════════════════════════
SECTION 4 — Failure-routing footer (after the 10 SMOKE blocks, before the sign-off block)
═══════════════════════════════════════════════════════════════════════════════

Add a `## Failure Routing` H2 section. One paragraph explaining the three D-08 options. Required content:

> If any SMOKE step fails, the tester chooses one of three follow-up actions at fail-time (not lock-time):
>
> 1. **Hotfix in v1.15.1** — open a decimal phase 50.X for the fix; the failing SMOKE step is re-tested after the fix lands; the gate stays open. Use this when the failure is small, fixable now, and blocks bedside use.
> 2. **Defer to v1.15.2 / v1.16** — capture as a backlog requirement; annotate the failing checklist line with the deferral target (e.g., "DEFERRED → v1.15.2 per P-12 mitigation"); the gate closes with the failure documented in PROJECT.md. Use this when the failure routes to known mitigation work that is out of v1.15.1 scope (e.g., SMOKE-09 → v1.15.2 per D-15/D-16).
> 3. **Re-test on different iPhone / iOS version** — annotate the line "RE-TEST PENDING" and re-run on a different device before deciding option 1 vs option 2. Use this when a likely device-specific regression needs reproduction first.
>
> Failure routing is the tester's judgment call at the bedside. Phase 50 plan does NOT pre-commit to which fails route to which action.

═══════════════════════════════════════════════════════════════════════════════
SECTION 5 — Tester sign-off block (at the very bottom of the file)
═══════════════════════════════════════════════════════════════════════════════

Add a horizontal rule (`---`) followed by a `## Tester Sign-Off` H2 section. The block has six fields the tester fills in at sign-off (per D-11). Each field is a markdown line with a label followed by an underscore stub. Required content:

```markdown
---

## Tester Sign-Off

**Tester:** ___________________

**Date:** ___________________

**iPhone Model:** ___________________

**iOS Version:** ___________________

**Calculator routes tested:** ___________________ (list the six calculator routes from SMOKE-10, e.g., "Morphine, Formula, GIR, Feeds, UAC/UVC, PERT — all 6")

**Overall verdict:** [ ] All 10 SMOKE steps PASS or DEFERRED-with-route — milestone close approved
```

═══════════════════════════════════════════════════════════════════════════════
COMMIT FOR TASK 1
═══════════════════════════════════════════════════════════════════════════════

After writing the file, commit with the message:

```
docs(50-01): generate iphone smoke checklist (SMOKE-01..10)
```

Body of the commit message (use heredoc-formatted git commit message):

> Generates `.planning/v1.15.1-IPHONE-SMOKE.md` from REQUIREMENTS.md SMOKE-01..10 acceptance text + ROADMAP.md Phase 50 success criteria. All 10 boxes UNCHECKED. Tester sign-off block stub at the bottom (empty fields). Closes the v1.13 D-12 deferral by elevating real-iPhone testing to primary verification surface for v1.15.1 (per Phase 49 D-25). Header includes CI-proxy disclaimer (Playwright cannot emulate iOS soft keyboard, Dynamic Island, or bfcache — PITFALLS.md P-19/P-20). Pass-or-Documented-Defer for ALL 10 entries is the precondition for Phase 51 / REL-04 / milestone close. Implementing CONTEXT.md D-01..D-27 verbatim.

Use `gsd-sdk query commit "..." .planning/v1.15.1-IPHONE-SMOKE.md` (or the equivalent `git add` + `git commit` pair) per project convention.
  </action>
  <verify>
    <automated>test -f .planning/v1.15.1-IPHONE-SMOKE.md && grep -c '^### SMOKE-' .planning/v1.15.1-IPHONE-SMOKE.md | grep -q '^10$' && grep -q 'D-12' .planning/v1.15.1-IPHONE-SMOKE.md && grep -q 'P-19\|P-20' .planning/v1.15.1-IPHONE-SMOKE.md && grep -q 'Tester Sign-Off' .planning/v1.15.1-IPHONE-SMOKE.md && grep -q 'Add to Home Screen' .planning/v1.15.1-IPHONE-SMOKE.md && grep -c '^  - \[ \] \*\*' .planning/v1.15.1-IPHONE-SMOKE.md | awk '$1 >= 6 {exit 0} {exit 1}' && grep -q 'Failure Routing' .planning/v1.15.1-IPHONE-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/v1.15.1-IPHONE-SMOKE.md` (NOT under `.planning/phases/50-…/`).
    - File contains exactly 10 `### SMOKE-` H3 block headers (verified by `grep -c '^### SMOKE-' === 10`).
    - Each SMOKE-XX block contains the six D-04 fields: title heading line, **Closes:** line, **Setup:** line, **Action:** line, **Expected:** line, **Pass:** [ ] / **Fail:** [ ] line.
    - SMOKE-XX **Expected:** text matches REQUIREMENTS.md SMOKE-XX bullet text + ROADMAP.md Phase 50 success criterion XX text verbatim — no paraphrasing (spot-check by reading SMOKE-04 and SMOKE-06 against ROADMAP lines 165, 167).
    - SMOKE-06 includes BOTH Option A (Phone app + back) and Option B (Lock + 30s + unlock) recipes per D-19/D-20.
    - SMOKE-07 includes the "DEFERRED — no hardware keyboard available" escape-hatch annotation + DEFERRED checkbox per D-17/D-18.
    - SMOKE-09 explicitly routes failure to v1.15.2 with the FOUC `<meta name="theme-color">` mitigation note per D-15/D-16.
    - SMOKE-10 has 6 sub-bullets one per calculator (Morphine, Formula, GIR, Feeds, UAC/UVC, PERT) per D-12/D-13 (verified by `grep -c '^  - \[ \] \*\*' >= 6`).
    - File header references the v1.13 D-12 deferral closure (verified by `grep 'D-12'`).
    - File header includes CI-proxy disclaimer citing Phase 49 D-24 + PITFALLS.md P-19/P-20 (verified by `grep 'P-19\|P-20'`).
    - File header includes "Add to Home Screen" tester install instructions (verified by grep).
    - File contains a `## Failure Routing` section with the three D-08 options (verified by `grep 'Failure Routing'`).
    - File contains a `## Tester Sign-Off` block at the bottom with the six stub fields (Tester / Date / iPhone Model / iOS Version / Calculator routes tested / Overall verdict) — fields are empty until Task 2 (verified by `grep 'Tester Sign-Off'`).
    - All 10 Pass/Fail boxes are UNCHECKED (i.e. `[ ]` not `[x]`) — Task 1 produces the unchecked template; Task 2 is the human's job to tick them.
    - Commit landed with message `docs(50-01): generate iphone smoke checklist (SMOKE-01..10)`.
  </acceptance_criteria>
  <done>
The `.planning/v1.15.1-IPHONE-SMOKE.md` file exists, contains all 10 SMOKE blocks with verbatim Expected text from REQUIREMENTS.md/ROADMAP.md, header includes D-12 closure note + CI-proxy disclaimer + tester install instructions, SMOKE-06 has both bfcache reproduction options, SMOKE-07 has the hardware-keyboard escape hatch, SMOKE-09 has the v1.15.2 routing note, SMOKE-10 has 6 sub-bullets, failure-routing footer + tester sign-off block stub are present, all checkboxes are unchecked, and the file is committed. Task 2 (the human bedside session) can now begin.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Tester runs the checklist on a real iPhone 14 Pro+ in standalone PWA mode and commits the signed state</name>
  <files>.planning/v1.15.1-IPHONE-SMOKE.md</files>
  <read_first>
    - /mnt/data/src/nicu-assistant/.planning/v1.15.1-IPHONE-SMOKE.md (the file Task 1 produced — the tester edits this in place)
    - /mnt/data/src/nicu-assistant/.planning/phases/50-wave-3-real-iphone-smoke-gate/50-CONTEXT.md (D-08 failure-routing options + D-21/D-22/D-23 single-artifact contract)
  </read_first>
  <what-built>
Claude (executor) generated `.planning/v1.15.1-IPHONE-SMOKE.md` in Task 1 — a 10-step checklist with all boxes UNCHECKED, a tester sign-off block stub at the bottom, a CI-proxy disclaimer in the header, and a failure-routing footer.

This task is **flagged `autonomous: false`** because it requires physical iPhone access — Claude cannot execute it. The project owner runs the checklist at the bedside on a real iPhone 14 Pro+ (or newer), edits the markdown inline, and commits the signed state.

There is NO pnpm gate, NO svelte-check, NO vitest, NO Playwright assertion to run for Phase 50 itself (per CONTEXT.md D-26). The "verification" is the tester's signature.
  </what-built>
  <how-to-verify>
**Step-by-step bedside session (~15 minutes):**

1. **Pre-session install.** Open the v1.15.1 deploy URL in Safari on the test iPhone. Tap Share → "Add to Home Screen". Launch the app from the Home Screen icon (NOT the Safari browser tab — browser-tab testing is INVALID per the file header instructions).

2. **Open the checklist file.** Open `.planning/v1.15.1-IPHONE-SMOKE.md` in your editor (or in the GitHub web UI for line-by-line editing). Read the header instructions before starting.

3. **Run each SMOKE-XX entry sequentially.** For each block:
   - Read the **Setup:** and **Action:** lines.
   - Perform the action on the iPhone in standalone PWA mode.
   - Compare the observed outcome against the **Expected:** rubric. Per D-06: binary pass/fail — if the observable outcome matches verbatim, it passes; if anything diverges (timing, visual offset, focus target, VoiceOver phrasing), it fails.
   - Tick the **Pass:** [x] or **Fail:** [x] box (or **DEFERRED:** [x] for SMOKE-07 if no hardware keyboard, or **Fail (route to v1.15.2):** [x] for SMOKE-09 if status-bar legibility fails).
   - Fill the Notes field with anything noteworthy. Required for any FAIL or DEFERRED.

4. **SMOKE-06 reproduction choice.** Pick Option A (Phone app round-trip) OR Option B (Lock + 30s + unlock) per D-19/D-20. Record which option used in the Notes field.

5. **SMOKE-07 hardware-keyboard escape hatch.** If no Bluetooth/Smart Connector keyboard is paired at smoke-time, check the **DEFERRED:** [x] box and note "no hardware keyboard available" — does NOT block sign-off (D-17/D-18).

6. **SMOKE-10 cross-calculator parity.** Tick Pass/Fail independently per the 6 sub-bullets (Morphine, Formula, GIR, Feeds, UAC/UVC, PERT) per D-12/D-13.

7. **Handle any failures.** For each FAIL, choose one of the three D-08 options at fail-time (hotfix in v1.15.1 / defer to v1.15.2 / re-test on different iPhone). Annotate the failing line's Notes field with the chosen route.

8. **Fill the sign-off block at the bottom.** Per D-11:
   - **Tester:** your name
   - **Date:** today's date
   - **iPhone Model:** the test device (e.g., "iPhone 14 Pro" or "iPhone 16 Pro")
   - **iOS Version:** the iOS version (e.g., "iOS 18.5")
   - **Calculator routes tested:** list the six routes (typically "Morphine, Formula, GIR, Feeds, UAC/UVC, PERT — all 6")
   - **Overall verdict:** Tick the box if all 10 SMOKE steps are PASS or DEFERRED-with-route. If any step is FAIL without a documented route, do NOT tick — milestone close is blocked until the failure is resolved.

9. **Commit the signed state.** Stage the edited file and commit:
   ```bash
   git add .planning/v1.15.1-IPHONE-SMOKE.md
   git commit -m "docs(50-01): record signed iphone smoke outcomes"
   ```
   (The commit message wording is the tester's discretion at sign-off — the above is a suggested default. Per D-25, this is the second of two atomic commits for Plan 50-01.)

10. **Verify the file is in the committed state.** Run `git status` — working tree should be clean. Run `grep -c '\[x\]' .planning/v1.15.1-IPHONE-SMOKE.md` — should return at least 11 (10 SMOKE Pass/Fail/DEFERRED ticks + 1 overall-verdict tick, assuming all PASS).

11. **Resume the workflow.** Type `signed` or `done` (or describe failures) at the resume prompt.
  </how-to-verify>
  <resume-signal>Type "signed" once the checklist is committed with all 10 boxes ticked Pass/Fail/DEFERRED, sign-off block fields populated, and `git status` shows a clean working tree. Type "needs hotfix" if any SMOKE step routed to D-08 option 1 (decimal phase 50.X required). Type "deferred to v1.15.2" if any SMOKE step routed to D-08 option 2 (e.g., SMOKE-09 status-bar legibility). Type "re-test pending" if any SMOKE step routed to D-08 option 3 and a different device is needed before deciding 1 vs 2.</resume-signal>
  <action>
HUMAN-ACTION TASK — flagged `autonomous: false` because Claude cannot execute it (requires physical iPhone access).

Tester runs the 10-step bedside session per the `<how-to-verify>` instructions above on a real iPhone 14 Pro+ (or newer) in standalone PWA mode. Tester edits `.planning/v1.15.1-IPHONE-SMOKE.md` inline:
1. Ticks the appropriate Pass / Fail / DEFERRED / Fail-route-to-v1.15.2 box for each of SMOKE-01..10.
2. Fills the Notes field for every FAIL or DEFERRED entry with the chosen D-08 routing option (hotfix-50.X / defer-to-v1.15.2 / re-test-pending).
3. For SMOKE-06: notes which reproduction option used (A or B) per D-19/D-20.
4. For SMOKE-07: ticks DEFERRED + notes "no hardware keyboard available" per D-17/D-18 if no Bluetooth/Smart Connector keyboard is paired (does NOT block sign-off).
5. For SMOKE-10: ticks Pass/Fail independently for each of the 6 calculator sub-bullets (Morphine, Formula, GIR, Feeds, UAC/UVC, PERT) per D-12/D-13.
6. Fills the Tester Sign-Off block at the bottom with: Tester name, Date, iPhone Model, iOS Version, Calculator routes tested, Overall verdict checkbox.
7. Commits the signed state with message `docs(50-01): record signed iphone smoke outcomes` (or tester's discretion at sign-off — second of two atomic commits per D-25).

There is no Claude action to perform during this task. Claude resumes the workflow once the human types the resume signal.
  </action>
  <verify>
    <automated>test -f .planning/v1.15.1-IPHONE-SMOKE.md && [ "$(grep -c '^### SMOKE-' .planning/v1.15.1-IPHONE-SMOKE.md)" = "10" ] && [ "$(grep -cE '\*\*Pass:\*\* \[x\]|\*\*Fail:\*\* \[x\]|\*\*DEFERRED:\*\* \[x\]|\*\*Fail \(route to v1\.15\.2\):\*\* \[x\]' .planning/v1.15.1-IPHONE-SMOKE.md)" -ge 10 ] &amp;&amp; grep -v '^#' .planning/v1.15.1-IPHONE-SMOKE.md | grep -E '^\*\*Tester:\*\* [^_[:space:]]' &amp;&amp; grep -v '^#' .planning/v1.15.1-IPHONE-SMOKE.md | grep -E '^\*\*Date:\*\* [^_[:space:]]' &amp;&amp; git diff --quiet .planning/v1.15.1-IPHONE-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - All 10 SMOKE-XX Pass/Fail boxes are ticked (one of: Pass, Fail, DEFERRED, or Fail-route-to-v1.15.2 for SMOKE-09 specifically).
    - Notes field is filled for every FAIL or DEFERRED entry with the chosen D-08 routing option (hotfix / defer / re-test).
    - SMOKE-10 has all 6 sub-bullets ticked Pass or Fail (one per calculator route).
    - Tester sign-off block fields are populated: Tester, Date, iPhone Model, iOS Version, Calculator routes tested, Overall verdict checkbox.
    - File is committed; `git status` shows clean working tree.
    - For Phase 51 / REL-04 unblock: every entry must be Pass OR DEFERRED-with-route (a bare FAIL with no route blocks milestone close until resolved).
  </acceptance_criteria>
  <done>
The signed checklist is committed. Phase 51 / REL-04 has the audit-trail anchor it needs. The v1.13 D-12 deferral is now formally closed — verifier (`gsd-verifier`) can confirm closure by reading the committed file and observing all 10 boxes ticked Pass or DEFERRED-with-route + populated sign-off block.
  </done>
</task>

</tasks>

<threat_model>
**Trust boundaries:** None — this phase produces a documentation-only markdown artifact with no code surface, no untrusted input processing, no PII storage, no network I/O, no auth flow, no third-party integration. The artifact is committed to a private repo via standard git workflow.

**STRIDE register:** Not applicable to this phase. STRIDE (Spoofing / Tampering / Repudiation / Information disclosure / Denial of service / Elevation of privilege) requires a system component or data flow as the threat target. The deliverable is plain markdown text consumed by humans (the tester at the bedside, the milestone reviewer at archive time, the Phase 51 release verifier).

**The actual risk surface for this phase is clinical-content correctness** — i.e., that the checklist's Expected rubric matches REQUIREMENTS.md/ROADMAP.md verbatim and the tester's signature is on a genuinely-passed bedside session. This is mitigated by:

| Risk | Mitigation |
|------|------------|
| Checklist Expected text drifts from REQUIREMENTS.md/ROADMAP.md (rubric corruption) | D-04 mandates verbatim copy; Task 1 acceptance criteria spot-check SMOKE-04 and SMOKE-06 against ROADMAP lines 165, 167; D-27 verifier closure-mapping check |
| Tester signs off without actually running the bedside session (false-pass / forged audit trail) | Project-owner-as-tester per D-09; signed git commit is the audit anchor per D-11; Phase 51 / REL-04 reads the file directly so a missing or unchecked box blocks milestone close |
| FAIL routes to wrong D-08 option (e.g., a hotfix-class failure routed as "defer to v1.15.2") | D-08 + Failure Routing footer document all three options; tester judgment call at fail-time; PROJECT.md captures the deferral for backlog visibility |
| Browser-tab testing instead of standalone PWA testing (invalid run) | D-09 + file header tester instructions explicitly require Add-to-Home-Screen install; SMOKE-01 Expected text repeats the "not Safari browser tab" guard verbatim |
| SMOKE-07 silently skipped without DEFERRED annotation (gap in audit trail) | D-17/D-18 escape hatch + dedicated DEFERRED checkbox + Notes field requirement |

No code-surface threats apply. `security_enforcement` (when enabled at the project level) does not gate non-coding phases of this shape.
</threat_model>

<verification>
**Phase 50 has no CI gate** per CONTEXT.md D-26. The plan does NOT add svelte-check, vitest, Playwright, or `pnpm build` steps. The verification artifact is the file itself + the signed sign-off block.

**Verifier (`gsd-verifier`) checks (per CONTEXT.md D-27):**

1. **Artifact existence:** `.planning/v1.15.1-IPHONE-SMOKE.md` exists at the canonical path.
2. **Box state:** All 10 SMOKE Pass/Fail boxes are ticked (Pass, Fail, DEFERRED, or Fail-route-to-v1.15.2). No bare unchecked box.
3. **DEFERRED routing:** Every DEFERRED or FAIL entry has a documented D-08 route in its Notes field (hotfix-50.X / defer-to-v1.15.2 / re-test-pending).
4. **Closure mapping:** Each SMOKE-XX block's **Closes:** line names the upstream NOTCH/FOCUS/DRAWER ID(s) that match REQUIREMENTS.md verbatim (per D-04 + D-27).
5. **Sign-off block:** The Tester Sign-Off block at the bottom has all six fields populated (Tester / Date / iPhone Model / iOS Version / Calculator routes tested / Overall verdict checkbox ticked).
6. **SMOKE-06 option:** The Notes field for SMOKE-06 records which reproduction option (A or B) was used.
7. **SMOKE-10 sub-bullets:** All 6 calculator sub-bullets are independently ticked Pass or Fail.
8. **Header content present:** The header references the v1.13 D-12 deferral closure, includes the CI-proxy disclaimer (Phase 49 D-24 + PITFALLS.md P-19/P-20), and includes Add-to-Home-Screen install instructions.

**Pass condition for Phase 51 / REL-04 unblock:** All 10 SMOKE entries are Pass OR DEFERRED-with-route. Any bare FAIL (no route) blocks milestone close.

**Manual spot-checks during verifier review:** Read SMOKE-04 and SMOKE-06 in the file and confirm the Expected text matches ROADMAP.md lines 165, 167 verbatim. Read the file header and confirm the v1.13 D-12 quote matches PROJECT.md line 9 verbatim.
</verification>

<success_criteria>
**Plan 50-01 is COMPLETE when:**

- [ ] `.planning/v1.15.1-IPHONE-SMOKE.md` exists at the canonical path (NOT under the phase directory per D-02).
- [ ] File contains 10 SMOKE blocks with the D-04 schema applied verbatim (title, **Closes:**, **Setup:**, **Action:**, **Expected:**, **Pass:** / **Fail:** lines).
- [ ] Each SMOKE-XX **Expected:** text matches REQUIREMENTS.md + ROADMAP.md verbatim (D-04, D-06).
- [ ] SMOKE-06 has both Option A (Phone-app) and Option B (Lock-then-wake) reproduction recipes (D-19, D-20).
- [ ] SMOKE-07 has the "DEFERRED — no hardware keyboard available" escape-hatch + dedicated DEFERRED checkbox (D-17, D-18).
- [ ] SMOKE-09 has the v1.15.2 routing + FOUC `<meta name="theme-color">` mitigation note (D-15, D-16).
- [ ] SMOKE-10 has 6 sub-bullets one per calculator (D-12, D-13).
- [ ] File header references v1.13 D-12 deferral closure, CI-proxy disclaimer (P-19/P-20 + Phase 49 D-24), and Add-to-Home-Screen install instructions.
- [ ] Failure-routing footer paragraph explains the three D-08 options.
- [ ] Tester sign-off block stub at the bottom (D-11) — six fields ready to fill.
- [ ] Task 1 commit landed: `docs(50-01): generate iphone smoke checklist (SMOKE-01..10)`.
- [ ] Tester ran the checklist at the bedside in standalone PWA mode (D-09).
- [ ] All 10 boxes ticked (Pass / Fail / DEFERRED / Fail-route-to-v1.15.2).
- [ ] Sign-off block fields populated (Tester / Date / iPhone Model / iOS Version / Routes / Overall verdict).
- [ ] Task 2 commit landed: `docs(50-01): record signed iphone smoke outcomes` (or tester's chosen wording).
- [ ] All 10 entries are Pass OR DEFERRED-with-route — Phase 51 / REL-04 is unblocked.
- [ ] `gsd-verifier` closure check passes (artifact + boxes + closure mappings + sign-off block).

**Phase 50 closes the v1.13 D-12 deferral.** PROJECT.md will be updated by Phase 51 (REL-02) to reflect the closure.
</success_criteria>

<output>
After completion, create `.planning/phases/50-wave-3-real-iphone-smoke-gate/50-01-SUMMARY.md` per the standard summary template.

**SUMMARY content guidance specific to this plan:**

- Record the bedside session metadata: date, iPhone model, iOS version, ~minutes-elapsed.
- Record the SMOKE-06 reproduction option used (A or B).
- Record per-SMOKE outcomes: 10 lines, each `SMOKE-XX: Pass | Fail | DEFERRED | Fail-route-to-v1.15.2 [+ route note]`.
- Record any divergences from the SMOKE-XX Expected text the tester observed (e.g., "drawer clearance measured 12 px, well above the 8 px floor — Pass").
- Record any D-08 routing decisions made at fail-time (which fails routed to which option, with rationale).
- Note that the v1.13 D-12 deferral is now formally closed.
- Note that Phase 51 / REL-04 is now unblocked (or list the open D-08 follow-ups that still need to land first).
</output>
