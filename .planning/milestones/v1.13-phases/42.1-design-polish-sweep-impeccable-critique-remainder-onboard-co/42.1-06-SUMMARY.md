---
plan_id: 42.1-06
phase: 42.1
status: complete
completed: 2026-04-24
---

# Plan 42.1-06 Summary — Polish Residue + Cross-Cutting Tests + UI-SPEC Amendment

## Outcome

All 8 plan tasks shipped. 4 additional e2e baseline cleanup tasks shipped on top per orchestrator direction. 12 total atomic commits land on this plan branch. Plan 5 (D-08 hero-fills-viewport structural redesign) was deferred at the orchestrator level — its absence is documented below as a known gap; Phase 42.1 closes without it because every other decision is independently shippable and the structural redesign was always gated.

## Plan tasks completed (8 / 8)

| Task | Commit | Decision honored |
|------|--------|-------------------|
| T1 — Move feeds SegmentedToggle into inputs card | `950846a refactor(42.1-06): move feeds SegmentedToggle into inputs card (D-27)` | D-27 |
| T2 — Verify lone `border-l` on morphine total-reduction cell is gone | (verification no-op — confirmed Plan 2 T4's HeroResult refactor already removed the cell) | D-28 |
| T3 — Symmetric padding on hamburger drawer header | `b76925b polish(42.1-06): symmetric padding on hamburger drawer header (D-29)` | D-29 |
| T4 — Promote `text-2xs` advisory copy to `text-ui` | `a5f231f polish(42.1-06): promote text-2xs advisory copy to text-ui per 11px Floor (D-30)` | D-30 |
| T5 — Append retroactive Phase 42 UI-SPEC amendment | `5698da2 docs(42.1-06): append retroactive Phase 42 UI-SPEC amendment (D-25)` | D-25 |
| T6 — `e2e/mobile-nav-clearance.spec.ts` (10 assertions) | `9a8c219 test(42.1-06): mobile-nav clearance e2e at 375 + 414 (D-09 verification)` | D-09 + D-32 |
| T7 — Visual regression capture README + protocol | `3a4cb55 docs(42.1-06): visual regression capture README + protocol (D-32)` | D-32 |
| T8 — Final regression sweep + DESIGN.md cross-cutting audit + this summary | (this commit) | D-31 + D-33 |

## E2e baseline cleanup tasks completed (4 / 4 — orchestrator additions)

These are the ~10 pre-existing e2e failures flagged during Plan 42.1-01..04 execution. All cluster in three categories; each cluster shipped as its own atomic commit:

| Cluster | Specs touched | Commit |
|---------|---------------|--------|
| **Em-dash text in test assertions** (`917ecf2` purged em-dashes from source; tests still searched for them) | `e2e/morphine-wean.spec.ts` (`Step 1 — Starting dose` → `Step 1: Starting dose`); `e2e/morphine-wean-a11y.spec.ts` (×2 same fix); `e2e/uac-uvc.spec.ts` (`4 of 4 favorites — remove…` → `… favorites. Remove…`; `/^feeds —/i` → `/^feeds\./i`) | `a761cb4 test(42.1-06): align e2e assertions with em-dash purge (917ecf2)` |
| **About button moved out of header → into hamburger drawer** (Plan 42.1-03 hoisted AboutSheet to +layout; tests still expected a header info button) | `e2e/navigation.spec.ts` (`top title bar shows app name, info, and theme toggle` → `… app name, hamburger, and theme toggle`; `info button opens the about sheet` → `hamburger drawer About row opens the about sheet`) | `ec5780c test(42.1-06): route About-button assertions through hamburger drawer` |
| **bits-ui Slider a11y** (axe `aria-input-field-name` + `getByRole('slider', { name: 'Weight slider' })` timeouts) | `src/lib/uac-uvc/UacUvcCalculator.svelte` (`aria-label="Weight slider"` moved from `<Slider.Root>` to `<Slider.Thumb>` — the thumb is the role=slider element). Fix lands in source not tests; same fix unblocks both axe specs and the Playwright slider-keyboard spec. | `fbca33a fix(42.1-06): move Slider aria-label from Root to Thumb for axe + e2e (D-32)` |

The 16 `getByRole('button', { name: /understand/i })` references across the suite are NO-OPS by design (each is wrapped in `.click({ timeout: 2000 }).catch(() => {})` so the missing modal does not fail the test). They pre-date the Plan 42.1-03 modal → banner refactor and stay green; no commit needed.

## Two design corrections captured in 42-UI-SPEC amendment (orchestrator addition)

The retroactive amendment in `5698da2` includes both post-shipment design corrections per orchestrator direction:

1. **D-05 third-cue revision: arrows dropped, label-promotion replaces icon-as-third-cue** (commit `b45ba06`). The 42-UI-REVIEW had restored `ArrowDownToLine` / `ArrowUpFromLine` as the D-05 third cue; user direction during Plan 42.1-03 checkpoint reversed that. Final hero pattern: `UAC` / `UVC` rendered at `text-title` `font-black` in identity color, with quieter `text-2xs text-text-secondary` "Arterial depth" / "Venous depth" qualifier. NO icons. Documented under §"D-05 third cue revision: arrows dropped, label-promotion replaces icon-as-third-cue".
2. **AboutSheet `.xlsx` source-citation removal** (commit `8c0cf95`). All four shipped calculators had their `.xlsx` source-of-truth filenames stripped from `aboutContent.notes`. Internal calc-comment xlsx references preserved. Documented under §"AboutSheet source-citation policy: spreadsheet filenames are internal artifacts, not user-facing provenance".

## DESIGN.md cross-cutting audit results (D-31)

| Rule | Audit grep | Findings | Status |
|------|------------|----------|--------|
| Side-stripe ban | `git grep -E '(border-l-[2-9]\|border-r-[2-9]\|border-t-[2-9]\|border-b-[2-9])\b' src/lib/` | **1 hit**: `src/lib/shell/NavShell.svelte` `border-b-2` on the desktop calculator tab. NOT a violation — this is the semantic active-tab underline shipped by Plan 42.1-01 D-02 (Clinical Blue accent, not identity color). The ban targets decorative side-stripes, not active-state underlines. | PASS |
| OKLCH-Only (no `#000`) | `git grep -F '#000' src/` | **0 hits in `src/lib/` and `src/app.css`** | PASS |
| OKLCH-Only (no `#fff`) | `git grep -F '#fff' src/` | **1 hit**: `src/lib/assets/favicon.svg` style attribute — a static SVG asset, not a CSS rule. Not in scope of the OKLCH-Only rule (which targets app stylesheets and component classes). | PASS |
| Em-dash ban (user-rendered copy) | `git grep -F '—' src/` excluding `*.test.*` | **0 hits in user-rendered strings**. All hits are in `src/app.css` and `src/app.html` comments, where em-dashes are documentation and never reach the DOM. | PASS |
| Identity-Inside (D-01) | `pnpm test:run src/lib/shell/identity-inside.test.ts` | **2/2 assertions PASS** — NavShell chrome and HamburgerMenu star block both clean of `var(--color-identity)` references. | PASS |
| 11 px Floor (D-30) | grep + manual classification of every `text-2xs` site | 30 sites enumerated; 28 are LABEL-role (eyebrow / in-card section label / range hint / tab caption / version caption / button label / cap caption) and stay at 11 px. 2 are PARAGRAPH advisory copy (gir:172, feeds:348) and were promoted to `text-ui` (13 px). | PASS |

## Plan summaries present (D-33)

| Plan | SUMMARY file | Status |
|------|--------------|--------|
| 42.1-01 — Chrome Alignment + Mobile-Nav Clearance | `42.1-01-SUMMARY.md` | present |
| 42.1-02 — `<HeroResult>` + 5-calculator refactor + decimal precision | `42.1-02-SUMMARY.md` | present |
| 42.1-03 — Disclaimer Banner Refactor | `42.1-03-SUMMARY.md` | present |
| 42.1-04 — Decoration Removal + Scoped Transitions + Server Redirect | `42.1-04-SUMMARY.md` | present |
| 42.1-05 — Hero-Fills-Viewport + Sticky Inputs Drawer (D-08) | (NOT EXECUTED — see Known Gaps) | absent |
| 42.1-06 — Polish Residue + Cross-Cutting Tests + UI-SPEC Amendment | `42.1-06-SUMMARY.md` | present (this file) |

## Final test counts

| Suite | Count | Status |
|-------|-------|--------|
| Unit tests (`pnpm test:run`) | **311 passed** across 29 files | green |
| `svelte-check` (`pnpm check`) | 0 errors / 0 warnings on 4554 files | green |
| Playwright e2e (`pnpm exec npx playwright test`) | **98 passed**, 3 skipped (PWA SW tests skipped in dev mode), **0 failed** | green |
| Axe sweeps (embedded in *-a11y.spec.ts) | All sweeps pass in light + dark | green |
| `pnpm build` | Wrote site to `build/`, no errors | green |
| New e2e spec from this plan: `e2e/mobile-nav-clearance.spec.ts` | **10/10 passing** (5 routes × 2 viewports) | green |

## Known gaps

1. **Plan 42.1-05 (D-08 hero-fills-viewport + sticky inputs drawer) was deferred** at the orchestrator level. Per RESEARCH §"Risks and Open Questions" #1, D-08 is the most invasive UX change in the phase and was always recommended to ship behind a localStorage feature flag for internal QA. Phase 42.1 closes without it because every other decision is independently shippable and D-08 is gated work that belongs in its own focused phase (42.2 candidate). Phase 43 (Release v1.13.0) does NOT depend on D-08; the structural redesign can ship in v1.14 without breaking the v1.13 contract. The `e2e/hero-drawer.spec.ts` referenced in PATTERNS §9 was never created for the same reason.
2. **Visual regression `b-*` captures** are deliberately deferred. The README at `.planning/ui-reviews/42.1-screenshots/README.md` documents the 23-capture protocol; a manual post-merge sweep (or a separate `/gsd-ui-review` invocation) will produce the captures and a `42.1-VERIFICATION.md` summary. Phase 42.1 ships with the protocol locked, not the captures.

## Phase 43 readiness

Phase 42.1 is **complete** for v1.13 release purposes. All decisions D-01..D-04, D-06..D-07, D-09..D-33 shipped. D-05 verification was a no-op (Amber stays semantic — no code change needed). D-08 is deferred as documented.

**Phase 43 (Release v1.13.0) can proceed.** Cross-cutting audit clean, 311 unit tests + 98 e2e tests + axe sweeps all green, svelte-check 0/0, build successful.
