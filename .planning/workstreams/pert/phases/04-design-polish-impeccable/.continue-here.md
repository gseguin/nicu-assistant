---
context: phase
workstream: pert
milestone: v1.15
phase: 04-design-polish-impeccable
task: pre-execution
total_tasks: 5 (Wave 1 + replan + Wave 2 [conditional] + Wave 3)
status: paused-pre-execution
last_updated: 2026-04-26T03:25:19.876Z
---

# Phase 4: Design Polish (`/impeccable` Critique Sweep) — Paused

**Branch:** `workspace/pert` (HEAD `257d100`)
**Status:** Fully planned + verified. Pre-execution pause to choose execution mode.

<current_state>
Phase 4 is the v1.15 release-readiness design-polish sweep. ALL planning artifacts are committed and verified:

- 04-CONTEXT.md (D-01..D-11 + sub-decisions, auto-resolved from v1.13 Phase 42.2 precedent + DESIGN.md/DESIGN.json contract + Phase 1/2/3.1 outcomes)
- 04-UI-SPEC.md (453 lines, design-contract-enforcement spec)
- 04-RESEARCH.md (340 lines, HIGH confidence; /impeccable v3.0.1 API verified end-to-end)
- 04-01-PLAN.md (Wave 1: critique sweep)
- 04-02-PLAN.md (Wave 2: PLACEHOLDER — `placeholder: true`; replan beat per D-07a)
- 04-03-PLAN.md (Wave 3: 18-gate verification + Phase 4 closure)

Plan-checker: VERIFICATION PASSED on all 10 dimensions; PERT-DESIGN-01..06 all covered.

The pause is BEFORE execution. Three execution modes were offered to the user; user chose `/gsd-pause-work` instead. Awaiting user direction on resume.
</current_state>

<completed_work>

Completed (this session, in order):
1. `/gsd-discuss-phase 4 --ws pert --auto` → 04-CONTEXT.md + 04-DISCUSSION-LOG.md (commit `f6661cf`)
2. STATE.md phase 4 context update (commit `857d8f2`)
3. `/gsd-ui-phase 4 --ws pert --auto` → 04-UI-SPEC.md (commit `b95b7b1`)
4. Phase researcher → 04-RESEARCH.md + planner → 3 PLAN.md files + ROADMAP.md update (commit `975dbb4`)
5. Plan-checker VERIFICATION PASSED (no commit; result captured in conversation)
6. STATE.md phase 4 planning-complete update (commit `257d100`)

Prior session (Phase 3.1 closure — CONTEXT for this session): ended at commit `9881bb5` with PERT-TEST-05 FULL closure + KI-1 RESOLVED at `f2da16d`. Phase 3 baseline: vitest 423/423, pert.spec 8/8. Phase 3.1 baseline: vitest 425/425, pert.spec 12/12, full Playwright 117/118 with `disclaimer-banner.spec.ts:28` baseline flake.
</completed_work>

<remaining_work>

Wave 1 — `/gsd-execute-phase 4 --wave 1 --auto --ws pert`:
- Task 0: Author PRODUCT.md at repo root (NEW file; D-08a 3rd named exception). Synthesize from CLAUDE.md §Design Context + DESIGN.md.
- Task 1: Start dev server (`pnpm dev --port 5173 --strictPort`), poll until ready, capture PID.
- Task 2: 8 sequential `/impeccable critique` passes (theme×viewport×mode); capture each transcript to `04-CRITIQUE-{theme}-{viewport}-{mode}.md`.
- Task 3: CLI scan: `npx --yes -p impeccable@latest impeccable detect --json <files> > 04-DETECT.json`.
- Task 4: Normalize all 8 critique transcripts + DETECT.json into `04-FINDINGS.md` triage table.
- Task 5: Run scripted DESIGN.md named-rule audit (`04-AUDIT.sh` per UI-SPEC + CONTEXT D-05a).
- Task 6: Tear down dev server (`kill -TERM $DEV_PID`).
- Task 7: Write 04-01-SUMMARY.md with P1/P2/P3 counts + Wave 2 recommendation.

(Recommended human pause-point here to review 04-FINDINGS.md before authorizing Wave 2.)

Wave 2 — REPLAN beat: `/gsd-plan-phase 4 --gaps --ws pert`:
- Reads 04-FINDINGS.md disposition column
- Auto-authors actual fix plans (per CONTEXT D-03 disposition rules: P1=fix-now; P2 ≤5 LOC=fix-now; P2 >5 LOC OR shared-component=defer; P3=defer unless <3 LOC + PERT-route only)
- Skip Wave 2 entirely if 0 fix-now findings

Wave 2 execute (only if fix plans were authored): `/gsd-execute-phase 4 --wave 2 --auto --ws pert`

Wave 3 — `/gsd-execute-phase 4 --wave 3 --auto --ws pert`:
- Re-run all 8 `/impeccable critique` passes on post-fix UI; capture into `04-CRITIQUE-FINAL-{...}.md`
- Re-run scripted named-rule audit; assert 0 violations
- Extract /40 score from each FINAL transcript; assert (a) ≥35/40 in ≥6/8 contexts, (b) aggregate ≥35/40, (c) zero P1 remaining
- Run 7-gate clinical sequence verbatim from Phase 3.1 Plan 04 (svelte-check 0/0; vitest 425/425; pnpm build OK ≤580 KiB; CI=1 pert-a11y 4/4; CI=1 pert.spec 12/12; CI=1 full Playwright 117/118 with disclaimer-banner.spec.ts:28 baseline flake; negative-space audit)
- Write 04-VERIFICATION.md certifying all 18 gates green
- Update STATE.md, ROADMAP.md, REQUIREMENTS.md (PERT-DESIGN-01..06 Active→Validated)

After Wave 3: Phase 4 closes; next phase is `/gsd-plan-phase 5 --ws pert` (Release v1.15.0).
</remaining_work>

<decisions_made>

This session (auto-resolved per --auto flags + research recommendations):
1. **8 critique passes** (theme×viewport×mode) per CONTEXT D-01 — covers Oral + Tube-Feed mode-specific issues
2. **Phase 42.2 P1/P2/P3 triage rubric** per CONTEXT D-02/D-03 — directly inherited from v1.13 precedent
3. **≥35/40 in ≥6/8 contexts + aggregate ≥35/40 + zero P1** per CONTEXT D-04 score-acceptance gate
4. **Scripted DESIGN.md named-rule audit** (10 rules with grep mappings) per CONTEXT D-05/D-05a
5. **3-wave structure with explicit Wave 2 replan beat** per CONTEXT D-07/D-07a — Wave 2 plans cannot be authored ahead of Wave 1 findings
6. **PERT-route-only negative-space contract** per CONTEXT D-08 + 4 named exceptions per D-08a (`app.css` `.identity-pert` block; `registry.ts` PERT entry; e2e/pert*.spec.ts selector fixes; PRODUCT.md repo-root setup file)
7. **`/impeccable` v3.0.1 invocation form** per RESEARCH §Tooling — `/impeccable critique <URL>` with browser state set first; NO --theme/--viewport/--mode flags
8. **CLI scan corrected** to `npx --yes -p impeccable@latest impeccable detect --json` (UI-SPEC initially had wrong form `npx impeccable --json`)
9. **PRODUCT.md authored manually** from CLAUDE.md §Design Context (no interactive `/impeccable teach` in --auto mode)
10. **Sequential per-pass parallelism** (1 dev server, 8 passes one after another)
11. **Wave 3 re-runs all 8 critique passes** on post-fix UI (not against Wave 1 transcripts)
12. **Clinical-gate counts inherit verbatim** from Phase 3.1 closure
</decisions_made>

<blockers>
None — execution-blocking. The only "block" is awaiting user choice of execution mode (a/b/c offered at pause).
</blockers>

## Required Reading (in order)

1. `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-CONTEXT.md` — LOCKED decisions D-01..D-11
2. `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-UI-SPEC.md` — design-contract-enforcement spec (453 lines; codifies the 8 critique contexts, 10-rule audit, 18-gate verification checklist)
3. `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-RESEARCH.md` — HIGH-confidence research (340 lines; concrete `/impeccable` commands, dev server lifecycle, 6 pitfalls)
4. `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-01-PLAN.md` — Wave 1 critique sweep
5. `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-PLAN.md` — Wave 2 placeholder (DO NOT execute; trigger replan after Wave 1)
6. `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-03-PLAN.md` — Wave 3 verification + Phase 4 closure
7. `DESIGN.md` (project root) — the contract being enforced
8. `.planning/workstreams/pert/STATE.md` — workstream state (Phase 3.1 closed, Phase 4 planned)

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Workstream-aware SDK calls return `phase_found:false` | The gsd-sdk init.phase-op / init.plan-phase / init.execute-phase commands look in `.planning/phases/`, NOT `.planning/workstreams/pert/phases/`. Workflows that depend on these (e.g. STATE.md auto-recording via state.begin-phase / state.planned-phase) silently no-op for workstream-scoped phases. | advisory | When invoking workflow orchestrators, always pass `--ws pert` AND verify the phase directory exists at `.planning/workstreams/pert/phases/04-design-polish-impeccable/`. Fall back to direct file edits + manual STATE.md/ROADMAP.md/REQUIREMENTS.md updates with workstream-local paths if SDK queries return `phase_found:false`. |
| `npx impeccable --json` form is wrong | UI-SPEC initially documented this form for the CLI scan; researcher corrected to `npx --yes -p impeccable@latest impeccable detect --json <files>` | advisory | Use the corrected form embedded in 04-01-PLAN.md Task 2 STEP D verbatim |
| `/impeccable` does NOT take --theme/--viewport/--mode flags | The skill is URL-only; browser state must be set BEFORE invocation | advisory | Set theme via `localStorage.setItem('nicu_assistant_theme', '...')`; mode via SegmentedToggle Playwright click; viewport via Playwright resize. All three set FIRST, then `/impeccable critique <URL>` is invoked |

## Infrastructure State

- **Dev server**: NOT running (Wave 1 will start `pnpm dev --port 5173 --strictPort`)
- **Branch**: `workspace/pert` (this is a git worktree of `/mnt/data/src/nicu-assistant`; commits stay on `workspace/pert` and merge back when v1.15 ships)
- **Working tree**: clean
- **HEAD**: `257d100` (docs(state): record phase 4 planning complete)
- **PRODUCT.md**: NOT yet authored at repo root (Wave 1 Task 0 creates it)

## Pre-Execution Critique Required

The plans were already verified by gsd-plan-checker (VERIFICATION PASSED on 10 dimensions). No additional pre-execution critique is required from the resuming agent. However, the resuming agent should:

1. Confirm with the user which execution mode (a/b/c) was chosen at the pause, OR offer the same three options again if the user comes back fresh
2. If the user picks Wave 1 only with manual review (option b — RECOMMENDED), pause after Wave 1 SUMMARY ships and present FINDINGS.md to the user before authorizing Wave 2 replan

<context>
The user has been running this workstream for ~2 days, shipping v1.15 incrementally. Phase 1 (architecture+identity+clinical-data), Phase 2 (calculator core+modes+safety), Phase 3 (tests with PARTIAL closure), and Phase 3.1 (KI-1 SelectPicker bridge fix with FULL PERT-TEST-05 closure) all shipped cleanly. Phase 4 is the polish-and-ship phase. After Phase 4, only Phase 5 (Release v1.15.0) remains.

The user explicitly invoked the auto chain (`--auto`) for both /gsd-discuss-phase 4 and /gsd-plan-phase 4, so they trust the orchestrator to make recommended decisions. The Wave 1 critique sweep is novel for this workstream — every prior phase shipped without `/impeccable`. The pause is appropriate: critique-then-fix has a discovery beat that benefits from human review of the FINDINGS.md triage before the replan beat fires Wave 2.

The mental model: Phase 4 = "audit + minimal-touch polish" not "redesign". The universal patterns from v1.13 (HeroResult, sticky InputDrawer, DisclaimerBanner v2, em-dash purge, Identity-Inside Rule) are ALREADY shipped. PERT inherited them by virtue of Phase 2 D-04 wiring. Phase 4 catches PERT-specific drift only.
</context>

<next_action>
On resume, ask user: "Three execution modes were offered before pause: (a) full auto Wave 1→Wave 3 chain; (b) Wave 1 only with human review of FINDINGS.md before Wave 2 [RECOMMENDED]; (c) review plans first then resume manually. Which mode do you want?"

If user picks (a): `/gsd-execute-phase 4 --auto --no-transition --ws pert`
If user picks (b): `/gsd-execute-phase 4 --wave 1 --auto --ws pert` then pause for FINDINGS.md review
If user picks (c): user resumes manually after their own review

Whichever mode, the next concrete command is one of the three above.
</next_action>
