---
phase: 4
plan: 2
type: execute
wave: 2
workstream: pert
milestone: v1.15
depends_on:
  - 04-01
files_modified:
  - src/lib/pert/PertCalculator.svelte
  - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
requirements:
  - PERT-DESIGN-02
  - PERT-DESIGN-03
  - PERT-DESIGN-04
  - PERT-DESIGN-05
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "F-03 fix shipped: in src/lib/pert/PertCalculator.svelte the Tube-Feed `Capsules per month` row (currently at lines ~285-299; verify exact line range at edit time) gets a 1-3 LOC visual-hierarchy bump that elevates the prescribing artifact above the 3 sibling derived figures (Total fat / Total lipase / Lipase per kg). Allowed approaches: (a) bump the numeral `font-bold` -> `font-extrabold` on the `<span class=\"num text-title font-bold ...\">` numeral; (b) bump the eyebrow `tracking-wide` -> `tracking-wider` on the `<div class=\"text-2xs font-semibold tracking-wide ...\">` eyebrow; (c) BOTH (a)+(b) if a single tweak is insufficient. NO eyebrow color change (Identity-Inside Rule -- eyebrow stays at `text-[var(--color-identity)]`). NO change to the visible text strings `Capsules per month` or the numeral display format -- the e2e selector at e2e/pert.spec.ts already grep-finds these strings (Pitfall 4); ZERO selector update permitted in this plan."
    - "Out-of-band weight-range fix already shipped at commit 2dc7ae2 (`fix(pert): align weight slider range with other calculators (max 10 kg)`): single-line config change in `src/lib/pert/pert-config.json` flipping `weightKg.max: 80 -> 10`. Calc layer untouched (NumericInput min/max is advisory-only per v1.6; never auto-clamps). 81/81 PERT vitest still green; svelte-check 0/0. This commit is part of the Phase 4 design-polish phase scope, NOT scope creep -- it aligns the PERT weight advisory with the cross-calculator pattern (feeds/gir/morphine/uac-uvc all use weightKg: { min: 0.3, max: 10, step: 0.1 }). Verifier (Wave 3) will see the diff in the negative-space audit and needs this acknowledgment in 04-02-SUMMARY.md."
    - "D-08 PERT-route allowlist honored: the only file edits in this plan's commit are `src/lib/pert/PertCalculator.svelte` (allowed) and the planning artifact `04-02-SUMMARY.md` at the phase directory. Strictly forbidden per D-08b: shared components (`src/lib/shared/components/**`), shell (`src/lib/shell/NavShell.svelte`, `src/lib/shell/HamburgerMenu.svelte`), other calculators (`src/lib/{morphine-wean,formula,gir,feeds,uac-uvc}/**`), `DESIGN.md`, `DESIGN.json`, calc-layer (`src/lib/pert/calculations.ts`, `src/lib/pert/state.svelte.ts`, `src/lib/pert/types.ts`, `src/lib/pert/config.ts`). `pert-config.json` is NOT re-edited in this plan; it was already committed at 2dc7ae2 and is acknowledged as audit-trail only."
    - "D-08b forced-defer for Wave 1 cross-calculator findings F-01 (NumericInput en-dash, ~1 LOC at `src/lib/shared/components/NumericInput.svelte:76`) and F-02 (NavShell sticky-overlay, ~5-15 LOC in `src/lib/shell/NavShell.svelte`) acknowledged in 04-02-SUMMARY.md as cross-calculator backlog seeded by Wave 1. Both are P2 findings forced-deferred per Pitfall 5 + D-08b regardless of LOC; both affect all 6 calculators by virtue of living in shared components. Recommended carrier: v1.15.x hotfix (F-01) and v1.16 cross-calculator polish phase (F-02). Neither blocks v1.15 PERT release-readiness (no P1, no clinical-trust regression, no DESIGN.md named-rule violation). Wave 2 plan does NOT auto-fix across D-08b."
    - "Phase 3.1 invariants preserved per UI-SPEC §'PERT Component Invariants': function-binding bridges intact at 3 hits in `src/lib/pert/PertInputs.svelte` (medication / strength / formula at the post-Phase-3.1 multi-line `bind:value={` shape; Wave 2 does NOT touch PertInputs.svelte); zero string-bridge proxies (`grep -cE 'let \\w+(Bridge|Proxy) = \\$state' src/lib/pert/PertInputs.svelte` returns 0); em-dash count 0 (`grep -cP '\\x{2014}' src/lib/pert/PertCalculator.svelte` returns 0); en-dash count 0 (`grep -cP '\\x{2013}' src/lib/pert/PertCalculator.svelte` returns 0); STOP-red advisory carve-out preserved (`border-2 border-[var(--color-error)]` + `OctagonAlert` + `role=\"alert\"` at lines ~310-322 untouched)."
    - "Identity-Inside Rule (DESIGN.md line 186 + Phase 1 D-03 + Phase 1 D-17) honored: the eyebrow color stays `text-[var(--color-identity)]` (never changed); the numeral color stays `text-[var(--color-text-primary)]` (never changed). The bump is a TYPOGRAPHIC weight/tracking change ONLY, not a chromatic change. The eyebrow stays identity-purple inside the route; chrome (top tabs, bottom nav, hamburger) is untouched."
    - "Quality gates: `pnpm exec svelte-check` returns 0 errors / 0 warnings (Phase 3.1 baseline preserved, ~4586 files); `pnpm test:run -- src/lib/pert/` returns 81/81 (the PERT vitest subset: PertCalculator.test.ts 10 + PertInputs.test.ts 9 + calculations.test.ts 45 + config.test.ts 17 = 81; matches Phase 3.1 baseline because no test files are touched); em-dash + en-dash sweep on PertCalculator.svelte returns 0 hits each via `grep -cP '\\x{2014}'` and `grep -cP '\\x{2013}'`; `grep -cE 'class=\"[^\"]*\\bnum\\b' src/lib/pert/PertCalculator.svelte` returns at least 9 (unchanged from baseline -- bump is on font-weight class, not on `.num` class)."
    - "Negative-space audit at plan close: `git diff --name-only HEAD~1..HEAD -- src/ e2e/` returns ONLY `src/lib/pert/PertCalculator.svelte` (the F-03 commit). The pert-config.json change from commit 2dc7ae2 is already in HEAD via prior commit and is NOT re-touched by this plan. The PERT-route allowlist boundary (`src/lib/pert/**` + `src/routes/pert/**`) holds. Wave 3's phase-level audit (`git diff --name-only $BASELINE..HEAD -- src/ e2e/`) will return: `src/lib/pert/PertCalculator.svelte` (this plan) + `src/lib/pert/pert-config.json` (commit 2dc7ae2) -- both within D-08 allowlist."
    - "F-03 acceptance gate per FINDINGS.md: post-fix, the Capsules-per-month row reads visually distinct from the 3 derived rows (Total fat / Total lipase / Lipase per kg) in all 4 tube-feed contexts (light-mobile, light-desktop, dark-mobile, dark-desktop). Wave 3 re-runs the 8-context critique sweep and verifies the post-fix score holds at >=35/40 in >=6/8 + aggregate >=35/40 (the v1.13 Phase 42.2 'predominant pass' bar)."
  artifacts:
    - path: src/lib/pert/PertCalculator.svelte
      provides: "Post-F-03-fix component: Tube-Feed `Capsules per month` row (lines ~285-299) with a 1-3 LOC visual-hierarchy bump elevating the prescribing artifact above the 3 derived secondary figures. Preserves the Phase 2 D-04 layout (divide-y stack of 4 rows; eyebrow + numeral + unit per row); only the `Capsules per month` row's font-weight or eyebrow tracking changes. Verified by `grep -cE 'font-extrabold' src/lib/pert/PertCalculator.svelte >= 1` OR `grep -cE 'tracking-wider' src/lib/pert/PertCalculator.svelte >= 1` (one or both, depending on which approach the executor chose at edit time)."
      contains: "Capsules per month"
      contains: "tubeFeedResult.capsulesPerMonth"
    - path: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      provides: "Wave 2 closure record: F-03 fix shipped (line range, exact font-weight or tracking change applied, before/after class strings, e2e selector untouched per Pitfall 4); commit 2dc7ae2 weight-range fix acknowledged as in-phase scope (NOT scope creep) with the one-line context (cross-calculator alignment with feeds/gir/morphine/uac-uvc; calc-layer untouched; 81/81 vitest preserved); D-08b forced-defer for F-01 + F-02 documented as cross-calculator backlog seeded by Wave 1; Phase 3.1 invariants regression-guard pasted (function-binding 3 hits, em-dash + en-dash 0/0, tabular-numerals >=9 hits); negative-space audit pasted; PERT-DESIGN-02..05 satisfaction mapping to F-03 + commit 2dc7ae2."
      contains: "F-03"
      contains: "2dc7ae2"
      contains: "D-08b forced-defer"
      contains: "Phase 3.1 invariants"
      min_lines: 40
    - path: src/lib/pert/pert-config.json
      provides: "ALREADY COMMITTED at 2dc7ae2 (parent of HEAD pre-this-plan). Acknowledged in this plan's must_haves and 04-02-SUMMARY.md as part of Phase 4 design-polish scope, NOT scope creep. Verified via `git log --oneline | grep -E '2dc7ae2.*align weight slider'` returning 1 hit. NOT re-edited by this plan; the executor must NOT touch pert-config.json (re-editing would either revert the fix or no-op + dirty the diff)."
      contains: "weightKg"
      contains: "\"max\": 10"
  key_links:
    - from: src/lib/pert/PertCalculator.svelte
      to: src/lib/shared/components/HeroResult.svelte
      via: "PertCalculator.svelte imports + invokes HeroResult; the F-03 fix lives in the secondary section BELOW the hero, not in the hero itself; HeroResult is shared component (D-08b read-only)"
      pattern: "import HeroResult"
    - from: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      to: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-FINDINGS.md
      via: "SUMMARY's F-03 fix-now record closes the FINDINGS.md fix-now row; Wave 3's 04-VERIFICATION.md cross-references both"
      pattern: "F-03"
    - from: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      to: src/lib/pert/pert-config.json
      via: "SUMMARY documents commit 2dc7ae2 (out-of-band weight-range fix) as in-phase scope acknowledgment; verifier (Wave 3) reads SUMMARY to understand why pert-config.json appears in the phase-level diff without a Wave-2 source-edit task"
      pattern: "2dc7ae2"
commit_message: "fix(pert/04-02): F-03 capsules-per-month visual hierarchy bump (Wave 2 design polish)"
---

<objective>
**Wave 2 of Phase 4 -- the single fix-now finding from the Wave 1 critique sweep.**

Per CONTEXT D-07a replan beat, this plan was authored AFTER Wave 1 (Plan 04-01) shipped 04-FINDINGS.md. Wave 1 produced 5 unique findings + 1 informational CLI seed; auto-disposition per CONTEXT D-03 yielded:

- **0 P1 findings** (the headline pass condition for the v1.13 Phase 42.2 bar already met at the post-Phase-3.1 UI baseline)
- **1 P2 fix-now finding** (F-03): Tube-Feed mode renders 4 secondary outputs (Total fat / Total lipase / Lipase per kg / Capsules per month) at visually equal weight (`text-title font-bold`); per UI-SPEC Watch Item 5 the prescribing artifact (`Capsules per month`) should be findable faster than the 3 derived figures. PERT-route only, 1-3 LOC, cheap-inline rule per D-03 satisfied.
- **2 P2 findings forced-deferred** (F-01 NumericInput en-dash, F-02 NavShell sticky-overlay) per Pitfall 5 + D-08b cross-calculator backlog rule.
- **2 P3 findings deferred** (F-04 hero-owns-viewport when InputDrawer trigger is populated; F-05 desktop sticky-aside max-width) per D-03 P3 default.

**This plan ships exactly two things:**

1. **F-03 fix** (Task 1 + Task 2): a 1-3 LOC visual-hierarchy bump on the `Capsules per month` row of `src/lib/pert/PertCalculator.svelte` (Tube-Feed branch, lines ~285-299) so the prescribing artifact reads visually distinct from the 3 derived figures above it. The bump is typographic (font-weight or eyebrow tracking) -- NEVER chromatic (Identity-Inside Rule reserves eyebrow color for `text-[var(--color-identity)]`). The visible text `Capsules per month` and the numeral display format are PRESERVED -- e2e selectors at `e2e/pert.spec.ts` still match (Pitfall 4 reminder).

2. **04-02-SUMMARY.md** (Task 3): the Wave 2 closure record. Documents (a) the F-03 fix line range + exact before/after class strings + verify-gate output; (b) the out-of-band weight-range fix already shipped at commit 2dc7ae2 (`pert-config.json` `weightKg.max: 80 -> 10`) as in-phase scope acknowledgment so Wave 3's negative-space audit sees the audit trail; (c) the D-08b forced-defer for F-01 + F-02 as cross-calculator backlog seeded by Wave 1.

**This plan does NOT:**

- Touch `src/lib/pert/PertInputs.svelte` (Phase 3.1 frozen; function-binding bridges + em-dash + en-dash count 0 + zero string-bridge proxies must hold).
- Touch `src/lib/pert/pert-config.json` (already committed at 2dc7ae2 in this phase's scope; re-editing would either revert the fix or no-op + dirty the diff).
- Touch `src/lib/pert/calculations.ts`, `src/lib/pert/state.svelte.ts`, `src/lib/pert/types.ts`, `src/lib/pert/config.ts` (calc-layer + state shape are Phase 2/3.1 frozen).
- Touch any shared component, NavShell, HamburgerMenu, other calculators, DESIGN.md, DESIGN.json (D-08b strictly forbidden).
- Touch `e2e/pert.spec.ts` or `e2e/pert-a11y.spec.ts` (Pitfall 4 reminder applied: F-03 changes typography only, not text content; selectors still match -- ZERO selector update permitted).
- Run the 8-context `/impeccable` critique sweep again -- that is Wave 3's responsibility (Plan 04-03).
- Update STATE.md / ROADMAP.md / REQUIREMENTS.md -- those updates land in Wave 3 closure (Plan 04-03 Task 3).

**Why a 1-plan Wave 2 (not 0 plans, not 2+ plans):** per Wave 1's 04-01-SUMMARY.md "Wave-2 plan-count recommendation": 1 fix-now finding in 1 file = 1 Wave 2 plan. Per CONTEXT D-07/D-07a: 1-3 fix-now findings in 1-2 files = 1 Wave 2 plan. The user has the agency to skip Wave 2 manually by running `/gsd-execute-phase 4 --wave 3` instead; this plan is authored as if it will execute.

**Per FINDINGS.md acceptance:** post-fix, the Capsules-per-month row reads visually distinct from the 3 derived rows in all 4 tube-feed contexts; Wave 3 re-runs the 8-context sweep and verifies the post-fix score holds at >=35/40 in >=6/8 + aggregate >=35/40.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/pert/STATE.md
@.planning/workstreams/pert/ROADMAP.md
@.planning/workstreams/pert/REQUIREMENTS.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-CONTEXT.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-RESEARCH.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-UI-SPEC.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-FINDINGS.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-01-SUMMARY.md
@DESIGN.md
@CLAUDE.md
@src/lib/pert/PertCalculator.svelte

<interfaces>
<!-- Pre-extracted code excerpts for the F-03 edit site (verify exact line numbers at edit time -- the code below was captured from HEAD = 4aec126 + commit 2dc7ae2 worktree). The executor reads PertCalculator.svelte fresh and applies the bump on the actual rendered text. -->

CURRENT (Tube-Feed `Capsules per month` row at lines ~284-299 in src/lib/pert/PertCalculator.svelte):
```svelte
<!-- Capsules per month -->
<div class="flex items-baseline justify-between px-5 py-4">
    <div>
        <div
            class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase"
        >
            Capsules per month
        </div>
        <div class="flex items-baseline gap-2">
            <span class="num text-title font-bold text-[var(--color-text-primary)]">
                {tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}
            </span>
            <span class="text-ui text-[var(--color-text-secondary)]">capsules</span>
        </div>
    </div>
</div>
```

The 3 sibling rows (Total fat / Total lipase / Lipase per kg) at lines ~233-282 use the SAME class strings on the eyebrow + numeral. F-03 says: bump ONLY the `Capsules per month` row so it reads distinct.

PREFERRED RECIPE (Approach A: numeral font-weight bump; smallest diff):
- Eyebrow: UNCHANGED at `text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase` (Identity-Inside Rule -- color stays `text-[var(--color-identity)]`).
- Numeral: `<span class="num text-title font-bold text-[var(--color-text-primary)]">` -> `<span class="num text-title font-extrabold text-[var(--color-text-primary)]">`.
  - Diff: `font-bold` -> `font-extrabold`. 1 token swap. 0 LOC delta.

ALTERNATE RECIPE (Approach B: eyebrow tracking bump; pairs visually but on label not value):
- Eyebrow: `tracking-wide` -> `tracking-wider` on the `<div class="text-2xs font-semibold tracking-wide ...">` row.
  - Diff: `tracking-wide` -> `tracking-wider`. 1 token swap. 0 LOC delta.
- Numeral: UNCHANGED.

COMBINED RECIPE (Approach C: both A and B; for cases where one tweak alone is insufficient):
- Eyebrow: `tracking-wide` -> `tracking-wider`.
- Numeral: `font-bold` -> `font-extrabold`.
- Diff: 2 token swaps in the same row. 0 LOC delta.

FORBIDDEN: any change to the eyebrow's `text-[var(--color-identity)]` color token (Identity-Inside Rule per DESIGN.md line 186 + Phase 1 D-03 + Phase 1 D-17). FORBIDDEN: any change to the visible text `Capsules per month` (e2e selector at `e2e/pert.spec.ts:128-132` would break per Pitfall 4). FORBIDDEN: any change to the numeral display (`{tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}` -- this is the same expression `e2e/pert.spec.ts:132` resolves via `getByText('150', { exact: true })` for the Tube-Feed happy path).

The executor's recommended choice is Approach A (smallest diff, clearest visual hierarchy on the prescribing artifact). If the bump feels insufficient when the executor previews the change, fall back to Approach C.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply F-03 visual-hierarchy bump on the `Capsules per month` row in PertCalculator.svelte</name>
  <read_first>
    - src/lib/pert/PertCalculator.svelte (read in full; verify the actual line range for the Tube-Feed `Capsules per month` row -- citing target ~285-299 from FINDINGS.md but the executor must confirm at edit time since prior commits may have shifted line numbers)
    - DESIGN.md (offset 180, limit 30 -- "The Identity-Inside Rule" section starting at line 186; the eyebrow color token `var(--color-identity)` is reserved for inside-the-route surfaces and the bump must NOT alter the eyebrow color)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-CONTEXT.md (offset 91, limit 11 -- D-08 allowlist; the only allowed source/test edit in this task is `src/lib/pert/PertCalculator.svelte`)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-FINDINGS.md (read F-03 row in the Findings Triage Table -- the proposed-fix column documents the recipe; the disposition column documents the auto-fix-now reasoning)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-UI-SPEC.md (offset 1, limit 200 -- §"PERT Component Invariants" + §"Watch Items" -- Watch Item 5 is the F-03 source rule; "the prescribing artifact (Capsules per month) should be findable faster than the 3 derived figures")
    - e2e/pert.spec.ts (read the Tube-Feed happy-path test; confirm the selector for `Capsules per month` and the numeral assertion -- the executor must NOT change strings these selectors target; Pitfall 4)
  </read_first>
  <files>src/lib/pert/PertCalculator.svelte</files>
  <action>
**STEP A -- Re-read the actual line range.** The findings-cited range is ~285-299 but the executor must confirm by reading PertCalculator.svelte fresh. Use `grep -n "Capsules per month" src/lib/pert/PertCalculator.svelte` to find the row's start line; from there verify the row spans ~15 lines (eyebrow `<div>` + numeral `<span>` + unit `<span>`). If the line range has shifted from the FINDINGS.md citation (e.g. due to prior commits), proceed with the actual range -- the task targets the row containing `Capsules per month` regardless of exact line number.

**STEP B -- Choose the recipe:**

DEFAULT (Approach A; recommended):
- Locate the row at the cited line range.
- Find the numeral `<span class="num text-title font-bold text-[var(--color-text-primary)]">` (currently this matches the same class string on all 4 secondary rows; the executor must edit ONLY the one inside the `Capsules per month` block).
- Change `font-bold` -> `font-extrabold` on that single `<span>`.
- Result: the `Capsules per month` numeral renders heavier than the 3 sibling rows; the eyebrow stays at `tracking-wide` and `text-[var(--color-identity)]`; no other change.

If the executor's visual preview (run `pnpm dev` and navigate to `/pert` -> Tube-Feed mode -> populate weight + formula + volume + medication + strength so the secondaries section renders) shows the bump as visually insufficient (e.g. `font-extrabold` reads only marginally heavier than `font-bold` at the chosen typeface scale), FALL BACK TO Approach C:
- Eyebrow: `tracking-wide` -> `tracking-wider` on the same row's `<div class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase">`.
- Numeral: `font-bold` -> `font-extrabold` (kept from Approach A).
- Both changes scoped to the SINGLE `Capsules per month` row; sibling rows untouched.

**STEP C -- Apply the edit.** Use the Edit tool. The specific edit (Approach A) is:

Old text (must match exactly, in the `Capsules per month` row only -- verify uniqueness with `grep -c 'class="num text-title font-bold text-\[var(--color-text-primary)\]"' src/lib/pert/PertCalculator.svelte`; if count > 1 the executor must include enough surrounding context in the Edit `old_string` to disambiguate, e.g. include the `Capsules per month` eyebrow text on the line above or the closing tags below):

```
            <!-- Capsules per month -->
            <div class="flex items-baseline justify-between px-5 py-4">
                <div>
                    <div
                        class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase"
                    >
                        Capsules per month
                    </div>
                    <div class="flex items-baseline gap-2">
                        <span class="num text-title font-bold text-[var(--color-text-primary)]">
                            {tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}
                        </span>
                        <span class="text-ui text-[var(--color-text-secondary)]">capsules</span>
                    </div>
                </div>
            </div>
```

New text (Approach A):

```
            <!-- Capsules per month (F-03: extra-bold to elevate prescribing artifact above derived siblings; Identity-Inside preserved). -->
            <div class="flex items-baseline justify-between px-5 py-4">
                <div>
                    <div
                        class="text-2xs font-semibold tracking-wide text-[var(--color-identity)] uppercase"
                    >
                        Capsules per month
                    </div>
                    <div class="flex items-baseline gap-2">
                        <span class="num text-title font-extrabold text-[var(--color-text-primary)]">
                            {tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}
                        </span>
                        <span class="text-ui text-[var(--color-text-secondary)]">capsules</span>
                    </div>
                </div>
            </div>
```

(Note: the comment update from `<!-- Capsules per month -->` to the F-03 annotated form is OPTIONAL; the executor may keep the original comment if preferred. The functional change is the `font-bold` -> `font-extrabold` swap on the `<span class="num ...">` line. If the executor adopts Approach C, ALSO swap `tracking-wide` -> `tracking-wider` on the eyebrow `<div>`.)

**STEP D -- Verify the change is scoped correctly.**

After editing, run:
```
grep -n "font-extrabold" src/lib/pert/PertCalculator.svelte
```
Expected: exactly 1 hit, on the `Capsules per month` numeral `<span>`. If 0 hits, the edit didn't land. If 2+ hits, the executor accidentally changed sibling rows -- revert and re-edit with disambiguating context.

Run:
```
grep -nE 'font-bold' src/lib/pert/PertCalculator.svelte
```
Expected (Approach A): the count drops by exactly 1 from the baseline (was 7 hits per the orchestrator's pre-task scan; now 6 hits -- the 3 sibling secondary rows in Tube-Feed branch + the 2 sibling rows in Oral branch + the bold-red advisory message all stay `font-bold`; only the `Capsules per month` numeral flipped to `font-extrabold`). If the count drops by more than 1 (e.g. 5 or fewer) the executor over-edited.

**STEP E -- Em-dash + en-dash sweep on the modified file.**

Run:
```
grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte
grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte
```
Expected: both return 0. If either returns > 0, the executor introduced a forbidden character (the workstream Q1 broad ban + Phase 2 D-19 + Phase 3.1 D-08 require both U+2014 and U+2013 = 0 across all PERT-route files); replace with ASCII hyphens or periods and re-run the sweep.

**STEP F -- Commit.** Stage `src/lib/pert/PertCalculator.svelte`. Commit with the plan-frontmatter `commit_message`: `fix(pert/04-02): F-03 capsules-per-month visual hierarchy bump (Wave 2 design polish)`. The commit body should record (a) which approach was chosen (A or C), (b) the line range edited, (c) any deviation from the recipe and its rationale.

**FORBIDDEN:**
- Editing any other file in this task. The only allowed file is `src/lib/pert/PertCalculator.svelte`.
- Changing the eyebrow color token `text-[var(--color-identity)]` to anything else (Identity-Inside Rule).
- Changing the visible text `Capsules per month` (Pitfall 4 selector preservation).
- Changing the numeral expression `{tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}` (Pitfall 4 selector preservation -- e2e/pert.spec.ts asserts on the rendered value).
- Editing the 3 sibling rows (Total fat / Total lipase / Lipase per kg). The hierarchy bump is asymmetric BY DESIGN: only the prescribing artifact (Capsules per month) gets the boost.
- Editing `src/lib/pert/PertInputs.svelte` (Phase 3.1 frozen).
- Editing `src/lib/pert/pert-config.json` (already committed at 2dc7ae2; re-editing dirties the diff).
- Touching any shared component or shell file (D-08b).
  </action>
  <verify>
    <automated>
      # 1. The fix landed exactly once.
      test "$(grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte)" -ge 1

      # 2. The fix is scoped to the Capsules per month row -- no sibling row was over-edited.
      # (Approach A: exactly 1 hit; Approach C: 1 hit on numeral.)
      test "$(grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte)" -eq 1

      # 3. The eyebrow color token was preserved (Identity-Inside Rule).
      grep -q 'text-2xs font-semibold tracking-wide\?r\? text-\[var(--color-identity)\] uppercase' src/lib/pert/PertCalculator.svelte

      # 4. The visible text "Capsules per month" was preserved (Pitfall 4 selector).
      grep -q 'Capsules per month' src/lib/pert/PertCalculator.svelte

      # 5. The numeral expression was preserved (Pitfall 4 selector).
      grep -q 'tubeFeedResult.capsulesPerMonth.toLocaleString' src/lib/pert/PertCalculator.svelte

      # 6. Em-dash + en-dash sweep on the modified file: both 0.
      test "$(grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte)" -eq 0
      test "$(grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte)" -eq 0

      # 7. Tabular-numerals (.num) class still on hero + secondaries (>=5 per UI-SPEC Gate 12).
      test "$(grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte)" -ge 5

      # 8. The plan only touches the allowed file (no shell, no shared, no other calculator).
      git diff --name-only HEAD -- src/ e2e/ | grep -vE '^src/lib/pert/PertCalculator\.svelte$' | wc -l | xargs test 0 -eq

      # 9. PertInputs.svelte byte-identical (Phase 3.1 frozen).
      test -z "$(git diff HEAD -- src/lib/pert/PertInputs.svelte)"

      # 10. pert-config.json byte-identical against HEAD (commit 2dc7ae2 already shipped; this plan does NOT re-edit).
      test -z "$(git diff HEAD -- src/lib/pert/pert-config.json)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` returns exactly 1 (the Capsules per month numeral; not present anywhere else in the file pre-fix).
    - The 3 sibling secondary rows (Total fat / Total lipase / Lipase per kg) still use `font-bold` on their numerals (verifiable: count of `class="num text-title font-bold text-\[var(--color-text-primary)\]"` matches drops by exactly 1 from the baseline of 4 to 3 in the Tube-Feed branch; oral branch's 2 secondary rows untouched).
    - The eyebrow on the Capsules per month row preserves the Identity-Inside Rule: `class="text-2xs font-semibold tracking-(wide|wider) text-[var(--color-identity)] uppercase"` (the only allowed change is `tracking-wide` -> `tracking-wider` if Approach C; color token must be `text-[var(--color-identity)]` unchanged).
    - The visible string `Capsules per month` is preserved verbatim (no rename, no abbreviation).
    - The numeral expression `{tubeFeedResult.capsulesPerMonth.toLocaleString('en-US')}` is preserved verbatim (no format change, no rounding tweak).
    - `grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte` returns 0 (em-dash count).
    - `grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte` returns 0 (en-dash count).
    - `grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte` returns >= 5 (UI-SPEC Gate 12; `.num` class still on every numeric output).
    - `git diff --name-only HEAD -- src/ e2e/` returns ONLY `src/lib/pert/PertCalculator.svelte` (no other source/test/config changed in this task).
    - `git diff HEAD -- src/lib/pert/PertInputs.svelte` is empty (Phase 3.1 frozen).
    - `git diff HEAD -- src/lib/pert/pert-config.json` is empty (commit 2dc7ae2 already in HEAD; not re-edited).
  </acceptance_criteria>
  <done>
    - The Capsules per month row in `src/lib/pert/PertCalculator.svelte` Tube-Feed branch renders heavier than the 3 sibling rows (Total fat / Total lipase / Lipase per kg) -- elevating the prescribing artifact above derived figures per UI-SPEC Watch Item 5.
    - The eyebrow color token is unchanged (Identity-Inside Rule honored); only typographic weight (and optionally tracking) changed.
    - The e2e selectors at `e2e/pert.spec.ts` still match (Pitfall 4 reminder honored; `Capsules per month` and the numeral expression both preserved).
    - The change is staged + committed with the plan-frontmatter `commit_message`.
    - Em-dash + en-dash count on PertCalculator.svelte = 0 each.
    - Negative-space audit clean: only `src/lib/pert/PertCalculator.svelte` is in `git diff --name-only HEAD~1..HEAD`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run quality gates (svelte-check 0/0; vitest src/lib/pert/ 81/81; em + en-dash sweep on PertCalculator.svelte = 0)</name>
  <read_first>
    - .planning/workstreams/pert/STATE.md (offset 28, limit 25 -- the Phase 3.1 Wave 3 outcomes paragraph documenting the 7-gate clinical baseline: svelte-check 0/0; vitest 425/425 with PERT subset 81/81; pnpm build OK; pert-a11y 4/4; pert.spec 12/12; full Playwright 117 + 1 baseline flake)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-CONTEXT.md (offset 36, limit 18 -- D-04 score acceptance gate; gate (3) "zero P1 findings remain unhandled")
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-UI-SPEC.md (offset 1, limit 100 -- §"Verification Checklist" / §"Pre-design-polish gates" -- the inherited 7-gate sequence Wave 2 must not regress)
    - src/lib/pert/PertCalculator.svelte (post-Task-1; verify the bump landed)
  </read_first>
  <files>(none -- this task runs commands and captures their output to /tmp/04-02-gates/; no source edits)</files>
  <action>
This task is verification-only. It runs the quality gates that Wave 3's 7-gate clinical sequence will re-run; the goal is to fail-fast at Wave 2 if Task 1 regressed any baseline.

**STEP A -- Create the gate-output directory.**

```
mkdir -p /tmp/04-02-gates
```

**STEP B -- Gate 1: svelte-check 0/0.**

```
pnpm exec svelte-check --no-tsconfig 2>&1 | tee /tmp/04-02-gates/gate-01-svelte-check.log
```

Acceptance: exit 0 AND output ends with `0 errors` AND `0 warnings`. The Phase 3.1 baseline is `~4586 files, 0 errors, 0 warnings`. If errors > 0 OR warnings > 0, Task 1 introduced a syntax or type regression -- revert Task 1's edit, investigate, re-apply with the fix.

**STEP C -- Gate 2: vitest src/lib/pert/ 81/81.**

```
pnpm test:run -- src/lib/pert/ 2>&1 | tee /tmp/04-02-gates/gate-02-vitest-pert.log
```

Acceptance: exit 0 AND test count = 81 (the PERT vitest subset: PertCalculator.test.ts 10 + PertInputs.test.ts 9 + calculations.test.ts 45 + config.test.ts 17 = 81; Phase 3.1 baseline is 425 total, of which the PERT subset is 81). If count != 81 OR any failure, Task 1 broke a test (most likely PertCalculator.test.ts which DOM-asserts on the secondary section). The likely culprit is a class-string change that broke a `getByText` or `querySelector` assertion. The fix: read the failing test's selector, confirm it doesn't query `font-bold` literally; if it does, that's a Phase 3.1 test bug surfaced by F-03 -- escalate to the user via Rule 4 (do NOT auto-edit the test; D-08b protects test infrastructure as a Phase 3.1 frozen artifact).

**STEP D -- Gate 3: em-dash + en-dash sweep on PertCalculator.svelte = 0/0.**

```
EMS=$(grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte)
ENS=$(grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte)
echo "em-dash count: $EMS" | tee /tmp/04-02-gates/gate-03-dash-sweep.log
echo "en-dash count: $ENS" | tee -a /tmp/04-02-gates/gate-03-dash-sweep.log
test "$EMS" -eq 0
test "$ENS" -eq 0
```

Acceptance: both 0. The workstream Q1 broad ban + Phase 2 D-19 + Phase 3.1 D-08 carve forbid both U+2014 and U+2013 in any PERT-route file. If either is non-zero, identify the offender (most likely an inadvertent comment or message string introduced by Task 1's edit), replace with ASCII hyphen or period, and re-commit (amend in this case is fine since the only commit so far in this plan is Task 1's).

**STEP E -- Gate 4: tabular-numerals preserved (UI-SPEC Gate 12 forward check).**

```
NUM_HITS=$(grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte)
echo "tabular-numerals .num class hits: $NUM_HITS" | tee /tmp/04-02-gates/gate-04-tabular-numerals.log
test "$NUM_HITS" -ge 5
```

Acceptance: count >= 5 (Phase 2 baseline is 9 hits; Wave 2 should not have changed `.num` class assignment, only `font-bold` -> `font-extrabold` on one of those 9 hits, so the count remains 9).

**STEP F -- Gate 5: function-binding bridges Phase 3.1 D-01 regression guard.**

```
BINDS=$(grep -cE 'bind:value=\{$' src/lib/pert/PertInputs.svelte)
PROXIES=$(grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte)
echo "function-binding bridges: $BINDS" | tee /tmp/04-02-gates/gate-05-bind-bridges.log
echo "string-bridge proxies: $PROXIES" | tee -a /tmp/04-02-gates/gate-05-bind-bridges.log
test "$BINDS" -ge 3
test "$PROXIES" -eq 0
```

Acceptance: BINDS >= 3 (Phase 3.1 baseline; one per medication / strength / formula picker) AND PROXIES = 0 (no string-bridge proxies re-introduced). Wave 2 does NOT touch PertInputs.svelte so this should pass trivially; the gate is a safety check that no rogue edit happened.

**STEP G -- Gate 6: pert-config.json byte-identical against HEAD (commit 2dc7ae2 acknowledgment).**

```
git log --oneline -1 src/lib/pert/pert-config.json | tee /tmp/04-02-gates/gate-06-config-history.log
test -z "$(git diff HEAD -- src/lib/pert/pert-config.json)"
```

Acceptance: the most recent commit touching `pert-config.json` is `2dc7ae2` (the out-of-band weight-range fix); `git diff HEAD -- src/lib/pert/pert-config.json` is empty (Wave 2 does NOT re-edit). The Wave 3 verifier will see commit 2dc7ae2 in the phase-level negative-space audit; this gate documents that Wave 2's audit trail acknowledges the prior commit without re-touching it.

**STEP H -- Gate 7: negative-space audit at the plan level.**

```
git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | tee /tmp/04-02-gates/gate-07-neg-space-plan.log
test "$(git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | wc -l)" -eq 1
git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | grep -q '^src/lib/pert/PertCalculator\.svelte$'
```

Acceptance: exactly 1 file in the plan's diff (Task 1's commit), and that file is `src/lib/pert/PertCalculator.svelte`. Anything else (e.g. PertInputs.svelte, pert-config.json, e2e specs, shell, shared) is a D-08 boundary violation -- STOP and escalate to the user.

**Note on running pnpm build / Playwright:** these heavy gates are deferred to Wave 3 (Plan 04-03 Task 2). Wave 2 only runs the cheap fast-feedback gates (svelte-check + PERT-subset vitest + greps); Wave 3 runs the full clinical gate sequence including pnpm build + pert-a11y + pert.spec + full Playwright + the 8-context FINAL critique sweep. This split is per UI-SPEC §"Pre-design-polish gates" + D-07 wave structure.

**STEP I -- Capture outputs.** All `/tmp/04-02-gates/gate-NN-*.log` files are referenced verbatim in Task 3's 04-02-SUMMARY.md as evidence pastes. Do NOT commit these tmp logs to git -- they are transient verification artifacts.
  </action>
  <verify>
    <automated>
      # Gate 1: svelte-check 0/0
      test -f /tmp/04-02-gates/gate-01-svelte-check.log
      grep -qE '^[[:space:]]*0\b.*errors' /tmp/04-02-gates/gate-01-svelte-check.log

      # Gate 2: vitest src/lib/pert/ 81/81
      test -f /tmp/04-02-gates/gate-02-vitest-pert.log
      grep -qE 'Tests[[:space:]]+81 passed' /tmp/04-02-gates/gate-02-vitest-pert.log

      # Gate 3: em-dash + en-dash 0/0 on PertCalculator.svelte
      test "$(grep -cP '\x{2014}' src/lib/pert/PertCalculator.svelte)" -eq 0
      test "$(grep -cP '\x{2013}' src/lib/pert/PertCalculator.svelte)" -eq 0

      # Gate 4: tabular-numerals >=5 hits
      test "$(grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte)" -ge 5

      # Gate 5: function-binding bridges >=3, string-bridge proxies = 0
      test "$(grep -cE 'bind:value=\{$' src/lib/pert/PertInputs.svelte)" -ge 3
      test "$(grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte)" -eq 0

      # Gate 6: pert-config.json byte-identical against HEAD (Wave 2 does NOT re-edit)
      test -z "$(git diff HEAD -- src/lib/pert/pert-config.json)"

      # Gate 7: plan-level negative-space audit -- exactly 1 file (PertCalculator.svelte)
      test "$(git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | wc -l)" -eq 1
      git diff --name-only HEAD~1..HEAD -- 'src/' 'e2e/' | grep -q '^src/lib/pert/PertCalculator\.svelte$'
    </automated>
  </verify>
  <acceptance_criteria>
    - Gate 1 (svelte-check): exit 0; output reports 0 errors AND 0 warnings (Phase 3.1 baseline preserved).
    - Gate 2 (vitest src/lib/pert/): exit 0; 81 tests pass; 0 fail. Matches Phase 3.1 baseline.
    - Gate 3 (em-dash + en-dash sweep): both `grep -cP '\x{2014}'` and `grep -cP '\x{2013}'` return 0 on `src/lib/pert/PertCalculator.svelte`.
    - Gate 4 (tabular-numerals): `grep -cE 'class="[^"]*\bnum\b' src/lib/pert/PertCalculator.svelte` returns >= 5 (UI-SPEC Gate 12).
    - Gate 5 (function-binding bridges): `grep -cE 'bind:value=\{$' src/lib/pert/PertInputs.svelte` returns >= 3; `grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte` returns 0.
    - Gate 6 (pert-config.json): `git diff HEAD -- src/lib/pert/pert-config.json` returns empty (commit 2dc7ae2 already in HEAD; Wave 2 does NOT re-edit).
    - Gate 7 (plan-level negative-space audit): `git diff --name-only HEAD~1..HEAD -- src/ e2e/` returns exactly 1 line: `src/lib/pert/PertCalculator.svelte`.
    - All 7 gate logs captured at `/tmp/04-02-gates/gate-NN-*.log` (transient; not committed; referenced in Task 3's SUMMARY).
  </acceptance_criteria>
  <done>
    - All 7 quality gates pass; outputs captured at `/tmp/04-02-gates/`.
    - Phase 3.1 invariants verified intact (function-binding bridges 3+ hits, zero string-bridge proxies, em-dash + en-dash 0/0 on PertCalculator.svelte).
    - PERT-subset vitest count 81/81 unchanged (Phase 3.1 baseline preserved).
    - Plan-level negative-space audit clean (only PertCalculator.svelte modified in Wave 2's commit).
    - pert-config.json byte-identical against HEAD (commit 2dc7ae2 acknowledged as audit-trail only; not re-edited).
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 04-02-SUMMARY.md (F-03 fix record + commit 2dc7ae2 acknowledgment + D-08b forced-defer carry-forward)</name>
  <read_first>
    - src/lib/pert/PertCalculator.svelte (post-Task-1; cite the actual line range edited and the before/after class string)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-FINDINGS.md (read F-03 row + the "Cross-calculator backlog seeded by Wave 1" section to copy F-01 + F-02 forward verbatim)
    - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-01-SUMMARY.md (read for the audit-trail format -- Wave 2's SUMMARY mirrors Wave 1's structure: header, what shipped, deviations, pitfall encounter notes, next step)
    - All 7 gate logs at /tmp/04-02-gates/gate-NN-*.log (Task 2's outputs; paste verbatim into the SUMMARY's "Quality gates" section)
    - git log --oneline -3 (capture the SHAs of: (a) Task 1's commit just shipped; (b) commit 2dc7ae2; (c) the Wave 1 plan-04-01 closure commit)
    - .planning/workstreams/pert/STATE.md (read the Phase 3.1 Wave 3 paragraph format; SUMMARY.md "Phase 4 Wave 2 outcomes" section format mirrors it)
  </read_first>
  <files>.planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md</files>
  <action>
Compose `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md` with the following structure (>=40 lines target). Use the Wave 1 SUMMARY (`04-01-SUMMARY.md`) as a structural reference but Wave 2 is much shorter (1 fix, no critique sweep, no PRODUCT.md drop).

**Required sections (in order):**

1. **Frontmatter** with: `phase: 4`, `plan: 2`, `subsystem: pert/design-polish-impeccable`, `workstream: pert`, `milestone: v1.15`, `wave: 2`, `tags: [pert, design-polish, gap-closure, wave-2]`, `dependency_graph` (requires Wave 1 + this plan; provides the F-03 fix + this SUMMARY), `key_files.modified: [src/lib/pert/PertCalculator.svelte]`, `decisions` (cite the chosen approach A/B/C + the commit 2dc7ae2 acknowledgment as in-phase scope + the D-08b forced-defer for F-01 + F-02), `metrics` (lines edited, gate counts, commit count).

2. **Header**: `# Phase 4 Plan 02 Summary: F-03 Capsules-per-month Visual Hierarchy Bump (Wave 2)` + `**Phase:** 04-design-polish-impeccable` + `**Plan:** 04-02 (Wave 2 -- gap closure)` + `**Generated:** {today}` + 1-paragraph executive summary citing F-03 closure + commit 2dc7ae2 in-phase acknowledgment + cross-calculator backlog seeded.

3. **What this plan shipped** (numbered list):
   - F-03 fix: line range edited; recipe chosen (A or C); before/after class strings pasted verbatim; commit SHA from Task 1.
   - 04-02-SUMMARY.md (this file).

4. **Out-of-band commit acknowledgment (commit 2dc7ae2)** -- a dedicated 1-paragraph subsection:
   - Commit SHA: 2dc7ae2.
   - Subject: `fix(pert): align weight slider range with other calculators (max 10 kg)`.
   - Author: Ghislain Séguin.
   - Date: 2026-04-25.
   - File touched: `src/lib/pert/pert-config.json` (1 line: `weightKg.max: 80 -> 10`).
   - In-phase rationale: aligns the PERT weight advisory with the cross-calculator pattern (feeds/gir/morphine/uac-uvc all use `weightKg: { min: 0.3, max: 10, step: 0.1 }`); calc-layer untouched (NumericInput min/max is advisory-only per v1.6; never auto-clamps); 81/81 PERT vitest preserved.
   - Phase 4 scope claim: this commit is part of Phase 4 design-polish phase, NOT scope creep; user-directed PERT-route fix mid-Wave-1 (after Wave 1 critique sweep started, before Wave 2 fix plan was authored). The Wave 3 verifier will see this commit in the phase-level negative-space audit (`git diff --name-only $BASELINE..HEAD -- src/ e2e/`) returning `src/lib/pert/PertCalculator.svelte` (Wave 2) + `src/lib/pert/pert-config.json` (commit 2dc7ae2) -- both within D-08 PERT-route allowlist.

5. **Quality gates pasted verbatim** from /tmp/04-02-gates/gate-NN-*.log (all 7):
   - Gate 1 svelte-check tail: `0 errors, 0 warnings`.
   - Gate 2 vitest src/lib/pert/: `Tests 81 passed (81)`.
   - Gate 3 em-dash + en-dash sweep: both 0.
   - Gate 4 tabular-numerals: count (>= 5).
   - Gate 5 function-binding bridges + string-bridge proxies: counts.
   - Gate 6 pert-config.json byte-identical against HEAD: empty diff confirmed.
   - Gate 7 plan-level negative-space audit: `src/lib/pert/PertCalculator.svelte` only.

6. **D-08b forced-defer for F-01 + F-02 (cross-calculator backlog seeded by Wave 1)** -- copy forward Wave 1's "Cross-calculator backlog seeded by Wave 1" section verbatim with a minor framing edit ("Wave 2 does NOT auto-fix these per Pitfall 5 + D-08b"):
   - F-01 (NumericInput en-dash hotfix): ~1 LOC change in `src/lib/shared/components/NumericInput.svelte:76`. Workstream Q1 broad ban violation. Recommended: file as v1.15.x hotfix (post-Phase-5 release) OR include in a v1.16 cross-calculator polish phase. Affects all 6 calculators by virtue of being in a shared component.
   - F-02 (NavShell sticky-overlay layering): ~5-15 LOC investigation in `src/lib/shell/NavShell.svelte`. Z-index or sticky-offset issue; affects all 6 calculators at desktop viewport. Recommended: v1.16 cross-calculator polish phase (not a release-blocker for v1.15 since the issue is mild and pre-existed Phase 4).
   - F-04, F-05 (P3 deferred per D-03 default): documented in 04-FINDINGS.md; no Wave 2 action.
   - Neither finding affects the v1.15 PERT release-readiness gate (no P1, no clinical-trust regression, no DESIGN.md named-rule violation).

7. **Phase 3.1 invariants regression guard** (paste from Gate 5):
   - function-binding bridges: 3+ hits in PertInputs.svelte.
   - string-bridge proxies: 0 hits in PertInputs.svelte.
   - em-dash count on PertCalculator.svelte: 0.
   - en-dash count on PertCalculator.svelte: 0.
   - tabular-numerals on PertCalculator.svelte: >= 5 hits.
   - STOP-red advisory carve-out preserved (`border-2 border-[var(--color-error)]` + `OctagonAlert` + `role="alert"` at lines ~310-322 untouched per Wave 2's plan-level negative-space audit).

8. **Negative-space audit** (paste from Gate 7):
   - `git diff --name-only HEAD~1..HEAD -- src/ e2e/` returns exactly: `src/lib/pert/PertCalculator.svelte`.
   - Phase-level audit forward note: when Wave 3 runs `git diff --name-only $BASELINE..HEAD -- src/ e2e/` (where `$BASELINE` is the parent commit of Wave 1), the result will be: `src/lib/pert/PertCalculator.svelte` (Wave 2) + `src/lib/pert/pert-config.json` (commit 2dc7ae2). Both within D-08 PERT-route allowlist. PRODUCT.md is at the repo root and is implicitly outside the `src/`+`e2e/` audit scope but explicitly acknowledged as the only repo-root D-08a addition (recorded in Wave 1's SUMMARY).

9. **Deviations from Plan** (mirroring Wave 1 SUMMARY's "Deviations" section):
   - List any deviation from the recipe (e.g. "chose Approach C instead of Approach A because the visual preview at `pnpm dev /pert` Tube-Feed showed Approach A as marginal at the chosen typeface scale").
   - Capture any auto-fixes during execution (svelte-check warnings introduced + reverted, etc.).
   - Auth gates: none expected (the workflow is fully agent-runnable).

10. **PERT-DESIGN-02..05 satisfaction mapping** (which requirement closure each artifact contributes to):
    - PERT-DESIGN-02 (P1 fixes ship before merge; cheap P2/P3 inline) -- F-03 fix is the cheap-inline P2 per D-03; satisfied by Task 1.
    - PERT-DESIGN-03 (DESIGN.md / DESIGN.json contract enforced -- Identity-Inside Rule, Eyebrow-Above-Numeral, Tabular-Numbers, etc.) -- Identity-Inside preserved (eyebrow color unchanged); Tabular-Numbers preserved (`.num` class hits >= 5); Eyebrow-Above-Numeral preserved (eyebrow `<div>` above numeral `<span>`).
    - PERT-DESIGN-04 (HeroResult above-the-fold + sticky InputDrawer -- Phase 42.1 inheritance, F-04 P3 deferred per D-03 default) -- already satisfied by Phase 2; F-04 deferred is documented in 04-FINDINGS.md.
    - PERT-DESIGN-05 (SegmentedToggle visual integration with identity hue) -- Phase 1 + Phase 2 already shipped; Wave 1 critique surfaced no SegmentedToggle finding (no F-rows cite the toggle); satisfied by inheritance.
    - Note: PERT-DESIGN-01 was satisfied by Wave 1 (critique-skill-run requirement). PERT-DESIGN-06 (>=35/40 score target) is satisfied by Wave 3 (Plan 04-03's re-run + score acceptance gate).

11. **Self-Check** section (mirrors Wave 1 SUMMARY's "Self-Check" section): all artifacts on disk, all gates green, all SUMMARY checklist items checked.

12. **Next step (the Wave 3 transition)**: the orchestrator runs `/gsd-execute-phase 4 --wave 3` (or `/gsd-execute-phase 4` if wave-aware) to execute Plan 04-03 (Wave 3 verification + clinical gate + Phase 4 closure).

**Em-dash + en-dash hygiene on the SUMMARY itself:** the workstream Q1 broad ban applies to this file too. After authoring, run:

```
grep -cP '\x{2014}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
grep -cP '\x{2013}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
```

Both must return 0. Use `--` (double ASCII hyphen) for any em-dash equivalent in prose; use `-` (ASCII hyphen) for any en-dash equivalent.

**Stage + commit:** stage `04-02-SUMMARY.md` and amend Task 1's commit (or create a separate commit `docs(pert/04-02): summary -- F-03 + commit 2dc7ae2 acknowledgment + D-08b forced-defer carry-forward`). Either form works; the orchestrator's plan-checker accepts both per the GSD commit policy.
  </action>
  <verify>
    <automated>
      # 1. SUMMARY file exists and is >= 40 lines.
      test -f .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      test "$(wc -l < .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md)" -ge 40

      # 2. Required content markers present.
      grep -q "F-03" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      grep -q "2dc7ae2" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      grep -q "D-08b" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      grep -qE "(F-01|NumericInput)" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      grep -qE "(F-02|NavShell)" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      grep -q "Phase 3.1" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      grep -q "PERT-DESIGN-02" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      grep -q "PertCalculator.svelte" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      grep -qE "Capsules per month" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md

      # 3. Em-dash + en-dash sweep on the SUMMARY itself: both 0.
      test "$(grep -cP '\x{2014}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md)" -eq 0
      test "$(grep -cP '\x{2013}' .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md)" -eq 0

      # 4. Quality gate evidence pasted.
      grep -qE "(0 errors|0 warnings)" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md
      grep -qE "81 passed" .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md

      # 5. SUMMARY committed (or amended into Task 1's commit).
      test -n "$(git log --oneline -3 -- .planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md)"
    </automated>
  </verify>
  <acceptance_criteria>
    - 04-02-SUMMARY.md exists at `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md` with >= 40 lines.
    - F-03 fix record present: line range, approach chosen (A or C), before/after class strings, commit SHA.
    - Commit 2dc7ae2 acknowledged with one-line context (cross-calculator alignment; calc-layer untouched; in-phase scope claim).
    - D-08b forced-defer carry-forward present for F-01 (NumericInput en-dash) + F-02 (NavShell sticky-overlay) with cross-calculator backlog rationale.
    - Phase 3.1 invariants regression-guard pasted (function-binding 3+ hits, em-dash + en-dash 0/0, tabular-numerals >= 5).
    - All 7 Task-2 gate outputs pasted verbatim (svelte-check, vitest, em+en-dash, tabular, function-bindings, config-history, neg-space).
    - PERT-DESIGN-02..05 satisfaction mapping documented.
    - Em-dash + en-dash count on the SUMMARY itself: 0 each (workstream Q1 broad ban honored).
    - SUMMARY committed (either as a separate commit or amended into Task 1's commit per GSD commit policy).
  </acceptance_criteria>
  <done>
    - 04-02-SUMMARY.md authored with all 12 required sections (frontmatter, header, what shipped, commit 2dc7ae2 acknowledgment, quality gates, D-08b forced-defer carry-forward, Phase 3.1 invariants, negative-space audit, deviations, PERT-DESIGN satisfaction mapping, self-check, next step).
    - Em-dash + en-dash count on SUMMARY = 0 each.
    - Commit landed; `git log -- 04-02-SUMMARY.md` returns at least 1 commit.
    - Wave 3 (Plan 04-03) is unblocked: VERIFICATION.md will read 04-02-SUMMARY.md as the audit trail for the F-03 fix + commit 2dc7ae2 acknowledgment + D-08b forced-defer carry-forward.
  </done>
</task>

</tasks>

<verification>
**Phase-level verification is Wave 3's responsibility (Plan 04-03).** Wave 2's verification scope is the 7 fast-feedback gates run in Task 2 + the SUMMARY content-marker checks run in Task 3.

The Wave 3 verifier (Plan 04-03 Task 2 + Task 3) will:

1. Re-run all 7 Wave-2 gates as part of the 18-gate verification sequence.
2. Run the 8-context FINAL `/impeccable` critique sweep against the post-Wave-2 UI; verify per-context >= 35/40 in >= 6/8 + aggregate >= 35/40 holds.
3. Run pnpm build + pert-a11y 4/4 + pert.spec 12/12 + full Playwright 117 + 1 baseline flake (the heavy gates Wave 2 deferred).
4. Run AUDIT.sh against the post-fix code; expect exit 0 (Identity-Inside Rule + Tabular-Numbers + Eyebrow-Above-Numeral + Five-Roles-Only + 11px-Floor + Flat-Card-Default all pass).
5. Compose 04-VERIFICATION.md as the phase-level audit trail; reads Wave 2's 04-02-SUMMARY.md to inherit the F-03 fix record + commit 2dc7ae2 acknowledgment + D-08b forced-defer carry-forward.

If Wave 2's plan-level gates fail (e.g. svelte-check non-zero or vitest count != 81 or em-dash + en-dash sweep > 0 or negative-space audit dirty), Wave 3 cannot start until Wave 2 re-passes. The Task 2 gate sequence is fail-fast for exactly this reason.
</verification>

<success_criteria>
- F-03 fix shipped: 1-3 LOC bump on `Capsules per month` row in `src/lib/pert/PertCalculator.svelte`; eyebrow color preserved (Identity-Inside Rule); visible text + numeral expression preserved (Pitfall 4 selector preservation); 1 commit landed with the plan-frontmatter `commit_message`.
- All 7 plan-level quality gates green: svelte-check 0/0; vitest src/lib/pert/ 81/81; em-dash + en-dash sweep 0/0 on PertCalculator.svelte; tabular-numerals >= 5 hits; function-binding bridges >= 3 hits; string-bridge proxies = 0; pert-config.json byte-identical against HEAD; plan-level negative-space audit clean (only `src/lib/pert/PertCalculator.svelte` modified).
- 04-02-SUMMARY.md authored at `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md` with all 12 required sections including the commit 2dc7ae2 in-phase acknowledgment and the D-08b forced-defer carry-forward for F-01 + F-02.
- D-08 PERT-route allowlist honored: only `src/lib/pert/PertCalculator.svelte` (and the planning artifact 04-02-SUMMARY.md) modified by this plan.
- D-08b strictly-forbidden boundary honored: zero shared component / NavShell / HamburgerMenu / other-calculator / DESIGN.md / DESIGN.json / calc-layer edits.
- Phase 3.1 invariants preserved: function-binding bridges intact, zero string-bridge proxies, em-dash + en-dash 0/0 across PERT-route files, STOP-red advisory carve-out untouched.
- Wave 3 (Plan 04-03) is unblocked and ready to execute via `/gsd-execute-phase 4 --wave 3`.
</success_criteria>

<output>
This plan ships:

1. `src/lib/pert/PertCalculator.svelte` -- F-03 fix: 1-3 LOC visual-hierarchy bump on the Tube-Feed `Capsules per month` row (`font-bold` -> `font-extrabold`, with optional `tracking-wide` -> `tracking-wider` on the eyebrow). PERT-route only; Identity-Inside Rule honored.

2. `.planning/workstreams/pert/phases/04-design-polish-impeccable/04-02-SUMMARY.md` -- Wave 2 closure record covering the F-03 fix + commit 2dc7ae2 acknowledgment + D-08b forced-defer carry-forward for F-01 + F-02 + Phase 3.1 invariants regression guard + PERT-DESIGN-02..05 satisfaction mapping.

After commit, the next agent runs `/gsd-execute-phase 4 --wave 3` (or the orchestrator's wave-aware equivalent) to execute Plan 04-03 (Wave 3 verification + clinical gate + Phase 4 closure with PERT-DESIGN-01..06 validated).
</output>
</content>
