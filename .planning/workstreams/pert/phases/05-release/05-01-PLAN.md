---
phase: 5
plan: 1
type: execute
wave: 1
workstream: pert
milestone: v1.15
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
  - 04-05
  - 04-06
files_modified:
  - package.json
  - .planning/workstreams/pert/PROJECT.md
  - .planning/workstreams/pert/REQUIREMENTS.md
  - .planning/workstreams/pert/ROADMAP.md
  - .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md
  - .planning/workstreams/pert/phases/04-design-polish-impeccable/.continue-here.md
  - __capture.mjs
  - .playwright-mcp/
  - 04-04-uat-light-desktop-tube-feed.png
  - 04-05-uat-oral-d12.png
  - 04-05-uat-tube-feed-d13.png
  - 04-06-uat-oral-helper.png
  - 04-06-uat-tube-feed-no-helper.png
  - 04-uat-oral-unchanged.png
  - 04-uat-tube-feed-f03.png
  - pert-after-f03-verify.png
requirements:
  - PERT-REL-01
  - PERT-REL-02
  - PERT-REL-03
  - PERT-REL-04
  - PERT-REL-05
autonomous: true
gap_closure: false
tags: [pert, release, v1-15-0, version-bump, clinical-gate, workstream-closure, project-md-foldback, orphan-cleanup]
must_haves:
  truths:
    - "D-01 + D-04 honored: `package.json` line 3 transitions from `\"version\": \"1.13.0\"` to `\"version\": \"1.15.0\"` via a SINGLE-line edit (jumping past v1.14, which is reserved for the in-flight main Kendamil milestone). NO other source/test/config/css/svelte file is edited by Task 3 (the version-bump task). pnpm-lock.yaml may be rewritten by `pnpm install` as a mechanical consequence per D-04a; if so the resulting diff is allowed in the bump commit (documented in 05-01-SUMMARY.md). NO edit to `vite.config.ts`, NO edit to `src/lib/shared/about-content.ts` (the AboutSheet auto-flows via the `__APP_VERSION__` Vite-define -- already wired and verified at 05-CONTEXT.md D-04: `vite.config.ts:11` defines `__APP_VERSION__: JSON.stringify(pkg.version)`; `about-content.ts:13` consumes it via `appVersion = ...`; all 6 calculator entries (lines 22, 34, 47, 60, 72, 83) use `version: appVersion`)."
    - "D-04 verification ladder: post-bump, `grep '\"version\"' package.json` returns exactly the line `  \"version\": \"1.15.0\",`; `pnpm dev` is started briefly; the dev server is polled until ready; the executor navigates to `/pert`, opens AboutSheet, and visually confirms the version string renders as `v1.15.0` (the executor SHOULD attempt this via Playwright-MCP if available; if Playwright-MCP is not available, the executor records that visual confirmation falls to the user at code review and documents which calculators were spot-checked headlessly via `curl http://localhost:5173/pert` + the AboutSheet route inspection); `pnpm dev` is then torn down. Visual confirmation evidence (screenshot path or DOM inspection note) is recorded verbatim in 05-01-SUMMARY.md under 'Version-bump evidence'."
    - "D-08 cleanup honored EXACTLY: Task 1 deletes the listed orphan files via `git rm` (or `git rm -r` for directories) and commits with the precise message `chore(pert): clean orphan UAT debug artifacts before v1.15.0 release`. Files DELETED: `.planning/workstreams/pert/phases/04-design-polish-impeccable/.continue-here.md` (stale post-Wave-6); `__capture.mjs` at the workstream root (Wave 1 ad-hoc Playwright capture script); `.playwright-mcp/` directory at the workstream root (orchestrator UAT cache, recursive); the 8 orchestrator UAT debug PNGs at the workstream root (`pert-after-f03-verify.png`, `04-uat-tube-feed-f03.png`, `04-uat-oral-unchanged.png`, `04-04-uat-light-desktop-tube-feed.png`, `04-05-uat-oral-d12.png`, `04-05-uat-tube-feed-d13.png`, `04-06-uat-oral-helper.png`, `04-06-uat-tube-feed-no-helper.png`). Files KEPT (audit trail per D-08): all 8 04-CRITIQUE-*.md Wave 1 transcripts; all 8 04-CRITIQUE-FINAL-*.md Wave 3 transcripts; all PLAN + SUMMARY files for phases 1, 2, 3, 3.1, 4, 5; all CONTEXT, RESEARCH, DISCUSSION-LOG, UAT, VERIFICATION canonical phase artifacts."
    - "D-03 clinical gate is the **7-gate sequence** (NOT the 18-gate Phase 4 sequence; Phase 4 already certified the visual/design surface at aggregate 36.25/40 with all 6 PERT-DESIGN requirements Validated; re-running the 8-context /impeccable sweep would add 15-20 minutes without producing new evidence per 05-CONTEXT.md D-03 rationale). The 7 gates are: (1) `pnpm exec svelte-check` returns 0 errors / 0 warnings; (2) `pnpm test:run` returns 425/425 passing; (3) `pnpm build` succeeds AND production bundle is within +/- 5 KiB of the post-Wave-6 baseline (the executor records the baseline at execute time by reading the most recent Phase 4 SUMMARY's 'Phase 4 vs Phase 3.1 baseline deltas' bundle line -- per 04-03-SUMMARY.md frontmatter `bundle_kib_phase_4: 575.99` -- and computes the delta); (4) `CI=1 pnpm exec playwright test pert-a11y` returns 4/4 passing; (5) `CI=1 pnpm exec playwright test pert.spec` returns 12/12 passing; (6) `CI=1 pnpm exec playwright test` (full suite) returns 117 passing + 1 baseline flake on `disclaimer-banner.spec.ts:28` (this matches Phase 1/2/3/3.1/4 precedent verbatim per STATE.md Phase 4 closure paragraph item 6 and 04-03-SUMMARY.md `full_playwright_phase_4: 117`; the executor DOCUMENTS the flake as expected and DOES NOT block on it -- the gate PASSES if and only if the failures count is 1 AND the failing spec is `disclaimer-banner.spec.ts:28`); (7) **PERT-route invariants** (the goal-backward Phase 4 byte-identity preservation gate): `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` = 2 (Wave 4 Tube-Feed eyebrow + Wave 5 Oral eyebrow per 05-CONTEXT.md D-03 line 46); `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` = 2 (Wave 2 Tube-Feed numeral + Wave 5 Oral numeral); helper text `Used for the 10,000 units/kg/day safety check, not the dose calculation.` present in `src/lib/pert/PertInputs.svelte` exactly 1 time; function-binding bridges 3 hits in `src/lib/pert/PertInputs.svelte` via `grep -cE '\\(v: string\\) =>' src/lib/pert/PertInputs.svelte` = 3 (the 3 SelectPicker setter shapes at the formula/medication/strength bridges); string-bridge proxies 0 via `grep -cE 'let \\w+(Bridge|Proxy) = \\$state' src/lib/pert/PertInputs.svelte` = 0 (Phase 3.1 D-01 invariant preserved); em-dash count 0 across the 4 PERT-route source files (`grep -cP '\\x{2014}' src/lib/pert/PertCalculator.svelte src/lib/pert/PertInputs.svelte src/lib/pert/pert-config.json src/routes/pert/+page.svelte` returns `0` for each); en-dash count 0 across the same 4 files (`grep -cP '\\x{2013}'` returns `0` for each, with the documented exception that `pert-config.json:137 invalidLipaseRate` carries a pre-existing en-dash that is OUT OF SCOPE per 05-CONTEXT.md D-10 cross-calculator backlog -- the executor SHOULD document this as `0` for Calculator/Inputs/+page and acknowledge the pre-existing 1 hit on pert-config.json:137 per the Phase 4 Wave 3 Gate 11 backlog note in 04-03-SUMMARY.md without blocking on it); pert-config.json byte-identical with Wave 1 commit `2dc7ae2` via `git diff 2dc7ae2 -- src/lib/pert/pert-config.json` returns empty; `src/lib/pert/calculations.ts` byte-identical with Phase 2 (the calc layer is frozen post-Phase-2; the Phase 4 waves all touched UI surfaces only) -- the executor verifies via `git log --oneline src/lib/pert/calculations.ts | head -5` and confirms no commits land between Phase 2's commit `f7683bd` (or its successor) and HEAD that touch the file. Gate 7 is verification-only with NO source diff."
    - "D-04a strict allowlist for the version-bump commit: only `package.json` (and possibly `pnpm-lock.yaml` if pnpm rewrites it as a mechanical consequence) is in Task 3's diff. The pre-commit invariant: `git diff --name-only HEAD~1..HEAD` after Task 3 returns at most `package.json` + `pnpm-lock.yaml`. If any other file shows up in the diff, the executor STOPS and reports the violation as a gap-closure trigger."
    - "D-05 + D-07 workstream-local artifact updates (Task 4): the canonical-record file at the workstream is `PROJECT.md` (NOT `MILESTONES.md` -- the executor verified at 05-CONTEXT.md gather time: only `PROJECT.md` exists at `.planning/workstreams/pert/`). PROJECT.md gets a v1.15 closure section appended (mirroring the v1.13 main-repo Phase 43 release commit pattern -- 6 v1.X bullets at the same depth as existing Validated entries) covering: (a) all 5 PERT-REL-01..05 flipped Active -> Validated with milestone tag `(v1.15.0 Phase 5)`; (b) cross-reference to all previously-Validated requirement IDs (PERT-ARCH-01..07, PERT-HUE-01..03, PERT-DATA-01..04, PERT-ORAL-01..08, PERT-TUBE-01..07, PERT-MODE-01..04, PERT-SAFE-01..04, PERT-TEST-01..06, PERT-DESIGN-01..06; total 48 IDs already Validated post-Phase-3.1 + Phase 4); (c) list of all 6 phase closures (Phase 1, 2, 3, 3.1, 4, 5) with completion dates + commit refs (extracted from STATE.md Phase 4 closure paragraph + the 4 Phase 3.1 plan SUMMARYs + the Phase 1/2/3 SUMMARYs in `.planning/workstreams/pert/phases/01-*`/`02-*`/`03-*`); (d) open backlog at workstream completion: F-01 NumericInput en-dash + F-02 NavShell sticky-overlay + `pert-config.json:137` invalidLipaseRate en-dash (cross-calculator backlog seeded by Phase 4 Wave 4 SUMMARY; explicitly out of v1.15 scope per 05-CONTEXT.md D-10 + REQUIREMENTS.md Future Requirements section); plus the post-Wave-6 weight-in-Oral UX evolution past Option 3 (also documented as out of scope per 05-CONTEXT.md D-10); (e) workstream-completion handoff note pointing at `/gsd-workstreams complete pert` with the exact command sequence the user runs from the main repo after Kendamil v1.14 ships. REQUIREMENTS.md gets PERT-REL-01..05 flipped `[ ]` -> `[x]` in the v1.15 Requirements section (lines 89-93 in REQUIREMENTS.md HEAD) and the traceability table rows for PERT-REL-01 through PERT-REL-05 flipped from Active to Validated (lines 173-177 in REQUIREMENTS.md HEAD). ROADMAP.md gets the Progress table (lines 113-120 in ROADMAP.md HEAD) all rows flipped to Complete with completion dates (Phase 1, 2, 3 inherit dates from their respective SUMMARYs; Phase 3.1, 4 already Complete with 2026-04-26; Phase 5 gets `[x] **Phase 5: Release** | 1/1 | Complete | 2026-04-26` with summary text mentioning v1.15.0 release readiness); the user's manual edit to the top `## Phases` list at lines 17-23 (the `[ ]` markers for Phase 1/2/3) is the user's display preference and MUST NOT be reverted -- the canonical state is the Progress table, NOT the Phases header section. Commit message exactly: `docs(pert/05): close workstream — v1.15.0 release readiness` (em-dash NOT used in commit message body to honor workstream Q1 broad ban; the en-dash in the commit subject is acceptable per Phase 4 commit precedent and per orchestrator commit-message acceptability)."
    - "D-06 boundary respected: Phase 5 does NOT edit `.planning/PROJECT.md` (the main project's PROJECT.md), `.planning/STATE.md` (the main project's STATE.md), `.planning/ROADMAP.md` (the main project's ROADMAP.md), `CLAUDE.md`, `DESIGN.md`, `DESIGN.json`, OR any other root-level file outside `package.json` (D-04 single-file scope) and the orphan cleanup list (D-08). The main-repo PROJECT.md fold-back is OUT OF SCOPE -- the workstream-completion handler `/gsd-workstreams complete pert` is the canonical mechanism for that fold-back and is invoked from the main repo AFTER `pert` merges to main, AFTER Kendamil v1.14 ships (per D-02). Phase 5 records all the data the completion handler will need (in 05-01-SUMMARY.md's 'Workstream-completion handoff' section + the workstream-local PROJECT.md update) but does NOT touch main."
    - "Phase 4 byte-identity preserved on ALL PERT-route source files: `src/lib/pert/PertCalculator.svelte` (Wave 5 D-12 Oral hierarchy 3-token swap from commit `f4565b4` + Wave 4 Approach C eyebrow from commit `d82aa30` + Wave 2 F-03 numeral from commit `29306e7` -- all byte-identical against HEAD); `src/lib/pert/PertInputs.svelte` (Wave 6 D-14 helper text from commit `b0fe1a0` + Phase 3.1 D-01 function-binding bridges from commit `f2da16d` -- all byte-identical against HEAD); `src/lib/pert/pert-config.json` (Phase 4 commit `2dc7ae2` byte-identical); `src/lib/pert/calculations.ts` (Phase 2 frozen); `src/lib/pert/state.svelte.ts` (Phase 2 frozen); `src/lib/pert/types.ts` (Phase 2 frozen); `src/lib/pert/config.ts` (Phase 2 frozen); `src/routes/pert/+page.svelte` (Phase 1 frozen). After Task 3's bump commits, `git diff HEAD -- src/lib/pert/ src/routes/pert/` returns empty (the bump commit edits ONLY `package.json`)."
    - "No new tests written; no test files modified. Task 2's clinical gate is verification-only (zero source diff). Task 3 modifies only `package.json` (and possibly `pnpm-lock.yaml`). Tasks 1, 4, 5 modify only planning artifacts under `.planning/workstreams/pert/` and the orphan-cleanup target list at the workstream root. NO edits to `e2e/`, `src/lib/pert/*.test.ts`, `src/lib/shared/components/`, OR any other test/source location."
    - "Workstream-completion handoff command documented verbatim in 05-01-SUMMARY.md per 05-CONTEXT.md D-06 + the `<specifics>` section recommendation: the SUMMARY's 'Workstream-completion handoff' section spells out the exact command sequence the user runs from the main repo after Kendamil v1.14 ships, of the form: `cd /mnt/data/src/nicu-assistant && git fetch origin workspace/pert && git merge workspace/pert && git worktree remove /home/ghislain/gsd-workspaces/pert/nicu-assistant && /gsd-workstreams complete pert` (the executor reads `/mnt/data/src/gsd-workspaces/pert/WORKSPACE.md` at execute time -- ABSOLUTE PATH; the file lives ONE directory above the worktree root, OUTSIDE the worktree's git tree. If the file is unreadable from the worktree for any reason, the executor MUST fall back to deriving the values from `git remote get-url origin` (main repo URL -> resolve to local path), `git rev-parse --show-toplevel` (worktree path), and `git rev-parse --abbrev-ref HEAD` (branch name = `workspace/pert`); the executor embeds the RESOLVED values verbatim in the SUMMARY -- never path placeholders)."
    - "`05-01-SUMMARY.md` is composed in Task 5 with min 50 lines covering: (1) frontmatter (phase: 5; plan: 1; type: execute; wave: 1; workstream: pert; milestone: v1.15; depends_on: [04-01..04-06]; files_modified mirroring this PLAN's frontmatter; metrics block with completion date 2026-04-26 + tasks_completed 5 + commits 4); (2) what this plan shipped (cleanup + clinical gate + version bump + workstream-local closure + summary); (3) PERT-REL-01..05 satisfaction mapping (each REQ-ID -> verifying task + verifying commit hash); (4) clinical-gate evidence paste (verbatim outputs from /tmp/05-release-gates/gate-NN-*.log files; svelte-check version line; vitest summary line; build PWA precache line; pert-a11y summary; pert.spec summary; full Playwright summary; PERT-route invariants grep counts); (5) version-bump evidence (package.json before/after diff; AboutSheet visual confirmation evidence verbatim); (6) Phase 5 vs Phase 4 deltas (LOC delta = 0 in src/ except package.json mechanical bump; vitest unchanged at 425; Playwright unchanged at 117/118 + baseline flake; bundle size delta within +/- 5 KiB recorded as the actual number); (7) out-of-scope items per 05-CONTEXT.md D-10 (main repo merge, GitHub release, cross-calculator backlog F-01/F-02/pert-config.json:137 en-dash, weight-in-Oral UX past Option 3); (8) workstream-completion handoff command sequence verbatim; (9) em-dash + en-dash count 0/0 across the SUMMARY itself (the SUMMARY uses ASCII characters only; the workstream Q1 broad ban applies to all newly-authored prose in this milestone)."
  artifacts:
    - path: package.json
      provides: "Post-bump root manifest with version field bumped from 1.13.0 to 1.15.0 per D-01 + D-04. Single-line edit at line 3 (the `\"version\":` field). All other fields (name, private, packageManager, type, scripts, devDependencies, dependencies) byte-identical pre-and-post Task 3."
      contains: "\"version\": \"1.15.0\""
    - path: .planning/workstreams/pert/PROJECT.md
      provides: "Post-Phase-5 workstream PROJECT.md with v1.15 closure section appended per D-05. Contains: (a) all 5 PERT-REL-01..05 IDs with `Validated` markers and milestone tag `(v1.15.0 Phase 5)`; (b) cross-reference list of 48 previously-Validated PERT-* IDs (ARCH 7 + HUE 3 + DATA 4 + ORAL 8 + TUBE 7 + MODE 4 + SAFE 4 + TEST 6 + DESIGN 6 + REL 5 = 54 total minus 5 just-flipped = 48 cross-referenced); (c) all 6 phase closure entries with dates + commit refs; (d) open backlog at workstream completion (F-01, F-02, pert-config.json:137 en-dash, weight-in-Oral UX past Option 3); (e) workstream-completion handoff command sequence."
      contains: "v1.15"
      contains: "PERT-REL-01"
      contains: "PERT-REL-05"
      contains: "Phase 5"
      contains: "/gsd-workstreams complete pert"
    - path: .planning/workstreams/pert/REQUIREMENTS.md
      provides: "Post-Phase-5 REQUIREMENTS.md with PERT-REL-01..05 flipped `[ ]` -> `[x]` in the v1.15 Requirements section (lines 89-93) and the traceability table rows for PERT-REL-01 through PERT-REL-05 flipped from Active to Validated (lines 173-177). All other rows byte-identical (PERT-DESIGN-01..06 stay Validated; PERT-TEST-05 stays Validated; ARCH/HUE/DATA/ORAL/TUBE/MODE/SAFE/TEST stay in their post-Phase-3.1 states)."
      contains: "[x] **PERT-REL-01**"
      contains: "[x] **PERT-REL-05**"
      contains: "| PERT-REL-01 | Phase 5 | Validated |"
    - path: .planning/workstreams/pert/ROADMAP.md
      provides: "Post-Phase-5 ROADMAP.md with all Progress table rows (lines 113-120) flipped to Complete with completion dates per D-07. Phase 5 row reads `[x] **Phase 5: Release** | 1/1 | Complete | 2026-04-26` with a brief summary mentioning v1.15.0 release readiness. The user's manual edit to the top `## Phases` list at lines 17-23 (the `[ ]` markers for Phase 1/2/3) is PRESERVED -- NOT reverted -- per the user's display preference per 05-CONTEXT.md D-07."
      contains: "Complete"
      contains: "2026-04-26"
      contains: "Phase 5"
    - path: .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md
      provides: "Phase 5 closure record (min 50 lines; em-dash + en-dash 0/0). Sections per the must_haves bullet on this artifact: frontmatter; what shipped; PERT-REL-01..05 satisfaction mapping; clinical-gate evidence verbatim paste; version-bump evidence; Phase 5 vs Phase 4 deltas; out-of-scope items; workstream-completion handoff. Authored at Task 5 with separate commit `docs(pert/05-01): summary — workstream pert v1.15.0 release-ready` per the Phase 4 SUMMARY-commit pattern."
      contains: "v1.15.0"
      contains: "PERT-REL-01"
      contains: "PERT-REL-05"
      contains: "clinical gate"
      contains: "/gsd-workstreams complete pert"
      min_lines: 50
  key_links:
    - from: package.json
      to: src/lib/shared/about-content.ts
      via: "vite.config.ts:11 defines `__APP_VERSION__: JSON.stringify(pkg.version)` at build time; `src/lib/shared/about-content.ts:13` consumes the define via `appVersion = \\`v${typeof __APP_VERSION__ !== 'undefined' ? __APP_VERSION__ : '0.0.0'}\\``; all 6 calculator entries (lines 22, 34, 47, 60, 72, 83) reference `version: appVersion`; therefore the bump in package.json line 3 propagates automatically to every AboutSheet entry without requiring an edit to about-content.ts. Task 3's verification step navigates to /pert + opens AboutSheet to visually confirm v1.15.0 renders."
      pattern: "__APP_VERSION__"
    - from: .planning/workstreams/pert/PROJECT.md
      to: .planning/workstreams/pert/REQUIREMENTS.md
      via: "Task 4 updates both atomically in a single commit. PROJECT.md's v1.15 closure section references PERT-REL-01..05 by their REQ-IDs; REQUIREMENTS.md flips the same 5 IDs from `[ ]` to `[x]` and updates the traceability table from Active to Validated. Both artifacts must be in sync at HEAD; if a future verifier reads one without the other, the cross-references must resolve."
      pattern: "PERT-REL-"
    - from: .planning/workstreams/pert/ROADMAP.md
      to: .planning/workstreams/pert/STATE.md
      via: "ROADMAP.md Progress table (lines 113-120) is the canonical phase-completion record; STATE.md tracks workstream-level position via the `progress` block in frontmatter (`completed_phases` field). Task 4 flips ROADMAP.md Progress rows; Task 5 SUMMARY references the post-Phase-5 STATE.md state implicitly. Note: 05-CONTEXT.md does NOT require Phase 5 to edit STATE.md (the optional STATE.md update is the orchestrator's job per the Wave-6 + Wave-3 closure pattern in Phase 4); the planner respects this and EXCLUDES STATE.md from files_modified for Task 4."
      pattern: "Phase 5"
    - from: .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md
      to: /mnt/data/src/gsd-workspaces/pert/WORKSPACE.md
      via: "The SUMMARY's 'Workstream-completion handoff' section documents the exact `/gsd-workstreams complete pert` command sequence the user runs from the main repo. The executor reads `/mnt/data/src/gsd-workspaces/pert/WORKSPACE.md` (ABSOLUTE PATH; ONE directory above the worktree root, OUTSIDE the worktree's git tree -- this is NOT `.planning/workstreams/WORKSPACE.md` which does not exist) at Task 5 time to extract the verified main-repo path + worktree path + branch name, and embeds those resolved values verbatim in the SUMMARY (no path placeholders). FALLBACK: if `/mnt/data/src/gsd-workspaces/pert/WORKSPACE.md` is unreadable, derive from `git remote get-url origin` (main repo) + `git rev-parse --show-toplevel` (worktree) + `git rev-parse --abbrev-ref HEAD` (branch); embed the resolved values verbatim either way."
      pattern: "/gsd-workstreams complete pert"
    - from: .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md
      to: .planning/workstreams/pert/phases/04-design-polish-impeccable/04-03-SUMMARY.md
      via: "The SUMMARY's 'Phase 5 vs Phase 4 deltas' section grounds its baseline against 04-03-SUMMARY.md frontmatter (`bundle_kib_phase_4: 575.99`, `vitest_count_phase_4: 425`, `pert_a11y_phase_4: 4`, `pert_spec_phase_4: 12`, `full_playwright_phase_4: 117`); the executor confirms each Phase 5 gate baseline equals the recorded Phase 4 baseline, with the bundle delta computed and recorded as the actual number."
      pattern: "bundle_kib_phase_4"
---

<objective>
Ship workstream `pert` v1.15.0 release-readiness: clean orphan UAT debug artifacts; run a 7-gate clinical gate WITHOUT visual /impeccable re-run (Phase 4 already certified the visual surface); bump `package.json` from v1.13.0 to v1.15.0 (D-01 + D-04); update workstream-local PROJECT.md / REQUIREMENTS.md / ROADMAP.md to record the closure (D-05 + D-07); compose 05-01-SUMMARY.md with the workstream-completion handoff to `/gsd-workstreams complete pert` (D-06). The actual main-repo merge happens AFTER Kendamil v1.14 ships, via the workstream-completion handler invoked from the main repo (D-02 + D-06).

Purpose: Phase 5 is the mechanical bookend. Phase 1 + 2 closed architecture + calc layer; Phase 3 + 3.1 closed the test surface; Phase 4 closed visual + functional polish at aggregate 36.25/40 with all 6 PERT-DESIGN requirements Validated. Phase 5 ships the version + the closure record so `pert` is ready for the main-repo merge after Kendamil ships.

Output:
- `package.json` at v1.15.0 (single-line edit; AboutSheet auto-flows via __APP_VERSION__).
- 7-gate clinical gate evidence at `/tmp/05-release-gates/gate-NN-*.log` (verification-only; no source diff).
- `.planning/workstreams/pert/PROJECT.md` with v1.15 closure section appended.
- `.planning/workstreams/pert/REQUIREMENTS.md` with PERT-REL-01..05 flipped Active -> Validated.
- `.planning/workstreams/pert/ROADMAP.md` Progress table all rows flipped to Complete.
- `.planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md` with the closure record + workstream-completion handoff.
- 4 commits: `chore(pert): clean orphan UAT debug artifacts before v1.15.0 release` + `chore: bump version to v1.15.0` + `docs(pert/05): close workstream — v1.15.0 release readiness` + `docs(pert/05-01): summary — workstream pert v1.15.0 release-ready`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/pert/PROJECT.md
@.planning/workstreams/pert/ROADMAP.md
@.planning/workstreams/pert/STATE.md
@.planning/workstreams/pert/REQUIREMENTS.md
@.planning/workstreams/pert/phases/05-release/05-CONTEXT.md
@.planning/workstreams/pert/phases/05-release/05-DISCUSSION-LOG.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-03-SUMMARY.md
@.planning/workstreams/pert/phases/04-design-polish-impeccable/04-06-SUMMARY.md
@package.json
@vite.config.ts
@src/lib/shared/about-content.ts

<interfaces>
<!-- Key contracts the executor uses without codebase exploration. Embedded for ZERO scavenger-hunt overhead. -->

### Version-flow wiring (already complete; Phase 5 only bumps the source value)

`package.json` (line 3 -- the SINGLE edit site for Task 3):

```json
{
  "name": "nicu-assistant",
  "private": true,
  "version": "1.13.0",            // <-- Task 3 edits this to "1.15.0"
  "packageManager": "pnpm@10.33.0",
  ...
}
```

`vite.config.ts` (line 10-12 -- read-only verification):

```ts
define: {
  __APP_VERSION__: JSON.stringify(pkg.version)
}
```

`src/lib/shared/about-content.ts` (lines 1-19 -- read-only verification):

```ts
import type { CalculatorId } from './types.js';

declare const __APP_VERSION__: string;

interface AboutContent {
  title: string;
  version: string;
  description: string;
  notes: string[];
  disclaimer: string;
}

const appVersion = `v${typeof __APP_VERSION__ !== 'undefined' ? __APP_VERSION__ : '0.0.0'}`;
```

All 6 calculator entries in `aboutContent` (morphine-wean line 22, formula line 34, gir line 47, feeds line 60, uac-uvc line 72, pert line 83) use `version: appVersion`. Task 3's bump propagates to all 6 automatically -- no edit to `about-content.ts`.

### PERT-route invariants (Gate 7 of the clinical gate -- verification-only grep counts)

`src/lib/pert/PertCalculator.svelte` (read-only; HEAD commit `b0fe1a0` -- Wave 6 -- byte-identical):
- Line 216: `class="text-2xs font-semibold tracking-wider text-[var(--color-identity)] uppercase"` (Wave 5 D-12 Oral eyebrow)
- Line 221: `<span class="num text-title font-extrabold text-[var(--color-text-primary)]">` (Wave 5 D-12 Oral numeral)
- Line 288: `class="text-2xs font-semibold tracking-wider text-[var(--color-identity)] uppercase"` (Wave 4 Approach C Tube-Feed eyebrow)
- Line 293: `<span class="num text-title font-extrabold text-[var(--color-text-primary)]">` (Wave 2 F-03 Tube-Feed numeral)
- `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` MUST equal `2`.
- `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` MUST equal `2`.

`src/lib/pert/PertInputs.svelte` (read-only; HEAD commit `b0fe1a0` -- Wave 6 -- byte-identical):
- Helper text at lines 125-129: the D-14 mode-conditional sibling `<p>` per Wave 6.
- Function-binding bridges at lines 161-164 (Formula), 196-199 (Medication), 205-208 (Strength).
- `grep -c 'Used for the 10,000 units/kg/day safety check, not the dose calculation.' src/lib/pert/PertInputs.svelte` MUST equal `1`.
- `grep -cE '\(v: string\) =>' src/lib/pert/PertInputs.svelte` MUST equal `3` (Phase 3.1 D-01 invariant; the 3 SelectPicker setters).
- `grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte` MUST equal `0` (zero string-bridge proxies).

`src/lib/pert/pert-config.json` (read-only; Wave 1 commit `2dc7ae2`):
- `git diff 2dc7ae2 -- src/lib/pert/pert-config.json` MUST return empty (byte-identical).
- KNOWN exception: line 137 `validationMessages.invalidLipaseRate` carries a pre-existing en-dash (U+2013) per 04-03-SUMMARY.md Gate 11 backlog note; OUT OF SCOPE for v1.15 per 05-CONTEXT.md D-10.

`src/lib/pert/calculations.ts` (read-only; frozen post-Phase-2):
- `git log --oneline src/lib/pert/calculations.ts` last commits should be Phase 2 era (no Phase 4 wave commits).

### PROJECT.md / REQUIREMENTS.md / ROADMAP.md edit sites

`.planning/workstreams/pert/PROJECT.md` (Task 4 -- append v1.15 closure section):
- Format pattern: 6 v1.X bullets + Validated list cross-reference + 6 phase-closure entries + open-backlog list + handoff command. Mirror the depth + structure of Phase 4 closure entries already in the file.

`.planning/workstreams/pert/REQUIREMENTS.md` (Task 4 -- 5 line edits + 5 traceability rows):
- Lines 89-93 (the v1.15 Release section): flip `- [ ]` -> `- [x]` for PERT-REL-01..05; mirror the `- [x] **PERT-TEST-05**: ... -- **FULL closure 2026-04-26 ...**` pattern from line 75 for the closure metadata format.
- Lines 173-177 (the traceability table): flip the `Status` column for PERT-REL-01 through PERT-REL-05 from `Active` to `Validated`.

`.planning/workstreams/pert/ROADMAP.md` (Task 4 -- Progress table flip; PRESERVE user's `## Phases` edit):
- Lines 17-23 (`## Phases` list with `[ ]` for Phase 1/2/3): PRESERVE byte-identical (user's display preference per 05-CONTEXT.md D-07).
- Lines 113-120 (the canonical Progress table): flip Phase 1, Phase 2, Phase 3 rows from `Not started` to `Complete` with their respective completion dates (extract from each phase's last SUMMARY); Phase 3.1 + Phase 4 already Complete with 2026-04-26; Phase 5 row gets `| 5. Release | 1/1 | Complete | 2026-04-26 |` with a one-line summary text mentioning v1.15.0 release readiness.

### Orphan cleanup target list (Task 1)

DELETE (D-08 list -- exact paths):
- `.planning/workstreams/pert/phases/04-design-polish-impeccable/.continue-here.md` (stale; pre-Wave-2 paused-pre-execution handoff)
- `__capture.mjs` (workstream root; Wave 1 ad-hoc Playwright capture)
- `.playwright-mcp/` (workstream root; orchestrator UAT cache; recursive)
- `pert-after-f03-verify.png` (workstream root; Phase 4 Wave 4 UAT debug)
- `04-uat-tube-feed-f03.png` (workstream root)
- `04-uat-oral-unchanged.png` (workstream root)
- `04-04-uat-light-desktop-tube-feed.png` (workstream root)
- `04-05-uat-oral-d12.png` (workstream root)
- `04-05-uat-tube-feed-d13.png` (workstream root)
- `04-06-uat-oral-helper.png` (workstream root)
- `04-06-uat-tube-feed-no-helper.png` (workstream root)

KEEP (audit trail per D-08):
- All 8 04-CRITIQUE-*.md Wave 1 transcripts under `.planning/workstreams/pert/phases/04-design-polish-impeccable/`
- All 8 04-CRITIQUE-FINAL-*.md Wave 3 transcripts (same dir)
- All PLAN + SUMMARY files for phases 1, 2, 3, 3.1, 4, 5
- All CONTEXT, RESEARCH, DISCUSSION-LOG, UAT, VERIFICATION canonical phase artifacts (FINDINGS, DETECT, AUDIT.sh, UI-SPEC also kept under Phase 4 dir)
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Cleanup orphan UAT debug artifacts (D-08)</name>
  <files>
    .planning/workstreams/pert/phases/04-design-polish-impeccable/.continue-here.md,
    __capture.mjs,
    .playwright-mcp/,
    pert-after-f03-verify.png,
    04-uat-tube-feed-f03.png,
    04-uat-oral-unchanged.png,
    04-04-uat-light-desktop-tube-feed.png,
    04-05-uat-oral-d12.png,
    04-05-uat-tube-feed-d13.png,
    04-06-uat-oral-helper.png,
    04-06-uat-tube-feed-no-helper.png
  </files>
  <action>
    Per 05-CONTEXT.md D-08 (workstream root cleanup; phase-4 stale `.continue-here.md` removal). The workstream has accumulated orchestrator UAT debug artifacts during Phase 4 Waves 4/5/6; Phase 5 cleans them before the bump commit so the bump diff is mechanical.

    Step 1: From the workstream root (`/home/ghislain/gsd-workspaces/pert/nicu-assistant`), confirm each cleanup target exists. The orchestrator already verified all 11 targets exist at planning-context-gather time (see `<interfaces>` "Orphan cleanup target list"). Run `ls -la __capture.mjs .playwright-mcp/ *.png` once at the workstream root and confirm all PNG names match the D-08 list. Run `ls -la .planning/workstreams/pert/phases/04-design-polish-impeccable/.continue-here.md` and confirm the stale file exists.

    Step 2: Remove via `git rm -rf` (handles the `.playwright-mcp/` directory recursively):

    ```
    git rm -rf .playwright-mcp/
    git rm __capture.mjs \
      pert-after-f03-verify.png \
      04-uat-tube-feed-f03.png \
      04-uat-oral-unchanged.png \
      04-04-uat-light-desktop-tube-feed.png \
      04-05-uat-oral-d12.png \
      04-05-uat-tube-feed-d13.png \
      04-06-uat-oral-helper.png \
      04-06-uat-tube-feed-no-helper.png \
      .planning/workstreams/pert/phases/04-design-polish-impeccable/.continue-here.md
    ```

    NOTE: if `git rm` reports any of these files as untracked (i.e., not in git's index even though present on disk), use `rm -rf` instead for the affected file/directory and proceed. The `.playwright-mcp/` cache in particular may be untracked; the same applies to debug PNGs created during orchestrator UAT runs that were never `git add`-ed. Document any untracked-vs-tracked distinction in the commit body.

    Step 3: Verify the cleanup landed cleanly: `git status` should show all 11 targets in the staged-deletion list (or removed from the working tree if untracked). Run `ls __capture.mjs 2>&1` and `ls .playwright-mcp/ 2>&1` and `ls *.png 2>&1` and `ls .planning/workstreams/pert/phases/04-design-polish-impeccable/.continue-here.md 2>&1` -- all four MUST return "No such file or directory" or empty.

    Step 4: Confirm the KEEP list is intact via `ls .planning/workstreams/pert/phases/04-design-polish-impeccable/04-CRITIQUE-*.md | wc -l` -- MUST return `8` (the 8 Wave 1 transcripts; the Wave 3 FINAL transcripts are matched by `04-CRITIQUE-FINAL-*` and excluded from this glob); `ls .planning/workstreams/pert/phases/04-design-polish-impeccable/04-CRITIQUE-FINAL-*.md | wc -l` MUST return `8`. Confirm `.planning/workstreams/pert/phases/04-design-polish-impeccable/{04-CONTEXT,04-RESEARCH,04-DISCUSSION-LOG,04-UAT,04-VERIFICATION,04-FINDINGS,04-UI-SPEC,04-DETECT,04-AUDIT}` all exist (mix of `.md`, `.json`, `.sh`).

    Step 5: Commit the cleanup with EXACT subject line `chore(pert): clean orphan UAT debug artifacts before v1.15.0 release` (use the heredoc form per the planner's CRITICAL_RULES on git commits). Body should briefly enumerate what was removed (11 files including 1 directory) and reference 05-CONTEXT.md D-08. Co-Authored-By trailer per the standard pattern.
  </action>
  <verify>
    <automated>[ "$(pwd -P)" = "$(git rev-parse --show-toplevel)" ] && test ! -f __capture.mjs && test ! -d .playwright-mcp && test ! -f pert-after-f03-verify.png && test ! -f 04-uat-tube-feed-f03.png && test ! -f 04-uat-oral-unchanged.png && test ! -f 04-04-uat-light-desktop-tube-feed.png && test ! -f 04-05-uat-oral-d12.png && test ! -f 04-05-uat-tube-feed-d13.png && test ! -f 04-06-uat-oral-helper.png && test ! -f 04-06-uat-tube-feed-no-helper.png && test ! -f .planning/workstreams/pert/phases/04-design-polish-impeccable/.continue-here.md && test $(ls .planning/workstreams/pert/phases/04-design-polish-impeccable/04-CRITIQUE-*.md 2>/dev/null | grep -v FINAL | wc -l) -eq 8 && test $(ls .planning/workstreams/pert/phases/04-design-polish-impeccable/04-CRITIQUE-FINAL-*.md 2>/dev/null | wc -l) -eq 8 && git log -1 --pretty=%s | grep -q '^chore(pert): clean orphan UAT debug artifacts before v1\.15\.0 release$' && echo OK</automated>
  </verify>
  <done>
    All 11 orphan targets removed (10 files + 1 directory). 8 Wave 1 transcripts + 8 Wave 3 FINAL transcripts intact (KEEP list preserved). Commit landed with exact subject `chore(pert): clean orphan UAT debug artifacts before v1.15.0 release`. Working tree clean except for the staged deletions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the 7-gate clinical gate (D-03; verification-only)</name>
  <files></files>
  <action>
    Per 05-CONTEXT.md D-03: run the 7-gate sequence WITHOUT visual /impeccable re-run. Capture evidence at `/tmp/05-release-gates/gate-NN-*.log`. STOP on any failure. This task has ZERO source diff.

    Step 0: Setup. Create the evidence directory: `mkdir -p /tmp/05-release-gates`. Run from the workstream root (`/home/ghislain/gsd-workspaces/pert/nicu-assistant`). Confirm `pnpm --version` returns a 10.x version matching `package.json` `packageManager` field.

    Gate 1 -- svelte-check: `pnpm exec svelte-check 2>&1 | tee /tmp/05-release-gates/gate-01-svelte-check.log`. PASS criterion: the final summary line reads `0 errors, 0 warnings` (the file count typically reports ~4586 files; the count is informational, not a gate). If errors or warnings appear, STOP and record the failure as a gap-closure trigger in the executor's notes; do NOT proceed to Gate 2.

    Gate 2 -- vitest: `pnpm test:run 2>&1 | tee /tmp/05-release-gates/gate-02-vitest.log`. PASS criterion: the final summary line reads `Tests <N> passed (<N>)` where `<N>` is exactly `425` (the Phase 4 baseline; matches 04-03-SUMMARY.md `vitest_count_phase_4: 425`). If `<N>` differs from 425, STOP and document the delta.

    Gate 3 -- pnpm build: `pnpm build 2>&1 | tee /tmp/05-release-gates/gate-03-build.log`. PASS criterion: exit 0 AND the PWA precache line reports a bundle size within +/- 5 KiB of 575.99 KiB (the Phase 4 baseline; 04-03-SUMMARY.md `bundle_kib_phase_4: 575.99`). The line in build output looks like `PWA v1.x.x precache <N> entries (<X> KiB)` -- `<X>` MUST satisfy `570.99 <= <X> <= 580.99`. Record the actual `<X>` value in the executor's notes; the SUMMARY references it. If outside the +/- 5 KiB tolerance, STOP and document the delta.

    Gate 4 -- pert-a11y: `CI=1 pnpm exec playwright test pert-a11y 2>&1 | tee /tmp/05-release-gates/gate-04-pert-a11y.log`. PASS criterion: the summary reads `4 passed`.

    Gate 5 -- pert.spec: `CI=1 pnpm exec playwright test pert.spec 2>&1 | tee /tmp/05-release-gates/gate-05-pert-spec.log`. PASS criterion: the summary reads `12 passed`.

    Gate 6 -- targeted disclaimer-banner Playwright run (NARROWED from full-suite per the planner's Warning-4 fix): `CI=1 pnpm exec playwright test e2e/disclaimer-banner.spec.ts 2>&1 | tee /tmp/05-release-gates/gate-06-disclaimer-banner.log`. Rationale: Phase 5's source diff is `package.json` line 3 only (no test selectors, no component code). Gates 4 + 5 already cover the entire PERT-route surface (pert-a11y 4/4 + pert.spec 12/12). The full-suite Playwright run (~5 minutes for 118 tests) only adds value as a sanity check on the existing `disclaimer-banner.spec.ts:28` baseline flake; running JUST that spec (~30 seconds) confirms the flake reproduces as expected. PASS criterion: the targeted run reports `1 passed, 1 failed` (the file has 2 tests; one passes, the `disclaimer-banner.spec.ts:28` test is the established baseline flake from Phase 1/2/3/3.1/4 per STATE.md Phase 4 closure paragraph item 6 + 04-03-SUMMARY.md `full_playwright_phase_4: 117`). EXPECTED OUTCOME (DOCUMENTED, NOT A FAILURE): the `disclaimer-banner.spec.ts:28` flake reproduces; if it does NOT reproduce (i.e., `2 passed, 0 failed`), record that as `2 passed, 0 failed` and treat as PASS-with-note (the flake may have flaked-out on this run). If the failure count is > 1 OR the failing spec is anything other than `disclaimer-banner.spec.ts:28`, STOP and document the delta -- that is evidence of a regression introduced post-Phase-4. If the executor wants additional confidence, the FULL-SUITE run is OPTIONAL and may be invoked as `CI=1 pnpm exec playwright test 2>&1 | tee /tmp/05-release-gates/gate-06-full-OPTIONAL.log`; the optional log is NOT required for Gate 6 PASS.

    Gate 7 -- PERT-route invariants. This gate is structured as 9 grep-based assertions (verification-only; no commit). Capture each assertion's output at `/tmp/05-release-gates/gate-07-invariants.log`. Run from the workstream root:

    ```
    {
      echo "=== Gate 7.1: tracking-wider count in PertCalculator.svelte (expect 2) ==="
      grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte
      echo "=== Gate 7.2: font-extrabold count in PertCalculator.svelte (expect 2) ==="
      grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte
      echo "=== Gate 7.3: helper text in PertInputs.svelte (expect 1) ==="
      grep -c 'Used for the 10,000 units/kg/day safety check, not the dose calculation.' src/lib/pert/PertInputs.svelte
      echo "=== Gate 7.4: function-binding bridges in PertInputs.svelte (expect 3) ==="
      grep -cE '\(v: string\) =>' src/lib/pert/PertInputs.svelte
      echo "=== Gate 7.5: string-bridge proxies in PertInputs.svelte (expect 0) ==="
      grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte
      echo "=== Gate 7.6: em-dash sweep across 4 PERT-route files (expect 0/0/0/0) ==="
      for f in src/lib/pert/PertCalculator.svelte src/lib/pert/PertInputs.svelte src/lib/pert/pert-config.json src/routes/pert/+page.svelte; do
        printf '%s: ' "$f"; grep -cP '\x{2014}' "$f"
      done
      echo "=== Gate 7.7: en-dash sweep across 4 PERT-route files (expect 0 for Calculator/Inputs/+page; 1 known on pert-config.json:137 -- backlog item) ==="
      for f in src/lib/pert/PertCalculator.svelte src/lib/pert/PertInputs.svelte src/lib/pert/pert-config.json src/routes/pert/+page.svelte; do
        printf '%s: ' "$f"; grep -cP '\x{2013}' "$f"
      done
      echo "=== Gate 7.8: pert-config.json byte-identical with Wave 1 commit 2dc7ae2 (expect empty diff) ==="
      git diff 2dc7ae2 -- src/lib/pert/pert-config.json
      echo "=== Gate 7.9: calculations.ts byte-identical with Phase 2 (expect last-commit hash matches Phase 2 era; no Wave 4/5/6 commits) ==="
      git log --oneline src/lib/pert/calculations.ts | head -3
    } | tee /tmp/05-release-gates/gate-07-invariants.log
    ```

    PASS criteria:
    - Gate 7.1: `2`. Gate 7.2: `2`. Gate 7.3: `1`. Gate 7.4: `3`. Gate 7.5: `0`.
    - Gate 7.6: all four files report `0`.
    - Gate 7.7: PertCalculator.svelte `0`, PertInputs.svelte `0`, +page.svelte `0`, pert-config.json `1` (the pre-existing en-dash on line 137; documented as out-of-scope per 05-CONTEXT.md D-10).
    - Gate 7.8: empty diff (zero output between the heading and the next heading).
    - Gate 7.9: the last 3 commits do NOT include any `78686f9`/`d82aa30`/`f4565b4`/`618a66f`/`b0fe1a0`/`a5ed7d7` (Phase 4 wave commits) -- the file is frozen post-Phase-2.

    If ANY assertion fails (mismatch on the expected count or non-empty diff on 7.8 or unexpected commit on 7.9), STOP and document the failure. The version bump in Task 3 only proceeds if all 7 gates PASS.

    Step Final: Echo a one-line summary at `/tmp/05-release-gates/SUMMARY.txt` of the form `Gate 1: PASS (0/0); Gate 2: PASS (425); Gate 3: PASS (X.XX KiB); Gate 4: PASS (4); Gate 5: PASS (12); Gate 6: PASS (disclaimer-banner targeted run: 1 passed + 1 baseline flake on :28; OR 2 passed if flake did not reproduce); Gate 7: PASS (all 9 invariants).` This text is later quoted verbatim in Task 5's SUMMARY.
  </action>
  <verify>
    <automated>[ "$(pwd -P)" = "$(git rev-parse --show-toplevel)" ] && test -f /tmp/05-release-gates/gate-01-svelte-check.log && grep -q '0 errors' /tmp/05-release-gates/gate-01-svelte-check.log && test -f /tmp/05-release-gates/gate-02-vitest.log && grep -qE 'Tests +425 passed' /tmp/05-release-gates/gate-02-vitest.log && test -f /tmp/05-release-gates/gate-03-build.log && grep -qE 'PWA.*precache.*KiB' /tmp/05-release-gates/gate-03-build.log && test -f /tmp/05-release-gates/gate-04-pert-a11y.log && grep -qE '4 passed' /tmp/05-release-gates/gate-04-pert-a11y.log && test -f /tmp/05-release-gates/gate-05-pert-spec.log && grep -qE '12 passed' /tmp/05-release-gates/gate-05-pert-spec.log && test -f /tmp/05-release-gates/gate-06-disclaimer-banner.log && grep -qE '[12] passed' /tmp/05-release-gates/gate-06-disclaimer-banner.log && test -f /tmp/05-release-gates/gate-07-invariants.log && test -f /tmp/05-release-gates/SUMMARY.txt && test "$(grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte)" -eq 2 && test "$(grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte)" -eq 2 && test "$(grep -c 'Used for the 10,000 units/kg/day safety check, not the dose calculation.' src/lib/pert/PertInputs.svelte)" -eq 1 && test "$(grep -cE '\(v: string\) =>' src/lib/pert/PertInputs.svelte)" -eq 3 && test "$(grep -cE 'let \w+(Bridge|Proxy) = \$state' src/lib/pert/PertInputs.svelte)" -eq 0 && test -z "$(git diff 2dc7ae2 -- src/lib/pert/pert-config.json)" && echo OK</automated>
  </verify>
  <done>
    All 7 gates green. Evidence at `/tmp/05-release-gates/gate-NN-*.log` with one-line summary at `/tmp/05-release-gates/SUMMARY.txt`. Working tree byte-identical pre and post (verification-only; `git status` returns clean). Bundle size recorded as a specific KiB value within +/- 5 KiB of 575.99. Phase 4 PERT-route invariants confirmed byte-identical at HEAD.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bump package.json from 1.13.0 to 1.15.0 (D-01 + D-04)</name>
  <files>package.json</files>
  <action>
    Per 05-CONTEXT.md D-01 + D-04 + D-04a: SINGLE-FILE source edit. Jump from 1.13.0 directly to 1.15.0 (skipping v1.14, which is reserved for the in-flight main Kendamil milestone per D-02). NO edit to `vite.config.ts`, `src/lib/shared/about-content.ts`, or any other file -- the AboutSheet auto-flows via the `__APP_VERSION__` Vite-define.

    Step 1: Edit `package.json` line 3 from `  "version": "1.13.0",` to `  "version": "1.15.0",` -- preserve EXACTLY the leading 2-space indent + the trailing comma. Use the Edit tool with `old_string: "  \"version\": \"1.13.0\","` and `new_string: "  \"version\": \"1.15.0\","`. Do NOT use Write (the rest of the file must be byte-identical).

    Step 2: Verify the edit landed cleanly: `grep '"version"' package.json` MUST return exactly `  "version": "1.15.0",`. Confirm the remainder of `package.json` is byte-identical via `git diff package.json` -- the diff MUST show only the single line change at line 3 (one `-` line and one `+` line; no other context modified).

    Step 3: Run `pnpm install` to allow pnpm to rewrite `pnpm-lock.yaml` if it chooses (per 05-CONTEXT.md D-04a -- this is allowed in the bump commit because it's a mechanical consequence of the version field change, not a manual edit). After `pnpm install` completes, run `git status`. If `pnpm-lock.yaml` shows up in the diff, that's expected and allowed (document the diff size in the SUMMARY). If any OTHER file shows up in the diff (e.g., `node_modules/.bin/...`, `.pnpm/...`, `package.json` showing additional changes beyond line 3), STOP and document the violation -- the D-04a strict allowlist is breached.

    Step 4: Verify AboutSheet auto-flow. Start the dev server in the background: `pnpm dev` (run in background). Poll until ready by checking `curl -s -o /dev/null -w "%{http_code}" http://localhost:5173/pert` returns `200` (poll up to 30 seconds; 1-second intervals). Once ready, attempt visual confirmation:
    - PRIMARY PATH: if Playwright-MCP is available, use it to navigate to `http://localhost:5173/pert`, click the AboutSheet trigger (the `Info` icon in the page header or wherever the AboutSheet entry sits per the existing PertCalculator layout), and capture a screenshot saved at `/tmp/05-release-gates/about-sheet-v1.15.0.png`. The screenshot MUST visibly contain the text `v1.15.0`.
    - FALLBACK PATH: if Playwright-MCP is NOT available, fetch the rendered `/pert` route HTML via `curl -s http://localhost:5173/pert > /tmp/05-release-gates/pert-route.html`, then run a headless DOM probe via `pnpm exec playwright test --grep "AboutSheet" 2>&1 | tee /tmp/05-release-gates/aboutsheet-probe.log` (if any AboutSheet test exists in `e2e/`); ALTERNATIVELY, if no programmatic AboutSheet check exists, document in 05-01-SUMMARY.md the EXACT phrase `visual confirmation deferred to user code review` (verbatim, lowercase, no punctuation variation -- the Task 3 verify gate greps for this exact string as one of the three acceptable evidence trails) and record the SHA of the built `about-content.ts` chunk that contains `appVersion`. Either of the three evidence trails counts as PASS for the Task 3 visual-evidence gate: (a) `/tmp/05-release-gates/about-sheet-v1.15.0.png` exists (Playwright-MCP path); (b) `/tmp/05-release-gates/aboutsheet-probe.log` exists (programmatic-fallback path); (c) the SUMMARY contains the verbatim phrase `visual confirmation deferred to user code review` (user-deferred path). Either path is acceptable; the SUMMARY must record which path was taken and the evidence captured.

    Tear down the dev server (kill the backgrounded `pnpm dev` process). Record the dev-server port + the path each evidence file was written to in the executor's notes.

    Step 5: Commit with EXACT subject line `chore: bump version to v1.15.0` (NOTE: the prefix is plain `chore:` per 05-CONTEXT.md Claude's-Discretion section + the v1.13 main-repo precedent commit `9a86738` `feat(43-01): bump v1.13.0 ...` -- but for v1.15.0 the planner uses the more conservative `chore:` prefix matching 05-CONTEXT.md D-09 Task 3 verbatim). Body should briefly note: (a) the +1 (or +2 if pnpm-lock.yaml landed) file diff scope; (b) the AboutSheet auto-flow verification evidence path; (c) the version jump rationale (skipped v1.14 per D-01 + D-02 since v1.14 is reserved for in-flight main Kendamil). Co-Authored-By trailer per the standard pattern. Stage ONLY `package.json` (and `pnpm-lock.yaml` if it changed) -- do NOT use `git add -A` or `git commit -a` (D-04a strict allowlist).
  </action>
  <verify>
    <automated>[ "$(pwd -P)" = "$(git rev-parse --show-toplevel)" ] && grep -q '"version": "1.15.0"' package.json && ! grep -q '"version": "1.13.0"' package.json && git log -1 --pretty=%s | grep -qx 'chore: bump version to v1.15.0' && [ "$(git diff HEAD~1 --name-only | grep -vE '^(package\.json|pnpm-lock\.yaml)$' | wc -l)" -eq 0 ] && ( test -f /tmp/05-release-gates/about-sheet-v1.15.0.png || test -f /tmp/05-release-gates/aboutsheet-probe.log || ( test -f .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md && grep -q 'visual confirmation deferred to user code review' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md ) ) && echo OK</automated>
  </verify>
  <done>
    `package.json` at v1.15.0; `pnpm-lock.yaml` allowed (and recorded if rewritten); no other file modified by this task. Commit landed with exact subject `chore: bump version to v1.15.0`. AboutSheet auto-flow verification evidence captured at `/tmp/05-release-gates/about-sheet-v1.15.0.png` (Playwright-MCP path) OR documented as user-review-required (fallback path) with the path noted in the SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update workstream-local PROJECT.md + REQUIREMENTS.md + ROADMAP.md (D-05 + D-07)</name>
  <files>
    .planning/workstreams/pert/PROJECT.md,
    .planning/workstreams/pert/REQUIREMENTS.md,
    .planning/workstreams/pert/ROADMAP.md
  </files>
  <action>
    Per 05-CONTEXT.md D-05 + D-07: update the three workstream-local artifacts to record the v1.15 closure. Main project files (`/.planning/PROJECT.md`, `/.planning/STATE.md`, `/.planning/ROADMAP.md`, `CLAUDE.md`, `DESIGN.md`, etc.) are NOT touched per D-06.

    Step 1: Append v1.15 closure section to `.planning/workstreams/pert/PROJECT.md`. Read the file first; then locate the `## Evolution` section (currently at lines 67-77 in HEAD) and INSERT a new `## v1.15 Closure (Pediatric EPI PERT Calculator -- 2026-04-26)` section immediately BEFORE `## Evolution`. The section MUST contain:

    ```
    ## v1.15 Closure (Pediatric EPI PERT Calculator -- 2026-04-26)

    Workstream `pert` v1.15.0 ships the sixth NICU Assistant calculator: Pediatric EPI PERT (Oral + Tube-Feed modes), parity-tested within ~1% epsilon against `epi-pert-calculator.xlsx` Pediatric tabs, integrated into the registry, navigation, AboutSheet, and PWA shell with zero regressions to the five shipped calculators (Morphine, Formula, GIR, Feeds, UAC/UVC) or the v1.13 favorites store / hamburger menu.

    ### Newly Validated requirement IDs (v1.15.0 Phase 5)

    - PERT-REL-01 -- `package.json` bumped to v1.15.0; AboutSheet auto-flows via `__APP_VERSION__` Vite-define
    - PERT-REL-02 -- AboutSheet reflects `v1.15.0` for all 6 calculator entries (no `about-content.ts` edit needed)
    - PERT-REL-03 -- workstream PROJECT.md Validated list updated; main `.planning/PROJECT.md` fold-back deferred to `/gsd-workstreams complete pert` per 05-CONTEXT.md D-06
    - PERT-REL-04 -- workstream ROADMAP.md Progress rows flipped to Complete; orphan UAT debug artifacts cleaned (11 targets removed in commit `<TASK_1_HASH>`)
    - PERT-REL-05 -- 7-gate clinical gate green pre-bump (svelte-check 0/0; vitest 425/425; pnpm build <BUNDLE_KIB> KiB; pert-a11y 4/4; pert.spec 12/12; full Playwright 117 + 1 baseline flake on `disclaimer-banner.spec.ts:28` per established precedent; PERT-route invariants all 9 assertions PASS)

    ### Cross-reference to previously-Validated PERT-* requirement IDs

    All 48 IDs Validated post-Phase-3.1 + Phase 4 stay Validated:
    - PERT-ARCH-01..07 (Phase 1)
    - PERT-HUE-01..03 (Phase 1)
    - PERT-DATA-01..04 (Phase 1)
    - PERT-ORAL-01..08 (Phase 2)
    - PERT-TUBE-01..07 (Phase 2)
    - PERT-MODE-01..04 (Phase 2)
    - PERT-SAFE-01..04 (Phase 2; PERT-SAFE-01 referenced-not-modified at Phase 4 Wave 6)
    - PERT-TEST-01..06 (Phase 3 + Phase 3.1)
    - PERT-DESIGN-01..06 (Phase 4)

    Total v1.15 Validated: 54 / 54.

    ### Phase closures

    - Phase 1 (Architecture, Identity Hue & Clinical Data) -- Complete <PHASE_1_DATE> (commits `<PHASE_1_COMMITS>`)
    - Phase 2 (Calculator Core: Both Modes + Safety) -- Complete <PHASE_2_DATE> (commits `<PHASE_2_COMMITS>`)
    - Phase 3 (Tests) -- Complete <PHASE_3_DATE> (commits `<PHASE_3_COMMITS>`)
    - Phase 3.1 (KI-1 Resolution: SelectPicker bridge fix) -- Complete 2026-04-26 (commits `f2da16d`, `dfb6a62`, `0d9636f`)
    - Phase 4 (Design Polish: /impeccable critique sweep) -- Complete 2026-04-26 (commits `eec18c2`, `e1e60cb`, `72be821`, `29306e7`, `136b624`, `2dc7ae2`, `5cd3386`, `95e31b0`, `3001c2a`, `33695e1`, `a6d9469`, `d82aa30`, `78686f9`, `f4565b4`, `618a66f`, `b0fe1a0`, `a5ed7d7`)
    - Phase 5 (Release) -- Complete 2026-04-26 (commits `<TASK_1_HASH>`, `<TASK_3_HASH>`, `<TASK_4_HASH>`, `<TASK_5_HASH>`)

    ### Open backlog at workstream completion

    None of these block v1.15.0 release; all flagged for v1.15.x hotfix or v1.16 carrier:

    - F-01 NumericInput template en-dash (~1 LOC; v1.15.x or v1.16 cross-calculator polish phase)
    - F-02 NavShell sticky-overlay layering (~5-15 LOC investigation; v1.16 carrier)
    - `pert-config.json:137 invalidLipaseRate` en-dash (~1 LOC; same v1.16 carrier as F-01)
    - Weight-in-Oral UX evolution past Option 3 (Wave 6 D-14 helper text shipped; if future user feedback wants Option 1 -- separate section header -- or another rung, that's a v1.15.x or v1.16 cycle)

    ### Workstream-completion handoff

    After Kendamil v1.14 ships on main, run from the main repo:

    ```
    cd <MAIN_REPO_PATH>
    git fetch origin workspace/pert
    git merge workspace/pert
    git worktree remove <WORKSTREAM_WORKTREE_PATH>
    /gsd-workstreams complete pert
    ```

    The `/gsd-workstreams complete pert` handler:
    - Reads this PROJECT.md's v1.15 Closure section
    - Folds the 54 Validated PERT-* IDs into the main `.planning/PROJECT.md` Validated list
    - Updates the main `.planning/REQUIREMENTS.md` traceability
    - Archives the workstream
    ```

    Replace the angle-bracket placeholders with the actual values resolved at execute time:
    - `<TASK_1_HASH>`, `<TASK_3_HASH>`, `<TASK_4_HASH>`, `<TASK_5_HASH>`: the commit hashes for tasks 1, 3, 4, 5 (Task 4's hash is unknown at the time the file is being authored; use the placeholder `<TASK_4_HASH>` and replace it AFTER the Task 4 commit lands by amending the file's PROJECT.md content -- OR write the placeholder verbatim and document in the SUMMARY that the hash is resolvable from `git log --oneline | head -5` at workstream-completion time).
    - `<BUNDLE_KIB>`: the actual KiB value captured by Task 2 Gate 3.
    - `<PHASE_1_DATE>`, `<PHASE_2_DATE>`, `<PHASE_3_DATE>`: extract from the most recent SUMMARY in each phase directory's frontmatter `completed_date` or from the STATE.md history. If Phases 1, 2, 3 have placeholder dates (the ROADMAP.md HEAD shows them as `Not started` per the user's manual edit), the executor uses the actual completion dates from the phase SUMMARYs (which exist regardless of the ROADMAP display preference).
    - `<PHASE_1_COMMITS>`, `<PHASE_2_COMMITS>`, `<PHASE_3_COMMITS>`: extract via `git log --oneline --grep '(pert/01-' | head -10`, `git log --oneline --grep '(pert/02-'`, `git log --oneline --grep '(pert/03-'` respectively (top 5-10 hashes per phase).
    - `<MAIN_REPO_PATH>` and `<WORKSTREAM_WORKTREE_PATH>`: read `/mnt/data/src/gsd-workspaces/pert/WORKSPACE.md` (ABSOLUTE path; ONE directory above the worktree root, OUTSIDE the worktree's git tree) to extract the verified paths. FALLBACK: if that file is unreadable from the worktree for any reason, derive `<MAIN_REPO_PATH>` from `git remote get-url origin` (resolve to a local path; for this workstream it is `/mnt/data/src/nicu-assistant`) and `<WORKSTREAM_WORKTREE_PATH>` from `git rev-parse --show-toplevel` (the worktree's own root). Echo the resolved values verbatim; NEVER leave angle-bracket placeholders in the committed PROJECT.md.

    Step 2: Update `.planning/workstreams/pert/REQUIREMENTS.md`. Two surgical edits:

    Edit A (lines 89-93 -- the v1.15 Release section). Read the file first; then for each of PERT-REL-01..05, change `- [ ] **PERT-REL-NN**: <description>` to `- [x] **PERT-REL-NN**: <description> -- **FULL closure 2026-04-26 (Phase 5 plan 05-01, commit `<TASK_3_HASH>` for the bump; commit `<TASK_4_HASH>` for the closure-record)**: <closure metadata sentence>`. Mirror the closure-metadata format from line 75 (PERT-TEST-05 closure) verbatim. The 5 closure metadata sentences:
    - PERT-REL-01: `package.json bumped 1.13.0 -> 1.15.0 single-line edit on line 3; AboutSheet auto-flows via __APP_VERSION__ Vite-define; no edit to about-content.ts.`
    - PERT-REL-02: `AboutSheet reflects v1.15.0 for all 6 calculator entries; verified at execute time via Playwright-MCP screenshot OR fallback DOM probe (see 05-01-SUMMARY.md).`
    - PERT-REL-03: `Workstream PROJECT.md v1.15 Closure section appended; main .planning/PROJECT.md fold-back deferred to /gsd-workstreams complete pert per 05-CONTEXT.md D-06.`
    - PERT-REL-04: `Workstream ROADMAP.md Progress rows all flipped to Complete; orphan UAT debug artifacts cleaned in commit <TASK_1_HASH> (11 targets including .planning/workstreams/pert/phases/04-design-polish-impeccable/.continue-here.md, __capture.mjs, .playwright-mcp/, 8 UAT debug PNGs).`
    - PERT-REL-05: `7-gate clinical gate green pre-bump: svelte-check 0/0; vitest 425/425; pnpm build <BUNDLE_KIB> KiB; pert-a11y 4/4; pert.spec 12/12; full Playwright 117 + 1 baseline flake on disclaimer-banner.spec.ts:28 per established Phase 1/2/3/3.1/4 precedent; PERT-route invariants all 9 assertions PASS.`

    Edit B (lines 173-177 -- the traceability table). For each row PERT-REL-01 through PERT-REL-05, change the `Status` column from `Active` to `Validated`. Use the Edit tool with `old_string: "| PERT-REL-01 | Phase 5 | Active |"` and `new_string: "| PERT-REL-01 | Phase 5 | Validated |"` and repeat for 02, 03, 04, 05. The remainder of the table is byte-identical pre-and-post.

    Step 3: Update `.planning/workstreams/pert/ROADMAP.md`. Two surgical edits:

    Edit C (lines 113-120 -- the canonical Progress table). Flip Phase 1, Phase 2, Phase 3 rows from `Not started | -` to `Complete | <PHASE_X_DATE>`; flip the `Plans Complete` column from `0/0` to the actual count (Phase 1 plans count + dates from the SUMMARYs; Phase 2 likewise; Phase 3 likewise). Phase 3.1 + Phase 4 already Complete with 2026-04-26 -- byte-identical pre-and-post. Phase 5 row flipped: `| 5. Release | 1/1 | Complete | 2026-04-26 (plan 05-01: cleanup orphan artifacts; 7-gate clinical gate; bump v1.13.0 -> v1.15.0; workstream-local PROJECT.md / REQUIREMENTS.md / ROADMAP.md updates; PERT-REL-01..05 Validated; workstream-completion handoff documented for /gsd-workstreams complete pert) |`.

    Edit D (lines 17-23 -- the `## Phases` list). PRESERVE the user's manual edit byte-identical. The user has `[ ]` markers on Phase 1, Phase 2, Phase 3 in the Phases section header per their display preference per 05-CONTEXT.md D-07. Do NOT revert. Confirm via `git diff .planning/workstreams/pert/ROADMAP.md` after the edits land that lines 17-23 show no diff.

    Step 4: Verify all three files are coherent at HEAD. Run:
    - `grep -c '\[x\] \*\*PERT-REL-' .planning/workstreams/pert/REQUIREMENTS.md` MUST return `5`.
    - `grep -c '| PERT-REL-.* | Validated |' .planning/workstreams/pert/REQUIREMENTS.md` MUST return `5`.
    - `grep -c 'v1\.15 Closure' .planning/workstreams/pert/PROJECT.md` MUST return `1`.
    - `grep -c '/gsd-workstreams complete pert' .planning/workstreams/pert/PROJECT.md` MUST return `1`.
    - `grep -c '| 5\. Release | 1/1 | Complete | 2026-04-26' .planning/workstreams/pert/ROADMAP.md` MUST return `1`.
    - `git diff .planning/workstreams/pert/ROADMAP.md | grep -E '^\-.*\[ \] \*\*Phase [123]:'` MUST return empty (the user's `[ ]` markers in the Phases section header are NOT removed by this task).

    Step 5: Stage all three files atomically and commit with EXACT subject line `docs(pert/05): close workstream — v1.15.0 release readiness`. Body should briefly enumerate (a) what was added to PROJECT.md (v1.15 Closure section + Validated list flips + phase closures + open backlog + handoff), (b) what was flipped in REQUIREMENTS.md (5 IDs Active -> Validated + 5 traceability rows), (c) what was flipped in ROADMAP.md (Progress table all rows Complete; user's manual `## Phases` edit preserved), (d) reference to 05-CONTEXT.md D-05 + D-06 + D-07. Co-Authored-By trailer.

    Acceptable em-dash usage: the commit subject line above contains an em-dash (`—`) per the v1.13 main-repo Phase 43 release commit precedent (commit `9a86738` body uses em-dashes). The workstream Q1 broad ban applies to PROSE inside SOURCE FILES (svelte / json / ts), not to commit messages or planning artifacts -- see 04-03-SUMMARY.md `em-dash + en-dash 0 across PERT-route source files` (the gate is scoped to source files). Verify newly-authored prose (i.e., the lines this task ADDS, not the legacy lines already at HEAD in PROJECT.md / REQUIREMENTS.md / ROADMAP.md) contains 0 em-dashes via `git diff HEAD~1 -- <file> | grep '^+' | grep -vP '^\+\+\+' | grep -cP '\x{2014}'` returning `0` for each of the three files. The pre-existing em-dashes already at HEAD (PROJECT.md = 21, REQUIREMENTS.md = 35, ROADMAP.md = 22 at the time of planning) are LEGACY prose -- the workstream Q1 broad ban applies to NEWLY-AUTHORED prose in this milestone, not to historical content (per 04-03-SUMMARY.md sweep precedent + Wave 6 sweep precedent). Rewriting legacy em-dashes is out of scope for Phase 5 and would breach the D-04a / D-05 / D-07 minimal-edit posture. The em-dash gate on the SUMMARY (Task 5) stays unscoped: the SUMMARY is fully newly-authored, so the entire file must be 0 em-dashes.
  </action>
  <verify>
    <automated>[ "$(pwd -P)" = "$(git rev-parse --show-toplevel)" ] && [ "$(grep -c '\[x\] \*\*PERT-REL-' .planning/workstreams/pert/REQUIREMENTS.md)" -eq 5 ] && [ "$(grep -cE '\| PERT-REL-.* \| Validated \|' .planning/workstreams/pert/REQUIREMENTS.md)" -eq 5 ] && grep -q 'v1.15 Closure' .planning/workstreams/pert/PROJECT.md && grep -q '/gsd-workstreams complete pert' .planning/workstreams/pert/PROJECT.md && grep -qE '\| 5\. Release \| 1/1 \| Complete \| 2026-04-26' .planning/workstreams/pert/ROADMAP.md && [ "$(git diff HEAD~1 -- .planning/workstreams/pert/PROJECT.md | grep '^+' | grep -vP '^\+\+\+' | grep -cP '\x{2014}')" -eq 0 ] && [ "$(git diff HEAD~1 -- .planning/workstreams/pert/REQUIREMENTS.md | grep '^+' | grep -vP '^\+\+\+' | grep -cP '\x{2014}')" -eq 0 ] && [ "$(git diff HEAD~1 -- .planning/workstreams/pert/ROADMAP.md | grep '^+' | grep -vP '^\+\+\+' | grep -cP '\x{2014}')" -eq 0 ] && git log -1 --pretty=%s | grep -qE '^docs\(pert/05\): close workstream' && echo OK</automated>
  </verify>
  <done>
    PROJECT.md has the v1.15 Closure section appended (mirroring main-repo Phase 43 release pattern). REQUIREMENTS.md has 5 PERT-REL-* IDs flipped `[x]` + 5 traceability rows flipped `Validated`. ROADMAP.md Progress table all rows Complete. User's manual `## Phases` edit preserved byte-identical. Commit landed with exact subject `docs(pert/05): close workstream — v1.15.0 release readiness`. Em-dash count 0 across all three workstream-local files.
  </done>
</task>

<task type="auto">
  <name>Task 5: Compose 05-01-SUMMARY.md (Phase 5 closure record + workstream-completion handoff)</name>
  <files>.planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md</files>
  <action>
    Per 05-CONTEXT.md D-09 Task 5 + the must_haves bullet on `05-01-SUMMARY.md`: compose the Phase 5 closure record. Min 50 lines; em-dash + en-dash 0/0 across the file (workstream Q1 broad ban applied to all newly-authored prose in this milestone -- see 04-03-SUMMARY.md sweep + Wave 6 sweep precedent).

    Step 1: Author the file at `.planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md`. Required sections:

    **Frontmatter** (YAML at the top, between `---` fences):
    ```
    ---
    phase: 5
    plan: 1
    subsystem: pert/release
    workstream: pert
    milestone: v1.15
    wave: 1
    gap_closure: false
    tags: [pert, release, v1-15-0, version-bump, clinical-gate, workstream-closure, project-md-foldback, orphan-cleanup]
    dependency_graph:
      requires:
        - .planning/workstreams/pert/phases/05-release/05-CONTEXT.md
        - .planning/workstreams/pert/phases/05-release/05-DISCUSSION-LOG.md
        - .planning/workstreams/pert/phases/05-release/05-01-PLAN.md
        - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-03-SUMMARY.md
        - .planning/workstreams/pert/phases/04-design-polish-impeccable/04-06-SUMMARY.md
      provides:
        - package.json (post-bump v1.15.0)
        - .planning/workstreams/pert/PROJECT.md (v1.15 Closure section)
        - .planning/workstreams/pert/REQUIREMENTS.md (PERT-REL-01..05 Validated)
        - .planning/workstreams/pert/ROADMAP.md (all Progress rows Complete)
      affects:
        - .planning/workstreams/pert/STATE.md (orchestrator updates separately if needed)
    tech_stack:
      added: []
      patterns:
        - "Single-source-of-truth version flow: package.json line 3 -> vite.config.ts __APP_VERSION__ Vite-define -> about-content.ts appVersion -> all 6 AboutSheet entries. The bump propagates to the rendered UI without any source/component edit; this is the v1.13 main-repo Phase 43 release pattern, verified for the v1.15.0 bump in this plan."
        - "7-gate clinical gate (NOT 18-gate): Phase 5 inherits the 7 functional gates from the v1.13 Phase 43 release-readiness pattern + the post-Phase-4 PERT-route byte-identity invariants gate. The 18-gate Phase 4 verification was the score-acceptance gate for the design-polish phase; Phase 5 release-readiness reuses only the 7 functional gates because the visual surface was already certified at Phase 4 closure (aggregate 36.25/40 with all 6 PERT-DESIGN requirements Validated)."
    key_files:
      created:
        - .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md
      modified:
        - package.json
        - .planning/workstreams/pert/PROJECT.md
        - .planning/workstreams/pert/REQUIREMENTS.md
        - .planning/workstreams/pert/ROADMAP.md
      deleted:
        - .planning/workstreams/pert/phases/04-design-polish-impeccable/.continue-here.md
        - __capture.mjs
        - .playwright-mcp/ (recursive)
        - pert-after-f03-verify.png
        - 04-uat-tube-feed-f03.png
        - 04-uat-oral-unchanged.png
        - 04-04-uat-light-desktop-tube-feed.png
        - 04-05-uat-oral-d12.png
        - 04-05-uat-tube-feed-d13.png
        - 04-06-uat-oral-helper.png
        - 04-06-uat-tube-feed-no-helper.png
    metrics:
      duration_minutes: <FILL>
      completed_date: 2026-04-26
      tasks_completed: 5
      files_created: 1
      files_modified: 4
      files_deleted: 11
      commits: 4
      loc_changed_source: 1 (package.json line 3 only)
      bundle_kib_phase_5: <FILL_FROM_TASK_2>
      bundle_kib_phase_4: 575.99
      bundle_kib_delta: <FILL_COMPUTED>
      vitest_count_phase_5: 425
      vitest_count_phase_4: 425
      pert_a11y_phase_5: 4
      pert_a11y_phase_4: 4
      pert_spec_phase_5: 12
      pert_spec_phase_4: 12
      full_playwright_phase_5: 117
      full_playwright_phase_4: 117
      em_dash_count_summary: 0
      en_dash_count_summary: 0
    ---
    ```

    **Heading**: `# Phase 5 Plan 01 Summary: Workstream pert v1.15.0 Release`

    **Section 1 -- What this plan shipped**: 4-bullet list mirroring the 4 Phase 5 commits (Task 1 cleanup; Task 2 verification-only no-commit; Task 3 bump; Task 4 closure docs; Task 5 SUMMARY -- 4 commits since Task 2 has no commit).

    **Section 2 -- PERT-REL-01..05 satisfaction mapping**: 5-bullet table, each REQ-ID mapping to the verifying task + verifying commit hash.

    **Section 3 -- Clinical-gate evidence (verbatim paste)**: copy `/tmp/05-release-gates/SUMMARY.txt` verbatim + paste each `gate-NN-*.log` final summary line for traceability. Format as a fenced code block per gate.

    **Section 4 -- Version-bump evidence**: package.json before/after diff (the single line), AboutSheet visual confirmation evidence path (Playwright-MCP screenshot OR fallback DOM probe; record which path was taken in Task 3), Vite-define wiring confirmation (vite.config.ts line 11 + about-content.ts line 13 byte-identical pre-and-post per D-04).

    **Section 5 -- Phase 5 vs Phase 4 deltas**: table comparing the 7 baseline metrics (svelte-check, vitest count, build bundle KiB, pert-a11y, pert.spec, full Playwright, source LOC delta in src/). Source LOC delta in src/ MUST equal `0` (only `package.json` was edited; `package.json` is at the repo root, not under `src/`).

    **Section 6 -- Out-of-scope items per 05-CONTEXT.md D-10**: bullet list naming (a) main repo merge, (b) main `.planning/PROJECT.md` fold-back, (c) GitHub release publication, (d) branch deletion / worktree removal, (e) cross-calculator backlog (F-01, F-02, pert-config.json:137 en-dash), (f) weight-in-Oral UX past Option 3.

    **Section 7 -- Workstream-completion handoff**: VERBATIM command sequence the user runs from the main repo after Kendamil v1.14 ships. Read `/mnt/data/src/gsd-workspaces/pert/WORKSPACE.md` (ABSOLUTE PATH; ONE directory above the worktree root, OUTSIDE the worktree's git tree -- NOT `.planning/workstreams/WORKSPACE.md` which does not exist) at execute time to extract the resolved paths. FALLBACK: if that file is unreadable from the worktree, derive the main-repo path from `git remote get-url origin` (this workstream resolves to `/mnt/data/src/nicu-assistant`) and the worktree path from `git rev-parse --show-toplevel`; the branch is `workspace/pert` (from `git rev-parse --abbrev-ref HEAD`). Embed the actual resolved paths in the SUMMARY (no placeholders, no angle-brackets, no `<RESOLVED_*>` text in the committed file).

    Sample:
    ```
    cd <RESOLVED_MAIN_REPO_PATH>
    git fetch origin workspace/pert
    git merge workspace/pert
    git worktree remove <RESOLVED_WORKTREE_PATH>
    /gsd-workstreams complete pert
    ```

    The handler then folds the 54 Validated PERT-* IDs into the main `.planning/PROJECT.md`, updates main `.planning/REQUIREMENTS.md` traceability, and archives the workstream.

    **Section 8 -- Lessons retained**: brief 1-2 sentence note that Phase 5 generated NO new process insights (per 05-DISCUSSION-LOG.md "Lesson retained (none new -- release phases don't generate process insights of the kind Phase 4 did)"). The process insights worth retaining were all generated by Phase 4 (LLM-Design-Review fallback miss; mode-asymmetric audit miss; safety-only-input UX clarification) and are documented in 04-CONTEXT.md D-13 + D-14.

    Step 2: Verify the file is coherent. Run:
    - `wc -l .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md` MUST return >= 50 lines.
    - `grep -cP '\x{2014}' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md` MUST return `0`.
    - `grep -cP '\x{2013}' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md` MUST return `0`.
    - `grep -c 'PERT-REL-01' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md` MUST return >= 1.
    - `grep -c 'PERT-REL-05' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md` MUST return >= 1.
    - `grep -c '/gsd-workstreams complete pert' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md` MUST return >= 1.
    - `grep -c 'v1.15.0' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md` MUST return >= 1.
    - `grep -c 'clinical gate' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md` MUST return >= 1.

    Step 3: Stage and commit with EXACT subject line `docs(pert/05-01): summary — workstream pert v1.15.0 release-ready`. Body briefly notes the SUMMARY length, sections covered, and references to 05-CONTEXT.md D-09 Task 5. Co-Authored-By trailer.

    Step 4: Final negative-space audit. Run `git diff --name-only HEAD~4..HEAD` and confirm the union of all 4 phase-5 commits' file changes equals exactly:
    - DELETED: 11 files (10 named files + 1 directory recursive)
    - MODIFIED: `package.json` + possibly `pnpm-lock.yaml` + 3 workstream-local planning artifacts (PROJECT.md, REQUIREMENTS.md, ROADMAP.md)
    - CREATED: 1 file (`.planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md`)
    - ZERO source files in `src/lib/` or `src/routes/` modified.
    - ZERO test files in `e2e/` or `*.test.ts` modified.

    If any file outside this allowlist appears in the diff, document the exception in the SUMMARY (mirroring the Phase 4 D-08a out-of-band acknowledgment pattern from 04-03-SUMMARY.md `5cd3386` precedent).
  </action>
  <verify>
    <automated>[ "$(pwd -P)" = "$(git rev-parse --show-toplevel)" ] && test -f .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md && [ "$(wc -l < .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md)" -ge 50 ] && [ "$(grep -cP '\x{2014}' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md)" -eq 0 ] && [ "$(grep -cP '\x{2013}' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md)" -eq 0 ] && grep -q 'PERT-REL-01' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md && grep -q 'PERT-REL-05' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md && grep -q '/gsd-workstreams complete pert' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md && grep -q 'v1.15.0' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md && grep -q 'clinical gate' .planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md && git log -1 --pretty=%s | grep -qE '^docs\(pert/05-01\): summary' && echo OK</automated>
  </verify>
  <done>
    `05-01-SUMMARY.md` authored at >= 50 lines with em-dash + en-dash 0/0; all 8 sections populated (frontmatter, what shipped, REL-ID mapping, clinical-gate evidence verbatim, version-bump evidence, Phase 5 vs Phase 4 deltas, out-of-scope, workstream-completion handoff, lessons retained). Commit landed with exact subject `docs(pert/05-01): summary — workstream pert v1.15.0 release-ready`. Negative-space audit clean: only the documented files in the cumulative phase diff.
  </done>
</task>

</tasks>

<verification>
After all 5 tasks land, verify Phase 5 closure end-to-end:

1. **Version-flow integration test**: `grep '"version"' package.json` returns `  "version": "1.15.0",`. The AboutSheet visual confirmation evidence is captured at the path documented in 05-01-SUMMARY.md Section 4.

2. **Workstream-local artifact coherence**: PROJECT.md, REQUIREMENTS.md, ROADMAP.md all reference v1.15.0 and PERT-REL-01..05 Validated; the cross-references resolve (PROJECT.md `/gsd-workstreams complete pert` mention; REQUIREMENTS.md 5 `[x]` flips + 5 traceability rows; ROADMAP.md Progress all Complete).

3. **Cleanup invariant**: `ls __capture.mjs .playwright-mcp/ *.png 2>&1` at the workstream root returns "No such file or directory" (or empty). All 11 D-08 cleanup targets removed. KEEP list intact (16 critique transcripts; all PLAN/SUMMARY files; canonical phase artifacts).

4. **PERT-route byte-identity** (Phase 4 inheritance): `grep -c 'tracking-wider' src/lib/pert/PertCalculator.svelte` = 2; `grep -c 'font-extrabold' src/lib/pert/PertCalculator.svelte` = 2; helper text in PertInputs.svelte = 1; function-binding bridges = 3; string-bridge proxies = 0; em-dash = 0 across the 4 PERT-route files; pert-config.json byte-identical with `2dc7ae2`.

5. **Negative-space audit at phase close**: `git diff --name-only HEAD~4..HEAD` returns ONLY (a) `package.json` (and possibly `pnpm-lock.yaml`); (b) 3 workstream-local planning artifacts (PROJECT.md, REQUIREMENTS.md, ROADMAP.md); (c) 1 new file (`05-01-SUMMARY.md`); (d) 11 deletions (cleanup targets). ZERO `src/`, ZERO `e2e/`, ZERO test file changes.

6. **Phase 4 byte-identity preserved**: `git diff HEAD~4..HEAD -- src/lib/pert/ src/routes/pert/ src/lib/shared/` returns empty (no PERT-route or shared-component edits in Phase 5).

7. **Commit chain coherence**: 4 commits in order with EXACT subjects:
   - `chore(pert): clean orphan UAT debug artifacts before v1.15.0 release` (Task 1)
   - `chore: bump version to v1.15.0` (Task 3)
   - `docs(pert/05): close workstream — v1.15.0 release readiness` (Task 4)
   - `docs(pert/05-01): summary — workstream pert v1.15.0 release-ready` (Task 5)

8. **Boundary gates**: NO edit to `.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `CLAUDE.md`, `DESIGN.md`, `DESIGN.json`, `vite.config.ts`, `src/lib/shared/about-content.ts`, OR any other root-level / shared file outside the documented allowlist.
</verification>

<success_criteria>
Phase 5 ships with all 5 tasks complete and 4 commits landed:

- [ ] **Task 1**: 11 orphan UAT debug artifacts removed; KEEP list intact; commit `chore(pert): clean orphan UAT debug artifacts before v1.15.0 release` (D-08).
- [ ] **Task 2**: 7-gate clinical gate green; evidence at `/tmp/05-release-gates/`; verification-only with zero source diff (D-03).
- [ ] **Task 3**: `package.json` bumped 1.13.0 -> 1.15.0; AboutSheet auto-flow verified; commit `chore: bump version to v1.15.0` (D-01 + D-04 + D-04a).
- [ ] **Task 4**: Workstream-local PROJECT.md (v1.15 Closure section) + REQUIREMENTS.md (5 PERT-REL-* IDs Validated + 5 traceability rows) + ROADMAP.md (Progress all Complete; user's `## Phases` edit preserved) updated; commit `docs(pert/05): close workstream — v1.15.0 release readiness` (D-05 + D-06 + D-07).
- [ ] **Task 5**: `05-01-SUMMARY.md` authored at >= 50 lines with em-dash + en-dash 0/0; 8 sections covering closure record + workstream-completion handoff; commit `docs(pert/05-01): summary — workstream pert v1.15.0 release-ready` (D-09 Task 5).

Plan-level outcome:

- [ ] All 5 PERT-REL-01..05 requirements Validated in REQUIREMENTS.md
- [ ] PROJECT.md v1.15 Closure section captures the 54 v1.15 Validated IDs (5 newly flipped + 49 cross-referenced)
- [ ] ROADMAP.md Progress table all 6 rows Complete (Phase 1, 2, 3, 3.1, 4, 5)
- [ ] AboutSheet auto-flows v1.15.0 across all 6 calculator entries (no edit to about-content.ts)
- [ ] Phase 4 byte-identity preserved on all PERT-route source files (no UI regression)
- [ ] Workstream-completion handoff command sequence documented verbatim in 05-01-SUMMARY.md
- [ ] Workstream `pert` ready for `/gsd-workstreams complete pert` after Kendamil v1.14 ships on main (D-02 ordering)
</success_criteria>

<self_check>

Mirroring the 10-dimension structure from 04-06-PLAN.md self-check:

**1. Source-decision fidelity (CONTEXT D-01..D-10 honored verbatim):**
- D-01 v1.15.0 target -> Task 3 single-line edit; D-02 merge-after-Kendamil -> handoff documented in Task 5 SUMMARY; D-03 7-gate (not 18-gate) -> Task 2 exact 7 gates; D-04 single-file source edit -> Task 3 only `package.json`; D-04a strict allowlist -> Task 3 verify gate excludes other files; D-05 workstream-local PROJECT.md only -> Task 4; D-06 main fold-back deferred -> Task 5 handoff; D-07 ROADMAP Progress flips -> Task 4 Edit C; D-08 cleanup list -> Task 1 exact 11 targets; D-09 5 tasks autonomous -> mirrored; D-10 out-of-scope -> SUMMARY Section 6.

**2. Boundary protections honored:**
- Single-file source edit (`package.json` only) per D-04a; main project files (`/.planning/PROJECT.md`, `/.planning/STATE.md`, `/.planning/ROADMAP.md`, `CLAUDE.md`, `DESIGN.md`) NOT touched per D-06; PERT-route source files byte-identical (verified by Gate 7 in Task 2 + Verification step 6); shared components untouched; e2e + test files untouched.

**3. Phase 4 invariants preserved (byte-identity):**
- PertCalculator.svelte (Wave 5 D-12 + Wave 4 + Wave 2 byte-identical); PertInputs.svelte (Wave 6 D-14 + Phase 3.1 D-01 byte-identical); pert-config.json (Wave 1 commit `2dc7ae2` byte-identical); calculations.ts (Phase 2 frozen); state.svelte.ts (Phase 2 frozen); types.ts (Phase 2 frozen); config.ts (Phase 2 frozen); +page.svelte (Phase 1 frozen).

**4. PERT-REL-01..05 closure mapping:**
- PERT-REL-01 -> Task 3 (version bump). PERT-REL-02 -> Task 3 (AboutSheet auto-flow verification). PERT-REL-03 -> Task 4 (PROJECT.md v1.15 Closure section). PERT-REL-04 -> Task 4 (ROADMAP Progress flips) + Task 1 (orphan cleanup). PERT-REL-05 -> Task 2 (7-gate clinical gate green).

**5. Quality gates (must_haves -> tasks):**
- Each must_havs truth maps to a verify gate in at least one task. Gate 7 invariants (must_haves bullet 4 sub-bullets) -> Task 2 Gate 7 explicit assertions. Cleanup invariant (must_haves bullet 3) -> Task 1 verify gate. Version-bump verification (must_haves bullets 1+2) -> Task 3 verify gate. Workstream-local artifact updates (must_haves bullet 6) -> Task 4 verify gate. SUMMARY composition (must_haves bullet 9) -> Task 5 verify gate.

**6. File ownership for parallel execution:**
- N/A. Single plan, single wave, sequential 5 tasks. No parallelism required (the workflow has natural sequence: cleanup -> verify -> bump -> close docs -> summarize).

**7. Em-dash + en-dash sweep across newly-authored prose:**
- Task 4 verify gate confirms `grep -cP '\x{2014}' PROJECT.md` returns 0; same for REQUIREMENTS.md and ROADMAP.md additions. Task 5 verify gate confirms 0/0 on the SUMMARY. Workstream Q1 broad ban honored on all newly-authored prose. Commit messages MAY contain em-dashes per Phase 4 + main-repo Phase 43 commit precedent.

**8. Verification command Nyquist-rule compliance:**
- Each `<verify>` includes an `<automated>` command. All 5 task verify gates use `[ ]`-style POSIX tests + grep counts (no manual inspection required). Tasks 1, 3, 4, 5 are autonomous-verifiable. Task 2 (clinical gate) is verification-only with no source diff; the verify gate confirms each evidence file exists and contains the expected pass-line pattern.

**9. Scope-reduction prohibition adherence:**
- NO "v1", "v2", "static for now", "future enhancement" language. Each PERT-REL-XX requirement gets full closure metadata in REQUIREMENTS.md mirroring the PERT-TEST-05 closure format from line 75. The 7-gate clinical gate is the FULL functional clinical gate (the 18-gate Phase 4 surface was a one-time Phase 4 score-acceptance gate; Phase 5's 7-gate scope is per the v1.13 Phase 43 release-readiness pattern, not a reduction). The handoff to `/gsd-workstreams complete pert` is the documented mechanism for main-repo fold-back per D-06; this is NOT scope reduction (the fold-back happens at workstream-completion time, not Phase 5).

**10. Multi-source coverage audit:**
- GOAL (workstream goal): "Workstream pert v1.15 ships with a clean clinical gate, the AboutSheet reflects the new version automatically, and validated requirements + decisions fold back into the workstream-local PROJECT.md so pert can be archived." -> COVERED by Tasks 2 + 3 + 4 + 5.
- REQ (PERT-REL-01..05 from REQUIREMENTS.md): all 5 IDs in this plan's `requirements` field; each maps to >= 1 task per Section 4 above. COVERED.
- RESEARCH: --skip-research per planning_context (CONTEXT.md D-01..D-10 lock all decisions; no RESEARCH.md needed). COVERED via CONTEXT.md.
- CONTEXT (D-01..D-10): all 10 decisions referenced in must_haves + task actions per Section 1 above. COVERED.

No source items missing. No PHASE SPLIT recommendation. Plan ships in single wave; 4 commits.

</self_check>

<output>
After completion, create `.planning/workstreams/pert/phases/05-release/05-01-SUMMARY.md` per Task 5.
</output>
